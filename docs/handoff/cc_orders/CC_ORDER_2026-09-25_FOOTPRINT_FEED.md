# הזמנה ל-cc-macbook — 25.09.2026 (cowork-dev, 11:45 IL) — T-478: להחזיר את פיד-הפוטפרינט (מערכת 3 — נתונים קודם)

> להדביק בסשן Claude Code על ה-MacBook. עבודה בלי שאלות (14.09). מחוץ לשעות-המסחר בלבד לכל דיפלוי-DLL/ריסטארט (לפני 16:10 או אחרי 23:00, פוזיציה 0).

**הרקע (מייקל 25.09 11:20: "יישום לייב מלא היום כולל מערכת 3"; פסיקה: נתונים קודם):** `v9_bars_footprint` — יוני 2,709,972 שורות ב-13 ימים, יולי 454/8, אוגוסט 181/5, ספטמבר 210/6. הפיד נעצר באמצע יוני (`FOOTPRINT_DISABLED=1`, `S3_MUTE=1`). בלי 15–20 סשנים של פוטפרינט אין ריפליי ל-S3 ואין מדידה — לכן S3 לא נכנס ללייב עד שיש נתונים ומספר.

**הפריט (מספר אחד, קומיט אחד, Rule 5):** (1) לאבחן למה הפיד נעצר — `~/SierraChart_Data/v9_export/footprint.json` (מתי עודכן לאחרונה? מה גודלו?), הסטרים ב-`bridge/v9_streams/` שמושך אותו, ו-`FOOTPRINT_DISABLED=1` (מי כיבה, מתי, למה — git log על `.env`/דוקים; **לא לשנות `.env` בלי פסיקה** — ההזמנה הזאת היא פסיקת-מייקל להחזיר את הפיד, לא להדליק את S3). (2) להחזיר את יצוא-הפוטפרינט מסיירה (ה-DLL המונוליטי אם נדרש — `scripts/build_monolithic_cpp.sh --deploy` עם snapshot, Remote Build, טעינת-study מחדש) ואת האינג׳סט ל-`v9_bars_footprint`, כך ש**כל בר-RTH** מקבל שורת-פוטפרינט (levels, delta, poc_price, stacked_buy/sell). (3) אימות אחרי סשן אחד: `select count(*), count(distinct ts) from v9_bars_footprint where ts::date = current_date` ⇒ ≥78 ברים. (4) לא לגעת במפיקי-S3 ולא ב-`S3_MUTE` — רק הנתונים. LIVE_CHANNEL עם הפלט הגולמי, TASK_LOG T-478, STATUS_BOARD.

**אחרי 15–20 סשנים (cowork-dev):** מדידת הפוטפרינט כמסנן על הכניסות הקיימות (ספיגה בקיצון, חוסר-איזון bid/ask בבר-הטריגר, stacked imbalances) — ריפליי יום-כולל, ואז לפי הכלל: חיובי ⇒ לייב.
