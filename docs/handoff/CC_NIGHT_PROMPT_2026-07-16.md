# פרומפט-לילה ל-Claude Code — תיקוני-שורש לקראת מסחר-נקי מחר · 15→16/07

**מבצע: Claude Code (cc-imac על ה-iMac; סעיפי-DEV גם על מק-הפיתוח). פסיקת-מייקל 21:58: "תיקונים שקלוד יבצע" — ‏cowork-dev מאמת בלבד, לא מתקן.**
מודל: ‏Sonnet. חוזה: ראיות-Rule-5 (פקודה+פלט ≤5 שורות) · ‏NOT-DONE מפורש · דיווח ב-EXECUTION_REPORT+SYNC · ‏commit+push לכל תיקון בנפרד.
**דדליין: 16:00 IL מחר. סדר לפי עדיפות — אל תתקדם לפריט הבא לפני שהקודם ירוק או מתועד-NOT-DONE.**

## P1 🔴🔴 — קפיאת-ייצוא-הווּדיס (הרוצח של הערב, שתי המכונות)
**הראיה המעודכנת (cowork, 22:15, מק-פיתוח):** **כל שלושת קבצי-ה-5-דקות** (‏5min / ‏5min_continuous / ‏woodies_5min) — ‏history קפוא ‏338 דק' (≈16:20 IL, כמעט-מהפתיחה!) בעוד הקבצים נכתבים-מחדש כל שנייה ו-live_price זורם. הברידג' דוחף את הברים הקפואים → שומר-הטריות בבקאנד דוחה אותם בצדק (‏"[bars/5min] _route_bar BLOCKED stale" — 5262 מופעים אצלי; ‏grep את זה אצלך!) → S2 (מנוי על '5min') מורעב. **לכן 3 ריסטארטים לא עזרו — אין "live-loop לתקן"; הקלט עצמו קפוא מצד-סיירה.** ‏DB התקדם חלקית דרך current_bar/נתיבים-קלים — זה מה שהטעה. **חשוד-ראשי: בילד-ה-DLL של היום** (שינויי-4-הזוגות) — ההיסטוריה מפסיקה להתגלגל אחרי ה-reload; ה-iMac קפא אחרי ה-Remote-Build שלו (15:18→~20:45?), שלי אחרי ה-reload שלו.
**תרופה-מיידית לבדיקה לפני הכל:** ‏reload לסטאדי/צ'ארטבוק → צפה: האם ‏history חוזר להתגלגל ואז נתקע שוב אחרי N ברים? (מלמד אם זה crash-פר-אירוע או תקיעה-חד-פעמית.)
**המשימה:**
1. שחזר: מתי בדיוק קפא ה-history בכל מכונה (epoch→IL), ומה קרה אז בסיירה (Message Log!) — חשד-1: ‏DLL-4-החוזים של היום (סלוטים 8/9, לולאות idx<4, buffer 1024) זורק exception בנתיב-אירוע מסוים והסטאדי מת חלקית; חשד-2: ‏chart data feed של צ'ארטבוק-הווּדיס בלבד.
2. קרא את בלוק-הייצוא ב-`sc_study/MES_AI_DataExport.cpp` (ראה גם השינויים של היום) — האם חריגה באחד הענפים עוצרת את עדכון-ה-history אבל משאירה את כתיבת-הקובץ?
3. תקן-שורש + הוסף ל-DLL שורת-בריאות בייצוא (`history_stale_s`) שה-backend יתריע עליה (feed_watchdog קורא אותה).
4. בילד+פריסה (snapshot!) + ‏reload ובדיקת-חיים: ‏history מתגלגל ≥3 ברים רצופים.

## P2 🔴 — Reclass-אחרי-נעילה (ה"נייטרל" השגוי של היום; מייקל: לבדוק שזה לא באג)
**הראיה (cowork):** ‏IB ‏7591.75–7619 (27.25); התרחבות-מטה 20 נק' (0.73×IB) מול 0.75 למעלה ⇒ ‏Normal_Variation-DOWN לפי-הספר, אבל החי נשאר ‏Neutral_Center ‏conf=0.67 ‏LOCKED_LOW_CONF כל-היום ⇒ ‏playbook חסם ‏ZLR/REV כל אחה"צ.
**פסיקת-מייקל (22:20): "זה יום נורמל-וריאציה, והמסחר היה יכול להיות מצוין מכיוון-לכיוון ואז POC"** — כלומר: התיקון חייב גם לוודא שה-playbook ביום ‏Normal_Variation מתיר fades-בקצוות עם יעד-POC (מסחר קצה-אל-קצה) ולא SKIP.
**המשימה:** ב-classifier/machine — האם ‏P0-1-v2 acceptance-reclass מנוטרל אחרי ‏LOCK? אם כן: נעילת-conf-נמוך (‎<0.7) חייבת להישאר פתוחה-לרוויזיה על קבלה-מאומתת (≥2 סגירות + ‏≥8% נפח מעבר לרפרנס). תקן + **טסט-רגרסיה שמריץ את ברי-15/07 ומאשר flip ל-Variation-DOWN עד 18:30**. בדוק גם למה ‏antiflap החזיק תווית-מוחזקת שעות.

## P3 🔴 — אימוץ-יתום + לכידת-fills (הפוזיציות העירומות של היום)
‏reconciler: ‏Sierra≠0/TM=0 ⇒ ‏adopt-or-alert (אימוץ עם stop-סינתטי-מיידי לפי כללי-הניהול, או ‏CRITICAL אם אי-אפשר). ‏fill_poller: למה fills הפסיקו להיכתב ~17:20 ב-iMac (חלון-הלוג סביב) — תקן. טסטים לשניהם.

## P4 — אימות-חיים אוטומטי אחרי ריסטארט (הלקח המרכזי)
סקריפט ‏`scripts/post_restart_verify.sh`: ‏(א) שורות-subscribe בלוג ‏(ב) בר-חדש ב-DB תוך 7 דק' ‏(ג) ‏v9_day_type_state מתקדם ‏(ד) ‏decisions-feed עונה ‏(ה) ‏woodies history_ts טרי. כשל ⇒ צבע-אדום + פוש-התראה. חבר ל-morning-protocol + אחרי כל kickstart. **בלי ירוק — אין מסחר.**

## P5 — כיול (עם ה-harness של cowork, יגיע עד-הבוקר)
‏s4_risk_cap חסם ×3 בערב על מק-הפיתוח (‏GHOST/cap ‏18pt?) — לוודא שהתקרה תואמת-4-חוזים ולא עוד cap=3-era. ‏RR-0.65 / ‏LSMA-2 ולידציה על המספרים.


## P6 — ביקורת-אפיון מלאה, קריאה-בלבד (פסיקת-מייקל 22:40: "בחינה שהכל תקין לפי האפיון בלי שיהרוס שם שום דבר")
**אחרי P1-P5, על ה-iMac. אסור-בהחלט: שינוי-קוד, פקודת-מסחר, ריסטארט, מחיקה — קריאה-בלבד.**
עבור על האפיון מול המציאות ותייג כל שורה ✅/❌/⚠️ עם ראיה:
1. **דגלים:** ‏flag_guard PASS מלא + ‏sync_env dry-run="already matches" + ‏FLAG_INDEX --check בלי drift.
2. **שרשרת-4-חוזים:** ‏effective_contracts=4 (דריל-קריאה, בלי ירי) · סכמה le=4 · ‏DLL 4-זוגות פרוס==ריפו (checksum).
3. **שערים לפי-דוקטרינה:** ‏playbook ‏Normal_Variation מתיר קצה-אל-קצה+POC (אחרי P2) · ‏location_gate · ‏RR_MIN_ROTATION · ‏LSMA=2 · ‏PATTERN_LOSS סופר-אמת-בלבד.
4. **יציאות:** ‏T0-T3 סולם מונוטוני · ‏MODIFY_STOP · ‏FLATTEN_ACCOUNT · אין caller ל-op=EXIT (עדיין אסור).
5. **ניהול-סיכון:** ‏RISK_HALT −$400 · ‏S6 protective · רקונסיילר-אימוץ (אחרי P3) · אין פוזיציה-עירומה ב-state.
6. **פיד+חיים:** ‏post_restart_verify ירוק · שלושת קבצי-ה-5min מתגלגלים · ‏decisions-feed עונה · אפליקציית-הכיס עונה מה-LAN.
7. **רישום:** עסקת-אתמול האחרונה מופיעה ב-v9_trades+ledger עם P&L נכון מול Sierra.
**דיווח:** טבלת-ביקורת מלאה ב-EXECUTION_REPORT + שורת-SYNC אל cowork-dev ("ביקורת-אפיון: X✅ Y⚠️ Z❌"). ‏cowork-dev מבקר את הדוח ומאשר למייקל לפני 16:15.

## P7 — גישת-כיס מכל-מקום (מייקל: "הלינק לא עובד... גם כשאני לא באותה רשת")
פתרון: **Tailscale** (רשת-VPN פרטית, מוצפנת, בלי חשיפה-פומבית ובלי port-forwarding).
1. על ה-iMac: ‏`brew install tailscale && brew services start tailscale` (או האפליקציה) → ‏`tailscale up` → יודפס קישור-אימות — **תן אותו למייקל** (ההתחברות שלו, לא שלך).
2. אחרי שמייקל מתחבר: ‏`tailscale ip -4` → דווח את ה-IP (‏100.x.y.z) ב-SYNC. הלינק הקבוע של מייקל מכל-מקום: ‏`http://<tailscale-ip>:8000/api/v9/mobile`.
3. ודא שה-bind ‏0.0.0.0 (כבר בוצע) מכסה גם את ממשק-tailscale, ‏curl מקומי על ה-100.x עונה.
(מייקל בטלפון: להתקין את אפליקציית Tailscale מה-App Store ולהתחבר לאותו חשבון — פעם אחת, ואז הלינק עובד מכל רשת כולל סלולר.)

## חלוקת-מכונות
‏P1: שתי המכונות (הבאג חי בשתיהן). ‏P2-P5: קוד משותף — תקן פעם-אחת, פרוס בשתיהן. בסוף: ‏flag_guard + ‏post_restart_verify ירוקים **בשתי המכונות** לפני 16:15 IL, וביקורת-P6 מדווחת ל-cowork-dev.
‏cowork-dev יבצע מחר 16:00-16:25 אימות-בוקר עצמאי על מק-הפיתוח ויאשרר ב-SYNC — לפי פסיקת-מייקל: קלוד-קוד מתקן, ‏cowork מאמת.
