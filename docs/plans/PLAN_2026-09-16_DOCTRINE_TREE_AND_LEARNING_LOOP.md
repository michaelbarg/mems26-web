# תוכנית-ביצוע 16.09 — עץ-דוקטרינה אחד + לולאת-למידה מהנתונים

**מקור:** מייקל 16.09 ‏09:40-10:20 — *"בנינו מערכת מרשימה שהיה לה שבוע של ניצחון יומיומי … אני רוצה לשחזר את ההצלחה … כל מערכת תדע לבצע את תפקידה, כניסה בזמן, מימוש שמתמקסם … אנחנו הולכים במעגלים"* · *"כשאני מתלונן למה אין עסקה — אני רוצה שהמערכת תשקלל את כלל המרכיבים (מיקום המחיר ביחס ליום הקודם, ווליום, ברים) ותייצר ענף נוסף בעץ-ההחלטה"* · *"יש לך את כל המידע ההיסטורי — אם ביום מסוים עשינו 10 נקודות והטווח היה 200, היו המון עסקאות שפספסנו; ואם הפסדנו — המערכת טעתה. זה מחייב שינוי גישה"* · *"יש לך כמה חודשים אחורה — למה להתחיל מאפס?"*
**מעמד:** תוכנית-ביצוע עם תאריכים. **הדוקטרינה** נשארת `docs/spec_authority/LEARNING_DOCTRINE_2026-09-09.md` (מסמך זה אינו מחליף אותה — הוא נותן לה מנגנונים ולוח-זמנים). **הסטטוס** של כל פריט חי רק ב-`docs/plans/TASK_LOG.md` (‏T-389…T-394) — כאן אין סטטוסים.
**נכתב:** cowork-dev, 16.09 ‏10:30 IL.

---

## 1 · המטרה

לחזור לשבוע-הניצחון ולהחזיק אותו: כל מערכת עושה את תפקידה, כניסה **בזמן** (זיהוי-פתיחה ⇒ כניסה, לא זיהוי-יום-בדיעבד ⇒ כניסה ב-POC אחרי שהמהלך נגמר), מימוש שממקסם, ו**למידה שמצטברת** — כל יום מוסיף ידע במקום דגל.

## 2 · האבחנה — למה אנחנו במעגלים (עם המספרים)

| # | הממצא | הראיה |
|---|---|---|
| 1 | **הדוקטרינה מפוזרת ב-322 משתני-env** (‏+90 מאז 12.08). כל פסיקה הפכה לדגל, וכל דגל הוא שער. | `env_loader: applied 322 vars` · `GAP_INVENTORY_2026-09-09`: 35/107 פריטים = "ההוראה האחרונה דרסה את הקודמת בלי מדידה"; 24 שערים, 5 כותבי-יעד, 3 משכתבי-סטופ |
| 2 | **העץ מכיר רק שני משתנים.** `_match_condition(cond, opening_type, day_type)` — כל כלל שמערב מיקום/הרחבה/ווליום/גיל-קיצון חייב קוד, ולכן הופך לדגל. | `backend/v9/services/dalton_playbook.py:102` |
| 3 | **לסוגי-יום שלמים אין דרך-כניסה.** ל-`PULLBACK` ול-`VALUE_RETURN` אפס מפיקים חיים (‏`TREND_STEP`/`RE_ACCEPTANCE` בצל); `REVERSAL` אינו באף שורת-שלב-C. ⇒ ביום-Trend המערכת יכולה רק BREAK. | `config/dalton_playbook.yaml` · `.env` · מפקד-שערים 15.09: **36 חסימות / 0 ירי** |
| 4 | **החלטות על 4-12 עסקאות, במודלים שונים, באמצע-סשן.** | 14-15.09: touch-2 לייב⇒צל⇒לייב; שני מודלי-P&L סותרים באותו יום |
| 5 | **אין לולאת-למידה.** הפער בין "מה היום הציע" ל"מה לקחנו" לא מחושב לעולם; הפסד לא הופך למאפיין. הדוקטרינה מ-09.09 תיארה את הלולאה — ואף מנגנון שלה לא נבנה; השבוע שאחריה הוסיף 90 דגלים. | `MEASUREMENTS.md` מודד דגלים, לא ימים |
| 6 | **יציאות: מערכת-6 מתריעה ולא פועלת.** | 15.09: 1,772 שורות · 70 `stuck_trade` · 107 `stop_too_wide` · **0 פעולות** |

**שבוע-הניצחון, במספרים:** 3 ימי-רווח עם 1-4 עסקאות ליום; ימי-ההפסד — 5-7 עסקאות ליום. ההצלחה לא הייתה "יותר ירי" — הייתה **ירי נכון ומעט**.

## 3 · שלושת המנגנונים החסרים

1. **וקטור-מצב** — המערכת רואה בכל בר-החלטה את כל המרכיבים בבת-אחת, ורושמת אותם: `day_type` + ביטחון · `phase` · `opening_type` · `zone` (מעל-ערך / VAH / אמצע / VAL / מתחת) · `extension` (כיוון + נקודות מעבר ל-IB) · `price_vs_prior_va` · `vol_ratio` (סיבתי: הבר מול חציון אותה-שעה ב-N סשנים קודמים) · `bars_since_extreme` · `dir_hint` · `atr_causal`. **לא מתחילים מאפס:** `v9_trades.cross_context` כבר נושא את רוב הווקטור על 186 עסקאות-לייב ו-1,320 עסקאות-צל מיוני; מהברים (85 סשני-RTH) מחשבים את השאר סיבתית.
2. **שפת-תנאים על הווקטור** — `_match_condition` מעריך ביטוי (‏`zone in [near_val, below_value] and extension == down and bars_since_extreme >= 2`) במקום שני שדות. **פסיקה = שורת-YAML**, לא דגל. הקוד שמחשב מאפיינים נשאר; הקוד שמחליט — עובר לעץ.
3. **ניתוח-פער יומי** — לכל סשן: מה היום הציע (טווח, 3 התנועות הגדולות) · מה נלקח (במודל-הקבוע) · לכל תנועה שהוחמצה: **לא היה setup** (פער-מפיק) / **נחסם** (פער-שער, איזה) / **ירה והפסיד** (הווקטור בכניסה). מצטבר לענפים מוצעים עם ראיה.

## 4 · השלבים

### שלב 0 — היום 16.09: יום-לייב על 2 חוזים, ואחריו הקפאה
- cc סיים T-367 (וריאציה עם הרחבה) ו-T-368 (S6 `stuck_trade`⇒BE אחרי T0). cowork מאמת בשער-הבטיחות בלבד (טסטים · ריצה אחת על 15.09+11.09 עם `T-335=0`/`Traceback=0` · רשימת-כתיבות · ארבעת ה-guards). ריסטארט 15:45 על `ruled_contracts()=2`, fire_drill, GO/NO-GO לטלפון.
- **מהריסטארט הזה: הקפאה.** אין דגל חדש, אין שינוי-config באמצע-סשן. השינוי הבא נכנס רק כגרסת-עץ (שלב 4).
- **תוצר:** יום-לייב תקין. **קריטריון:** `ruled_contracts()=2` בתהליך הרץ · אפס `Traceback` · דוח-EOD.

### שלב 1 — ניתוח-הפער ההיסטורי (cowork · הלילה ⇒ 17.09 בוקר) · T-389
- `scripts/gap_analysis.py` על **85 סשני-RTH** (יוני-ספטמבר): ברים מ-`v9_bars_5min_woodies` + `v9_trades` (לייב+צל, `cross_context`). חישוב קל — לא ההרנס; לא רץ בתוך RTH.
- לכל סשן: תווית-יום סופית · סוג-פתיחה · טווח · נלקח-לייב במודל-הקבוע · 3 התנועות הגדולות ומעמדן (אין-setup / נחסם:שער / ירה-ניצח / ירה-הפסיד) · לכל הפסד: הווקטור בכניסה.
- **תוצר:** `docs/reports/GAP_ANALYSIS_2026-09-17.md` — טבלה לפי סוג-יום, יחס נלקח/טווח, ורשימת **ענפים-מועמדים**: מצב ⇒ פעולה ⇒ ראיה (‏N ימים · N עסקאות · $ במודל-הקבוע). **סף-כניסה להצעה: N ≥ 15.** כולל תשובה במספר לשאלה הפתוחה: `TREND_STEP`/`RE_ACCEPTANCE` לייב או לא — מייקל מכריע אם זה מחכה ל-v2 או נכנס מיד.
- **קריטריון:** כל שורה בדוח ניתנת-לשחזור מפקודה + פלט גולמי (Rule 5).

### שלב 2 — וקטור-מצב + שפת-תנאים + רישום (cc · 17-18.09) · T-390, T-391
- T-390: `SituationVector` מחושב פעם אחת בשער לכל בר-החלטה; נרשם לטבלה `v9_decision_vectors` (בר · וקטור · תוצאת-השער · `blocked_by`) — **גם כשאף מפיק לא ירה**. זה סוגר את החור היחיד בהיסטוריה.
- T-391: `_match_condition` מעריך ביטויים על הווקטור (מעריך בטוח, ללא `eval`), עם טסטים; שורות-YAML קיימות ממשיכות לעבוד כמו שהן.
- **קריטריון:** התנהגות-זהה — אותן החלטות על 15.09+11.09 בהרנס לפני/אחרי (מחוץ ל-RTH); `flag_guard`/`guard_tests` ירוקים.

### שלב 3 — הגירת הכללים הקיימים לעץ, בלי שינוי-התנהגות (cowork כותב · cc מחווט · 18-20.09) · T-392
- לשורות-YAML: T-319b (מיקום שלב-C) · T-329 (פטור) · T-355 (מיקום שלב-B) · T-365 (פתיחה-לפני-סוג-יום, שלבים C/D) · T-367 (וריאציה+הרחבה) · כללי edge_fade · T-366 (כבוי ⇒ אין שורה). כל שורה נושאת `measured:`.
- **כלל "מסירים לפני שמוסיפים":** כל כלל שהיגר — הדגל שלו נמחק מ-`.env` ומהקוד. היעד: 322 ⇒ מתחת ל-250 עד סוף השלב.
- **תוצר:** `config/dalton_playbook.yaml` v2-draft — **עמוד אחד** שמייקל קורא ומאשר ביום א' 20.09 (לפני הצהריים — ערב יום-כיפור).
- **קריטריון:** `scripts/replay_admits.py` על 85 הסשנים ⇒ **אפס הבדל** בהחלטות מול הבסיס. רק אז הדגלים נמחקים.

### שלב 4 — גרסה v2: הענפים הראשונים מהנתונים (מייקל מאשר · לייב 22.09) · T-394
- מהענפים-המועמדים של שלב 1: מייקל מסמן ✓/✗ על העמוד. המאושרים נכנסים כשורות. **גרסה אחת**, נבדקת כמכלול על 85 הסשנים לפי שער-הדוקטרינה §2.4 (משמרת ימי-רווח · דוחה ימי-הפסד · משפרת Σ$) — עם המספר בקובץ.
- **לייב ביום ג' 22.09** (לא ב-21.09 — יום-כיפור, מייקל לא ליד המחשב; 21.09 רץ על הבסיס המוקפא).
- **קריטריון:** מספר-ריפליי בגרסה; `RULED_FLAGS` מצביע לגרסה ולא לדגל.

### שלב 5 — יציאות עם מספרים (cc · במקביל, לא חוסם) · T-368 ✅ + מדידה בשלב 1
- T-368 הוא הצעד הראשון של S6 מ"מתריע" ל"פועל" (‏protective בלבד: MODIFY_STOP). שלב 1 מודד על 186 עסקאות-הלייב גם: `STRUCTURE_EXIT_REALIZE_PRE_T1` (פסיקת 10.09, כבוי), הידוק-S6 אחרי T1, ו-BE-אחרי-T1 לפי סוג-יום. כללי-יציאה שעוברים את השער נכנסים ל-v2 כשורות — לא כדגלים.

### שלב 6 — קצב-העבודה הקבוע (מ-17.09) · T-393
- **יומי, אוטומטי, בלי הקלוד-אפ פתוח:** `scripts/eod_gap_report.py` ב-LaunchAgent ‏23:10 IL ⇒ הודעת-טלפון אחת (≤500 תווים): טווח · נלקח · 3 התנועות הגדולות ומה עצר אותנו. (המשימות-המתוזמנות של Cowork רצות רק כשהאפליקציה פתוחה — 15.09 15:45 לא ירה; לכן launchd.)
- **שבועי, יום א':** דוח-ענפים מהפערים המצטברים ⇒ מייקל מאשר ⇒ גרסה חדשה ⇒ לייב יום ב'. **גרסה אחת בשבוע.** באמצע השבוע — אפס שינויים; תלונה של מייקל ("למה לא הייתה עסקה ב-17:05?") נענית **מהווקטור שנרשם**, והמקרה מצטרף לדוח של יום א'.
- **מודל-הערכה קבוע אחד** (סולם 3/4/5, T0=±3, BE אחרי T1, $5/נק'/חוזה, 5-דק', AMBIG לא-מוקצה). לא מחליפים מודל כדי לקבל תשובה אחרת.

## 5 · לוח-זמנים

| מתי | מה | מי |
|---|---|---|
| ד' 16.09 | שלב 0: אימות T-367/T-368 · ריסטארט 15:45 על 2 · GO/NO-GO · הקפאה | cowork |
| ד' 16.09 לילה | שלב 1: `gap_analysis.py` על 85 סשנים | cowork |
| ה' 17.09 בוקר | דוח-הפער + ענפים-מועמדים + תשובה במספר על TREND_STEP/RE_ACCEPTANCE | cowork ⇒ מייקל |
| ה'-ו' 17-18.09 | שלב 2: וקטור + שפת-תנאים + `v9_decision_vectors` · שלב 6: דוח-EOD ב-launchd | cc |
| ו'-ש' 18-19.09 | שלב 3: הגירה לעץ, אפס-הבדל על 85 סשנים, מחיקת דגלים | cowork+cc |
| א' 20.09 (עד הצהריים) | מייקל קורא עמוד אחד: העץ המהגר + ענפי-v2 המוצעים ⇒ ✓/✗ | מייקל |
| ב' 21.09 | יום-כיפור — הבסיס המוקפא רץ (או לא סוחרים — פסיקת-מייקל) | — |
| ג' 22.09 | v2 לייב · מכאן: גרסה אחת בשבוע | cowork |

## 6 · חוקי-המשחק (חלים על cowork, cc, phone-claude)

1. אין דגל-env חדש. פסיקה = שורת-YAML עם `measured:`.
2. אין שינוי-config בתוך סשן-מסחר. אין ריסטארט 16:10-23:00 בלי מייקל.
3. אין החלטה על < 15 מופעים. אין החלפת מודל-הערכה.
4. אין ריפליי "האם דגל X עוזר". יש ניתוח-פער "איפה פספסנו ולמה".
5. מסירים לפני שמוסיפים: כלל שנכנס לעץ — הדגל שלו נמחק.
6. כלל-דוקטרינה יושב בשער/בעץ פעם אחת לכל המפיקים — לא בגלאי.
7. גודל וסיכון (‏`FIXED_CONTRACTS_*`, `RISK_*`) — פסיקת-מייקל בלבד, לעולם לא מהנתונים.
8. ראיה = פקודה + פלט גולמי (Rule 5). סטטוס = `TASK_LOG.md` בלבד.

## 7 · מה משתנה עבור מייקל

- **מפסיק:** לאשר דגלים; לשאול "למה אין עסקה" בלי לקבל תשובה מנתונים; להחליט על 4 עסקאות.
- **מקבל:** כל ערב — דוח-פער בטלפון. כל יום א' — עמוד אחד: הענפים המוצעים עם הראיות, ✓/✗. פסיקות-דוקטרינה נשארות שלו — הן פשוט נכתבות כשורה שהוא יכול לקרוא.
- **ממשיך לבד:** גודל, סיכון, arm/disarm.

## 8 · מדדי-הצלחה (נמדדים כל יום א')

| מדד | היום | יעד |
|---|---|---|
| נלקח / טווח (ממוצע-שבועי) | 15.09: $0 על טווח 44 נק' · 14.09: −$137 על טווח 36 נק' | עולה שבוע-על-שבוע |
| ימי-רווח / ימי-מסחר | ספטמבר: 27 עסקאות-לייב, −$704 | ≥ 60% |
| עסקאות ליום | 0 או 5-7 | 1-4 (כמו בשבוע-הניצחון) |
| משתני-env | 322 | < 250 (20.09) ⇒ < 200 (31.10) |
| שינויים באמצע-שבוע | ~90 ב-4 שבועות | 0 |
| דוח-EOD אוטומטי | אין | כל יום-מסחר |

## 9 · מה **לא** מובטח, ומה הסיכון

- **לא מובטח $200/יום.** מובטחת לולאה שכל שבוע יודעת יותר מהשבוע שלפניו, ומספר לכל שינוי לפני שהוא נוגע בכסף.
- **סיכון א':** ההגירה (שלב 3) לא מגיעה לאפס-הבדל ⇒ לא מוחקים דגלים עד שכן; v2 מתעכב שבוע, הלייב ממשיך על הבסיס.
- **סיכון ב':** 85 סשנים על תת-סוגי-יום נדירים (Neutral, Nontrend) ⇒ N < 15 ⇒ אין ענף; המערכת נשארת שם כמו היום עד שיצטבר.
- **סיכון ג':** יום-כיפור 21.09 — המערכת לבד. או בסיס מוקפא בלבד, או לא סוחרים. פסיקה של מייקל עד יום א'.
