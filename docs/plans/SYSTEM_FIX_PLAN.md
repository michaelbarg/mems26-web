# תוכנית-תיקון המערכת

**נפתחה:** 2026-08-25 · **בעלים:** cowork-dev · **מקור-האמת לסדר-העבודה**
**מבוססת על:** `FORENSICS_BREAK_2026-08-19.md` · `SYSTEM_MAP_S1_S2_S4_2026-08-25.md` ·
`GAP_VS_CURSOR_PLAN_2026-08-25.md` · `SCID_DIFF_RCA_2026-08-25.md`
**משלימה את** `MAXIMIZATION_INFRA_EXECUTION_PLAN_2026-08-24.md` (התוכנית של קורסור) —
לא מחליפה אותה. שם ה-Tasks; כאן הסדר, הראיה והשער.

---

## העיקרון שקובע את הסדר

```
עם-המגמה, בימים שזזים ≥20 נק':   +$8,040   על n=254
כל השאר:                          -$14,464   על n=307
```

**כל שורה בתוכנית הזו קיימת כדי שנסחור יותר בתא הראשון ופחות בשני.**
פריט שלא משרת את זה — לא נכנס.

והסדר נובע מתלות אחת: **אי-אפשר לשפר את מה שאי-אפשר למדוד, ואי-אפשר למדוד ממקור משקר.**
לכן: לעצור זיהום → לתקן מדידה → לתקן כיוון → לבנות מכריע → להגדיל.

---

## שלב −1 · שלושת השורשים  ✅ **נפסק 25.08: "מאשר"**
*קודם לכל השאר. **ההחזר הגבוה ביותר בריפו.***

ביקורת-אמת של 204 הדגלים (`FLAG_AUDIT_2026-08-25.md`): **59 מ-174 הדלוקים אינם מבצעים
את מה שנפסק** — 18 מתים, 41 חלקיים, רק 84 (48%) מוכחים. **אבל אלה לא 59 באגים.**

| # | השורש | מה הוא הורג | הראיה |
|---|---|---|---|
| **−1.1** | **`backend.v9.app` מול `backend.main`** — קוד קורא מאובייקט-FastAPI שאיש לא כותב אליו | **חמישה דגלים מתכונה אחת:** `MARKET_CONTEXT_V1` ❌ ← `OPENING_FIRST_TRADE_STRICT_V1` חוסם 7/7 ← `OPENING_CONF_ENGINE_FUSE_V1` ❌ ← `OPENING_PLAYBOOK_V1`+`OPENING_WINDOW_FIRE_V1` בלי קלט. וגם `S1_DAY_DIRECTION_V1` | `balance=UNKNOWN` ב-**673/673** · הכותב היחיד `main.py:674` · הפתרון קיים פעמיים בריפו (`trading_gateway:195` · `daytype_watchdog._resolve_app_state`) |
| **−1.2** | **`_pattern_family()` → None** ל-`S2_DELTA_DBL` (`daytype_position_gate.py:55-72`) | התבנית בלתי-נראית ל-**4 שערים** ולפלייבוק | **105 מתוך 108 חסימות-הגייטוויי.** תיקון שורה אחת — אך **פותח** מסלולים ⇒ §D מלא |
| **−1.3** | **שני כותבים לסטופ** | `StepLadder` דורס את `StopResolver` | **68 מתוך 69** · **אבחון תחילה** — ראיית-P&L, ואז פסיקת-מייקל מי גובר |
| **−1.4** | **שן שנייה ל-`flag_guard`** | הכלי עובר 204/204 ועיוור לכל 59 | דגל בלי אתר-קריאה ⇒ NO-GO · `INERT (parent OFF)` תופס 6 · `INERT (no-op env line)` תופס 5 |

**פקודת-עבודה:** `docs/handoff/CC_WORKORDER_3ROOTS_2026-08-25.md`

---

## שלב 0 · לעצור את הדימום
*כל יום שעובר בלי זה מזהם עוד סשן. זה קודם לתיקון היסטוריה.*

| # | פריט | הראיה | שער |
|---|---|---|---|
| **0.1** | **‏`TS-OFFSET-GATE` דולף** — `bars.py:596-599` מעביר מנה לא-מתקדמת ("pass but logged"), ו-`bars.py:556` מחזיק את המצב **בזיכרון** ⇒ הדחיפה הראשונה אחרי **כל ריסטארט** עוברת בלי בדיקה | 12 סשנים מוסטים **+60 דק'**. ‏07-17: כל 78 הברים זהים-בייט ל-MES אך שעה מאוחר — "הסשן" ב-DB הוא 10:30→16:55, שעת-הפתיחה חסרה | הגייט דוחה מנה-ישנה גם אחרי ריסטארט · טסט מריץ-קוד |
| **0.2** | **מפתח-CVD** — `bars.py:1096` כותב `bar_id = f"cvd_{idx}"`, אינדקס-גרף שמתמספר מחדש בכל טעינה | **246 חותמות סותרות**; דוגמה 07-14 16:35 עם **4 שורות** משלושה תאריכי-יצירה, אחת מהן ‎−351/4352 מול 2710/2080 | `UNIQUE(ts, symbol, source_version)` · 0 סתירות |
| **0.3** | **‏CVD חתום שנייה מוקדם** — `bars.py:1067` כותב `HH:M4:59`; הצרכן קוטם שניות ⇒ השורה נופלת בשקט | **881 מ-2,694 שורות (32.7%)** נעלמות | 0 שורות עם שנייה שאינה 00 |
| **0.4** | **‏TPO לא-סיבתי** — 🎁 **השורש נמצא 30.08: באג-TZ של 3 שעות**, לא פיגור אמיתי. הכותב מייצר מחרוזת-UTC **נאיבית** ו-PG מתייג ‎+03 | **728 מ-729** עם `created_at > ts` (נמדד 30.08), אפס `market_ts`/`available_at`. אימות: `ts=14:30+03` ב-27.08 מחזיק `ib=7730.5/7702.75` = קיצוני 09:30-10:25 ET; אחרי `+3h` הבּיָשנוּת החציונית **15 דק'** | `timestamptz` מודע · `market_ts`+`available_at` · 🔴 **דחוף — `DALTON_EDGE=live` כבר רץ על הקלט הזה** |
| **0.5** | **יומן-המילויים מת** מ-21.08 22:52 | ‏`pnl_sierra` מאוכלס ב-5 מ-64 עסקאות; **0 מ-13** ב-24.08 ⇒ אין אמת-ברוקר לאמת מולה | היומן כותב · `pnl_sierra` מאוכלס על עסקה חדשה |

| **0.6** | **‏🔴🔴 T-160 · P&L סינתטי** — `sierra_position_reconciler.py:881` סוגר `phantom_reconcile` **בלי `exit_price`**; `manager.py:1658` נופל למחיר-**היעד** | ‏20 יום `mode='live'`: הכל **+$360.00** (n=46) · **בלי phantom −$46.25** (n=35) · phantom **+$406.25** (n=11), **`exit_price` ריק ב-11/11 וכולן מנצחות.** רחב מהפאנטום: `T2_HIT` 3/3 · `T3_HIT` 1/1 · `MAE_SCRATCH` 7/7 · `SIERRA_FLAT` 3/3 | `PNL_REQUIRES_EXIT_PRICE_V1`: סגירה בלי מחיר ⇒ `NULL` + `pnl_status='UNPRICED'`, **לא מספר** · `pnl_trusted` על ההיסטוריה **בלי לגעת בשורות** |
| **0.7** | **באג-יחידה ב-`lib.atr_from`** — מחשב **ATR יומי** (טווח-יום + פער-לילה) ונצרך בשם `ATR14` כאילו הוא של ברי-5-דק' | **91.18 נק' מול 7.20 האמיתי — גורם 12.7.** אומת מול `v9_bars_5min_woodies`, **0/45 אי-התאמות**. אותה מחלקה ככלל-4 ב-CLAUDE.md (עמימות-יחידה) | יחידה בשם · לספור צרכנים בייצור · ⚠️ **ייתכן שספי-סטופ ותקרות-סיכון חיים מנופחים פי-12.7** |
| **0.8** | **‏T-152 · §D על הצל מוטה מבנית** — לצל אין הגבלת-סלוטים ⇒ מנפח כל מנגנון · **‏T-153** אין ליגר מאוחד | **חוסם את 8 הדגלים שבצל** ואת יום-הצל כולו | ‏**VIEW** ולא טבלה רביעית (מקור-שלישי = תנאי-עצירה) · §D מודע-סלוטים + **גורם-הניפוח שנמדד** |

---

## 📌 ממצא-מחקר 30.08 — סף-החדירה (מועמד-חי, לא ממצא-מוכח)

**‏3.5 נק' חדירה מעבר לקצה מבני ⇒ `P(4 נק' חזרה לפני 4 נק' המשך) = 70.8%`** ·
‏n=72/32 סשנים · CI ‏[58.3%, 82.6%] · **jackknife לא מתהפך** · שני חצאי-OOS חיוביים ·
**פלצבו מתהפך בסימן** (בר-גדול הרחק מרמה **ממשיך**, ליד רמה **חוזר**; ניגוד z=+3.49).

**‏🔴 והממצא הלא-אינטואיטיבי: הסף אינווריאנטי, לא יחסי.** מייקל ביקש יחסיות; נבדק
מול חמישה מכנים (VA · IB · ATR5 · טווח-יום · טווח-בר) — **15/15 לטובת המוחלט.**
עומק-החדירה **בלתי-תלוי בסקאלת-היום** (spearman ‎+0.02…+0.05) ⇒ אין אפקט-קנה-מידה
להסיר, ונירמול מוסיף רעש. אותו חתך 3.50 בחצי-הצר מול הרחב: **65-75%, אף חצי לא נכשל.**

**החלון:** 1-3 ברים. **החזרה מגיעה פי 2.2-3.5 מהר יותר מההמשך** (4 דק' מול 13) —
וזה בדיוק מסביר למה מבחן-ההמתנה נכשל: המתנה מפספסת את הרגל הרווחית.

**מה שנשלל ואל תחזור אליו:** סמן-מיצוי חוזה (`P`=49/33/36/50%) · המתנת-1-2-ברים
(אחרי בר 51/49, אחרי שניים בדיוק אותו דבר) · התניה על סוג-יום (מבולבל-לוח-שנה) ·
התניה על נפח (פרוקסי לעומק; **דלתון הופרך פעמיים** — נפח גבוה בקצה הולך עם **חזרה**).

**המבחן שנותר: צל קדימה** — 1,430 תאים מצטברים, עוד חתך לא יוסיף. **וגם הוא חסום
מאחורי 0.8.**

---

## שלב 1 · מדידה כנה
*בלי זה כל מספר שנפיק הוא ניחוש מנומק.*

| # | פריט | הראיה | שער |
|---|---|---|---|
| **1.1** | **‏`RESOLVED` + MFE/MAE** — הליגר רושם מועמדים ולא תוצאות | `grep RESOLVED\|mfe_3` בקוד-הייצור → **אפס**. שער-ה-300 של Task 6 עומד על 0 | אידמפוטנטי · סשן פגום = `NOT_JUDGEABLE` **ולא נזרק** · מחוץ לנתיב-הירי |
| **1.2** | **להחזיר 9 סשנים** (כשלי-CVD בלבד; הברים כבר זהים-בייט) | ‏SCID מכסה 78 ברים ב-**כל 34** התאריכים — **אפס אבודים**. ‎9 מיידיים ⇒ 18/34 | diff→RCA→snapshot→dry-run→repair→four-axis |
| **1.3** | **12 סשני-ההיסט** — הברים ניתנים לתיקון; **שדות-ווּדיס לא ניתנים לשחזור מ-SCID** | ‏cci/lsma/ema יושבים על חותמת שגויה | 🔴 **פסיקת-מייקל:** `NULL` כן, ייצוא-מחדש מסיירה, או חישוב-מחדש (=מקור-אמת שני, אסור) |
| **1.4** | **הרפליי רץ על mirrors** — `good_pattern_fix.py:189` מצהיר "live-faithful **mirror** of S2" | תנאי-עצירה של התוכנית **מתקיים**: replay/live בפונקציות שונות | הרפליי מייבא את הגלאי החי · שער נאכף, לא הדפסה |
| **1.5** | **‏Stage 0C** — הקרנל מייצר מועמדים | `candidate_engine_id="none-stage0b"` ⇒ 5 ימי-anchor לא ניתנים להשוואה | ‏`ReplayCandidateEngine`+`Policy`+`ExecutionModel` · parity על 3 עוגנים |

---

## שלב 2 · שרשרת-הכיוון
*כאן הכסף. מתחיל רק אחרי ש-1.1 ו-1.4 ירוקים — אחרת נמדוד את עצמנו בראי.*

| # | פריט | הראיה | שער |
|---|---|---|---|
| **2.1** | **מערכת 1 → `day_direction`** · 🔵 **בצל מהלילה** | `v9_day_type_state.direction` מאוכלס **26/32**: `with_extension(UP\|DOWN)` = יום-מגמה, לסחור עם · `fade_both(UP\|DOWN)` = יום-איזון, לדהות שני קצוות. הגייטוויי שולף `confidence` בלבד (`:1423`) | §D + שני מבחני-סיכון: `Trend_Normal`×SHORT (n=52, ‎−7.01) **משתפר** · "בתוך-IB" (n=53, ‎+3.93) **לא נפגע** |
| **2.2** | **‏`fade_both` = אין דרישת-כיוון** — לא לתרגם אותו ל-UP/DOWN | 13 מ-26 השורות. תרגום ל-UP/DOWN אומר "סחור עם DOWN" בעוד מערכת 1 אומרת "דהה את שני הקצוות" | הצל של הלילה מודד בדיוק את זה |
| **2.3** | **לפצל `Variation`** — `main.py:532` + `trade_context.py:602` ממפים `Normal_Variation → Variation` | **0 מופעים ב-641 עסקאות**. ‏`Variation` = **55% מהספר**, שלילי בכל 4 התאים בשני העידנים | shadow תחילה (n=248 זמין) · §D פר תת-סוג |
| **2.4** | ✅ **נפסק 25.08: "להמשיך עם דלתון בינארי"** — 8 ספי-האחוזים יורדים | `direction_compass.py:187-192` קובע **כיוון** מציון מול סף · `opening_type_gate.py:229-234` **הופך כיוון** ב-`if pos >= 0.66` · ועוד 6 | 🔴 **פסיקת-מייקל** על כל אחד: להסיר או להצדיק |
| **2.5** | **‏`TREND_LEG_CHASE_EXEMPT_V1` מת** — `trading_gateway.py:1998` קורא `.get()` על מחרוזת, `AttributeError` נבלע ב-`:2011` | לא יכול לירות לעולם | תיקון + טסט שנכשל אם המקור מת |
| **2.6** | **נגיעות-T1 שלא נרשמו** | **18 מקרים בטיקים** · זיכוי `+$127.50` (A) / `+$207.50` (B) · חסם-עליון `+$925`/`+$957` תחת פסיקת "אחרי T1 → BE" | טסט על #750 ועל 17 האחרים |

---

## שלב 3 · המכריע
*‏Task 5 של קורסור. אפס קוד היום. נבנה רק כששלב 2 ירוק — מכריע על נתונים שבורים הוא מכריע שגוי.*

| # | פריט | שער |
|---|---|---|
| **3.1** | `CONTEXT_ENTRY_V1` — pure function: מועמד + מצב-מבני + מיקום + רמות + זרימה ⇒ `ALLOW/SKIP/WAIT/EXPIRE` + סטופ/יעדים | אותו קלט מסודר ⇒ פלט **זהה-בייט** ב-unit/replay/shadow · flag-OFF זהה-בייט · shadow ≥10 סשנים |

---

## שלב 4 · להגדיל
| # | פריט | שער |
|---|---|---|
| **4.1** | ‏Ranker | ‏**≥300 outcomes נקיים** מהליגר (היום **0**) · OOS נפרד · schema קפוא |
| **4.2** | גודל-עסקה | רק אחרי ש-2.1–2.3 חיים ונמדדו ≥10 סשנים |

---

## שלושת החוסמים שדורשים פסיקת-מייקל

| # | הפסיקה | למה זה שלך |
|---|---|---|
| **1.3** | שדות-ווּדיס ב-12 סשני-ההיסט: `NULL` · ייצוא-מחדש · חישוב-מחדש | חישוב-מחדש יוצר **מקור-אמת שני** — תנאי-עצירה של התוכנית |
| ~~2.4~~ | ~~8 ספי-האחוזים~~ | ✅ **נפסק 25.08 — דלתון בינארי. בביצוע.** |
| ~~1.2~~ | ~~תיקון-DB היסטורי~~ | ✅ **נפסק 25.08 "לבצע מיידית" — בהכנה אצל cc; כתיבה אחרי 23:00** |

---

## איך התוכנית נשארת מסודרת

1. **הקובץ הזה הוא מקור-האמת לסדר.** ‏`TASK_LOG.md` עוקב אחרי הביצוע היומי;
   `STATUS_BOARD.md` הוא הרישום ההיסטורי. **שלושה קבצים, שלושה תפקידים, בלי כפילות.**
2. **פריט נכנס רק עם ראיה מספרית** — כמה זה שווה או מה זה חוסם. בלי ראיה, לא נכנס.
3. **פריט נסגר רק עם פקודה+פלט גולמי** (חוק-5) ועם השער שכתוב בשורה שלו.
4. **שינוי-משטח-מסחר לא עולה בלי §D** — דלתא **וגם** חציון-יום, פר-דגל בנפרד.
   היום שלושה דגלים נראו נכונים ונפלו ב-§D; אחד מהם חסם 11 מנצחות.
5. **דגל נפרד לכל תיקון.** ב-19.08 הודלקו 33 דגלים תוך 5 ימים בלי בידוד, ולכן
   שבוע אחרי אי-אפשר לייחס את ההפסד לאף אחד מהם.
6. **מה שאי-אפשר לאמת — נשאר כבוי ומדווח.** "נראה הגיוני" אינו ראיה.

---

## מצב נוכחי · 2026-08-25 16:00

**חי:** 3 חוזים (T0/T1/T2, backend ו-DLL מסכימים) · `flag_guard` 204 · `fire_drill` 🟢 GO
**בצל מהלילה:** `S1_DAY_DIRECTION_V1` (שני מקורות-כיוון זה מול זה) · `CANDIDATE_LEDGER_V1`
**כבוי אחרי כישלון §D:** `MORNING_LABEL_CONFIRM_V1` · `DAY_DIRECTION_STRUCTURAL_V1` · `VARIATION_SUBTYPE_V1`
**בעבודה אצל cc:** 1.1 (`RESOLVED`)

---

## פיקוח מסודר (נוסף 26.08 לפי פסיקת-מייקל)

**קצב יומי קבוע של cowork — שלוש נקודות-בקרה, כל אחת עם פלט כתוב:**

| מתי | מה נבדק | תוצר |
|---|---|---|
| **בוקר (עד 12:00)** | `git pull` · דוח-cc מאתמול מול השערים · flag_guard+liveness · אימות ✓/✗ פר-פריט עם פקודה+פלט | GO/NO-GO ב-LIVE_CHANNEL |
| **טרום-פתיחה (15:45)** | fire_drill · feed · effective_contracts · slot · הצללים כותבים · אין פוזיציה לא-מוסברת | שורת 🟢/🔴 לפני 16:10 |
| **‏EOD (אחרי 23:00)** | ‏P&L ‏live/shadow מול הצפי · מה הליגר רשם (מועמדים·חסימות·RESOLVED) · הצל של S1DayDir (הסכמת-מקורות) · חריגים | סיכום-יום ב-LIVE_CHANNEL + עדכון התוכנית |

**כללי-הפיקוח:** cc לא מדליק ולא מריסטארט — רק cowork+מייקל · כל "עבר" של cc נבדק מחדש
בפקודה+פלט (חוק-5, טעות-9) · דגל חדש בלי טסט-מוטציה לא עובר שער · §D לפני כל הדלקה ·
ופעם בשבוע: הרצת FLAG_AUDIT מחודשת — המספר 59 חייב לרדת, לא לעמוד.
