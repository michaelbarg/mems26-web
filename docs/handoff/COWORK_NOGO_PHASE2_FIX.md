# 🔴 NO-GO ממוקד · פאזה 2 — העוגן נכשל וה-WHATIF הוא מראה

**מאת:** cowork · **אל:** cc · 2026-08-26 10:20
**אומת ✅:** VA_FADE כבוי-בקוד · flag_guard 208+liveness · ענף-shadow לשורש-1 בקוד · INBOX מקומי.

## ממצא 1 · עוגן-25.08 נכשל

```
$ VA_FADE_V1=1 python3 scripts/whatif_report.py --session 2026-08-25
=== 78 bars, 11 setups ===  ← אפס VA_FADE. כל ה-11 הם setups ישנים
$ grep -c "va_fade" scripts/whatif_report.py
0
```

## ממצא 2 · השורש — WHATIF אינו מה שהוא מצהיר

ה-docstring: *"runs them through the live detection chain"*. בפועל: `SELECT ... FROM
v9_five_min_setups` — **הוא קורא setups שנשמרו בעבר ומשחזר את התוצאה שלהם.**
הוא לא מריץ אף גלאי, ולכן **לעולם לא יגלה מועמד חדש** (VA_FADE, או כל תבנית שהמערכת
הישנה פספסה). זו בדיוק מחלקת-המראות (תנאי-עצירה 1.4), והמטרה של מייקל — *"מי יירה
עם המצב המעודכן"* — לא מתקיימת: זה "מה היו שווים הזיהויים הישנים", לא "מה המערכת
של היום הייתה רואה".

## התיקון

1. **WHATIF מריץ גלאים, לא קורא טבלה.** בלולאת-הברים: לייבא ולהריץ את הגלאים
   **החיים** — לפחות `va_fade.detect()` + גלאי-S2 (reactive/initiative) — על חלון-הברים
   עד בר-ההחלטה. ‏VAH/VAL לרפליי: מ-`v9_tpo_history` של אותו יום (עם הסתייגות
   הסיבתיות הידועה — לציין בדוח), או חישוב-VA מהברים עצמם עד אותו רגע. אסור לקרוא
   `v9_five_min_setups` כמקור-מועמדים; מותר להשאירו כעמודת-השוואה "מה זוהה אז".
2. **לתקן את ה-docstring** — כלל-5: הכלי אומר מה שהוא עושה.
3. **להריץ מחדש את עוגן-25.08:** ‏4 הרוטציות (‏10:30-11:50 VAL · 12:00-13:00 VAH ·
   ‏13:25-14:15 VAL · 14:45-15:00 VAH, ‏VAL=7680.25 VAH=7691.5) חייבות להניב מועמדי
   VA_FADE בכיוון-הדוקטרינה. להדביק את הפלט המלא.
4. אם `_load_sierra_tpo()` לא עובד בהקשר-רפליי — זה בדיוק סוג-הפער לדווח, לא לעקוף.

עד שהעוגן עובר — **שער-פאזה-2 סגור** ו-VA_FADE לא עולה אפילו לצל.
