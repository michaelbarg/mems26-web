# MEMS26 · טיוטות-עיצוב — 2026-07-13

מקור: `PATTERN_EOD_2026-07-13.md` (EOD 15:12 CT). **קריאה-בלבד — לא-מומש.** כל design מוכן-להפיכה-לפרומפט.
מקרא: **[TRADING-LOGIC]** = משנה risk/fire/exit-surface → **דורש אישור-Michael + סטופ-אסטרטגי** · **[DISPLAY/SAFE]** = תצוגה/instrumentation, ללא-שינוי-מסחר · **[SoT→CC]** = דורש הצלבת Sierra v9_export/DB/לוג ע"י CC לפני-מימוש.

**היום שונה מ-07-09:** אתמול-שעבר הבעיה הייתה targets-קורסים-מתחת-לכניסה (I-54) שיצרו phantom-fill (I-64) והיפוך shadow↔live (I-65) — **shadow הפך מנצחים להפסדים.** **היום המערכת עבדה: יום-ירידה, 4 שורטי-בוקר מנצחים אמיתיים (+$297.5 סים / +$92.5 live), ו-shadow תאם-live על 2/3 צמדים.** הבעיה-הדומיננטית עברה מ-targets ל-**R-חשבונאות (I-22)**: ΣR-רשום +68.3R מול +8.1R-אמת. + **חשוד-חדש GHOST (I-67)** — תבנית-S4-לא-מתועדת שירתה נגד-מגמה עם targets-שבורים. ה-target-engine **השתפר** (collapse-07-09 לא-חזר) אך לא-נרפא (GHOST עדיין `t2>t3`).

---

## §1 · I-22 (אומת + שורש-נראה) — `pnl_r` מנופח/הפוך-סימן: מחלק ב-stop-נגרר · **[TRADING-LOGIC/DATA] · [SoT→CC]**

**שורש (נצפה):** id364 רשם `pnl_r=+33.33` על עסקה של **+2.88R-אמת** (T3 12.25pt / 1R=|7585.25−7589.5|=4.25pt); id366 רשם `pnl_r=+31` על **עסקת-הפסד** (`pnl_usd=−1.25, outcome=LOSS`) — **סימן-הפוך.** בשתיהן ה-`stop`-המוצג **נגרר לכמעט-כניסה** אחרי T1/T2 (364: `stop=7585` מול `stop_initial=7589.5`; 366: `stop=7570.5` מול `stop_initial=7574.5`, מרחק ~0.25pt). ⇒ נוסחת-R מחלקת ב-`entry−stop`(הנגרר, →0) במקום ב-`stop_initial` → R מתפוצץ. ΣR-רשום היום **+68.3R** מול **+8.1R-אמת** (ניפוח ×8.4, כולו מ-364/366).

**הצלבה-נדרשת לפני-כל-מהלך (CC):** לאתר את חישוב-`pnl_r` (`v9_trades` write-path / gateway pnl-calc / `fill_poller`) ולאשר: (א) המכנה הוא `entry−stop`(נוכחי/נגרר) ולא `entry−stop_initial`; (ב) אין אכיפת `sign(pnl_r)==sign(pnl_usd)`. לצלב raw של 364/366 מול הלוג.

**שינוי מוצע (`.../pnl_calc`):**
1. **מכנה-R קבוע:** `pnl_r = pnl_pts / |entry − stop_initial|` — תמיד `stop_initial`, לעולם-לא-ה-stop-הנגרר. (הרווח-מ-trailing כבר-בא-לידי-ביטוי ב-מונה pnl_pts; אין-להזרים-אותו-גם-למכנה.)
2. **אכיפת-סימן:** `assert sign(pnl_r) == sign(pnl_usd)`; פער → `logger.warning` (rate-limited) + `pnl_r` נגזר-מ-pnl_usd.
3. **תקרת-שפיות:** `|pnl_r| ≤ R_MAX` (למשל 10); חריגה → סימון `pnl_r_suspect=true` + לוג, לא-הזרקה-שקטה ל-ΣR.
4. **instrumentation:** לשמור `one_r_pts` (=`|entry−stop_initial|`) ב-record ל-אימות-EOD ישיר.

**Regression שיוכיח:** `test_pnl_r_uses_stop_initial` — fixture SHORT (entry 7585.25, stop_initial 7589.5, exit T3 7573, stop-trailed 7585): מוודא `pnl_r≈2.88` (לא 33.33). `test_pnl_r_sign_matches_usd` — הפסד (pnl_usd<0) ⇒ `pnl_r<0` (anti-regression מול 366: +31R על −$1.25). Anti-regression מפורש מול 364/366 של-היום.

**עדיפות:** 🔴🔴 — **הבעיה #1 עכשיו.** חוסמת כל מדד-מבוסס-R (ΣR/win-rate/expectancy) וב-LIVE תעוות sizing/risk. **3-החמורים · #1.**

---

## §2 · I-67 (חדש) — תבנית `GHOST` (S4) לא-מתועדת, נגד-מגמה, targets מופרי-סדר · **[TRADING-LOGIC] · [SoT→CC]**

**שורש (נצפה):** ids 367(shadow)/368(live), 12:10 CT, `pattern_id="GHOST"/trigger="GHOST"`, **LONG ביום-ירידה** (נגד-מגמה), נעצרו −1R מיָדית (bars=1). `t1=7589.5 < t3=7603.5 < t2=7610.5` ⇒ **t2>t3** (הפרת-מונוטוניות ל-LONG). GHOST **אינה** ברשימת-S4 של מפרט-הדיאגנוסטיקה (ZLR/TLB/TT/GB100/HFE/HTLB/FAMIR). שני-הנתיבים (shadow/live) הסכימו ⇒ **תבנית-אמת** (לא-באג-נתיב) עם התנהגות+targets-חשודים.

**הצלבה-נדרשת (CC):** לזהות את-מקור-`GHOST` בקוד (`backend/v9/systems/woodies/` detectors / S4 pattern-map) — האם: (א) S4-detector-חדש שנוסף, (ב) כינוי-fallback/placeholder כשהתבנית-האמיתית לא-זוהתה, (ג) באג-שיוך (pattern_id שגוי). מדוע נורתה LONG נגד-המגמה-הדומיננטית (day-trend RED, opening_type OPEN_DRIVE-מטה). ומקור ה-target-calc שנתן `t2>t3`. לצלב לוג 12:10 CT id367/368 + Sierra v9_export (woodies/CCI-14 גולמי לאותו-בר).

**שינוי מוצע (מותנה-בממצא):**
- אם **תבנית-אמת-חדשה** → לתעד ב-`SYSTEM_INDEX.md`/מפרט-הדיאגנוסטיקה (הוספה לרשימת-S4) + לבחון את-תנאי-הדריכה שלה (למה נגד-מגמה); להעביר את-ה-target-calc שלה דרך אכיפת-כיוון+מונוטוניות (§3).
- אם **fallback/placeholder** → לתקן שלא-ייכתב כ-`pattern_id` של עסקה-אמת (יסתיר את-התבנית-האמיתית); לתייג `pattern_unresolved=true`.
- אם **באג-שיוך** → תיקון-מיפוי + regression.

**Regression שיוכיח:** `test_ghost_pattern_documented_or_flagged` — כל `pattern_id` שנכתב ל-`v9_trades` חייב להיות ב-enum-התבניות-המתועד; "GHOST" לא-מתועד → נכשל (מכריח החלטה: תעד או תקן). `test_s4_targets_monotonic` — כל targets-S4 (כולל-GHOST) `entry<t1<t2<t3` ל-LONG.

**עדיפות:** 🟠 — תבנית-לא-מובנת שיורה עסקאות-אמת נגד-מגמה עם targets-שבורים; חייבת-אפיון לפני-LIVE. **3-החמורים · #2.**

---

## §3 · I-65 (חזר, מתון) + I-64 (חזר, מתון) — shadow↔live hit-divergence + phantom-H1 + exit_price=null · **[TRADING-LOGIC/DATA] · [SoT→CC]**

**שורש (נצפה):** 365(shadow)/366(live) = **אותו ZLR_SHORT** (11:50, e7570.75, stop_initial 7574.5), אך **hit-detection שונה**: shadow=noHit; live=**H1-פנטום** (שפל-אמת 7564 > t1 7563.5 — לא-נגע, אך `t1_hit=true`; ה-בר-11:50 שפל 7563.5 = t1 בורדרליין — כנראה `>=` מול `>`). ה-H1-הפנטום גרר את-ה-stop → הזין את I-22 על 366. בנוסף, **כל 4 ה-`T3_HIT` (360/361/362/364) נסגרו `exit_price=null`** למרות ניצחון-אמת. **הפעם בלי היפוך-סימן** (07-09: shadow הפך winner ל-loss; היום 360/361 + 367/368 תאמו — רק 365/366 סטו) ⇒ **שיפור מ-07-09**, אך עדיין לא-דטרמיניסטי.

**הצלבה-נדרשת (CC):** shadow/live hit-detection-path (`fill_poller.py`/shadow-sim-engine): (א) האם ההשוואה ל-t1 היא `bar.low<=target` (live) מול `bar.low<target` (shadow), או timing-snapshot-שונה? (ב) למה `exit_price=null` על 4-ה-T3_HIT (הרי היה fill-אמת @7573). לצלב לוג 11:50–12:01 CT id365/366 + 11:25 CT id360/361/362/364.

**שינוי מוצע:**
1. **hit-detection אחיד + מדויק:** shadow ו-live משתמשים ב-**אותו** predicate (`bar.low<=t` ל-SHORT-target / `bar.high>=t` ל-LONG-target), על אותו-snapshot-ברים. target-hit נספר רק אם נגע-בפועל (anti-phantom: `mfe_pts` תואם).
2. **exit_price חובה:** סגירה עם `exit_price=null` אסורה — כל exit (כולל T*_HIT) חייב מחיר-אמת (מחיר-ה-target שנגע / bar-fill). anti-regression מול 4-ה-null של-היום.
3. **signal_key משותף:** shadow+live של אותה-כניסה חולקים `signal_key` — כך ש-EOD משווה ישירות (במקום ניחוש-לפי-זמן), ו-hit/exit נבדקים לזהות.
4. **תלוי-§1:** ה-R-inflation של 366 נובע מ-phantom-H1+trail; §1 (מכנה-stop_initial) מנטרל את-הנזק-ה-R גם-אם ה-phantom-H1 נשאר; §3 מנקה את-השורש.

**Regression שיוכיח:** `test_shadow_live_hit_parity` — סיגנל-אחד מזרים shadow+live → hit-flags זהים (anti-regression מול 365/366). `test_no_phantom_hit` — t1 7563.5 עם שפל-בר 7564 → `t1_hit=false` (לא-נגע). `test_exit_price_never_null_on_close` — כל CLOSED חייב `exit_price!=null`.

**עדיפות:** 🔴 — ה-shadow הוא ה-oracle לפני-LIVE; hit-nondeterminism + null-exit-price מערערים-אמון. **מתון מ-07-09** (בלי היפוך-סימן). **3-החמורים · #3 (מאחד §1-נזק).**

---

## §4 · I-23 (השתפר) — gateway מונה חי; לקבע החרגת-drill + פילוח live/shadow · **[DISPLAY/SAFE]**

**שורש (נצפה):** בניגוד ל-07-09 (הכל 0), היום `daily_pnl=92.5` = **בדיוק** סכום 3-ה-live (361+366+368), `trades_today=3`, `shadow_active_count=7` = 7-ה-shadow. ⇒ המונים **מחווטים**. שארית: לוודא (א) `daily_pnl` = live-בלבד-במכוון, (ב) ה-drill (359) מוחרג, (ג) פילוח live/shadow מפורש.

**שינוי מוצע:**
1. **תיעוד-כוונה:** לקבע ב-`FLAG_INDEX.md`/`SOURCE_OF_TRUTH.md`: "`gateway.daily_pnl/trades_today` מונים **live-בלבד**; shadow ב-`shadow_active_count`". (אם מכוון — display-only; אם shadow אמור-להיספר-ב-pnl → [TRADING-LOGIC].)
2. **החרגת-drill:** `pattern!=SIM_TEST && is_synthetic!=true` בכל מונה (תלוי-§5).
3. **פילוח:** לחשוף `daily_pnl_live` + `daily_pnl_shadow` בנפרד ל-observability.

**Regression:** `test_gateway_counts_live_only` — הזרקת 3 live + 7 shadow + 1 drill → `daily_pnl=Σlive`, `shadow_active_count=7`, drill מוחרג. Anti-regression מול היום (92.5/3/7).

**עדיפות:** 🟢 — safe, שיפור-מ-07-09; לקבע לפני שה-counters נסמכים ל-LIVE.

---

## §5 · I-66 (נמשך) — דריל `SIM_TEST` `is_synthetic=false` · **[DISPLAY/SAFE]**

**שורש (נצפה):** id359 (00:11 CT טרום-RTH, `pattern=SIM_TEST`, entry 7583.5, `exit_reason=SIM_PROOF_0713_manual_close_re`) — דריל, אך **`is_synthetic=false`** (לא-מסומן). מזהם `/trades/recent`. היום דריל-אחד (מ-10 ב-07-09), אך הדגל-`is_synthetic` **עדיין-לא-נאכף**.

**הצלבה (CC):** מדוע `is_synthetic=false` על דריל-מפורש (`SIM_TEST`/`SIM_PROOF_*`)? היכן נכתב הדריל, ולמה לא-סומן.

**שינוי מוצע:**
1. **סימון-במקור:** כל דריל (`pattern∈{SIM_TEST}` או `exit_reason` matches `sim_*`/`drill_*`/`SIM_PROOF_*`/`*_cleanup`) נכתב עם `is_synthetic=true`.
2. **סינון:** כל endpoint/counter/סוכן-EOD מסנן `is_synthetic=true` כברירת-מחדל (`include_synthetic=false`).

**Regression:** `test_drills_flagged_and_excluded` — הזרקת SIM_TEST/SIM_PROOF → `is_synthetic=true` + לא-נספר במונים/EOD; `include_synthetic=true` חושף.

**עדיפות:** 🟡 — safe, חשוב לפני שה-counters נסמכים pre-LIVE.

---

## §6 · I-54 (הוקל) — אכיפת-כיוון+מונוטוניות עדיין-נדרשת (GHOST חשף) · **[TRADING-LOGIC] · [SoT→CC]**

**שורש (נצפה):** ה-collapse-מתחת-לכניסה של 07-09 (`7566.5×3`) **לא-חזר** — השורטים היום קיבלו targets יורדים-תקינים. **אך GHOST (367/368) `t2=7610.5 > t3=7603.5`** (הפרת-מונוטוניות ל-LONG) ⇒ הבאג-המבני **חבוי, מתבטא-סלקטיבית** בתבנית-חדשה. השיפור על REACTIVE/ZLR **לא-פותר** את-השורש הגלובלי.

**שינוי מוצע (`.../target_calc`, שכבה-אחידה מעל-כל-התבניות):**
1. **אכיפת-כיוון:** LONG → כל target>entry; SHORT → כל target<entry. הפרה → fallback ל-R-multiple (T1=+1R…T3=+3R).
2. **אכיפת-מונוטוניות:** LONG `entry<t1<t2<t3`; SHORT `entry>t1>t2>t3`. הפרה (כמו GHOST t2>t3) → clamp/reorder/fallback.
3. **fail-loud:** target שמפר כיוון/סדר → `logger.warning` + `target_invalid=true` (לא-שקט).
4. **חל על GHOST גם:** המנוע-האחיד עוטף **כל** מקור-target, כולל תבניות-חדשות.

**Regression שיוכיח:** `test_targets_direction_monotonic_all_patterns` — fixtures לכל תבנית (כולל GHOST-LONG entry 7575.5): כל target>entry, `t1<t2<t3`, אין `t2>t3`. Anti-regression מפורש מול GHOST-07-13 (`t2>t3`) + 07-09 (`7566.5×3`) + 07-08 (7564.5) + 07-06 (7558).

**עדיפות:** 🔴 — הבאג-המבני לא-נעלם (רק-סלקטיבי); אכיפה-אחידה חוסמת-הישנות. תלוי-§2 (GHOST-אפיון).

---

## §7 · נמשכים (עיצומים-עומדים, ללא-שינוי) — I-44 · I-62 · chop-enum · I-41/I-50-inv · bars_in_db · limit

- **🔴 I-44 (day_type wrapper-קפוא) — [DISPLAY/SAFE · SoT→CC]:** `session_min=0/vote_history=[]`. `ib_width=EXTREME` השתנה מ-WIDE (07-09) ⇒ לא-קפוא-בין-ימים. עיצוב עומד מ-`CC_DAYTYPE_SOURCE_CONSISTENCY_2026-06-30.md` (איחוד-endpoint למקור-החי + דגל-`stale`). `test_daytype_endpoint_live_source`.
- **🟡 I-62 (demo-off/live-active) — [TRADING-LOGIC · SoT→CC]:** `demo=[]`+`live=[2,4]` ירתה 3-live (+$92.5). מותנה: אם מכוון → תיעוד ב-FLAG_INDEX (display); אם שוגג → החזרת-demo (Michael). `test_enabled_systems_matches_intended`.
- **🟡 chop_state="FOUND" (יום-3) — [DISPLAY/SAFE · SoT→CC]:** enum-לא-מוכר, 3-ימים-ברציפות. אם באג-מיפוי → תיקון; אם חדש → תיעוד. `test_chop_state_enum_valid`.
- **🟡 I-41/I-50-inv (לונג-נגד-מגמה, n=3) — [SoT→CC]:** 363/367/368 לונגים נגד-ירידה, כולם −1R-מוצדק. מראה-מהופך של הטיית-השורט. watch — האם שער-המשפחה אמור-לסנן כיוון-נגד-מגמה. `test_family_gate_counter_trend` (watch, לא-מימוש עדיין).
- **🟡 bars_in_db 1549→1779 (watch) — [SoT→CC]:** עלייה, עדיין מתחת-3206. לצלב hydration/restart.
- **🟢 limit — תחזוקת-מפרט:** `/chart/bars5min?limit=200` התקבל; `/trades/recent` נותר `le:100`. מפרט-סוכן: limit=100 ל-trades.

---

## סיכום-עדיפויות

| # | פריט | class | דחיפות | חוסם-מימוש |
|---|------|-------|--------|-----------|
| §1 | I-22 `pnl_r` מנופח/הפוך (364 +33R · 366 +31R על-הפסד) | TRADING/DATA | 🔴🔴 | SoT (נוסחת-R) + אישור-Michael |
| §2 | I-67 GHOST — תבנית-S4-לא-מתועדת, נגד-מגמה, t2>t3 | TRADING | 🟠 | SoT (מקור-GHOST) + אישור-Michael |
| §3 | I-65/I-64 shadow↔live hit-divergence + xp=null (365/366) | TRADING/DATA | 🔴 | SoT (hit-path) |
| §4 | I-23 gateway-מונה-חי (השתפר) | DISPLAY/SAFE | 🟢 | — (safe) |
| §5 | I-66 drill `is_synthetic=false` | DISPLAY/SAFE | 🟡 | — (safe) |
| §6 | I-54 אכיפת-כיוון/מונוטוניות (GHOST חשף) | TRADING | 🔴 | תלוי-§2 + אישור-Michael |
| §7 | I-44 · I-62 · chop-enum · לונג-נגד-מגמה · bars · limit | DISPLAY/SAFE→TRADING | 🔴…🟢 | handoff/SoT |

**3 החמורים (לנוטיפיקציית-Michael):** §1 I-22 (R מנופח ×8.4 — +68R-רשום מול +8R-אמת; 366 +31R על הפסד — **חוסם כל מדד-R + LIVE-sizing**) · §2 I-67 GHOST (תבנית-S4-לא-מתועדת יורה נגד-מגמה עם targets-שבורים) · §3 I-65/I-64 (shadow↔live hit-divergence על 365/366 + exit_price=null). **דגש: המערכת עצמה עבדה היום (כיוון-נכון, +$297.5) — הבעיות הן חשבונאות-R + תבנית-חדשה + shadow-determinism, לא הפסדי-מסחר-אמיתיים.**
**מיָדית-safe (ללא-אישור-trading):** §4 (gateway-מונה) · §5 (drill-flag) · §7-display (chop-enum/I-44/limit). **§1/§2/§3/§6 דורשים SoT/אישור-Michael.**

**מקור-אמת → CC (Rule 2/5):** נוסחת-`pnl_r` (stop-נגרר מול stop_initial) · מקור-`GHOST` בקוד + target-calc-שלה · shadow/live hit-predicate + exit_price=null · demo/live promotion (git/.env/FLAG) · chop-enum · is_synthetic ל-drill. **שום קוד/flag/.env/DB לא-שונה בריצה זו.**
