# מערכת 6 — דוח סוף-יום · 2026-07-22

> ⛔ **הדוח לא הופק אוטומטית — ה-EOD חסום.** ה-backend קפוא (`http=000`) כי ה-DB נעול.
> `s6_eod_report.py` קורא הכל דרך `http://localhost:8000`, ולכן ריצה אוטומטית הייתה
> מייצרת דוח-שקר: "עסקאות היום: 0 · אין ממצאים חריגים". נכתב במקומו דוח-אמת ידני
> (כלל Pre-LIVE: כשל-כן > ערך-סינתטי; Rule 5: ראיה, לא הצהרה).

## מצב (23:07 IDT / 20:07Z)

- `GET /health` → **`http=000`** אחרי 6.0ש (timeout). `GET /api/v9/trades/recent?limit=5` → זהה.
- לכן **אי-אפשר לחשב היום שיפוטי סטופ-חכם / מימוש-חכם / wick-out** — אין נתוני-עסקאות זמינים.
- אין לפרש כ"אפס עסקאות" או "אין ממצאים" — זו **השבתה**, לא יום-שקט.

## שורש — שרשרת-נעילה ב-Postgres (ראיה: `pg_stat_activity`)

| PID | מצב | גיל-טרנזקציה | שאילתה |
|-----|-----|--------------|---------|
| **28917** | `idle in transaction` | **1h19m** (idle 59דק') | `SELECT ... FROM v9_trades` — פתוח, לא-committed |
| 42496 + 5 נוספים | `active · Lock` | ~59דק' | `ALTER TABLE v9_trades ADD COLUMN pnl_sierra` (migration 023, ×6 מוערמים) |
| 14 שאילתות | `active · Lock` | 16–59דק' | reconciler / feeder / backend readers — כולן ממתינות בתור |

- **28917** מחזיק `AccessShareLock` על `v9_trades` (SELECT פתוח שלא נסגר).
- ה-`ALTER` דורש `AccessExclusiveLock` → מתנגש עם 28917 → נתקע בראש-התור.
- ALTER-תקוע-בראש-התור **חוסם כל קורא חדש** (סמנטיקת-תור של PG) → כל ה-DB קפוא.
- אומת: `SELECT count(*) FROM v9_trades WHERE entry_ts::date=CURRENT_DATE` → `statement timeout` ב-5ש.
- זהה ל-`project_0722_dblock_backend_wedge` — נותב ל-CC ב-19:35Z, **עדיין פעיל** ב-20:07Z (אותו PID 28917).

## כסף — בטוח 🟢

לפי ה-LIVE channel: Sierra **-3 @ 7558.58 ממוגן מלא** (stop 7549.5 qty3 + target 7504 qty3,
אפס-חוזה-עירום). ה-bracket אוטונומי בצד-סיירה — **בלתי-תלוי** ב-backend הקפוא. ההשבתה היא
תפעולית (עיוורון-ניטור/reconcile), לא סיכון-כסף מיידי על הפוזיציה הפתוחה.

## מה נדרש (פעולת-שחזור — לא בוצעה כאן; מחוץ להיקף דוח-EOD, בעלים=CC)

1. `SELECT pg_terminate_backend(28917);` — לשחרר את ה-idle-in-transaction (restart-לבד לא יעזור).
2. **לעצור את לולאת-הריטרייז של migration 023** — היא מערימה `ALTER` חדש כל כמה דקות → נועלת מחדש.
3. אחרי שחרור: לאמת `/health`=200 · reconcile ל--3 · restart-feeder (PID625 קפוא מ-14:47Z).
4. ⚪ terminate של backend חי = שינוי-משטח-סיכון → פסיקת-מייקל אם CC לא פינה עד הבוקר.

## חוב-כלי פתוח (לא-חדש — DEV_BACKLOG #17)

`mfe_mae` ב-`s6_eod_report.py` עיוור: משווה מחרוזות-זמן בפורמט לא-תואם (bars "רווח" מול
`entry_ts` "T") → `after` ריק → MFE=None לכל העסקאות → **עמודות מימוש-חכם + wick-out ריקות גם
כשה-backend חי**. עד שזה יתוקן, ה-EOD לא ייתן שיפוט-מימוש/wick-out אמין.

## המלצות תיקון (מערכת 6 → מייקל / CC)

- 🔴 **קריטי (מחר):** migration 023 `ALTER pnl_sierra` מערים מאחורי idle-in-txn ונועל את כל
  ה-DB (הפרה חוזרת 07-22). → להפוך אידמפוטנטי (בדוק `information_schema.columns` לפני ALTER) +
  לעטוף ב-advisory-lock + להגדיר `idle_in_transaction_session_timeout` שיהרוג txn דולף.
- לסגור #17 (נרמול-זמן ב-`mfe_mae`) לפני שסומכים על שיפוט-מימוש/wick-out של ה-EOD.

_דוח-אמת ידני — נכתב ע"י cowork-dev (EOD מתוזמן) כי הריצה האוטומטית הייתה מייצרת false-empty.
פסיקות: מייקל בלבד._
