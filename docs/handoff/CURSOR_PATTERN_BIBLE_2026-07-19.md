# משימת-קורסור: ספר-התבניות — כל תבנית × כל סוג-יום, מהקוד

**פסיקת-מייקל 2026-07-19:** *"אני רוצה שקורסור יבדוק את כל התבניות ואת ההתנהגות שלהם בקוד עם כל סוג
יום. לעבור אחת-אחת ולראות את הגאומטריה שלה. לראות שאין מחסומים לאף אחת ואיך המימושים עובדים.
לבדוק למה ווּדיס תקוע ולמה תמיד מערכת-2 יורה בשלב מאוחר יחסית."*

**מבצע: cursor-agent (ניתוח-קוד בלבד — לא משנה קוד, לא מריץ מסחר).** מאמת: cowork-dev.
**תוצר: `docs/handoff/PATTERN_BIBLE_2026-07-19.md`** — מסמך אחד שמייקל עובר בו תבנית-תבנית.

## החוק החשוב ביותר
**כל שורה במסמך חייבת להיות מהקוד עם `file:line`.** אפס דוקטרינה-מהזיכרון, אפס "בערך".
אם משהו לא מוכרע מהקוד — כתוב **"לא-מוכרע מהקוד"** ומה חסר. זו ההוראה שהכי חשובה לי.

---

## חלק א' — כרטיס לכל תבנית (15 תבניות, אחת-אחת)

**התבניות:** S4/ווּדיס — `ZLR · TLB · TT · GB100 · HTLB · VEGAS · GHOST · FAMIR · HFE(מושבת)`
· S2/חמש-דקות — `REACTIVE · INITIATIVE · HNS · DBDT · FLAGS · CONFLUENCE_RI_ZLR`

לכל אחת, **כרטיס בפורמט אחיד**:

1. **גאומטריה** — מה הצורה בפועל, מהקוד: איזה תנאי-CCI/מחיר/נפח, ספי-מספרים מדויקים, וכמה נרות
   (חלון-סריקה + מינימום). מקור: `backend/v9/systems/woodies/patterns/*.py` ·
   `backend/v9/systems/five_min/five_min_system.py` + `five_min/patterns/*.py`.
   ⚠️ שים לב לדגלי-SPEC_V2 — יש תבניות שרצות בגרסה אחרת ממה שכתוב בתיעוד (ל-VEGAS זה כבר קרה).
2. **טריגר-הכניסה** — מה בדיוק הבר שמפעיל, ומתי בתוך התנועה זה קורה (התחלה/אמצע/סוף).
3. **טבלת סוגי-יום** — 8 עמודות (Trend_Normal · Trend_DD · Variation · Normal · Neutral_Center ·
   Neutral_Extreme · Nontrend · Nonconviction): מה הפסק (FULL/REDUCED/SKIP) מ-`config/daytype_playbook.yaml`,
   וכמה חוזים בפועל.
4. **שרשרת-המחסומים** — **כל** שער שיכול לחסום את התבנית הזו, בסדר-הריצה, עם `file:line` ומה התנאי.
   מקור: `backend/v9/gateway/trading_gateway.py` (~27 שערים) + `anti_patterns.py` +
   הדחיות-השקטות ב-`calculate_size`/`emit_t1_setup`. **סמן במיוחד שער שיכול לחסום בשקט**
   (לפני `route_setup` → לא מופיע בפיד-ההחלטות).
5. **מימוש (ניהול-העסקה)** — סטופ (עוגן + רצפה/תקרה), יעדים (סולם T0-T3), BE, ראנר, time-stop.
   מקור: `config/stop_anchors.yaml` · `config/targets.yaml` · `structural_targets.py` ·
   `stop_resolver.py` · `daytype_playbook.yaml` (`daytype_style`). **סמן היכן זה שונה פר-סוג-יום.**
6. **🔴 דגלי-אזהרה** — סתירות בין הקוד לדוקטרינה, ספים שנראים לא-סבירים, מחסום שחוסם את התבנית
   בסוג-יום שבו הפלייבוק אומר FULL.

---

## חלק ב' — שתי החקירות שמייקל ביקש במפורש

### B1 · "למה ווּדיס תקוע"
היפותזה מהראיות (07-17): **צבע-הטרנד מפגר אחרי ה-CCI בכ-6 ברים.** ב-16:45→17:05 ה-CCI זינק
‎-36→+182 (ראלי 7473→7534) בעוד הצבע נשאר GRAY עד 17:15 — כשהצבע הכחיל, התנועה נגמרה.
**קבע מהקוד:**
- מי קובע את `trend_state` ובאיזו נוסחה — DLL (`MES_AI_DataExport.cpp`) מול פייתון
  (`a1_strategic_gate.py`, `BARS_PERSISTENCE_REQUIRED=6`) מול `TREND_CCI_DIRECT_V1` (`bars.py`).
  **מי מנצח בפועל?** (יש 3 מנגנונים — צריך להכריע מי גובר.)
- **כמה ברים לוקח לצבע להכחיל/להאדים** מרגע שה-CCI חוצה — חשב מהנוסחה, אל תנחש.
- **אילו תבניות נעולות על הצבע** ולא על ה-CCI (‎GB100/TT דורשות BLUE/RED מפורש) — רשימה מלאה.
- האם `TREND_CCI_DIRECT_V1=1` (סף 50) שהודלק ב-07-17 **באמת** פתר את הפיגור, או שנשארו נתיבים
  שקוראים את הצבע הישן? **חפש כל קריאה ל-`trend_state` ובדוק מאיזה מקור.**

### B2 · "למה S2 תמיד יורה מאוחר"
היפותזה: **REACTIVE/INITIATIVE דורשים רצף של 4 ברים (B1→B4) + 3 ברי-רקע = 7 ברים = 35 דקות**,
ובר-האישור B4 מגיע רק אחרי שהתנועה כבר יצאה לדרך. **קבע מהקוד:**
- כמה ברים **מינימום** מהאיתות-הראשון ועד הירי, לכל תבנית-S2 (`five_min_system.py` +
  `first_hour_buffer.py`). כתוב את זה **בדקות**.
- **איפה בתוך התנועה** יושבת נקודת-הכניסה — אחרי כמה נקודות מהקצה בממוצע? השתמש ב-
  `scripts/audit_pattern_miss.py` (מכסה כבר 11 תבניות) על 15/16/17-07 והבא **מספרים**.
- **שער השעה-הראשונה** (FHB): מתי בדיוק S2 משתחרר, ומה זה עלה לנו ב-07-17.
- **תנאי `b2_volume_drop`**: מול איזה ממוצע-נפח הוא נמדד? יש חשד שהממוצע מורעל מברי-נפח-ענקיים
  (פתיחה/ספייקים) ולכן "בר-שקט" כמעט בלתי-אפשרי ביום פעיל. **אמת או הפרך מהקוד.**
- **השוואה:** כמה מהר S4 יורה על אותה תנועה מול S2 — כדי לכמת את "המאוחר יחסית".

---

## חלק ג' — מטריצת-סיכום (עמוד אחד)
טבלה 15 תבניות × 8 סוגי-יום. בכל תא: הפסק (FULL/REDUCED/SKIP) + **הסימון 🚫 אם קיים שער
שחוסם את התבנית למרות שהפסק אינו SKIP** — זה הלב של "לראות שאין מחסומים".
מתחת: רשימת **כל הסתירות** שמצאת, ממוינת לפי כמה עסקאות היא עלולה לעלות.

## מקורות (קרא, אל תנחש)
`config/daytype_playbook.yaml` · `config/stop_anchors.yaml` · `config/targets.yaml` ·
`backend/v9/systems/woodies/patterns/*.py` · `anti_patterns.py` · `a1_strategic_gate.py` ·
`backend/v9/systems/five_min/five_min_system.py` + `patterns/*.py` · `first_hour_buffer.py` ·
`backend/v9/gateway/trading_gateway.py` · `stop_resolver.py` · `structural_targets.py` ·
`scripts/sim_matrix.py` · `scripts/audit_pattern_miss.py` ·
`docs/handoff/PATTERN_MGMT_AUDIT_2026-07-17.md` · `PATTERN_MISS_AUDIT_2026-07-17.md` ·
`frontend/.../planHelp.ts` (יש שם כבר גאומטריה מצוטטת עם file:line — **הצלב מולה, אל תסתמך עליה**).

## מה אסור
❌ לשנות קוד · ❌ להריץ מסחר/סים · ❌ לגעת ב-.env/RULED · ❌ לכתוב דוקטרינה בלי `file:line`.
✅ מותר ומומלץ: להריץ `sim_matrix.py` ו-`audit_pattern_miss.py` (קריאה-בלבד) לאיסוף מספרים.

## תוצר
`PATTERN_BIBLE_2026-07-19.md` + שורת-LOG ב-`LIVE_CHANNEL.md`. עברית, כרטיס לתבנית, ומטריצה אחת.
בסוף: **"מה לא הצלחתי להכריע מהקוד"** — סעיף חובה.
