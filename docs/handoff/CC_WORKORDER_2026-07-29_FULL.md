# CC — הזמנת-עבודה מלאה 29.07 (פסיקת-מייקל: "אני רוצה עסקאות")

פעל לפי `CC_HANDOFF_CONTRACT.md` (חוק-5 · NOT-DONE חובה · anti-tautological).
הכל flag-gated OFF אלא אם צוין; cowork מאמת. **סדר-הביצוע מחייב.**

## הרקע במשפט אחד

אתמול היו שתי עסקאות גדולות — **שורט-פתיחה** על הצניחה החדה, ואחריו **לונג עד סוף
היום** אחרי הדחייה מ-7417. המערכת לא איתתה אף אחת מהן: השערים "צדקו" כשחסמו
איתותי-פייד גרועים באמצע — אבל **העסקאות הנכונות מעולם לא נוצרו**. הפתרון שמייקל
פסק: **תבנית-פתיחה מוגדרת לכל סוג-פתיחה** + רכיבת-ראנר עד סוף היום.

## P0 — עוגן-הפתיחה (cowork תיקן היום 15:15; עליך לאמת + לחזק)

השורש שנמצא: ‏`_is_open_bar` השווה `hour==16:30` על ts שפורש **בפורמט-תלוי-מזל**
(‏epoch→IL, ‏ISO-נאיבי→UTC). תוצאה: ‏replay-בוט ירה פנטום-DRIVE ב-07:38 והפתיחה
האמיתית מעולם לא זוהתה. התיקון (commit של היום, `test_opening_anchor_tz.py`) מנרמל
הכל ל-IL-דרך-UTC. **המשימה שלך:**
1. ‏replay מלא של 07-28 עם התיקון: חייב להפיק `OPEN_DRIVE SHORT` בפתיחה (צניחה
   7449→7417) ואחריו `ORR/EXTREME_REJECT LONG` על החזרה דרך הפתיחה. הדבק פלט גולמי.
2. העברת העוגן מ-16:30-IL ל-**09:30-ET** (חוק-4 — ‏5 שבועות-DST בשנה העוגן הנוכחי
   שגוי). דגל `OPENING_ANCHOR_ET_V1`, טסטים לשני צדי-ה-DST.
3. שריון נגד-פנטום: איתות-פתיחה נוצר **רק** כשגיל-הבר < ‏10 דק' מזמן-אמת
   (‏replay/hydration לא מייצרים איתותים). טסט: הזרמת ברי-אתמול לא מפיקה כלום.

## P1 — תבנית-פתיחה לכל סוג-פתיחה (`config/opening_playbook.yaml`)

מיפוי מחייב, קובץ-קונפיג + מנוע דק שקורא אותו (לא לוגיקה מפוזרת). לכל סוג:
כניסה · סטופ · ‏T1 · ראנר · ביטול. בסיס — הטריגרים הקיימים ב-`opening_entry.py`;
מה שחסר מסומן ⭐:

| סוג-פתיחה | העסקה | כניסה | סטופ | ביטול |
|---|---|---|---|---|
| ‏OPEN_DRIVE | עם-הנסיעה, מיד | סגירת-בר מעבר לקצה-טווח-הפתיחה | הצד השני של טווח-הפתיחה | חזרה-דרך-הפתיחה (⭐ סטופ-ביטול פעיל, לא רק אי-כניסה) |
| ‏OPEN_TEST_DRIVE | עם-הנסיעה אחרי הבדיקה | סגירה חוזרת מעל/מתחת לפתיחה אחרי בדיקת-קצה | קצה-הבדיקה ±חוצץ | בדיקה שנייה שנכשלת |
| ‏ORR (דחייה-והיפוך) | **העסקה של אתמול** — היפוך אחרי דחיית-קיצון | סגירה חוזרת דרך הפתיחה נגד-הנסיעה | קיצון-הדחייה ±חוצץ | סגירה חוזרת מעבר לקיצון |
| ‏AUCTION_OUT | הרחבת-טווח עם קבלה | ‏RE ראשון עם קבלה (מערכת-0 acceptance) | קצה-הערך הנגדי | חזרה לתוך ערך-אתמול |
| ‏AUCTION_IN | פייד-קצוות בלבד | קצה-ערך + probe (‏location_gate הקיים) | מעבר-לקצה | קבלה מחוץ לערך |

**פטור-שערים לעסקות-פתיחה:** הן פטורות מ-`awaiting_release` ו-`lsma_flat`
(הן-עצמן ההקשר; אין "אזור-הידבקות" בדקה 3 של היום) — **נשארות** תחת margin,
‏bracket-slots, ‏chase, ‏news. פטור = שדה בקונפיג, לא hardcode.

## P2 — רכיבת-ראנר ("לונג עד הסוף")

אחרי T1 של עסקת-פתיחה: הראנר לא נסגר ב-T3 קבוע אלא נגרר לפי המבנה —
‏trail על שפל/שיא-30-דק' (עם-הרגל), ‏BE אחרי T1 (קיים), יציאה ב-15:45-ET ביום-טרנד
(‏C4 הקיים) או בחציית-LSMA נגדית. דגל `OPENING_RUNNER_RIDE_V1`. טסט-replay על
07-28: ‏ORR-לונג מ-~7433 חייב להחזיק עד אזור 7470+ (ולא להיסגר ב-T2).

## P3 — שומר-תפרים בקליטה (`BAR_SEAM_REJECT_V1`)

כמפורט ב-`CC_SYSTEM0_MARKET_CONTEXT_2026-07-29.md` §D3 — ברי-07-28 נכתבו-מחדש
בלילה (תפר 31.5 נק'). בלי זה, ‏replay-האימות של P0-P2 רץ על נתונים חשודים: השתמש
בברים עד 18:10 של 07-28 (אומתו מול לייב באותו ערב) לכל אימותי-היום.

## P4 — מערכת-0 (הספק המלא שכבר אצלך)

‏`CC_SYSTEM0_MARKET_CONTEXT_2026-07-29.md` — ‏Phases A-E כולל D1/D2 (‏TEST_DRIVE
שלא זוהה, ‏DD שלא סווג, ‏Trend-מוקדם). ‏P1 כאן תלוי ב-acceptance של Phase B4.

## P5 — מהתור הקיים (TASKS_2026-07-29_PREOPEN.md)

היסטרזיס-רגל · ‏S6 כיסוי-בראקט · ‏S6 ביקורת-שערים-EOD · סטופ-מבני-ליומן.

## קבלה כוללת (חוק-5)

רגרסיה ירוקה · ‏flag_guard · ‏replay-07-28 מפיק את שתי העסקאות עם הפלט הגולמי ·
‏FLAG_REGISTRY+gen_flag_index · דוח `docs/reports/CC_OPENING_PLAYBOOK_REPORT.md`.
**אסור:** ‏op=EXIT · דגלים-פסוקים · הדלקה-עצמית · נגיעה ב-reconciler.
