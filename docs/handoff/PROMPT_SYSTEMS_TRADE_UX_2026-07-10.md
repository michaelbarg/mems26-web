# צ'אט-מערכות — שקיפות-עסקה מלאה בדשבורד (פסיקות מייקל 07-10 ערב)

**פרונטאנד בלבד** (+2 endpoints קטנים ל-CC, מסומנים). אימות-דפדפן חובה (screenshot+DOM).
רקע: מייקל הסתכל ברשימת "אחרונות" וחשב שרק S2 יורה — כי אין תג-מערכת. בפועל 344/350
(ואחרות) הן S4/ZLR. וגם: ‏shadow-twins מוצגות כמו עסקאות אמיתיות ומבלבלות.

## 1. תג-מערכת בכל שורה וכרטיס (מיידי, הכי חשוב)
בכל שורת-עסקה וכרטיס-פעילה: תג **S2/S4** (צבע שונה) ליד התבנית. מקור: ‏firing_system
שכבר קיים ברשומה. ‏DoD: מייקל רואה במבט מי ירה.

## 2. הפרדת צל מאמת ברשימה
‏SHDW ↔ ‏LIVE/SIM: או קיבוץ (שורת-צל מוסתרת תחת toggle "הצג צל"), או עמעום חזותי חזק
לצל. ברירת-מחדל: לא מציגים צל ברשימת "אחרונות" (יש לשונית Shadow ייעודית).

## 3. כל שורה לחיצה → חלון פירוט-עסקה (פסיקת מייקל: "שכל אחד יהיה לחיץ")
‏drawer/מודאל עם ציר-זמן מלא של העסקה: כניסה (שעה+מחיר+fill), כל T-hit, **כל תזוזת-סטופ
עם הסיבה** (קיים ב-cross_context audit: event=stop_move, from, to, reason), חסימות/אירועים,
יציאה+P&L פר-רגל. כפתור "פתח ב-TradeReview" (deep-link לטאב הקיים). מקור: הרשומה +
‏management-log (אם צריך endpoint מרוכז — ‏CC מוסיף GET /api/v9/trades/{id}/timeline).

## 4. מערכת 6 על הכרטיס (פסיקת מייקל: "שמערכת 6 תהיה שם ורואים מה היא חושבת")
על כרטיס העסקה הפעילה: בלוק "מערכת 6" חי — 9 האינווריאנטים (diagnose_trade) עם ✓/✗
פר-בדיקה + שורת-שיפוט אחרונה, מתעדכן כל 15ש'. **צריך endpoint מ-CC:**
‏GET /api/v9/s6/diagnose/{trade_id} שמריץ diagnose_trade() על העסקה ומחזיר את הרשימה.
‏+ קישור "יומן הסוכן" → ‏/board#system6 (הכניסה של הסוכן שמייקל ביקש — נגישה מהכרטיס).

## 5. תג-הקשר על הכרטיס
ה-"UNKN" שמופיע היום = ‏day_type בעת-הירי — להציג ברור: "יום: Variation (0.18)" ולא
צ'יפ סתום. אם UNKNOWN בעת-ירי — להציג "סיווג בהתהוות".

‏DoD כולל: צילומי לפני/אחרי + קליק-דרך מוקלט על שורה→פירוט→TradeReview.

## 6. 🆕 חיווי "הסוכן חי" ב-TopBar (פסיקת מייקל 07-11)
המפקח-החי (session-watch) כותב בכל ריצה `docs/reports/AGENT_HEARTBEAT.json`:
{ts, verdict:"🟢|🟡|🔴", note, checks}. הצג ב-TopBar נקודה ירוקה/צהובה/אדומה + tooltip
(note + "לפני X דק'"). ts ישן מ->40 דק' בשעות-מסחר ⇒ אפור "הסוכן לא נבדק" (אמת, לא המצאה).
קריאה דרך endpoint סטטי קיים או route קטן שמגיש את הקובץ.

## 7. 🆕 עסקת LIVE על הגרף (פסיקת מייקל 07-11)
**Audit קודם (KEEP/ADAPT):** קיים `chart/TradeMarkerOverlay.tsx` — בדוק מה הוא כבר מצייר
ולאילו מודים, אל תבנה כפול. הדרישה: עסקה חיה (live/sim) מוצגת על גרף-המחיר הראשי:
- קו-כניסה (ירוק, מקווקו) + חץ-כיוון ▲▼ בנקודת-המילוי האמיתית (fill, לא ההוראה).
- קו-סטופ (אדום) **שזז בזמן-אמת** עם כל MODIFY (זה העיקר — FIX-12/15 מזיזים אותו עכשיו
  כל בר; המקור: trades/active שכבר נסקר כל 5ש').
- קווי T1/T2/T3 (כחולים; מומש = מלא + תג ✓ במחיר-המילוי).
- באנר-מיני על הגרף: "‏#ID תבנית · P&L חי · חוזים".
- עסקת-צל לא מוצגת על הגרף (רק live/sim/demo) — הגרף הוא מציאות.
DoD: screenshot עם עסקה חיה שרואים בה כניסה+סטופ+יעדים, ו-gif של תזוזת-סטופ.
