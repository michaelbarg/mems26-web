# שורה-תחתונה 30.08 · מה cc חייב לבצע · **סריקת-קוד, לא זיכרון**

כל שורה כאן אומתה ב-`grep` על קוד-ייצור (בלי טסטים), על `.env` ועל ה-DB. תאריך: 30.08.

## 🔴 החוסם שמבטל את יום-הצל של מחר — לתקן ראשון

**אין ולו דגל אחד מהעבודה החדשה ב-`.env`.** כולם נופלים ל-default `"0"` = **כבוי לגמרי**,
לא "צל". ⇒ **מחר, יום-הצל היחיד, לא יירשם ולו אירוע אחד מהחדש.**

**ובנוסף — אין תשתית-מדידה בכלל.** אין טבלת-לִיגר מאוחדת (`v9_shadow_ledger` **לא קיימת**).
יש רק שלוש טבלאות צרות פר-פיצ'ר:
```
v9_s7_shadow_log                148 שורות (73 מהשבוע)
v9_tsf_shadow_log               148 שורות (73 מהשבוע)
v9_day_type_shadow_transitions   50 שורות (**0** מהשבוע)
```
⇒ **לוח-הקידום שנדרש לשער-הערב אין לו ממה לקרוא.** זה השורש של "2 אירועי-צל מאז 26.08" —
לא הגלאים שקטים; **אין להם לאן לכתוב.**

> **המשמעות:** בלי התיקון הזה, מחר בערב השער יראה אפס אירועים — ולפי הפסיקה שלך,
> *"אפס אירועים = תקלת-גלאי, לא סיבה להאריך צל"*. כלומר נבזבז את היום-האחד.

**הנדרש (לפני פתיחת-מחר, 16:30):**
1. **טבלת-לִיגר אחת** לכל דגלי-הצל: ‏`ts · flag · trade_id · pattern · direction · price ·
   decision · outcome · pnl_sim`. כל דגל-צל כותב אליה בכל הפעלה.
2. **להכניס ל-`.env`** את כל דגלי-העבודה-החדשה במצב `=shadow` (רשימה למטה).
3. **`scripts/shadow_promotion_board.py`** קורא ממנה ⇒ `docs/reports/SHADOW_BOARD.md` + תיק + טלפון.
4. **התרעת-צל-ריק** לטלפון בערב אם אירועים=0.
5. **ספירת אירועים-ייחודיים** (תבנית+כיוון בחלון), לא גולמיים — הצל הוא סלוטים-ללא-הגבלה
   ומנפח כל מנגנון שמתקבץ (T-152).

## מפת-המצב המאומתת

### ✅ נבנה **ומחווט** (חסר רק `=shadow` ב-`.env`)
| יכולת | ראיה |
|---|---|
| גלאי תקרה/רצפה | `ceiling_floor_state.py` 15,105B · 5 אזכורים · מחווט ל-`five_min_system` |
| יציאה דרגה-A (כישלון-פריצה) | `bar_level_detector.py:1439` → `should_exit_on_failbreak:1480` |
| יציאה דרגה-B (תקרה-כפולה) | `bar_level_detector.py:1529` → `should_exit_on_double_top:1542` |

### ⚠️ נבנה אבל **יתום — אפס קוראים בייצור**
| יכולת | ראיה |
|---|---|
| **איכות-מיקום** | `entry_location_quality.py` 4,385B — **רק הפניות-עצמיות.** אף אחד לא קורא לו |

*(זה הפריט עם ההחזר הגבוה ביותר לפי תובנות-הצל — 54 סטופים מול 19 יעדים. הוא בנוי ומנותק.)*

### 🔴 **לא נבנה כלל** — 0 אזכורים בקוד-ייצור
| דגל | מה זה |
|---|---|
| `PATTERN_STACK_GUARD_V1` | **שער-הערימה** (T-150) — 61% מהנזק של היום הגרוע |
| `CEILING_LONG_LOCK_V1` | נעילת-קצה — חוסמת קניות-לתוך-התקרה |
| `CEILING_EXIT_V1` | בנק-רווח בכישלון-תקרה |
| `CEILING_FLIP_SHORT_V1` | היפוך-לשורט בבר-האישור |
| `CVD_CANDLE_CONFLUENCE_V1` | קונפלואנס וולום×נרות |
| `STRUCTURE_EXIT_REVERSAL_V1` | יציאה דרגה-C |
| `scripts/shadow_promotion_board.py` | לוח-הקידום עצמו |

### ℹ️ כבר חי — בלי דגל, בלי כיבוי
`_position_reference_price` (מודל-הפוזיציה/סטופ-מהממוצע) **מחווט ופעיל בייצור**
(`manager.py:95` הגדרה · `manager.py:792` + `bar_level_detector.py:1049` קריאות) —
**ללא דגל וללא מתג-כיבוי.** זה תיקון-נכונות נכון, אבל **אין לו killswitch**. לעטוף בדגל
default-ON כדי שיהיה ניתן לכיבוי מהטלפון, ולהוסיף את טסט-השחזור של 28.08
(`sierra pos=-3 avg=7740.83` ⇒ שלושת הסטופים מ-7740.83).

### ✅ תקין
`flag_guard`: **PASS — 219 דגלים · liveness נקי** (כל דגל דלוק בעל אתר-קריאה בייצור).

## הסדר שאני מבקש מ-cc

**‏A · עד 16:30 מחר (בלי זה אין יום-צל):** לִיגר מאוחד → `.env=shadow` לכל החדש →
לוח-קידום → התרעת-צל-ריק → אירועים-ייחודיים.
**‏B · באותו חלון:** לחווט את `entry_location_quality` לגייטוויי (עוגן: 17:00/17:35 עוברות,
18:15/18:25 נפסלות) · לבנות `PATTERN_STACK_GUARD_V1` (עוגן: 24.08 — 17:00 עוברת, 4 נחסמות).
**‏C · היום, לא ממתין לצל:** דריל-סים עם ACK מסיירה ל-`MODIFY_STOP`/`MODIFY_TARGET`/
`FLATTEN_ACCOUNT` ⇒ בקשת-הדלקה לתיקוני-הנכונות. **`op=EXIT` אסור.**
**‏D · בירור:** ‏T-151 — אין שורת-סוג-יום ב-24/25.08; לוודא שרושם מחר.
**‏E · נדחה** (לא לפני שהצל מודד): `CEILING_EXIT` · `CEILING_FLIP_SHORT` · `CVD_CONFLUENCE` ·
דרגה-C · `POSITION_MISMATCH_BLOCK` (דורש בדיקת-בעלות לפני כל הדלקה).
