# S1+S2 · מפת-הפערים המלאה של שרשרת-הירי — "שלא תפוספס עסקה" (2026-08-22)

**מייקל:** "מערכת 1, מערכת 2 צריכות לעבוד ולירות. אני רוצה רשימת פערים לכך שלא תפוספס עסקה."
**נכתב:** cowork-dev/agent · 2026-08-22 · **READ-ONLY** (אפס שינויי קוד/דגל/env, אפס ריסטארטים; DB בקריאה-בלבד).
**HEAD בזמן הכתיבה:** `git log -1` בקומיט של המסמך. כל `file:line` נקרא היום.

> ## 🔴 הפסיקה העומדת שהמסמך הזה אוכף (מייקל, 22.08 — נפסקה שוב, בכעס)
> **זיהוי סוג-היום הוא מבני ובינארי לפי דלטון.** יום **הוא** סוג מסוים או **איננו**
> (או "טרם-נקבע" מוקדם). **אסור שאחוזי-ביטחון ישבו בשום מקום בנתיב-ההחלטה.**
> התיוג-המבני-בדיעבד של מחקר-הפלייבוק (`classifier_core.classify_session`,
> `is_eod=True` — זה שסיווג 54/54 סשנים עקבית) הוא **מימוש-הייחוס**, והוא חייב
> להפוך למסווג-החי. סעיף B בתוכנית-התיקונים הוא המפרט.

**מקורות שאוחדו (לא נגזרו מחדש):** `GOOD_PATTERNS_FIX_2026-08-22` · `CVD_EFFORT_RESULT_2026-08-22` ·
`DEAD_PATTERNS_REVIVAL_2026-08-22` · `DEAD_SYSTEMS_AUDIT_2026-08-22` · `EOD_DIAGNOSIS_2026-08-21` ·
`DAILY_EXTREMES_PLAYBOOK_2026-08-20` · `REPLAY_ENTRY_SIDE/EXIT_SIZE_2026-08-22` · TASK_LOG (T-47/F6).
**פרובים שרצו היום (psycopg2 readonly):** מעברי-תווית 08-20/21 · שני משטחי-הפרסום · setups/יום ·
כיסוי `v9_bars_5min.cumulative_delta` — פלט גולמי בנספח.

**מקרא סטטוס:** 🔴 שבור-ועולה-כסף · 🟠 שבור-מבנית (מפר את הפסיקה) · 🟡 חסר/לא-נמדד · 🟢 תקין — לא לגעת.

---

## 1 · הטבלה הראשית — S1: שרשרת סוג-היום, חוליה-חוליה

| # | חוליה | מימוש היום (file:line) | מה שבור מול פסיקת-דלטון-בינארי | התיקון | שעות | ראיה |
|---|---|---|---|---|---|---|
| **S1-1** | **פיד-ברים → מסווג** | bridge → `v9_bars_5min_woodies` (276/יום, בריא) → BarRouter `'5min'` → `_day_type_on_bar` (`backend/main.py:250`, subscribe `:961`); הידרציית-boot `BOOT_DAYTYPE_REPLAY_V1=1` (`main.py:823-880`) | 🔴 **ריסטארט באמצע-סשן מוחק את זיכרון-הייצוב**: `app.state._daytype_stability` (N=2 של F6), `_s1_conf_smooth`, ומוני-cold-start חיים ב-RAM בלבד. 08-21: 5 ריסטארטים ⇒ התווית התהפכה Normal→Variation ב-18:27:54 **בגלל ריסטארט, לא שוק** (שורות-state עם `opening_type` כ-repr-enum + `lock=PENDING`) | הידרציה מלאה מה-DB בעלייה: תווית-אחרונה + מצב-stability + מונה-ברים; שורת-boot שמוכיחה "המשכתי, לא התחלתי" | 4-6 | `EOD_DIAGNOSIS §1.2` · פרוב: 08-21 = 4 מעברי-תווית, conf 0.00-0.67 |
| **S1-2** | **מעקב-IB** | IB מ-Sierra TPO (`v9_tpo_sessions` CASH) אל `machine.ib_high/low`; `S1_IB_SANITY_V1=1` (`classifier_core.py:82-92`) מתקן מול 12-הברים-הראשונים **בתוך המסווג** | 🟡 השורות שנכתבות ל-`v9_day_type_history` עדיין נושאות את ה-IB המיוצא — חורג >2pt ב-**8/54 ימים** (קיצוני: 06-09 124.2 מול 26.2). המסווג רואה IB נכון; הדוחות/UI לא | לפרסם `ib_high_used/ib_low_used` (כבר קיימים בתוצאה, `classifier_core.py:304-305`) גם להיסטוריה | 1 | `DAILY_EXTREMES §3` |
| **S1-3** | **הערכת-קריטריונים מבנית** (אילו צדדים נשברו · הרחבה מול IB · נדידת-ערך) | `classify_session` פר-בר (`main.py:478-492`) → `classify()` (`daytype_classifier.py:185-519`) — סולם first-match-wins: acceptance-reclass → FORMING → Nontrend → Neutral (sides==2) → Trend_DD/Trend_Normal (שלושה מסלולים) → Normal_Variation → Normal. **הקריטריונים עצמם כבר בינאריים-מבניים** (sides, rib, one_tf, DD, value-migration, acceptance) | 🟠 **הסמנטיקה היא רה-הערכה-פר-בר של אגרגטים רצים** (sides/close_pos/rib) ולא **אירועים מבניים** ⇒ 200 קפיצות-תווית ב-54 סשנים, 49% מריצות-הביניים הן A→B→A טהור, `Trend_Normal` שחי דקה (08-21 19:00→19:01). בנוסף: **0 שורות-לוג ב-`classify()`** — 12 דגלי-S1 בלתי-ניתנים-לשיוך (DEAD_SYSTEMS C.i) | **סעיף B** — פרסום-מונע-אירועים (החלפה המרכזית) + `logger.info` פר-ענף-פוסק | ראה B | `label_stability.py:29-53` · `daytype_stability_study.py` |
| **S1-4** | **שכבת-ה-confidence** | `_confidence()` (`daytype_classifier.py:75-126`) — ציון 0-1 "חלק-הראיות-המיושרות"; `S1_CONFIDENCE_V2=1` + החלקה `S1_CONF_SMOOTH_V1=1` (`:153-182`); מתפרסם ל-`state.confidence` (`main.py:620-638`) ול-`v9_day_type_state` | 🟠 **ההפרה הישירה של הפסיקה — conf יושב בנתיב-ההחלטה בחמישה מקומות:** (1) `trading_gateway.py:159,1289-1290` — `DAYTYPE_PLAYBOOK_MIN_CONF=0.4` מדרדר וטו-פלייבוק; (2) `setup_emitter.py:98-133` — `AUTH_LOWCONF_REDUCED` הופך SKIP ל-REDUCED לפי conf; (3) `.env:559` `ENTRY_BUDGET_QUALITY_MIN_CONF=0.75` (כרגע בלתי-נגיש, אבל פסוק-ON); (4) `five_min_system.py:1598` EDGE_FADE דורש conf≥0.4; (5) `lock_state=LOCKED_LOW_CONF` מתפרסם כסטטוס. ב-08-21 conf נע 0.00↔0.67 תוך שהתווית נכונה | **סעיף B**: להחליף כל צרכן-conf בבינארי `{DETERMINED / PROVISIONAL(טרם-IB) / NOT_YET}`; conf נשאר לוג-תצפית בלבד או נמחק | 3-4 (הצרכנים) | פרוב C: 08-21 conf 0.0-0.67, 2 lock_states |
| **S1-5** | **פרסום-התווית** | נקודת-קידום יחידה `main.py:529-643`; **F6 חי מ-20.08 18:33**: `DAYTYPE_RECLASS_STABILITY_V1=1` (N=2, `label_stability.py`), `DAYTYPE_HONEST_PRELOCK_V1=1`, `DAYTYPE_ONE_SOURCE_V1=1` (re-sync `main.py:583-615`), fast-track ל-sides==2 (`dual_ib_break`) | 🟢 חלקית — **נמדד היום: 08-20 = 3 מעברים, 08-21 = 4** (מול 3.7/סשן לפני F6; "12 המעברים" של EOD כללו גם קפיצות-conf). 🔴 שנשאר: מצב-הייצוב נמחק בריסטארט (S1-1); `DAYTYPE_ANTIFLAP_V1` (600s) עדיין מסנן את ה-READ בלבד — א-סימטריה מוצהרת; והפרסום עדיין פר-בר, לא פר-אירוע | S1-1 (הידרציה) + סעיף B (אירועים). לא לגעת ב-N=2 — הוא עובד | ב-B | פרוב 1 + `TASK_LOG` T-47 |
| **S1-6** | **משטחי-אמת** | `v9_day_type_state` (`state_persist.py`, ~36-39 שורות/יום עם conf+lock) · `v9_day_type_history` (דרך `to_classification`, `main.py:752-800`) · חותמת `day_type_at_entry` על עסקאות (`trade_context.get_live_day_type`, `:565-643`) | 🔴 **היסטורית: 46% סתירה בין שני מקורות-לייב** (T-47). פרוב-היום: 08-14 שלוש תוויות שונות על עסקאות אותו יום; 08-17 hist=Trend_DD מול entries={Normal,Trend_DD,Variation}; 08-19 hist=Neutral_Extreme מול {Normal,Variation}. **מאז ONE_SOURCE (ערב 08-20): 08-21 מסכימים** — n=1, להמשיך לוודא | לוודא 5 סשנים של הסכמה מלאה; להוסיף שאילתת-guard יומית ל-`fire_drill` (hist==at_entry) | 2 | פרוב A |
| **S1-7** | **צרכני-התווית** | (א) **playbook** `daytype_playbook.py:142-240` → `trading_gateway.py:1312` — תאי FULL/REDUCED/SKIP + `require_with_trend` (REACTIVE=FULL בכל יום-מסחר אך with-trend-only); (ב) **budget** `trading_gateway.py:1753-1820` — `DAYTYPE_ENTRY_BUDGET_V1=0` (כובה 08-21 19:13); (ג) **Auth-Table** `auth_table_v1.py` — תאים בינאריים פר-(תבנית×סוג-יום); (ד) **S2-staging**: `confirm_tol_atr` פר-סוג-יום (חי), מקדמי-נפח פר-סוג-יום (**קוד-מת**, S2-3), מכפיל-רצפת-INITIATIVE (`five_min_system.py:219-241`), `chart_patterns_allowed`; (ה) F5 `RUNNER_TRAIL_V2=1` — **בלי שער-סוג-יום** (A2 של DAILY_EXTREMES); (ו) רוחב-סטופ פר-סוג-יום — לא קיים (A7, קונפיג בלבד) | 🔴 שלוש בעיות: (1) כשהתווית שגויה/מהבהבת — הצרכן הגדול (playbook על REACTIVE, החוסם-מס'-1 של התבניות-הטובות: 78 הזדמנויות, MFE>MAE 69%) חוסם לפי תווית+כיוון שגויים; (2) **C-2**: budget=0 הפך את `ENTRY_BUDGET_SKIP_LOSERS_V1=1`+`QUALITY_V1=1` לקוד-מת — שלוש פסיקות מאותו יום סותרות; (3) **fail-OPEN**: 18/32 זהויות-תבנית לא ממופות בפלייבוק ⇒ `Decision("FULL")` שקט (`daytype_playbook.py:189-190`) — TREND_STEP וכל ה-OPENING עוקפים את מטריצת-סוג-היום | (1)=סעיף B; (2)=שורת-פסיקה אחת של מייקל (להדליק תקציב **מותנה-סוג-יום** A1 או לכבות את הפנימיים); (3)=ברירת-מחדל REDUCED+WARNING לתבנית לא-ממופה | (2)=0.5 · (3)=1 | `DEAD_SYSTEMS` C-2, B-8 · `GOOD_PATTERNS §5` |

**שורת-הזהב של S1:** הקריטריונים כבר דלטון-בינאריים; **מה ששבור הוא הסמנטיקה של הפרסום
(פר-בר במקום פר-אירוע), שכבת-conf שדולפת לנתיב-ההחלטה, ואובדן-זיכרון בריסטארט.**
ימי-אי-הסכמה = ‎−$728.75 מול ימי-הסכמה ‎+$1,048.75 (n=26, קורלציה).

---

## 2 · הטבלה הראשית — S2: שרשרת-הירי של five_min, חוליה-חוליה

| # | חוליה | מימוש היום (file:line) | מה שבור | התיקון | שעות | ראיה |
|---|---|---|---|---|---|---|
| **S2-1** | **קליטת-ברים** | `FiveMinSystem.process_bar` ← stream `'5min'`; ATR מ-woodies; `v9_bars_5min` חלקי (126/יום מול 276 של woodies) | 🟡 `FiveMinAggregator` מנוי ל-`tick_reversal_15` שטבלת-ה-DB שלו קפואה 51 יום ⇒ אין היסטוריה לשחזור אחרי ריסטארט (הזרם החי עובד) | לשחזר ingest או להסיר את התלות; לא דחוף לירי | 2 | `DEAD_SYSTEMS §D` |
| **S2-2** | **חוצץ + הידרציה + cold-start** | `_bar_buffer` חסום ל-**20** (`five_min_system.py:457-458` בהידרציה, `:1338,:1354` בלייב); `_det_buf=buffer[:-1]` ⇒ חלון-גילוי **19 ברים** (`:1727`); `cold_start_guard` קורא `bars_processed_today` **מ-TPO** (`trading_gateway.py:886-913`) | 🔴 שלוש תקלות: (1) ריסטארט מאפס את המונה ⇒ 9 חסימות-cold-start ב-08-21 כולל HTLB 16:30:05 של רגל-A (‎−8.75pt‎); (2) תקלת-מונה 16:54 — `bars_processed=0` בעוד buffer=60; (3) חלון-19 הופך H&S/‏SEARCH_WINDOW=30 לבלתי-ניתן-להשגה. **אבל:** הרחבה ל-32 (F5) נמדדה ‎$0.00‎ בזרם ו-‎−$372‎ בבידוד — **לא להרחיב לבד**, רק כזוג עם תיקון-Adam (C4) | מונה מה-DB (נגזר, לא RAM); ריסטארט לא מאפס הקשר (S1-1); חלון-32 רק בתוך C4 | 2-3 | `EOD §1.2,3.2` · `GOOD_PATTERNS §4ב` |
| **S2-3** | **גלאי REACTIVE** | `_detect_reactive` (`five_min_system.py:767-1000`): גיאומטריית b1-b4 + `b2_drop` **UNION** (`:821-829`; A_VSA∨B_RVOL∨C_STRICT) + `confirm_tol_atr` פר-סוג-יום (חי) + שער-CVD (`:905-995`) | 🟢 **הגלאי אינו הבעיה — נמדד סופית:** b2_drop עובר 25.3% והוא החוסם-היחיד ב-263 ברים, אבל **כל הרפיה מפסידה בזרם** (הטובה RG1 = ‎+$92/34 סשנים‎); ‎256 ירי = המספר הנכון. 🔴 מה שכן שבור: (א) **המקדמים-הפסוקים פר-סוג-יום הם קוד-מת** — `:830` הוא `elif` תחת `if S2_VSA_VOLUME` (=1) ⇒ החצי-הנפחי של `s2_reactive_calibration.yaml` לא נקרא מעולם (הדלקתו נמדדה ‎−$762‎ — **לתעד, לא להדליק**); (ב) שער-ה-CVD רץ על חלון מיושר-שגוי (S2-6) | (א) הערת-קוד + FLAG_INDEX; (ב) ראה S2-6 | 0.5 | `GOOD_PATTERNS §4,6א` |
| **S2-4** | **גלאי INITIATIVE** | `_detect_initiative` (`:1104-1169`): רצפת-התפשטות `max(P80×20, 0.55×ATR14)×מכפיל-סוג-יום` (`:219-241`) + `b3_join` (מתוקן, 93% עובר) + וטו-CVD כיווני (`:1117-1149`) | 🟢 הרצפה חוסמת ~74% **בהגדרה** (P80) — הרפיות ×0.85/×0.70 נמדדו ‎−$1,218 / לא-בזרם‎. עיוור-נפח לחלוטין — אבל אין מדידה שמראה שכניסת-נפח שם שווה כסף | אין-פעולה על הספים; הדלתא נכנסת כזרם נפרד (C1) | 0 | `GOOD_PATTERNS §4א` |
| **S2-5** | **תבניות-גרף (שרשרת-הסלוט)** | סדר סדרתי, ראשון-תופס (`:1727-1803`): REACTIVE→INITIATIVE→HLST(כבוי)→iHNS→**HNS (רק כ-fallback)**→DBT_EE→DT_AA→דגלים→RE_PULLBACK(כבוי); dedup פר-תבנית (`:1805-1816`) | 🔴 (1) **HNS_TOP לא יורה לעולם**: 3 פסגות-סווינג + `PIVOT_LOOKBACK=2` בחלון-19 ⇒ 1 ירי/34 סשנים, וגם נבדק רק אם iHNS ריק (`:1756-1758`); רגל-C של 08-21 = ‎$300‎ שלא נלקחו; (2) **DT_AA = 0 ירי**: סבילות-Adam משותפת עם Eve (`double_bt.py:44-48`) — התיקון **רק כזוג** (סבילות-2-טיקים + חלון-32) = ‎+$582 בזרם‎; (3) **BULL_FLAG חסום אריתמטית**: `t1_r≤0.8` (`:2246` + `stop_anchors.yaml:103-107`) מול `rr_min=1.0` בימי-Trend (`trading_gateway.py:647-676,3039`) ⇒ בלתי-אפשרי בשני תאי-ה-FULL שלו; אחרי תיקון ‎+$180 בזרם‎; (4) **הסלוט עצמו הוא הצוואר**: 591 מועמדים→206; TREND_STEP אוכל סלוט ושווה ‎IS +$4,354 / OOS −$2,011‎ — הוצאתו (X1) = ‎+$834‎, ועם כלל-המגמה (X4) = ‎+$1,332 (OOS +$1,011)‎ | (1) 2-פסגות+צוואר או lookback=1 — needs-ruling; (2)+(3) = C4/C5; (4) = C3 (X4 — SHADOW, מבטל פסיקת-14.08) | (1)=3 · (2)=2 · (3)=1 | `EOD §2,3.3` · `DEAD_PATTERNS §2` · `GOOD_PATTERNS §6-8` |
| **S2-6** | **הקלט החסר: דלתא-נכנסת/ספיגה** | **אין.** כל שערי-הנפח של S2 הם **ירידת-נפח**; ה-CVD היחיד: `_compute_setup_cvd` חלון-4 (`:728-763`) + דיברגנס יחיד בכיוון ההפוך מזה של מייקל (`:919-921`) | 🔴 (1) **שער-ה-CVD רץ נכון ב-16.3% מהברים בלבד** — 55.4% על 2-3 שורות בלי בדיקת-אורך (הפרת כלל-1; ‎$585‎ מ"ערך-השער" הנמדד הוא ארטיפקט-כיסוי); (2) מקור-הגייטוויי `direction_context_live.py:71-101` קורא `v9_bars_5min.cumulative_delta` — ריק כמעט לגמרי יולי→08-06 (פרוב D: מאוכלס 84-126/יום מאז 08-17, אך `v9_bars_cumulative_delta` נשאר חציון-63% כיסוי, 5 סשנים ריקים); (3) **אין טריגר כניסת-דלתא**: `P2_DBL_VOL` מותנה-סוג-יום = ‎**+$2,254/34 סשנים @6c, חיובי IS+OOS+שלוש-רמות-סליפג'**‎ — הפריט הכספי הגדול היחיד שנמצא; (4) ספיגה (P1a) ככניסה = ‎−$2,871‎ — **רק שדה-הקשר** | (1) `len<window ⇒ return None + warning` — SHIP; (2) השלמה מ-scid (r=1.00 מאומת) + repoint המקור; (3) = **C1** `S2_DELTA_DBL_V1`; (4) = C2 שדה-בלבד | (1)=1 · (2)=4-6 · (3)=6-8 | `CVD_EFFORT §3-6` |
| **S2-7** | **Auth-Table** | `auth_table_v1.py`: YAML גובר כשיש ≥70 תאים (`:122-134`); `auth_matrix.yaml`=**70 בדיוק** (10×7); ה-dict הקשיח=84 (12×7); asserts בודקים את הטבלה **הלא-בשימוש** (`:136-146`); `get_auth_cell` (`:149-163`) | 🔴 (1) **RE_PULLBACK → KeyError** (חסר מה-YAML) נבלע בשקט ב-`five_min_system.py:2443`; (2) **HLST → ValueError** (השמות לא ב-`PatternName`, `output_schema.py:13-20` = 12 שמות; נבנה ב-`:2199`); (3) שניהם חסרים גם מ-`contract_split._SPLIT_MAP` (10 ערכים); (4) הדלקת HLST **תדכא את DOUBLE_BOTTOM החי** (רץ לפני 5a) — והריפליי אומר ‎HLST −$4,574‎ / ‎RE_PULLBACK −$1,625‎ ⇒ להשאיר כבויים | לתקן את החיווט (14 תאי-YAML + assert על `AUTH_TABLE` הנטען + שמות) **ולהשאיר את הדגלים כבויים** — התיקון מונע את מחלקת-הבאג הבאה, לא מדליק תבניות | 1-2 | `DEAD_SYSTEMS B-1/B-2` · `DEAD_PATTERNS #12,#16` |
| **S2-8** | **פליטה/סכימה** | `setup_emitter.py` → `gateway.route_setup`; T1/T2 פר-תבנית (`five_min_system.py:2201-2270`); `AUTH_LOWCONF_REDUCED` hook (conf — ראה S1-4) | 🟠 ה-hook של conf<0.4 יושב כאן בנתיב-ההחלטה (הפרת הפסיקה) | סעיף B (בינאריזציה של הצרכן) | ב-B | `setup_emitter.py:98-133` |
| **S2-9** | **שערי-הגייטוויי, בסדר-ריצה** | `kill_switch:867` → `session_gate:876` → `cold_start:896` → `eod_entry_cutoff:925` → `feed_watchdog:940` → chop`:1009`(כבוי-בפסיקה) → drive_exhaustion`:1090`(כבוי) → **`daytype_playbook:1312`** → `location_gate:1434` → **`cont_trend_filter:1553` / `direction_context:1606`** → `direction_compass:1643` → `lsma_flat:1737` → `day_entry_budget:1816`(=0) → `extreme_chase:2028` → S7-shadow`:2063` → **`awaiting_release:2105-2165`** → `entry_not_confirmed:2928` → `rr_entry_gate:3017,3040` → cluster`:3213` → `pre_send_entry_guard:4102` | 🟢/🔴 **המדידה המכרעת: הריפליי חסר-השערים כולו = ‎+$771/34 סשנים‎ ⇒ פתיחת-שערים איננה המקום שבו הכסף.** פר-שער: `daytype_playbook` על REACTIVE (78 הזדמנויות, MFE>MAE 69%) — התיקון הוא **התווית+הכיוון (סעיף B)**, לא הסרת-השער; `awaiting_release` — הגדול-במניין (119 הזדמנויות, 40-62%) — **לא-חד-משמעי**, לכמת קודם (D4); `cont_trend` על INITIATIVE, `lsma_flat` על GB100, `location_gate` על HTLB, `eod_cutoff` על ZLR — **חוסכים כסף, לא לגעת**; `cold_start`/`session_gate` — 98% מחסימותיהם על ברי-הידרציה = לא-הזדמנויות; 🔴 `pre_send_entry_guard` חסם 4 שורטים תקפים ב-08-21 **בגלל באג-הספרים** (A6), לא שיקול-מסחר | B (תווית) + A6 (ספרים) + D4 (מדידת-release) + פסיקת-budget (S1-7) | — | `GOOD_PATTERNS §5` · `EOD §1.3 D8` |
| **S2-10** | **גודל** | `ruled_contracts()`=6 (`contract_size.py:58-74`), סולם (1,2,2,1) (`:40-47`); REDUCED של playbook/auth חותך; S7 sizing **צל-בלבד** (7 was-blocked/75) | 🟢 עובד כפסוק. 🟡 `FIXED_CONTRACTS_4=1` עתידי יהיה no-op שקט מאחורי ה-6 (C-3) | לקרוס ל-`CONTRACTS_N` יחיד — תחזוקה, לא דחוף | 1 | `DEAD_SYSTEMS C-3` |
| **S2-11** | **מכניקת-כניסה (איחור)** | גילוי על **בר-סגור בלבד** (`_det_buf` בלי הבר-הנוצר, `:1727`); אישור-פיבוט N=2 (`double_bt.py:31-34`); שבירות-צוואר **בסגירה בלבד**; בר-אישור `S4_ENTRY_CONFIRM_V1=1` (`trading_gateway.py:2907-2931`); release-gate (`release_gate.py`) | 🔴 תקציב-האיחור המדוד של 08-21 = **36.25pt = ‎$725@4c‎**, ופירוקו: ~חצי **תקלות** (תקציב-17-חסימות — כבר כובה; ריסטארטים/cold-start ‎~8.75pt‎; באג-ספרים ‎~6.75pt‎) ו~חצי **מבנה**: בר-סגור ‎14.75pt‎ · פיבוט ‎~5pt‎ · בר-אישור ‎0.75pt‎ · release ‎~9pt‎ (אך הוא גם מציל — 07-27). **טיק-ריפליי כבר פסק:** כניסה-על-נגיעה = ‎−$1,314‎; כניסה תוך-בר **עם אישור-זרימה** = ‎+$178‎; תקרת-שיפור-מחיר ‎+$1,787‎ ⇒ אין win פשוט — כניסה מוקדמת דורשת את פיד-הדלתא (A2) | D2: intrabar+flow-confirm — לבנות רק אחרי C1/A2, SHADOW; D3: שבירת-שפל+אישור מול שבירת-סגירה — needs-ruling; לא לגעת ב-release בלי D4 | D2=8-10 | `EOD §3.2` · `REPLAY_ENTRY_SIDE` |
| **S2-12** | **הירי עצמו + הספרים** | `route_setup` → TM → DLL bracket פר-חוזה; `pnl_reconcile` מיישב מול `trade_fills_journal` | 🔴 הספרים מפספסים מילויי-יציאה (4/7 עסקאות ב-08-21, ‎$96.25‎ פער; #764 רשם 1/4 חוזים) ⇒ phantom-heal-על-שקר ⇒ **`pre_send_entry_guard` חוסם כניסות תקפות** (4 ב-08-21). `pnl_reconcile` הכריז DIVERGENT-0 באותו יום — עיוור למחלקה שלו | A6: ספר-הברוקר (`TradeActivityLog*.data` · `Closed Trade Profit/Loss`) כמקור-אמת-יציאות; `incomplete` נכשל ולא מושמט | 6-10 | `EOD §1.3,5.1` |

---

## 3 · תוכנית-התיקונים — מסודרת: "שלא תפוספס עסקה"

### A · תיקוני-באג בלי פסיקה (נכונות; לא משנים משטח-סיכון)

| # | תיקון | קובץ | $ נמדד | תלות | מוכן-שני? |
|---|---|---|---|---|---|
| **A1** | יישור-חלון-CVD: `len(cums)<window ⇒ None + logger.warning` | `five_min_system.py:728-763` | מסיר הטיית ‎$585‎ (יוריד $ נמדד — וזה נכון, כלל-1) | — | ✅ **שני** (1ש') |
| **A2** | השלמת `v9_bars_cumulative_delta` מה-scid (r=1.00 מאומת) + repoint מקור-הגייטוויי מ-`v9_bars_5min` | סקריפט-backfill + `direction_context_live.py:80-86` | משחרר את מצפן-ה-CVD (נמנע ~26/34 סשנים) + תנאי-קדם ל-C1/D2. **אין $ ישיר נמדד** | — | ✅ backfill שני; repoint אחרי אימות (יום-יומיים) |
| **A3** | עצירת זיהום-pytest של פיד-ההחלטות + טיהור הארכיון | `tests/conftest.py` (העתקת fixture מ-`backend/v9/tests/conftest.py:17-24`) | ‎8%‎ מהפיד פיקציה — כל אבחון "למה לא ירה" נקי מעכשיו | — | ✅ **שני** (1ש') |
| **A4** | שלמות-Auth: 14 תאי-RE_PULLBACK ל-YAML + שמות-HLST + assert על `AUTH_TABLE` **הנטען** + ערכי-split; **דגלים נשארים כבויים** | `auth_table_v1.py:136-146` · `config/auth_matrix.yaml` | ‎$0‎ היום; הורג את מחלקת-ה-KeyError-הנבלע | — | ✅ **שני** (1-2ש') |
| **A5** | חסינות-ריסטארט: הידרציית תווית+מצב-stability+מוני-ברים מה-DB בעלייה; מונה-cold-start נגזר-DB | `main.py` boot · `label_stability.py` | חלק מ-‎$315‎ של רגל-B 08-21 (ריסטארט הפיל תווית+תקציב) | — | ✅ **שני-שלישי** (4-6ש') |
| **A6** | ספרים: `Closed Trade Profit/Loss` מספר-הברוקר כמקור-יציאות; `pnl_reconcile` נכשל על `incomplete` | `sierra_pnl_reconcile.py` + exit-tracker | ‎$96.25/יום‎ שקר-ספרים + 4 חסימות-`pre_send_entry_guard` תקפות ב-08-21 (‎~$135‎ על #768 בלבד) | — | 🔶 שבוע (6-10ש'); ה-guard-החוסם מרוויח מזה מיד |
| **A7** | ארכוב-יומי של `backend.err.log` + תיקון מתזמן `decisions_archive` (פספס את 08-21) | סקריפט EOD | ‎$0‎ — יכולת-חקירה | — | ✅ **שני** (1ש') |
| **A8** | fail-OPEN של הפלייבוק → ברירת-מחדל REDUCED + WARNING על תבנית לא-ממופה | `daytype_playbook.py:189-190` | TREND_STEP/OPENING עוקפים היום את המטריצה | — | 🔶 בנייה שני, **הדלקה = פסיקה** (משנה גודל בפועל) |

### B · החלפת-המסווג המבנית — **הפריט הגדול** (הפסיקה כבר ניתנה; בנייה→אימות→הדלקה בלי אישור-חוזר, עם pointer ב-`RULED_FLAGS`)

**מפרט קונקרטי (`S1_STRUCTURAL_BINARY_V1`):**

1. **קלטים:** ברי-RTH מ-`v9_bars_5min_woodies` · IB = 12 הברים הראשונים (Sierra כ-cross-check בלבד — היפוך ההיררכיה של היום) · PDH/PDL/VAH/VAL קודמים · DD-features · value-migration. CVD = **אישור-תצפית בלבד, לעולם לא שער** (כמו היום ב-classify).
2. **טבלת-קריטריונים בינארית פר-סוג** (כולה כבר קיימת ב-`daytype_classifier.classify()` — משתמשים בה כמו-שהיא): Nontrend = sides0+vol≤0.5+rib≤1.15+range≤18 · Normal = sides0+rib≤1.30+IB-לא-צר · Normal_Variation = הרחבה חד-צדדית מקובלת · Trend_Normal = חד-צדדי+one_tf/מדרגות+סגירה-בקיצון (שלושת המסלולים הקיימים) · Trend_DD = מבנה-DD · Neutral_C/E = הרחבה דו-צדדית, תת-סוג לפי סגירה · Nonconviction = OA-in-value+אפס-RE.
3. **אירועי-מעבר במקום רה-הערכה-פר-בר:** התווית משתנה **רק** על: (א) נעילת-IB; (ב) acceptance ראשון מעבר ל-reference (2 סגירות+נפח — `level_acceptance` הקיים); (ג) acceptance של הצד-השני ⇒ Neutral מיידי (fast-track קיים); (ד) failed-break revert; (ה) היווצרות/מילוי-צוואר-DD; (ו) round-trip דרך הפתיחה; (ז) EOD. בין אירועים — **התווית מוחזקת**. תוצאה צפויה: ‎≤3‎ מעברים/יום מבניים, אפס A→B→A.
4. **מצב UNKNOWN:** לפני נעילת-IB = `NOT_YET_DETERMINED` + התווית-הפרוביזורית הקיימת (HONEST_PRELOCK) — מידע, לא וטו. אין FORMING אחרי הנעילה.
5. **צריכה בינארית:** כל הצרכנים (playbook, budget, auth, S2-staging, F5-gate, stop-width) מקבלים `{label, determined: bool, direction}` — **שדה-conf נמחק מהחוזה**. מסלולי conf<0.4 / conf≥0.75 / EDGE_FADE-conf מוחלפים ב-`determined`-check. lock_state מצטמצם ל-{NOT_YET, DETERMINED}.
6. **אימות (מבחן-הקבלה):** ריצת-צל על 54 הסשנים — התווית-הסופית חייבת להתלכד עם `classify_session(is_eod=True)` **54/54** (המסווג כבר עקבי-עם-עצמו; משנים רק סמנטיקת-פרסום); מעברים-תוך-יומיים ≤ מספר-האירועים; 08-20 (Neutral באיחור 1:50ש') חייב להיתפס ב-(ג) בזמן-אמת.
7. **תלות:** A5 (חסינות-ריסטארט) — בלעדיה גם המסווג-החדש יימחק ב-restart. **מאמץ:** מפרט+בנייה 1-2 ימים, **צל ≥10 סשנים** (השוואת old/new + counterfactual של שערי-התווית). **$:** מאפשר את כל פלייבוק-סוג-היום (A1/A2/A7 של DAILY_EXTREMES ≈ ‎+$108/יום‎ מודל, מותנה-תווית-נכונה); ה-$ הישיר של הבינאריזציה עצמה — **אין מדידה עדיין** (מסומן ביושר).

### C · תוספות-גלאי

| # | פריט | $ נמדד (34 סשנים @6c) | תלות | סטטוס-פסיקה | מתי |
|---|---|---|---|---|---|
| **C1** | **`S2_DELTA_DBL_V1`** — תחתית/פסגה-כפולה עם **טריגר-דלתא-נכנסת**, מותנה `day_type ∈ {Normal, Trend_Normal, Trend_DD}`, זרם נפרד (לא בסלוט) | **‎+$2,254‎** (‎$66/יום‎; IS ‎+$1,218‎ / OOS ‎+$1,036‎; חיובי ב-s0/s1/s2) | A2 (דלתא) + B (תווית — ההתניה היא כל ההבדל: בלי-התניה ‎−$2,038‎) | דורש פסיקה (גלאי-כניסה חדש) → צל 10 סשנים | בנייה השבוע, צל מיד |
| **C2** | שדה-`absorption` (P1a) על כל סטאפ — הקשר בלבד, לא כניסה (ככניסה ‎−$2,871‎) | ‎$0‎ (מדידה) | — | לא | ✅ שני (2ש') |
| **C3** | **X4**: הוצאת TREND_STEP מתחרות-הסלוט (ממשיך להירשם כמועמד-צל) + כלל-עם-מגמת-היום הקיים | **‎+$1,332‎** (OOS ‎+$1,011‎; חציון-יום ‎$146‎ מול ‎−$18‎) | — | **דורש שורת-מייקל** — מבטל בפועל את פסיקת-TREND_STEP מ-14.08 (שכוילה in-sample: מאז ההדלקה ‎−$420‎ ריפליי, ‎−$197.50‎ ספרים) | צל 10 סשנים → ship |
| **C4** | DT_AA: סבילות-Adam נפרדת (2 טיקים) **+** חלון-32 — **רק כזוג** (לבד: ‎−$375‎) | ‎+$582 בזרם‎ (אוגוסט שלילי — זהירות) | — | דורש פסיקה (חלון-32 נוגע ב-DBT החי: ‎+$365→−$7‎ בבידוד, ‎$0‎ בזרם) | צל |
| **C5** | BULL_FLAG: `t1_r_max 0.8→1.05` או מבחן-RR משפחתי (תקדים `CONFLUENCE_RR_MIN`) | ‎+$180 בזרם‎ (‎$1,054/$1,081‎ משני ימים — זנב) | — | דורש פסיקה (משנה מבחן-RR) | צל |
| C6 | HNS_TOP: 2-פסגות+צוואר או lookback=1 | ‎$160-300‎ ברגל-C אחת — **אין ריפליי מלא** | — | דורש פסיקה | מדידה קודם |

### D · תזמון-כניסה

| # | פריט | $ | תלות | הערה |
|---|---|---|---|---|
| **D1** | האיחור-התקלתי (ריסטארטים, מונים, ספרים, תקציב-שכובה) | ‎~50%‎ מ-‎36.25pt/יום‎ | = A5+A6 + פסיקת-budget | זה החצי הזול — תקלות, לא שיקול |
| **D2** | כניסה תוך-בר **עם אישור-זרימה** (טיק-ריפליי: נגיעה ‎−$1,314‎ · עם-זרימה ‎+$178‎ · תקרה ‎+$1,787‎) | ‎+$178‎ מדוד, תקרה גדולה | A2+C1 | SHADOW; אין win בלי דלתא |
| **D3** | שבירת-צוואר בשפל+אישור במקום סגירה-בלבד (DT 08-21: שעה איחור) | **אין מדידה** | — | needs-ruling + ריפליי ייעודי |
| **D4** | רישום `mfe_after_block` על כל חסימת-`awaiting_release`/playbook (המדידה שתכריע אם לפתוח) | ‎$0‎ (אינסטרומנטציה) | — | ✅ שני (2ש') |

### מוכן-עד-שני מול צריך-צל

- **שני (בלי פסיקה, בלי סיכון):** A1 · A3 · A4 · A7 · C2 · D4 · A5 (התחלה) · A2-backfill · מפרט-B.
- **שבוע, צל:** B (המסווג-הבינארי, ≥10 סשנים) · C1 · A6 · D2.
- **שורת-פסיקה אחת של מייקל:** C3 (TREND_STEP-מהסלוט) · C4 · C5 · C6 · budget מותנה-סוג-יום (פתרון C-2) · A8-הדלקה · ZLR-בלי-FULL (#13 בביקורת) · D3.
- **לא לגעת (נמדד כחוסך):** `cont_trend` על INITIATIVE · `lsma_flat` על GB100 · `location_gate` על HTLB · `eod_cutoff` על ZLR · ספי-הגלאים של REACTIVE/INITIATIVE (כל הרפיה מפסידה בזרם).

**אזהרות-יושר:** ‎34 סשנים / OOS=13‎ — מדגם דק; אוגוסט שלילי כמעט בכל זרוע (X4 משפרת ‎+$930‎ ולא הופכת); C1 נבחר מתוך רשת 42 תאים (השוואה-מרובה) — הצל הוא ההגנה; ה-$ של B עצמו לא נמדד ישירות — מה שנמדד: ימי-אי-הסכמה ‎−$728.75‎, T-47 ריפליי 12/12 ימים מתלכדים, וכל פלייבוק-סוג-היום חסום עליו.

---

## נספח · פרובים שרצו היום (READ-ONLY, psycopg2)

```
-- 1) מעברי-תווית ב-v9_day_type_state (אחרי הדלקת F6 ב-20.08 18:33):
(2026-08-20, rows=39, transitions=3) · (2026-08-21, rows=36, transitions=4)

-- 2) שני משטחי-הפרסום (hist מול day_type_at_entry):
08-14 hist=Variation | entry={Trend_DD,Trend_Normal,Variation}   ← לפני F6
08-17 hist=Trend_DD  | entry={Normal,Trend_DD,Variation}          ← לפני F6
08-19 hist=Neutral_Extreme | entry={Normal,Variation}             ← לפני F6
08-20 hist=Neutral_Extreme | entry={Neutral_Extreme,Variation}    ← F6 עלה באמצע-סשן
08-21 hist=Variation | entry={Variation}                          ← ✅ הסכמה מלאה (n=1)

-- 3) v9_five_min_setups/יום: 08-17=2 · 08-18=4 · 08-19=6 · 08-20=7 · 08-21=20
-- 4) 08-21 confidence: min=0.0 max=0.67, lock_states=2
-- 5) v9_bars_5min.cumulative_delta rows/יום: 08-17=84 · 08-18..21=125-126 (העידן-הריק: יולי→08-06)
```

*בוצע READ-ONLY ב-2026-08-22 ע"י cowork-dev/agent. לא שונו קוד-רץ, דגל, `.env`, DB או שירות.*
