# פקודת-עבודה — הכנת יום שני (כסף חי) · פסיקות-מייקל 22-23.08
**§0 חל** (להכריע, לתעד, להמשיך). **המפה:** `docs/plans/S1_S2_FIRING_GAP_MAP.md` — לקרוא ראשונה.
**כלל-§D:** כל שינוי-התנהגות עובר week_replay לפני .env; חציון-יום לא נפגע.

## פסיקת-מייקל (23.08): TREND_STEP → צל + הצעת-תיקון
**ציטוט: "מאשר להעביר לצל ולהציע תיקון לאיך היא הופכת טובה יותר."**
1. `TREND_STEP_ENTRY_V1` → מצב-צל (מזהה+מתעד, לא תופס סלוט-לייב; לשמר את
   ה-struct-exempt של F4 בצל). RULED_FLAGS: לציין שזה מבטל את הדלקת-14.08 בפסיקה
   חדשה. הראיה: IS ‎+$4,354 מול OOS ‎−$2,011; מאז-הדלקה ‎−$198 בספרים; הוצאתה
   מהסלוט ‎+$1,332 (OOS ‎+$1,011).
2. **הצעת-התיקון (לחקור, לא להדליק):** לאבחן *למה* היא נשברה מחוץ-למדגם —
   (א) הספים שלה (אימפולס 8-45pt, ריטרייס 20-55%) כוילו על יולי — למדוד אותם
   מחדש כיחסיים-ל-ATR; (ב) hee התנאי stair-OR שהוסף 18.08 — למדוד בנפרד;
   (ג) לבחון תנאי-דלתא (absorption בפאוזה — הפרימיטיב של מייקל) כתנאי-איכות;
   (ד) בחינת כניסת-ריטסט במקום שבירה. כל וריאנט: רפליי מלא IS/OOS.
   → `docs/reports/TREND_STEP_FIX_PROPOSAL.md` עם טבלה והמלצה אחת.

## A · תיקוני-אמת ליום שני (בלי פסיקה, לפי המפה)
A1 יישור חלון-ה-CVD + מקור (רץ נכון 16.3%!) · A3 עצירת זיהום-pytest מפיד-ההחלטות ·
A4 שלמות-Auth (RE_PULLBACK KeyError · HLST ValueError שמדכאת DOUBLE_BOTTOM — להסיר
את HLST מהשרשרת) · A5 חסינות-ריסטארט ל-S1 (הידרציית זיכרון-ייצוב מה-DB) + מונה
cold_start מה-DB · A6 תיקון pnl_reconcile (משווה פקודה ולא מילוי; מחריש FLATTEN/BE)
· A7 ארכוב-לוגים לפני גלגול · C2 שדה-absorption זמין לגלאים · D4 רישום MFE-אחרי-חסימה.

## B · המסווג-הבינארי (הפסיקה ניתנה 22.08 — "או שיום הוא אחד מהימים או שלא")
לבנות לפי המפרט במפה: קריטריונים מבניים פר-סוג (צדדי-IB שנפרצו · הרחבה מול IB ·
נדידת-Value) · תווית = {DETERMINED:<type> / NOT_YET} · מעבר = אירוע מבני · **אפס
confidence בכל שרשרת-ההחלטה** (חמש הנקודות במפה). אימות: רץ על 54 הימים ומתכנס
לסיווג-הבדיעבד של הפלייבוק; מדד = כמה מוקדם ביום מגיע לתווית הסופית.
רץ **בצל מול החי** מיום שני; החלפה אחרי ≥10 סשנים ירוקים.

## C · צל נוסף מיום שני
C1 `S2_DELTA_DBL_V1` — גלאי-הדיברגנס-דלתא של מייקל, מותנה Trend/Normal (‏+$2,254,
OOS ‎+$1,036) — צל · X4 קונפלואנס S2×S4 להגדלה — צל · P3 הגדלה-מחושבת + סטופ-ממוצע-
אחד + איסור-קצה (‏+$27/יום) — **חי** (נמדד, עם רפליי-אישור) · סגירת-T-10 — **חי**.

## סדר: פסיקת-TREND_STEP → A → B → C. בסוף: flag_guard·fire_drill·דיווח חתום.
