# פרה-אופן 2026-07-28 · 16:07–16:15 IL (פתיחה 16:30) — READ-ONLY

**פסק: 🔴 NO-GO ללייב · 🟢 GO טכני (התשתית בריאה)**
שני חוסמים שרק מייקל יכול לפתוח, שניהם **לא-קוד**: (1) סיירה על **Sim1**, (2) מרג'ין.

---

## חוסמים

### 🔴 1. סיירה על חשבון סים — שום הוראה לא תגיע ללייב
`~/SierraChart_Data/v9_export/sierra_state.json` (ts 16:10:51):
```
"is_sim":1, "trade_account":"Sim1", "send_orders_to_trade_service":0,
"order_placement_armed":1, "position_qty":0, "daily_pnl":-87.50, "daily_total_qty_filled":22
```
ה-22 חוזים + −$87.50 של היום הם פעילות-הסים/בדיקות (control-test של PLACE_BRACKET ב-06:01 + היתום שהושטח 07:19). **החשבון שטוח.**
ב-07-18 הערוץ תיעד `is_sim=0` mode=live — היום הפוך. מעבר Sim→Live הוא ידני-מייקל בלבד.

### 🔴 2. מרג'ין — 0 חוזים אפשריים בלייב
מ-OPS_LOG היום 02:50 (הרשומה שלך): `$97.68 available` מול `$276.21 לחוזה` →
`MARGIN_AWARE_SIZING_V1=1` יחזיר 0 חוזים (או דחיית-ברוקר בלעדיו). לא ניתן לאמת עכשיו כי
החשבון הנבחר הוא Sim1 (שדות ה-acct מחזירים DBL_MAX = לא-מאותחל). **צמצום ה-6-lot הידני =
התנאי היחיד.**

---

## 8 הבדיקות

| # | בדיקה | תוצאה |
|---|---|---|
| 1 | מאזינים יחידים | ✅ `Python 1424 → :8000` · `node 15296 → :3000` |
| 2 | Backend חי ומעודכן | ✅ `{"status":"ok","version":"v9.0.0"}` · עלה `Tue Jul 28 13:08:17` · קומיט אחרון `13:08:30 c1bf7c91` · **אין `.py` שהשתנה אחרי העלייה** → הקוד שרץ = עץ-העבודה |
| 3 | פיד | ✅ המקור הקנוני חי · 🟡 ה-endpoint מציג ישן — ראה למטה |
| 4 | דגלי-ריצה | ✅ `[env_loader] applied 189 vars … HFE_DISABLED=1 NONTREND_DISABLE_ALL=1 DIRECTION_LSMA_VETO=1 S1_NEW_CLASSIFIER=1 ZLR_SPEC_V2=1 VEGAS_SPEC_V2=1 DAYTYPE_POSITION_GATE=0 DAYTYPE_PLAYBOOK=1` · `flag_guard PASS 136/136` |
| 5 | READINESS | 🟡 `ready_to_route=false` אך `classification=NO_SETUP`, `decision_tree={}`, `active_patterns=[]` — **אין תבנית טרום-פתיחה, לא חסימה מבנית.** אף שער-קריטי חסר ל-S2/S4 (`crit-missing: []` לשניהם) |
| 6 | גייטוויי פנוי | ✅ `demo_slot:null` · `live_slot:null` · `cooldown_active:false` · `live_enabled_systems:[2,4]` · `trades_today:0` · `chop_state:EXPANDING` |
| 7 | Tracebacks | ✅ 0 ב-300KB האחרונים |
| 8 | פוזיציות פתוחות | ✅ `SELECT COUNT(*) … NOT IN ('CLOSED','CANCELLED') → 0` |

### פירוט §3 — הפיד
```
v9_bars_5min_woodies | MAX 2026-07-28 16:05:00 | 187 שורות היום   ← המקור הקנוני (SoT שורה 14)
v9_bars_5min         | MAX 2026-07-27 23:55:00 | 0 שורות היום
build/pattern-status: woodies_5min = [FRESH] 0s ago · 2026-07-28 16:05:00  (critical ✅)
bridge: [bars_5min] New data — export_ts=1785244038 (push #33049)  ·  "API push FAILED to https://" = 0
```
`build/pattern-status` מסמן `bars_5min`/`cumulative_delta`/`imbalance` כ-**critical+DEAD** —
**זה תקין טרום-פתיחה**: הם מגיעים מצ'ארט-הקאש שמכסה 09:30–19:55 ET (ב-DB: 13:30–23:55, כלומר
ET+4=UTC). ‏`5min.json` נגמר ב-`15:55 ET` של אתמול = הבר האחרון לפני סגירה; היום עוד לא נפתח
(‏09:30 ET = 16:30 IL). `imbalance_flags.bars=[]` מאותה סיבה. **צפוי שיתעוררו ב-16:30.**
תופעת-לוואי: **30,106** אזהרות `TS-OFFSET-GATE: non-advancing batch … stale re-push` מ-00:05 היום — רעש, אמור להיפסק בפתיחה.

`live_price` תקין ולא מושפע: `{"price":7457.62,"bid":7457.5,"ask":7457.75,"source":"cache","age_ms":177}`
(תיקון-מידפוינט `_best_price` מטפל בסגירת-הצ'ארט התקועה 7453.25) → **שער `ZONE_LIMIT_ENTRY_V1=1`
לא בסכנת חסימת-שווא ממחיר-ישן.**

---

## פתוחים שאינם חוסמי-פתיחה
- **22/22ב W8** — הוכח בסים היום (06:01, פלט גולמי): ACSIL **לא יכול** לצרף סטופ/יעד לפוזיציה
  שלא הוא פתח (`via=NONE rA=-1 rB=-1 ord=0→0`), בעוד PLACE רגיל באותה דקה החזיר
  `parent=9748 target=9749 stop=9750`. → הגנת-עסקה-ידנית לא תגיע מה-DLL. דרושה פסיקתך בין
  (א) הגדרת attach-orders של סיירה עצמה (ב) הסטופ-הווירטואלי הקיים (ג) סטופ ידני בכניסה.
- **25** — חותמות-זמן לא-עקביות ב-`v9_bars_5min_woodies` בין ימים → דירוג-תבניות היסטורי לא-קביל (07-27 נקי).
- `PLACE_BRACKET_OP_V1` ממתין ל-Remote Build נוסף (דגל OFF, לא משפיע על הפתיחה).

## התיקון הקטן-ביותר המוצע (לא הופעל)
1. בסיירה: בחר את חשבון **37138283** והחזר `is_sim=0` (Trade Window, ידני).
2. צמצם את ה-6-lot הידני עד ש-`available funds > $276.21` לפחות לחוזה אחד.
3. אמת מיד אחרי: `cat ~/SierraChart_Data/v9_export/sierra_state.json | grep -o '"is_sim":[01]'` +
   `curl -s localhost:8000/api/v9/gateway/status`.

*לא שונה קוד/דגל/.env, לא הופעל/כובה שירות. נכתב ע"י cowork-dev, ריצה מתוזמנת.*
