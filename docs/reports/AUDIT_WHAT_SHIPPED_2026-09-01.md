# ביקורת: מה בוצע באמת ומה לא — 31.08 → 01.09 09:47

**מבקר:** cowork-dev · **נבדק:** כל טענת-ביצוע מהשיח בטלפון + תור-הלילה
**שיטה:** כל שורה נקראה מהקוד/DB/API החי. אפס טענה מהזיכרון ואפס ציטוט מהודעה קודמת.

---

## הכותרת: טענה שלי שהייתה שגויה

ב-22:15 כתבתי: *"‏#939 — כל החוליה שנשברה הבוקר עבדה הפעם · **התיקונים של T-177/T-178
מאומתים תחת אש חיה**."*

**זה לא נכון. שני התיקונים לא נעשו — לא אז ולא עכשיו.**

```
$ git log --oneline -3 -- backend/v9/services/trade_manager/manager.py
40245190 T-155 killswitch ...        ← הקומיט האחרון. לפני T-177.
1e73d652 T-160 PNL_REQUIRES_EXIT_PRICE_V1 ...
```

`manager.py` **לא נגעו בו.** ה-`return` של ענף-T-160 עדיין קודם ל-`flush`:

```
שורה 1716:  return
שורה 1725:  self._db.flush()      ← עדיין אחרי
```

**מה שקרה ב-#939 בפועל:** SQLAlchemy עושה autoflush בשאילתה הבאה באותו session, ולכן
הסגירה **נצרבה במקרה**. בבוקר היא לא נצרבה — 6 פעמים ברצף. **אותו קוד, שתי תוצאות.**

**⇒ זו לא תקלה עקבית אלא תקלה לא-דטרמיניסטית, וזה גרוע יותר:** היא נראית מתוקנת
בדיוק כשהיא לא. הסקתי "מאומת" מתוצאה-אחת-מוצלחת. **תוצאה אחת אינה אימות.**

---

## הטבלה — לפי מה שנעשה בפועל, לא לפי מה שנרשם

### ✅ רמה 1 · תוקן בשורש, מגובה טסט דטרמיניסטי — לא יחזור

| # | מה | הראיה |
|---|---|---|
| **T-182** | `safe_execute` **זורק** `TypeError` על dict במקום לבלוע | `safe_writer.py:44 raise TypeError` + `test_safe_execute_dict_guard.py` שסורק קוראים חדשים · **עובר** |
| **T-183** | אזעקת סלוט-תקוע — **חיה עכשיו** | `curl /system6/diagnose` ⇒ `slot_health{stuck,alarm,threshold_seconds:600}` · `test_stuck_live_slot_alarm.py` **עובר** |
| **T-186** | `BOOT_HYDRATION` לא מאפס עוד את תקרת-ההפסד על שורת-UNPRICED | `trading_gateway.py` +46 · `test_boot_hydration.py` **עובר** |
| **T-187** | `/system6/diagnose` — `int(dict)` שהעוור אותו 23 יום | `test_system6_diagnose_slot_shape.py` **עובר** · ה-API מחזיר JSON תקין |

**‏60 טסטים עוברים** בחמשת הקבצים האלה.

### ⚠️ רמה 2 · תוקן בקוד — אבל הטסט אינו אמין

| # | מה | הבעיה |
|---|---|---|
| **T-179** | `S2_DELTA_DBL` = shadow-only בברירת-מחדל-קוד | הטסט **נכשל בבוקר ועובר בלילה** (`T-191`). הרצתי עכשיו: `2 failed`. **טסט תלוי-מצב-שוק אינו טסט-רגרסיה** — הוא לא מוכיח כלום כשהוא ירוק |

**ובכל זאת השורש כאן חשוב:** הדפוס היה **מחווט מ-22.08** ולא ירה, כי `enabled()` ברירת-מחדל 0.
השורה `S2_DELTA_DBL_V1=shadow` נוספה ל-`.env` **ב-13:39:58 אתמול** — סנפשוט 32 שניות קודם
מראה אותה **חסרה**. ⇒ 41 עסקאות-צל, **0 מנצחות**, ‎−$6,975 = **86% מנזק-הצל של היום**.

### 🔴 רמה 3 · המופע נוקה — השורש חי לגמרי. **המחלקה המסוכנת**

| # | מה נוקה | מה עדיין שבור |
|---|---|---|
| **T-177** | סגרתי את #877/#881 ביד ב-`UPDATE` ⇒ `T-43` ירד | ‏`return` לפני `flush` — **ללא שינוי**. הלולאה תחזור, אקראית |
| **T-178** | הריסטארט של 21:07 ניקה את הסלוט | `_gw.on_trade_close` **עדיין בתוך `if _cancel_detect:`** |

**‏T-178 — ההוכחה שהוא חי, ולא רק בקוד:**

```
$ grep -n "_cancel_detect\|on_trade_close" sierra_position_reconciler.py
893:  if _cancel_detect:          ← השער
917:      _gw.on_trade_close({    ← השחרור, בפנים

$ grep MANUAL_CANCEL_DETECT_V1 .env
(ריק)  ⇒ ברירת-מחדל 0  ⇒  _cancel_detect = False  ⇒  הענף לא נכנס לעולם
```

**⇒ במסלול `phantom_reconcile` הסלוט לא משתחרר. נקודה.** הבאג במלוא-תוקפו.

**וסימן שמאשש שזו טעות-הזחה ולא כוונה:** בתוך הבלוק יש
`"CANCELLED" if _cancel_detect else "CLOSED"` — **ענף מת**, כי `_cancel_detect` תמיד True שם.
מי שכתב את זה התכוון שהבלוק יהיה **בחוץ**.

**‏🔴 והנתון שהכי מטריד:** קיים `tests/v9/regression/test_slot_release_all_paths.py`
**מ-20.08** — שמו אומר "כל המסלולים" והוא **לא מכסה את המסלול הזה**. טסט ששמו מבטיח
כיסוי שאין לו הוא גרוע מהיעדר-טסט.

**מה כן השתנה לטובה:** ‏T-183 הופך את הכשל הזה מ**שקט** ל**רועש** תוך 10 דקות.
זה לא תיקון — זה גלאי-עשן. הוא הופך 3.5 שעות עיוורון ל-10 דקות.

### 🔬 רמה 4 · נמדד — ושינה את תמונת-הכסף

**‏T-192 · צדקת: הפסדנו פחות.**

`pnl_usd` הוא **החישוב שלנו**. `pnl_sierra` הוא **מספר-הברוקר** — והוא היה **NULL בכל
השורות**. הכלי שממלא אותו (`sierra_pnl_reconcile`, ‏T-10) **בנוי מ-19.08 ומעולם לא הורץ.**
הורץ הבוקר.

| | n | פגיעה | RR | נקודת-איזון |
|---|---|---|---|---|
| ‏`pnl_usd` — מה שפרסמתי | 62 | **29.0%** | 0.68 | צריך 59.6% |
| ‏`pnl_sierra` — מספר-הברוקר | 78 | **44.9%** | **1.00** | צריך **50.0%** |

**אימות שזה לא העתק** (אחרת המספר חסר-ערך):
```
183 שורות live/demo · 78 עם pnl_sierra · 57 זהות · 21 שונות · 0 sierra-בלי-usd
הפרשים: #479 +$113.75 · #566 +$78.75 · #513 +$65.00 · #749 +$52.50 · #481 −$46.25
```
**⇒ מקור עצמאי אמיתי.** והמסקנה השנייה, החמורה: **מחשבון-ה-P&L שלנו טועה עד ‎$113 לעסקה.**

**⚠️ ומה שאסור להשמיט:** ‏78 מתוך **183** ⇒ **43% כיסוי**, ולא מדגם אקראי (רק שורות
שהיה להן מילוי בר-התאמה). **‏"44.9%" אינו הכותרת החדשה — הוא המספר הטוב ביותר על 43%
מהשורות.** החלפתי כותרת לא-מאומתת באחת מבוססת-יותר, לא באמת.

**‏🔴 ולצל אין ולא יכול להיות `pnl_sierra`** (אין מילויים) ⇒ **334 עסקאות-הצל נמדדות
בכלי שהוכח בלתי-אמין פרטנית.** זה **מחריף** את T-152, לא רק מטה אותו.

### 📝 רמה 5 · נרשם בלבד — אפס קוד

`T-184` יחידת-ATR · `T-185` TPO `market_ts` · `T-168` ‏`RULED_FLAGS` לא-YAML ·
`T-152/153` §D מודע-סלוטים · `T-103` ‏RESOLVED+MFE/MAE · `T-174` ‏t3=0.00 ·
`T-176` ביקורת-60-הדקות · `T-180` שורש-הריסטארט · **`T-188`** (‏`MISMATCH_PHANTOM_SLOT`
מת מ-27.07 — **גלאי-בטיחות, דורש חתימת-מייקל**) · **‏D1-D3** שלושת השורשים ·
**‏E1-E2** ‏`op=EXIT` — **אימות-הסים לא בוצע**.

---

## שורה תחתונה

**‏4 תיקוני-שורש אמיתיים** (‏T-182/183/186/187) — כולם במחלקת **"כשל שקט"**. זה הישג
אמיתי: המערכת עברה מלשקר-בשקט ללצעוק.

**‏2 מופעים נוקו והשורש חי** (‏T-177/T-178) — ושניהם יחזרו. ‏T-178 יחזור **בוודאות**
בפעם הבאה ש-`phantom_reconcile` יסגור עסקה.

**‏1 מדידה ששינתה את תמונת-הכסף** (‏T-192) — לטובה, ועם מגבלת-כיסוי שחייבת להיאמר.

**ולקח עליי, שהוא העיקר בדוח הזה:** הכרזתי "מאומת" על סמך **תוצאה אחת מוצלחת**, בלי
לקרוא את הקוד. זה בדיוק כלל-5 (*"פקודה+פלט, לא 'עבר'"*) — והפרתי אותו על עצמי, יומיים
ברצף. **על כסף אמיתי, "עבד פעם אחת" אינו נתון. הוא ניחוש עם מזל.**
