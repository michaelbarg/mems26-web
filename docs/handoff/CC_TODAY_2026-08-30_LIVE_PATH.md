# פקודת-היום ל-cc · פסיקת "יום צל אחד ⇒ לייב"

**פסיקת-מייקל 30.08:** *"לא רוצה צל ארוך כל כך — זה יום אחד של צל וממשיכים ללייב."*
המדיניות המלאה: `docs/plans/SHADOW_TO_LIVE_POLICY.md`. **זה משנה את סדר-העדיפויות שלך.**

## מה זה אומר בפועל

**לוח-הזמנים החדש:** מחר (31.08) — יום-צל **אחד** לכל מה שמוכן · **מחר בערב** — שער בן
4 בדיקות · **שלישי (01.09)** — לייב למה שעבר.
⇒ **כל מה שלא יהיה מחווט ופועל עד פתיחת-מחר, מפסיד יום שלם.** זו העדיפות.

## עדיפות 1 — שיהיה מה למדוד מחר (הכי דחוף)

**האבחון שהוביל לפסיקה:** רק **2 אירועי-צל** נצברו מאז 26.08 (מהליגר). הצל היה כמעט ריק
ואיש לא ידע. לכן, לפני כל בנייה נוספת:

1. **‏`scripts/shadow_promotion_board.py`** — טבלה יומית: פר-דגל-צל — אירועים-היום ·
   אירועים-מצטבר · ימים-בצל · ‏P&L-מדומה עם עמלות · החטאות · 🟢/🟡/🔴.
   נכתבת ל-`docs/reports/SHADOW_BOARD.md` ומוזרמת לתיק-המוכנות ולטלפון.
2. **התרעת-צל-ריק:** דגל-צל עם **אפס אירועים ביום-מסחר** ⇒ הודעה לטלפון באותו ערב:
   "הצל לא רץ — בדוק את הגלאי". **אפס אירועים אינו סיבה להאריך צל; זו תקלה.**
3. **לוודא שכל דגל-צל באמת רושם ללִיגר** בכל הפעלה (‏`DETECTED`/`GATE_DECISION` עם
   `pattern` מזוהה) — בלי זה הלוח ריק והשער עיוור.

## עדיפות 2 — להשלים חיווט עד פתיחת-מחר (16:30)

בסדר הזה. כל אחד: דגל · `shadow` · מסלול-כבוי זהה-בייט · טסט-מוטציה · `RULED_FLAGS`.
1. **‏`CEILING_LONG_LOCK_V1`** — שער-הנעילה טרם נבנה. עוגן: 3 כניסות-הצל 18:15–18:25 נחסמות.
2. **חיווט `ENTRY_LOCATION_QUALITY_V1` לגייטוויי** — הגלאי קיים, חסר החיבור לשרשרת-השערים.
   עוגן: 17:00/17:35 עוברות · 18:15/18:25 נפסלות.
3. **‏`STRUCTURE_EXIT`** — מחווט ✅. לוודא שהוא **רושם ללִיגר בכל הפעלה** (עדיפות 1.3).

## עדיפות 3 — תיקוני-נכונות: לא ממתינים אפילו ליום
שלושת תיקוני-הסופ"ש (מודל-פוזיציה · נרמול-CVD · מקור-כיוון) הם **תיקוני-באג**, לא
תבניות. הם מתקנים התנהגות שכבר עלתה כסף. הנדרש מהם: **טסט שמשחזר את הכשל** +
**דריל-סים עם ACK מסיירה** (`MODIFY_STOP` · `MODIFY_TARGET` · `FLATTEN_ACCOUNT`) — ואז
בקשת-הדלקה אליי. **הכן את הדריל היום.**

## עדיפות 4 — חוב שחוסם מדידה
מיגרציית-נרמול ה-CVD (~5,498 שורות, snapshot, אחרי 23:00, four-axis). בלעדיה הקונפלואנס
לא ניתן למדידה כלל.

## שער-הערב של מחר (4 בדיקות — זה כל השער)
1. הצל רץ? אירועים > 0 · 2. מה עשה — אירוע-אירוע עם שעה/מחיר/החלטה/תוצאה, **עם עמלות** ·
3. אין נזק ואין סתירה לדוקטרינה · 4. מסלול-כבוי זהה-בייט + ניתן לכיבוי מהטלפון בשנייה.
**עובר ⇒ לייב בשלישי. נכשל ⇒ תיקון ועוד יום. לא "עוד שבוע".**

## כללים (ללא שינוי)
טסטים עם `.env` טעון · רגרסיה מול בסיס (HEAD = 235; לדווח שני מספרים) · **הדלקה:
cowork+מייקל בלבד** · `op=EXIT` אסור · שורת-STATUS_BOARD מאומתת · **`NOT-DONE` מפורש** ·
**בדוק `ast.parse` על כל קובץ-ייצור לפני commit** (אתמול תו שגוי חסם את כל הסוויטה).
