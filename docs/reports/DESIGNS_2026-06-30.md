# MEMS26 · טיוטות-עיצוב — 2026-06-30

לכל בעיה: **שורש · השינוי-המוצע (קובץ/שכבה) · regression שיוכיח · trading-logic (→Michael) או display/safe.**
**אל תממש — טיוטה מוכנה-לפרומפט-CC.** מסומן אם דורש הצלבת-Sierra/לוג תחילה (Rule 2/5).

> **רקע-יום (06-30):** feed-בריא (יום-2). **אפס-עסקאות** ביום-מגמה-עולה-חלק (+44.75pt). **השורש כבר-מאובחן ע"י סוכן-הפיתוח תוך-הסשן** (STATUS_BOARD 09:25/09:45/12:40/14:15) — דוח-זה **מאחד+מתעדף**, לא מאבחן-מחדש. הנושא: שער-המשפחה **over-blocks CONT-LONG** כי ה-day_type שעליו הוא נשען **מרצד+תקוע** (I-44 + D-daytype-stability). תרומת-ה-EOD: כימות עלות = **+3..+9R/יום**.

---

## עדיפות-1 (שורש-העל) — D-daytype-source-consistency (I-44/I-50/I-51) (🔴 trading-risk → Michael) · **handoff קיים**

**שורש (מאובחן-חי היום):** ה-gate (`daytype_position_gate.py`) + S2 Auth Table (`auth_table_v1.py:52,144`) **לא קוראים את ה-day_type ה-7-טיפוסי החי** (`v9_day_type_state`/`classify_replay`/`app.state`), אלא מנוע-ישן/snapshot-תקוע. ראיה: 14:25:04 UTC — `[V2Sizing] (ZLR, Trend_Normal)` (חי) מול `[Gateway] BLOCKED ... balanced day (Normal)` (תקוע) **באותה שנייה**. + UNKNOWN פרה-נעילה → `Auth Table SKIP` (Neutral_Center) חסם INITIATIVE_LONG@14:00. **תוצאה:** כל ה-CONT-LONG (המשפחה-הנכונה ליום-Trend) נחסמו → 0 עסקאות → +3..+9R הוחמצו.

**השינוי-המוצע (כבר ב-handoff `CC_DAYTYPE_SOURCE_CONSISTENCY_2026-06-30.md`):** (1) gate+Auth יקראו אותו מקור-7-טיפוסי-חי; (2) חלון-UNKNOWN-פרה-נעילה + OPEN_DRIVE → הרשאה לפי opening-type ולא Neutral_Center→SKIP. **לא להחזיר Stage-1 — הלוגיקה נכונה, הקלט תקוע.**

**regression:** `test_gate_reads_live_daytype.py` — fire CONT-LONG ביום Trend_Normal → assert לא-נחסם על "balanced". `test_auth_unknown_open_drive_allows.py` — UNKNOWN+OPEN_DRIVE → לא-SKIP. replay 06-30-fixture → CONT-LONG מנותב.

**סיווג:** **trading-risk-surface → Michael (SHADOW first).** **שורש-העל** — מתקן את I-51/I-50/אפס-הירי בבת-אחת.

---

## עדיפות-2 — D-daytype-stability (חדש היום, 🔴 → Michael)

**שורש:** `current_day_type` הפר-בר **מרצד** (UNKNOWN→Trend_Normal→Normal→Variation→Neutral_Extreme→Variation תוך 45 דק'), תחת `LOCKED_LOW_CONF`. החלטות-השער רוכבות על קריאה-רגעית-שבירה — היום עקבי-פנימית, אך אם המרצד יקרא "trend" ברגע שירי-CONT-שגוי נכנס ביום-מאוזן, השער יתיר בטעות. escalation-only לא-נאכף.

**השינוי-המוצע (→Michael):** אכוף escalation-only / hysteresis על `current_day_type` post-lock (אל-תרד-מ-Trend ל-balanced בלי-סף); או הצמד את החלטת-השער ל-day_type-הנעול ולא לקריאה-הפר-בר. מקור: `day_type_machine`/`daytype_position_gate.py`.

**regression:** `test_daytype_no_downgrade_postlock.py` — סדרת-ברים-מעורבת post-lock → assert `current_day_type` לא-מרצד מתחת לסף-hysteresis.

**סיווג:** **trading-risk-surface → Michael.** echoes I-44/D-guards-bus.

---

## עדיפות-3 — D-S4-A7-fire_setup (LIVE-blocker מבני, 🔴 → Michael) · **handoff קיים**

**שורש (מאובחן-חי):** תבניות-וודי יורות (ZLR LONG + GHOST SHORT, דו-כיווני) אך נכשלות ב-A7 (`_a7_universal` `decision_tree.py:351`): `fire_setup=None` למרות patterns+sizing≠reject → `ready_to_route=False` → 0 ניתוב. **חשד:** `fire_setup` (`woodies_system.py:614-823`) נבנה רק אם `best.entry_price and best.stop` (L616), ו-V2-sizing מחשב סטופ בנפרד ⇒ `best.stop=None`.

**השינוי-המוצע (ב-handoff `CC_WOODIES_ROUTE_A7_FIRE_SETUP_2026-06-30.md`):** בנה `fire_setup` לכל routable-pattern מ-V2-stop (לא `best.stop`). **NOT-DONE:** מחזיר ניתוב בלבד — GHOST(REV) עדיין ייחסם ע"י שער-המשפחה ביום-CONT (רצוי); CONT-לונג ינותב ויעבור → העסקה-S4 הראשונה.

**regression:** `test_a7_builds_fire_setup_from_v2_stop.py` — pattern routable + best.stop=None → `fire_setup≠None` · `ready_to_route=True`.

**סיווג:** **trading-risk-surface → Michael.** מבני (לא restart).

---

## D-I51 — כימות over-block (🔴 trading-logic → Michael) · **תת-תוצר של עדיפות-1+2**

**שורש:** = D-daytype-source-consistency + D-daytype-stability (אין שורש-נפרד). **תרומת-ה-EOD = כימות:** CF על setups-אמיתיים-שנחסמו (INITIATIVE_LONG@09:00, ZLR_LONG@09:25) → **+3R כ"א**; short-נגד → −1R. ⇒ over-block עולה **+3..+9R/יום**. **אין עיצוב-נפרד** — נסגר ע"י עדיפות-1+2. **משמש כ-regression-fixture:** replay 06-30 אחרי-תיקון → assert CONT-LONG מנותב+עובר, REV/short נחסם. **שמור 06-30 כ-golden-fixture** (over-block; זוג ל-06-29 under-block).

---

## D-I52 — "Missing data.mode_context" label (🟢 display-only, נמוך)

**שורש:** snapshot post-close מציג S2 `blocked: "Missing data.mode_context"` — אך זהו **מצב-לילה-OVERNIGHT תקין** (S2 נדרך+פלט setups ב-RTH). **לא-באג.**

**השינוי-המוצע (safe, אופציונלי):** ב-`build/pattern-status`, label post-close כ-`blocked: overnight (expected)` במקום `missing-input` ⇒ הסרת false-positive-אבחוני.

**regression:** `test_pattern_status_overnight_label.py`. **סיווג:** **display/safe, עדיפות-נמוכה.**

---

## D-daytype-source-endpoint (I-44, display) — endpoint קפוא (🟡 safe)

**שורש:** `/day_type/state` החזיר ערכים **זהים-בית-לבית ל-06-29** ⇒ wrapper-מת/קפוא (נפרד מ-gate-source לעיל, אך אותו שורש-משפחתי). מטעה כל-אבחון-day_type + ייתכן מקור ל-I-1.

**השינוי-המוצע (safe):** הפנה `/day_type/state` ל-`app.state.day_type_machine`/`v9_day_type_state`; בטל wrapper-מת. **regression:** `test_daytype_endpoint_not_frozen.py` (session_min מתקדם). **סיווג:** display→safe. (כדאי לאחד עם עדיפות-1 — אותו מקור-אמת.)

---

## D-limit-cap (I-25, 🟢 safe/quick)

**שורש:** `/trades/recent?limit=200`→422. **שינוי:** הרחב cap ל-≥200 או עדכן ספֵק ל-100. **regression:** `test_trades_recent_limit_200_ok.py`. **safe.**

---

## ממשיכים — פתוחים

- **⚪ D-guards-bus (I-48+D-037+D-049+I-23) — חוסם-LIVE, לא-נבחן היום** (0 fills). עיצוב מ-DESIGNS_2026-06-29 עומד. **trading-safety → Michael.** דורש הצלבת-CC.
- **🟡 D-lag-validation (I-46) · D2/D9 (I-31/I-32):** safe, לא-שוחזרו היום. נשארים מומלצים.
- **⚪ D35 · D-targets · D-I47-root · D-I40:** ללא-שינוי.

---

## סדר-עדיפויות מוצע

1. **D-daytype-source-consistency (I-44)** — שורש-העל; handoff קיים; מתקן אפס-הירי/I-51. →Michael, SHADOW.
2. **D-daytype-stability** — flicker; →Michael.
3. **D-S4-A7-fire_setup** — handoff קיים; →Michael. (#1 ZLR-divergence verdict 07-01 09:45 CT.)
4. **D-daytype-source-endpoint (I-44 display) + D-limit-cap (I-25)** — safe, מהירים.
5. **D-guards-bus** — חוסם-LIVE מ-06-29; להצליב ישירות.

**הערה:** השורש-של-היום **כבר-מאובחן** (2 handoffs קיימים + D-daytype-stability) — דוח-זה **מאחד+מתעדף+מכמת**, **לא מאבחן-מחדש** (Rule 2). פריטי trading-risk → **Michael**. **שום-קוד לא-שונה בריצה-זו** (read-only EOD).
