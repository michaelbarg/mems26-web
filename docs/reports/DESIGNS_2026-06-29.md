# MEMS26 · טיוטות-עיצוב — 2026-06-29

לכל בעיה מאומתת: **שורש · השינוי-המוצע (קובץ/שכבה) · regression שיוכיח · trading-logic (→Michael) או display/safe.**
**אל תממש — טיוטה מוכנה-להפיכה-לפרומפט-CC.** כל פריט מסומן אם דורש הצלבת-Sierra `v9_export` תחילה (Rule 2/5).

> **רקע-יום (06-29):** feed-בריא (I-47 לא-שוחזר). 4 INITIATIVE_SHORT בשפל-היום (7409) ביום שעלה 96pt; כולן נעצרו (−4R). תחזית-נגד: הסטופים הצילו ~28R. כל מפסקי-הזרם (cooldown/cluster/SSV/counters) inert מול 4-stops. הנושא של היום: **בחירת-כיוון שגויה + שכבת-הגנה מנותקת** — לא יעדים/feed.

---

## D-guards-bus — גוש מפסקי-הזרם המנותקים (I-48 + D-037 + D-049 + I-23) (🔴 trading-safety) · **חוסם-LIVE**

**שורש:** 4 fills (כולל 1 demo) + 4 stops רצופים, ו-**אף גייט לא נדרך**: `cooldown.consecutive_stops=0` · `cluster_guard.recent_attempts=0` · `ssv.recent_outcomes=0` · `gateway.{trades_today,daily_pnl,shadow_active_count,consecutive_losses}=0`. כולם מזינים-מ/קוראים-את אותו זרם-fills, וה-**מונה לא קולט shadow/demo-fills** ⇒ כל ארבע שכבות-ההגנה inert בו-זמנית. זה לא 4 באגים — זה **באג-מקור-אחד עם 4 צרכנים**.

**צעד-0 (אבחון, CC, חובה-לפני-קוד):** אתר את נקודת-העדכון של מוני-הגייט ב-`backend/v9/gateway/trading_gateway.py` (חפש `consecutive_stops`, `recent_attempts`, `recent_outcomes`, `trades_today`). קבע: (א) מאיזה event הם אמורים-לעלות (trade CLOSED/FILLED)? (ב) האם ה-event-הזה נשלח עבור shadow/demo או רק live? (ג) האם כולם תלויים במונה-יחיד או בארבעה-נפרדים.

**השינוי-המוצע:**
1. **מקור-אמת-אחד למוני-יום (safe):** חווט `trades_today / daily_pnl / shadow_active_count / consecutive_losses` ל-`v9_trades` (כולל shadow+demo) — אותה שאילתה שמזינה את top-bar (שמציג-נכון). זהה ל-D-gateway-counters מ-06-26.
2. **חיווט gate-counters ל-fill-event (trading-safety):** `consecutive_stops` (cooldown), `recent_attempts` (cluster), `recent_outcomes` (SSV) — כולם יתעדכנו ב-trade-close-event **כולל shadow/demo (לפחות לצורך מנייה+לוג)**. מדיניות-החסימה נשארת בשליטת-Michael (ר' להלן), אך ה-**ספירה** תמיד-אמיתית.
3. **logger.warning כשנדרך** (§No-silent-failures) — לא debug.

**regression שיוכיח (anti-tautological — מול fills, לא מול-עצמו):**
- `test_guard_counters_ingest_shadow_fills.py` — הזרק 4 shadow-STOP רצופים באותו-כיוון תוך-60s → assert `consecutive_stops==4` · `cluster_guard.recent_attempts==4` · `ssv.recent_outcomes==4` · `trades_today==4`.
- `test_cooldown_trips_after_2_stops.py` — STOP-3 אחרי 2 → `cooldown_active==true` (ב-demo/live).
- `test_cluster_guard_trips_on_same_dir_burst.py` — 3 כניסות-אותו-כיוון/60s → `cluster_guard_active==true`.

**סיווג:** מוני-התצוגה (D1) = **safe**. חיווט ה-gate-counters והאם הם **חוסמים** demo/live = **trading-logic/risk → אישור-Michael**. **חוסם-LIVE** (זהו מפסק-הזרם של LIVE). הצד-המנייתי safe ויכול להיכנס מיד; ההחלטה אם cooldown/cluster/SSV **חוסמים בפועל** = Michael.

**הערה:** D-037 ו-D-049 נבנו **בדיוק** לתרחיש-של-היום (אשכול-אותו-כיוון, suffering-side) אך inert ⇒ עדיפות-עליונה. שילוב עם D-I48/D-gateway-counters מ-06-26 = אותו תיקון-מקור.

---

## D-I50 — אשכול-שורט-נגד-מגמה בנקודת-היפוך (🔴 trading-logic) · **→ Michael** · ר' thread #68/playbook

**שורש (חשוד, דורש הצלבת-Sierra תחילה):** 4 INITIATIVE_SHORT נורו 09:15–09:24 CT בשפל-היום 7409 (בר-היפוך L7409/C7425.75), על `woodies=RED` (flicker-בוקרי), מול `day_type=Trend_Normal`/`opening_type=OPEN_DRIVE`. שלושה כשלים-אפשריים, לא-בלעדיים:
- (א) **trend-mismatch:** המנוע פעל לפי woodies-RED-flicker ולא לפי day_type=Trend_Normal (cci_14 התהפך ל-+59 עד-הנעילה — ה-RED היה חולף, I-15).
- (ב) **אין reversal/exhaustion-filter:** short נורה בבר עם סגירה גבוהה-משמעותית-מהשפל (C7425.75 מול L7409 = +16.75pt off-low) — חתימת-היפוך קלאסית.
- (ג) **day-type×direction gate לא חוסם counter-trend-short ביום-Trend** (DAYTYPE_PLAYBOOK חוסם HFE-on-Trend אך כנראה לא INITIATIVE_SHORT-on-Trend_Normal).

**צעד-0 (אבחון, CC, הצלבת-Sierra — חובה):** `v9_export` סביב 09:10–09:25 CT — ערך woodies/CCI-14 הגולמי לבר-09:15; האם RED אמיתי-ב-Sierra או artifact-backend (I-15). + קרא `five_min_system._detect_initiative` — על מה-בדיוק נדרך SHORT, ומה היחס ל-day_type.

**השינוי-המוצע (לדיון, לא-מוכרע — →Michael):** אחד-או-יותר:
1. **day-type×direction gate מורחב:** ביום `Trend_Normal`/`OPEN_DRIVE` עם כיוון-יום מזוהה — דכא counter-trend INITIATIVE (short ביום-up). מקור: `gateway/trading_gateway.py` (DAYTYPE_PLAYBOOK decide()).
2. **reversal-bar filter ל-INITIATIVE:** חסום INITIATIVE_SHORT אם בר-ההדק סוגר >K·ATR מעל-השפל-שלו (התאוששות = היפוך-אפשרי). מקור: `five_min/five_min_system.py`.
3. **trend-source reconciliation:** אל-תדליק INITIATIVE לפי woodies-RED אם day_type/opening סותרים — דרוש קונצנזוס. (קושר I-15.)

**regression:** backtest פר-day_type — assert ירידה ב-counter-trend-INITIATIVE ביום-Trend ללא-פגיעה ב-with-trend; replay 06-29 → 0 short ב-09:15 / +LONG-fired. **דורש feed-תקין** (06-29 תקין — שמור כ-fixture).

**סיווג:** **trading-logic/risk → אישור-Michael.** מתחבר ל-thread S1-recalibration + DAYTYPE_PLAYBOOK. **דורש הצלבת-Sierra תחילה** (לאמת ש-RED הוא flicker-אמיתי ולא artifact). **שמור 06-29 כ-golden-fixture** — יום-מגמה-נקי-עם-shakeout, נדיר-וברור.

---

## D-lag-validation (I-46/I-20) — freshness משקר על lag-שלילי (🟡→🔴 safety-net · ר' D-I47 §2)

**שורש:** bridge `lag_seconds=−10723 (בר-עתידי) · fresh=true · last_bar_ts=null`. lag-שלילי = בלתי-אפשרי, אך הדגל עדיין `fresh=true`. אף שאין-שכפול היום, **רשת-הביטחון של D-I47 §2 (ולידציית-lag-שלילי) טרם-מומשה** ⇒ אם I-47 יחזור, הגייט שוב-לא-יחסום.

**השינוי-המוצע (safe):** ב-מחשב-ה-freshness (`build/pattern-status` + bridge stream): `lag<0 ⇒ fresh=false` + `logger.warning` (rate-limited). הפרד `data_current` (תמיד) מ-`fresh_for_trading` (gate). זהה ל-D36/I-46 מ-06-26.

**regression:** `test_freshness_rejects_negative_lag.py` — בר עם ts עתידי → `fresh=false` + warning.

**סיווג:** **safe/display-adjacent** (לא-נוגע-ירי). מהיר. מומלץ-לבנות-מיד כרשת-ביטחון נגד-הישנות-I-47.

---

## D-daytype-source (I-44) — פיצול-מקור day_type (🟡 display→מטעה-אבחון)

**שורש:** trade-stamp=`Trend_Normal` (מסווג-7-טיפוסים, `S1_NEW_CLASSIFIER=1`) ↔ `/day_type/state` endpoint=`Variation/0.18` (ה-wrapper-המת — CLAUDE.md §Index מתעד שה-endpoint קורא instance-מת; האמת ב-`app.state.day_type_machine`+`v9_day_type_state`).

**השינוי-המוצע (safe):** הפנה את `/day_type/state` ל-`app.state.day_type_machine` (אותו מקור שמטביע על trades) ⇒ endpoint↔stamp תואמים. בטל/הסר את ה-wrapper-המת.

**regression:** `test_daytype_endpoint_matches_stamp.py` — fire shadow-trade → assert `trade.day_type == /day_type/state.day_type`.

**סיווג:** **display/observability → safe.** מסיר מקור-בלבול-אבחוני קבוע. (קושר I-1: gם `session_min=0`/`vote_history=[]` ייתכן-מקורם ב-wrapper-המת.)

---

## D2 (I-31) · D9 (I-32) — over-count + gaps (🟡 display/diagnostic)

**שורש:** `five_min.fired_today_count=5` מול DB-S2=4. + gap-ids 253/254/255 (252→256). או double-count-תצוגה או fire-שלא-persisted.

**צעד-0 (CC):** `SELECT id FROM v9_trades WHERE id BETWEEN 252 AND 256;` + לוג-backend סביב ה-insert. אם sequence-advanced-on-rollback → תמים; אם 5-fires-ב-engine מול 4-rows → persist-fail-שקט (חמור-יותר, §No-silent-failures).

**השינוי-המוצע:** (D2) תקן join/group ב-build_status להתאמה-מדויקת על `pattern_id` (לא LIKE/firing_system) ⇒ fired_count==DB. (D9) אם persist-fail — הוסף `logger.warning` + retry/assert ב-insert-path.

**regression:** `test_fired_count_matches_db.py` · `test_no_silent_insert_gap.py`.

**סיווג:** D2 = **safe** (display). D9-אבחון = safe; אם persist-fail אמיתי → trading-adjacent (עסקה-שאבדה).

---

## ממשיכים מ-06-26 (לא-הוחמרו היום, נשארים פתוחים)

- **D-I47 (שכפול +3h):** 🟢 **לא-שוחזר היום** — אך השורש (promoter/ingest) טרם-אומת+נסגר ע"י CC. רשת-הביטחון (ingest-dedup + D-lag-validation) **עדיין-מומלצת** למניעת-הישנות. נשאר חוסם-LIVE עד-שהשורש מאומת + dedup קיים.
- **D35 (EOD-flatten + reconcile-at-last-known):** 🟢 נקי-היום (4/4 CLOSED, אין-יתום) — אך הלוגיקה טרם-נבנתה; יחזור-להיות-רלוונטי ביום עם פוזיציה-פתוחה-ב-EOD. נשאר חוסם-LIVE.
- **D-targets (I-49):** לא-רלוונטי-להיום (היעדים לא-נגעו כי הכיוון היה שגוי, לא כי היו מחוץ-לטווח). נשאר →Michael, אחרי D-I50.
- **D-I40-residue (id247):** הצלבת-Sierra עדיין-פתוחה.

---

## סדר-עדיפויות מוצע (חוסמי-LIVE קודם)

1. **D-guards-bus** (I-48+D-037+D-049+I-23) — מפסק-הזרם של LIVE; היום הוכיח שכולו inert. תיקון-מקור-אחד. **חלק safe (מונים) מיד; חלק חוסם → Michael.**
2. **D-lag-validation** (I-46) — רשת-ביטחון safe נגד-הישנות-I-47. מהיר.
3. **D-I50** (short-cluster נגד-מגמה) — הצלבת-Sierra → day-type×direction/reversal-filter. **→Michael.** שמור 06-29 כ-fixture.
4. **D-daytype-source** (I-44) + **D2/D9** (I-31/I-32) — תחזוקת-אבחון safe.
5. ממשיכים: D-I47-root · D35 · D-targets · D-I40 (הצלבות-Sierra).

**הערה:** 3 פריטים דורשים הצלבת-Sierra `v9_export` תחילה (D-I50, D-I47-root, D-I40). פריטי trading-logic/safety → **אישור-Michael**: D-guards-bus (חלק-החוסם), D-I50, D35, D-targets, וכל re-enable-של-גייט-מושבת. היתר display/safe.
