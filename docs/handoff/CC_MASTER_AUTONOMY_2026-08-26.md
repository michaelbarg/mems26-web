# פקודת-אב · המערכת סוחרת אוטונומית — cc מבצע, cowork מנהל

**מאת:** cowork-dev · **אל:** cc-macbook · **תאריך:** 2026-08-26
**פסיקת-מייקל 26.08:** *"תשלח לקלוד קוד את עבודת הפועלים ותהיה עסוק בתכנון, יישום,
בדיקה והשגחה שכל הצינורות מחוברים."*
**‏§0 חל:** אתה מחליט, מתעד, ממשיך — לא שואל. עצירה רק בשערי-פאזה (cowork) ובפסיקות (מייקל).

## התכלית — ולמה היא לא מתקיימת

המערכת מזהה, מסננת, מנהלת — **ולא סוחרת.** שלושה מונעים, כולם מאובחנים עם ראיות:

| # | המונע | הראיה |
|---|---|---|
| A | **‏`pre_send_entry_guard` חוסם הכל** — פוזיציה זרה על החשבון | ‏19/19 ב-25.08, 8/8 ב-24.08 — **בדיוק הימים עם אפס-לייב**. `entry_guard.py:112-131` |
| B | **אין מחולל-רוטציה** — `Variation` (55% מהספר) נסחר כמומנטום | ‏25.08: 4 רוטציות VAL↔VAH, אפס ירי-דוקטרינה; הצל דהה **הפוך** ‎−$863.75, ושתי העסקאות שדהו נכון — **שתיהן ניצחו** |
| C | **שרשרת-הכיוון מנותקת ממערכת 1** | ‏`v9_day_type_state.direction` מאוכלס והגייטוויי קורא רק `confidence` |

מה שכבר בנוי (1f2ac909): ‏liveness ב-`flag_guard` ✅ · שורש-1 `app.state` בנוי-OFF ·
שורש-2 `pattern_family` בנוי-OFF · אבחון-סטופ (75 דריסות, 89% מהדקות).

---

# פאזה 1 · לשחרר את הירי (היום)

### 1.1 · שומר-הכניסה מודע-בעלות
‏`entry_guard.py:112-131` חוסם על כל `pos != 0`. להרחיב לפי המנגנון הקיים
`RECONCILER_OWNERSHIP_AWARE` (פסיקת 24.07): פוזיציה שאינה-של-TM **ואינה מוסברת** ⇒ חסימה
(כמו היום); פוזיציה מוסברת (מכונה-2 / שיוך ידני של מייקל) ⇒ ירי מותר עם תיוג.
דגל `ENTRY_GUARD_OWNERSHIP_V1`, ‏OFF. **טסט-מוטציה + סימולציית שני התרחישים.**
**🔴 פסיקת-מייקל נדרשת:** מי מחזיק את ‎-3 על 37138283? עד הפסיקה — הדגל בנוי ולא דולק.

### 1.2 · להעיר את שורש-1 בצל
‏`APP_STATE_ROOT_FIX_V1=shadow` — לוג משווה ישן-מול-חדש בלי לשנות פסק. סשן אחד, ואז §D.

### 1.3 · ‏`RESOLVED` + תיקון-DB — שתי הפקודות שכבר כתובות
‏`CC_WORKORDER_RESOLVED_2026-08-25.md` + `CC_WORKORDER_DB_REPAIR_2026-08-25.md` (שלבים 1-5
קריאה-בלבד ביום, כתיבה אחרי 23:00). **לא בוצעו — לבצע.**

**שער-פאזה-1 (cowork):** שומר בנוי+טסטים · שורש-1 צל רץ · resolver כותב RESOLVED ·
‏dry-run של ה-DB מודפס.

---

# פאזה 2 · מחולל-הרוטציה — `VA_FADE_V1` (יום 2-3)

**התכונה החסרה.** לא מסנן — **מחולל** שפולט מועמד:

```
תנאי-לונג:  day_type ∈ {Variation, Normal, Neutral_*} AND direction=fade_both (או אין-מגמה)
            AND low ≤ VAL+tol AND בר-דחייה (סגירה חזרה מעל VAL)
            ⇒ candidate VA_FADE_LONG · סטופ מתחת לקצה-הדחייה · יעדים POC / VAH
תנאי-שורט:  מראה.
```

- ‏ADAPT את `edge_fade.py` (`:73,:176` — השלד הקיים): להחליף `day_hi/day_lo` ב-VAH/VAL
  מ-`/api/v9/key_levels` (המקור שהגייטוויי כבר קורא — **לא** `v9_tpo_sessions` הארטיפקט).
- נכנס לשרשרת כמו כל תבנית: emit → gateway → כל השערים חלים. **בלי עקיפות.**
- ‏`_decide_variation` (`daytype_position_gate.py:266-305`) — לתקן את הסתירה: ביום-איזון
  דהיית-VA היא **עם** הדוקטרינה, לא "נגד ההרחבה".
- דלתון בינארי: אפס ספי-אחוזים. בר-דחייה = הגדרה גיאומטרית.
- ‏**§D על 26 ימי-ה-Variation ההיסטוריים** + ‏25.08 כעוגן (4 הרוטציות חייבות להניב מועמדים
  בכיוון-הדוקטרינה). ‏shadow ≥ 3 סשנים לפני פסיקת-הדלקה.

**שער-פאזה-2:** ‏§D חיובי · עוגן-25.08 עובר · צל רץ.

---

# פאזה 3 · הכיוון ממערכת 1 (יום 3-4)

‏`S1_DAY_DIRECTION_V1` מ-shadow ל-apply — עם המקור המלא:
`with_extension(X)` ⇒ דרישת-כיוון X ב-`Trend_*` · `fade_both` ⇒ **אין דרישת-כיוון**
(מזין את VA_FADE) · חלקי/ריק ⇒ `UNDETERMINED`. לתקן את הפרסר (ערך בלי סוגריים).
‏§D + שני מבחני-הסיכון (‏`Trend_Normal`×SHORT משתפר · "בתוך-IB" +3.93 לא נפגע).
ולהעיר את שורש-2 (`pattern_family`) באותו מסלול: צל → §D → פסיקה.

**שער-פאזה-3:** ‏§D עובר · הצל מ-25-26.08 מאשר הסכמה בין המקורות.

---

# פאזה 4 · המכריע — `CONTEXT_ENTRY_V1` (שבוע 2)

הכלי שמייקל ביקש: מקבל מועמד (S2/S4/VA_FADE) + מצב-מבני (S1) + מיקום + רמות + זרימה ⇒
`ALLOW / SKIP / WAIT / EXPIRE` + מדיניות סטופ/יעדים. ‏pure function, זהה-בייט
ב-unit/replay/shadow, לפי החוזה בתוכנית-קורסור Task 5. מחליף בהדרגה את ערימת-השערים —
**שער אחד שרואה הכל במקום 20 שרואים חלקים.** ‏shadow ≥10 סשנים. חסום עד ששלב-2 ירוק.

---

# מקבילית — ניקוי הדגלים (רץ ברקע כל הזמן)

מ-`FLAG_AUDIT_2026-08-25.md`: לכל ❌/⚠️ שלא כוסה בפאזות — לתקן או להסיר לפי הסדר:
‏`SCALE_IN_V1` (‏`stop_price`→`stop`) · `RISK_HALT_V1` (‏P&L גם מצל) ·
`OPENING_FIRST_TRADE_STRICT_V1` (קם עם שורש-1) · `PATTERN_RISK_CAPS` (סדר-חישוב) ·
‏31 ה-❓ ימדדו מהליגר. **דגל שמת ואין לו הצדקה — להסיר עם רישום, לא להשאיר זומבי.**

---

## חוקי-הביצוע (כולם, בלי יוצא)

1. דגל נפרד לכל שינוי · OFF · זהה-בייט · **טסט-מוטציה** · `RULED_FLAGS` באותו commit.
2. ‏§D לפני כל הדלקה: דלתא **וגם** חציון-יום, live/shadow בנפרד, ספירת שינויי-פסק
   (‏0 = NOT_JUDGEABLE).
3. ‏**הדלקות = cowork+מייקל בלבד.** אתה בונה עד השער ועוצר.
4. אסור: `op=EXIT` · כתיבת-DB בזמן סשן · ריסטארט אחרי 16:10 · סינתזת ערך חסר ·
   אחוזי-ביטחון בשרשרת · `logger.debug` על כשל.
5. כל "עבר" = פקודה+פלט גולמי. דוח לכל פאזה: `docs/reports/CC_PHASE<N>_<date>.md` +
   ‏LOG ב-`LIVE_CHANNEL` + commit+push.

## חלוקת-העבודה

**cc:** כל הקוד, הטסטים, הרפליי, הדוחות. **cowork:** אימות פר-שער, §D-ביקורת, הדלקות,
ניטור-צינורות יומי (feed·flag_guard·fire_drill·ledger·shadows), תיקונים קטנים בלבד.
**מייקל:** שתי פסיקות פתוחות — (1) בעלות ה-‎-3 על 37138283 (2) סטופ: ‏StopResolver
או StepLadder, על בסיס אבחון-75-הדריסות.
