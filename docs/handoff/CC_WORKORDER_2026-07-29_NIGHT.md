# CC — הזמנת-עבודה לילה 29→30.07 (פסיקת-מייקל: "שום מחסום שמייצר כושלות + זיהוי-עצמי מלא")

פעל לפי `CC_HANDOFF_CONTRACT.md` (חוק-5 · NOT-DONE חובה · anti-tautological). הכל
flag-gated OFF אלא אם צוין; cowork מאמת ומדליק. **סדר מחייב P0→P4.** רקע-חובה:
`LIVE_CHANNEL.md` ערכי 21:40+21:45 של היום — ביקורת 60/0, ‏29 מנצחות שנחסמו, התיקונים
שכבר חיים (029581d7, c6381035).

## P0 — שורש ה-5h-skew + שומר-תפרים v2 (לפני פתיחה!)

הוכח היום בלייב: ‏ts בייצוא `5min.json` מפגר **5 שעות** מהזמן האמיתי (אמת = epoch+5h;
אומת 1:1 מול קריאת-לייב), שני נתיבי-קליטה מתקנים אחרת ⇒ ברי-לילה דרסו את חלון-הפתיחה
⇒ ‏conf=0.0 כל היום ⇒ כל שערי-ה-playbook עבדו על רעש.

1. **מצא ותקן את מקור ה-skew בנקודה אחת** — DLL (חותם זמן-צ'ארט כ-epoch?) או ברידג'.
   תיקון-ts יחיד בכניסה לצנרת; מחק/אחד כל תיקון-כפול (`WOODIES_TS_HOUR_FIX` וחבריו).
   טסט: בר-ייצוא טרי ⇒ ts קנוני UTC אחד בכל הצנרת.
2. **שומר-תפרים v2** — שלושה פגמים מוכחים היום:
   א. **נעילת-זיהום**: הבר האמיתי נדחה 1,063 פעמים כי ה-slot ב-DB כבר הכיל זבל.
      בר-נכנס שסותר slot-קיים ⇒ **quarantine לטבלת-צד + אזעקה**, לא דחייה-שקטה.
   ב. **שכן-בתוך-batch**: הפוש משווה לשכן-ב-batch ולא לשכן-בזמן — ב-21:46 ברים
      אמיתיים (20:40) נדחו מול prev_HL=(7469.25,7460.50) — ערך **שלא קיים בסשן היום**
      (שיא 7461). מצא מאיפה הבר הזר ותקן את בחירת-השכן (לפי ts-סמיכות, לא סדר-הגעה).
   ג. **150,309 שורות-לוג** לאותה דחייה — rate-limit לפי ts (פעם ב-5 דק' לכל bar-ts).
3. חקור מי כתב את ברי-הזבל — **דגימה חיה מדויקת מהערב**: בין 21:41 ל-21:56 IL
   ברי-הראלי החיים (21:45–21:55) נכתבו *גם* בהיסט −1ש' לתאי 20:45–20:55 ודרסו את
   הברים האמיתיים, בלי שהשומר עצר (הנתיב עוקף אותו). זה קרה פעמיים היום (גם חלון-
   הפתיחה 16:30). חלון-זמן צר + timestamps מדויקים = מצא את הכותב וסגור אותו.

## P1 — סיווג-יום v2: ‏Neutral round-trip + DD (שאלת-מייקל הערב: "היום התהפך לנייטרלי — למה לא זוהה?")

שני ימי-דגימה עם אמת-ידועה:
- **07-29 (היום):** ‏80 נק' מטה ⇒ היפוך-V מלא חזרה לאזור-הפתיחה = **Neutral** (דלתון:
  הרחבה שנכשלת + חזרה-לערך). ‏classify_replay נתן `Normal_Variation` + "accepted
  prior_VA break UP" — כלומר קרא את ההתאוששות כהמשך-כיווני במקום חזרה-לאיזון.
  **חסר כלל-reclass**: הרחבה שחזרה >70% ונסגרת בתוך ערך/טווח-הפתיחה ⇒ ‏Neutral
  (escalation-only נשמר: רק round-trip מלא מתיר את ה-reclass הזה, עם לוג).
- **07-28 (אתמול):** ‏DD לא סווג. היום ה-classifier כבר רואה: `bar.bimodal=True`,
  ‏neck 7409–7451, ‏second_ratio 0.932 — אבל `detected=False` כי מסלול-הפרופיל לא
  מאשר. אבחן למה (מקור-פרופיל חסר? סף?) ותקן כך ששני המסלולים מצביעים ⇒ DD.
- ‏Trend מוקדם (D2 המקורי): נשאר בתוקף — escalation-only + אחרי נעילת-IB בלבד.
טסטים: ‏replay של שני הימים מפיק Neutral (29) ו-DD (28) + יום-טרנד אמיתי עדיין Trend.

## P2 — עצירת איתותי-boot-replay (בטיחות)

בעליית-backend ‏18:27:56Z נפלטו איתותים @5900/@5250 (מחירי-זבל) — נבלמו רק ע"י
feed_watchdog. אנטי-פנטום גלובלי **ברמת-הפליטה** (כמו בתיקון-הפתיחה): איתות נוצר רק
אם גיל-הבר < 10 דק' מזמן-אמת **וגם** ‏|entry − close-אחרון| < 2% ⇒ ‏hydration/replay
לא מייצרים איתותים לעולם. טסט: הזרמת יום-שלם היסטורי ⇒ 0 איתותים.

## P3 — כיול lsma_flat (נתון-הערב)

‏21:30: ‏ZLR LONG נחסם על שיפוע 0.2133 מול סף 0.25 — והשוק עלה 29 נק' ב-10 דק'.
‏0-חסך/2-עלה היום. הרץ את ‏release_gate_review על 10 ימי-החלטות אחרונים עבור
lsma_flat בלבד ⇒ המלצת-סף מגובה-נתונים (או הורדת-scope מ-ALL). **אל תשנה סף בלי
הנתון** — דוח בלבד, מייקל פוסק.

## P4 — היגיינת-דגלים

‏`RELEASE_TREND_BYPASS_PTS` (15) + `DAYTYPE_PLAYBOOK_MIN_CONF` (0.4) —
רישום ב-`FLAG_REGISTRY.yaml` + ‏`gen_flag_index` + ‏flag_guard ירוק.

## P5 — שני חוסמי-S2 פנימיים (נמצאו 22:00, הרגו את עסקאות-הערב לפני השערים)

1. **pre_fire_validator R:R — מודל-reward שבור בפריצה:** ‏21:40 ‏DOUBLE_BOTTOM_EE_LONG
   ‎@7444 נדחה "R:R<1.0 (risk 33.75, reward 16.34)" — ה-reward נמדד עד השיא-הישן
   (7461−7444=16.3) בזמן שהשוק פורץ לשיאים חדשים (רץ ל-7482). ביום-פריצה מדידת-reward
   לשיא-הישן = דחייה אוטומטית של כל פריצה. תקן: מצב-breakout (מחיר בתוך 5 נק' מקצה-
   הסשן או מעברו) ⇒ reward = measured-move (גובה-הבסיס) או ‏ATR-multiple, לא המרחק
   לקצה-הישן. flag-gated + טסט על 21:40 (חייב לעבור) + על פייד-ברוטציה (ללא שינוי).
2. **Auth Table על תווית-רעש:** ‏17:00 ‏INITIATIVE_SHORT × ‏UNKNOWN ⇒ SKIP (העסקה
   הגדולה של היום) · ‏21:55 ‏INITIATIVE_LONG × "Normal" (ביטחון-אפס) ⇒ SKIP. אותה
   מחלה שתוקנה הערב ב-playbook-הגייטוויי, יושבת גם ב-setup_emitter. הצעה (ממתינה
   לפסיקת-מייקל, אל תדליק): ‏conf<0.4 ⇒ ‏SKIP-של-הטבלה הופך ל-REDUCED (2 חוזים)
   במקום 0. בנה מוכן-להדלקה + טסטים; ההדלקה רק אחרי מילת-מייקל.
3. באג ‏TradeManager: ‏InvalidTransition ‏PARTIAL→PARTIAL ב-on_target_hit (ספאם-לוג;
   ישבור רישום ‏T2 אחרי ‏T1 בעסקה חיה) — הפוך ל-idempotent (כבר PARTIAL ⇒ אל תעבור
   מצב, רק רשום את היעד). טסט: ‏T1 ואז ‏T2 על אותה עסקה ⇒ אין חריגה.

## קבלה (חוק-5)

רגרסיה ירוקה · replay שני-הימים עם פלט גולמי · flag_guard · דוח
`docs/reports/CC_NIGHT_2026-07-29_REPORT.md` עם פקודות+פלט, לא "confirmed".
**אסור:** op=EXIT · דגלים-פסוקים · הדלקה-עצמית · reconciler.
