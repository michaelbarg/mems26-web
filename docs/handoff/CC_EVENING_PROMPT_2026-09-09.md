# תור-הלילה 09.09 (אחרי 23:00) — **הערב שבו הוראה חדשה נחתה ואין לה עדיין מספר**

**נכתב ע"י cowork-scheduled, נמדד 23:06 (‏`date` נקרא ברגע-הכתיבה).**
לא היה קובץ-ערב ל-09.09 עד עכשיו. הסדר להלן הוא הסדר הפסוק
(‏DB → מחזיק-שני-S1-BINARY → שערים → ריסטארט-צל → resolver/WHATIF → fills/Redis → דוח-EOD).

---

## מצב-פתיחה (נמדד, לא מזיכרון)

```
23:00-23:05  health 200 / 1.6ms · backend PID 16477 (מ-18:32:46)
             בר-ווּדיס 22:55:00, גיל 0.3 דק'
             position_qty=0 · working_orders=0 · live_slot=null
             daily_total_qty_filled=98 · acct_available_funds $3,610.09
ERROR/CRITICAL מ-21:00 (מעוגן-תאריך $1=="2026-09-09")  ⇒  0
QueuePool מ-21:00                                       ⇒  0
QueuePool אחרון: 16:51:58  ⇒  T-284 מחזיק ~6ש'
```

### ספרי-הלייב של היום (גולמי)

```
#1328 VEGAS      LONG  @7643.25  19:05:06 → 19:07:48 STOP_HIT   -$40.00
#1337 DOUBLE_BOTTOM_EE_LONG @7651.25  entry_ts=NULL  → 20:46:11  +$30.00  ← T-290, פנטום
#1343 BULL_FLAG  LONG  @7652.50  20:55:08 → 21:08:16 STOP_HIT  -$131.25
                                              סה"כ ספרים: -$171.25
צל: 60 עסקאות · -$1,611.25 · 26 בלי מחיר-יציאה ⇒ הסכום מעורב-בסיס, אסור לצטט כמספר אחד
```

**חלון 22:50 (T-289) עבר כ-no-op** — 34 דגימות `position_qty=0` מ-22:39 עד 22:55,
אפס שורות `T-10`/`FLATTEN_ACCOUNT`, `command_queue` ריק. **הפגם לא תוקן, רק לא הוצת.**

---

## 🔴 סעיף-1 · DB — **T-284: השורש לא הוכח, רק התסמין נעלם**

התיקון נחת 16:51 והבקאנד עלה 16:52. מאז אפס `QueuePool`. **אבל זו ראיית-תסמין.**
הכשל קרה **פעמיים היום** (12:14 ו-16:31), ומה שחזר פעמיים יכול לחזור שלישית.

**המשימה — להוכיח את השורש, לא לחגוג את השקט:**

1. למדוד עכשיו: כמה חיבורים `idle in transaction` יושבים ב-PG, ומה השאילתה שלהם.
   ```sql
   SELECT state, count(*), max(now()-state_change) AS oldest,
          left(query,120) AS q
   FROM pg_stat_activity WHERE datname='mems26' GROUP BY state, left(query,120)
   ORDER BY 2 DESC;
   ```
2. **הראיה הנדרשת:** אם ה-14 החיבורים שדלפו היו כולם אותה שאילתת-ORM על `v9_bars_5min` —
   למצוא את הנתיב שפותח סשן ולא סוגר אותו (חסר `with`/`finally`/`db.close()`), **ולצטט file:line**.
3. **אל תסגור את T-284 על "אפס מאז 16:51".** סגירה דורשת: השורש מצוטט בקוד **וגם** מבחן-רגרסיה
   שמפיל את הנתיב הישן. אם השורש לא נמצא — להשאיר 🔴 ולכתוב במפורש **NOT-DONE: השורש לא אותר**.

---

## 🔴 סעיף-2 · מחזיק-שני S1-BINARY

לפי הסדר הפסוק. לאמת מי המחזיק השני של `S1_BINARY`, ולוודא שאין שני בעלים לאותו תפקיד —
זה בדיוק מה שדוקטרינת-הלמידה אוסרת ("בעל אחד לכל תפקיד; אין שרשרת האחרון-כותב-מנצח").
פלט: טבלת `flag → קורא → כותב`, ואם יש שניים — **מי הבעל, ומי נמחק**.

---

## 🟡 סעיף-3 · שערים — **T-291 חדש הערב, ויש לו עדיפות בתוך הסעיף**

### T-291 — הוראת-מייקל 22:39: "אין להיכנס באזור ה-POC באף סוג יום"

**מה כבר נמדד (cowork, 23:00-23:01) — אל תמדוד שוב, בנה על זה:**

```
.env:353 DAYTYPE_LOCATION_GATE=1 · :124 DALTON_PLAYBOOK_V1=1
.env:129 DAYTYPE_POSITION_GATE=0 · :96  REACTIVE_LOCATION_GATE=0
location_gate.py:154 zone_of() → above_value | near_vah | mid_value | near_val | below_value
```
**‏POC אינו דלי.** אזור-ה-POC נופל ב-`mid_value` שהוא **דלי-מעבר**. השער בנוי סביב
**VAH/VAL** וחל על **משפחות-היפוך בלבד**. ⇒ **אין שער POC. מייקל צודק.**

**המשימה — ולפי דוקטרינת-הלמידה: ריפליי קודם, דגל אחר-כך. התשובה היא מספר, לא קומיט.**

ריפליי `POC_ZONE_VETO` על סט-הרגרסיה, **במקביל על ארבעה רוחבי-אזור**:
`±2` · `±4` · `±8` טיקים · `±0.1×ib_width`. לכל רוחב, שלושה מספרים:

| רוחב | עסקאות נחסמות | P&L שנחסך (או אבד) | שיעור-הצלחה של הנחסמות |
|------|----------------|---------------------|--------------------------|

**כללי-הכנות לסעיף הזה:**
* **אל תדליק כלום.** לא `shadow`, לא לייב. הריפליי מייצר מספר; מייקל בוחר רוחב.
* **אל תסיק שה-POC הפסיד.** `#1328` נכנסה ב-7643.25 = בדיוק VAH, 13 טיקים מה-POC (7640),
  והפסידה $40. זה מוכיח ש**אף שער לא עצר אותה** — לא שה-POC הוא הסיבה.
* להכריע ולתעד: מייקל אמר **"באף סוג יום"** ולא ציין משפחה ⇒ להציג את המספרים
  **גם** ל"כל המשפחות" **וגם** ל"היפוך בלבד", ולתת למספר להכריע.

### T-286 — פיגור סוג-יום מול שער-הפלייבוק (~10 דק', 2 ברים)
שורת-`Normal` נבחרה ב-18:15/18:20 בעוד הטבלה כבר `Variation`. **התיקון אינו "לרענן מהר יותר"**
לפני שנמדד כמה החלטות הושפעו. לספור על כל הסשן: כמה חסימות `dalton_intent:kind` נשאו
`cond=day_type == X` בעוד `v9_day_type_state` כבר אמרה `Y`.

### T-279 — שתי פסיקות סותרות על אותו נתיב-ירי, ואף שומר לא רואה זאת
פריט-פסיקה. **אל תכריע לבד** — להציג למייקל את שתי הפסיקות זו-מול-זו + מה כל אחת מייצרת בפועל.

---

## 🟠 סעיף-4 · ריסטארט-צל — T-252

הגלאי רץ מחדש על **הבר-הנבנה** ⇒ כפילות-צל. **⚠️ סעיף שכבר הופל פעמיים.**
כל מדד-צל היסטורי חשוד כל עוד זה פתוח — ולכן **אסור** לצטט את `-$1,611.25` של הצל
כמספר-אמת בשום דוח עד שזה נסגר.

---

## 🟠 סעיף-5 · resolver / WHATIF

לפי הסדר הפסוק. אם אין ממצא חדש — **לכתוב NOT-DONE מפורש**, לא לדלג בשקט.

---

## 🔴 סעיף-6 · fills / Redis — **T-290 + T-288 + T-289**

### T-290 — הספרים רשמו WIN של +$30 על עסקה שמעולם לא נפתחה
`#1337`: `entry_ts=NULL`, ‏`exit_price=7652.45`. **המפריד המבני:** `7652.45 / 0.25 = 30609.8`
⇒ **המחיר אינו מיושר-טיק**, כלומר אינו יכול להיות פילוי אמיתי — הוא ממוצע שנגזר מסגירת אתי.
**המשימה:** שער ב-`fill_poller` שמסרב לתמחר עסקה **בלי `entry_ts`**, + backfill שמסמן
את `#1337` כ-`CANCELLED` ולא `WIN`. **זה משנה את ספרי-היום מ-‏-$171.25 ל-‏-$171.25 בלי הפנטום** —
לוודא שהשינוי לא מזייף לכיוון השני.

### T-289 — שער-בעלות ל-`_eod_close_t10`
לפי הצעדים ב-TASK_LOG: לדלג על עסקה בלי `entry_ts`/submit-ack, ולא לשלוח `FLATTEN_ACCOUNT`
כשהפוזיציה הוכרה זרה. **‏`EOD_CLOSE_T10_V1` נשאר דלוק** — פסיקת-מייקל 24.08.

### T-288 — `live_slot` מוחזק ע"י עסקה שמעולם לא התמלאה
החסימה כבר לא פעילה (`live_slot=null`), אבל **המבנה לא תוקן**: תנאי-הסף של phantom-heal
הוא `sierra_qty == 0`, ופוזיציה זרה מפרה אותו מבנית.

---

## סעיף-7 · דוח-EOD לטלפון-מייקל

עברית, קצר, **מספרים ולא רשמים**. חובה לכלול:
* ספרי-לייב: 3 שורות, `-$171.25`, ו**אמירה מפורשת** אם ה-+$30 של `#1337` עדיין בפנים.
* מה נסגר הלילה ומה **NOT-DONE** — במפורש, בשם.
* **T-291: המספרים מהריפליי.** זו ההוראה שמייקל נתן הערב; אם אין מספר — לומר "אין מספר עדיין" ולא לעקוף.

---

## כללים (מחייבים)

* **Rule 5** — פלט גולמי, לא "אומת". פקודה + פלט, מודבקים.
* **קומיט פר-סעיף.** לא קומיט-ענק אחד.
* **snapshot לפני כל נגיעה מחוץ-לגיט** (`.env`, LaunchAgents, DLL): `scripts/mems26_snapshot.sh "why"`.
* **בלי הדלקות שלא נפסקו.** דגל חדש = `shadow` + `measured:` ב-`RULED_FLAGS.yaml`, או כלום.
* **NOT-DONE מפורש** על כל סעיף שלא נסגר — סעיף שנשמט בשקט הוא סעיף שאבד.
* **אל תיגע בפוזיציות.** אל תריץ ריסטארט אם נפתחה פוזיציה.
* `python3 scripts/task_log_guard.py` + `python3 scripts/flag_guard.py` חייבים PASS לפני push.
