# מקבץ אוטונומי 15.09 — שלוש משימות ברצף, בלי לעצור בין אחת לשנייה

**מייקל 15.09 10:40: "תשלח לקלוד קוד את כל המשימות שלו שיבצע באופן אוטונומי."**
בצע **1 → 2 → 3 לפי הסדר**, בלי שאלות ובלי לחכות לאישור בין משימה למשימה.
**דדליין קשיח: 15:00.** מה שלא נגמר עד אז — NOT-DONE ולעצור.

**רקע שצריך להיות ברור:** ‏`CEILING_FLIP_TOUCH2_V1` הוחזר ל-`shadow` ב-`10:38`
(פסיקת-מייקל, אחרי מדידה על 22 סשנים: בצל `$890` מול חיה `$216`, דלתא `-$673.75`).
`FRESH_EXTREME_GATE_V1=0`. **אל תיגע באף אחד מהשניים.**

---

## משימה 1 · T-365 — בפתיחה יורים לפי דוקטרינת-הפתיחה, לא לפי סוג-היום
**פסיקת-מייקל 14.09 17:07.** ההזמנה המלאה:
`docs/handoff/cc_orders/CC_ONE_2026-09-14_08_OPENING_BEFORE_DAYTYPE.md` — בצע אותה כלשונה.

בקצרה: ב-`backend/v9/gateway/trading_gateway.py:1419-1431`, בלוק `NO_LABEL_NO_FIRE_V1` —
להתנות את **כל** הבלוק ב-`_resolve_phase(_dp_il_hhmm) in ("C","D")`. בשלבים **A/B** לדלג
ולרשום `logger.info("[Gateway] §5a skipped: phase=%s — opening doctrine governs")`.
כישלון ב-`_resolve_phase` ⇒ **fail-closed** (התנהגות כמו היום). שום שינוי אחר, ואל תיגע
בדגל `NO_LABEL_NO_FIRE_V1` ב-`.env`.

**למה:** ב-14.09 `16:45:05` ננעל `OPEN_REJECTION_REVERSE dir=UP`, נוצרו שני לונגים
תואמי-שורה, ו-`§5a` שלח את שניהם לצל כי `day_type=None`. התווית הגיעה ב-`17:00:04`.
בשלבים A/B השורה נבחרת לפי `opening_type` ו**אינה משתמשת ב-`day_type` בכלל**.

**גולדן:** ריפליי על `2026-09-14` ⇒ `16:45:05 OPENING_ORR LONG 7678.25` עם `blocked_by=None`;
השורה `§5a skipped: phase=B` מופיעה; `§5a NO_LABEL` **לא** מופיע לפני `17:30`; ובחמשת
סשני-הבסיס כל מופע של `§5a NO_LABEL` שהיה **אחרי** `17:30` נשאר — עם מפקד "נעלמו לפני / נשמרו אחרי".

---

## משימה 2 · T-362 — שאילתת ה-CVD של S2 נכשלת בכל קריאה
**‏1,743 מופעים בלוג של 14.09.** ‏`_compute_setup_cvd` ב-
`backend/v9/systems/five_min/five_min_system.py` (~:800-835) מריץ
`SELECT cumulative FROM v9_bars_cumulative_delta WHERE ts >= :t0 AND ts <= :t1`
ומקבל `psycopg2.errors.DatetimeFieldOverflow: date/time field value out of range: "1789383300"` —
**epoch-שניות מוזרק לשדה `timestamptz`**.

**התיקון:** להמיר את `t0`/`t1` ל-`datetime` מודע-אזור לפני השאילתה (הברים נושאים `ts`
כ-epoch; ראה `_iso()` באותה פונקציה). **לא לשנות את הלוגיקה, רק את הטיפוס.**
אם ההמרה נכשלת ⇒ להחזיר `None` כמו היום (fail-open), לא להמציא ערך.

**למה זה חשוב:** `absorption`/`net_delta` הם קלט לבחירת-הווריאנט של S2 והם יוצאים ריקים
מאז 11.09. המקור תקין — `cumulative_delta.json` טרי, הגשר מזרים. זה באג-טיפוס אצלנו.

**גולדן:** אחרי התיקון, ריפליי על `2026-09-11` ⇒ `grep -c DatetimeFieldOverflow` = **0**,
ולפחות setup אחד נושא `absorption.net_delta` שאינו `None`. **אפס שינוי בכתיבות** ב-5 הסשנים —
ואם כן השתנתה כתיבה, לדווח אותה במפורש עם השרשרת ולא לבלוע.

---

## משימה 3 · אבחון התקרה-הכפולה — **מדידה בלבד, אפס קוד**
מייקל: *"הפיתוח שלה לא נכון — צריך לתקן אותה באופן שהיא תעשה כסף."*
**אל תתקן. אבחן.** קוד ללא אבחון הוא בדיוק מה שהכשיל אותנו אתמול.

הנתונים **כבר על הדיסק**: `/tmp/oos_on/` ו-`/tmp/oos_off/` — 22 סשנים, שתי תצורות.
מודל-ההערכה הקבוע (ואין אחר): `n=min(5,floor(225/(5×risk)))`, `n<3` ⇒ אין כניסה,
סולם `C1→T0(entry±3) · C2→T1 · C3→T2 · C4→T3`, **BE אחרי T1**, `$5`/נק' לחוזה,
סטופ-או-יעד על ברי-5-דק', שניהם באותו בר ⇒ `AMBIG` ולא מיוחס.

**הפלט — טבלה אחת של כל עסקאות ה-`CEILING_FLIP_TOUCH2` ב-22 הסשנים**, ולכל שורה:
`session · שעה · כיוון · כניסה · סטופ · risk · n · תוצאה · $` ובנוסף, **כל אלה מחושבים
מברים שנסגרו לפני הכניסה בלבד — אפס מבט-קדימה**:
- מרחק הכניסה מקצה-אזור-הערך של **הסשן הקודם** (VAH/VAL/POC של אתמול)
- מרחק הכניסה מקצה-אזור-הערך של היום, ו-`zone` (‏near_vah/mid_value/near_val/…)
- גיל הקיצון הנגדי של הסשן, בברים
- שלב (A/B/C/D) וסוג-היום באותו רגע
- `rej%` של בר-הכניסה = `(high-close)/(high-low)` לשורט, מראה ללונג
- ווליום בר-הכניסה חלקי **חציון 20 הברים הסגורים שלפניו**

**ואז, ורק אז:** לכל אחד מששת המשתנים בנפרד, לחלק את העסקאות לשלישים ולתת סה"כ-$ ומספר
מפסידות לכל שליש. **בלי לחפש שילובים, בלי לכוונן ספים, בלי להמליץ.** אם משתנה אינו מפריד —
לומר זאת. גודל-מדגם לכל תא חייב להופיע.

**מה אסור:** לבחור סף שמקסם רווח על המדגם הזה. זה אוברפיטינג וזו הטעות שביטלה את
`CC_ONE_2026-09-14_06_VOLUME_TRIGGER.md` אתמול.

---

## כללי-המקבץ
- **אפס ריסטארט.** ההרמה היא של cowork בלבד, ב-15:45.
- **אפס נגיעה ב-`.env`**, בפוזיציות, בסלוט או בתור-הפקודות.
- ‏`git -C /Users/michael/Downloads/mems26_web_git`, אף פעם לא `cd` יחד עם `git`.
- **קומיט נפרד לכל משימה** + `TASK_LOG` + `STATUS_BOARD` באותו קומיט.
- אחרי כל משימה: שורת-LOG חתומה ב-`LIVE_CHANNEL` עם הפלט הגולמי, **וממשיכים מיד לבאה**.
- בסוף: `flag_guard` · `task_log_guard` · `wire_guard` · `guard_tests` — כל אחד בפקודה
  נפרדת, לא דרך `| tail`, ולהדביק את קוד-היציאה של כל אחד.
- ואז **"סיים מקבץ 15.09"** ולעצור.
