# פקודת-עבודה · תיקון-DB היסטורי — הכנה עכשיו, כתיבה אחרי הסגירה

**מאת:** cowork-dev · **אל:** cc-macbook · **תאריך:** 2026-08-25 16:05 IL
**פסיקת-מייקל:** *"סעיף 3 לבצע עם קלוד קוד במיידי"* (= תיקון-DB היסטורי)
**מקור:** `docs/reports/SCID_DIFF_RCA_2026-08-25.md` · `docs/plans/SYSTEM_FIX_PLAN.md` §1.2

---

## 🔴 מגבלה אחת, ואינה נתונה לשיקול

**‏16:30 השוק נפתח. אין כתיבה ל-DB בזמן סשן.**

`v9_bars_5min_woodies` היא הטבלה שהמערכת החיה קוראת ממנה כדי לסחור. ‏07-22 כבר לימד
אותנו מה קורה כשמשהו נועל אותה בזמן מסחר — הבאקנד נתקע 35 דקות.

**לכן: שלבים 1–5 עכשיו (הכל קריאה-בלבד). שלב 6 — הכתיבה — רק אחרי 23:00 IL.**
זה גם הסדר שהתוכנית של קורסור מחייבת: `diff → RCA → snapshot → dry-run → repair → four-axis`.

---

## מה כבר ידוע — אל תחקור מחדש

```
34 סשנים · PASS=9 · FAIL=25
2,652 משבצות-בר: correct=1,986 (74.9%) · shifted+60m=508 (19.2%) · partial=57 (2.1%) · empty=101 (3.8%)
SCID: 78 ברי-RTH בכל 34 התאריכים — אפס סשנים אבודים
```

| מחלקה | סשנים | שורש | file:line |
|---|---|---|---|
| **D1** היסט +60 דק' | 12 | הגייט מעביר מנה-ישנה; המצב בזיכרון ⇒ דחיפה ראשונה אחרי ריסטארט עוברת | `bars.py:596-599` · `:556` |
| **D2** ברים חסרים | 6 (101) | 5 מהם תוצר-לוואי של D1; רק 08-04 הוא עצירת-פיד אמיתית ב-12:50 ET | — |
| **D3a** סתירות-CVD | 9 | `bar_id="cvd_{idx}"` — אינדקס-גרף שמתמספר מחדש בכל טעינה | `bars.py:1096-1104` |
| **D3b** כיסוי-CVD | 20 | **881/2694 (32.7%) חתומות `HH:M4:59`** — שנייה מוקדם; הצרכן קוטם ⇒ נופלות בשקט | `bars.py:1067` · `data_source.py:243` |
| **D4** בר-חלקי קפוא | 57 ברים | `current_bar` דורס את `history[-1]`; בלי דחיפה הבאה הוא נשאר לנצח | `bars.py:1250-1255` |
| **D5** תפרים | 5 | **לא מחלקה עצמאית** — כולם יושבים על גבולות-ההיסט של D1 | מת עם D1 |

---

## מה לבצע — בסדר הזה

### שלבים 1–5 · עכשיו · **קריאה-בלבד**

**1 · דו"ח-diff פר-סשן** — לכל אחד מ-25 הכשלים: אילו ברים, איזו מחלקה, ומה הערך הנכון
מ-SCID. פלט ל-`docs/reports/DB_REPAIR_DIFF_2026-08-25.md`.

**2 · שכבה A — 9 הסשנים הקלים.** ‏`07-07 · 07-08 · 07-09 · 07-10 · 07-13 · 08-07 · 08-10 ·
08-11 · 08-13` — הברים כבר **זהים-בייט**; רק ה-CVD נכשל. אלה מחזירים אותנו ל-**18/34**
בלי לגעת בטבלת-הברים בכלל. **התחל מהם.**

**3 · `UNIQUE(ts, symbol, source_version)` על `v9_bars_cumulative_delta`** — בלי זה
אי-אפשר לתקן CVD. ⚠️ שים לב: `safe_writer` **מנחש** את עמודת-הקונפליקט; כל יעד
`ON CONFLICT` חייב אילוץ תואם או שהכתיבה נכשלת בשקט.

**4 · snapshot** — `scripts/mems26_snapshot.sh "pre-db-repair"` **ובנוסף `pg_dump`
של שתי הטבלאות**. ‏`mems26_snapshot.sh` **אינו** מצלם סכימת-DB או נתונים.

**5 · dry-run** — להדפיס כל `UPDATE`/`INSERT` בלי להריץ. ספירות פר-מחלקה.
**זה התוצר של היום.** עצור כאן ודווח.

### שלב 6 · **אחרי 23:00 IL בלבד**

`SET lock_timeout='3s'` לפני כל פקודה · אצווה פר-סשן · אימות four-axis
(Quality/Recency/Cardinality/Latency) · `pg_isready` + החלטה-חיה אחרי כל אצווה.

---

## אסור

- **כתיבה ל-DB לפני 23:00 IL.**
- `scripts/rebuild_bar_truth.py` — קורא-SCID **שני** שה-docstring שלו אומר
  `America/Chicago` והקוד אומר `UTC`. אומת אמפירית ש-SCID הוא UTC. **תיקון דרכו
  יזיז הכל ב-5-6 שעות.** השתמש **רק** ב-`backend/v9/replay/scid_validator.py`.
- **לגעת ב-12 סשני-ההיסט (D1) בשלב הזה** — שדות-הווּדיס שם ממתינים לפסיקת-מייקל
  (`SYSTEM_FIX_PLAN` §1.3). ‏D1 = מנה נפרדת.
- להמציא ערך. חסר = `NULL` + `NOT_JUDGEABLE`, לעולם לא סינתזה.
- `.env` · ריסטארט · נתיב-הירי.

## מסירה

`docs/reports/CC_DB_REPAIR_PREP_2026-08-25.md` — פקודה+פלט גולמי לכל שלב ·
ספירות פר-מחלקה · פלט ה-dry-run במלואו · NOT-VERIFIED מפורש.
LOG ב-`LIVE_CHANNEL`. `commit`+`push`. **ואז עצור עד 23:00.**
