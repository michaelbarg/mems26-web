# עץ-ההחלטות V3 — עץ אמיתי, בשלבים: סוג-פתיחה → סוג-יום → תבנית → פיצולי-נסיבה (מייקל 25.09 12:10)

**מייקל:** *"עץ ההחלטות שלנו צריך להיות בשלבים: קודם סוג פתיחה, סוג היום, תבנית מתאימה לפי הסדר… את הענפים של כל מערכת אנחנו צריכים לפצל כדי לדייק למצב בפועל. ממש צריך להיות עץ! ולא טבלה שפועלת — או הטבלה צריכה להיות שכל קוביה שם מתפצלת לענפים ועצים."*

## 1 · מה יש היום — ולמה זה לא עץ

| רכיב | מה הוא | הבעיה |
|---|---|---|
| `config/dalton_playbook.yaml` | טבלה שטוחה: שלב × (סוג-פתיחה \| סוג-יום) → הטיה + רשימת-סוגים | שורה אחת לכל מצב; אין פיצול מתחתיה; קוראת תווית מפגרת |
| Auth Table V2 (`auth_matrix.yaml`) | טבלה: תבנית × סוג-יום → FULL/REDUCED/SKIP | מנותקת מהשלב, מהמיקום ומההרחבה; "קוביה" שלא מתפצלת |
| שרשרת-השערים בגייטוויי (~30) | וטו אחרי וטו, כל אחד עם דגל | ההתנהגות היא שארית של סירובים, לא החלטה |
| `dalton_tree_v2_draft.yaml` | רשימת-כללים "הראשון-שמתאים-מנצח" (צל) | עדיין שטוח |

## 2 · המבנה החדש — עץ מקונן אחד, בסדר של מייקל

```
שורש
└─ סוג-פתיחה  (OPEN_DRIVE · OPEN_TEST_DRIVE · OPEN_REJECTION_REVERSE · OPEN_AUCTION_IN · OPEN_AUCTION_OUT · UNKNOWN)
   └─ שלב  (A 16:30–16:45 · B –17:30 · C –21:00 · D 21:00+)
      └─ סוג-יום  (בשלב A/B: FORMING; מ-C: Variation · Trend_Normal · Trend_DD · Normal · Neutral_Center · Neutral_Extreme · Nontrend)
         └─ מבנה בפועל  (הרחבת-IB: none · up · down · two_sided — מה שהמחיר עשה, לא התווית)
            └─ תבנית / מערכת  (OPENING_DRIVE, REACTIVE_*, INITIATIVE_*, GB100, ZLR, CEILING_FLIP_*, VAR_CONT, DBL_BOTTOM_ABS, …)
               └─ פיצולי-נסיבה של התבנית:  כיוון מול ההטיה (עם/נגד) · מיקום מול הערך (above/near_vah/mid/near_val/below) ·
                  קצה-IB/קיצון · test (פולבק לפני) · ווליום/דלתא · גודל-הסטופ ב-ATR
                  └─ עלה = פעולה מפורשת:  TAKE (סטופ · יעד · גודל) | SHADOW | SKIP  + measured: {n, win, Σ$, מקור, תאריך}
```

- **כל צומת שואל שאלה אחת** על הנסיבה. ענף `"*"` = ברירת-מחדל לצומת.
- **עלה = כלל, לא ציון.** TAKE נושא את מדיניות-היציאה שלו (סטופ מבני / 1.5R / הרמה הבאה).
- **הפיצול מתקדם ככל שיש נתונים:** עלה עם n ≥ 30 ותוצאה מעורבת מקבל את השאלה הבאה בסדר הדוקטרינרי (מיקום → test → ווליום). עלה עם n < 15 נשאר SHADOW.
- **הסדר של הפיצולים הוא הדוקטרינה** (דלתון: פתיחה → מבנה → סוג-יום; זוהר: תבנית → מיקום → ווליום), **המספר רק מאשר או פוסל**.

## 3 · איך זה נכנס למנוע (בלי לשבור את הלייב)

1. **`config/decision_tree_v3.yaml`** — העץ המקונן. **`backend/v9/services/decision_tree.py`** — `walk(vector) → leaf, path`.
2. **הגייטוויי** תחת דגל `DECISION_TREE_V3` (0 = זהה-בית): במקום `dalton_intent` + רשימות-הסוגים + Auth-Table + שער-המיקום — הליכה אחת בעץ. SKIP ⇒ `blocked_by=tree:<path>` · SHADOW ⇒ shadow_only · TAKE ⇒ ממשיך לשערי-הבטיחות בלבד (R:R-רצפה, סלוט, EOD, פוזיציה) עם מדיניות-היציאה של העלה. הנתיב-בעץ נרשם על כל החלטה.
3. **אבן-דרך 1 — זהות (parity):** העץ-הזורע מקודד את ההתנהגות של היום (הפלייבוק + Auth-Table + מיקום + ענף-הדרייב). הרנס 58 סשנים עם `DECISION_TREE_V3=1` חייב לתת **את אותם ימים** כמו הבסיס. עד אז — לא נוגעים בלייב.
4. **אבן-דרך 2 — פיצולים:** כל פיצול חדש = ענף עם `measured:`; הרנס יום-כולל מול הבסיס; חיובי ⇒ TAKE ⇒ לייב לפי הכלל (24.09).
5. **הלולאה הלילית:** `scripts/tree_measure.py` — לכל עלה: n · win · Σ$ מההרנס ומהלייב, כתיבה חזרה ל-`measured:`, וסימון עלים "בשלים לפיצול". **לוח-העץ** מצייר את העץ המקונן עם המספרים (שדה-הנוירונים).

## 4 · לוח-זמנים

| מתי | מה | קריטריון |
|---|---|---|
| 25.09 (היום) | סכימה + מעריך + טסטים + עץ-זורע מהדוקטרינה + הוק בגייטוויי (דגל כבוי) | `pytest` ירוק; `walk()` על 3,676 המועמדים של הבסיס מחזיר נתיב לכולם |
| 25.09 לילה | הרנס-זהות 58 סשנים | ימי `TREE_V3=1` == הבסיס (או הפרש מוסבר) |
| 26–27.09 | הפיצולים הראשונים לפי המפה (Auction·B, Drive·C·with, Drive·D·against, Trend·C) — כל אחד ענף עם מספר | יום-כולל ≥ בסיס |
| 28.09 שער 15:30 | `DECISION_TREE_V3=1` לייב אם אבן-דרך 1 ו-2 עברו | הכלל של מייקל |
| מתמשך | כל ערב: מדידה לעלים, עלים בשלים לפיצול, לוח-העץ | |

## 4א · מצב (25.09 12:45)

- **אבן-דרך 1 — זהות: עברה.** יוניט: רשת ממצה של >20,000 נסיבות, העץ == השרשרת הישנה (`tests/v9/regression/test_decision_tree_v3.py`). מנוע אמיתי: 58 סשנים, 3,841 מועמדים, 0 הבדלי-פסק, 58/58 ימים זהים, Σ+1,220$ == Σ+1,220$.
- **הכלים:** `scripts/tree_measure.py` (כל צומת/עלה: n · win · Σ$ מועמד-לבד + לייב; `config/decision_tree_v3.measured.json`; דוח `docs/reports/TREE_V3_MEASURE_<date>.md`) · לוח `tree_v3.html` (שדה-הכוכבים) · וריאנט-עץ לריפליי: `DECISION_TREE_V3_PATH=<yaml>`.
- **הפיצולים הראשונים** (`harness_out/t480/tree_v1/v2/v3b.yaml`, יום-כולל מול הבסיס): V1 דרייב·שלב-D·וריאציה·נגד-ההרחבה · V2 אוקציה·שלב-B·תווית-טרנד·BREAK · V3b = V2 + תווית-וריאציה. התוצאה נרשמת ב-TASK_LOG T-480 ובעלה עצמו (`measured:`).
- **הערה על `measured:`:** המספרים הליליים נכתבים ל-`decision_tree_v3.measured.json` (לפי נתיב) ולא לתוך ה-YAML (עוגני-YAML וההערות היו נמחקים); `measured:` בתוך ה-YAML נשאר לפסיקות — המספר שהכריע את הענף.

## 6 · העץ המלא — הכל עובר דרכו, מהזיהוי עד הניהול (מייקל 25.09 16:05: "שהכל יעבור דרכו מרגע הזיהוי עד רגע הניהול… מערכת מלאה של החלטות שמתחדדת מיום ליום")

היום העץ מחליט רק את **הקבלה** (TAKE/SHADOW/SKIP). לפניו — המפיקים יורים מה שהם מזהים; אחריו — ~25 שערי-בטיחות עם דגלים גלובליים (ELQ · בר-אישור · R:R · סלוט · EOD · קיצון-טרי · רדיפה…); ואחרי הכניסה — ניהול (S6: BE, טריילינג, יעדים) עם דגלים גלובליים. 3 מ-4 הסימונים של 24.09 נחסמו **אחרי** העץ. לכן ההרחבה, שלב-אחר-שלב, כל שלב בשיטה שעבדה היום (זהות קודם, פיצולים עם מספר יום-כולל אחר-כך):

| שלב | מה עובר לעץ | איך | מדידה |
|---|---|---|---|
| **2 · השערים שאחרי** | כל שער-בטיחות הופך למדיניות של העלה: `gates: {ELQ: on/off, confirm: on/off, rr_floor: x, chase: on/off…}`. ברירת-מחדל = הכל דלוק (זהות). פיצול אומר: "בנסיבה הזו ELQ לא חל" | הגייטוויי קורא `leaf.gates` לפני כל שער שאחריו | הרנס יום-כולל, כמו היום |
| **3 · היציאה והניהול** | העלה נושא `exit: {stop: structural/producer/ATR×k, t1: 1.5R/רמה-הבאה/POC, t2, runner, be_at, trail}`. נשמר על העסקה (`tree_path` + `exit_policy`); S6 מנהל לפי העלה ולא לפי דגל גלובלי | הגייטוויי מעביר את מדיניות-העלה לסולם ול-S6 | הרנס (יעדים/סטופים) + `pre_t1_management.py` (BE/טריילינג) |
| **4 · המפיקים — "מה לחפש"** | היפוך: הצומת הנוכחי מגדיר את **רשימת-המעקב** — אילו תבניות/סוגי-כניסה ובאילו רמות (VAH/VAL/קצות-IB/POC) רלוונטיים עכשיו; מפיק שלא ברשימה לא רץ. זה בדיוק ה"מה הוא מתכנן" שהלוח כבר מציג | `plan` מהצומת → `five_min_system`/`woodies` מריצים רק את המותר; הרמות מוזרקות למפיקים | זהות (רשימה מלאה = היום) ואז צמצום נמדד |
| **5 · הזמן כשאלה** | "כל חצי שעה יודעים יותר" — `slot` (16:30 · 17:00 · 17:30 …) כתכונה לפיצול, לצד `structure` (מה המחיר עשה מול ה-IB) | קיים בווקטור (`hour`); מוסיפים `slot` | פיצולים |
| **6 · הלולאה היומית** | סימונים (שלך + האורקל) → `marks_vs_tree` → מועמדי-פיצול → ריפליי יום-כולל → ענף עם `measured:` → שער → לייב. אוטומטי כל לילה: `tree_measure` + `marks_vs_tree` + הלוח | תור-הלילה 23:00 | — |

**סדר:** שלב 2 הלילה (קוד + זהות + הפיצולים הראשונים על הנסיבות של הסימונים: ELQ בלונג-עם-ההטיה ביום וריאציה · בר-האישור בשלב B · TOUCH2 חי היכן שהעץ מאשר) ⇒ שער 28.09 · שלב 3 בשבוע הבא · שלב 4 אחריו. **הכלל לא משתנה:** כל הרחבה = זהות מוכחת קודם, ואז ענפים עם מספר; שום דגל גלובלי חדש.

## 5 · מה זה לא

לא עוד שער. לא ציון. לא טבלה נוספת. העץ **מחליף** את הפלייבוק, את Auth-Table, את רשימות-הסוגים ואת שער-המיקום — ארבע טבלאות שטוחות הופכות לעץ אחד שכל קוביה בו יכולה להתפצל.
