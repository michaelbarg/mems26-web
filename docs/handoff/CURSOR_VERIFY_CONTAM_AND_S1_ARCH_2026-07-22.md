# cursor — בדיקה עצמאית: (א) הזיהום (ב) S1=מוח / S2+S4=זרועות / סוג-פתיחה (מייקל 2026-07-22 ערב)

**מייקל:** *"איזה זיהום? + מערכת-1 צריכה גם לזהות סוג-פתיחה ולבצע ירי בעזרת מערכת 2-4."*
**cursor: אמת עצמאית (אל תסמוך על cowork/cc — חוק-5, פקודות+פלט-גולמי). read-only.** cc באמצע תיקון-רגרסיות
(אל תתנגש). המערכת בסים.

## חלק א — אמת/הפרך את הזיהום (cowork טוען שהוא חי-עכשיו)
1. **זוגות-כפולים +1h היום:** SQL join על `v9_bars_5min_woodies` + `v9_bars_5min` (זהה OHLC, ts+1h,
   `ts::date=current_date`). cowork ראה 12. **כמה אתה רואה? באיזו שעה נוצר כל זוג?**
2. **המנגנון:** `bars.py` `_hour_shift_fix` (~:460) — חלון ±120s מול בר-300s. grep `TS-HOUR-FIX applied|SKIPPED`
   ב-`/tmp/backend.err.log` היום: כמה applied vs skipped? האם באמת נכתב בר-רפאים ב-ts+3600?
3. **מי מורעל:** אמת ש-`v9_tpo_sessions` היום נותן VA לא-סביר (cowork: VAH 7561.75/VAL 7558.25 = **3.5pt**);
   הצלב מול ה-ATR ורמות-המפתח. **הכרע:** האם השורש ב-DLL (חותמת-מקור) או בתיקון-הקליטה? האם `WOODIES_TS_HOUR_FIX=0`
   (תיקון-cc, לא-דלוק עדיין) באמת פותר, או משאיר ברים ‎−1h? (חשש-cowork — אמת בסים).
4. **דווח:** 🟢CONFIRMED / 🔵SUSPECT / 🔴REFUTED לכל טענה, עם הפלט.

## חלק ב — אמת: S2/S4 לא כפופות ל-S1 בזיהוי
cowork טוען: הזיהוי של S2 (`_detect_reactive(bars_5m)`) ו-S4 (תבניות-CCI) **לא מקבל** סוג-יום/רמות מ-S1 —
רק גאומטריה; S1 מודבק בסוף כ-sizing+reject. ראיה שהובאה: `v9_five_min_setups.day_type_at_fire` **ריק** ב-8
ה-setups של היום. **אמת עצמאית:** קרא את חתימות-הזיהוי; בדוק אם הכניסות של היום היו בקצה (VAH/VAL) או באמצע;
אשר/הפרך ש-`S2_DETECTION_LIVE_DAYTYPE_V1` (G2/G3) בנוי אך OFF.

## חלק ג — סוג-פתיחה מזוהה, אך לא מניע ירי (הנקודה החדשה של מייקל)
S1 **כן מסווג** סוג-פתיחה (`detect_opening_type` → OPEN_DRIVE/TEST/REJECTION/AUCTION; ראינו OPEN_DRIVE היום)
ומראה `stance` בפאנל — **אבל אין טריגר-כניסה שמניע את S2/S4 לפי סוג-הפתיחה.** מנגנון-הפתיחה הקיים
(`OPENING_TYPE_GATE`/`OPENING_WINDOW_FIRE`) הוא **מתיר/חוסם בלבד**; `OPENING_ENTRY_V1` (טריגרי-הכניסה שבנינו
היום) רץ ב-SHADOW. **אמת:** האם היום סוג-הפתיחה (OPEN_DRIVE-up) הניע ירי-כלשהו של S2/S4? (צפוי: לא — רק
צל). זו בדיוק הזרוע החסרה.

## חלק ד — הארכיטקטורה הרצויה (מייקל): S1 מוח, S2+S4 זרועות
תעד (לא-לבנות) את הפער והיעד:
- **S1 = מקור-ההקשר היחיד:** סוג-יום + **סוג-פתיחה** + IB/VAH/VAL/POC + expansion + כיוון — אובייקט-הקשר אחד.
- **S2/S4 מקבלים אותו בשלב-הזיהוי:** reactive-fade רק בקצה-המתאים-לסוג-היום · continuation רק עם-הכיוון
  באזור-פולבק · בפתיחה — לפי סוג-הפתיחה (DRIVE→עם-הכיוון, REJECTION→היפוך). לא "לזהות-בכל-מקום-ואז-לפסול".
- **סדר-תלות (קריטי):** אי-אפשר לחבר לפני ניקוי-הזיהום (אחרת S1 מוסר רמות-זבל) ולפני ש-cc סוגר רגרסיות.

**תוצר:** שורת-LOG חתומה + טבלת-ממצאים (🟢/🔵/🔴 פר-טענה, עם ראיה) + המלצת-סדר. הכרעות-בנייה = מייקל, אחרי הבדיקה.
