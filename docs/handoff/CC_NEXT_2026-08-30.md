# פקודה ל-cc · 30.08 אחה"צ — חיווט חסר + חמישה שלא נבנו

**קרא ראשון:** `docs/plans/STRUCTURE_EXIT_DOCTRINE.md` (הדוקטרינה + פסיקת-מייקל "מאשר")
ו-`docs/handoff/CC_BUILD_STRUCTURE_EXIT_2026-08-30.md` (המפרט). זו פקודת-ההשלמה.

## 🔴 1 · לפני הכל — `STRUCTURE_EXIT` הוא כרגע קוד יתום (T-143)
`grep -rn "should_exit_on_failbreak" backend/ --include=*.py` ללא הקובץ והטסטים ⇒ **אפס**.
הלוגיקה נכונה ו-14 טסטים ירוקים, אבל **אף אחד לא קורא לה, והיא לא מבצעת** — שלוש
הפונקציות מחזירות `dict` ואינן קוראות ל-`_emit_modify_target`/`_emit_modify_stop`/FLATTEN.
- לחווט על כל בר-סגור עם פוזיציה-פתוחה. **התקדים המדויק:** `_maybe_ceiling_floor_state`
  ב-`five_min_system.py:2171` — אותו מבנה, `os.getenv` בזמן-קריאה, תמיכה ב-`shadow`.
- לבצע בפועל דרך `trade_manager` (המנגנונים המאומתים בלבד; `op=EXIT` אסור).
- **טסט-מוטציה:** מחיקת הקריאה ⇒ טסט נכשל. בלי זה זה חוזר אליי.

## 2 · חמישה שטרם נבנו — לפי סדר (דגל OFF · מוטציה · §D-עם-עמלות · עוגן-28.08)
1. **`CEILING_LONG_LOCK_V1`** — קיים כדגל, לוודא שהוא מחווט ופועל: אין כניסה חדשה
   באותו כיוון מעל `P1 − 0.15×ATR` עד פריצה-בקבלה. **עוגן: 3 כניסות-הצל 18:15–18:25 נחסמות.**
2. **`ENTRY_LOCATION_QUALITY_V1`** — 🔴 לא נבנה. פסילת "רודף", **הכל יחסי**:
   `pos=|entry−R|/L > 0.66` בלי פולבק · `stop/ATR > 1.5` · `(entry−VAH)/VA_width > 0.25`.
   פרמטרים ב-YAML. **עוגן: 17:00/17:35 עוברות (pos≈0.38/0.40) · 18:15/18:25 נפסלות (0.84/0.91).**
3. **`CVD_CANDLE_CONFLUENCE_V1`** — 🔴 לא נבנה. שיאים-יורדים + CVD-מצטבר יורד + דחייה-מקצה
   ⇒ שורט ליעד-POC. **בלי** לדרוש פולבק/התרחבות/תורן. **תלוי במיגרציית-CVD (סעיף 3).**
4. **‏SA-3** — `DIRECTION_CONTEXT=0` סוגר בלוק שמכיל את אתר-האכיפה היחיד של
   `cont_trend_filter` (`trading_gateway.py:1781`) ואת שני אתרי `_compass_or` ⇒ **4 דגלים
   פסוקים-דלוקים בלתי-נגישים.** להוציא לתנאים עצמאיים + טסט שכל אחד פועל כשהדגל כבוי.
5. **‏SA-7** — `opening_entry.py` משווה ל-`OPEN_DRIVE_DOWN`/`OPEN_DRIVE_UP` (2 מופעים,
   אפס אתרי-ייצור). הייצור מחזיר `OPEN_DRIVE` והכיוון ב-`opening_dir` נפרד ⇒ **הווטו
   חוסם אפס מקרים.** להעביר `opening_dir` ולהשוות נכון; הטסט הנוכחי טאוטולוגי.

## 3 · חוב-נתונים שחוסם מדידה
**מיגרציית-נרמול CVD** על ~5,498 השורות ההיסטוריות (snapshot לפני, אחרי 23:00, four-axis).
בלעדיה כל §D של סעיף 2.3 נמדד על ~26 חורים ליום. + טסט על מסלול-הייצור `bars.py` ·
איחוד שני מימושי-הנרמול · חיווט או הסרה של תג-`coverage` (מת) · `CVD_WINDOW_MIN_PCT`
ל-RULED_FLAGS · `test_cvd_point_parser`.

## 4 · המדידה (חובה לכל דגל לפני בקשת-הדלקה)
כמה פעמים נדלק · מה קרה ל-MFE **אחרי** · דלתא-$ **וגם** חציון-יום · **ספירת-החטאות**
(עסקאות שנסגרו והמשיכו לרווח גדול יותר — **המדד המכריע** של יציאה-על-כישלון-פריצה) ·
פילוח פר-סוג-יום · **T-140: שני הווריאנטים זה מול זה** (אישור-מוקדם מול קו-הצוואר).

## כללים
טסטים **עם `.env` טעון** · רגרסיה מול worktree-בסיס (HEAD כרגע = 234; לדווח שני מספרים) ·
דגל נפרד · OFF · מסלול-כבוי זהה-בייט · `RULED_FLAGS` באותו קומיט · **הדלקה: cowork+מייקל
בלבד** · שורת-STATUS_BOARD מאומתת (ממצא→תיקון→ראיה) · **`NOT-DONE` מפורש**.
