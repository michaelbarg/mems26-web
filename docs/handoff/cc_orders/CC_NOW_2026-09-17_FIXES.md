# CC_NOW 2026-09-17 13:30 — תיקוני-סקירה לעץ ולסריקה (cowork-dev → cc-macbook) — 6 פריטים, ברצף, בלי לעצור

**הקשר:** ההזמנה של אתמול (`CC_NOW_2026-09-16_TREE_BUILD.md`) בוצעה ואומתה (routes זהים על 15.09+11.09, 98 טסטים). הקוד נטען היום 13:26 (pid 56123). הסקירה שלי + הריצה הראשונה של `gap_analysis.py` (11:50) מצאו **חמישה פערים** שהופכים את הפלט ללא-ראוי-להכרעה. אותם גבולות כמו אתמול: **אפס ריסטארט · אפס `.env` · אפס דגל · ריצות כבדות (הרנס/85 סשנים) רק אחרי 23:05** (בדיקה קצרה על סשן אחד עם `--dry` מותרת). קומיט-פר-פריט, טסטים, LOG עם פלט גולמי, בלי שאלות. ⚠️ **אל תטען שקובץ-טסט "קיים ועובר" בלי `ls` + `pytest` בפלט** — אתמול נטען `test_dalton_playbook.py 32/32` על קובץ שאינו קיים (LIVE_CHANNEL 23:46).

---

## F1 · T-389b — `day_type_final` מהברים, לא מה-`cross_context` (הדוח: 42 סשנים UNKNOWN + "None")

`gap_analysis.py` לוקח את תווית-היום מהעסקה הראשונה ⇒ UNKNOWN/None ברוב הסשנים ⇒ טבלת-סוגי-היום חסרת-ערך. **לתקן:** `day_type_final` = `classify_replay` על ברי-ה-RTH של הסשן (המנוע המאומת לפי `docs/SOURCE_OF_TRUTH.md`; אם החתימה דורשת IB/VA — לחשב מהברים כפי שההרנס עושה). `day_type_at_entry` (התווית החיה) נשמר כשדה נפרד לכל עסקה — **שניהם בדוח**: "מה היום היה" ו-"מה המערכת חשבה בכניסה". אם `classify_replay` לא מכריע ⇒ `UNRESOLVED` (לא None, לא ניחוש). **טסט:** סשן סינתטי עם הרחבה חד-כיוונית מעל 2×IB ⇒ Trend; סשן בתוך ה-IB ⇒ Normal/Neutral.

## F2 · T-389c — מסנן-שפיות לברים לפני הזיגזג (הדוח: 175 / 143 / 134 נק' ב-20 דק')

10.06 ו-29.07 מציגים תנועות בלתי-אפשריות ל-MES (‏104 נק' ב-5 דק'). **לתקן:** לפני הזיגזג — בר ש-`|close−prev_close| > max(25 נק', 6×ATR14-סיבתי)` או `high−low > 40 נק'` מסומן `SUSPECT`; סשן עם ≥1 בר-SUSPECT מקבל `data_quality: SUSPECT` ו**מוצא מהטבלאות המסכמות ומהענפים** (מדווח בנפרד ברשימת "סשנים שהוצאו + הסיבה"). **אל תתקן את הברים** (Rule 1 — אין סינתזה); רק לסמן ולהוציא. לבדוק גם `v9_bars_5min_woodies` מול `v9_bars_5min_continuous` באותם רגעים ולדווח מה מקור הקפיצה (גלגול-חוזה? טיק שגוי?). **טסט:** בר של 100 נק' ⇒ SUSPECT; בר של 12 נק' ⇒ נקי.

## F3 · T-389d — הסבר פער-המודל: "נלקח −1,030 נק' במודל" מול −45 נק' בברוקר

הדוח אומר שכניסות-הלייב הפסידו 1,030 נק' במודל הקבוע; הברוקר (‏`v9_trades.pnl_usd` לייב יולי-ספטמבר: −491 + 971 − 704 = −224$ = −45 נק'-חוזה) אומר אחרת פי-20. **לחקור ולדווח, לא לתקן את המודל:** (א) האם המודל סופר נק'×חוזים (5 חוזים בסולם) בעוד הברוקר סחר 2-5? — לדווח גם `pts_per_contract`; (ב) האם המודל מקצה סטופ-מלא לעסקאות שבפועל יצאו ב-BE/MAE-scratch (מערכת-6)? — לספור כמה מהעסקאות-החיות נסגרו `MAE_SCRATCH`/`BE`/`STRUCTURE_*` ומה המודל נתן להן; (ג) עסקאות עם `entry_ts` בלי בר תואם (Globex) — כמה. **הפלט:** סעיף בדוח "מודל מול ברוקר" עם שלושת המספרים. **כלל:** המודל הקבוע נשאר כמו שהוא (הוחלט 14.09) — הדוח מסביר את הפער, לא מזיז את המודל.

## F4 · T-390b — לחבר ברים לווקטור בשער (‏`vol_ratio` · `bars_since_high/low` · `atr_causal` יוצאים None בלייב)

ב-`trading_gateway.py` הווקטור מחושב עם `bars_rth_today=None, prior_sessions_bars=None` ("לא זמין בקלות") ⇒ שלושת השדות ש"ווליום וברים" נשענים עליהם ריקים. **לתקן:** (א) `bars_rth_today` — מהבאפר של `five_min_system` (‏`_cf_bars`, 120 ברים סגורים) מסונן ל-RTH של היום; (ב) `prior_sessions_bars` — קריאה אחת ל-DB לכל סשן (10 סשני-RTH קודמים, `v9_bars_5min_woodies`), **בקאש** במודול (‏`_PRIOR_CACHE[date]`), לא בכל החלטה; (ג) `atr_causal` על ברים סגורים בלבד. ולחשב גם את שורות-ה-**BAR** (החור שנשאר): ב-`bar_router` אחרי ה-handlers, על כל בר-RTH-5-דק' סגור — `log_decision_vector(kind='BAR', system=0, classification='BAR', vector=…)`. הכתיבה — **בתהליכון-רקע** (‏`threading.Thread(daemon=True)` או תור) — היום `safe_execute` סינכרוני על נתיב-השער; ו-`read_one` של `confidence` פעם-בבר (קאש), לא פעם-בהחלטה. **טסט:** וקטור על 3 ברים סינתטיים + 5 סשנים-קודמים ⇒ `vol_ratio` מספרי, `bars_since_low` נכון, וה-BAR-row נרשם.

## F5 · T-392b — `[TREE-DIFF]` משווה אוצר-מילים שונה + שורות-Placeholder + Rule A רחב מדי

(א) `_real_decision` הוא `blocked_by`-string או `"FIRED"`; `_tree_decision` הוא `allow/block/stand_down` ⇒ **כל** החלטה נרשמת כ-diff. **מיפוי:** real FIRED ≡ tree allow · real blocked ≡ tree block/stand_down; רק אי-התאמה אמיתית נרשמת, ובשורת-`TREE_SHADOW` נשמרים שני הערכים הגולמיים. (ב) `config/dalton_tree_v2_draft.yaml`: שתי שורות-Placeholder (T-329, T-355) — להוסיף ל-`SituationVector` את השדות שחסרים להן (‏`classification_prefix` = 12 התווים הראשונים של `classification`, ו-`prior_zone` כבר קיים) ולכתוב אותן כ-`expr:` אמיתי; (ג) **Rule A רחב מדי** — `day_type in [Variation…] and extension != 'none'` מתיר גם כניסה **נגד** ההרחבה. לתקן ל-`… and ((extension == 'down' and direction == 'SHORT') or (extension == 'up' and direction == 'LONG')) and entry_kind in ['BREAK','PULLBACK']`, ולהוסיף שורה: כניסה **נגד** ההרחבה ביום-Variation מותרת רק ב-`zone in ['above_value','below_value']` (הקיצון — פסיקת-מייקל 15.09 23:15 "לחכות שיגיע לקיצון"), אחרת `block`. **טסטים:** golden 15.09 19:10 GHOST SHORT (with-extension, mid_value) ⇒ allow; LONG באותו רגע ⇒ block; LONG ב-below_value ⇒ allow.

## F6 · T-392c — שתי שורות-טיוטה חדשות לעץ v2 מהלקח של 16.09 (צל בלבד, לא בנתיב-הירי)

מקור: [[T-397]] (פסיקת-מייקל 17.09 14:05, שלב D פתוח לימי-מגמה — כבר חי בפלייבוק מ-14:02) + ריפליי 16.09: העסקה שנתפסה (21:50 GHOST SHORT) לקחה 18 נק' מתוך mfe 81, ו-INITIATIVE_SHORT 21:30 @7678 (בר 25,987 חוזים מול ~4k) נשאר חסום כי התווית הייתה עדיין Variation.
1. **ראנר על המשך-מגמה בשלב D:** שורת-`expr:` ב-`config/dalton_tree_v2_draft.yaml`: `phase == 'D' and day_type in ['Trend_Normal','Trend_DD','Neutral_Extreme'] and ((extension == 'down' and direction == 'SHORT') or (extension == 'up' and direction == 'LONG'))` ⇒ `decision: allow`, שדה חדש `runner: true` (מידע לצל בלבד; `[TREE-DIFF]` מדפיס `runner=` כדי שנספור כמה פעמים זה היה חל). **לא** לשנות את הפלייבוק החי (`runner: false` נשאר — פסיקה).
2. **ענף-ווליום לשבירת-IB:** `entry_kind == 'BREAK' and vol_ratio >= 3.0 and ((extension == 'down' and direction == 'SHORT') or (extension == 'up' and direction == 'LONG') or extension == 'none') and zone in ['near_val','below_value','near_vah','above_value']` ⇒ `allow` (ה-21:30 של 16.09: הבר עצמו שובר את ה-IB-low, `vol_ratio ≈ 6`). דורש F4 (‏`vol_ratio` חי). ב-`gap_analysis.py` (F1-F3) לחשב `vol_ratio` היסטורי לכל setup מהברים (אותה הגדרה סיבתית: הבר מול חציון אותה-דקה ב-10 סשנים קודמים) כדי שהדוח ייתן N ו-Σ$ לענף הזה על 85 הסשנים.
טסטים: שתי השורות נטענות, golden 16.09 21:50 GHOST SHORT ⇒ allow+runner, golden 16.09 21:30 INITIATIVE_SHORT עם vector{vol_ratio:6.2, extension:'none', zone:'near_val', entry_kind:'BREAK'} ⇒ allow.

---

**דיווח:** אחרי כל פריט — LOG חתום עם פלט גולמי, `TASK_LOG` (‏T-389b/c/d כתת-שורות של T-389; T-390b; T-392b), commit+push, "סיים פריט N" **וממשיך**. אחרי F1-F3: להריץ `gap_analysis.py --dry --session 2026-09-15` ולהדביק את הפלט (סשן אחד — מותר בזמן RTH). הריצה על 85 הסשנים — cowork, הלילה אחרי 23:05; הקוד של F4/F5 נטען בריסטארט של מחר.

---

# חלק ב' (cowork 17.09 14:55, אחרי אימות F1-F6 והרצת הסריקה v1 על 76 סשנים) — F7-F10, ברצף, אותם גבולות

**מה נמצא בסקירה של v1 (‏`GAP_ANALYSIS_2026-09-17.md` 14:45):** (1) F4 לא עבד בלייב — `five_min_system._cf_bars` הוא משתנה מקומי, לא attribute, ושאילתת-הסשנים החזירה שורות בלי `ts` ⇒ תוקן ע"י cowork (‏`c16d5d19`: `situation_vector.load_bars_for_vector`) — **ללמוד מזה: "הטסט עבר" על פונקציה טהורה אינו ראיה שהחיווט חי; לכל חיווט — probe על ה-DB האמיתי בפלט.** (2) `NO_SETUP` מכיל גם setups **שנחסמו בשער ולא נרשמו כעסקה** — מבחינה היסטורית הם קיימים רק ב-`~/SierraChart_Data/v9_export/gateway_decisions.jsonl` + `decisions_archive/` ⇒ הדוח לא מבדיל פער-מפיק מפער-שער. (3) "No matching bar: 148 מתוך 186 עסקאות-לייב" — באג התאמה: בדגימה של 5 עסקאות יש 2 ברים בתוך ±5 דק' לכל אחת. (4) 06-09/06-10 הם ימים אמיתיים (‏65k חוזים בבר, טווח 306) **אבל** `v9_bars_5min_woodies` ו-`v9_bars_5min_continuous` נבדלות ב-60 נק' ב-06-10 23:00 (‏7274 מול 7334) — גלגול-חוזה יוני (‏MESM26→MESU26, ‏11-12.06) — ה-"175 נק' ב-22:40-23:00" הוא ארטיפקט-גלגול, לא תנועה. (5) שורות-BAR ב-`v9_decision_vectors` עם חותמות של אתמול (25 שורות עד 23:55 של 16.09) — ה-hook רושם ברי-הידרציה בבוט.

## F7 · T-389e — `BLOCKED:<gate>` מארכיון-החלטות-השער, לא רק מעסקאות
לקרוא `gateway_decisions.jsonl` + `decisions_archive/*` (לבדוק את הסכימה בפועל — `head -3` בפלט), לבנות אינדקס `(session, il_time±5min, direction) → blocked_by/FIRED`, ובסיווג-התנועה: setup חסום בשער בתוך 30% הראשונים של התנועה ובכיוונה ⇒ `BLOCKED:<blocked_by>` (לא `NO_SETUP`). בדוח: פילוח `NO_SETUP` מול `BLOCKED:*` לפי שער, לכל סוג-יום. **טסט:** תנועה עם החלטת-שער חסומה בחלון ⇒ BLOCKED; בלי ⇒ NO_SETUP.

## F8 · T-389f — באג "no matching bar" (148/186)
לאתר את ההשוואה (‏tz-naive מול aware? שוויון מדויק במקום floor ל-5 דק'?) ולתקן: הבר של הכניסה = הבר שמכיל את `entry_ts` (‏floor ל-5 דק' ב-UTC). להדפיס לפני/אחרי: `matched/unmatched` מתוך 186. **טסט:** entry_ts 16:50:07 IL ⇒ בר 16:50.

## F9 · T-389g — חלונות-גלגול וסדרת-הברים לניתוח
לסמן חלון-גלגול (‏3 ימי-מסחר לפני פקיעת-החוזה הרבעונית: יוני 17-19, ספט' 16-18 של 2026 — לחשב מהלוח, לא לקודד תאריכים) כ-`data_quality: ROLL`, ולהוציא מהסיכומים כמו SUSPECT; ולדווח לכל סשן `|woodies_close − continuous_close|` ב-23:00 — סשן עם הפרש > 10 נק' ⇒ `ROLL_MISMATCH`. **לא** להחליף את מקור-הברים (SoT: woodies היא הסדרה החיה); רק לסמן ולדווח.

## F10 · T-390c — שורות-BAR: רק ברים חיים, לא הידרציה
ב-hook של `bar_router`: לרשום `kind='BAR'` רק כשהבר סגור **בתוך 10 הדקות האחרונות** (‏`now − bar_ts < 600s`), ולדלג על הידרציה/ריפליי (אותו anti-phantom כמו `five_min_system.py:1780`). לנקות את 25 שורות-ההידרציה מ-16.09 (‏`DELETE … WHERE kind='BAR' AND ts < '2026-09-17'`) — פקודה אחת, בפלט. **טסט:** בר בן שעה ⇒ לא נרשם.

אחרי F7-F9: להריץ `gap_analysis.py --start 2026-06-01 --end 2026-09-16` **רק אחרי 23:05** (בזמן RTH: `--dry --session 2026-09-16` בלבד) ולהדביק את טבלת-סוגי-היום ואת פילוח NO_SETUP/BLOCKED. cowork כותב מזה את עמוד-הענפים ליום א'.

---

# חלק ג' (cowork 17.09 17:35, מהעסקה החיה #1806) — F11, מחוץ ל-RTH, נטען מחר לפני הפתיחה

## F11 · T-404a — שדות-פתיחה בווקטור (‏`SituationVector`)
מהברים הסגורים של היום (‏`bars_rth_today` — כבר מחוברים דרך `load_bars_for_vector`): `open_price` (open של בר 16:30) · `bars_since_open` (מספר ברי-RTH סגורים) · `move_from_open_pts` (‏`close_last − open_price`, חתום) · `move_from_open_atr` (‏`|move| / atr_causal`; None כש-ATR None) · `open_move_dir` (‏'up'/'down'/'flat' לפי סימן ה-move ו-|move| ≥ 0.5×ATR) · `retrace_from_open_pct` (כמה מהמהלך-מהפתיחה כבר חזר: `(extreme − close_last)/(extreme − open) × 100`, 0-100). סיבתי, ברים סגורים בלבד, fail-open. להוסיף ל-`asdict` ולשמות המותרים ב-`expr:`. **טסטים:** 3 ברים סינתטיים: פתיחה 7713.5, שפל 7680.5, סגירה 7683 ⇒ `open_move_dir='down'`, `move_from_open_atr` ≈ 30/6.9, `retrace_from_open_pct` ≈ 7.6; ושורות T-404/T-403 בטיוטה נטענות ומוערכות על הווקטור הזה (‏#1806 ⇒ t403 block; 16:40 FAILED_BREAK_SHORT עם bars_since_open=2, move 3.0×ATR, retrace 20% ⇒ t404 allow). **probe בפלט** על `v9_decision_vectors` של היום אחרי הריסטארט של מחר — לא רק pytest.

---

# חלק ד' (cowork 17.09 18:00, מייקל בזמן-אמת: "אני רואה מבנה נרות נשבר ווליום שמעיד על שבירה — למה אין לי תבנית שיודעת לזהות את זה?") — F12 + F13, מחוץ ל-RTH

**הראיה (17.09 17:45 IL):** הבר 7699.5→7691.25 (סגירה 7693.25) שבר את השפלים של 17:20-17:40 (‏7696-7697) עם **דלתא −3,093** (חציון-|דלתא| של הסשן: 834 ⇒ ×3.7) — ולא ירה שום מפיק-BREAK. מה שירה: VA_FADE_SHORT (צל) ו-ZLR SHORT ב-17:50 (צל), ושניהם נחסמו `location zone=mid_value`. **מפקד-המפיקים (1,490 כניסות לייב+צל יוני-ספט', מודל אחיד 2 רגליים):** כל המפיקים שליליים חוץ מ-HTLB (+$26/עסקה, N=16) ו-TLB (+$3); חציון-MFE 2-5 נק' — המפיקים יורים **אחרי** המהלך (INITIATIVE = תבנית 4 ברים ⇒ 17:10 בשפל). `S2_DELTA_DBL`: 174 כניסות, **0% הצלחה** — המפיק היחיד שמשתמש בדלתא שבור.

## F12 · T-406 — מפיק חדש `BREAK_DELTA` (הגדרה מדויקת, קודם מדידה, אחר-כך צל)
**ההגדרה (בר 5-דק' סגור, סיבתי):** (1) **שבירת-מבנה:** `close < min(low של 3-6 הברים הקודמים)` לשורט (סימטרי ללונג) **או** close מעבר ל-LSMA/VAL/VAH/IB-edge כשהבר הקודם היה בצד השני; (2) **דלתא מאשרת:** `bar_delta <= −2.5 × median(|delta|)` של הסשן עד כה (מ-`v9_bars_cumulative_delta`, 83 סשנים מיוני) **ו-**`vol_ratio >= 1.3`; (3) **סגירה בקצה:** close ב-30% התחתונים של טווח-הבר; (4) **לא מרדף:** `move_from_open_atr < 3` **או** `bars_since_low >= 2` (שבירה של פולבק, לא של קצה טרי — הלקח של #1806). **כניסה:** פתיחת הבר הבא (‏market) · **סטופ:** מעל high של בר-השבירה + 1 טיק (או מעל הסווינג אם קרוב יותר מ-1.5×ATR) · **יעדים:** T1 = 1×ATR, T2 = מהלך-נמדד של הרגל הקודמת, ראנר ביום-מגמה. `entry_kind = BREAK`, `classification = BREAK_DELTA_SHORT/LONG`, דגל `BREAK_DELTA_V1` **ברירת-מחדל `shadow`** בקוד (לא env).
**קודם מדידה:** `scripts/pattern_census.py` (חדש; לשמור את הקוד שהריץ cowork ב-17:35 — הטבלה `pattern × phase × N × win% × $/tr × medMFE` — ולהוסיף לו את BREAK_DELTA כסימולציה על 83 הסשנים עם דלתא): N, win%, $/עסקה, MFE/MAE, לפי שלב וסוג-יום-סופי. **סף לצל-חי:** N ≥ 30 ו-$/עסקה > 0 על 83 סשנים; אחרת NOT-DONE עם המספר.

## F13 · T-407 — מפקד-המפיקים כחלק מהסריקה הלילית + שני כיבויים לפסיקה
להעביר את `pattern_census` לתוך `gap_analysis.py` (סעיף "Producers") כדי שכל לילה יוצא: לכל תא `pattern × phase`: N · win% · $/עסקה · medMFE · medMAE. **ולדוח:** שני המפיקים החיים עם התאים הגרועים ביותר, למייקל לפסיקת-כיבוי: `OPENING_DRIVE` בשלב A (‏N=11, 9% הצלחה, −$121/עסקה — העסקה הראשונה של 16.09) ו-`DOUBLE_TOP_AA` (‏N=19, 5%, −$26). **cc לא מכבה** — מדווח.

---

# חלק ה' (cowork 17.09 18:15, מייקל: "לייצר תרחיש על כל הזמן-עבר … כניסה שבוחנת מחיר, מיקום, ווליום … תקרה כפולה, רצפה כפולה") — F14, מחוץ ל-RTH

**מה כבר קיים:** `scripts/oracle_study.py` (cowork, 18:05) — מתייג כל בר-RTH-5-דק' ב-62 סשנים נקיים לפי מה שקרה **אחריו** (GOOD_SHORT = ירידה ≥max(8, 1.5×ATR) לפני עלייה ≥max(5, 1×ATR) תוך 12 ברים; סימטרי ללונג), מחשב מאפיינים **סיבתיים** באותו בר (ווליום-יחסי, דלתא-יחסית, מיקום-סגירה, שבירת-5-ברים, ברים מהקיצון, מהלך-מהפתיחה ב-ATR, רצפה/תקרה כפולה עם שבירת-צוואר) ומדפיס לכל תנאי N · good% · lift. **הפלט הראשון (3,949 ברים):** בסיס שורט 40% / לונג 32% · שבירה+דלתא≤−2×+ווליום≥1.3: 44% (שלב C: **57%**, N=34; שלב D: 27%) · שבירה בשפל-טרי אחרי ≥3×ATR: **34%** (−6pp — הלקח של #1806 במספר) · **רצפה-כפולה עם שבירת-צוואר: 50% (+18pp), N=21** · שבירה-למעלה+דלתא+ווליום לא-בקצה: 43% (+11pp).

## F14 · T-408 — ה-Oracle כמנוע-גילוי בסריקה הלילית
1. להעביר את `oracle_study.py` לתוך `gap_analysis.py` (סעיף "Oracle") ולהריץ על כל הסשנים הנקיים; להוסיף **ניהול** לתיוג: לכל בר-GOOD — MFE/MAE ב-12 הברים, כדי שכל תנאי יקבל גם `$/עסקה` במודל הקבוע (סטופ = 1×ATR מעבר לבר, T1 = 1.5×ATR, T2 = 2.5×ATR, BE אחרי T1) ולא רק good%.
2. **תנאים נוספים, כולם סיבתיים:** ראש-כתפיים (3 שיאים, האמצעי גבוה, שבירת-צוואר) · ספל-ידית (שפל מעוגל ≥8 ברים + ידית ≤4 ברים + שבירה) · פולבק-במגמה מדויק: `bars_since_extreme 2-6 ∧ retrace 30-60% מהרגל ∧ בר-חידוש (סגירה בשליש הנכון) ∧ vol_ratio ≥ 1.0` · שבירה מאזור-ערך (VAL/VAH מהסשן הקודם — `pd_ctx`) עם דלתא · מיקום: `zone` מול VA מתפתח (לחשב TPO-VA סיבתי מהברים, כמו בהרנס). לכל תנאי גם פילוח לפי **שלב** ו-**סוג-יום-סופי** (F1).
3. **פלט לדוח:** טבלת "10 התנאים הטובים" (‏N ≥ 30, lift ≥ +8pp, $/עסקה > 0) ו-"10 הגרועים" (מה **לא** לעשות — למשל מרדף בקצה) — כל אחד כשורת-`expr:` מוכנה לעץ. **זה מה שמייקל מאשר ביום א'.**
4. טסטים: תיוג על סדרה סינתטית (ירידה חדה ⇒ GOOD_SHORT; רוטציה ⇒ BAD/NONE) · רצפה-כפולה סינתטית ⇒ `dbl_b=True` רק אחרי שבירת-הצוואר.

---

# חלק ו' (cowork 17.09 18:25, מייקל: "יש משהו שקלוד קוד יכול לבדוק כדי לדייק את זה?") — F15 · T-409: פרוטוקול-דיוק ל-Oracle, מחוץ ל-RTH

**המטרה:** שהמספרים של `oracle_study.py` (‏62 סשנים, 3,949 ברים) יהיו **ראיה** ולא רושם. כל בדיקה = פונקציה + פלט גולמי בדוח `docs/reports/ORACLE_VALIDATION_2026-09-18.md`. אין לשנות את הגדרות-התנאים כדי "לשפר" תוצאה — מדווחים מה יצא.

1. **עמידות-התיוג (threshold sweep):** להריץ את התיוג עם אופק K ∈ {6, 12, 24} ברים ויחסי יעד/סטופ ∈ {1.5×ATR/1×ATR, 2×/1×, 1×/1×}. לכל תנאי בטבלה: ה-lift בכל 9 השילובים. תנאי "אמיתי" שומר סימן-lift ברוב השילובים; תנאי שמתהפך — נזרק.
2. **מחוץ-לדגימה (OOS):** לחלק את הסשנים: גילוי = יוני-יולי, אימות = אוגוסט-ספטמבר. לדווח לכל תנאי: lift בגילוי, lift באימות. **רק תנאים שה-lift שלהם חיובי בשני החצאים** עוברים הלאה. בנוסף: פילוח לפי יום-בשבוע ולפי שעה (16:30-18:00 / 18:00-21:00 / 21:00-23:00).
3. **מובהקות:** רווח-בר-סמך Wilson 90% ל-good% של כל תא; תא נחשב רק אם **הגבול התחתון** מעל הבסיס. להדפיס N, good%, [lo, hi].
4. **ריאליזם של ניהול:** להמיר good% ל-**$/עסקה**: כניסה בפתיחת הבר הבא (לא בסגירה), החלקה 1 טיק לכל צד, עמלה+דמי-סליקה MES ≈ $1.30 לסיבוב לחוזה (לאמת מול חשבונית-הברוקר האחרונה ב-Sierra), סטופ = 1×ATR מעבר לבר-האות (מינימום 5 נק'), T1 = 1.5×ATR, T2 = 2.5×ATR עם BE אחרי T1, AMBIG לא-מוקצה, סגירה ב-EOD. לכל תנאי: expectancy, MFE/MAE חציוניים, אחוז-AMBIG.
5. **רשת-פרמטרים עם אזהרת-overfit:** חלון-שבירה {3, 5, 8} ברים · מכפיל-דלתא {1.5, 2, 3} · ווליום {1.0, 1.3, 1.6} · רצפה-כפולה: סובלנות {0.2, 0.3, 0.5}×ATR, מרחק-מינימלי {3, 5} ברים. **לדווח את הרמה (plateau), לא את התא הטוב ביותר:** תנאי טוב הוא זה שכל השכנים שלו ברשת גם חיוביים.
6. **פירוק-שילובים:** לכל תנאי מורכב (A∧B∧C) — lift של A לבד, B לבד, A∧B, A∧B∧C. איזה רכיב תורם, איזה רק מקטין N.
7. **המפיקים מול ה-Oracle:** לכל כניסת-מפיק (לייב+צל, 1,490) — האם הבר שלה היה GOOD לפי ה-Oracle? ⇒ לכל מפיק: "בחירת-ברים" (good% של הברים שהוא בחר מול הבסיס). ובכיוון ההפוך: כמה מהברים-GOOD ב-62 הסשנים לא נבחרו ע"י אף מפיק — זה פער-המפיקים ברמת-בר, המספר המדויק ל-"אין setup".
8. **איכות-נתונים:** כיסוי-דלתא לפי סשן (ברים בלי דלתא — כמה, ואיפה), כפילויות ב-`v9_bars_cumulative_delta`, הפרשי-ts בין הברים לדלתא, סשנים חלקיים (<70 ברים); לכל פגם — האם הוא משנה תא כלשהו בטבלה (להריץ עם ובלי).
9. **walk-forward של 5 התנאים הטובים** (אחרי 1-6): סימולציה יום-אחרי-יום עם המודל הקבוע, סלוט אחד, 2 חוזים: עקומת-הון, drawdown מקסימלי, עסקאות/יום, אחוז-ימים ≥ $200, אחוז-ימים מפסידים. **זה המדד של מייקל** — לא good%.
10. **טסטים:** תיוג על סדרות סינתטיות (ירידה חדה ⇒ GOOD_SHORT · רוטציה ⇒ BAD/NONE · שני יעדים באותו בר ⇒ AMBIG) · רצפה-כפולה סינתטית עם/בלי שבירת-צוואר · OOS-split דטרמיניסטי.

**פלט:** `ORACLE_VALIDATION_2026-09-18.md` — טבלה אחת: תנאי · N · good% [CI] · lift-גילוי · lift-אימות · $/עסקה · plateau ✓/✗ · המלצה (עץ / לזרוק / N קטן). **בלי שאלות; מה שלא ניתן לחישוב — NOT-DONE עם הסיבה.**

---

# חלק ז' (cowork 17.09 19:00, סקירת `ORACLE_VALIDATION_2026-09-18.md`) — F16: שלושה פגמים בבודק עצמו, לפני שמאמינים לפרטים

**מה הדוח כבר אומר ולא ישתנה:** רוב התנאים נופלים בעשר הבדיקות (זו הצלחת השיטה) · **המפיקים בוחרים ברים גרוע מאקראי** (‏S2: 85 GOOD מול 140 BAD · S4: 105 מול 263; אקראי = 32-40% GOOD) ו-**85.3% מהברים-הטובים (1,893/2,218) אף מפיק לא בחר** · walk-forward של 5 התנאים הטובים: 36 עסקאות/62 ימים, +$320, DD $675, 0% ימים ≥ $200.

## F16 · T-410 — תיקוני-בודק
1. **רשת-הפרמטרים לא מיישמת את `window`:** `oracle_validate.py:285-300` — `sel()` קורא `r['break_dn']` שחושב מראש עם חלון 5 ⇒ כל שלוש עמודות-החלון זהות (‏41.0/41.2/40.4 בכל w) ו-`plateau=True` חסר-משמעות. לחשב `break_dn_w{3,5,8}` בשלב-המאפיינים ולהשתמש בהם ברשת. אותו דבר לרצפה-כפולה (סובלנות, מרחק).
2. **645 חותמות-זמן כפולות ב-`v9_bars_cumulative_delta`** מיוני (למשל 25.08 23:45/23:49:59/23:55 ×2) ⇒ `delta_ratio` עלול להיות מוטה. לפני החישוב: `DISTINCT ON (ts) … ORDER BY ts, created_at DESC` (השורה האחרונה לכל ts), ולדווח כמה ברים השתנו בעקבות זה. **לא** למחוק מה-DB (זו כתיבה בטבלת-ייצור — פריט נפרד לכל היותר, לא היום).
3. **Step 8 מדפיס `Total sessions: 0`** — באג-דיווח (המשתנה לא מאוכלס); לתקן, ולהוסיף: 4 הסשנים עם פערי-ts > 10 דק' — אילו, והאם הם ברשימת-ההוצאה.
4. **תוספת אחת לתוכן (אחרי 1-3):** תאי **תנאי × שלב × סוג-יום-סופי** לחמשת התנאים ששרדו (רצפה-כפולה, שבירה+דלתא+ווליום לא-בקצה לשני הכיוונים, פולבק-במגמה-לונג, פולבק-לונג-קלאסי) — עם אותם CI/OOS. השאלה: האם ההקשר (שלב, סוג-יום) הופך תנאי-גבולי לתנאי-ברור. להריץ מחדש את walk-forward על התאים שעוברים.
פלט: `ORACLE_VALIDATION_2026-09-18.md` מעודכן (v2), אותה טבלה + סעיף "תאים". מחוץ ל-RTH מותר הכל חוץ מהרנס; בזמן RTH — `nice`, תהליך אחד.

---

# חלק ח' (cowork 17.09 19:15, מייקל: "תבנית = מיקום × רצף-ברים × בר-טריגר × ווליום") — F17 · T-411: מנוע-סטאפים רב-ברי + אימות שני הסטאפים הראשונים

**הסמכות:** `docs/spec_authority/SETUP_GRAMMAR_2026-09-17.md` — הדקדוק (מיקום · הקשר · רצף · טריגר · ווליום), סטאפ #1 "רצפה כפולה בקצה-הערך עם ספיגה" (17.09 16:40-17:10, הדוגמה של מייקל, הברים והדלתא בתוך המסמך) וסטאפ #2 "שבירת רוטציה אחרי דרייב" (17.09 17:45). **F16 (תיקוני-הבודק) קודם, ואז F17.**

1. `scripts/setup_engine.py` (חדש): `detect_setups(bars_with_delta, levels, params) -> list[SetupHit]` — לכל בר-סגור בודק אם הוא **טריגר** של סטאפ לפי הדקדוק; מחזיר `{setup_id, direction, trigger_ts, entry, stop, t1, t2, sequence:[bars], location, evidence:{delta_seq, delta_trigger, vol_ratio_trigger, absorption}}`. סיבתי-מוחלט: רק ברים סגורים עד הטריגר; רמות-הערך של אתמול מ-`pd_ctx`/TPO-של-אתמול, VA-מתפתח מחושב מהברים (כמו בהרנס). דלתא: `DISTINCT ON (ts)`.
2. **שני הסטאפים בדיוק כפי שכתובים במסמך**, הפרמטרים כ-`params` (לא קבועים בקוד) — כדי שרשת-ה-plateau תוכל להזיז אותם.
3. **golden:** 17.09 — סטאפ #1 חייב לירות LONG בטריגר 17:10 (כניסה בפתיחת 17:15, סטופ 7680.25, T1 ≈ IB-mid 7698.4, T2 ≈ VAH 7703.75) ⇒ T1+T2 נפגעים ב-17:20-17:25; סטאפ #2 חייב לירות SHORT ב-17:45 (כניסה 17:50 open, סטופ 7704.25) ⇒ T1 ב-17:50. **ואסור** שיירה LONG ב-16:50 (רק נגיעה ראשונה, אין ספיגה עדיין) ולא SHORT ב-17:10 (זה הכיוון ההפוך של #1806).
4. **אימות:** אותו `oracle_validate` — לכל סטאפ: N על כל ההיסטוריה הנקייה · good% [CI] · lift-גילוי/אימות · $/עסקה עם החלקה+עמלות · plateau על 3 הפרמטרים המרכזיים של כל סטאפ (סובלנות-השפלים, מכפיל-הספיגה, סף-דלתא-הטריגר / N-ברי-הרוטציה, התכווצות, ווליום-הטריגר) · walk-forward של שני הסטאפים יחד (סלוט אחד, 2 חוזים). **פלט:** `docs/reports/SETUP_VALIDATION_2026-09-18.md` + כל ה-hits ל-`harness_out/setups/hits.json` (לצפייה בצ'ארט).
5. **חיבור לעץ (צל):** hit של סטאפ ⇒ `classification = SETUP_DBL_BOTTOM_ABS_LONG` / `SETUP_ROTATION_BREAK_SHORT`, `entry_kind` = REVERSAL / BREAK; שורת-`expr:` בטיוטת-העץ לכל סטאפ (`setup_id == …` ⇒ allow) — צל בלבד, `[TREE-DIFF]` יראה מחר בלייב מתי הם היו יורים. **לא נתיב-ירי.**
6. טסטים: golden 17.09 (שני הסטאפים, שני ה-"אסור"); סדרות סינתטיות: רצפה-כפולה בלי ספיגה ⇒ אין hit; רוטציה בלי התכווצות-ווליום ⇒ אין hit; טריגר בלי סגירה-בקצה ⇒ אין hit.

---

# ⚠️ תיקון-כיוון ל-F17 (cowork 17.09 19:40, מייקל: "לבחון על התבניות שלנו — בעיקר ריאקטיב ואיניאטיב — ולדייק, לא לבנות מחדש")

**F17 כפי שנכתב ("מנוע-סטאפים חדש") — מבוטל בצורתו.** במקומו: **F17b · T-412 — דיוק REACTIVE/INITIATIVE הקיימים ב-`five_min_system.py` עם ראיות מיקום · נר-טריגר · ווליום/דלתא, ושימוש-חוזר בגלאי-הצורה שכבר קיימים ב-`scripts/oracle_engine.py`.** הדקדוק (`SETUP_GRAMMAR_2026-09-17.md`) נשאר הסמכות למה שנחשב ראיה; הוא מיושם **בתוך** המפיקים הקיימים, לא לצידם.

**הראיה — `scripts/pattern_evidence_study.py` (cowork 19:35) על 164 כניסות REACTIVE/INITIATIVE (לייב+צל, יוני→, מודל קבוע 2 רגליים):**
```
                      עם trigger_ok                בלי trigger_ok
REACTIVE LONG    N=11  win 64%  +$41.7/עסקה      N=28  win 11%  −$70.6
REACTIVE SHORT   N=12  win 42%   +$4.6           N=42  win 19%  −$22.9
INITIATIVE LONG  N= 8  win 38%  −$25.6           N=24  win 17%  −$76.3
INITIATIVE SHORT N= 9  win 78%  +$52.8           N=30  win 10%  −$32.0
trigger_ok = בר-הכניסה סוגר ב-30% הקיצוניים בכיוון העסקה (לונג: cp ≥ 0.7; שורט: cp ≤ 0.3)
INITIATIVE SHORT + delta_with (δ_trigger ≤ −1×חציון): N=6 win 67% +$59.8  · בלי: N=33 win 18% −$25.5
INITIATIVE SHORT + vol_trig  (v_trigger ≥ 1.3×חציון-5): N=4 win 75% +$27.8
loc_edge (שורט בקצה-ערך עליון / ליד VAL-VAH של אתמול): REACTIVE S N=10 +$43.5 · INITIATIVE S N=6 +$28.8 (בלי: שלילי)
absorption (ההגדרה של cowork): N=0 — ההגדרה קשוחה/שגויה, לתקן (ראה 3)
```
**המסקנה המדידה:** "בר 4 = אישור" של ה-Constitution בודק רק `close > open`; מה שמפריד מנצחות ממפסידות הוא **עוצמת בר-הטריגר** (סגירה בקצה), ואצל שורטים גם **דלתא וווליום בטריגר** ו**מיקום בקצה-הערך**. זה תיקון בתוך המפיקים, לא מפיק חדש.

## F17b · T-412 — מה לבנות
1. **`backend/v9/systems/five_min/evidence.py` (חדש, טהור, סיבתי):** `trigger_quality(bar, direction) -> {cp, body_ratio, range_atr, ok}` · `delta_with(bar_delta, median_abs_delta, direction)` · `vol_trigger(bar_v, prev5_median)` · `location(price, dev_va, prev_va, ib, atr) -> {zone, near_prev_edge, at_edge_for(direction)}` (לונג = ליד **VAL**/קצה-תחתון, שורט = ליד **VAH**/קצה-עליון — לא "מתחת לערך") · `absorption(bars[-3:-1], direction, median_abs_delta, atr)` — **הגדרה מתוקנת:** לפחות בר אחד מהשניים עם `|δ| ≥ 1.5×חציון` **נגד** כיוון-העסקה, והתקדמות-המחיר בכיוון-הנגד ≤ 1×ATR (17.09 17:00-17:05: δ −2,855/−1,372, התקדמות 7.75 נק' ⇒ True) · **צורות** — להעביר את `detect_head_shoulders_*`, `detect_cup_handle_long`, `double_bottom/top` (מ-`pattern_evidence_study.py`) לכאן, אותה חתימה, כדי שהמפיקים והסריקה ישתמשו **באותו קוד**.
2. **REACTIVE / INITIATIVE:** ב-`_detect_reactive` ו-`_detect_initiative` — אחרי שהגיאומטריה הקיימת עברה, לחשב את הראיות ולצרף ל-`info["evidence"]` (נכנס ל-`metadata` ומשם ל-`cross_context`/וקטור). **מתג `S2_TRIGGER_QUALITY_V1`** (ברירת-מחדל `shadow` בקוד): `shadow` = רק רישום `[S2-EVIDENCE] trigger_ok=… delta_with=… loc=…` על כל ירי; `1` = ירי רק עם `trigger_ok` (ולשורט: גם `delta_with or vol_trig`). **לא להדליק** — הריפליי על ההיסטוריה (3) מכריע, ואז פסיקת-מייקל.
3. **ריפליי:** `pattern_evidence_study.py` עם ההגדרות המתוקנות (מיקום לפי כיוון, ספיגה מתוקנת) + פילוח שלב; ו-**הרנס** על 5 סשנים עם `S2_TRIGGER_QUALITY_V1=1` (env-עותק) מול `shadow`: routes, live_cmds, Σ$ במודל הקבוע — **אחרי 23:05**.
4. **golden 17.09:** INITIATIVE_SHORT 17:10 @7683 — `trigger_ok=False` (הבר 17:05 סגר ב-36% מהטווח, לא בקצה; ובכל מקרה 0 ברים מהשפל) ⇒ בצל: נרשם "היה נחסם"; לונג 17:10 (אם REACTIVE_LONG היה מזהה): cp 0.82, δ +3,319, ⇒ `trigger_ok=True, delta_with=True`.
5. טסטים: כל פונקציית-ראיה על ברים סינתטיים; `_detect_reactive` עם `S2_TRIGGER_QUALITY_V1=shadow` ⇒ אותה תוצאה כמו היום (אפס שינוי-התנהגות) + `evidence` ב-info.
**סדר:** F16 → F17b. F14/F15 כבר בוצעו. **אין מנוע חדש.**

---

# חלק ט' (cowork 17.09 20:20, מייקל: "הרצה על כל הימים — הסטאפים שהיו צריכים להיכנס כדי ליהנות מהתנועות הארוכות, בכל יום יש לפחות 2") — F18 · T-413

**מה כבר רץ (`scripts/ideal_entries.py`, cowork 20:15):** 55 סשנים נקיים · 2 הרגליים הגדולות בכל יום (זיגזג 1×ATR, רגל ≥ max(10, 1.5×ATR)) = 110 רגליים, חציון 29 נק'. לכל רגל — **הכניסה שהייתה צריכה לקרות:** הבר הראשון בחצי-הראשון של הרגל שממנו עסקה בכיוון הרגל, עם סטופ ≤ 1×ATR מעבר לבר, מגיעה ל-1.5×ATR בלי להיעצר.
```
וריאנט A — הבר המוקדם ביותר (גם בלי אישור):        65/110 רגליים (59%) · כניסה בבר 0 = בר-הפיבוט (מזוהה רק בדיעבד) · דלתא-עם 6%
וריאנט B — בר-טריגר חזק (סגירה ב-30% הקיצוניים):   67/110 (61%) · בר 1 של הרגל · נלקח חציוני 23.5 נק' · סטופ 5.5
וריאנט C — טריגר חזק + (ווליום ≥1.3 או דלתא-עם):    49/110 (45%) · בר 1 · נלקח חציוני 21.5 נק' · סטופ 5.8
   מה נראה בבר-הכניסה של C: טווח ≥0.8×ATR 88% · דלתא-עם 69% · ווליום-מתרחב 45% · שבירת-מבנה 35% · פולבק-לפני 49% ·
   ליד קצה-IB 29% · עם-היום 45% (חצי מהרגליות הגדולות הן היפוכים) · אזור: בתוך-ערך 51% / מעל 33% / מתחת 16% ·
   22 ברים (חציון) מהקיצון בכיוון-העסקה — כלומר **לא** בקיצון; זה ההבדל מ-winner_profile (המשך-בקיצון-עם-ווליום)
```
**המשמעות:** ~0.9 כניסות-מאושרות ליום × 21.5 נק' × 2 חוזים ≈ $215/יום **אם תופסים את כולן** — זה גודל ההזדמנות, לא תחזית.
**מה חסר, וזה F18:** בחירת "2 הרגליים הגדולות" היא בדיעבד. המאפיינים סיבתיים, הבחירה לא. השאלה הפתוחה היא **דיוק (precision):** כמה ברים בהיסטוריה נראים כמו כניסות-C (טריגר חזק + דלתא/ווליום + טווח ≥0.8×ATR [+ שבירת-מבנה / פולבק-לפני]) — ומה חלקם שהובילו ל-≥1.5×ATR לפני 1×ATR נגד? בלי זה יש רשימה של "איך נראתה כניסה טובה" ואין "מתי להיכנס".

## F18 · T-413 — precision לחתימות של הכניסות-האידיאליות
1. להעביר את `ideal_entries.py` ל-`oracle_engine` (אותם ברים/דלתא/מאפיינים — קוד אחד), לשמור את 49 הכניסות-C כ-**סט-golden** (`harness_out/oracle/ideal_C.json`) עם `d, entry_il, dir`.
2. **חתימות:** S1 = `trigger_ok ∧ range≥0.8ATR ∧ (delta_with ∨ vol_trig)` · S2 = S1 ∧ `structure_break` · S3 = S1 ∧ `pullback_before` · S4 = S1 ∧ `near_ib_edge` · S5 = S1 ∧ `zone != IN_VA` · S6 = S1 ∧ `with_day`. לכל חתימה על **כל** הברים ב-55 הסשנים: N-מופעים · recall (כמה מ-49 ה-golden נתפסו) · precision (כמה מהמופעים הגיעו ל-1.5×ATR לפני 1×ATR) · $/עסקה במודל הקבוע (כניסה בבר הבא, החלקה+עמלות) · OOS יוני-יולי/אוג'-ספט' · Wilson CI. **plateau** על סף-הדלתא (1.0/1.5/2.0) וסף-הווליום (1.2/1.3/1.5).
3. **walk-forward** של החתימה הטובה (precision×recall) יום-אחרי-יום, סלוט אחד, 2 חוזים: עקומת-הון, DD, עסקאות/יום, % ימים ≥ $200 — **המספר שמייקל שואל עליו.**
4. **פילוח לשתי המשפחות:** (א) כניסת-תחילת-רגל (‏`bars_from_extreme ≥ 5`, לרוב היפוך/רגל חדשה) · (ב) המשך-בקיצון (‏`at_extreme ∧ vol_trig ∧ stop≤1ATR` מ-winner_profile) — מספרים נפרדים, כי אלה שני סטאפים שונים.
5. פלט: `docs/reports/IDEAL_ENTRIES_2026-09-18.md` — טבלת-חתימות + walk-forward + 10 דוגמאות-golden עם הברים (לצ'ארט). טסטים: golden 17.09 17:10 (רגל +23.5 מהשפל, טריגר δ +3,319 ⇒ S1 ∧ S3) ו-17:45 (S2).
