# פספוס-עסקאות מול כניסה-מאוחרת — ביקורת לפני פתיחת 2026-08-17

**סוכן:** cowork-dev (מק-1) · **מצב:** קריאה-בלבד — לא שונה קוד/דגל/שירות/DB · **נכתב:** 06:30 IL
**שאלת-מייקל:** *"לוודא שאנחנו מוכנים למסחר ללא פספוס של עסקאות וכניסה מאוחרת מדי."*

---

## 0. TL;DR — הפסיקה שנדרשת היום

| # | ממצא | פעולה |
|---|------|-------|
| **A** | **פוזיציה ידנית חיה של 8 חוזים פתוחה עכשיו** ⇒ נותרו **$524.61** מרג'ין פנוי מול **$1,100.44** שהמערכת תבקש ב-4 חוזים. `MARGIN_AWARE_SIZING_V1=0` ⇒ **אין קיצוץ** ⇒ הירי הראשון צפוי להידחות ע"י הברוקר. | **לסגור/להקטין את ה-8 לפני 16:30** — אפס-קוד, אפס-דגל |
| **B** | כניסות-מאוחרות מפסידות באופן חד-משמעי: **33 איתותים, 3 מנצחים, −$2,338.75** מול **+$841.88** בכניסות-בזמן | לא לגעת היום — המגמה כבר יורדת לבד (35%→12%) |
| **C** | `ZONE_LIMIT_ENTRY_V1` הוא **מתג, לא מסנן**: 381 "חסימות" ביומן, מתוכן **1 אמיתית**. ביום שהוא היה דלוק הוא עצר **0 מתוך 16** הכניסות-המאוחרות | להשאיר כבוי (=מצבו היום). לתקן את ה-**מדידה**, לא את הדגל |
| **D** | `scripts/gate_profit_audit.py` **שבור** (הפרש-TZ 3 שעות + לא מסנן פיקסצ'רים) — **פסיקת 08-14 שכיבתה את ZONE_LIMIT נסמכה על מספר בלתי-אפשרי** | לתקן את הסקריפט; לתקן את הנימוק ב-`RULED_FLAGS.yaml:162` |
| **E** | יומן-ההחלטות **~85% רעש** (פיקסצ'רים + ריפליי-מת) ⇒ כל ספירת-חסימות שמופיעה במסמכים אינה אמינה | לסנן במקור |

**המלצה אחת למייקל להיום: לא לגעת בשום דגל. הפעולה היחידה = לטפל בפוזיציה הידנית של 8 החוזים לפני הפתיחה.**

---

## 1. מצב-המערכת ברגע-הכתיבה (raw)

```
$ ps -o lstart=,etime= -p 43765
Mon Aug 17 05:01:25 2026     01:11:27

$ curl -s http://localhost:8000/api/v9/health
{"status":"ok","version":"v9.0.0"}

$ python3 scripts/flag_guard.py | tail -3
  ✓ ZONE_LIMIT_ENTRY_V1: expected=0 actual=0
FLAG-GUARD: PASS — all 175 ruled flags match.
```

`~/SierraChart_Data/v9_export/sierra_state.json` (גיל 0.3 שנ'):

```
  is_sim = 0                       trade_account = 37138283
  position_qty = 8                 avg_price = 7807.25      open_pnl = +90.0
  working_orders = 1               orders: {"id":10237,"type":3,"bs":2,"price":7806.0,"qty":8}
  acct_available_funds = 524.61    acct_margin_req = 2200.88
  acct_account_value = 2725.49     acct_daily_pl = -270.0
  acct_under_margin = 0            acct_loss_limit_reached = 0
```

**הפוזיציה מוגנת** — הזמנת-סטופ אחת של 8 לוטים ב-7806.00 מכסה את כל 8 החוזים.
זו **לא** פוזיציה עירומה ולא אורפן. אבל היא **צורכת את כל המרג'ין**.

---

## 2. ממצא A — סיכון-הפספוס האמיתי של היום הוא מרג'ין, לא שער

`backend/v9/services/margin_sizing.py` נכתב בדיוק בשביל המצב הזה (מייקל 07-28, אחרי
6 דחיות *"Insufficient Account Value (NLV) for margin"*). החישוב מנתוני-החשבון החיים:

```
  open position       : 8 contracts @ 7807.25
  margin_req          : $2200.88  -> per contract $275.11
  available_funds     : $524.61
  usable (buffer 50)  : $474.61
  contracts affordable: 1
  system will request : 4  (FIXED_CONTRACTS_4=1)  = $1100.44
  SHORTFALL           : $625.83
  MARGIN_AWARE_SIZING_V1 = 0 -> no reduction applied
```

`cap_contracts()` יוצא מיד ב-`"MARGIN_AWARE_SIZING_V1 off"` (`margin_sizing.py:39,146`) ⇒
המערכת תשלח 4 חוזים שהחשבון לא נושא. **הזמנה שנדחית גרועה מהזמנה קטנה** — האיתות
נצרך, הסלוט מתחלף, `ORDER_FAILED` מזהם את הספרים, ואף חוזה לא מוגן. זהו **פספוס-עסקה
שאף שער לא אשם בו.**

**חשוב — זו אינה סתירה לפסיקת 08-13.** הפסיקה ההיא (`RULED_FLAGS.yaml:253`, *"קבעתי 4
חוזים — אני מבקש שזה יהיה 1:1"*) נכונה על **חשבון שטוח**: אז ה-$2,719 היו פנויים
וקבוע-המרג'ין $889 היה שמרני-כוזב. היום ערך-החשבון כמעט זהה ($2,725.49) אבל
**$2,200.88 ממנו כבר תפוסים** ע"י הפוזיציה הידנית. הפסיקה עצמה הורתה:
*"אם יחזרו דחיות-מרג'ין אמיתיות — לחזור למייקל עם הנתון, **לא להדליק לבד**."*
**זה הנתון. לא הודלק דבר.**

**הפתרון המומלץ אינו דגל:** לסגור או להקטין את 8 החוזים לפני 16:30 מחזיר
~$2,200 מרג'ין ומייתר את כל הדיון. אם מייקל רוצה להשאיר את הפוזיציה — אז ורק אז
`MARGIN_AWARE_SIZING_V1=1` הופך 4→1 חוזה במקום דחייה (מקטין-בלבד, לעולם לא מגדיל).

> `grep -ic "ORDER_FAILED\|NLV\|insufficient" /tmp/backend.err.log` → **0** —
> עדיין לא היו דחיות. זו תחזית מהאריתמטיקה, לא תקרית שקרתה.

---

## 3. ממצא E — יומן-ההחלטות מורעל, ולכן כל ספירה קודמת חשודה

מקור: `~/SierraChart_Data/v9_export/gateway_decisions.jsonl` + `decisions_archive/`
(7,351 שורות, 2026-07-22 → 2026-08-17).

```
raw: 7351 | clean candidates: 248
dropped: {'no_gate/entry': 2187, 'outside_RTH_utc': 2073, 'bar_gap': 1356,
          'FIXTURE_7600': 843, 'STALE_price': 593, 'dup_same_bar': 51}
```

שלושה מקורות-זיהום:

1. **פיקסצ'רים של טסטים — 1,104 שורות עם `entry=7600.00`.** מחיר עגול שחוזר
   בדיוק אותו דבר ב-07-22, 07-28 וב-08-16. `conftest.py:17 _isolate_gateway_decisions`
   נכתב כדי למנוע את זה ("510 phantom lines") — **אבל שורות 7600 עדיין נכתבו ב-08-16**,
   כלומר הבידוד דולף.
2. **ריפליי-מת אחרי ריסטארט — מחיר-כניסה קפוא.** לדוגמה:
   `2026-07-23T06:49 CONFLUENCE_RI_ZLR | signal age 566994s > max 180s (bar_ts=2026-07-16 17:20:00)`
   — איתות בן **6.5 ימים**. ו-`2026-07-24T10:36 | adverse drift 129.00pt (entry=7589.50 live=7460.50)`
   — 129 נקודות אינן "רדיפה", זה מחיר-כניסה מסשן אחר.
3. **שכפול** — אותו איתות נרשם מחדש כל שנייה (173 שורות `zone_limit` ביום אחד).

**מסקנה:** כל היסטוגרמה של `blocked_by` שנלקחה מהיומן הגולמי — כולל זו שמופיעה
בדוחות קודמים — מנפחת פי ~30. אחרי ניקוי נשארו **248 מועמדים אמיתיים ב-24 ימי-מסחר**.

---

## 4. ממצא D — `gate_profit_audit.py` שבור, והפסיקה שנסמכה עליו נסמכה על מספר בלתי-אפשרי

### באג 1 — הפרש-TZ של 3 שעות

`scripts/gate_profit_audit.py:77` עושה `b[0].replace(tzinfo=None)` על הברים ו-
`datetime.fromisoformat(ts).replace(tzinfo=None)` על ההחלטות. אבל:

```
$ psql postgresql://localhost/mems26 -c "SELECT ts,close FROM v9_bars_5min_woodies ORDER BY ts DESC LIMIT 2;"
           ts           |  close
------------------------+---------
 2026-08-17 06:15:00+03 | 7808.25
```

הברים חוזרים מ-PG **מודעי-TZ ב-+03 (שעון ישראל)**, ההחלטות ב-**+00 (UTC)**.
הפשטת `tzinfo` משני הצדדים משווה שעון-ישראל מול UTC ⇒ **הסקריפט מחפש את הבר
3 שעות אחרי ההחלטה**. זהו בדיוק מלכוד חוק-4 (איסור-עמימות-TZ).

### באג 2 — מסנן-הפיקסצ'רים לא תופס

`gate_profit_audit.py:107` מסנן `if entry < 7000: continue` — **7600 עובר את המסנן.**
843 שורות-טסט נכנסות לחישוב.

### התוצאה: הפסיקה מ-08-14

`config/RULED_FLAGS.yaml:162` מכבה את `ZONE_LIMIT_ENTRY_V1` ומנמק:

> *"על 12 ימי-ארכיון חסם 5 מנצחים מול 2 מפסידים … כולל **ZLR LONG 11.08 16:26 @7600
> שרץ +192.5 נק'**"*

בדיקה מול הנתונים (חוק 2):

```
$ psql -c "SELECT min(low),max(high),max(high)-min(low) FROM v9_bars_5min_woodies WHERE ts::date='2026-08-11';"
 day_low | day_high | range_pts
---------+----------+-----------
 7739.25 |     7796 |     56.75

$ psql -c "SELECT min(low),max(high) FROM v9_bars_5min_woodies WHERE ts >= '2026-08-01';"
 aug_low | aug_high
---------+----------
 7542.75 |   7838.5
```

- **הטווח של כל יום 08-11 היה 56.75 נק'.** ריצה של +192.5 נק' פיזית בלתי-אפשרית.
- ב-19:26 IL ב-08-11 המחיר היה ~7763. **כניסה ב-7600 רחוקה 163 נקודות מהשוק** —
  האיתות מעולם לא היה קיים. זו שורת-פיקסצ'ר (ובכלל `blocked_by=cold_start_guard`,
  לא `zone_limit`).

**⇒ הפסיקה נכונה בכיוונה אך שגויה בנימוקה.** הדגל באמת חסר-ערך (סעיף 5), אבל
הראיה שהובאה למייקל הייתה תוצר-לוואי של באג. **הכלי עדיין בריפו ויפיק את אותם
מספרים בפעם הבאה.** יש לתקן את `gate_profit_audit.py` ואת הנימוק ב-`RULED_FLAGS.yaml:162`.

---

## 5. כניסה-מאוחרת — התמונה הכמותית

### 5.1 המדד

לכל עסקה ב-`v9_trades` מ-07-21: `drift = מחיר-הכניסה − סגירת-הבר-הסגור-האחרון לפני
הכניסה` (בכיוון העסקה). `drift > +2pt` = "רדפנו". `ext` = מרחק משיא/שפל-הסשן.
שכפולי shadow/live אוחדו (146 איתותים ייחודיים מתוך 200 רשומות).

### 5.2 התוצאה — על P&L אמיתי, לא על MFE

```
  LATE  (drift > +2.0pt)    n= 33 scored= 29 wins=  3 sum=$-2338.75 avg=$ -80.65
  BORDER(+0.25..+2.0pt)     n= 24 scored= 24 wins= 10 sum=$  -70.00 avg=$  -2.92
  ON-TIME (drift <= 0)      n= 89 scored= 83 wins= 49 sum=$  841.88 avg=$  10.14
```

לפי גודל-הרדיפה — מונוטוני, בלי יוצא-דופן:

```
  drift 2-5pt    n=12 wins= 1 sum=$ -567.50
  drift 5-10pt   n= 6 wins= 1 sum=$ -311.25
  drift 10-20pt  n= 9 wins= 1 sum=$ -873.75
  drift >20pt    n= 6 wins= 0 sum=$ -586.25
```

לפי מרחק-משיא-הסשן:

```
  AT/BEYOND (<=0)   n= 3 sum=$ -288.75   <- כניסה בשיא עצמו
  0-3pt             n=24 sum=$-1096.25
  3-6pt             n=36 sum=$ -244.37
  6-12pt            n=29 sum=$+1012.50   <- אזור-הרווח היחיד
  >12pt             n=53 sum=$ -950.00
```

**מסקנה:** 3 מנצחים מתוך 29 עסקאות-רדיפה. זה לא רעש. **כניסה מאוחרת מפסידה שיטתית.**
"אזור-המתיקות" הוא 6–12 נק' מהקיצון — לא צמוד לקיצון ולא באמצע-הטווח.

### 5.3 אבל — הבעיה כבר מצטמצמת מעצמה

```
=== late-share per week ===
   2026-W30: total= 48  late= 17  (35%)
   2026-W31: total= 29  late=  7  (24%)
   2026-W32: total= 45  late=  6  (13%)
   2026-W33: total= 24  late=  3  (12%)

=== late $ by day ===
   07-21..07-31 : 24 signals, -$1,950.00, max drift 31.50pt
   08-05..08-14 :  9 signals,   -$389.00, max drift  9.00pt
```

הרדיפות-הענק (20–31 נק') **נעלמו לחלוטין אחרי 07-31**. השארית ב-08-14 היא
`max drift = 5.25pt` — בדיוק סדר-הגודל שתיקון **T6** (הבר-המתהווה בגלאי-המדרגה,
"כניסה 5.25 נק' נמוך מדי") נועד לסגור, והוא נכנס ב-08-16, כלומר **טרם נמדד בשוק**.

---

## 6. ממצא C — `ZONE_LIMIT_ENTRY_V1` הוא מתג, לא מסנן

381 חסימות ביומן. סיווג מלא:

```
  A_test_fixture_7600        134   (entry=7600.00)
  B_stale_age_gt_1h           47   (איתות בן שעות/ימים אחרי ריסטארט)
  E_stale_drift_gt20pt       199   (drift 20-129pt = מחיר-כניסה מסשן אחר)
  F_REAL_drift_block_RTH       1   <-- היחיד
```

החסימה האמיתית היחידה ב-4 שבועות:
`2026-07-31T13:30:05Z | adverse drift 2.88pt > max 2.00pt (entry=7498.50 live=7501.38)`
— בשנייה החמישית של הפתיחה.

**וההוכחה המכריעה:** הדגל היה **דלוק** ב-07-22/23/24/28/31. באותם ימים בדיוק:

```
On those SAME days: 41 unique signals were TAKEN.
  of which LATE (drift > +2pt): 16  -> P&L $-1727.50  (wins 1/16)
  i.e. the gate logged 381 'blocks' those days and stopped NONE of them.

    #479 07-23 15:25Z live   SHORT INITIATIVE_SHORT   drift=+31.50pt pnl=-161.25
    #460 07-22 14:55Z live   SHORT ZLR                drift=+25.00pt pnl= -67.50
    #494 07-23 19:05Z shadow SHORT ZLR                drift=+22.75pt pnl= -63.75
    #576 07-31 13:35Z shadow LONG  OPENING_PULLBACK   drift=+20.25pt pnl=-216.25
    #575 07-31 13:35Z live   LONG  OPENING_DRIVE      drift=+19.25pt pnl=-198.75
    ... (16 סה"כ)
```

**הדגל היה דלוק, רשם 381 "חסימות", ועצר אפס מתוך 16 הכניסות-המאוחרות שעלו −$1,727.50.**

**השורש:** הגייט (`trading_gateway.py:2749-2754`) משווה `setup["entry_price"]` מול
**המחיר החי באותו רגע**. כשהכניסה היא market-at-current, שני הערכים זהים ⇒ `drift≈0`
⇒ הגייט לא יורה לעולם. הרדיפה האמיתית מתרחשת **מוקדם יותר** — בין סגירת בר-האיתות
לבין בניית ה-setup — ושם הגייט לא מסתכל. הדלקה מחדש של הדגל **לא תשנה דבר**.

---

## 7. שערים — מי מרוויח את החסימות ומי חוסם-יתר

הרצה נקייה (TZ מתוקן, בלי פיקסצ'רים, בלי מחירים-מתים, RTH-בלבד, מנוכה-כפילויות),
248 מועמדים · חלון 07-22..08-17:

```
gate                          n days  KILL  SAVE  NEU   net_pts  net_$@4c
cont_trend_filter            32    9    18     9    5    +320.8     +6416
daytype_playbook             46   14    22    21    3    +261.2     +5224
awaiting_release             47   12    18    19   10    +241.8     +4837
extreme_chase_guard          19    5    12     7    0    +193.1     +3863
eod_entry_cutoff             26   11    13    12    1    +168.9     +3377
rr_entry_gate                11    5     6     3    2    +104.8     +2095
lsma_flat                    23   11     9    14    0     +65.2     +1305
location_gate                20    9     5    12    3      +8.4      +167
rr_hard_floor                 4    1     1     2    1      -1.9       -38
duplicate_fire                1    1     0     1    0      -5.2      -105
```

### ⚠️ אזהרה מתודולוגית — הדולרים האלה הם **חסם-עליון בלבד**

השיטה (זו של `gate_profit_audit.py` גם) **א-סימטרית**: ל-KILLER היא זוקפת את
ה-**MFE המלא** (התנועה הטובה ביותר ב-60 דק'), ול-SAVER רק את גודל-הסטופ. אבל
המערכת לעולם לא מממשת MFE — היומן של 08-14 מתעד עסקה שמימשה **$11.25 מתוך $65.00
(החזרה 83%)**. ⇒ **כל שער נראה "חוסם-יתר" בשיטה הזו.** הדירוג היחסי אינפורמטיבי;
המספרים המוחלטים לא.

### מה בכל זאת אפשר לומר

- **הראיה דקה בכל שער.** אין שער עם n≥50; `extreme_chase_guard` = 19 מועמדים
  על 5 ימים, `rr_entry_gate` = 11 על 5 ימים. **אף אחד מהמספרים האלה לא מצדיק
  שינוי-דגל לבדו.**
- **סתירה גלויה שיש להשאיר פתוחה:** כאן `cont_trend_filter` יוצא החוסם-יתר #1
  (18 מנצחים מול 9 מפסידים), בעוד `EOD_2026-08-14` קובע שהוא **חסך $1,022.50 על
  17 סשנים** ומורה "לא להרפות אותו". ההפרש מוסבר במלואו ע"י א-סימטריית-ה-MFE:
  בסיס-17-הסשנים מודד יציאות אמיתיות, אני מודד MFE. **בסיס-17-הסשנים אמין יותר.**
- **התרכזות ביום אחד:** רוב ה-KILLERS של `cont_trend_filter`/`daytype_playbook`/
  `awaiting_release` נופלים על **07-29** — היום שבו שער-השחרור היה שבור
  (60 החלטות/0 עסקאות, תוקן ב-`029581d7`). זו תקרית שכבר נסגרה, לא דפוס חי.
- **`extreme_chase_guard` הוא היוצא-דופן המעניין** (12 KILLER מול 7 SAVER):
  זהו שער **הרדיפה** — ובזמן שהוא חוסם רדיפות, הנתונים בסעיף 5.2 מראים שדווקא
  אזור 6–12 נק' מהקיצון הוא היחיד הרווחי, וה-`EXTREME_MIN_DIST_PTS=6.0` יושב
  בדיוק על הגבול התחתון שלו. **מועמד לכיול — לא היום, ולא על n=19.**

---

## 8. פספוס-עסקאות — האפיזודות ההיסטוריות

| תאריך | מה קרה | מקור | מצב היום |
|-------|--------|------|----------|
| 06-30 | 0 עסקאות ביום-מגמה **+44.75pt**; שער-המשפחה חסם CONT+REV דו-כיוונית. CF: 3 setups LONG שזוהו-ונחסמו = **+3R כ"א** ⇒ over-block ≈ **+3..+9R/יום** | `STATUS_BOARD.md:376` | שער-המשפחה שוחרר |
| 07-01 | שער-משפחה כבוי → 2 counter-fade LONGs; `ready_to_route` חסם +3R | STATUS_BOARD | נסגר |
| 07-29 | 60 החלטות / 0 עסקאות, 29 מנצחות נחסמו → שער-שחרור תוקן (`trend_bypass` + מסלול-V, `029581d7`) | memory + זיהוי-שיא ב-§7 | **תוקן** — עדיין מזהם את ה-CF |
| 08-13 | `OPENING_TYPE_GATE` חסם 2/2 מנצחות → מייקל כיבה | `.env:113 OPENING_TYPE_GATE=0` ✓ | כבוי, מאומת |
| 08-14 | החמצת-הפתיחה: השעה הראשונה משוחררת = **+$286.25**; `OPENING_FIRST_TRADE_STRICT` עלה **$227.50** | `LIVE_CHANNEL` EOD 08-14 §3 | `OPENING_FIRST_TRADE_STRICT_V1=1` — **עדיין דלוק** |
| 08-16 | `OPENING_DIR_FUSION` עבר 0/8 אי-פעם — באג יחידות-נפח (T7) | LIVE_CHANNEL | **תוקן 08-16, טרם נמדד בשוק** |

**שלושה תיקוני-פתיחה (T6/T7 + פיוז'ן) נכנסו ב-08-16 ומעולם לא רצו בשוק חי.**
היום הוא מבחן-האמת הראשון שלהם. זו סיבה נוספת **לא** להוסיף משתנה נוסף היום.

---

## 9. הפסיקה — האם היום נפספס עסקה או נרדוף אחת?

**התשובה: נפספס — אבל לא בגלל שער.**

- **רדיפה:** הסיכון נמוך היום. `EXTREME_CHASE_GUARD_V1=1` דלוק ופעיל
  (62 חסימות אמיתיות, מחירים אמיתיים, כולן ב-RTH). `TREND_LEG_CHASE_EXEMPT_V1=1`
  ו-`CHASE_MIN_SESSION_BARS=8` מרפים אותו רק היכן שנפסק. הרדיפות-הענק נעלמו מ-08-01,
  ותיקון T6 (08-16) מכוון בדיוק לשארית של 5 נק'.
- **פספוס:** הסיכון גבוה — **ממצא A**. 4 חוזים מול $474.61 שמישים = דחיית-ברוקר
  על הירי הראשון, שהוא לרוב הטוב ביותר ביום (`OPENING_FIRST_TRADE_STRICT` הוכח
  ב-08-14 כעולה $227.50 לבדו).

### הפעולה היחידה המומלצת

> **לסגור או להקטין את הפוזיציה הידנית של 8 החוזים לפני 16:30 IL.**

זו הפעולה עם יחס התועלת/סיכון הטוב ביותר: היא **אינה שינוי-תצורה** (אפס דגלים,
אפס קוד, אפס ריסטארט), היא מחזירה ~$2,200 מרג'ין ומחזירה את גודל-העסקה ל-4 החוזים
שנפסקו, והיא הפיכה לחלוטין.

### מה **לא** לעשות היום

- ❌ **לא להדליק `ZONE_LIMIT_ENTRY_V1`.** הוכח בסעיף 6 שהוא לא עוצר כניסות-מאוחרות.
- ❌ **לא לכייל `EXTREME_MIN_DIST_PTS`.** n=19 על 5 ימים, ובשיטה מוטה.
- ❌ **לא לגעת ב-`cont_trend_filter`** למרות שהוא ראשון בטבלה — בסיס-17-הסשנים סותר.
- ❌ **לא להדליק `MARGIN_AWARE_SIZING_V1` בלי מייקל** — פסיקת 08-13 הורתה במפורש
  "לחזור למייקל עם הנתון, לא להדליק לבד".
- ❌ **בכלל — לא לשנות תצורה שעות לפני סשן חי.** ב-08-13 שינוי כזה
  (`cold_start_guard`) עלה 12 מועמדים. שלושה תיקונים חדשים (T6/T7/פיוז'ן) נמדדים
  היום בפעם הראשונה; משתנה נוסף יהרוס את הייחוס.

---

## 10. פתוח — לתיקון אחרי הסגירה (לא היום)

| # | פריט | קובץ |
|---|------|------|
| M1 | `gate_profit_audit.py` — תיקון TZ (הכל UTC-מודע) + סינון פיקסצ'ר `entry==7600` + ניכוי-כפילויות | `scripts/gate_profit_audit.py:77,107` |
| M2 | תיקון הנימוק ב-`RULED_FLAGS.yaml:162` — המספר "+192.5 נק'" בלתי-אפשרי (טווח 08-11 = 56.75) | `config/RULED_FLAGS.yaml:162` |
| M3 | דליפת-פיקסצ'רים ל-`gateway_decisions.jsonl` — 843 שורות 7600, כולל ב-08-16 למרות `conftest.py:17` | `backend/v9/tests/conftest.py` |
| M4 | ריפליי-מת אחרי ריסטארט — setups עם `bar_ts` בן ימים עדיין מנותבים לגייטווי | `trading_gateway.py` |
| M5 | `zone_limit` v2 — למדוד drift מול **סגירת בר-האיתות**, לא מול המחיר-החי (הסיבה שהגייט עיוור) | `trading_gateway.py:2749` |
| M6 | `EXTREME_MIN_DIST_PTS` — הנתונים מרמזים שאזור 6–12 נק' הוא הרווחי; הסף יושב על 6.0. לכייל אחרי n≥40 | `.env:466` |

---

## נספח — פקודות-שחזור

```bash
# היסטוגרמת חסימות גולמית (מזוהמת)
python3 /tmp/mm_hist2.py

# סיווג חסימות zone_limit (פיקסצ'ר/מת/אמיתי)
python3 /tmp/mm_zl2.py

# ביקורת-שערים נקייה (TZ מתוקן)
python3 /tmp/mm_clean2.py 3.0

# מדידת כניסה-מאוחרת על עסקאות אמיתיות
python3 /tmp/mm_late2.py

# ההוכחה: הגייט היה דלוק ועצר 0/16
python3 /tmp/mm_proof.py
```

*כל הסקריפטים קריאה-בלבד (SELECT בלבד + קריאת JSONL). לא נכתב דבר ל-DB,
ל-`~/SierraChart_Data`, ל-`.env` או לשירותים.*
