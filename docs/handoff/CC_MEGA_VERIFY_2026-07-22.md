# CC — מגה-אימות: המערכת פועלת בדיוק כפי שהיא צריכה (מייקל 2026-07-22 ערב)

**מייקל:** *"כל מה שעשינו לא נעשה כמו שצריך. אני רוצה בדיקה שהמערכת פועלת בדיוק לפי איך שהיא צריכה
להיות — כולל הצינורות."* **המערכת בסים** (MEMS26_MODE=sim ✓, Sierra is_sim=1 ✓) — מותר ורצוי לירות/לתקן.
**מבצע: cc-macbook (המודל-החזק). cursor מאמת פר-תחנה. cowork מאמת-סימטרית ומרכז.**

## פרוטוקול (חובה)
תחנה-אחת-בכל-פעם, לפי הסדר למטה · לכל בדיקה **פקודה+פלט-גולמי** (חוק-5) · כל תיקון: שורש→קוד→טסט-אנטי-
טאוטולוגי→אימות-סים · שינוי-התנהגות = דגל (ברירת-מחדל לפי נוהל-הקבע: מימוש-פסיקה-קיימת מודלק אחרי אימות)
· **סעיף NOT-DONE חובה בסוף כל תחנה** · שורת-LOG חתומה ב-LIVE_CHANNEL פר-תחנה · אל תמציא — שאל.

הראיות-החיות של היום (22/07) הן ה-fixtures. כל תחנה כוללת את התקלה-שנצפתה + קריטריון-קבלה.

---

## P1 — ייצוא-DLL (המקור)
**נצפה:** אתמול הייצוא קפא 21:20 (חלון-50-ברים); היום תקין. **בדוק:** כל קבצי-הייצוא (woodies/5min/state/
tpo/cvd) מתעדכנים בקצב-הנכון בסים; `export_ts` מתקדם; אין קיפאון תחת עומס. **קבלה:** freshness<בר אחד לכל
קובץ לאורך שעה + התראת-קיפאון (promoter watch) פועלת.

## P2 — Bridge→Ingest + TS-gate
**נצפה:** TS-gate (חדש) דוחה-כנה; אתמול +1h זיהם. **בדוק:** אפס-דחיות-שווא היום בסים (grep TS_OFFSET);
הזרקת-batch-מעופש מלאכותית → נדחית+נרשמת; אפס כפילויות-+1h חדשות ב-DB. **קבלה:** 0 false-rejects · הזרקה
נדחית · `select count(*) from dups-join` = 0 להיום.

## P3 — שלמות-דגלים ייצוא→DB 🔴
**נצפה חי:** `woodies_5min.json` עם `zlr_detected=True` בעוד עמודת-ה-DB `zlr_detected=0` על אותם ברים (כל
הבוקר!). **מצא את נקודת-הנפילה** (נתיב-הכתיבה bars.py:1087± / current_bar override) ותקן: הדגל-הסופי של
בר-סגור חייב להישמר ב-DB. **קבלה:** טסט השוואת-ייצוא-מול-DB על יום-מלא = 100% התאמה בדגלי zlr/hfe/trend.

## P4 — זיהוי-S4: מרוץ-הדגל 🔴
**נצפה חי (18:53):** `wb.zlr=True is_new_bar=False — detection SKIPPED (non-new-bar push)` שוב-ושוב —
הדגל מתיישב על ה-bar אחרי ה-push-הראשון, אבל הזיהוי רץ רק על push-ראשון (dedup) → ZLR מוחמץ. לעתים ה-push
הראשון כן נושא (18:00, 18:50 — "WILL run") = מרוץ. **תקן:** edge-trigger — כשדגל עולה False→True על אותו
ts שטרם-הוערך → הרץ זיהוי פעם-אחת (עם שמירת-dedup-ירי הקיימת בשער). **קבלה:** טסט-סים: bar עם דגל-מאוחר →
זיהוי רץ בדיוק פעם-אחת; אפס-כפילויות-ירי (dedup_fire_guard נשאר).

## P5 — זיהוי-S2: מחקר-כיול (לא שינוי-חם)
**נצפה:** `b1_exp=0` כל-היום — ספי-התפרצות 4.8-12.8pt מול ברים 1.5-4pt; REACTIVE מקסימום 3/5 תנאים.
**בצע מחקר:** על 31 הסשנים — כמה מועמדי-INITIATIVE בטווח 10%/20% מהסף, ומה היה קורה (win-rate סימולציה כמו
מחקר-הפתיחה). **תוצר: דוח-מספרים לפסיקת-מייקל** (לא לשנות ספים בלי פסיקה).

## P6 — סוג-יום: שלוש-התקיעות של היום 🔴🔴 (העדיפות-העליונה)
**נצפה חי:** (א) בוקר — תקוע-Trend (אסקלציה-בלבד, אין demotion); (ב) אחה"צ — override-Normal קפא מול פריצה;
(ג) ערב — boot-replay החזיר Normal-0.12 בעוד הקנוני אומר Variation, והקידום-שאבד-בריסטארט לא חזר (אירוע-קצה
שעבר). **בנה לפי `CC_ACCEPTANCE_DEMOTION_2026-07-22.md` + הרחבה:**
1. `DAYTYPE_BOOT_SEED_CANONICAL_V1` — ה-boot זורע את התווית-החיה מ**חישוב-היום-הקנוני** (מסקנה, לא שחזור-אירועים).
2. `DAYTYPE_ACCEPTANCE_DEMOTION_V1` — ירידה על acceptance-חזרה (K=3 ברים ב-IB) לפי פסיקת-D2.
3. אסקלציה חייבת לשרוד restart (נגזרת של 1 — הקנוני רואה את הפריצה-שכבר-קרתה).
**Fixtures חובה:** שחזור שלוש-התקיעות של היום מברי-היום האמיתיים — כל אחת חייבת להיפתר. **קבלה:** restart
בכל שעה ביום-סים → התווית-החיה == הקנונית תוך דקה; escalation+demotion דו-כיווני פועל.

## P7 — שערים
**מצב:** 19 טסטי-route ירוקים (T1-guards, no-stomp). **בדוק:** כל blocked_by עם reason מדויק (ה-6 החסרים
מ-07-20 הושלמו?) · rr עם T1-מבני/1R-exhausted (תוקן 18:55) · location-v2 probe · REV-edges · LSMA-flat.
**קבלה:** מטריצת-סים (sim_matrix) מלאה PASS + decisions מציג reason לכל חסימה.

## P8 — ביצוע (סים — עכשיו אפשר!) 🔴
**נצפה חי (18:53:24):** ‎#462 ירה 4 חוזים → **Sierra r=-1 → CANCELLED**. חשד-מרכזי: מגבלת-פוזיציה (מייקל
החזיק +8 ידני; 8+4=12). **בדוק בסים:** (א) ירי-נקי → 4 חוזים = 8 הוראות-OCO-פר-חוזה, סטופ=מבני, T0/T1
נכונים; (ב) שחזר את-r=-1: פוזיציה-ידנית-בסים 8 → ירי-מערכת → האם נדחה? מצא את המגבלה (Sierra Trade
settings) והכרע עם מייקל: העלאה / הקטנת-גודל-דינמית / תור; (ג) **כנות-תצוגה: decision שנכשל-ביצוע חייב
outcome=`order_failed`** (היום הופיע "live" בפאנל בעוד העסקה CANCELLED — מטעה!) — תקן בפיד+פאנל; (ד) A1.6:
אימות-סים ל-`PLACE_STOP` (ההגנה-לאורפן שממתינה מ-07-18) + `ORPHAN_AUTO_STOP_V1` מחזור-מלא בסים: פוזיציה-
יתומה → סטופ-אוטו מוצב. **קבלה:** דריל-מלא ירוק + שחזור-r=-1 מוסבר + תצוגה-כנה.

## P9 — רשומות ורקונסיליאציה (Task#6) 🔴
**נצפה:** `trade_fills.json`=0B תמידית; P&L מחושב≠Sierra (‎+37.50 מול 0 הבוקר); יומן-הפעילות החמיץ את חלון
17:50-18:10 (פער-לכידה גם אחרי תיקון-הסים). **בנה:** מילוי `trade_fills`/live_ledger מ-`trade_activity_events`
(שם ה-P&L האמיתי) + סגירת פערי-הלכידה (רגקסים לייב+סים לכל הפורמטים שנצפו היום) + `pnl_usd` ברשומה מקבל
עמודת-אח `pnl_sierra` (לא דורס — הצלבה). **קבלה:** יום-סים עם 3 עסקאות → fills מלאים, P&L-Sierra=P&L-רשום,
אפס-חורים ביומן.

## P10 — אמת-בפרונט
**בדוק:** תווית אחת בכל מקום (TopBar=panel=gates; "—" כשאין) · decisions עם outcome-כן (order_failed!) ·
פאנל-הפתיחה (טריגרים+stance) · FRONTEND_INDEX מעודכן באותו-קומיט לכל שינוי. **קבלה:** צילומי-לפני/אחרי +
אפס-סתירות בין רכיבים.

---

## ⚡ עדכון 19:30 — ביקורת-הסוכן הושלמה (10 תחנות): סדר-העדיפויות תוקן לפי הראיות
דוח-הסוכן המלא בשיחת-cowork; תמצית-פסקים: P1 PASS · **P2 FAIL (חמור: 11 זוגות-+1h חדשים היום בשתי הטבלאות
— הזיהום פעיל עכשיו: hour-fix ±120s קצר מ-300s-בר → כל בר נכתב נכון ב-2 הדק' הראשונות ואז נכתב-שוב ב-−1h;
ה-TS-gate עיוור ל-batch-לא-מתקדם, bars.py:515)** · **P3 FAIL (הדגלים כן נכתבים — אבל לסלוט-השגוי: 4/7 zlr
בסלוטי-−1h-רפאים)** · P4 PASS-בעיקרו (**edge-trigger כבר בנוי — Mechanism-C woodies_system.py:439-447, עבד
היום; לאמת בטסט, לא לבנות**) · P6 SUSPECT (מתכנס-כרגע; antiflap×restart מגיש-מעופש; cross_check משווה
מחרוזות Normal_Variation≠Variation=באג; replay קורא את הטבלה-המזוהמת) · P7 PASS (60/60) · **P9 FAIL (fills=0B;
פידר-הפעילות בכלל לא עוקב אחרי Sim1 — offset מ-07-21; פערי-33+95 דק' בלעו את עסקת-17:55)** · **P10 FAIL
(אין outcome=order_failed בכלל בקוד — ‎#462 מוצג "live"; OPS_LOG כתב 'BE' על עסקה-שלא-בוצעה; decisions
נמחק-בריסטארט)** · **וגם: r=-1 קרה פעמיים (‎#464 ב-18:58 — לא רק ‎#462!)**.

## סדר-עבודה מתוקן (לפי הראיות): P2+טיהור → P8 (שחזור-r=-1 — הספסל נקי) → P9 (Sim1-tail+fills) → P10 (order_failed+persistence) → P3 (סלוט-נכון) → P6 (boot-seed+antiflap+cross_check) → P4 (טסט-ל-Mechanism-C) → P7 → P1 → P5
לכל תחנה: אמת → תקן → אמת-סים → LOG → הבא. אם תחנה חושפת-חדש — רשום ב-GAP_REGISTER, אל תסטה.

## חזרה-ללייב (מייקל: "בלי זמן-המתנה")
**כל-התחנות-ירוק = חוזרים ללייב מיד** — אין תקופת-צינון/soak. תהליך-החזרה (מוכן-מראש, מבוצע ברגע-הירוק):
snapshot → `MEMS26_MODE=live` → restart → flag_guard PASS → אימות boot (תווית-קנונית נזרעה ✓, streams ✓,
brackets-דריל אחרון בסים לפני-החלפה) → מייקל מעביר Sierra ל-is_sim=0 → חימוש. **הפסיקה כבר נתונה** —
הירוק-המלא הוא התנאי-היחיד; אין אישור-שני.

## תוצר-חובה ל-cowork: דוח-תקלות-ותיקונים מרכזי
בסוף (או תוך-כדי, תחנה-אחר-תחנה): **`docs/reports/FAULTS_AND_FIXES_2026-07-22.md`** — טבלה אחת מרכזת:
| # | תקלה (כפי-שנצפתה-חי, עם שעה) | שורש | תיקון (קומיט+דגל) | הוכחת-סים (פקודה+פלט) | סטטוס |
כולל את כל תקלות-היומיים: תקיעות-התווית ×3 · צינור-getter-מת · zlr-ייצוא≠DB · מרוץ-דגל-S4 · r=-1/#462 ·
outcome-מטעה · fills-ריקים/פערי-יומן · הזנב-המזוהם · קיפאון-ייצוא · S2-ספים · breakout-blind-spot · +כל
מה שתמצא. **cowork מאמת כל שורה סימטרית (חוק-5) לפני שהיא נחשבת סגורה** — הדוח הזה הוא בסיס-ה-GO.
