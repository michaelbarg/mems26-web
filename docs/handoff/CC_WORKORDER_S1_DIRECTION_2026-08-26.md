# פקודת-עבודה · לחבר את כיוון מערכת 1 לגייטוויי

**מאת:** cowork-dev · **אל:** cc-macbook · **תאריך:** 2026-08-25 (לביצוע 26.08 בבוקר)
**פסיקת-מייקל 25.08:** *"הכיוון שלנו צריך להיקבע ע"י מערכת 1 ועל ידי ה-IB לפי דלתון"* → **"כן"**

## הממצא בשורה אחת

**מערכת 1 כבר מחשבת את הכיוון. הגייטוויי לא שואל אותה.**

```
v9_day_type_state (הסשן האחרון):
  day_type   = Variation
  direction  = with_extension(DOWN)        ← הכיוון, מהרחבת-IB חד-צדדית
  reason     = 1-sided extension = Expanded Typical (rib 1.0631)
  sides      = 1
  rib        = 1.0631
```

```
מי קורא את העמודה direction:
  backend/v9/systems/balance_imbalance_toggle.py:138   ← היחיד
  backend/v9/gateway/trading_gateway.py:1423           ← קורא confidence בלבד
```

והגייטוויי לוקח `day_direction` מ-`trade_context.py:850,:874-877` —
`get_live_dir_bias(window=6, min_frac=0.6)` = **רוב-60% על 6 ברי-ווּדיס = 30 דקות**.

**זה כל הפער.** לא צריך לבנות מכריע — צריך לחבר את מה שכבר קיים.

## הראיה הכספית

```
עם-המגמה, בימים שזזים ≥20 נק':   +$8,040   על n=254
כל השאר:                          -$14,464   על n=307
```
והתא `Trend_Normal`×SHORT לבדו: **n=52, −7.01 נק'/חוזה** — עסקאות נגד-כיוון-היום.

## מה לבנות — `S1_DAY_DIRECTION_V1`

`day_direction` נגזר **מפלט מערכת 1**, בסדר הזה:

1. `v9_day_type_state.direction` — לפרסר את `with_extension(UP|DOWN)` ל-`UP`/`DOWN`.
2. אם אין — `accepted_break` מ-`_resolve_live_cls()` (‏`trading_gateway.py:195`, כבר קיים ועובד).
3. אם אין — `value_migration`.
4. אם אין אף אחד — **`UNDETERMINED`**.

### דלתון בינארי — פסיקת-מייקל
הפלט הוא `UP` / `DOWN` / `UNDETERMINED`. **בלי אחוז-ביטחון בשום שלב.**
`UNDETERMINED` פירושו **"אין דרישת-כיוון"**, לא "נחש". *"או שיום הוא אחד מהימים או שלא."*

### תיחום — חובה
לחול על `Trend_*` בלבד, כמו `NEVERFADE_TREND_ONLY_V1=1` היום.
**לא להרחיב ל-`Variation`/`Normal`** — הראיה הנגדית: "בתוך-IB" בעידן-A היה
**+3.93 נק'/חוזה על n=53**, וזו תגובה רווחית בימים דו-צדדיים.

### כללים
- דגל **נפרד** · flag-OFF **זהה-בייט** · כשל-בטוח (חריגה ⇒ השרשרת הישנה) ·
  תג-לוג ייחודי `[S1DayDir]` בכל פעם ששינה פסק.
- **`RULED_FLAGS.yaml` באותו commit.**
- **טסט שנכשל אם המקור מת** — זו הייתה התקלה של 25.08.
- **§D פר-דגל:** דלתא **וגם** חציון-יום. שני מבחני-סיכון: `Trend_Normal`×SHORT
  חייב להשתפר · "בתוך-IB" לא נפגע.

## אסור
`.env` · ריסטארט · לגעת בגודל/סולם/סטופים/יעדים · `op=EXIT` · אחוזי-ביטחון בשרשרת.

## מסירה
`docs/reports/CC_S1_DIRECTION_2026-08-26.md` — פקודה+פלט גולמי · file:line · §D · NOT-VERIFIED.
LOG ב-`LIVE_CHANNEL`. `commit`+`push`. **ואז עצור** — הדלקה = שער cowork+מייקל.

---

## תור-העבודה אחרי זה

| # | פריט | למה |
|---|---|---|
| 2 | `RESOLVED` + MFE/MAE (`CC_WORKORDER_RESOLVED_2026-08-25.md`) | בלעדיו הליגר אוסף מועמדים בלי תוצאות; שער-300 של Task 6 לא זז |
| 3 | לסגור את `TS-OFFSET-GATE` הדולף (`bars.py:556,596-599`) | הדחיפה הראשונה אחרי כל ריסטארט עוברת בלי בדיקה — זה מה שייצר +60 דק' ב-12 סשנים |
| 4 | להחזיר 9 סשנים (כשלי-CVD בלבד) → 18/34 | ‏SCID מכסה 78 ברים בכל 34 התאריכים; אפס אבודים |
| 5 | ‏Stage 0C — הקרנל מייצר מועמדים | היום הרפליי מודד mirrors, לא את הקוד החי |
