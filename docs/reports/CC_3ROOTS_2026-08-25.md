# 3ROOTS — Build Report · 2026-08-25

## Status

| # | Item | Status | file:line |
|---|------|--------|-----------|
| **4** | flag_guard liveness | ✅ DONE | `scripts/flag_guard.py:86-128` |
| **1** | Root 1: app.state | ✅ BUILT (OFF) | `main.py:820-833`, `trading_gateway.py:195-208` |
| **3** | Root 3: stop diagnosis | ✅ EVIDENCE | 75 overrides, 89% tighter |
| **2** | Root 2: pattern_family | ✅ BUILT (OFF) | `daytype_position_gate.py:42-55` |
| flag_guard | 206 | ✅ PASS | + liveness clean |

## Item 4 — flag_guard second tooth

New `_second_tooth()` function reports ON flags with zero production code read-sites.
Informational — does not block GO.

```
$ python3 scripts/flag_guard.py | tail -2
FLAG-GUARD: PASS — all 206 ruled flags match.
  ── LIVENESS REPORT: all ON flags have ≥1 production read-site ──
```

## Root 1 — APP_STATE_ROOT_FIX_V1

`opening_type_result` was NEVER written to `app.state` → `market_context.py:112` always
got None → `balance=UNKNOWN` 673/673 times.

Fix: `main.py:820-833` — writes `app.state.opening_type_result` on every bar (opening_type
+ direction + confidence). Flag-gated OFF.

`_resolve_live_cls()` (`trading_gateway.py:195-208`) — already fixed in previous commit
(NO-GO fix). Reads `last_cls_result` from `backend.main.app.state` via lazy import.

## Root 3 — Stop writer diagnosis (evidence only)

```
StepLadder OVERRODE StopResolver: 75 times
  Set TIGHTER stop: 67 (89%)
  Set WIDER stop:    8 (11%)
```

**Decision for Michael:** StepLadder is generally more aggressive (closer stop). Which
should govern? The P&L split needs a replay. NOT-CHANGED — evidence only.

## Root 2 — PATTERN_FAMILY_DELTA_DBL_V1

`S2_DELTA_DBL_LONG/SHORT` added to `_REV_PATTERNS` in `daytype_position_gate.py:42-55`.
Flag-gated OFF. Without this, the pattern is invisible to position gate + playbook
(105/108 gateway blocks were on this unmapped pattern).

**This is a trading risk change** — it OPENS paths that were closed. §D required.

## NOT-VERIFIED

- §D for Root 1 and Root 2 (required before enabling)
- Root 3 P&L split (requires replay)
- Tests for Root 1 (mutation test: reverting to v9.app import should fail)
- Live verification (requires restart, which is cowork+Michael gate)

## Commit

`1f2ac909` — pushed. All flags OFF. **Enable = cowork+Michael after §D.**
