# פריט 8 מתוך 8 — T-365: בפתיחה יורים לפי דוקטרינת-הפתיחה, לא לפי סוג-היום

**פסיקת-מייקל 14.09 17:07, מילה במילה:**
> *"בהרצת פתיחה המערכת יורה ללא קשר לסוג היום אלא לפי דוקטרינת הפתיחה של דלתון,
> שעוזרת לזהות גם את סוג היום."*

**זו פסיקה שמתקנת סדר-לוגי הפוך:** סוג-היום **נגזר** מהתפתחות הפתיחה. לדרוש תווית-יום
לפני שמותר לסחור את הפתיחה = לדרוש את הפלט לפני הקלט.

## מה זה עלה לנו היום — עסקה אמיתית, עם מספר
```
16:45:05  [FiveMin] OPENING_ENTRY ORR LONG entry=7678.25 stop=7666.50 t1=7695.88 (live-eligible)
16:45:05  [CeilingFlipTouch2] FLOOR_TOUCH2_REJECT → LONG entry=7677.50 stop=7667.75 (live)
16:45:05  [Gateway] §5a NO_LABEL: IB locked, day_type=None → shadow      ← שניהם נהרגו כאן
16:45:05  [S1-OPENING] T-314: locked opening=OPEN_REJECTION_REVERSE dir=UP at bar 4
17:00:04  [DayType] Classification changed: UNKNOWN -> Normal (conf=0.35)   ← 15 דק' מאוחר מדי
```
ומה שהמחיר עשה מיד:
```
16:45  H=7689.50  L=7677.50     ← T1 (7688.50 אחרי TARGET_REALISM) נפגע בבר-הכניסה עצמו
16:50  H=7689.75
17:00  H=7689.25  C=7687.00      הסטופ 7666.50 לא התקרב — השפל היה 7677.50
```
‏`10.25` נק' × `n=3` × `$5` = **`+$153.75`** ברגל הראשונה בלבד.

**הכיוון היה מאושר:** סוג-הפתיחה שננעל הוא `OPEN_REJECTION_REVERSE dir=UP`, ושורת-שלב-B
שלו היא `bias: reversal_direction` (=UP) · `entry_kinds: [REVERSAL, PULLBACK]`. הלונג תאם
את השורה בדיוק. **השורה בחרה לפי סוג-הפתיחה ולא השתמשה בסוג-היום בכלל** — ובכל זאת `§5a`
דרש תווית-יום.

## מה לבנות (בדיוק זה)
`backend/v9/gateway/trading_gateway.py:1419-1431`, בלוק `NO_LABEL_NO_FIRE_V1`:

1. **להוסיף תנאי-שלב:** הבלוק פעיל **רק כאשר** `_resolve_phase(_dp_il_hhmm) in ("C","D")`.
   בשלבים **A ו-B** — לא נוגעים ולא מנתבים לצל. להשתמש באותו
   `from backend.v9.services.dalton_playbook import _resolve_phase` שכבר מיובא ב-T-355.
2. כשהבלוק **מדולג** בגלל שלב A/B, לרשום שורה מפורשת:
   `logger.info("[Gateway] §5a skipped: phase=%s — opening doctrine governs, day_type not required (Michael 14.09 17:07)", _phase)`.
3. **שום שינוי אחר.** ‏`§5a` בשלבים C/D נשאר כפי שהוא (הנימוק המקורי — "20/46 עסקאות-לייב
   ירו על `None`" — תקף שם, כי שם השורה נבחרת **לפי סוג-היום**).
4. אם `_resolve_phase` נכשל מכל סיבה ⇒ **fail-closed**: להתנהג כמו היום (לנתב לצל).

## golden (שורות גולמיות בדיווח)
- **ח1** — ריפליי על **היום (2026-09-14)** ברגע שיהיו ברים: `16:45:05 OPENING_ORR LONG 7678.25`
  ⇒ **`blocked_by=None` ו-ADMIT**, לא shadow. להדביק את שורת-השער המלאה.
- **ח2** — אותה ריצה: `§5a skipped: phase=B` מופיע, ו-`§5a NO_LABEL` **לא** מופיע לפני 17:30.
- **ח3** — **שלב C לא נפגע:** בכל 5 סשני-הבסיס, כל מופע של `§5a NO_LABEL` שהיה אחרי 17:30
  נשאר כפי שהוא. מפקד: כמה מופעים לפני-17:30 (אמורים להיעלם) וכמה אחרי (אמורים להישמר).
- **ח4** — **אפס שינוי בכתיבות** ב-5 הסשנים + `--restart-at 18:29`, **אלא אם** נוספת כתיבה
  בשלב A/B — ואם כן, **לדווח אותה במפורש עם השרשרת המלאה** ולא לבלוע.
- **ח5** — `T-335`=0 · `Traceback`=0 · דגל `NO_LABEL_NO_FIRE_V1` לא נגעת בו (נשאר כפי שהוא ב-`.env`).
- **ח6** — מבחן `test_no_label_phase_gate.py`: שלב B + `day_type=None` ⇒ **לא מנותב לצל** ·
  שלב C + `day_type=None` ⇒ **כן מנותב** · `_resolve_phase` זורק ⇒ מנותב (fail-closed).

## הערה על סדר-הביצוע
**זה הפריט הראשון של הערב — לפני פריט 7 (המדידות).** הוא עלה לנו עסקה מרוויחה היום,
והוא ישוב ויעלה מחר בפתיחה. המדידות יכולות לחכות שעה.

## דיווח
קומיט אחד · `TASK_LOG` (T-365) + `STATUS_BOARD` באותו קומיט · LOG חתום עם ח1–ח6 ·
ואז **"סיים פריט 8"** ולעצור. אין ריסטארט לפני 23:00, אין `.env`, אין נגיעה בפוזיציות.
