# MEMS26 · מוכנות ליום-מסחר אוטונומי — שישי 2026-08-21

**נכתב:** 2026-08-20 22:10–22:35 IDT · cowork-dev/agent · **קריאה-בלבד על מערכת-המסחר**
(לא נגעתי ב-`.env`, לא בוצע ריסטארט, לא נשלחה פקודה לסיירה, לא הודלק/כובה שום דגל).
הכתיבה היחידה: המסמך הזה · `LIVE_CHANNEL` · `TASK_LOG` · `STATUS_BOARD` · `FLAG_INDEX`
(רגנרציה).

**דרישת-מייקל:** שהמערכת תסחור מחר **לבד**, בלי התערבות אנושית.

---

## 0 · שורה-תחתונה

**כן — המערכת מסוגלת לסחור מחר לבד, ואין `.env` להוסיף ואין ריסטארט לבצע.**
ההנחה שאיתה נכנסתי לבדיקה (*"4 דגלים ממתינים ל-`.env`"*) **התיישנה**:
`cc-macbook` כבר הוסיף את כל ארבעתם ב-18:33:30 והפעיל ריסטארט ב-18:35:52 —
כלומר **כל F1/F4/F5/F6 חיים מאז אחר-הצהריים**, ולשלושה מהם יש שורות-לוג של
הפעלה-בפועל **אחרי** אותו בוט (ראיה ב-§1).

**מה כן יכול לעלות כסף מחר, ואף אחד מהם אינו דגל ואינו קוד:**

| # | חוסם | חומרה | מי חייב לתקן |
|---|---|---|---|
| 1 | **מייקל סוחר ידנית באותו חשבון** ⇒ 3 יריות-לייב נחסמו היום בשער-הבעלות | 🔴 הכי-יקר | **מייקל** (החלטה, לא קוד) |
| 2 | **מרג'ין דק** — $1,892.99 ⇒ 6 חוזים בלתי-אפשריים, 4 עם עודף $298 בלבד | 🔴 | **מייקל** (הפקדה) |
| 3 | **T-43 · סיירה לא ב-Login Items** — ריבוט = אין פיד ואין ביצוע | 🔴 | **מייקל** (30 שניות) |
| 4 | **T-42(ב) · על סוללה המק ישן אחרי דקה** (`sleep 1`) | 🟠 | **מייקל** (`sudo`) |
| 5 | **מק-2 כבוי** (10.1.118.70 — 100% packet loss) | 🟠 | **מייקל** (אם רוצים גיבוי) |
| 6 | **מסלול-ההתראות-לפלאפון לא נבחן היום** (0 שליחות ⇒ "לא-נבחן", לא "עובד") | 🟠 | cc-macbook |

---

## 1 · דגלים + קוד — **PASS**

### 1.1 · ארבעת הדגלים "הממתינים" — כבר באוויר

```
$ python3 scripts/flag_guard.py | tail -3
FLAG-GUARD: PASS — all 186 ruled flags match.
```

**אפס ✗.** כל ארבעת הדגלים קיימים ב-`.env` **וגם** ב-`RULED_FLAGS.yaml` עם
`expected: "1"`:

```
$ grep -E "^(TREND_STEP_STRUCT_EXEMPT_V1|RUNNER_TRAIL_V2|DAYTYPE_RECLASS_STABILITY_V1|DAYTYPE_HONEST_PRELOCK_V1|DIRECTION_COMPASS_V1|S2_ADAPTIVE_THRESHOLDS_V1)=" .env
TREND_STEP_STRUCT_EXEMPT_V1=1
RUNNER_TRAIL_V2=1
DAYTYPE_RECLASS_STABILITY_V1=1
DAYTYPE_HONEST_PRELOCK_V1=1
DIRECTION_COMPASS_V1=1
S2_ADAPTIVE_THRESHOLDS_V1=1
```

**החלון קובע — ה-`.env` נכתב לפני שהתהליך עלה, ולכן נטען:**

```
$ stat -f "%Sm  %N" -t "%Y-%m-%d %H:%M:%S" .env
2026-08-20 18:33:30  .env
$ ps -o pid,lstart -p $(pgrep -f "uvicorn backend.main:app" | head -1)
 8852 Thu Aug 20 18:35:52 2026
$ grep "\[boot\] logging OK" /tmp/backend.err.log | tail -1
2026-08-20 18:35:56 [INFO] [mems26.boot] [boot] logging OK level=INFO pid=8852 commit=c1ba247a stream=stderr
```

`.env` ‏18:33:30 → תהליך 18:35:52 → **הפרש 2:22 לטובת ה-`.env`.**
הקומיט-הרץ `c1ba247a` (F6) מכיל את F4+F5+F6; ‏`HEAD=e28736d4` הוא **דוקומנטציה בלבד**
(T-71..T-73, read-only) ⇒ **אין פער-קוד בין הריפו למה שרץ.**

### 1.2 · ראיית-הפעלה בפועל אחרי הבוט (לא רק "הדגל מוגדר")

| דגל | שורת-לוג חיה (אחרי 18:35:56) |
|---|---|
| `RUNNER_TRAIL_V2` | `18:55:05 [SierraCmd] F5 RUNNER_TRAIL_V2: trade 756 runner leg c4 placed STOP-ONLY (no fixed target) — exit is the structural swing trail` |
| `DAYTYPE_RECLASS_STABILITY_V1` | `20:25:02 [S1-NEW-CLS] DAYTYPE_RECLASS_STABILITY held: Variation → Neutral_Extreme not published (pending Neutral_Extreme 1/2, PROVISIONAL)` |
| `TREND_STEP_STRUCT_EXEMPT_V1` | `21:25:03 [Gateway] TREND_STEP_STRUCT_EXEMPT: A1 structural_targets_wrong_side SKIPPED for TREND_STEP SHORT entry=7674.5 — leg ladder kept (stop=7679.25 t1=7666.0 t2=7659.25 t3=7649.75)` |
| `DAYTYPE_HONEST_PRELOCK_V1` | `20:30:04 [S1-NEW-CLS] promoted: Variation → Neutral_Extreme (Neutral_Extreme, PROVISIONAL)` — תגית `PROVISIONAL` היא הסימון שהדגל מוסיף |

### 1.3 · שני הדגלים "כבר-חיים" שנתבקשתי לאמת

**`S2_ADAPTIVE_THRESHOLDS_V1` — חי ומדיד.** הסף אינו קבוע אלא נגזר-בר:

```
2026-08-20 22:05:03 [INFO] [mems26.systems.five_min] [S2-DL] INITIATIVE ts=2026-08-20 19:00:00+00:00
  b1_exp=0 b2_test=1 b3_join=1 b4_test=1 b1_bull=1 b1_bear=0 b1_range=4.5 exp=[5.5,10.5]
2026-08-20 22:10:03 [INFO] [mems26.systems.five_min] [S2-DL] INITIATIVE ts=2026-08-20 19:05:00+00:00
  b1_exp=0 b2_test=1 b3_join=1 b4_test=1 b1_bull=0 b1_bear=1 b1_range=3.0 exp=[5.6,10.8]
```

שני ברים סמוכים, **שני חלונות שונים** (`[5.5,10.5]` מול `[5.6,10.8]`) ⇒ אדפטיבי,
לא קבוע. ‏**134 שורות `[S2-DL]` היום.**

**`DIRECTION_COMPASS_V1` — חי, וירה לראשונה.** ‏T-72 תיעד ש-`grep -c "BLOCKED by
direction-compass"` היה **0** ⇒ ה-+$325 של רפליי-F1 היה ≈$0 בייצור. **היום זה השתנה:**

```
2026-08-20 18:55:01 [INFO] [Gateway] BLOCKED by direction-compass: FAMIR LONG against compass DOWN (conf 1.00): fused score -1.00 (conf 1.00) → DOWN · confirmed
2026-08-20 18:55:01 [WARNING] [Gateway] BLOCKED system=4 pattern=FAMIR dir=LONG entry=7696.75 blocked_by=direction_compass
```

⚠️ `n=1` — סימן-כיוון, לא מסקנה. ‏T-72 (המצפן אינו מזין את `lsma_flat`) נשאר פתוח.

---

## 2 · שערים — **PASS (‏1 אזהרה קוסמטית)**

```
$ python3 scripts/mems26_arming_gate.py
══ MEMS26 arming gate · localhost:8000 · RTH ══
  ✅ ARMED  S2 · Five-Minute Patterns    fired=0 waiting=10 broken=0
  ✅ ARMED  S4 · Woodies CCI Patterns    fired=2 waiting=7  broken=0
  ✅ ARMED  S3 · Footprint Patterns      fired=0 waiting=4  broken=0
  ✅ ARMED  S1 · Day Type Classification fired=0 waiting=1  broken=0
  ✅ core streams healthy (5min · woodies_5min · live_price)
══ ✅ ALL SYSTEMS ARMED ══
```

```
$ python3 scripts/fire_drill.py | tail -14
  ✓ flag_guard — FLAG-GUARD: PASS — all 186 ruled flags match.
  ✓ effective_contracts == 4 (נפילת-מרג'ין מהפסיקה 08-19; הפסוק 6) — got 4
  ✓ wire_guard — 48 call sites bound against 11 guarded signatures
  ✓ task_log_guard — 73 items, last committed 0.1 days ago
  ✓ backend health
  ✓ T-61 שכבת-INFO בלוג (backend.err.log) — 2026-08-20 18:35:56 [INFO] [mems26.boot] [boot] logging OK level=INFO pid=8852 commit=c1ba247a stream=stderr · 3340 שורות INFO אחריה
  ✓ T-61 רמת-INFO זורמת בפועל (לא רק שורת-הבוט)
  ✓ feed טרי (<30s) — age=153ms
  ✓ live_slot פנוי — slot=None
  ✓ live_enabled == [2,4] — [2, 4]
  ✓ day_type קיים — Neutral_Extreme conf=0.38
🟢 GO — כל שרשרת ההחלטה כשרה לירי.
```

**בדיקת-בוט-הלוגינג עוברת עבור ה-PID הרץ** — `pid=8852` בשורת-הבוט **זהה**
ל-`pgrep -f "uvicorn backend.main:app"` = `8852`. זו בדיוק המלכודת של §3.9
ב-`COWORK_DAILY_READ` (כל 19.08 היה עיוור), והיא סגורה.

```
$ bash scripts/mems26_verify.sh | tail -12
  ✅ backend :8000 → HTTP 200 · ✅ bridge running · ✅ export promoter running
  ✅ com.mems26.{backend,bridge,export_promoter} running
  ✅ deployed DLL == committed monolith (SierraChart)
  ✅ compiled DLL is newer than its source (2026-08-18 10:47)
  ✅ sc_study/ clean in git
  ⚠️  FLAG_INDEX drift → run: python3 scripts/gen_flag_index.py
  ✅ woodies_5min.json fresh (2s)
  ✅ v9_bars_5min_woodies last bar lag: 00:03:10
════ verdict: OK · 1 warn ════
```

האזהרה היחידה **אינה דריפט-קוד**: `gen_flag_index.py --check` יוצא `rc=0` ומדווח
**39 דגלים ללא-סמנטיקה** ב-`FLAG_REGISTRY.yaml` (חוב-תיעוד ותיק, לא משטח-סיכון).
רגנרציה בוצעה והוכנסה לקומיט הזה (5 שורות השתנו).

---

## 3 · חוסמי-אוטונומיה — הבדיקה האמיתית

### (a) T-42 · שינת-חצות — **PASS חלקי** 🟠

```
$ pmset -g sched
Scheduled power events:
 [0]  wake at 08/20/2026 23:00:00 by '…donotdisturb…'
 [1]  wake at 08/21/2026 02:55:39 by '…osanalytics…'
```

**אין שום אירוע-שינה מתוזמן, ואין `repeat`** ⇒ החצי הראשון של T-42
(`sudo pmset repeat cancel`) **בוצע ואומת**. ✅

**אבל החצי השני לא בוצע:**

```
$ pmset -g custom
Battery Power:  … sleep 1 …  disksleep 10 …
AC Power:       … sleep 0 …  disksleep 0  …
```

**על חשמל — לעולם לא ישן (תקין). על סוללה — ישן אחרי דקה.**
כלומר: אם הכבל יישלף/ייפול, המערכת מתה תוך דקה, באמצע הסשן.
**הצעד:** `sudo pmset -b sleep 0 disksleep 0` (דורש sudo — **רק מייקל**).
עד אז: **הכבל חייב להישאר מחובר.**

### (b) T-43 · סיירה לא ב-Login Items — **FAIL** 🔴

```
$ osascript -e 'tell application "System Events" to get the name of every login item'
ZeroTier
```

**פריט-התחברות אחד בלבד, וזה לא סיירה.** אין גם plist של סיירה:

```
$ ls ~/Library/LaunchAgents/ | grep -ci sierra
0
```

סיירה כן רצה כרגע (‏CrossOver, מ-13Aug26) — **כלומר כל עוד אין ריבוט, הכל תקין.**
אבל ריבוט/קריסה/עדכון-מערכת = **אין פיד, אין ברים, אין ביצוע** — וכל שאר
ה-LaunchAgents יעלו יפה ויציגו מערכת "בריאה" בלי שום מחיר.
**זה החוסם היחיד שהופך "אוטונומי" ל"אוטונומי כל עוד לא היה ריבוט".**

### (c) LaunchAgents — **PASS**

```
$ launchctl list | grep mems26
2061   0    com.mems26.mobile_relay
8852  -15   com.mems26.backend
-      0    com.mems26.update_check
95424  0    com.mems26.export_promoter
-      0    com.mems26.eod_handoff
-      0    com.mems26.startup_check
21673  0    com.mems26.frontend
774    0    com.mems26.bridge
2064   0    com.mems26.activity_feed
```

תשעה סוכנים, **כולם exit-0**. ה-`-15` על ה-backend הוא **SIGTERM של הריסטארט
המכוון ב-18:35** — לא כשל. שלושת ה-`-` (בלי PID) הם סוכני-אירוע:
`update_check` (כל שעה), `eod_handoff` (23:05, א׳–ו׳), `startup_check` (RunAtLoad).

**כולם `RunAtLoad=True` + `KeepAlive={SuccessfulExit:False}`** ⇒ אחרי ריבוט הכל
חוזר לבד — **חוץ מסיירה** (§b).

**`update_check` נבדק ואינו מסוכן:** הוא רק `git fetch` + התראת-macOS. אין בו
`git pull`, אין `launchctl restart` ⇒ **הקוד לא ישתנה מתחתינו בלילה.**

### (d) startup_check — **PASS** (אומת בהרצה אמיתית תחת launchd)

```
$ launchctl kickstart -k gui/$UID/com.mems26.startup_check
$ cat /tmp/mems26_startup_report.txt
MEMS26 — בדיקת-הפעלה 2026-08-20 22:17:18 IDT
מצב: ✅ כל המערכות תקינות   (עבר:7  אזהרות:0  נכשל:0)   RTH=כן
  ✅ Backend :8000 · ✅ Postgres · ✅ Bridge · ✅ Export promoter
  ✅ Sierra feed — fresh (0s, RTH) · ✅ Frontend :3000 · ✅ Bridge → backend — pushing
$ launchctl list | grep startup_check
-	0	com.mems26.startup_check
```

**7/0/0, יציאה 0.** באג-נוי אחד: `/bin/bash: line 16: BASH_SOURCE[0]: unbound
variable` — הסקריפט מוזרם ב-`bash -s` ולכן `BASH_SOURCE` ריק תחת `set -u`.
**לא מזיק** כי ה-plist מגדיר `cwd=` שורש-הריפו ולכן `.env` נטען יחסית.
**התיקון (🔵 נמוך):** `${BASH_SOURCE[0]:-$0}` בשורה 16.

⚠️ **מגבלה שחשוב לדעת:** ‏`startup_check` **מדווח בלבד** — הוא לא מפעיל שום דבר,
ובפרט לא את סיירה. אחרי ריבוט הוא ייתן ❌ על הפיד ויציג נוטיפיקציה — **וזהו.**

### (e) שטח-דיסק — **PASS** ✅ (השתפר דרמטית)

```
$ df -h /System/Volumes/Data
/dev/disk1s1   932Gi   811Gi    95Gi    90%   /System/Volumes/Data
```

**95 GB פנויים (10%)** — היה 2.4% . יש מקום לוגים, ייצואים ו-DB ליום שלם.

### (f) מק-2 — **FAIL (המכונה כבויה)** 🟠

```
$ curl -m 10 -w "http=%{http_code}\n" "http://10.1.118.70:8000/api/v9/mobile/data?key=***"
http=000  time=10.009s  bytes=0
$ ping -c 3 10.1.118.70
3 packets transmitted, 0 packets received, 100.0% packet loss  ("Host is down")
$ zerotier-cli info
200 info da554d54c3 1.16.2 ONLINE        ← ZeroTier של המכונה הזו תקין
200 listnetworks 08752e18b1e32b2e Michael … OK PRIVATE feth3995 10.1.118.147/24
```

**הרשת תקינה — המכונה כבויה.** אי-אפשר לקבוע אם היא "רוסטרטה/נקייה/סים" —
**לא ניתן לקבוע** (חוק-הברזל §0). זה T-04 + T-63, שניהם כבר פתוחים.
**לא חוסם את מחר** — המסחר רץ על המכונה הזו (10.1.118.147, `MACHINE_TAG=מק-1`).

### (g) מרג'ין — **PASS ל-4 חוזים · FAIL ל-6** 🔴

```
$ python3 -c "import json;d=json.load(open('~/SierraChart_Data/v9_export/sierra_state.json'))…"
is_sim                     = 0            ← LIVE
trade_account              = 37138283
position_qty               = 0            ← שטוח כרגע
acct_available_funds       = 1892.99
acct_daily_pl              = 90.0
daily_pnl                  = -183.75
daily_total_qty_filled     = 94
acct_loss_limit_reached    = 0
acct_trading_disabled      = 0
acct_under_margin          = 0
acct_daily_net_loss_limit  = -1081.79
```

| גודל | דרישה (386.20×n + 50 חיץ) | זמין | פער |
|---|---|---|---|
| **4 חוזים** | **$1,594.80** | $1,892.99 | **✅ +$298.19** |
| **6 חוזים** | **$2,367.20** | $1,892.99 | **❌ −$474.21** |

והמערכת כבר יודעת זאת ופועלת לפי הפסיקה:

```
[SierraCmd] MARGIN SIZING 6 → 4 — MARGIN FALLBACK 6→4 (Michael 2026-08-19):
            $1842.99 usable cannot carry 6×$386.20
$ python3 scripts/fire_drill.py | grep effective
  ✓ effective_contracts == 4 … — got 4
```

🔴 **הסכנה:** ‏`margin_sizing.cap_contracts` הוא **6-או-4 בלבד** —
`MARGIN_FALLBACK_CONTRACTS = 4`, ומתחת ל-4 אין נפילה נוספת ("אם גם 4 לא עובר,
הברוקר דוחה"). **העודף של $298 שווה 0.77 חוזה.** אם מייקל יחזיק פוזיציה ידנית
בבוקר, המרג'ין הפנוי יירד ו-**כניסות-הלייב יידחו ע"י הברוקר** — בשקט יחסית.

### (h) `live_slot` ועסקאות תקועות — **PASS**

```
$ curl -s localhost:8000/api/v9/gateway/status
{"live_slot": null, "live_slot_system": null, "daily_pnl": 0.0, "trades_today": 1,
 "consecutive_losses": 0, "live_enabled_systems": [2,4],
 "cooldown": {"cooldown_active": false}, "cluster_guard": {"cluster_guard_active": false},
 "ssv": {"veto_active": false}, "chop_state": "EXPANDING"}
```

```sql
SELECT state, mode, count(*) FROM v9_trades WHERE entry_ts > now()-interval '30 days' GROUP BY 1,2;
 CLOSED    | shadow | 171
 CLOSED    | live   |  59
 CANCELLED | live   |   1
 FILLED    | shadow |   1     ← #758, צל, נכנס 14:25 ET, השוק עוד פתוח
```

**אפס עסקאות-לייב לא-סגורות.** כל ה-`CANCELLED` הן היסטוריות
(`ORDER_FAILED:-1` מיוני-יולי · `PHANTOM_*` מ-07-27). ‏#758 היא **צל** ⇒
לא נועלת סלוט ולא מסכנת כסף.

עסקאות היום (ET):

| id | mode | state | pat | dir | ET | exit | P&L |
|---|---|---|---|---|---|---|---|
| 754 | shadow | CLOSED | ZLR | LONG | 09:56 | STOP_HIT | −80 |
| 755 | shadow | CLOSED | TREND_STEP | SHORT | 11:55 | STOP_HIT | −70 |
| **756** | **live** | CLOSED | TREND_STEP | SHORT | 11:55 | MAE_SCRATCH | **0 (BE)** |
| 757 | shadow | CLOSED | ZLR | LONG | 13:00 | STOP_HIT | −155 |
| 758 | shadow | FILLED | TREND_STEP | SHORT | 14:25 | — | פתוחה |

### (i) התראות-פלאפון — **לא-נבחן היום** 🟠 (לא "עובד", לא "שבור")

```
$ grep -c "^2026-08-20.*pushover" /tmp/backend.err.log
0
$ grep -iE "pushover|ntfy" /tmp/backend.err.log | tail -4
pushover send FAILED after 3 attempts (rc=6 ) — title='[מק-1] 🔴 MEMS26: DIVERGENCE'. Michael did NOT receive this.
pushover send FAILED after 3 attempts (rc=6 ) — title='[מק-1] 🔴 MEMS26: NAKED STOP SUSPECT'. Michael did NOT receive this.
```

**היום: אפס כשלים — אבל גם אפס שליחות.** לפי מלכודת §3.6, זה **"לא-נבחן"**,
ואסור להסליק ל"עובד". ‏`rc=6` = curl "couldn't resolve host" (DNS). כרגע הנתיב פתוח:

```
$ nslookup api.pushover.net → 172.66.129.115   ·   ntfy.sh → 159.203.148.75
$ curl -o /dev/null -w "%{http_code}" https://api.pushover.net/1/messages.json → 400 (מגיב)
$ curl -o /dev/null -w "%{http_code}" https://ntfy.sh/ → 200
```

**הצעד לפני מחר (cc-macbook, דקה):** לשלוח התראת-בדיקה אחת דרך
`backend/v9/services/phone_alert.py` ולוודא שהיא הגיעה לפלאפון של מייקל.
בלי זה, אזעקת-אמת מחר עלולה למות בשקט — **וזו בדיוק הצורה שהרגה את 12.08**.

---

## 4 · מה `cc-macbook` צריך לעשות הלילה

### 4.1 · שורות-`.env` להוסיף — **אין. אף אחת.**

ההנחיה ב-`LIVE_CHANNEL` (‏5 שורות 🔴 אל cc-macbook) **כבר בוצעה ב-18:33** —
`flag_guard` מחזיר `PASS — all 186`. **אין מה להוסיף, ואין מה "לתקן".**
אם למרות זאת ייפתח `.env` — **לא לגעת בשום דגל אחר** (כל דגל שם הוא פסיקה עומדת;
CLAUDE.md § Standing Decisions).

### 4.2 · ריסטארט — **לא נדרש**

| מה | ערך | מסקנה |
|---|---|---|
| קומיט-רץ | `c1ba247a` | מכיל F1+F4+F5+F6 |
| `HEAD` | `e28736d4` | **דוקומנטציה בלבד** (T-71..T-73) |
| `.env` mtime | 18:33:30 | **לפני** הבוט |
| בוט | 18:35:52 / `pid=8852` | טען את ה-`.env` הנוכחי |

**ריסטארט הלילה יעלה ולא ירוויח** — הוא מאפס מונים, מאבד את `sierra_targets`
שבמטמון-זיכרון (T-13), ומכניס חלון של דקות בלי הגנה.
**אם בכל-זאת נדרש ריסטארט** (למשל אחרי `git pull` שנוגע בקוד):

```bash
cd /Users/michael/Downloads/mems26_web_git
git pull --rebase                       # רק אם יש קוד חדש
launchctl kickstart -k gui/$UID/com.mems26.backend
sleep 25
```

**לעולם לא `pkill`** — ה-LaunchAgent הוא הבעלים (‏`KeepAlive={SuccessfulExit:false}`).
לפני כל נגיעה ב-`.env`/LaunchAgent: `scripts/mems26_snapshot.sh "why-label"`.

### 4.3 · מה כן לעשות (2 פריטים, ~5 דקות)

1. **בדיקת-פלאפון אחת** (§3i) — לשלוח התראה ולוודא קבלה.
2. **לוודא שהמק על חשמל** ולא על סוללה (§3a) — או להריץ
   `sudo pmset -b sleep 0 disksleep 0` אם מייקל זמין.

---

## 5 · אימות-בוקר (2026-08-21, לפני 09:30 ET / 16:30 IDT)

הפרוטוקול המלא: `docs/runbooks/PRE_OPEN_PROTOCOL_2026-08-16.md`.
המינימום שחייב לעבור — **הפקודה + הפלט-הצפוי**:

```bash
cd /Users/michael/Downloads/mems26_web_git

# 1) שער-הדגלים — חייב PASS, אפס ✗
python3 scripts/flag_guard.py | tail -1
#  ← FLAG-GUARD: PASS — all 186 ruled flags match.

# 2) שכבת-הלוג רואה, ועבור ה-PID שרץ (מלכודת §3.9 — היא הרגה את כל 19.08)
PID=$(pgrep -f "uvicorn backend.main:app" | head -1); echo "running pid=$PID"
grep "\[boot\] logging OK" /tmp/backend.err.log | tail -1
#  ← …[boot] logging OK level=INFO pid=<אותו PID> commit=…
#  אם ה-pid שונה — לעצור. אל תדווח "0 שורות" על שום דבר.

# 3) שרשרת-ההחלטה
python3 scripts/fire_drill.py | tail -3
#  ← 🟢 GO — כל שרשרת ההחלטה כשרה לירי.

# 4) חימוש 4 המערכות
python3 scripts/mems26_arming_gate.py | tail -2
#  ← ══ ✅ ALL SYSTEMS ARMED ══

# 5) עקביות-מערכת (DLL↔ריפו · פיד · DB)
bash scripts/mems26_verify.sh | tail -1
#  ← ════ verdict: OK ════   (או "OK · 1 warn" על FLAG_INDEX — קוסמטי)

# 6) 🔴 החשבון שטוח לפני הפתיחה — הבדיקה הכי-חשובה של מחר
python3 -c "import json;d=json.load(open('/Users/michael/SierraChart_Data/v9_export/sierra_state.json'));print({k:d[k] for k in ('is_sim','position_qty','acct_available_funds','acct_trading_disabled','acct_under_margin')})"
#  ← {'is_sim': 0, 'position_qty': 0, 'acct_available_funds': >1594.80,
#     'acct_trading_disabled': 0, 'acct_under_margin': 0}
#  position_qty ≠ 0 ⇒ כל ירי-לייב ייחסם כל היום (§6.1). available < 1594.80 ⇒ גם 4 חוזים ידחו.

# 7) סיירה חיה (ריבוט = היא לא תעלה לבד — T-43)
ls -lt ~/SierraChart_Data/v9_export/ | head -3        # mtime של עכשיו
curl -s localhost:8000/api/v9/live_price | head -c 120 # age_ms < 30000

# 8) סלוט-לייב פנוי + מערכות מחומשות
curl -s localhost:8000/api/v9/gateway/status | python3 -m json.tool | grep -E "live_slot|live_enabled"
#  ← "live_slot": null   ·   "live_enabled_systems": [2, 4]
```

**אחרי הסגירה — לספור את ארבעת הדגלים החדשים** (‏F1/F4/F5/F6 חייבים להיות מדידים
מהיום הראשון; זו דוקטרינת-F3):

```bash
D=$(date +%F)
for k in "BLOCKED by direction-compass" TREND_STEP_STRUCT_EXEMPT \
         "RUNNER_TRAIL_V2" "DAYTYPE_RECLASS_STABILITY held" "ONE-SOURCE re-sync" PROVISIONAL; do
  printf "%-38s %s\n" "$k" "$(grep "^$D" /tmp/backend.err.log | grep -c -- "$k")"
done
```

---

## 6 · מה עלול לעלות כסף מחר — לפי סדר

### 6.1 🔴 **מייקל סוחר ידנית באותו חשבון — זה חסם 3 יריות-לייב היום**

**הממצא הכי-יקר של הסריקה.** החשבון (37138283) משותף למערכת ולמסחר-הידני,
ושער-הבעלות **חוסם כל כניסת-לייב** כשעומדת פוזיציה שאינה של ה-TM:

```
2026-08-20 16:56:00 [CRITICAL] [Gateway] LIVE fire BLOCKED pre-send: standing position +5 on the account at fire time (live slot was free → not TM-managed). Sierra's recipe rejects opposite entries (AllowOppositeEntryWithOpposingPositionOrOrders=0)…
2026-08-20 20:00:03 [CRITICAL] [Gateway] LIVE fire BLOCKED pre-send: standing position +4 …
2026-08-20 21:25:03 [CRITICAL] [Gateway] LIVE fire BLOCKED pre-send: standing position -2 …
```

שלוש החסימות נופלות **בדיוק** על שלוש עסקאות-הצל: #754 (09:56 ET) · #757 (13:00) ·
#758 (14:25). כלומר **מ-4 מועמדי-לייב היום, 3 נחסמו — 75%.** היחיד שעבר (#756,
11:55 ET) עבר כי החשבון היה שטוח באותה דקה בדיוק.

הפוזיציה הזרה זזה כל היום — `45 שורות DIVERGENCE`:
`14:30 −4 → 15:04 −3 → 16:55 +5 → 17:53 +4 → 18:38 −4 → 21:03 −5 → 21:35 −1`,
מול `daily_total_qty_filled=94` ורק **5 פקודות** מהמערכת (‏#401 `PLACE`,
‏#402–#405 `FLATTEN_ACCOUNT`) ⇒ לפי §3.5 **הנפח הוא של מייקל**, לא fill-שאבד.

**היום זה דווקא חסך ~$235** (שלוש הצללות: −80/−155/פתוחה) — **אבל זה מזל, לא מדיניות.**
**זו פסיקה של מייקל, לא באג ולא קוד.** שלוש אפשרויות, כולן דורשות אותו:
1. **לא לסחור ידנית מחר** באותו חשבון בשעות ה-RTH ⇒ המערכת אוטונומית באמת.
2. **חשבון נפרד** למסחר-הידני (הפתרון היציב).
3. **להשאיר כמו-שהוא** ולדעת שהמערכת תירה רק בחלונות שבהם הוא שטוח.

### 6.2 🔴 **מרג'ין דק — 6 חוזים בלתי-אפשריים, ו-4 עם עודף $298**

ראה §3g. **הצעד:** אם מייקל רוצה את 6-החוזים הפסוקים — דרושים
**≥$2,367.20 פנויים** (חסר $474.21). אחרת המערכת תרוץ ב-4, בהתאם לפסיקת 19.08,
ו-**זה בסדר** — כל עוד לא נאכלים גם ה-$298 האלה ע"י פוזיציה ידנית.

### 6.3 🔴 **T-43 · ריבוט = מערכת מתה בשקט**

ראה §3b. **הצעד (מייקל, 30 שניות):** System Settings → General → Login Items →
`+` → `SierraChart_64 (Y_SierraChart)`. **או** להחליט מפורשות שזה ידני — ואז
ריבוט מחייב הפעלה-ידנית של סיירה **לפני** 09:30 ET.

### 6.4 🟠 **T-42(ב) · שינה על סוללה אחרי דקה**

ראה §3a. עד `sudo pmset -b sleep 0 disksleep 0` — **הכבל חייב להיות מחובר.**

### 6.5 🟠 **מסלול-ההתראות לא-נבחן**

ראה §3i. אזעקת-אמת מחר עלולה למות בשקט. בדיקה אחת פותרת.

### 6.6 🟠 **T-45 · ספאם NAKED_STOP — נסגר, אבל לא-נבחן על אזעקת-אמת**

```
NAKED_STOP_SUSPECT   today=0   all=13816     (19.08 לבדו: 3,139)
ORPHAN               today=0   all=641
```

תיקון `3eff854c` עובד — **הספאם נעלם**. אבל 0 שורות היום ⇒ **אזעקת-אמת עדיין
לא נבחנה** (§3.6). נשאר 🔴 ב-TASK_LOG עד שתיבחן.

### 6.7 🔵 **‏4 פקודות `FLATTEN_ACCOUNT` כפולות ב-3 שניות**

```
18:55:05 #401 op=PLACE            (trade 756)
19:02:58 #402 op=FLATTEN_ACCOUNT  pending_before=0 fast_path=True
19:02:59 #403 op=FLATTEN_ACCOUNT  pending_before=1
19:03:01 #404 op=FLATTEN_ACCOUNT  pending_before=1
19:03:01 #405 op=FLATTEN_ACCOUNT  pending_before=2
19:03:02 [ExitVerify] trade 756 exit CONFIRMED flat after 3.6s (mae_scratch) — books closed
```

‏`S6 MAE SCRATCH` ירה 4 פעמים בזמן שהיציאה הייתה `PENDING`.
**לא הזיק כאן** — `account_has_foreign_contracts(4)` על פוזיציה של −3 מחזיר
`False` ⇒ רק הפוזיציה שלנו נסגרה, ו-`FLATTEN` אידמפוטנטי.
**אבל** על חשבון משותף כל שליחה כזו היא הזדמנות נוספת למרוץ מול §6.1.
**הצעד:** דה-דופ בין `MAE_SCRATCH` ל-`ExitVerify PENDING`. לא-חוסם.

### 6.8 · פתוחים שאינם משפיעים על מחר

`T-72` (המצפן אינו מזין `lsma_flat` — דורש פסיקה) · `T-71` (`awaiting_release`
לא-מכומת) · `T-51` (3 מסלולי-גודל עיוורים; מסלול-ה-1 מת מאחורי `EDGE_FADE_V1=0`) ·
`T-03` (‏`op=MODIFY_TARGET` לא קיים ב-DLL) · `T-04`/`T-63` (מק-2 כבוי) ·
39 דגלים ללא-סמנטיקה ב-`FLAG_REGISTRY.yaml`.

---

## 7 · חדשות-רקע ליום שישי

`config/news_calendar.yaml` (עודכן היום 11:35 ET) — ל-**2026-08-21**:

| שעה ET | אירוע | חומרה |
|---|---|---|
| 09:45 | S&P Global Services / Manufacturing / Composite PMI Flash ×3 | 🟠 orange |
| 13:00 | Baker Hughes Rig Counts ×2 | 🟡 yellow |

**אין אירוע אדום ⇒ אין חלון-חסימה.** לפי פסיקת-מייקל 07-13 (*"אמרתי שרק אדום
חוסם מסחר"*): `window_for('red')=(10,5)` · `orange=(0,0)` · `yellow=(0,0)`.
אומת בהרצה. **הערה:** שלושת ה-PMI ב-09:45 הם 15 דקות לתוך הסשן —
תנודתיות בחלון-הפתיחה, בלי חסימה.

---

*כל פקודה במסמך הזה הורצה על ה-Mac דרך Desktop Commander ב-2026-08-20 22:10–22:35 IDT.
כל פלט הוא ציטוט גולמי. שום דבר לא הוסק מהסנדבוקס (חוק-הברזל §0).*
