# MEMS26 · טיוטות-עיצוב — 2026-06-19

עיצובים מוכנים-להפיכה-לפרומפט לבעיות שאומתו היום (`PATTERN_EOD_2026-06-19.md`).
**לא מומש דבר — design בלבד.** כל פריט: שורש · שינוי מוצע (קובץ/שכבה) · regression · סיווג trading-logic(→Michael)/display-safe · הצלבת-Sierra ל-CC.
**D-IDs עקביים עם 06-11→06-18** (עיצובים מתמשכים); עודכנו בראיות-היום. **חדשים: D22, D23.**

> **תזכורת standing-decision (CLAUDE.md, 2026-06-08):** chop-gates (S2 `choppiness_ok`, Layer-0 `chop_state`) ו-COT/AMT (S2⟂S3) **מנוטרלים בכוונה**. **אף עיצוב כאן אינו מציע לאפס flag או "להחזיר" שער-מנוטרל.** D18 (צ'ופ) מובא כ-**ראיה-להחלטת-Michael בלבד** (strategic-stop), לא כהמלצה-להדלקה — re-enable = trading-risk → sign-off מפורש.

> **🔴🔴 שינוי-מהותי היום (קריטי לקריאת כל העיצובים):** היום **הזנת-הנתונים מתה ~12:00 CT** (I-38) — חצי-RTH חסר. **העדיפות העליונה היום אינה מסחר אלא תשתית: D22 (feed-watchdog) הוא blocker-LIVE מסדר-ראשון.** כל עיצוב-מסחרי נשען על מדגם-בוקר-בלבד ⇒ משקלו זמני עד יום-data-מלא.

---

## D22 — חדש 🔴🔴 (operational → trading-impact): feed-death watchdog + EOD completeness-guard · **blocker-LIVE #0**

**שורש (מאומת 3 אותות):** `readiness.checks[bridge_streams_fresh]=passed:false "dead: bars_5min"`; בר-אחרון 11:55 CT; `last_fire` S2=12:00/S4=11:50; Bridge `data_freshness.fresh=false`. ⇒ הזנת-הברים מתה ~12:00 והמערכת נותרה עיוורת חצי-RTH **בלי התרעה** — התגלה רק ב-EOD. עסקה-פתוחה בעת-המוות (186/187) נותרה לא-מנוהלת. סיבה משוערת: backend-restart לפריסת `daytype_playbook` (864a345) שאחריו bridge/Sierra לא חזר.
**שלב-0 (verify, Rule 2 — CC):** `/tmp/bridge.err.log` + `~/SierraChart_Data/v9_export/` mtime סביב 12:00 + LaunchAgent `com.mems26.bridge` — **קריסת-bridge עצמאית** או **ריסטארט-ידני-שלא-החזיר**? קובע אם זה באג-יציבות או process-discipline.
**שינוי מוצע (שתי שכבות):**
  (א) **watchdog זמן-אמת:** משימה תקופתית (כל ≤2 דק') בודקת `bridge_streams_fresh`; אם `bars_5min` dead >N דק' ב-RTH ⇒ (1) נטיפיקציה מיידית ל-Michael, (2) `app.state.trading_halted=True` (עצור ירי-חדש), (3) flag עסקאות-פתוחות ל-manual-review. קובץ: בודק health קיים + `gateway` (לא לפתוח ירי כש-feed dead).
  (ב) **EOD completeness-guard:** סוכן-ה-EOD בודק `last_bar_ct` מול 14:55; אם פער >M דק' ⇒ כותרת-אזהרה בדוח + נטיפיקציה (מומש כבר היום ידנית — להפוך לאוטומטי).
**Regression:** הרג `bars_5min` ב-RTH סינתטי ⇒ watchdog מזהה תוך ≤N דק', מתריע, ו-`trading_halted=True`; ירי-חדש נדחה עם `blocked_by="FEED_DEAD"`.
**סיווג:** **operational/safe** ברובד-הניטור, אך **הפער עצמו = trading-risk** (פוזיציה-יתומה + חצי-יום עיוור) ⇒ **blocker-LIVE.** ה-halt-on-death נוגע ב-gateway ⇒ תיאום-Michael לפני הפעלה.
**Sierra/לוג:** `v9_export` mtime + bridge log (CC, §הצלבה 1).

## D23 — חדש 🟡→trading-adjacent: reconciliation לעסקה-תקועה + duplicate-fire-guard

**שורש (מאומת):** id186/187 (INITIATIVE_SHORT, 12:00, entry 7557, sizing=3) `state=FILLED` `exit_ts=null` — 3 שעות פתוחים (תת-תופעה של I-38). **בנוסף:** שתי עסקאות כמעט-זהות באותה דקה ⇒ אולי **duplicate-fire**; יחד עם gap-185 אולי triple.
**שינוי מוצע (שתי שכבות):**
  (א) **session-close reconciliation:** ב-EOD/at-close, כל עסקה `state∈{FILLED,OPEN}` ללא exit ⇒ force-close ל-last-known-price + `exit_reason="RECON_FEED_GAP"` + flag (לא להשאיר תלויה ל-LIVE).
  (ב) **duplicate-fire-guard:** ב-`trading_gateway` — דחיית setup זהה (system+pattern+direction+entry±ε) תוך חלון-T שניות מקודמו.
**Regression:** סיגנל כפול תוך T שניות ⇒ השני `blocked_by="DUPLICATE"`; עסקה-פתוחה-בעת-EOD ⇒ נסגרת-מתואמת, 0 עסקאות `FILLED` post-close.
**סיווג:** **trading-adjacent → Michael** (משנה ניהול-פוזיציה/ירי). נספח ל-`feedback_full_decision_pipeline_wiring`.
**Sierra:** ticks אחה"צ (אם היו) לתמחור-יציאה אמיתי ל-186/187.

## D18 — I-13/צ'ופ 🔴 (ראיה-להחלטת-Michael · standing-decision-gated): REACTIVE_SHORT whipsaw

**שורש (מאומת מהברים):** 3 עצירות-מלאות (ids 180/182/183, REACTIVE_SHORT, 10:45–11:15 = **−$247.50**), כולן נסחטו ב-snap-back ל-7567.25 בטווח-בוקר **14.5pt בלבד**. דילוג ⇒ בוקר **−$211.85 → +$35.65**. החתימה של **Layer-0 `chop_state=SEARCHING`** (`backend/v9/gateway/trading_gateway.py`) — **המושבת-בכוונה** (`LAYER0_CHOP_GATE` unset, default-bypass, עדיין מחושב+נרשם).
**שלב-0 (verify, Rule 2/5 — CC):** לאמת בלוג ש-`chop_state` היה **SEARCHING** בחלון 10:45–11:30 (טווח 14.5pt אמור להדליק; אם לא היה SEARCHING ⇒ "indicator-tuning" ולא "re-enable").
**ההחלטה (Michael בלבד — strategic stop):** **איני ממליץ על אף-אחת:** (א) re-enable `LAYER0_CHOP_GATE=1` (trading-risk → sign-off + restart, 06-08); או (ב) שער-חדש-נפרד **cooldown-after-2-stops** (אחרי 2 עצירות תוך M דק' → pause N דק') שאינו נוגע ב-flag-המנוטרל ונקי יותר מטווח-צר (שבו SEARCHING אולי פעיל-רוב-הבוקר וחוסם גם זכיות).
**Regression:** replay 06-19 ⇒ 180/182/183 נחסמות; וידוא ש-176/177 (ZLR-bounce זוכים) נשמרים בגרסה-ההדוקה.
**סיווג:** **trading-risk → Michael (strategic stop). אין לאפס flag ללא sign-off.**
**Sierra/לוג:** `chop_state` בחלון (CC) — חוסם-ראייה.

## D19 — I-36 🟡→trading-adjacent: שערי-כיוון מותנים ב-confidence (אל-תפעל ב-LOCKED_LOW_CONF)

**שורש (מאומת היום):** `day_type=Variation conf=0.38 LOCKED_LOW_CONF` על בוקר-טווח-צר (14.5pt). SHORT הפסיד (1W/4L) — וטו-"דלג-SHORT" היה עוזר, אך ביום-low-conf אסור לוטו-כיווני קשיח. **המערכת עצמה** סימנה ביטחון-נמוך.
**שינוי מוצע:** כל שער-כיווני (D3/TREND_GATE) קורא `gate_active = (day_type∈TREND_SET) AND (confidence ≥ CONF_MIN) AND (lock_state != LOCKED_LOW_CONF)`. `CONF_MIN` ב-YAML (~0.50). low-conf ⇒ **אל-תחסום**.
**Regression:** replay 06-19 (conf 0.38, LOCKED_LOW_CONF) ⇒ TREND_GATE **לא** פעיל; replay יום-מגמה (conf גבוה) ⇒ פעיל.
**סיווג:** **trading-adjacent → Michael.** תנאי-קדם הכרחי ל-D3 בטוח. חוזר מ-06-18.
**Sierra:** לא נדרש (confidence פנימי); תלוי בסיווג-day_type נכון (D10).

## D11 — I-34 🔴 (verify-then-fix; אם dead-wire → trading-risk): חיווט verdict ה-sizing

**שורש (מאומת, חזק):** כל ההפסדים `implied_contracts=3.0` ללא תלות ב-sizing. הבולט היום: **id180 `sizing=2` הפסיד −$105 (=7×5×3, full-3)** במקום −$70; וכל ה-S4 `"half"` הפסידו full-3 (id175/178/179). ⇒ ה-verdict (string/numeric) לא מגיע לנתיב-PnL/contracts.
**שלב-0 (verify, Rule 2):** (א) shadow-PnL נומינלי-3 בכוונה ⇒ תמים; (ב) `sizing→contracts→pnl_usd` מנותק ⇒ **trading-risk**. נתיב: `daytype_playbook.decide()`/sizer → `trading_gateway.route_setup` → `services/trade_manager/manager.py`.
**שינוי מוצע (רק אם ב'):** ב-trade_manager — `effective_contracts = base × size_factor` (`half→×0.5`, `2→2`, `3→3`, `0→SKIP`) בנקודת-חישוב-PnL+גודל, לא רק ב-metadata.
**Regression:** עסקת-`sizing=2`+stop ⇒ `pnl_usd==−(risk×5×2)` (id180 → −$70 לא −$105); `"half"` ⇒ ×0.5; `0` ⇒ לא-נפתחת.
**סיווג:** **verify-first; אם (ב) → trading-risk → Michael.** נספח ל-`feedback_full_decision_pipeline_wiring`. חוזר מ-06-16/18.
**Sierra:** לא נדרש (execution פנימי).

## D20 — I-37 🟡→trading-logic: sizing מנורמל-סיכון (R↔$)

**שורש (חלש-יותר היום):** היום ΣnetR=−3.90R **ו**-Σ$=−$211.85 — **שניהם שליליים** (אין divergence כמו 06-18) כי כל ה-stops היו צרים-ודומים (2.25–7pt). אך השורש המבני נשאר: stop-משתנה בקיבוע-3-חוזים ⇒ $/R לא-קבוע (id180 risk-7pt = −$105 מול id178 risk-2.25pt = −$33.75 — אותו −1R, $ שונה פי-3).
**שינוי מוצע:** `contracts = round(RISK_BUDGET$ / (risk_pt × 5))` (risk-per-trade קבוע, YAML) במקום 3-קבוע. משתלב עם D11+D8. מימוש memory `project_config_tunable_stop_exits_contracts`.
**Regression:** replay 06-19 ⇒ כל עסקה risk≈$RISK_BUDGET קבוע; וידוא ≤MAX_CONTRACTS.
**סיווג:** **trading-logic — Michael.** חוסם-LIVE עם D8/D11.
**Sierra:** רמות-stop (D8).

## D8 — I-3/I-13 🔴 (trading-logic → Michael): טבלת stop/target פר-תבנית×day-type — **LIVE #1**

**ללא-שינוי-מהותי.** ראיות-היום מחזקות: stop נע **2.25–7pt** באותו `MAX_RISK` אחיד. TLB-runner נחנק שוב (זכיות T1→BE זעירות: id181 +$27.5, id184 +$45). Michael מגדיר את הטבלה (memory `project_stop_target_placement_table`).
**Regression:** YAML ⇒ כל תבנית×day-type מקבלת stop/T1–T5/contracts; replay מאמת stops-ייעודיים + runner-hold.
**סיווג:** **trading-logic — Michael** (חוסם-LIVE).
**Sierra:** רמות-stop להצלבה.

## D2 — I-31 🔴 (display-safe, עדיפות-גבוהה): ספירת-ירי שקרית ~2.17×

**שורש (מאומת היום):** `S2=10 + S4=16 = 26` מול **12** ב-`v9_trades` (S2 2.0×, S4 2.29×). חוזר 5 ימים, החמיר.
**שינוי מוצע:** ב-build_status inspector — מקור-הספירה ל-`SELECT firing_system,COUNT(*) FROM v9_trades WHERE session_date=today GROUP BY 1`. הפרדת `signals_count` מ-`fired_count`.
**Regression:** היום ⇒ endpoint S2=5/S4=7; `Σfired==COUNT(v9_trades WHERE date=today)`.
**סיווג:** **display-safe — חוסם-כיול. חוזר מ-D2(06-12→18) — לא מומש.**
**Sierra:** לא נדרש (DB).

## D1 — I-22 🔴 (display-safe): `pnl_r` שבור + לא-עקבי בין נתיבים

**שורש (מאומת היום):** id181 +$27.5→`pnl_r=22` (÷$1.25, נתיב T1/BE); id176→`3.38`, id184→`4.5` (בסיס-אחר). נתיב-הפסד תקין (−1.0).
**שינוי מוצע:** ליישר כל הנתיבים (T1/BE, T2, STOP) לנוסחה אחת: `pnl_r = pnl_usd / (abs(entry−stop_initial) × 5 × contracts)`. backfill/`r_known_bad=true`.
**Regression:** עסקת-T1/BE סינתטית ⇒ `pnl_r≈0.37` לא 22; T2 זהה; STOP −1.0.
**סיווג:** **display/analytics-safe. חוזר מ-D1(06-12→18) — לא מומש.**
**Sierra:** לא נדרש.

## D12 — I-35 🟡 (display-safe): precedence ל-`metadata.sizing` type-mixed

**שורש (מאומת היום):** S4=`"half"` (string) · S2=`2`/`3` (numeric). מקור-אמת-מעורפל לגודל.
**שינוי מוצע:** נרמול ל-type אחד (factor מספרי) + precedence מתועד (DAYTYPE_PLAYBOOK "half/full" מול sizer-מספרי — מי גובר). מתחבר ל-D11.
**Regression:** כל `metadata.sizing` numeric אחיד; precedence נבדק.
**סיווג:** **display-safe** (מקדים את D11). → CC לקבוע בעלות.
**Sierra:** לא נדרש.

## D10 — 🟡 (display/diagnostic-safe): dead-wrapper day_type — **חזר היום (split)**

**שורש:** `state=Variation` (conf 0.38) מול `readiness=Trend_Normal`. ב-06-18 היו עקביים; **היום שוב מתפצלים** ⇒ הסיכון-המבני (route קורא instance-wrapper ישן) חי. ה-readiness מציג מצב-בוקר-מת.
**שינוי מוצע:** לתקן מקור-הקריאה בכל route ל-`app.state.day_type_machine`/`v9_day_type_state` (לא wrapper-instance).
**Regression:** `readiness.day_type == day_type/state.day_type == day_type של עסקה-אחרונה`.
**סיווג:** **display/diagnostic-safe.** חוזר.
**Sierra:** לא נדרש.

## D7 — I-33 🟢 (display-safe): רדאקציה toggling — **לא-פעיל היום**

**שורש (היום הפוך מ-06-18):** `session_min`/`session_date`/`rtb_session` **גלויים** היום (06-18 חסומים). ⇒ ה-redactor **מתחלף בין ימים** (matcher לא-דטרמיניסטי/תלוי-מצב).
**פעולה:** CC לאמת מה משתנה בין הימים (allowlist↔denylist? תלוי-mode/restart?). לא-חוסם.
**Regression:** `session_min` עקבי-גלוי; מפתח-סוד-אמיתי נשאר חסום.
**סיווג:** **display-safe.**
**Sierra:** לא נדרש.

## D6 — תפעולי/safe (יום-9): סוכן-ה-DIAG-30-דק' לא רץ

**שורש:** אין `PATTERN_DIAG_` מאז 06-10 (9 ימים) — ה-trigger מת. בלעדיו ה-EOD עיוור לנחסמים. **היום מצטרף ל-I-38 (feed-death) ל-D21 (shadow-no-block) = עיוורון-משולש.**
**שינוי מוצע:** (א) לבדוק scheduled-task/LaunchAgent של ה-DIAG (נעצר מתי/למה); (ב) guard-בוקר: עד 09:30 CT אין snapshot ⇒ נטיפיקציה.
**Regression:** יום-הבא ≥8 snapshots; היעדר עד 09:30 ⇒ נטיפיקציה.
**סיווג:** **תפעולי/safe. חוזר מ-D6(06-12→18) — לא טופל; חוסם 9 ימי-CF.**
**Sierra:** לא נדרש.

## D21 — 🟡 תפעולי/safe: שערי-SHADOW לא חושפים would-block

**שורש:** TREND_GATE/REACTIVE_LOCATION/RUNNER_TRAIL/DAYTYPE_PLAYBOOK — `blocked_by=null` על כל 12 ⇒ אין shadow-decision-log ⇒ אי-אפשר CF עליהם.
**שינוי מוצע:** כל שער-SHADOW כותב `would_block`/`shadow_verdict` ל-`v9_trades.metadata` (או טבלת-shadow) גם כשלא-פעיל ⇒ ה-EOD מחשב "מה היה אילו הופעל".
**Regression:** עסקה שהשער היה-חוסם מקבלת `metadata.shadow_verdict='BLOCK'`+סיבה; EOD סופר.
**סיווג:** **תפעולי/display-safe** (רישום בלבד). חוזר מ-06-18.
**Sierra:** לא נדרש.

## D9 — לאימות-CC (קטנים): I-32 · I-25

- **I-32 🟡:** gap-id **185** (טווח 175–187). חוזר 5 ימים. **חדש:** ייתכן triple-fire (185/186/187 אותו INITIATIVE_SHORT 12:00, 185 גולגל) — קשור ל-I-39/duplicate. CC לאמת מול sequence+gate-log סביב 11:55–12:00.
- **I-25 🟢:** trades `limit=200`→cap=100. enhancement; היום 12<100 ⇒ ללא-אובדן.

## D3 — I-26/I-28 🟡 (trading-logic → Michael): veto-מגמה · **מותנה-D19**

**ללא-שינוי-מהותי מ-06-18.** היום (low-conf, Variation) D3 **לא** היה נדלק (D19) — נכון. נשאר מותנה-D19 כתנאי-קדם. SHORT הפסיד היום נגד-**איזון** (לא נגד-מגמה) ⇒ הפתרון הוא שער-צ'ופ/מיקום (D18/REACTIVE_LOCATION), לא וטו-מגמה.
**סיווג:** **trading-logic — Michael.** מותנה-D19.

## D4 — I-30 🟡 (trading-logic → Michael): cluster-guard — **מותנה-רֵז'ים**

**ללא-שינוי.** היום אין cluster משמעותי (REACTIVE_SHORT 180/182/183 הם cluster-מפסיד — culling היה **עוזר** הפעם). תלוי-רֵז'ים; פעיל רק כש-D19 gate_active.
**סיווג:** **trading-logic — Michael.** משני ל-D18/D3.

---

**סדר-עדיפות מוצע ל-Michael:**
**D22 (feed-watchdog — blocker-LIVE #0, החצי-יום-העיוור; אחרי אימות-CC לסיבת-המוות)** → **D11 (verify חיווט-sizing — `sizing=2` ירה full-3)** → **D23 (reconciliation לעסקה-תקועה + duplicate)** → **D18 (צ'ופ — החלטה-שלך; אחרי אימות chop_state)** → **D19 (confidence-gate — תנאי-קדם ל-D3)** → **D8+D20 (טבלת-stops + sizing מנורמל-סיכון — חוסמי-LIVE)** → **D2** (I-31) → **D1** (I-22) → **D6+D21** (DIAG + shadow-log) → **D10/D12/D7/D9/D3/D4**.

*היום שינה את התמונה: **התשתית קודמת למסחר.** D22 (feed מת חצי-יום) הוא ה-blocker-LIVE הברור-ביותר — אסור LIVE-futures עם feed שמת-בשקט באמצע-סשן ומשאיר פוזיציה-יתומה. **D11 (sizing מת — `sizing=2` ירה full-3)** הוא ה-trading-risk-execution הברור. **D18 (צ'ופ)** הוא המנוף-המסחרי החוצה-רֵז'ים אך standing-decision-gated → החלטת-Michael בלבד, לא flag-flip. כל מסקנה-מסחרית מהיום היא בוקר-בלבד עד יום-data-מלא.*
