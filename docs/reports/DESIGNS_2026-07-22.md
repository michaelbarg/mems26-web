# MEMS26 · טיוטות-עיצוב — 2026-07-22

**מקור:** `PATTERN_EOD_2026-07-22.md` + `OPS_LOG_2026-07-22.md` + `FAULTS_AND_FIXES_2026-07-22.md` (מגה-אימות cc + אימות-cowork סבב-1/2) + `EOD_REVIEW_2026-07-22.md` + `git log` (HEAD `f5a087d5`) + counterfactual עצמאי (`cf_2026-07-22.py`, ΣR-נגד +2R).
**כלל:** עיצוב-בלבד — **לא מומש**. כל פריט: שורש · שינוי-מוצע (קובץ/שכבה) · regression-שיוכיח · **[TRADING-LOGIC → דורש-Michael]** או **[DISPLAY/SAFE]**. כל `[SoT→CC]` דורש הצלבת Sierra/DB/לוג לפני-מימוש (Rule 2/5). דגלים default-OFF, flag-guard, regression-לכל-תיקון.

**דירוג-חומרה:** **D0 (🔴🔴 בטיחות-חיה — אורפן-עירום, חוזר-4-בשבוע)** · D1 (🔴 rr_entry/lsma_flat over-block — הדליפה-של-היום +2R) · D2 (🔴 order_failed r=−1 ביצוע-live) · D3 (🔴 TPO/VA מ-ברים-מזוהמים) · D4 (🔴 offset-ברים +1h/−1h) · D5 (🟠 S2/S4 עיוורים-ל-S1, ארכי') · D6 (🔴 dev — דליפת-state בטסטים) · D7 (🟠 migration 023) · D8 (🟡 buffer-reset + הדלקת-demotion).

> **המשכיות:** D0 כאן = המשך-ישיר של `DESIGNS_2026-07-20 §D0` + `DESIGNS_2026-07-17 §D1` (I-68) — **לא-לממש-מחדש**, להצביע. D1 = המשך ה-rr_entry false-block מ-`PATTERN_EOD_2026-07-20` (I-70 family). D3/D4/D5 = רצף-הפסיקה `RULED_SEQUENCE_S1_ARMS_2026-07-22.md` (S0→S4).

---

## D0 — 🔴🔴 אורפן-עירום −10 על-כסף-אמת (I-68/S-13, **חוזר-פעם-4**) · **[TRADING-SAFETY · חוסם-LIVE · SoT→CC]**

**שורש (מ-OPS_LOG 18:07 IL + התראות-מייקל):** נתיב-כניסה יצר פוזיציית-ברוקר **−10 @7538.12 בלי-הגנה** (`working=0`), בעוד TM=0 → records≠reality; מחיר-שוק 7548.75 → **−$531 פתוח**. קדם #460 ZLR SHORT live 17:55 @7535 → STOP 7539.5 (נרשם CLOSED) — כלומר פוזיציה-שנרשמה-סגורה השאירה חשיפת-ברוקר. reconciler התריע q30s + המליץ stop @7548 אך **חסום מ-heal** (`ORPHAN_AUTO_STOP_V1=0`, אין PLACE_STOP sim-מאומת → `streak 0/3`). `trade_activity_events.jsonl`/journal **ריק ל-17:50–18:10** → הרקונסיילר מאבד את ה-POSITION_CHANGE (שורש-משנה I-81). מייקל שיטח ידנית.

**שינוי-מוצע:** זהה ל-`DESIGNS_2026-07-20 §D0` — 3 שכבות (auto-flatten על-divergence-מתמשך · `working_orders`-אמת · חסימת-entry-על-divergence) + תיקון feed-האירועים-התקוע (העדף `src=state` כש-`events.mtime>Nדק'`, Rule 2). **חדש היום:** ה-−10 (לא −3/−5) + הקדימה-של-#460-CLOSED מרמזים על **דליפת-bracket מרובת-חוזים** — לבדוק אם ה-attached-OCO של הכניסה משאיר legs לא-מקושרים בסגירה (קשור op=EXIT-broken).

**regression:** `test_naked_detected_from_state` (state טרי `qty≠0 ∧ working=0` → naked=TRUE מיד) · `test_reconciler_rejects_stale_events` (events.mtime>N → src=state) · `test_closed_trade_leaves_no_broker_qty` (סגירת-#460 → assert broker_qty==0). flag-OFF byte-identical.

**סיווג:** **[TRADING-SAFETY → דורש-Michael]** (auto-flatten/PLACE על-כסף-אמת). **[SoT→CC]:** אימות Sierra 37138283 בזמן-האורפן + שורש `phantom-heal 0/3` + feed-תקוע + מקור-ה-−10 (bracket-leak vs ידני). **חוזר-פעם-4 → הבעיה-החמורה-ביותר. חוסם-LIVE.**

---

## D1 — 🔴 `rr_entry_gate` + `lsma_flat` חוסמים כניסות-מבניות-תקפות (הדליפה-של-היום, +2R) · **[TRADING-LOGIC · SoT→CC]**

**שורש (counterfactual §3 ב-EOD, מאומת `cf_2026-07-22.py`):** 4/12 החסימות היו על **מנצחים-פוטנציאליים (+8R)**, כולן מאותה-משפחה: **`rr_entry_gate` ×3** (REACT_LONG 11:22 @7552.25 · ZLR_LONG 11:40 @7554 · ZLR_SHORT-fade 13:50 @7552.75) + **`lsma_flat` ×1** (GB100_LONG 11:10 @7548.75). כולן על **זחילת-הבוקר 7548→7562** — מהלך-איטי-אמיתי. `lsma_flat` חסם לונג בזמן-שהמחיר-עלה-ברציפות 15דק' → **LSMA לא-באמת-שטוח שם** (סף-שיפוע צר-מדי / חלון-קצר-מדי). `rr_entry_gate` חסם על **חשד-R:R-הדוק** בעוד המבנה תקף והמהלך המשיך — **המשך-ישיר של הפספוס-הבודד ב-07-20 13:25** (I-70 family). *הבחנה:* 6/12 החסימות האחרות (`daytype_playbook`/`entry_not_confirmed`/`cont_trend`) **מוצדקות** (חסכו 6R) — **אל-תיגע בהן.**

**שינוי-מוצע (שני דגלים נפרדים, default-OFF):**
1. **`LSMA_FLAT_SLOPE_TOL_V2`** — הרחב את חלון/סף-השיפוע של `lsma_flat` כך שזחילה-מונוטונית (N ברים באותו-כיוון) **לא** נספרת כ"שטוח". לצלב מול ה-LSMA-slope הגולמי מ-Sierra ב-11:10 (↻CC) — אם השיפוע היה חיובי-ברור, זה false-block-ודאי.
2. **`RR_ENTRY_STRUCTURAL_BYPASS_V1`** — כש-`rr_entry_gate` עומד-לחסום אך הכניסה על **קצה-מבני-מאומת** (swing/zone-edge, אותו probe של REV_EDGE שכבר-נבנה היום), אפשר להעריך R:R מול **היעד-המבני** במקום היעד-הצמוד. **לא לשחרר את הגייט גורף** — רק על-קצה-מבני-מאומת (מונע רגרסיה של פתיחת-שער).

**regression:** `test_lsma_not_flat_on_monotonic_climb` (11 ברי-זחילה 7548→7562 → `lsma_flat=False`) · `test_rr_entry_bypass_on_structural_edge` (ZLR_LONG 11:40 entry 7554 על-קצה → עובר; entry-אמצע-טווח בלי-קצה → עדיין-נחסם). **anti-tautology:** flag-OFF → 4 החסימות נשארות (byte-identical); flag-ON → 4 עוברות, 6 המוצדקות **עדיין-נחסמות**.

**סיווג:** **[TRADING-LOGIC → דורש-Michael]** (משנה מי-נכנס). **[SoT→CC]:** LSMA-slope raw @11:10 + entry-prices מול `v9_bars_5min_woodies` 10:45–13:00. הדליפה-הכלכלית-היחידה של-היום (+2R).

---

## D2 — 🔴 `order_failed r=−1` על כניסת-live (I-76, חדש) · **[EXECUTION · SoT→CC]**

**שורש (OPS_LOG 11:53/11:58 + P8a/P10a):** #462/#464 ZLR LONG נורו **live** → Sierra דחתה `ORDER_FAILED:-1`. P8a/P10a תיקנו **כנות-תצוגה בלבד** (`outcome_override="CANCELLED"`, `/decisions` מציג "Sierra דחתה" אדום, `gateway_decisions.jsonl` שורד-ריסטארט) — **השורש-מדוע-Sierra-דוחה פתוח.** חשד: OCO-per-contract (כל-חוזה attached-bracket מלא → אין-חוזה-חופשי / כפילות-order-id), אותה מחלקה כמו op=EXIT-broken (CLAUDE.md §op=EXIT).

**שינוי-מוצע (אבחון-לפני-תיקון, Rule 1):**
1. **↻CC לפני-כל-קוד:** לחלץ מ-Sierra-log את קוד-הדחייה המדויק של r=−1 על ENTRY (לא EXIT) — האם duplicate-order-id / no-free-contract / rejected-price. **אל-תניח.**
2. אם duplicate/OCO: להזרים order-id ייחודי פר-כניסה או להפריד bracket-attach מ-entry-submit.
3. **גישור-כנות (כבר-נבנה, לאמת):** `order_failed`≠BE≠LOSS — לוודא שלא-נספר ב-daily_pnl (P8b) וש-#462/#464 לא-מזהמים R-artifact (I-22).

**regression:** `test_order_failed_not_counted` (r=−1 → outcome=order_failed, לא-נספר) · אחרי-אבחון-CC: `test_unique_order_id_per_entry`. **סיווג:** **[EXECUTION → דורש-Michael לפני-live-retry]**. **[SoT→CC]** — אבחון-Sierra ראשון.

---

## D3 — 🔴 TPO/VA מחושב-מ-ברים-מזוהמים במקום Sierra הקנוני (I-80, חדש) · **[DATA/DISPLAY · SoT→CC]**

**שורש (commit `f5a087d5` diagnose, cowork measured):** `v9_tpo_sessions` id1523 VA=**3.5pt** מול Sierra `tpo.json` **בריא 19.5pt** (VAH7555/VAL7535.5, `va_ok=True`). ה-backend **מחשב-מחדש TPO מ-`v9_bars_5min` (המזוהמים)** ומתעלם מ-VA הקנוני של Sierra. מפר CLAUDE.md §Source-of-truth (Sierra=SoT) + Rule 1. **נפרד לחלוטין מזיהום-הברים (D4)** — cursor הציע "לבנות-מחדש-TPO-מברים-נקיים" וזה **מנגנון-שגוי** (מייקל correction, LIVE_CHANNEL 16:08).

**שינוי-מוצע (`TPO_CANONICAL_VA_V1`, default-OFF):** לקרוא VA/VAH/VAL/POC **ישירות מ-`tpo.json` הקנוני** (אותו נתיב-קריאה שבו IB/key_levels כבר-משתמשים), **לא** מחישוב-מברים. אם `tpo.json` חסר-שדה → propagate `None`/"missing" (Rule 1 — לא-לסנתז מברים).

**regression:** `test_tpo_va_matches_sierra` (id1523: `tpo.json`=19.5 → DB-VA==19.5, לא-3.5) · `test_tpo_missing_propagates_none`. **סיווג:** **[DATA/SAFE — display/levels, לא-נתיב-ירי-ישיר]** אך VA מזין location-gates → **לאמת שאינו-משנה-ירי בטעות** לפני-הדלקה. **[SoT→CC]:** id1523 מול `tpo.json` raw.

---

## D4 — 🔴 offset-ברים: `WOODIES_TS_HOUR_FIX=0` מסיר-כפילות אך משאיר −1h (I-73/I-40) · **[DATA/INGEST · SoT→CC]**

**שורש (P2 + cowork measured, LIVE_CHANNEL 16:02):** 12 זוגות-ברים +1h היום (`_hour_shift_fix` ב-`bars.py:460` תפס re-sends נורמליים בחלון ±120s; ה-TS-gate רץ *אחרי* התיקון → ראה ts-מוזז≈0). cc בנה `WOODIES_TS_HOUR_FIX=0`+gate-לפני-fix (P2) — **נכון-חלקית.** מדידת-cowork: ה-offset ה-raw = **−5h מ-wall-clock** (DLL −5h + bridge +4h = −1h; hour-fix פיצה באגי ב-+1h). לכן `=0` **מסיר-כפילות אך משאיר ברים ב-−1h**.

**שינוי-מוצע (רצף):** (1) `WOODIES_TS_HOUR_FIX=0` + gate-לפני-fix + gate-לוגג-stale-non-advancing (בנוי, P2). (2) **תיקון-offset-אמיתי בכניסה** — לזהות את מקור-ה-−1h (DLL-vs-bridge) ולתקן שם, **לא** להסתמך על =0. (3) ניקוי ghost-rows קיימים. **↻CC חובה לפני-הדלקה:** בסים, אמת שבר-טרי נוחת ל-ts-הנכון (לא −1h) — אחרת התיקון מעביר-בעיה.

**regression:** `test_ts_offset_ingest_gate.py` (8/8 בנוי) + חדש `test_fresh_bar_lands_correct_ts` (ingest בר-חי → ts==wall-clock, לא −1h). **סיווג:** **[DATA/INGEST — לא-trading-logic ישיר אך מזין הכל]**. **[SoT→CC]:** raw-export-ts מול wall-clock; sim-verify.

---

## D5 — 🟠 S2/S4 עיוורים ל-S1 בזיהוי + opening-type לא-מניע-ירי (I-79, חדש) · **[ARCHITECTURE · TRADING-LOGIC · SoT→CC]**

**שורש (triple-confirmed cursor+cowork+cc):** `day_type_at_fire` **ריק 8/8** על כל setups היום; `_detect_reactive` לוקח רק-ברים (לא-סוג-יום). `OPEN_DRIVE` סווג אך `OPENING_ENTRY_V1=shadow` → לא-מניע-live. ⇒ S1="מוח" שלא-מחובר-לידיים. **סיכון-הפוך (arch-guard, קריטי):** אם מגבילים זיהוי-על-S1 ו-S1-נתקע (כמו היום Trend_Normal) → **מעוור את-כל-הידיים.**

**שינוי-מוצע (רצף-הפסיקה S3, **smallest-arm-first**, לא-big-bang):** ארם-אחד-בכל-פעם — התחל ב-**location-aware reactive detection ב-S2** (בדיוק תקלת-"wrong-location"). **חובה-לזווג עם:** `DAYTYPE_ACCEPTANCE_DEMOTION_V1` + `BOOT_SEED_CANONICAL` **חיים** + fail-open observability (S1-נתקע → הידיים ממשיכות עם honest-degrade, לא-עיוורון). opening→live נשאר shadow עד-הוכחת-shadow.

**regression:** `test_reactive_uses_daytype_location` (REACTIVE_SHORT ביום-עולה במרכז-value → נחסם; ב-VAH-ceiling → עובר) · `test_stuck_s1_fails_open` (S1=UNKNOWN/stuck → arm-עדיין-מעריך, לא-0). **סיווג:** **[TRADING-LOGIC → דורש-Michael, per-arm]**. עיצוב-בלבד עד-פסיקה (RULED_SEQUENCE S3). **[SoT→CC]:** `day_type_at_fire` על-8-setups מול `v9_day_type_state`.

---

## D6 — 🔴 דליפת-state בטסטים מ-`_hydrate_decisions` + סיכון-פרודקשן (I-77, dev) · **[SAFE/DEV → אימות-חבילה]**

**שורש:** `_hydrate_decisions` (`trading_gateway.py:240`) טען `gateway_decisions.jsonl` האמיתי **לכל בניית-gateway כולל-בטסטים** (len16 vs 2) → 12–15 regressions (daytype_gate_live/honest_prelock/position_gate/decisions_feed/rr_graded). **סיכון-פרודקשן:** gateway-חדש בריסטארט **בולע-החלטות-קודמות**. **HEAD `b00346e9` "decisions hydrate opt-in"** נחת אחרי-NO-GO.

**שינוי-מוצע (רובו-בנוי, לאמת):** hydration **opt-in בלבד** (לא-תחת-test/ללא-app-state) + נתיב-מוזרק (לא-קובץ-אמיתי-קשיח) + `sys.modules['backend.main']` reset בין-טסטים (fixture). **↻CC:** הרץ **חבילה-מלאה** parent-parity **145/145** (לא-קובץ-בודד — הדליפה מסתתרת בחבילה) + הדבק raw (Rule 5) לפני-GO.

**regression:** `test_gateway_no_hydrate_under_test` (בניית-gateway בטסט → `len(decisions)==0`) · חבילה-ירוקה 145/145. **סיווג:** **[SAFE/DEV — בידוד-טסטים, לא-trading-logic]** אך **חוסם-return-to-live** עד-חבילה-ירוקה.

---

## D7 — 🟠 migration 023 (`pnl_sierra`) לא-אומת-רץ (I-78, חדש) · **[SAFE/OPS · SoT→CC]**

**שורש:** קובץ `backend/v9/db/migrations/versions/023_pnl_sierra_column.py` קיים + מודל `trades.py` עודכן, אך `\d v9_trades`→`pnl_sierra count=0` (cowork ~22:40) → P9c (cross-check Sierra-vs-computed) **inert**. זה בדיוק הכלי-לסגור פער-הרקונסיליאציה (Sierra +37.50 vs computed 0).

**שינוי-מוצע:** הרץ migration 023 (מקומי-Postgres, §DB-CLAUDE.md — לא-cloud); אמת עמודה קיימת; חבר את fill_poller/reconciler לכתוב `pnl_sierra` מ-`CLOSED_TRADE_PNL`. **regression:** `test_pnl_sierra_column_exists` + `test_reconcile_flags_pnl_mismatch` (computed≠sierra → flag). **סיווג:** **[SAFE/OPS]**. **[SoT→CC]:** הרץ + `\d` raw (Rule 5).

---

## D8 — 🟡 buffer-S2 reset-בריסטארט + הדלקת day-type-demotion (I-74 + I-1/I-71) · **[SAFE/HYDRATION]**

**שורש:** ~6 ריסטארטים היום → buffer-S2 מתאפס → S2 עיוור 15–20דק' פר-ריסטארט (סיבה-מרכזית ל-0-דריכה של INITIATIVE/HNS/FLAGS §2 ב-EOD). בנוסף `DAYTYPE_ACCEPTANCE_DEMOTION_V1`+`DAYTYPE_BOOT_SEED_CANONICAL_V1` (P6a/b, מתקנים day-type-stuck שגרם ל-override-ידני) **ב-RULED=1 אך לא-ב-.env** → לא-חיים.

**שינוי-מוצע:** (1) `BOOT_BUFFER_HYDRATE_V1` — hydrate buffer-S2 מ-`v9_bars_5min_woodies` בריסטארט (last-N-bars) → S2 חי-מיד. (2) **הדלקת P6a/P6b:** הוסף 3 הדגלים ל-.env (WOODIES_TS_HOUR_FIX=0, DEMOTION=1, BOOT_SEED=1) **אחרי** D6-חבילה-ירוקה → flag_guard יעבור + התיקונים חיים (per standing-rulings: כבר-RULED, בנוי-ומאומת → הדלקה-בלי-אישור-שני, רשום-מצביע ב-RULED_FLAGS).

**regression:** `test_s2_buffer_hydrated_on_boot` (post-restart → `buffer_size>0`) · `test_demotion_trend_to_variation_k3` (K=3 ברים-בתוך-IB → Trend→Normal_Variation). **סיווג:** **[SAFE/HYDRATION]** (demotion כבר-ruled) — הדלקה-אחרי-חבילה-ירוקה.

---

### סיכום-סיווג לפרומפט-CC

| # | פריט | trading-logic? | חוסם-live? | ↻CC-הצלבה |
|---|------|:---:|:---:|:---:|
| D0 | אורפן-עירום −10 | SAFETY→Michael | 🔴 כן | כן |
| D1 | rr_entry/lsma_flat over-block | כן→Michael | לא | כן |
| D2 | order_failed r=−1 | execution→Michael | 🔴 כן | כן |
| D3 | TPO/VA מ-ברים-מזוהמים | לא (data/display) | לא | כן |
| D4 | offset ברים +1h/−1h | לא (ingest) | לא | כן |
| D5 | S2/S4 עיוורים-ל-S1 | כן→Michael (per-arm) | לא | כן |
| D6 | דליפת-state טסטים | לא (dev) | 🔴 כן (עד-ירוק) | כן |
| D7 | migration 023 | לא (ops) | לא | כן |
| D8 | buffer-reset + demotion-enable | לא (demotion ruled) | לא | לא |

**מדגיש: עיצוב-בלבד — לא-מומש. שום קוד/flag/.env/DB לא-שונה בריצה זו.**
