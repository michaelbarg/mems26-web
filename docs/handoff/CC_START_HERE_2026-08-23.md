# ⚡ נקודת-התחלה — cc-macbook, סדר-ביצוע מחייב (23.08)
**מייקל: "תתחיל."** §0 חל: להכריע, לתעד, להמשיך. אל תשאל.
**ריסטארט מותר עד 16:10 IL בימי-מסחר; היום (שבת) בכל שעה.**

## הסדר — אחד אחרי השני, לא במקביל

**1 · סימולציית-דלתון (קריאה-בלבד)** → `CC_TASK_DALTON_SIM_FIRST.md`
   כולל תוספת "דלתון דינמי" (מכונת-מצבים פר-בר + מבחן-התכנסות).
   מוצר: `docs/reports/SIM_DALTON_CONTEXT_WEEK.md`. **אין קוד-ייצור בשלב הזה.**

**2 · תיקוני-האמת ליום שני** → `CC_WORKORDER_2026-08-24_MONDAY.md` §A
   **ובראשם שני פערים שהתגלו אחרי כתיבתה** (`docs/reports/UNKNOWN_GAPS_2026-08-23.md`):
   - 🔴 `S2_CVD_DETECTION_V1` **no-op מוחלט** — השאילתה משווה `str(datetime)` עם רווח
     מול עמודת-varchar עם `T` ⇒ 0 שורות ב-76/76 חלונות. שער-ה-CVD מעולם לא רץ.
   - 🔴 **11 טבלאות `ts=varchar`**, 32.8% מהשורות מחוץ לרשת-5-הדקות; ושלוש הגדרות
     סותרות ל"תאריך" (IL/UTC/ET). זו שכבת-הנתונים שכל השאר נשען עליה.
   ואז שאר §A: שלמות-Auth (RE_PULLBACK KeyError · HLST ValueError שמדכאת
   DOUBLE_BOTTOM) · חסינות-ריסטארט ל-S1 · `pnl_reconcile` · ארכוב-לוגים · absorption.

**3 · פסיקת-TREND_STEP** → לצל (הפסיקה ניתנה 23.08), + `TREND_STEP_FIX_PROPOSAL.md`
   שכבר כתבת — לצרף לו רפליי IS/OOS לכל וריאנט.

**4 · חי ליום שני (נמדדו, יש רפליי):** P3 הגדלה-מחושבת + סטופ-מבני-אחד לממוצע +
   איסור-חיזוק-בקצה · סגירת T-10.

**5 · צל מיום שני:** `S2_DELTA_DBL_V1` (גלאי-הדלתא של מייקל, Trend/Normal בלבד) ·
   קונפלואנס S2×S4 להגדלה · המסווג-הבינארי (§B) אם הספקת.

**6 · לפני שמסיימים:** `flag_guard` PASS · `fire_drill` 🟢 · `mems26_verify` ·
   שורת-`[boot] logging OK` עם ה-PID החי · דיווח חתום ב-LIVE_CHANNEL + TASK_LOG.

## כללי-ברזל
- כל שינוי-התנהגות: **רפליי לפני .env** (כלל §D). חציון-יום לא נפגע ⇒ אחרת לא נדלק.
- אפס אחוזי-ביטחון בשרשרת-ההחלטה (פסיקת-מייקל 22.08).
- אף תבנית לא מושבתת בגלל הפסדים; אפס הגבלת-שעות.
- `op=EXIT` שבור — לא לחווט. יציאות: OCO · MODIFY_STOP · FLATTEN_ACCOUNT.

---

## 2ב · תיקוני-תבניות ליום שני (דרישת-מייקל 23.08, מצילום 08-21)
מייקל מצביע על ‎$1,000+ בגרף של 21.08 שמערכות 1+2 היו צריכות לתפוס. אומת:
תחתית-כפולה ‎+$283.50@6c‎ + רגל-הירידה 18:55-21:30 (‏−25.00 נק' = ‎$750@6c‎ שורט)
= **‎$1,033**. שים לב: את הקטע השני מייקל קרא כ"דשדוש"; הנתונים אומרים **רגל-ירידה
עם ΔCVD ‎−7,205** ⇒ ההזדמנות הייתה **שורט**, לא סגירה. לתעד את ההבחנה הזו.

**להוסיף ל-§A של יום שני (כל אחד עם רפליי לפני .env, כלל §D):**
1. **DOUBLE_BOTTOM_EE — להסיר את החסימות:** ‏HLST מדכאת אותה באותו בר (‏ValueError
   נבלע) · לוודא ש-`eve_variant` לא פוסל שוקות-Adam חדות (ב-21.08: `T1 width=2 ·
   T2 width=2 ✗`). הגלאי עצמו תקין — הוא נחסם, לא נכשל.
2. **DOUBLE_TOP_AA — 0 ירי ב-34 סשנים.** מבחן-פסגת-Adam חולק את סבילות-Eve
   (0.75×ATR = 4.15pt ⇒ חציון רוחב-פסגה 11 ברים, 7/74 עוברות). לתקן לסבילות
   נפרדת + חוצץ-32; נמדד **‎+$582**. **דורש פסיקת-מייקל** — לצרף את המספר ולבקש.
3. **H&S TOP — לא להשקיע כרגע.** בבידוד ‎+$156, **בזרם ‎$0** (מפסידה את הסלוט).
   לתקן רק את סבילות-הפיבוט הכללית אם היא ממילא נוגעת בקוד; לא לרדוף אחריה.
4. **זיהוי קונים/מוכרים (`S2_DELTA_DBL_V1`) — צל בלבד.** ‏+$2,254 על 34 סשנים
   אבל **‎−$239 בשבוע שעבר עצמו**. לא להדליק על סמך מדגם.
