# MEMS26 · טיוטות-עיצוב — 2026-07-09

מקור: `PATTERN_EOD_2026-07-09.md` (EOD 15:13 CT). **קריאה-בלבד — לא-מומש.** כל design מוכן-להפיכה-לפרומפט.
מקרא: **[TRADING-LOGIC]** = משנה risk/fire/exit-surface → **דורש אישור-Michael + סטופ-אסטרטגי** · **[DISPLAY/SAFE]** = תצוגה/instrumentation, ללא-שינוי-מסחר · **[SoT→CC]** = דורש הצלבת Sierra v9_export/DB/לוג ע"י CC לפני-מימוש.

**היום שונה מ-07-08:** אתמול הבעיה הייתה חיתוך-מוקדם (I-63, +6.5R על-הרצפה); **היום הבעיה חמורה-יותר — targets שקרסו מתחת-לכניסה (I-54) יצרו phantom-fill (I-64) שהפך 3 מנצחי-ראלי להפסדים-רשומים (I-65).** ה-live (id333, +$117.5) הוכיח שהכיוון-נכון. **תיקון-השורש של ה-targets הוא תנאי-קדם ל-LIVE** — אם live יאמץ אותם targets, עסקאות-אמת ייסגרו בהפסד-מיָדי.

---

## §1 · I-54 (החמיר) — targets קרסו לקבוע-יחיד 7566.5 מתחת-לכניסה+סטופ · **[TRADING-LOGIC] · [SoT→CC]**

**שורש (נצפה):** 3 ה-LONG-shadow (328/330/332) קיבלו `t1=t2=t3=7566.5` **זהה** — נמוך-מהכניסה (7574–7576.75) **וגם נמוך-מהסטופ** (7569.5–7572.25). ל-LONG זה היפוך-כיוון (target חייב מעל-הכניסה). 334 (SHORT): `t2=7529.5 < t3=7573.25` (היפוך). מקור 7566.5 (≈7564.5 של-07-08, ≈7558 של-07-06) = **רמת-מבנה-גלובלית-קבועה** מוזרקת כ**כל** ה-targets ללא בדיקת-כיוון/סדר/מרחק. השנה: ההערך קרס לנקודה-אחת (t1=t2=t3), לא-רק "רחוק".

**הצלבה-נדרשת לפני-כל-מהלך (CC):** לחלץ את חישוב-ה-targets ב-`backend/v9/systems/five_min/five_min_system.py` (REACTIVE/INITIATIVE) + woodies-target-calc (ZLR) — **מה מזין 7566.5** (POC? VAH? settlement? swing-יומי? רמה-קבועה-בקונפיג?), ולמה כל-שלושת-ה-targets מקבלים אותו-ערך כשהוא נמוך-מהכניסה. לצלב מול Sierra v9_export אם 7566.5 הוא רמת-מבנה-קיימת (POC/VAH אתמול?).

**שינוי מוצע (`.../target_calc`):**
1. **אכיפת-כיוון (critical):** LONG → כל target > entry; SHORT → כל target < entry. target שמפר-כיוון → **פסילה + fallback ל-R-multiple** (T1=entry+1R, T2=+2R, T3=+3R). מונע "target מתחת-לכניסה ל-LONG".
2. **אכיפת-מונוטוניות:** LONG `entry<t1<t2<t3`; SHORT `entry>t1>t2>t3`. אם רמה-מבנית מפרה-סדר → clamp/השמט.
3. **תקרת-מרחק ATR-מודעת:** `|target−entry| ≤ k·ATR_daily`; רמה-רחוקה → clamp ל-ATR-cap.
4. **מקור-target ב-record:** `t1_source/t2_source/t3_source` (structural/atr/rmult) ל-CF-עתידי.
5. **fail-loud:** אם ה-target-calc מייצר t1=t2=t3 או target<entry-ל-LONG → `logger.warning` (rate-limited) + סימון-trade `target_invalid=true`, לא-סגירה-שקטה.

**Regression שיוכיח:** `test_targets_direction_monotonic_bounded` — fixture-LONG (entry 7574, stop 7569.5, ATR נתון): מוודא כל target>entry, `t1<t2<t3`, `|t−entry|≤k·ATR`, **ואין t1=t2=t3**. fixture-SHORT הפוך. Anti-regression מפורש מול 07-09 (7566.5×3 ל-LONG) + 07-08 (7564.5) + 07-06 (7558-שורטים).

**עדיפות:** 🔴🔴 — **הבעיה #1 ל-LIVE.** אם live מאמץ targets-אלה → סגירה-מיָדית-בהפסד על עסקאות-אמת. **3-החמורים · #1.**

---

## §2 · I-64 (חדש) — phantom-fill: `T3_HIT ∧ LOSS ∧ xp=null` במחיר-שלא-נסחר · **[TRADING-LOGIC/DATA] · [SoT→CC]**

**שורש (נצפה):** 328/330/332 נרשמו `t1_hit=t2_hit=t3_hit=true` + `exit_reason=T3_HIT` + `outcome=LOSS` + `exit_price=null`, כש-pnl חושב כאילו-יצאו ב-7566.5 (מתחת-לכניסה). **המחיר מעולם-לא-נסחר ב-7566.5** (שפל-חלון-אחרי-כניסה = 7573, מעל כל-הסטופים). כלומר ה-shadow-sim: (א) "זיהה" target-hit על target-מתחת-לכניסה **מיָדית** (בבר-הכניסה); (ב) הצמיד תווית-`T3_HIT` (ניצחון) ל-`outcome=LOSS` (סתירה); (ג) סגר ב-מחיר-פנטום. זו הסלמת I-58 (07-02: phantom-STOP) → עכשיו **phantom-TARGET שסוגר מנצח כהפסד**.

**הצלבה-נדרשת (CC):** לחלץ את shadow-sim fill/exit-path (`fill_poller.py` / shadow-sim-engine) — כיצד נקבע `t*_hit`: האם ההשוואה היא `bar.high>=target` **בלי** בדיקת `target>entry`? למה `T3_HIT` נורה כש-`outcome=LOSS` (שני-שדות-סותרים לא-נאכפים). לצלב `/tmp/*.log` 10:38–10:50 CT מול id 328/330/332.

**שינוי מוצע:**
1. **שער-שפיות ב-hit-detection:** target-hit ל-LONG נספר **רק** אם `target>entry` **וגם** `bar.high>=target`; ל-SHORT הפוך. target-לא-חוקי (מ-§1) → לא-מייצר-hit. (תלוי-§1: אם §1 מונע target-לא-חוקי מהמקור, §2 הוא שכבת-הגנה-שנייה.)
2. **אכיפת-עקביות תווית↔תוצאה:** `exit_reason=T3_HIT ⇒ outcome∈{WIN}` ו-`pnl_r>0`; אחרת סתירה → `logger.warning` + `outcome` מ-ה-pnl (לא מ-התווית).
3. **exit_price חובה:** סגירה עם `exit_price=null` אסורה — כל exit חייב מחיר-אמת מבר-שנסחר.
4. **instrumentation:** `exit_trigger` מפורש + `exit_price_source` (bar_high/bar_low/manual/phantom-guard) + `bars_held`.

**Regression שיוכיח:** `test_no_phantom_target_hit` — shadow-LONG (entry 7574) עם target-פגום 7566.5<entry מוזרק + ברים שכולם>7573 → הסים **לא** מסמן T3_HIT, **לא** סוגר בהפסד-פנטום; `exit_price` תמיד מבר-אמת. `test_exit_label_matches_outcome` — `T3_HIT` ⇏ `LOSS`.

**עדיפות:** 🔴 — חוסם כל-אמון-ב-shadow-sim; היום הפך +9R-פוטנציאל ל-−6.77R-רשום. **3-החמורים · #2.**

---

## §3 · I-65 (חדש) — היפוך shadow↔live על סיגנל-זהה · **[SoT→CC] → [TRADING-LOGIC-shadow]**

**שורש (נצפה):** 332(shadow) ו-333(live) = **אותו REACTIVE_LONG** (10:45, entry 7576.75, stop 7572.25). אך targets-שונים: 332 קיבל `7566.5×3` (§1) ונסגר `T3_HIT`-פנטום @10:50 (−2.28R, §2); 333 קיבל `t1=t2=7588.5, t3=7566.5` והוחזק ידנית ל-12:11 → **+3.92R ($117.5) כסף-אמת**. ⇒ ה-shadow-oracle **מְהַפֵּךְ-סימן** ביחס-ל-live על אותו-קלט. הסלמת I-56 (07-01: shadow↔demo divergence) — עכשיו shadow↔live, עם עלות-הוכחה כספית.

**הצלבה-נדרשת (CC):** למה שני-נתיבים (shadow/live) של **אותו-סיגנל** מקבלים targets-שונים? האם ה-target-calc רץ-פעמיים עם קלט-שונה (timing/snapshot-שונה), או ה-live עבר manual-override? לצלב לוג 10:45 CT id332 מול id333 (target-assignment) + 12:11 (exit).

**שינוי מוצע:**
1. **target-parity:** shadow ו-live של אותו-signal-key **חייבים targets-זהים** (חישוב-אחד, מוזן-לשניהם). אם ה-live קיבל 7588.5 וה-shadow 7566.5 — זה באג-מקור (§1) + חוסר-שיתוף-חישוב.
2. **תלוי-§1+§2:** משתקם-אוטומטית אם ה-targets נכונים ואחידים; §3 בעיקר **אימות** שהתיקון תופס בשני-הנתיבים.
3. **instrumentation:** `signal_key` משותף ל-shadow+live של אותה-כניסה, כדי שכל-EOD יוכל להשוות ישירות (במקום ניחוש-לפי-זמן/מחיר).

**Regression שיוכיח:** `test_shadow_live_target_parity` — signal-אחד מזרים shadow+live → שניהם מקבלים targets זהים ו-outcome עקבי (עד-כדי exit-discretion-ידני מתועד). Anti-regression מול 332/333.

**עדיפות:** 🔴 — ה-shadow הוא ה-oracle לפני-LIVE; אם הוא מהפך-סימן מול live, הוא חסר-ערך (גרוע-מ-07-08). **3-החמורים · #3 (מאחד §1+§2).**

---

## §4 · I-62 — DEMO כבוי / LIVE פעיל (id333 +$117.5) · **[TRADING-LOGIC] · [SoT→CC]**

**שורש (נצפה):** `demo_enabled_systems=[]` (מ-07-06) + `live_enabled_systems=[2,4]` ירתה **id333 (S2 REACTIVE_LONG, +$117.5, +3.92R, נוהלה ידנית ע"י Michael)**. live-WIN גדול-מ-07-08 (id310 +$22.5). מחזק promotion-מכוון-pre-LIVE.

**הצלבה-נדרשת (CC):** `git log`/`.env`/`FLAG_INDEX.md`/`FLAG_REGISTRY.yaml` — לאשר `demo=[]`+`live=[2,4]` = החלטת-Michael מתועדת, מתי/איזה-commit.

**שינוי מוצע (מותנה):**
- אם **מכוון** (הסביר) → **לא-לגעת.** לתעד ב-`FLAG_INDEX.md`+`SOURCE_OF_TRUTH.md`: "demo כבוי-במכוון מ-07-06, S2/S4=live-enabled, LIVE=הנתיב-הקובע" + **לעדכן סוכן-EOD** להצליב live-fills כנתיב-הקובע. **[DISPLAY/SAFE]**
- אם **שוגג** → החזרת `demo_enabled_systems`. **[TRADING-LOGIC] — Michael בלבד.**
- **⚠️ תלות ב-§1/§2:** כל-עוד ה-targets שבורים (I-54/I-64), **live בסיכון-ממשי** — id333 ניצל רק-כי Michael התעלם מ-t3=7566.5 והחזיק ידנית. עסקה-אוטומטית-לייב עם targets-אלה הייתה-נסגרת-בהפסד-מיָדי. **⇒ §1/§2 חוסמים-הרחבת-live-אוטומטי.**

**Regression שיוכיח:** `test_enabled_systems_matches_intended` — טוען gateway-config, מוודא `demo_enabled`/`live_enabled` תואמים ערך-מוצהר ב-`FLAG_REGISTRY.yaml`; `--check` נכשל על drift-לא-מתועד.

**עדיפות:** 🟡 (כוונה) → **🔴 בהצטלבות-עם-§1** (live+targets-שבורים = סיכון-כספי).

---

## §5 · I-66 (חדש) — 10 דרילי-דמו `SIM_TEST` בטבלת-עסקאות · **[DISPLAY/SAFE]**

**שורש (נצפה):** ids 316,318,320–327 (03:32–05:28 CT, טרום-RTH): `pattern=SIM_TEST`, entry אחיד 7515.25, `exit_reason` ∈ {sim_cleanup, sim_drill_complete, drill_exit_test, phase0_test, pre_restart_cleanup, drill_complete, drill_cleanup, stale_cleanup}, R-אבסורדי (321/323 R48; 318 +$541.95). דרילי-בדיקה pre-LIVE — לגיטימיים כתרגול, אך **מזהמים `/trades/recent`** ועלולים לזלוג ל-counters/דוחות.

**הצלבה (CC):** לבדוק אם השדה `is_synthetic` (קיים ב-trade-schema) מסומן `true` לדרילים; ומה מקור ה-restart (`pre_restart_cleanup`) שהוריד `bars_in_db` 3206→1549.

**שינוי מוצע:**
1. **סימון+סינון:** דרילי-`SIM_TEST` מקבלים `is_synthetic=true` (או `is_drill=true`); כל-endpoint/counter/סוכן-EOD מסנן אותם כברירת-מחדל (פרמטר `include_drills=false`).
2. **סוכן-EOD:** להוסיף מסנן `pattern!=SIM_TEST && !is_synthetic` בשליפת-הסיגנלים (מומש-היום ידנית; לקבע במפרט).
3. **בידוד-סביבה (אופציונלי):** דרילים לטבלה/namespace נפרד, לא-מעורבב ב-`v9_trades` החי.

**Regression:** `test_drills_excluded_from_counters` — הזרקת SIM_TEST → `trades_today`/EOD-count לא-כוללים; `include_drills=true` חושף אותם.

**עדיפות:** 🟡 — safe, אך חשוב לפני שה-counters/דוחות נסמכים על-הטבלה pre-LIVE.

---

## §6 · chop_state="FOUND" (יום-2) — מיפוי-enum חשוד · **[DISPLAY/SAFE] · [SoT→CC]**

**שורש:** `/gateway.chop_state`+`/chop_score.state`="FOUND" (score=4), **יומיים-ברציפות** (07-08+07-09). ערך לא-מוכר (קודם EXPANDING/SEARCHING/CONTRACTING). רציפות ⇒ פחות-artifact-חד-פעמי, יותר enum-חדש/שגוי.

**הצלבה (CC):** Layer-0 chop-composite (`trading_gateway.py`/chop-module) — אֶנוּם-המצבים; האם "FOUND" לגיטימי-חדש או באג-מיפוי score→state.

**שינוי מוצע:** אם באג → תיקון-מיפוי; אם חדש → תיעוד ב-`FLAG_INDEX.md`/מקרא-chop. chop-off ⇒ display-only.
**Regression:** `test_chop_state_enum_valid` — `state ∈ {מורשים}`; "FOUND" נכשל אם-לא-מתועד.
**עדיפות:** 🟡 watch — לא-חוסם (chop-off).

---

## §7 · נמשכים (עיצובים-עומדים, ללא-שינוי) — I-44 · I-23 · I-22 · bars_in_db · limit

- **🔴 I-44 (day_type wrapper-קפוא) — [DISPLAY/SAFE · SoT→CC]:** `session_min=0/vote_history=[]`. עיצוב עומד מ-`CC_DAYTYPE_SOURCE_CONSISTENCY_2026-06-30.md` (איחוד-endpoint למקור-החי + דגל-`stale`). `test_daytype_endpoint_live_source`.
- **🟡 I-23 (gateway counters=0) — [DISPLAY/SAFE]:** לחווט `trades_today`/`daily_pnl`/`shadow_active_count` (פילוח live/shadow, **החרגת-drills §5**). `test_gateway_counts_fires`. עומד מ-06-09.
- **🟡 I-22 (pnl_r id333) — [SoT→CC]:** 3.92R מול 2.61R מחושב-מ-stop-המוצג (7572.25). לצלב `stop_initial` (אולי 1R=3pt→3.92R). לא-חמור (WIN אמיתי), אך שווה-אימות-R.
- **🟡 bars_in_db 3206→1549 (watch) — [SoT→CC]:** חשד DB-reset/restart טרום-RTH (תואם `pre_restart_cleanup`, §5). לצלב `/tmp/*.log` boot + git/מיגרציה.
- **🟢 limit — תחזוקת-מפרט:** `/chart/bars5min?limit=200` התקבל היום; `/trades/recent` נותר `le:100`. מפרט-סוכן: limit=100 ל-trades בלבד.

---

## סיכום-עדיפויות

| # | פריט | class | דחיפות | חוסם-מימוש |
|---|------|-------|--------|-----------|
| §1 | I-54 targets קרסו מתחת-לכניסה (7566.5×3) | TRADING | 🔴🔴 | אישור-Michael + SoT (מקור 7566.5) |
| §2 | I-64 phantom-fill `T3_HIT∧LOSS∧xp=null` | TRADING/DATA | 🔴 | SoT (hit-detection path) |
| §3 | I-65 היפוך shadow↔live (332/333) | SoT→shadow | 🔴 | תלוי-§1+§2 |
| §4 | I-62 demo-off/live-active (id333 +$117.5) | TRADING/SoT | 🟡→🔴 עם-§1 | אישור-Michael (כוונה) |
| §5 | I-66 דרילי SIM_TEST בטבלה | DISPLAY/SAFE | 🟡 | — (safe) |
| §6 | chop_state="FOUND" (יום-2) | DISPLAY/SAFE | 🟡 | SoT (enum) |
| §7 | I-44 · I-23 · I-22 · bars_in_db · limit | DISPLAY/SAFE | 🔴-בלבול…🟢 | handoff/SoT |

**3 החמורים (לנוטיפיקציית-Michael):** §1 I-54 (targets קרסו ל-7566.5 מתחת-לכניסה+סטופ — **הבעיה #1 ל-LIVE**) · §2 I-64 (phantom-fill הפך מנצחים ל-`T3_HIT`-הפסד) · §3 I-65 (shadow −2.28R מול live +3.92R על סיגנל-זהה). **כל-שלושתם שורש-אחד: ה-target-engine + shadow-exit-path שבורים** — ותיקונם **תנאי-קדם ל-LIVE**.
**מיָדית-safe (ללא-אישור-trading):** §5 (drills) · §6 (chop-enum) · §7 (I-23/I-44-display/limit). **§1/§2/§3/§4 דורשים SoT/אישור-Michael.**

**מקור-אמת → CC (Rule 2/5):** מקור-הערך 7566.5 (Sierra v9_export — POC/VAH/settlement?) · shadow phantom-`T3_HIT` path · targets-diff 332↔333 · git/.env/FLAG ל-I-62 · is_synthetic ל-drills · chop-enum · R-חישוב id333. **שום קוד/flag/.env/DB לא-שונה בריצה זו.**
