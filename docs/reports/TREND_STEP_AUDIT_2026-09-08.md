# TREND_STEP — למה הוא בצל, האם הוא עובד, ומה קרה היום (08.09.2026)

**השאלה (מייקל, 08.09 18:05):** *"למה זה מעולם לא הודלק? אולי זה לא עובד נכון"* — עם הקריטריון:
**מבנה-נר ונפח יחד עם המחיר.** התיק-המבחן הוא הטייפ של היום: ירידה → מדרגה ב-16:45 →
מדרגה ב-16:55 → ירידה נוספת עד 7680 ב-17:10.

**התשובה בשורה אחת:** הגלאי **לא ראה אף אחת משתי המדרגות של היום**, וההסבר אינו נפח ואינו
מבנה-נר — אלא פרמטר יחיד, `ZZ_REV=5.0`, **שמעולם לא נסרק באף מדידה**. הוא קטן מטווח-הבר
הממוצע של היום (8.75 נק'), ולכן כל בר בודד מספיק כדי להכריז היפוך-סווינג.

---

## 1 · למה הוא בצל — שובל-הנייר

**הפסיקה** — `config/RULED_FLAGS.yaml:301`, מייקל 23.08:

> *"מאשר להעביר לצל ולהציע תיקון לאיך היא הופכת טובה יותר"* — **מבטל את פסיקת-ההדלקה 14.08.**
> הראיה: IS +$4,354 מול OOS −$2,011; מאז-הדלקה −$198 בספרים; הוצאתה מהסלוט +$1,332 (OOS +$1,011).

**המדידה שמאחוריה** — `docs/reports/GOOD_PATTERNS_FIX_2026-08-22.md` (T-93), 34 סשנים:

| מדד | ערך | מקור |
|---|---|---|
| IS (חלון-הכיול 07-15→08-12, 21 סשנים) | **+$4,353.50** | §7 |
| OOS (07-07→07-14 + 08-13→08-21, 13 סשנים) | **−$2,010.50** | §7 |
| בספרים מאז ההדלקה 08-14 | **−$197.50** · 4 עסקאות-לייב · W/L **0/3** | §3א שורה 90 |
| בצל באותו חלון | **−$810.00** · 12 עסקאות | §3א שורה 90 |
| ‏leave-one-out — הוצאתו מהסלוט (X1) | **+$834.37** (IS +$100.49 · **OOS +$733.88**) | §6ב |
| ‏X4 = X1 + כלל-עם-מגמת-היום | **+$1,331.99** (OOS +$1,011) | §6ב |

⇒ הוא הופל **בצדק ועל מספר**: הוא היה היחיד מבין שישה יצרנים שהוצאתו-מהסלוט חיובית
**בשני החצאים** של הפיצול IS/OOS.

### 🔴 הממצא של הסעיף הזה: התיקון שהובטח מעולם לא נמדד

`docs/reports/TREND_STEP_FIX_PROPOSAL.md` (24.08, cc-macbook) הגדיר תוכנית-מדידה מפורשת —
‏**V0** בסיס · **V1** ספים יחסיים-ל-ATR · **V2** V1 בלי STAIR_OR · **V3** V1 + סינון-בליעת-דלתא ·
**V4** כניסת-ריטסט — כל אחת "full replay with IS/OOS split".

**אף אחת מהן לא רצה, 15 יום:**

- אין קובץ-דוח: `ls docs/reports/ | grep -i trend_step` ⇒ רק `FORENSIC_..._2026-08-14.md`
  ו-`TREND_STEP_FIX_PROPOSAL.md` עצמו.
- אין דריסת-פרמטר בשום סקריפט: `grep -rn "TSE_ZZ_REV\|TSE_RETR\|TSE_IMP_MIN" --include=*.py .` ⇒ **0 שורות**
  (המנגנון קיים — `detector.py:64-73` קורא `TSE_<PARAM>` — ואיש לא השתמש בו).
- אין שורה ב-`docs/reports/MEASUREMENTS.md`.

⇒ **הצל אינו הכרעה — הוא המתנה למדידה שלא בוצעה.** זו התשובה הישירה ל-*"למה מעולם לא הודלק"*.

### האחים — מה הם עושים, והאם הצל מנטרל אותם

| דגל | מצב | מה הוא עושה | האם הצל מנטרל |
|---|---|---|---|
| `TREND_STEP_STAIR_OR_V1=1` | ✅ ON | `detector.py:61` → נצרך ב-`:184`: מדרגה-מאומתת (שפל **וגם** שיא נמוכים-יותר מזוג-הסוינג הקודם) מחליפה את דרישת קיצון-הסשן | **לא — פעיל לחלוטין.** הוא יושב בתוך `detect_trend_step`, ו-`enabled()` (`:47`) מקבל `"shadow"` ⇒ הוא משנה **אילו מועמדים בכלל נולדים**. היום הוא עבד: ב-16:50 `stair=True` |
| `TREND_STEP_STRUCT_EXEMPT_V1=1` | ✅ ON | `trading_gateway.py:3113` — פטור מווטו A1 `structural_targets_wrong_side`, בתוך ענף `all_wrong_side` בלבד | **כן, בפועל.** הוא **רץ וכותב WARNING**, אך הסטאפ נופל אחריו ב-`:3992` — `[Gateway] shadow_only setup (%s) — recorded, not routed` ⇒ הפטור לא יכול להוליד עסקה חיה. **מריץ-קוד, אפס-אפקט** |

---

## 2 · המכניקה — במילים פשוטות

`backend/v9/systems/trend_step/detector.py` (360 שורות, מודול יחיד).

הגלאי בונה **זיגזג קוזאלי** על ברי-5-דק' סגורים עם סף-היפוך `ZZ_REV=5.0` נק' (`:76-130`), ומחפש
זוג-פיבוטים סמוכים: מקור (`org`) וקיצון (`ext`). המרחק ביניהם הוא ה**אימפולס**, שחייב להיות
‏**8–45 נק' בתוך ≤10 ברים** (`:162-166`). הקיצון חייב להיות **קיצון-הסשן** — אלא אם
`TREND_STEP_STAIR_OR_V1` והמדרגה **מאומתת** (שפל-נמוך-יותר וגם שיא-נמוך-יותר מזוג-הסוינג
הקודם), שאז המדרגה מחליפה את הדרישה (`:168-192`). ה**פאוזה** היא מספר הברים שחלפו מאז בר-הקיצון:
חייב להיות **1–3** (`:194-196`), והמחיר בתוכה חייב לתקן **20%–55% מהאימפולס** (`:197-204`).
‏**LSMA** חייב להיות בשיפוע מסכים (`|slope| ≥ 0.15` על 3 ברים, `:206-213`), ו**נפח-הפאוזה** חייב
להיות ‏≤1.10× נפח-האימפולס (`:220-224`). אם כל אלה עברו — **הכניסה היא הסגירה של בר-הפאוזה**
(`entry = bars[i]["c"]`, `:235`), הסטופ הוא **קיצון-הפאוזה + 10% מהאימפולס**, מהודק ל-2.5–9.0 נק'
(`:236-243`), והיעדים הם **0.45 / 0.80 / 1.30 × האימפולס** (`:244-248`). חתך-שעה 15:00 ET (`:143`).
הסטאפ נושא `stop_source="TREND_STEP_LEG"` כדי ש-F3 לא ידרוס את הסולם (`:343`), ו-`step_id`
יציב כדי שמדרגה אחת תיתן כניסה אחת ולא אחת-לכל-בר-ששרד (`:266`).

**נקודת-המפתח למי שקורא את זה כדי לענות למייקל:** הכניסה היא **בתוך הפאוזה**, לא אחרי חידוש
התנועה. תיאור-המדרגה של מייקל הוא בן שלושה שלבים — יורד · בונה מדרגה · **יורד שוב**. הגלאי
רואה רק שניים ומהמר על השלישי. אין בו שום בר-אישור.

---

## 3 · הקריטריון של מייקל — נפח ומבנה-נר

### נפח — קיים, אבל חצי ממנו וכמעט-אינרטי

```python
# detector.py:220-224
iv = [bars[j].get("v", 0) for j in range(org_i + 1, ext_i + 1)]   # ברי האימפולס
pv = [bars[j].get("v", 0) for j in range(ext_i + 1, i + 1)]       # ברי הפאוזה
if iv and pv and sum(iv):
    if (sum(pv) / len(pv)) / (sum(iv) / len(iv)) > p["VOL_RATIO_MAX"]:   # 1.10
        continue
```

- ✅ **כן** — יש השוואה של נפח-המדרגה לנפח-רגל-האימפולס. זהו החצי הראשון של דלתון
  ("פאוזה על נפח נמוך").
- ❌ **אבל הסף הוא 1.10, כלומר הפאוזה מותרת להיות רועשת ב-10% מהאימפולס.** זהו פוסל-פיצוץ,
  לא דורש-שקט. **המדידה:** במפקד התנאי-הראשון-שנכשל על **2,347 הערכות-בר** ב-34 סשנים
  (`GOOD_PATTERNS_FIX_2026-08-22.md` §5), `vol_ratio` היה החוסם-הראשון **6 פעמים = 0.26%**
  — מול `impulse_size` 638 ו-`retracement` 169. הוא כמעט לא נוגע בכלום.
- ❌ **ואין שום מבחן-התפשטות.** הכניסה היא סגירת בר-הפאוזה (`:235`) — **בר-ההמשך עדיין לא
  קיים**. המדרגה של דלתון היא *פאוזה על נפח נמוך* **ואז** *התפשטות על נפח גבוה*; הגלאי מודד
  את החצי הראשון ואינו יכול, מבנית, למדוד את השני.

**היום זה היה מודגם בדיוק:** בר-הפאוזה 16:55 נפח 20,381 = **0.895×** ממוצע-האימפולס (22,764) —
פאוזה שקטה, דלתונית לתפארת — ובר-ההמשך 17:00 נפח **27,203 = 1.33× מהפאוזה**, התפשטות נקייה.
**חתימת-הנפח של דלתון הייתה שם במלואה. הגלאי אישר את חציה ולא ראה את חציה, וממילא נפל
על הגיאומטריה לפני-כן.**

### מבנה-נר — כמעט ואינו קיים

- 🔴 **הגלאי לעולם אינו קורא את מחיר-הפתיחה.** מפקד-שדות על `detect_trend_step` (`:133-271`):
  הוא נוגע ב-`h`, `l`, `c`, `hhmm` (וכן `v`, `lsma` דרך `.get`). `live_bars` **כן** טוען `o`
  (`:309`) — **ואיש אינו צורך אותו.** ⇒ **יחס גוף/טווח אינו בר-חישוב בקוד הזה בכלל.**
- 🔴 אין מבחן מיקום-סגירה בתוך הבר.
- 🟠 מבחן-הנר היחיד שקיים הוא `REQUIRE_EXHAUST` (`:226-233`) — לשורט: `h[i] <= h[i-1]` **וגם**
  `c[i] <= c[i-1]` — והוא **ברירת-מחדל 0 = כבוי** (`:34`).
- ✅ מה **כן** קיים: רצף שיאים-יורדים/שפלים-יורדים — אבל על **פיבוטי-זיגזג**, לא על נרות
  (`stair`, `:168-172`).

**⇒ התשובה הישירה ל-*"אולי זה לא עובד נכון"*:** הגלאי אינו עיוור-לנפח, אבל הוא **חצי-רואה**
אותו (פאוזה בלבד, בסף שאינו מסנן), והוא **עיוור-לנר** (אינו קורא פתיחה כלל). ובכל זאת — ראה §4 —
**לא זה מה שהפיל אותו היום.**

---

## 4 · היום, בר-אחר-בר

הגלאי הורץ על הטייפ של היום דרך **הקוד שלו עצמו** (`detect_trend_step`, פרמטרים חיים מ-`_p()`,
‏`TREND_STEP_STAIR_OR_V1=1`). ‏`hhmm` הומר ל-ET כפי ש-`live_bars` עושה.

| שעה IL | ‏ET | ורדיקט (SHORT) |
|---|---|---|
| 16:30–16:45 | 09:30–09:45 | **לא נבדק** — `i < 4` (`:141`), צריך ≥5 ברי-RTH. **המדרגה הראשונה של מייקל נופלת כאן** |
| 16:50 | 09:50 | ❌ `PAUSE_BARS 0` (מותר 1–3) — אימפולס 10.25 תקין, `stair=True` |
| **16:55** | **09:55** | ❌ **`RETRACEMENT 124.4%`** (מותר 20–55%) · אימפולס 10.25 (7698.75→7688.50) · פאוזה 1 בר עד 7701.25 · **`vol_ratio 0.961` היה עובר** |
| 17:00 | 10:00 | ❌ `PAUSE_BARS 0` — אימפולס 15.50 |
| 17:05 | 10:05 | ❌ `PAUSE_BARS 0` — אימפולס 17.25 |
| 17:10 | 10:10 | ❌ `PAUSE_BARS 0` — אימפולס 21.25 |

**אומת מול הלוג החי** (`docs/reports/OPS_LOG_2026-09-08.md`) — אפס מועמדי-TREND_STEP עד 17:10;
המועמד הראשון של היום הגיע **רק ב-10:20:06 ET (17:20 IL)**:

```
[2026-09-08T10:20:06-04:00] [gateway] [WARN] BLOCKED TREND_STEP SHORT @7685.75 gate=entry_not_confirmed (sys4)
[2026-09-08T11:05:07-04:00] [gateway] [INFO] FIRED[shadow_only] TREND_STEP LONG @7697.25 trade=True (sys4)
```

⇒ **הוא זיהה את המדרגה 25 דקות מאוחר מדי ו-9 נקודות גרוע יותר, ואז נחסם.**

### השורש: `ZZ_REV=5.0` קטן מרעש-הבר של היום

טווחי הברים היום: `10.75, 7.00, 10.75, 6.50, 9.25, 8.00, 11.25, 7.00, 8.25` ⇒ **ממוצע 8.75 נק'**.

> **‏9 מתוך 9 הברים היום היו בעלי טווח ≥ 6.50 נק' — כלומר גדול מסף-היפוך-הסווינג עצמו (5.00).**
> ‏`ZZ_REV` הוא **0.57×** מטווח-הבר הממוצע. **הרעש של בר בודד מספיק כדי להכריז היפוך.**

התוצאה המכנית: הזיגזג הופך כל אחת מהמדרגות של מייקל ל**פיבוט**, וכך מקצץ את הרגל האמיתית
‏(7717.75 → 7688.50 = **29.25 נק'**) לגדמים של 10.25 / 15.50 / 17.25 נק'. וכשהאימפולס הוא גדם
בן בר אחד, ריבאונד של 12.75 נק' הוא **124% ממנו** — ולכן "פאוזה" נקראת "היפוך".

### הנגד-עובדתי: שינוי יחיד, `ZZ_REV` 5.0 → 8.0 (כל השאר בסיס)

| | ערך |
|---|---|
| כניסה | **SHORT 7694.75** בסגירת בר-16:55 |
| סטופ | **7703.75** (‏9.00 נק' — קיצון-פאוזה 7701.25 + 10%×29.25 = 2.93, מהודק ב-`STOP_MAX`) |
| יעדים | ‏T1 **7681.50** · T2 7671.25 · T3 7656.75 |
| אימפולס · ריטרייס · נפח | ‏**29.25 נק'** · **43.6%** · `vol_ratio` **0.895** — כולם בתוך הבסיס |

מהלך קדימה על הברים (סטופ קודם ליעד באותו בר — לא נדרש כאן, אין התנגשות):

| בר | ‏H | ‏L | סטופ 7703.75 | ‏T1 7681.50 |
|---|---|---|---|---|
| 17:00 | 7697.00 | 7685.75 | לא | לא |
| 17:05 | 7691.00 | 7684.00 | לא | לא |
| 17:10 | 7688.25 | 7680.00 | **לא** | ✅ **נפגע** |

- **‏T1 נבנק ב-17:10:** `7694.75 − 7681.50 = +13.25 נק'` — **חוזה 1 מתוך 2 ⇒ +$66.25**.
- **ראנר ל-BE אחרי T1** (פסיקה עומדת); ב-17:55 המחיר 7698.75 מעל 7694.75 ⇒ **$0.00**.
- **סה"כ @2 חוזים ≈ +$66.25.** מארק-טו-מרקט בסגירת 17:10 (7685.00) ללא ניהול:
  ‏`+9.75 נק' × 2 × $5 = +$97.50`.
- בדיקת-סימן: שורט שנעצר היה נותן `7694.75 − 7703.75 = −9.00 נק'` — **שלילי**, כנדרש.

### מול מה שהמערכת עשתה בפועל — והמפריד אינו הכניסה, הוא הסטופ

```
[09:55:09] FIRED[live] INITIATIVE_SHORT SHORT @7694.0 trade=1224 (sys2)
[09:55:11] ENTRY-FILL #1224 SHORT @7694.0 mode=live stop=7699.25
[09:56:01] STOP-HIT  #1224 @7699.0 outcome=LOSS pnl_usd=-50.0
[10:19:47] FIRED[live] INITIATIVE_SHORT SHORT @7684.0 trade=1231 (sys2)
[10:19:49] ENTRY-FILL #1231 SHORT @7684.0 mode=live stop=7696.75
[10:50:49] STOP-HIT  #1231 @7695.25 outcome=LOSS pnl_usd=-103.75
```

‏**#1224 נכנס במחיר כמעט-זהה** (7694.00 מול 7694.75 של המודל) **ובאותו כיוון** — ומת אחרי
**52 שניות**, כי הסטופ שלו היה **5.25 נק'** והמחיר עלה ל-7699.00.

**קיצון-הפאוזה של הבר הזה הוא 7701.25** — בדיוק הפרינט שהרג את #1224. מודל-הרגל של
TREND_STEP מציב את הסטופ **מעל** קיצון-הפאוזה מעצם-הבנייה (`pause_ext + 10%×imp`) ⇒ **7703.75,
‏2.50 נק' מעל הפרינט הקטלני**. ‏S2 הציב אותו **2.00 נק' מתחתיו**.

⇒ **שתי המערכות ראו את אותה עסקה. אחת מהן ידעה איפה הסטופ צריך לשבת, ולא הצליחה לזהות
את התבנית; השנייה זיהתה, ונשחטה על הסטופ.** זוג-הלייב של היום: **−$153.75**.

---

## 5 · ורדיקט

### **(ב) לא-מכויל — והתנאי שדוחה את המדרגות האמיתיות הוא `ZZ_REV`.**

**המספר:** `ZZ_REV=5.0` מול טווח-בר ממוצע **8.75 נק'** היום, כאשר **9 מ-9 הברים** בעלי טווח
מעל הסף. שינוי יחיד ל-8.0 מייצר, על אותו טייפ בדיוק, כניסה **SHORT 7694.75** שהסטופ שלה
‏(7703.75) לא נגוע ו-T1 שלה נפגע ב-17:10 ⇒ **+$66.25 @2 חוזים** (מארק +$97.50), מול **−$153.75**
של שתי עסקאות-הלייב.

**וזו גם החומרה של הממצא:** `ZZ_REV` **אינו מופיע באף אחת משבע השורות של מפקד-התנאים**
ב-`GOOD_PATTERNS_FIX_2026-08-22.md` §5 (`not_session_extreme` · `impulse_size` · `pause_bars` ·
`cutoff` · `retracement` · `lsma_slope` · `vol_ratio`), ואינו מופיע גם ברשימת V1 של
`TREND_STEP_FIX_PROPOSAL.md`. ⇒ **הפרמטר שקובע מהי "רגל" ומהי "פאוזה" מעולם לא נבדק** —
כל המדידות ההיסטוריות רצו על זיגזג יחיד ולא-מוטל-בספק.

**⚠️ הסתייגות מוצהרת, והיא מהותית:** `ZZ_REV=8.0` נגזר **מהטייפ של היום**, `n=1`. זו **בדיוק
החטא** שהפיל את הדגל מלכתחילה (IS +$4,354 מול OOS −$2,011). ‏**אין להדליק על סמך הדוח הזה.**

### הצעד הבא — לפי סדר

1. **להפוך את `ZZ_REV` ליחסי** (`k × ATR14`, לפי העיקרון הקבוע של מייקל "בלי נקודה קבועה"),
   ולהוסיף לו שורה במפקד-התנאים. ‏`detector.py:64-73` כבר תומך ב-`TSE_ZZ_REV` ⇒ אפס-קוד לסריקה.
2. **להריץ סוף-סוף את V0–V4** מ-`TREND_STEP_FIX_PROPOSAL.md` (24.08) **בתוספת סריקת-`ZZ_REV`**,
   פיצול IS/OOS מלא, על מנוע קיים (`scripts/replay_trend_step_entry.py`).
3. רק אם OOS חיובי — פסיקת-מייקל להחזרה ללייב. עד אז **שדואו, וזו החלטה נכונה.**

**בלי פסיקה, מותר מיד:** להוסיף למטא-דאטה של כל מועמד-צל את `vol_expansion_next_bar`
(נפח בר-ההמשך / נפח-הפאוזה) — **מדידה בלבד** — כדי שהחצי החסר של חתימת-דלתון יהיה
בר-מדידה בפעם הבאה שמייקל שואל. היום הוא היה **1.33**.

---

### נספח — מגבלת-מדידה מוצהרת (Rule 1: כישלון-כן > ערך-סינתטי)

**‏`v9_trades` לא נשאל בדוח הזה.** ה-Postgres המקומי יושב על ה-Mac; סביבת-הריצה של הסוכן
היא סנדבוקס Linux נפרד — `/dev/tcp/localhost/5432` ⇒ `Connection refused`, ו-`psycopg2`/
`sqlalchemy` אינם מותקנים בו. ‏`data/mems26_local.db` (מראה-SQLite, 28.08) מכיל 405 שורות
מעידן-SQLite ו-`v9_bars_5min_woodies` בו מחזיר `database disk image is malformed`.
**לכן `n` של אוכלוסיית-הצל לא נספר כאן מחדש** — כל מספרי-האוכלוסייה בדוח מצוטטים מ-T-93
‏(34 סשנים, 2,347 הערכות-בר, 4 לייב + 12 צל) ומהלוג של היום. הריצה של §4 היא **דטרמיניסטית
ואינה תלויה ב-DB**: היא מריצה את `detect_trend_step` עצמו על הברים המצוטטים.

**סייג שני:** ‏`lsma` לא היה זמין לריצת-§4 (עמודת `lsma_value` יושבת ב-DB). שער-ה-LSMA
‏(`:206-213`) הועבר כ"עובר" לטובת הגלאי בסריקה, ובטבלת-הבר-אחר-בר הוא **מעולם לא הגיע
לתורו** — כל ששת הברים נדחו לפניו על גיאומטריה. ⇒ המסקנה אינה תלויה בו.

*נכתב 08.09.2026 · READ-ONLY: אפס עריכת-קוד, אפס נגיעה ב-`.env`, אפס ריסטארט, אפס פקודה.*
