# cc-macbook — יום-לייב 07-20: מיון-טסטים + תצפית-חיה (פסיקת-מייקל "מאשר")

**המכונה עכשיו LIVE** (is_sim=0, armed, flat). iMac כבוי. **חוק-ברזל להיום:**
לא להריץ סימים / לא להציב הזמנות / לא `is_sim=1` על מכונת-המסחר תוך-כדי הסשן — זה מבטל את הלייב.
כל האימותים של היום = **תצפית קריאה-בלבד על עסקאות-לייב אמיתיות**. עבודת-טסטים מותרת (לא נוגעת במסחר).
`git pull` לפני · `commit`+`push` אחרי · חוק-5 (פקודה+פלט גולמי ל-LIVE_CHANNEL).

## משימה 1 · ⭐ מיון 92 טסטי-הרגרסיה הנכשלים (עכשיו — עדיפות)
הסוויטה אדומה: `pytest tests/v9/regression/ -q` → **92 failed, 1155 passed, 0 import-errors** (בסיס ~90 = פרה-קיים, לא מהשבוע). כולם נראים הפרשי-ציפייה, אבל **לא מוינו**. סכנה: באג-אמת מוסתר בין ה-92.

לכל טסט-נכשל — סווג לאחד משלושה + פעל:
- **(א) ציפייה-מיושנת מפסיקת-07-20** (6T במקום 3T · caps 800/999 · cutoff 15 במקום 14 · fixed-contracts · G2/G3/G6/T16 ON) → **עדכן את הטסט לערך-הפסוק** (כמו שעשה cowork ל-test_stop_anchor_e2e/test_stop_resolver_v1). תעד "stale→ruled".
- **(ב) ריקבון פרה-קיים** (fixture/DB/מבנה ישן, לא-קשור לפסיקה) → תקן אם זול, אחרת סמן `xfail` עם סיבה + שורת-TODO. אל תמחק.
- **(ג) 🔴 באג-אמת** — הקוד-החי מתנהג שגוי (לא הטסט מיושן) → **עצור מיד + דווח ל-LIVE_CHANNEL**. אל "תתקן" ע"י עריכת-הטסט.

**חוק-ברזל:** לתקן טסטים ע"י עדכון-ציפייה-לערך-הפסוק בלבד. **אסור** לשנות קוד-מסחר כדי לירֵק טסט. מטרה: 0 מקרי-(ג), והסוויטה ירוקה (או xfail-מתועד). דווח ספירה סופית + רשימת כל (ג) אם היו.
מקבצים ידועים: confluence_ri_zlr(11) · zone_limit(8) · structural_targets(6) · daytype_position_gate(6) · risk_caps(4) · fixed_contracts_2/3(7) · sizing_consolidation(4) · target_side_guard(3) · d_rvx(3) · opening_fire(4) · woodies_stop_v2/stop_anchor(4).

## משימה 2 · תצפית-חיה על עסקאות (קריאה-בלבד, כל הסשן)
- **ירי 4-חוזים:** כשעסקת-לייב נפתחת עם 4 חוזים → `python3 scripts/verify_t17_e2e_4contract_sim.py --trade-id <LIVE_ID>` (מצב-קריאה על העסקה-האמיתית) → אמת: 4 מילויים · **BE אחרי T1-האמיתי** (לא T0) · **סטופ 6T מעבר-למבנה**. פלט גולמי ל-LOG.
- **T16 (S6 היפוך):** על היפוך-משמעותי אחרי T1 — אמת שנפלט `MODIFY_STOP` הידוק + `MODIFY_TARGET` קירוב (לא op=EXIT). 
- **G2/G3/G6:** אמת שסוג-היום שה-S2/S4 סוחרים לפיו == `get_live_day_type()` (לא מקור-מפגר/מסונתז). צוד `blocked_by` מפתיע.
- **ORPHAN:** אם צצה פוזיציה-לא-רצויה — התראה מיידית ל-LIVE_CHANNEL (ההגנה-האוטו כבויה בפסיקה; מייקל משטח ידנית מהכיס).

## משימה 3 · דחוי לאחרי-הסגירה (או iMac-סים) — לא היום-בלייב
T17 4-חוזים-בסים · ORPHAN re-sim (מבני+$200) · STOP_WIDEN סים. דורשים `is_sim=1` → רק אחרי סגירת-הסשן (flip לסים) או על iMac. **לא** על מכונת-המסחר תוך-כדי לייב.

עצור אחרי משימה-1 לאימות-cowork. אם מצאת מקרה-(ג) — עצור מיד.
