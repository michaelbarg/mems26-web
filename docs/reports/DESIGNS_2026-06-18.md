# MEMS26 · טיוטות-עיצוב — 2026-06-18

עיצובים מוכנים-להפיכה-לפרומפט לבעיות שאומתו היום (`PATTERN_EOD_2026-06-18.md`).
**לא מומש דבר — design בלבד.** כל פריט: שורש · שינוי מוצע (קובץ/שכבה) · regression · סיווג trading-logic(→Michael)/display-safe · הצלבת-Sierra ל-CC.
**D-IDs עקביים עם 06-11/12/15/16** (עיצובים מתמשכים); עודכנו בראיות-היום. חדשים: D18–D21.

> **תזכורת standing-decision (CLAUDE.md, 2026-06-08):** chop-gates (S2 `choppiness_ok`, Layer-0 `chop_state`) ו-COT/AMT (S2⟂S3) **מנוטרלים בכוונה**. **אף עיצוב כאן אינו מציע לאפס flag או "להחזיר" שער-מנוטרל.** D18 (צ'ופ) מובא כ-**ראיה-להחלטת-Michael בלבד** (strategic-stop), לא כהמלצה-להדלקה — re-enable = trading-risk → sign-off מפורש. שערי-trend/regime/confidence (D3/D5/D19) הם **חדשים ונפרדים**.

> **⚠️ היפוך-יום (קריטי לקריאת כל העיצובים):** היום (06-18) **טווח/צ'ופ**, ו-**LONG זכה / SHORT הפסיד** — הפוך מ-5 הימים הקודמים. ⇒ **D3/D4 (וטו-כיווני/cluster) הם תלויי-רֵז'ים** ועברו מ"עדיפות-עליונה" ל"דורש-תנאי-רֵז'ים". המנוף החוצה-רֵז'ים הוא **שער-צ'ופ (D18)**.

---

## D18 — חדש 🔴 (ראיה-להחלטת-Michael · standing-decision-gated): צ'ופ-whipsaw 09:10–09:30

**שורש (מאומת מהברים):** 4 עצירות-מלאות רצופות ב-20 דק' (ids 148-L/149-S/151-S/152-S = **−$851.25**), **שני הכיוונים** — 148 נעצר על תחתית 7541, 149/151/152 על snap-back 7570. הברים 09:10–09:35 מראים נדנוד 7541↔7570 (29pt) **בלי follow-through**. דילוג ⇒ היום **−$56.30 → +$794.95**. זוהי החתימה המדויקת של **Layer-0 `chop_state=SEARCHING`** (`backend/v9/gateway/trading_gateway.py`) — **המושבת-בכוונה** (`LAYER0_CHOP_GATE` unset, default-bypass, עדיין מחושב+נרשם לתצפית).
**שלב-0 (verify, לפני כל דיון-החלטה — Rule 2/5):** **CC לאמת בלוג** ש-`chop_score`/`chop_state` היה אכן **SEARCHING** בחלון 09:10–09:35 (CLAUDE.md: "still computed + logged"). אם **לא** היה SEARCHING ⇒ הגייט-הקיים לא היה תופס ⇒ הממצא הוא "indicator-tuning", לא "re-enable". אם **כן** ⇒ הגייט-הקיים *היה* חוסם.
**ההחלטה (Michael בלבד — strategic stop):** שתי אופציות, **איני ממליץ על אף-אחת** — מציג לבחירתך: (א) re-enable `LAYER0_CHOP_GATE=1` (trading-risk-surface → sign-off + restart, החלטת-העמידה 06-08); או (ב) להשאיר מושבת ולתכנן **שער-חדש-נפרד** (cooldown-after-2-stops: אחרי 2 עצירות תוך M דק' → pause N דק') שאינו נוגע ב-flag-המנוטרל.
**Regression (לכשתוכרע):** replay 06-18 ⇒ 148/149/151/152 נחסמות; וידוא ש-150/153 (long-רצפה זוכים, +$99) נשמרים בגרסה-ההדוקה.
**סיווג:** **trading-risk → Michael (strategic stop).** **אין לאפס flag ללא sign-off.**
**Sierra/לוג:** `chop_state` בחלון (CC) — חוסם-ראייה לכל השאר.

## D19 — חדש 🟡→trading-adjacent (I-36): שערי-כיוון מותנים ב-confidence (אל-תפעל ב-LOCKED_LOW_CONF)

**שורש (מאומת היום):** `day_type=Trend_Normal` סוּוַּג על **יום-טווח** (טווח 46pt, נטו −12.5, נתיב-V), אך עם `confidence=0.38 LOCKED_LOW_CONF` — **המערכת עצמה** סימנה ביטחון-נמוך. כל שער-מבוסס-day_type (DAYTYPE_PLAYBOOK, TREND_GATE-343a41b) שמחיל לוגיקת-מגמה היה פועל על יום-לא-מתאים. הוכחה: וטו-כיווני (D3) היה חוסם היום את ה-LONGs-הזוכים (+$239.60).
**שינוי מוצע:** כל שער-כיווני (D3/TREND_GATE) קורא `gate_active = (day_type∈TREND_SET) AND (confidence ≥ CONF_MIN) AND (lock_state != LOCKED_LOW_CONF)`. `CONF_MIN` ב-YAML (התחל ~0.50). כש-low-conf ⇒ **אל-תחסום** (תן לתבניות לפעול דו-כיוונית).
**Regression:** replay 06-18 (conf 0.38) ⇒ TREND_GATE **לא** פעיל ⇒ LONGs-הבוקר עוברים; replay 06-16 (יום-מגמה conf גבוה יותר) ⇒ פעיל ⇒ חוסם counter-trend.
**סיווג:** **trading-adjacent → Michael** (משנה מתי שער-כיווני פעיל). תנאי-קדם הכרחי ל-D3 בטוח.
**Sierra:** לא נדרש (confidence פנימי); אך תלוי שסיווג-day_type נכון (ראה §הצלבה 4).

## D3 — I-26/I-28 🟡 (trading-logic → Michael): veto-מגמה · **עודכן — תלוי-רֵז'ים, לא חוק-על**
> **שינוי-מהותי מ-06-16:** היום (יום-טווח) הוטו-הכיווני **היה מפסיד** — LONG זכה +$239.60. ⇒ D3 **חייב** את D19 (confidence-gate) כתנאי-קדם, אחרת מפסיד בימי-טווח.

**שורש (5 ימים בעד, 1 נגד היום):** ב-06-10/11/12/15/16 LONG-נגד-מגמה דימם (06-16: −$1,285, יחס 13:1). **אך היום הפוך** — אותו "דלג-LONG" היה עולה $295.90. גם price-slope ("block LONG כש-price<open−k·ATR") היה חוסם את REACTIVE_LONG-הבוקר (150/153/154, ~25pt מתחת ל-open) — **שלושתם זכו (+$145.45)**.
**שינוי מוצע (מעודכן):** וטו fire ל-counter-trend **רק כש**: `D19 gate_active` (day_type=Trend מאומת **AND** conf≥סף) **AND** price_slope-יורד על N≥6 ברים (`price < opening − k·ATR` או EMA-slope). **בלי D19, D3 אסור** (יחסום ימי-טווח). הסף + k + N ב-YAML.
**Regression:** replay 06-16 ⇒ 12 LONGs veto, 18 SHORTs עוברים (+$627.40); replay **06-18** ⇒ D19 חוסם את ההפעלה (conf 0.38) ⇒ **0 veto**, LONGs-הזוכים נשמרים. שני ה-replays חייבים לעבור.
**סיווג:** **trading-logic — Michael.** ירד מ"עדיפות-עליונה" (06-16) ל"מותנה-D19".
**Sierra:** `woodies_trend`+TCCI מול slope (06-16 הראה צבע≠slope).

## D11 — I-34 🔴 (verify-then-fix; אם dead-wire → trading-risk): חיווט verdict ה-sizing
> **התחזק היום — smoking gun.**

**שורש (מאומת, חזק מ-06-16):** כל ההפסדים `implied_contracts=3.0` ללא תלות ב-sizing. הבולט: **id156 `sizing=0` ירה 3-חוזים-מלאים והפסיד −$165** — `sizing=0` שמשמעו **SKIP/no-trade** ירה full. גם אף `"half"` לא חצה חוזה (id173/152/167 full-3). ⇒ ה-verdict (string או numeric) **לא מגיע לנתיב-ה-PnL/contracts**.
**שלב-0 (verify, Rule 2):** (א) shadow-PnL נומינלי-3 בכוונה ("half"/sizing יחול ב-LIVE) ⇒ תמים; או (ב) `sizing→contracts→pnl_usd` מנותק ⇒ **trading-risk** (השער-לגודל לא מגן, וגם נורמליזציית-סיכון אבודה — ראה D20). נתיב: `daytype_playbook.decide()`/sizer → `trading_gateway.route_setup` → `services/trade_manager/manager.py`.
**שינוי מוצע (רק אם ב'):** ב-trade_manager — `effective_contracts = base × size_factor` (`half→×0.5`, `0→SKIP`, `1/2/3→n`) בנקודת-חישוב-PnL+גודל, לא רק ב-metadata.
**Regression:** עסקת-`sizing=0` ⇒ **לא נפתחת** (0 שורות contracts_pnl); עסקת-`"half"`+stop ⇒ `pnl_usd==−(risk×5×base×0.5)`. replay 06-18: id156 → $0 (לא −$165).
**סיווג:** **verify-first; אם (ב) → trading-risk → Michael.** נספח ל-`feedback_full_decision_pipeline_wiring`.
**Sierra:** לא נדרש (execution פנימי).

## D20 — חדש 🟡→trading-logic (I-37): sizing מנורמל-סיכון (R↔$ divergence)

**שורש (מאומת היום):** **ΣnetR=+3.36R אך Σ$=−$56.30.** הסיבה: stop נע 2.5–25pt בקיבוע-3-חוזים ⇒ $/R לא-קבוע — ההפסדים נפלו על stops-רחבים ($165/R) מול זכיות stops-צרים ($112/R). ⇒ יום חיובי-ב-R הופך לשלילי-ב-$.
**שינוי מוצע:** `contracts = round(RISK_BUDGET$ / (risk_pt × 5))` (risk-per-trade קבוע, YAML) במקום 3-קבוע. משתלב עם D11 (אותו נתיב-execution) ו-D8 (טבלת-stops). זהו המימוש של memory `project_config_tunable_stop_exits_contracts`.
**Regression:** replay 06-18 ⇒ כל עסקה risk≈$RISK_BUDGET קבוע ⇒ Σ$ עוקב ΣnetR (חיובי); וידוא שאין עסקה >MAX_CONTRACTS.
**סיווג:** **trading-logic — Michael** (משנה גודל-פוזיציה). חוסם-LIVE יחד עם D8/D11.
**Sierra:** רמות-stop (D8) ל-risk_pt.

## D8 — I-3/I-13 🔴 (trading-logic → Michael): טבלת stop/target פר-תבנית×day-type — **LIVE #1**

**ללא-שינוי-מהותי.** ראיות-היום מחזקות: stop נע **2.5–25pt** (id169 2.5 מול id147 25) באותו `MAX_RISK` אחיד ⇒ הוא השורש ל-divergence (D20). TLB-runner נחנק שוב (זכיות T1→BE זעירות מול id161 mfe-29 שנלקד 1.6R). Michael מגדיר את הטבלה (memory: `project_stop_target_placement_table`).
**Regression:** YAML ⇒ כל תבנית×day-type מקבלת stop/T1–T5/contracts; replay מאמת stops-ייעודיים + runner-hold.
**סיווג:** **trading-logic — Michael** (חוסם-LIVE).
**Sierra:** רמות-stop להצלבה.

## D2 — I-31 🔴 (display-safe, עדיפות-גבוהה): ספירת-ירי שקרית ~2.08×

**שורש (מאומת היום):** `five_min=23 + woodies=31 = 54` מול **26** ב-`v9_trades` (~2.08×; S2 1.8×, S4 2.4×). חוזר 4 ימים.
**שינוי מוצע:** ב-build_status inspector — מקור-הספירה ל-`SELECT firing_system,COUNT(*) FROM v9_trades WHERE session_date=today GROUP BY 1`. הפרדת `signals_count` מ-`fired_count`.
**Regression:** היום ⇒ endpoint 13/13; `Σfired==COUNT(v9_trades WHERE date=today)`.
**סיווג:** **display-safe** — **חוסם-כיול. חוזר מ-D2(06-12/15/16) — לא מומש.**
**Sierra:** לא נדרש (DB).

## D1 — I-22 🔴 (display-safe): `pnl_r` שבור + לא-עקבי בין נתיבים

**שורש (מאומת היום):** id166 +$26.9→`pnl_r=21.52` (÷$1.25); אך id172→`4.25`, id170→`3.08` (בסיס-אחר). נתיב-הפסד תקין (−1.0). ⇒ מנופח **וגם** לא-עקבי בין branches.
**שינוי מוצע:** ליישר את כל הנתיבים (T1/BE, T2, STOP) לנוסחה אחת: `pnl_r = pnl_usd / (abs(entry−stop_initial) × 5 × contracts)`. backfill/`r_known_bad=true` להיסטוריה.
**Regression:** עסקת-T1/BE סינתטית ⇒ `pnl_r≈0.27` לא 21; T2 זהה; STOP −1.0.
**סיווג:** **display/analytics-safe. חוזר מ-D1(06-12/15/16) — לא מומש.**
**Sierra:** לא נדרש.

## D4 — I-30 🟡 (trading-logic → Michael): cluster-guard — **עודכן: תלוי-רֵז'ים**
> **ראיה-נגדית היום:** ה-clusters היום היו **זוכים** (REACTIVE_LONG@7540.5: 153/154/162; TLB-L@7548.5: 170/171/172) ⇒ cluster-guard היה **מקפח** ~$252. תלוי-רֵז'ים (עזר ב-06-16 −$647, הזיק היום).

**שינוי מוצע (מעודכן):** `MAX_CONCURRENT_PER_PATTERN` פעיל **רק** כש-D19 gate_active (יום-מגמה מאומת) — לא בימי-טווח. אחרת מקפח fade-clusters תקפים.
**Regression:** replay 06-16 ⇒ culls counter-trend cluster; replay 06-18 ⇒ **לא** פעיל (low-conf) ⇒ winners נשמרים.
**סיווג:** **trading-logic — Michael.** משני ל-D18/D3.
**Sierra:** מקור-רמות-stop משותפות.

## D21 — חדש 🟡 תפעולי/safe: שערי-SHADOW לא חושפים would-block

**שורש:** TREND_GATE (חי-SHADOW מהיום) + REACTIVE_LOCATION/RUNNER_TRAIL/DAYTYPE_PLAYBOOK (343a41b/5c7a4b2/eb605f7/864a345) — הגֵייט **suppress-ו-return-טרם-shadow** ⇒ setup-חסום **נעדר** מ-`v9_trades` (לא `blocked_by`), ייתכן כ-gap-id (I-32). ⇒ **אי-אפשר לאמת מ-trades API מה הגֵייט חסם** (היום 0 גלוי, אך זה לא ניתן-לאישור בלי לוג). עיוורון-CF על הגייטים-החדשים-עצמם.
**שינוי מוצע:** כל שער-SHADOW כותב `would_block`/`shadow_verdict` ל-`v9_trades.metadata` (או טבלת-shadow ייעודית) גם כשלא-פעיל ⇒ ה-EOD יכול לחשב "מה היה אילו הופעל".
**Regression:** עם שער-SHADOW דלוק, עסקה שהוא היה-חוסם מקבלת `metadata.shadow_verdict='BLOCK'` + סיבה; EOD סופר אותן.
**סיווג:** **תפעולי/display-safe** (רישום בלבד, לא משנה ביצוע).
**Sierra:** לא נדרש.

## D6 — תפעולי/safe (יום-שמיני): סוכן-ה-DIAG-30-דק' לא רץ

**שורש:** אין `PATTERN_DIAG_` מאז 06-10 (8 ימים) — ה-trigger מת. בלעדיו ה-EOD עיוור לנחסמים (אין CF-קלאסי). יחד עם D21 = **עיוורון-CF כפול**.
**שינוי מוצע:** (א) לבדוק scheduled-task/LaunchAgent של ה-DIAG (נעצר מתי/למה — error/TZ?); (ב) guard-בוקר: עד 09:30 CT אין snapshot ⇒ נטיפיקציה ל-Michael.
**Regression:** יום-המסחר-הבא ≥8 snapshots; היעדר עד 09:30 ⇒ נטיפיקציה.
**סיווג:** **תפעולי/safe. חוזר מ-D6(06-12/15/16) — לא טופל; חוסם 8 ימי-CF.**
**Sierra:** לא נדרש.

## D7 — I-33 🟡 (display-safe): רדאקציה — **אולי רגרסיה-חוזרת, לאמת**

**שורש (היום הפוך מ-06-16):** `session_min`/`session_date`/`rtb_session` חזרו ל-`[BLOCKED: Sensitive key]` (ב-06-16 דווחו גלויים/נסגרים). או חשיפה-זמנית-אז או רגרסיית-matcher.
**פעולה:** CC לאמת אם ה-redactor שונה (allowlist↔denylist) בין 06-16 ל-06-18. לא-חוסם.
**Regression:** `session_min` מספר; מפתח-סוד-אמיתי נשאר חסום.
**סיווג:** **display-safe.**
**Sierra:** לא נדרש.

## D10 — 🟢 (display/diagnostic-safe): dead-wrapper day_type — **היום עקבי**

**שורש:** ב-06-16 ה-endpoint הראה Trend_Normal מול trades=Variation (dead-wrapper). **היום שניהם Trend_Normal** ⇒ ה-split לא-משחזר. הסיכון-המבני (route קורא instance-wrapper) **נשאר** — לתקן את מקור-הקריאה ל-`app.state.day_type_machine`/`v9_day_type_state` לפני שזה מטעה שוב.
**Regression:** `day_type/state.day_type` == day_type של העסקה-האחרונה.
**סיווג:** **display/diagnostic-safe.**
**Sierra:** לא נדרש.

## D9 — לאימות-CC (קטנים): I-32 · I-25 · readiness

- **I-32 🟡 (היפותזה חדשה):** gap-ids **160,164** (טווח 147–174). חוזר 4 ימים (06-12/15/16/18). **חדש:** `TREND_DIRECTION_GATE`/`REACTIVE_LOCATION_GATE` עלו ב-SHADOW היום (suppress-ו-return-טרם-shadow) ⇒ setup-חסום = שורה-נעדרת ⇒ **160/164 אולי gate-suppressions, לא rollback** (חלונות 10:20–10:50 / 12:05–12:25). CC לאמת מול gate-log: אם כן ⇒ I-32 = תופעת-לוואי-תקינה (לא באג), אך חוסר-רישום ל-trades API = D21. (ימי 06-12/15/16 היו **לפני** הגֵייט ⇒ ייתכן שורש שני.)
- **I-25 🟢:** trades `limit=200`→**422** (cap=100). enhancement; היום 26<100 ⇒ ללא-אובדן.
- **readiness 🟢:** **READY** post-close (bridge fresh, s1 classified Trend_Normal, s4 trend=BLUE). תקין.

---

**סדר-עדיפות מוצע ל-Michael:**
**D18 (צ'ופ — החלטה-שלך; אחרי אימות-CC של chop_state בלוג)** → **D11 (verify חיווט-sizing — `sizing=0` ירה full)** → **D19 (confidence-gate — תנאי-קדם ל-D3 בטוח)** → **D8+D20 (טבלת-stops + sizing מנורמל-סיכון — חוסמי-LIVE)** → **D3 (veto-מגמה — מותנה-D19 בלבד)** → **D2** (I-31) → **D1** (I-22) → **D6+D21** (DIAG + shadow-log — עיוורון-CF כפול) → **D4** (cluster, מותנה-רֵז'ים) → **D10/D7/D9**.

*היום שינה את התמונה: **D3/D4 (וטו-כיווני/cluster) ירדו בעדיפות** — הם תלויי-רֵז'ים והיום היו מפסידים. **D18 (צ'ופ)** הוא המנוף החוצה-רֵז'ים (+$795 CF) אך **standing-decision-gated → החלטת-Michael בלבד, לא flag-flip.** **D11 (sizing מת — `sizing=0` ירה full)** הוא ה-trading-risk הברור ביותר ל-verify-מיידי. **D19 (confidence-gate)** הוא הלקח-המרכזי: המערכת אמרה `LOCKED_LOW_CONF 0.38` — שערי-כיוון חייבים לכבד זאת.*
