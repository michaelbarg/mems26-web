# Phase 1 Report — 2026-08-26

## Status

| Item | Status | file:line |
|------|--------|-----------|
| **1.1** Entry guard ownership | ✅ BUILT (OFF) | `entry_guard.py:112-158` |
| **1.2** APP_STATE_ROOT_FIX shadow | ✅ BUILT (OFF) | `main.py:820-833` |
| **1.3** Candidate resolver | ✅ BUILT | `scripts/candidate_resolver.py` |
| **DB repair 1-5** | ✅ READ-ONLY done | 6 sessions ≠78 bars, 16 sessions CVD>78 |
| **Stop replay** | ✅ EVIDENCE | 95 overrides, 83% tighter, 7.58pt avg |
| **flag_guard** | 207 PASS | + liveness clean |

## 1.1 — ENTRY_GUARD_OWNERSHIP_V1

`entry_guard.py:112-158` — when flag ON and `pos != 0`:
1. Checks if TM has an open trade matching the position direction → explained
2. Checks reconciler ownership attribution → explained
3. Neither → blocks (same as today)
4. Explained → allows with warning tag

**Flag OFF = byte-identical** to today (blocks on any pos != 0).
**Michael ruling pending:** who holds -3 on 37138283?

3 tests: `test_entry_guard_ownership.py`

## 1.2 — APP_STATE_ROOT_FIX_V1

Already built in previous commit (`main.py:820-833`). When flag=1, writes
`opening_type_result` to `app.state` on every bar. Pending: shadow mode to
compare old (None) vs new (real) values.

## 1.3 — Candidate Resolver

`scripts/candidate_resolver.py` — EOD script per CONTRACT §9:
- Reads DETECTED events from JSONL + archive
- Computes MFE/MAE at 3/6/12 bar horizons from `v9_bars_5min_woodies`
- Writes RESOLVED events (idempotent)
- NOT_JUDGEABLE for sessions failing quality checks (Rule 1)
- Runs outside firing path (verified: no import from route_setup)

## DB Repair Steps 1-5 (READ-ONLY)

```
Bar count failures (≠78):
  07-15(60) · 07-16(65) · 07-21(71) · 07-22(57) · 07-28(73) · 08-04(41)
  Total: 6 sessions

CVD duplicate rows (>78):
  16 sessions with excess rows (conflicts from bar_id indexing)
```

Step 6 (writes) deferred to after 23:00 IL per work order.

## Stop Override Analysis (Root 3)

```
95 overrides parsed from backend.err.log
StepLadder tighter: 79/95 (83%)
StepLadder wider:   16/95
Average difference: 7.58 pts (~$114/trade at 3c)
Max difference:     16.00 pts
```

**Evidence for Michael:** StepLadder is generally more aggressive (closer stop,
89% of overrides). The average difference is 7.58 points — significant at $5/pt.
This is a trade-off: tighter stops = smaller losses on losers but more frequent
stopouts. A live A/B or replay comparison is needed to determine net P&L impact.

## NOT-VERIFIED

- Entry guard live integration (requires restart)
- Candidate resolver on live data (ledger only recently enabled)
- DB repair write step (Step 6, after 23:00)
- APP_STATE_ROOT_FIX shadow session comparison
- §D for any flag (cowork gate)

## Commit

`67481649` — pushed.

*cc-macbook · 2026-08-26. No .env changes. No restart.*
