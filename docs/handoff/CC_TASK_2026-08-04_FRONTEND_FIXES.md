# cc — תיקוני-פרונטאנד (מייקל 04.08, אחרי-סגירה)
1. **גודל-טבלה נשמר:** הטבלה שמייקל משנה לה גודל קופצת-חזרה — אתר את הרכיב (חשד:
   TradeReviewTab רשימת-עסקאות / גובה-הצ'ארט ב-V9Dashboard) והוסף persist ל-localStorage
   בדפוס chartH הקיים (SSR-safe hydrate). לאמת: שינוי → ריענון → הגודל נשמר.
2. **ספאם "dropped stale bar update" ב-ChartV5b** (מאות שורות): שורש — useLivePricePoll מזרים
   טיקים ישנים מול בר-אחרון חדש; לתקן את התנאי (לא רק לדווח) + rate-limit ללוג.
3. **רכיב-התקלה:** מייקל מדווח על רכיב שמציג תקלה — לזהות מול צילום-מסך שלו/קונסול, לתקן שורש.

## עדכון מצילום-מייקל 22:55 (פאנל-TPO) — שלוש תקלות/דרישות
4. **שינוי-גובה לא עובד** בחלון-הצף (MultidayProfilePanel): פינת-ה-resize משנה רוחב אך הגובה
   לא מגיב/קופץ. לתקן (pointer-events, בדוק שהקנבס מקבל h מעודכן וגדל בפועל), לאמת גרירה
   לשני הצירים + persist.
5. **זמן-אמת:** בצילום 22:55 ביום-מסחר: "היום: UNKNOWN 0%" ואין עמודת-היום — כלומר radar
   day_type=UNKNOWN וה-today_block ריק בזמן שהיה מסחר פעיל. שורש: כנראה v9_day_type_state
   התאפס בריסטארט-בוקר / server-restart מחק את השורה, או ששאילתת-היום לא מוצאת ברים.
   לתקן כך שהסיווג-החי ועמודת-היום מתעדכנים תוך-יומית תמיד (וגם אחרי ריסטארט — rehydrate).
6. **מערכת-1 סורקת את המאזן:** פסיקת-מייקל — S1 חייבת לצרוך את הקשר-7-הימים לאימות-הסיווג
   שלה (open_vs_balance7 + נדידה שכבר נבנו ב-stage-D, flag MULTIDAY_CONTEXT_V1). הרץ את
   replay-הקבלה (21.07 וטו · 30.07 drive · 14 ימים) ⇒ דוח ⇒ פסיקת-מייקל להדלקה. זו הדרך
   שבה "מערכת-1 יודעת שהיא זיהתה נכון" — מצליבה את עצמה מול המאזן.
