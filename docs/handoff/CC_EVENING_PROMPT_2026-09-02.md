# תור-הלילה 02.09 (אחרי 23:00) — השורש: **הספרים לא מודדים את מה שקרה בשוק**

> נכתב ע"י `cowork-dev` ב-23:15 IL אחרי בדיקת-`git log` — **אפס פעילות-cc** מאז 22:41
> (כל 15 הקומיטים האחרונים `cowork`), אין שורת-claim, אין תהליך-`claude` חי.
> ⇒ `cowork` תופס claim ומבצע בעצמו, לפי פסיקת-מייקל 27.08.

## מצב-פתיחה (נמדד 23:08, לא מזיכרון)

```
/api/v9/health            http=200  t=0.0049s  {"status":"ok"}
sierra_state.json         age=0.27s  is_sim=0  last_price=7681.00
                          position_qty=0  acct_daily_pl=+602.50
                          cash=3,241.64  avail=3,241.64  margin_req=0.0  under_margin=0
/api/v9/system6/diagnose  active=false · slot_trade_id=null
backend PID 31460         up since 15:06:18 (8h02m)
```

⇒ **החשבון שטוח · הסלוט פנוי · אין פוזיציה ואין פקודה פתוחה** ⇒ ריסטארט מותר (אחרי 23:00).

---

## 🔴 סעיף-1 (עדיפות עליונה) · T-227 + T-229 — תמחור-הספרים ממקור-האמת

**המספר:** יומן-סיירה `TradeActivityLog_2026-09-02_UTC.37138283.data`, שדה
`Closed Trade Profit/Loss` — **23 רשומות, SUM = 602.50**, מול `acct_daily_pl = 602.50`,
**delta = 0.00**. השרשרת נסגרת ⇒ הפירוק אמין.

```
רשומות  1-6   cum → 571.25   מסחר-ידני (מייקל/אתי)
רשומות  7-10  cum → 733.75   הצמד 953+955          = +162.50
רשומות 11-14  cum → 596.25   #963 (MAE_SCRATCH)     = -137.50
רשומות 15-18  cum → 526.25   #968 (MAE_SCRATCH)     =  -70.00
רשומות 19-23  cum → 602.50   #971                   =  +76.25
                             ⇒ תרומת-המערכת         =  +31.25
```

**הספרים אומרים `+422.50`. הפער ≈ `$391`.** ⚠️ סייג-חובה: החשבון משותף עם אתי,
ולכן חלוקת-הרשומות היא שחזור-מצטבר ולא ייחוס-מוכח פר-עסקה. **לכתוב את הסייג בדוח.**

### הראיות שכבר נאספו היום (לא לחזור עליהן — לאמת ולתקן)

1. **`#963`/`#968` UNPRICED** — יציאת `MAE_SCRATCH` פותחת פקודת-שוק חדשה (נצפתה: `10909`),
   ו-`FillPoller` רשום רק על פקודת-הכניסה + היעדים + הסטופ ⇒ **מילוי-היציאה בלתי-נראה**.
   `T-160` עשה את הדבר הנכון (`exit_price=NULL` במקום P&L מומצא) — **אסור "לתקן" את זה בחזרה.**
2. **`T-229` — מיפוי-מילויים לפי קרבת-מחיר במקום `order_id`.** נתפס חי על `#971`:
   ```
   20:13:47  fill kind=T1 order=10924 price=7676.25
   20:57:55  fill kind=T2 order=10927 price=7679.50
   ⇒ בספרים: t1_hit_ts = 20:57:55 · t2_hit_ts = NULL · t3_hit_ts = NULL
   ```
   מילוי-T2 תויק כ-T1 (כי `7679.50` הוא ה-`t1` בספרים), ומילוי-T1 האמיתי נעדר.
   תוצאת-לוואי: `T-43` סופר `TM=5 Sierra=4` וצועק `BLOCKING` — **פעמיים היום, ~2 שעות**.
3. **דריסת-עמודת-יעד:** על `#953` נמצא `t2=7686.75 > t3=7684.75` בלונג — בלתי-אפשרי.
   התאום-בצל `#952` (אותה שנייה, אותו מחיר) מחזיק `t2=7679.25` הנכון. מחיר-המילוי נכתב לעמודת-היעד.
4. **היקף היסטורי (`T-227` הורחב ב-22:10):** יציאת `MAE_SCRATCH` **מעולם** לא תומחרה —
   `10` עסקאות-לייב בחודש, `9` בלי מחיר; ו-**7 מהן (19/20/28.08) נרשמו `$0`/BE** —
   הצהרת-אפס בלי מדידה. מול זה: `STOP_HIT` = `28/28` מתומחרות.

### המשימה

1. **`FillPoller` — לרשום את פקודת-היציאה.** כל פקודה שנשלחת בשם העסקה (כולל
   `MAE_SCRATCH`/flatten/scratch) נרשמת ב-`FillPoller` ברגע-השליחה. טסט-רגרסיה:
   סימולציית-scratch ⇒ `exit_price IS NOT NULL`.
2. **מיפוי-מילוי לפי `order_id`, לא לפי מחיר.** להחליף את התאמת-קרבת-המחיר במפתח
   `order_id → leg`. טסט-מוטציה: להחזיר את המיפוי-לפי-מחיר ⇒ הטסט חייב ליפול.
3. **איסור-כתיבה:** מחיר-מילוי לא נכתב לעולם לעמודת-יעד (`t1..t4`). היעדים הם תכנון.
4. **שער-שפיות שנכשל בקול:** לונג ⇒ `t1<t2<t3<t4`; שורט ⇒ הפוך. הפרה = `ERROR` + שורת-לוג
   מזוהה, לא `debug`.
5. **תמחור-גיבוי ממקור-האמת:** כשמילוי-היציאה בלתי-נראה — לגזור P&L מ-
   `TradeActivityLog_<date>_UTC.<acct>.data` שדה `Closed Trade Profit/Loss`
   (הקובץ `latin-1`/מעורב-בינארי — `strings | grep -o "Closed Trade Profit/Loss: *-\?[0-9.]*"`).
   לאכלס `pnl_sierra` (היום `NULL` ב-100% מהשורות) — **לא** לדרוס `pnl_usd`.
6. ⛔ **`backfill` למפרע אסור** — שינוי P&L רשום (`#953/#955/#963/#968/#971` + 7 ההיסטוריות +
   ה-`backfill` של `T-211`) **דורש פסיקת-מייקל**. לבנות את הכלי, **לא להריץ**; לצרף
   `--dry-run` שמדפיס את הדלתא לכל שורה.

---

## 🟠 סעיף-2 · T-226 — SCALE_IN מייצר שורת-רפאים בכל ירי

**מדוד פעמיים היום, אותה שרשרת מילה-במילה:** `17:30 → #955` · `21:18:53 → #979`.
הילד נבנה עם יעד-אחד ל-4 חוזים, שומר-`T-214` דוחה את ה-`PLACE` — **וה-TM כבר יצר את השורה**.
תוצאה: עסקת-לייב `PENDING` בלי כניסה + `DIVERGENCE` כל 30 שניות.

**הכלה עבדה פעמיים** (שומר `POSITION_TRUTH` סגר `18:48:33` ו-`21:41:25`, שניהם
`UNPRICED` בלי P&L מומצא, סלוט שוחרר) — **הכלה, לא תיקון.**

שלושת התיקונים: (א) הילד יורש את הגודל שנתבקש (נצפה: ביקשו 2, נבנה 4);
(ב) הילד מקבל יעד-לכל-חוזה; (ג) **`PLACE` שנדחה מגלגל אחורה את שורת-ה-TM.**
טסט-מוטציה על (ג): דחיית-`PLACE` ⇒ `SELECT count(*) FROM v9_trades WHERE state='PENDING'` = 0.

---

## 🟡 סעיף-3 · T-228 — EDGE_ENTRY_LOCATION_FIX_V1 רץ על גיבוי בלבד

פסיקת-מייקל 02.09 13:35. השאילתה לאזור-הערך-של-אתמול שואלת עמודות שאינן קיימות בסכימה
⇒ נכשלת בכל קריאה **בשקט**, והגיבוי (קובץ-TPO) הוא שנושא את התיקון בפועל.
**הסיכון ליום-הפער הבא:** קובץ-TPO מתיישן/נעלם ⇒ התיקון נעלם איתו בלי שורת-לוג,
והשער חוזר לחסום קצוות בדיוק כמו לפני הפסיקה.

לתקן את השאילתה לעמודות האמיתיות + **כישלון קולני** (`warning` מזוהה) כשהיא נופלת.
⚠️ זה קוד בתוך `location_gate` = משטח-מסחר ⇒ **בלי לשנות התנהגות-שער**, רק מקור-הנתון והקול.

---

## סעיף-4 · תחזוקה שוטפת (הסדר מפסיקת-27.08)

1. **DB** — נעילות ממתינות, `idle in transaction`, שלמות-ברים (78 ברי-RTH/יום),
   חותמות כפולות, התאמת `UNIQUE` בכל טבלת-כתיבה חמה.
2. **מחזיק-שני `S1-BINARY`** — האם נותר מחזיק-מצב שני שמחזיר מצב-מומצא בלי סימון.
3. **שערים** — `flag_guard` + `task_log_guard` + שומרי-התנהגות; `RULED_FLAGS.yaml` תקין
   כ-YAML (`T-168` פתוח: `yaml.safe_load` נופל).
4. **ריסטארט-צל** — מותר אחרי 23:00 **רק** אחרי אימות `position_qty=0` + סלוט-פנוי
   מ-`sierra_state` בן <2ש'. `snapshot` לפני כל נגיעה ב-`.env`/LaunchAgent/DLL.
5. **`resolver` / `WHATIF`** — כולל 22 עסקאות-הצל של היום.
   ⚠️ **11 מתוכן הן אותה תזת-לונג `18:06-19:10` שכולן עצרו בסטופ** ⇒ תזה אחת שנספרה 11 פעמים,
   לא 11 טעויות. הלייב לקח אחת כי הסלוט תפוס — **מגבלה מבנית**. אסור להסיק
   "לייב טוב פי-11" ואסור לצטט `shadow -1,813.75` כהפסד-מדיניות.
6. **`fills` / Redis** — Redis לא מותקן על ה-MacBook (נמצא 28.08, אומת שוב 01.09),
   אין לו מנוי בקוד ⇒ **רעש, לא תקלה**. לא להתקין.
7. **דוח-EOD לטלפון-מייקל** — בעברית, קצר, עם המספרים של סעיף-1 והסייג של החשבון-המשותף.

---

## כללים (מחייבים)

- **Rule-5: פלט-גולמי.** "תוקן"/"עובד" בלי `command + raw output` = לא-בוצע.
- **קומיט-פר-סעיף.** `git pull` לפני, `commit`+`push` אחרי.
- **`snapshot` לפני כל שינוי out-of-git** (`scripts/mems26_snapshot.sh "why-label"`).
- **בלי הדלקות שלא נפסקו.** כל דגל חדש = `RULED_FLAGS.yaml` + פסיקה-קיימת, אחרת OFF.
  `STALL_EXIT`/`OPPOSITE_EXIT_V1` נשארים OFF (op=EXIT שבור). `T-212` (`STRUCTURE_EXIT_FAILBREAK_V1`)
  **לא להעלות ל-live** — `n=1`, ממתין לפסיקת-מייקל.
- **`git stash` אסור על קובץ-לוג חי** (קטם 1,843 שורות `OPS_LOG` היום ב-21:45).
- **`NOT-DONE` מפורש** בסוף — מה לא בוצע ולמה. סעיף בלי `NOT-DONE` ייחשב לא-מאומת.
- **אפס נגיעה בפוזיציות.** החשבון משותף עם אתי.
