## הזמנת-בוקר 11.09 (שישי) — מאחדת את 354c8c9f · 70b3e27c · 598cdd06 · 141edaae · 46072c56. **אפס קומיטים של cc מאז 10.09 20:30** — כל החמש פתוחות. פתיחה 16:30, ריסטארט-קדם-פתיחה 15:45 (מתוזמן), שער = `fwd_harness.py` בלבד. אין pytest כבד אחרי 16:10.

### ⚠️ מספור
המספרים T-298…T-303 שנתתי אתמול **תפוסים** ב-TASK_LOG ע"י שורות אחרות (המשמרת של הערב). המיפוי: T-298→**T-313** · T-299→**T-314** · T-300→**T-318** · T-301→**T-317** · T-302→**T-315** · T-303→**T-316**. חדשות: **T-311**, **T-312**. לא לפתוח שורות עם המספרים הישנים.

### תיקון ל-T-297 (שלי)
`backend/main.py:366/395/964` **כן** כותב `day_type_machine._opening_gate_bars = _cls_rth_bars`. הטענה "הרג'יסטרי ריק בתהליך-החי" הוסקה ולא נדגמה; אתמול `ot` הגיע (ORR 16:45, AUCTION_IN 17:05). מה שנשאר מ-T-297 = **בדיקת-חיווט** שבונה את השער דרך החיווט של main.py (לא דרך ההרנס) — נכנס ל-T-312.

### סדר-היום (לפי עדיפות, כל פריט = קומיט משלו + שורת TASK_LOG + LOG חתום ב-LIVE_CHANNEL עם פלט גולמי)
**T-311 · P0 — שחרור `live_slot` כשהחשבון-שלנו שטוח, בלי תלות בפוזיציה זרה** ([[T-309]] של אתמול: הסלוט הוחזק 18:55→22:09+ כי `T-43c` בודק `Sierra position_qty==0` ואתי הייתה בפוזיציה; היום הסלוט פנוי רק בזכות ה-reset). המפריד לבעלות = `POSITION_CHANGE.order_id` מול רשומת ה-Sierra-IDs שלנו (זיכרון 10.09), לא אריתמטיקת-נטו. + לולאת-שחרור (לא רק `on_trade_close`) + `[StuckSlot]` מ-ALERT ל-פעולה כשהבעלות מוכחת. golden: 10.09 18:55:17 ⇒ slot freed ⇒ ה-shadow שאחריו (19:05 / 20:30) נבחן ללייב. אין FLATTEN, אין op=EXIT.

**T-313 · P0 — מסירת-תווית בנעילת-IB** (היה T-298, מפרט מלא ב-354c8c9f): בנעילה הקריאה של S1 מחליפה מיד את התווית הפרוביזורית; ההיסטרזיס רק locked→locked. golden: 10.09 17:30:09 ⇒ `Normal` ⇒ INITIATIVE_LONG 7606.5 חסום `kind`.

**T-314 · P0 — נעילת סוג-הפתיחה + הפרכה** (היה T-299, ב-354c8c9f): קפוא ב-16:45; AUCTION ממשיך לקרוא עד 17:30; הפרכה = סגירה מעבר לקיצון-שנדחה; `opening_locked_at`/`negated_at` בתצוגה. golden: 10.09 16:45 ORR קפוא עד 17:20, ואז AUCTION_OUT.

**T-315 · P1 — נפילה-הלאה בשרשרת-S2 + `starved_by` בפאנל** (היה T-302, ב-141edaae). golden: 10.09 19:05 ו-20:30 ⇒ DOUBLE_TOP_AA_SHORT emitted.

**T-312 · P1 — אינדקס-החיווט + בדיקה-חיה (בקשת-מייקל 11.09: "צריך להיות אינדקס מלא על כל המערכת כדי שהכל יעבוד כראוי")**
מה יש: `SYSTEM_INDEX.md` (קבצים) · `SOURCE_OF_TRUTH.md` (מקורות-נתונים) · `FLAG_INDEX.md` (דגלים) · `wire_guard.py` (חתימות-פונקציות) · `detector_contract_guard.py` (מפתחות-בופר) · `config_consumer_guard.py` (שדות-YAML). **מה אין: מפת-הזרימה-בזמן-ריצה** — מי כותב מפתח/תכונה, מי קורא, והאם הוא מלא בתהליך-החי. זו המחלקה של T-297/T-309/T-268.
1. `scripts/gen_wiring_index.py` → `docs/WIRING_INDEX.md`: לכל קישור — **כותב** (file:line) → **מפתח** → **קורא** (file:line) — עבור: מפתחות `system_registry` (main.py:1326-1332) + תכונות שנקראות עליהם (`_opening_gate_bars`, `ib_locked`, `opening…`) · מפתחות `cross_context` (`tpo_system`, `woodies_system`, `_cls_rth_bars_ref`…) · `app.state.*` · מפתחות `setup[...]`/`info[...]` שהגלאים מנפיקים והשער צורך · `_load_sierra_tpo()`. `--check` נכשל על **קורא בלי כותב** ועל כותב בלי קורא (dead wiring). KEEP/ADAPT: להרחיב את `gen_index.py`/`sot_map_guard.py` אם מתאים, לא מחולל שלישי במקביל.
2. `GET /api/v9/wiring` — לכל קישור: `populated` · `len` · `age_s` · `source`. Rule 1: ריק = `null`, לא ערך מומצא.
3. `scripts/wiring_guard.py` — רץ ב-`fire_drill.py` ובשער-הקדם-פתיחה (`mems26-preopen-gate`): נכשל כשקורא-מפתח ריק בשלב שבו הוא חייב להיות מלא (למשל `_opening_gate_bars ≥ 3` ב-16:45, `tpo_system.ib_locked` ב-17:30).
4. **מבחן-חיווט**: `fwd_harness.py --variant head` בונה את השער **דרך פונקציית-החיווט של main.py** (לחלץ אותה לפונקציה אם היא inline) — חייב להיכשל על HEAD אם יש קורא-בלי-כותב.

**T-316 · P1 (מדידה בלבד, אין דגל) — kind לפי מיקום-העוגן** (היה T-303, מפרט ב-46072c56): הטבלה admitted/blocked לכל תבנית×סוג-יום, Σ$ (לייב = ברוקר; תאומים = סימן), רשימת "היה נחסם→עובר / היה עובר→נחסם". **מייקל רואה מספר לפני שמשהו נדלק.** מבטל את עריכת-המפה-לפי-שם ב-T-318.

**T-317 · P2 — מימוש על כפולה מאושרת לפני-T1 (פסיקת-מייקל 10.09 18:45, ב-598cdd06)**: ריפליי (`measured:`) → בנייה תחת `STRUCTURE_EXIT_REALIZE_V1` → RULED_FLAGS. אופציה ב' צל.

**T-318 · P3 — CEILING_FLIP/DOUBLE_* ב-EDGE_FADE** (היה T-300): **מוקפא** — נבלע ב-T-316; ה-golden (17:20 CEILING_FLIP_LONG admitted) עובר ל-T-316.

**מהמשמרת של הערב (שורות קיימות, לפי "הצעד-הבא" שלהן):** [[T-308]] (3 באגים ש-T-307 חשף) · [[T-310]] (נוסח-החסימה של `entry_guard` שקרי) · [[T-304]] (System6 target_divergence — אזעקת-שווא מבנית, להשתיק במקור).

### כללים
הרנס על 08-03 · 08-04 · 09-09 · **10.09** אחרי כל פריט; לוג גולמי; אפס נגיעה ב-`RISK_BUDGET_USD`/`RISK_MIN_CONTRACTS`/`FIXED_CONTRACTS_5`; snapshot לפני `.env`; ריסטארט רק ב-15:45 המתוזמן ורק אם ההרנס ירוק — אחרת ה-HEAD של אתמול (b2a3f46d) נשאר, ומייקל מחליט על החימוש.
