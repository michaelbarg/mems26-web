# ⚡ בצע עכשיו — הכנת יום שני (כסף חי, אוטונומי)
**פסיקת-מייקל 24.08: "לבצע הכל ולהכין את יום שני."** §0 חל: להכריע, לתעד, להמשיך.
**זה מבטל כל סדר קודם. המחקר הסתיים — עכשיו קוד.**
קרא ראשון: `CURSOR_BRIEF_MEMS26_v3.md` (המצב המדויק).

## 0 · לפני הכל
`git add -A && git commit && git push` את מה שכבר בעץ (classifier_core, replay_dalton,
test_binary_convergence). אין להשאיר עבודה לא-מקומטת.

## 1 · אמת-בנתונים — הראשון, כי כל השאר נשען עליו
- **`pnl_reconcile` שבור**: משווה מחירי-**פקודה** ולא מילוי · מדלג על FLATTEN/BE ·
  מסמן `incomplete` במקום לצעוק. ב-21.08 הכריז "0 סטיות" כשהפער ‎$96.25. **לתקן
  ולהריץ על כל עידן-הלייב** — לדווח את הסכום המתוקן.
- **זמנים כטקסט ב-11 טבלאות** (32.8% מחוץ לרשת-5-דקות) + **3 הגדרות-תאריך סותרות**
  (IL/UTC/ET). לאחד למקור אחד; לדווח כמה שורות הושפעו.
- **A3** בידוד pytest מפיד-ההחלטות (8% זיהום; 396 שורות HFE סינתטיות).
- **A7** ארכוב-לוגים לפני גלגול.

## 2 · בטיחות — חוסמי-GO לאוטונומי
- **desync** "POSITION NOT IN BOOKS" (2-6 חוזים) — פוזיציה שלא-בספרים = **חריגה**
  (פסיקת-מייקל: אין יותר מסחר ידני), לא "של מייקל". חקירה + התראה.
- **NAKED_STOP W3** — לאמת מול `orders[]` בפועל, לא מול גיל-ACK.
- **334 בליעות-חריגה שקטות** בנתיב-החי — לפחות: לוג-WARNING + מונה לכל בליעה
  בנתיב-הירי/מילוי/יציאה/התראה.
- **A5** חסינות-ריסטארט ל-S1 (הידרציית זיכרון-ייצוב מה-DB) + מונה cold_start מה-DB.
- אזעקת-קיפאון: לקרוא heartbeat ולא טבלה (26 אזעקות-שווא).

## 3 · שחרור-ראייה (בלי לשחרר שערים)
- **A4 שלמות טבלת-ההרשאות** — `RE_PULLBACK` KeyError (70 תאים במקום 84).
- **להסיר `HLST` מהשרשרת** — ValueError נבלע, **ומדכאת את `DOUBLE_BOTTOM` באותו בר**.
- אחרי שניהם: **להריץ מחדש את מבחן-הכיסוי של 3 הימים** ולדווח את האחוז החדש
  (בסיס: 16 מ-98 = 16%). זה מדד-ההצלחה של השלב.

## 4 · חי ביום שני (מאומת-replay, פסיקות קיימות)
- **P3** — 1.5×ATR (**לא** 0.5) · מרווח ‎≥1.5R ליעד המבני הבא · **סטופ-מבני אחד
  לממוצע** (חישוב הממוצע מראש; win 45%→55%) · **איסור-חיזוק-בקצה** (31/49 נכנסו
  בקיצון ומתו).
- **סגירת T-10** (10 דק' לפני סגירת-RTH).
- **`TREND_STEP_ENTRY_V1` → צל** + עדכון RULED_FLAGS (IS ‎+$4,354 / OOS ‎−$2,011;
  מאז-הדלקה ‎−$198; הוצאה מהסלוט ‎+$1,332).

## 5 · ממתין לפסיקת-מייקל — לא להדליק, להכין
- **`S2_CVD_DETECTION_V1`** — התיקון **מדליק מסנן שמעולם לא רץ** (0/76 חלונות) ⇒
  פחות ירי ⇒ שינוי-משטח-סיכון. לתקן, למדוד ברפליי, **ולהשאיר כבוי** עד פסיקה.
- **`DOUBLE_TOP_AA`** — סבילות-Adam נפרדת + חוצץ-32; ‎+$582 מדוד. להכין, לא להדליק.

## 6 · שער-מוכנות (חובה לפני שאתה אומר "סיימתי")
`flag_guard` PASS · `mems26_verify` · `mems26_arming_gate` · `fire_drill` 🟢 ·
שורת `[boot] logging OK` עם ה-PID החי · `sierra_state`: is_sim=0, armed=1, שטוח,
מרג'ין מספיק · אפס עסקאות-לייב פתוחות.
**ריסטארט: היום (ראשון) בכל שעה. ביום שני — לא אחרי 16:10 IL.**

## 7 · דיווח
`LIVE_CHANNEL.md` חתום + `TASK_LOG.md` + `STATUS_BOARD.md` (ממצא→תיקון→ראיה).
לכל פריט: פקודה + פלט גולמי (חוק-5). מה שלא הספקת — לרשום מפורשות כ"לא בוצע".
**כלל §D בתוקף:** אין שינוי-התנהגות ל-.env בלי `week_replay` — דלתא חיובית **וגם**
חציון-יום לא-נפגע.
