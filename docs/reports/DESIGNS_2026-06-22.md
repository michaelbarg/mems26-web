# MEMS26 · טיוטות-עיצוב — 2026-06-22

עיצובים מוכנים-להפיכה-לפרומפט לבעיות שאומתו היום (`PATTERN_EOD_2026-06-22.md`).
**לא מומש דבר — design בלבד.** כל פריט: שורש · שינוי מוצע (קובץ/שכבה) · regression · סיווג trading-logic(→Michael)/display-safe · הצלבת-Sierra.
**D-IDs חדשים היום: D24 (ברים-מושחתים), D25 (all-short audit), D26 (שער-כיוון+opening-lag), D27 (שער-מיקום-REACTIVE).** מתמשכים מעודכנים: D1/D2/D9/D11/D19/D23.

> **תזכורת standing-decision (CLAUDE.md, 2026-06-08):** chop-gates (S2 `choppiness_ok`, Layer-0 `chop_state`) ו-COT/AMT (S2⟂S3) **מנוטרלים בכוונה**. **אף עיצוב כאן אינו מציע לאפס flag ללא sign-off של Michael.** **D18 (שער-צ'ופ) — לא רלוונטי היום:** יום-מגמה רחב (69.5pt, `chop_state=FOUND`) ⇒ אין ראיה-חדשה לטובת re-enable.

> **🔴 הקשר-קריטי לקריאת כל העיצובים:** היום היה **יום-מגמה-יורד מלא** (הזנה חיה כל-הסשן — ≠06-19), והמערכת **הפסידה −$1,311.63 ב-19 SHORTs**. שני שורשים: **(1) ברים-מושחתים בבוקר (D24)** שייתכן הדליקו **(2) פתיחה נגד-מגמה (D26)**; ו-**(3) over-firing של REACTIVE מהשפל (D27)**. **עדיפות-העל:** D24 (data-quality) — כי אם הסיגנלים-הבוקר נולדו מבר-מושחת, זה תיקון-ingest, לא תיקון-לוגיקת-ירי.

---

## D24 — חדש 🔴🔴 (source-of-truth → trading-impact): פיצול-מקור-בבוקר (חשד גלגול-חוזה) + ניקוי דליפת-orphan

**שורש (מאומת מ-3 endpoints):** ביום-הראשון-אחרי-פקיעת-יוני, נתוני-הבוקר **סותרים**: `v9_trades`/`chart/bars5min` = ~7590/BLUE/HFE-fired; `v9_bars_5min_woodies` (קנוני-ל-S4 לפי `SOURCE_OF_TRUTH.md`, `bad_count=0`) = ~7540/RED/no-HFE; הפער גָדֵל 28→48pt לאורך-הבוקר (לא offset-קבוע); אחה"צ מתכנס ~7540. **בנוסף:** `chart/bars5min` נושא זבל מ-`v9_bars_5min_continuous` (close=6274 — מתועד ב-SoT כיתום). **שתי בעיות:** (א) דליפת-orphan ל-endpoint-התצוגה; (ב) פיצול-מקור ביצוע↔סיגנל אפשרי.
**שלב-0 (verify, Rule 2 — CC, חובה-לפני-כל-תיקון):** מול `~/SierraChart_Data/v9_export/` — **איזה חוזה (יוני-פוקע/ספטמבר) כל-מקור קרא ב-08:30–09:30?** האם העסקאות נורו על חוזה אחד והסטאדי על אחר? (זו ההכרעה בין "באג-roll" ל"באג-source-selection"). **ההשערה-המקורית "בר-מושחת→CCI→S4" נסוגה** — S4 לא קורא מ-`chart/bars5min`.
**שינוי מוצע (אחרי-אבחון, שתי שכבות):** (א) **source-selection:** ודא שכל-המקורות (trades, chart, woodies, study) נעולים על **אותו חוזה-פעיל** סביב-גלגול; הוסף roll-aware contract-pin. (ב) **endpoint-hygiene:** `/chart/bars5min` יפסיק לאחד/ליפול-ל-`v9_bars_5min_continuous` (היתום); לקרוא רק מהמקור-הקנוני (`v9_bars_5min_woodies`, fallback ל-`v9_bars_5min`) — תואם §C ב-STATUS_BOARD-היום (strip-v2 fix).
**Regression:** replay session-גלגול ⇒ trades/chart/woodies מציגים אותו-מחיר (±tick) באותו ts; `chart/bars5min` `bad_count=0` (אין 6274); אין פער-בוקר.
**סיווג:** **endpoint-hygiene (ב) = display/safe.** **source-split (א) = trading-impact → blocker-LIVE** (ביצוע וסיגנל על מכשירים-שונים) — אך **אבחון-Sierra ראשון** קובע אם (א) בכלל קיים.
**Sierra/לוג:** `v9_export` 5min JSON + contract-id סביב 08:30–09:30 (CC, §הצלבה 1). **תלוי-ב-`SOURCE_OF_TRUTH.md` (06-22) + memory roll-day caveat.**

## D25 — חדש 🔴 (detection-audit): מקור ההטיה החד-כיוונית 19/19 SHORT (I-41)

**שורש (מאומת מ-`v9_trades`, איתן — בלתי-תלוי בסתירת-המקורות):** 19/19 עסקאות SHORT, 0 LONG לאורך כל-הסשן. אף LONG לא נורה גם כש-ה-trade-record מתעד `woodies_trend=BLUE`.
**שלב-0 (verify — CC):** `five_min/setups` + `woodies/signals` intraday-log לבוקר — **האם בכלל היו LONG-setups armed** (ZLR-UP/INITIATIVE_LONG/REACTIVE_LONG) שלא נותבו? שתי השערות: (א) ה-detectors מזהים רק SHORT-setups (באג-סימטריה); (ב) bias מבני (day_type=UNKNOWN→Variation / opening_type=OPEN_DRIVE) משתק את צד-ה-LONG. **בדיקה מבחינה ביניהן.**
**שינוי מוצע (תלוי-אבחון):** אם (א) — תיקון-סימטריה ב-detector (long-mirror של ה-short-trigger); אם (ב) — לוודא שה-gate לא חוסם כיוון-שלם על day_type-לא-ודאי.
**Regression:** replay יום-עם-ראלי-פתיחה ⇒ ≥1 LONG-setup armed מזוהה; יחס long/short fires לא 0:N על יום דו-כיווני.
**סיווג:** **detection-audit** (read-first); תיקון = **trading-logic → Michael**.
**Sierra:** CCI-14/TCCI בבוקר — האם היה ZLR-UP חוקי (Rule 2).

## D26 — חדש 🔴 (trading-logic → Michael): אימות שער-כיוון + opening-lag כתנאי-קדם

**שורש (תלוי-בוקר, contested — ראה D24):** 5 SHORTs בבוקר (188/190/191/193/194) רשמו ב-`v9_trades` `woodies_trend=BLUE` + `day_type=UNKNOWN` (08:30–08:55) = **נגד-מגמה לכאורה, −$516**. **אך המקור-הקנוני `/woodies/chart` מראה RED לאותו חלון** ⇒ ייתכן with-trend. `TREND_DIRECTION_GATE` (`343a41b`, default-OFF) **לא חסם**. **⚠️ עיצוב-זה תלוי בהכרעת-D24** (איזה מקור-בוקר נכון).
**שלב-0 (verify, Rule 5 — CC):** (1) מצב-ה-flag כרגע (`restart→default-OFF`?). (2) **על-מה השער נסמך** — `woodies_trend` או `day_type`? אם `day_type` והוא היה **UNKNOWN בפתיחה (I-1)** ⇒ השער היה **עיוור-מבנית** ב-30-הדקות-החשובות. **השרשרת: I-1 → שער-מגמה עיוור → counter-trend חמק.**
**שינוי מוצע (שתי שכבות):**
  (א) **שער-כיוון על `woodies_trend`** (לא רק day_type): `direction==SHORT && woodies_trend==BLUE` (או הפוך) ⇒ `blocked_by="COUNTER_TREND"`. עוקף את עיוורון-I-1.
  (ב) **תנאי-קדם opening:** עד שיש day_type/trend ודאי (לא UNKNOWN), שער-המגמה משתמש ב-`woodies_trend` כ-fallback.
**Regression:** replay 06-22 ⇒ 188/190/191/193/194 נחסמות (`COUNTER_TREND`), היום משתפר ל-−$795.63; וידוא ש-INITIATIVE-הזוכות (195/196, נורו ב-RED/GRAY) **לא** נחסמות.
**סיווג:** **trading-risk → Michael (strategic stop).** הפעלת flag = שינוי-משטח-סיכון — אין לאפס ללא sign-off.
**Sierra:** `woodies_trend` בבוקר מול CCI-Sierra (Rule 2) — **חסום מאחורי D24** (אם הבוקר-הקנוני RED ~7540, ה-BLUE שבעסקה שגוי וה-CF מתבטל).

## D27 — חדש 🔴 (trading-logic → Michael): שער-מיקום ל-REACTIVE + de-dup

**שורש (מאומת):** 10 REACTIVE_SHORTs (−$888.76, −6.68R) נורו כולם באזור 7529–7543 = **SHORT אל-תוך תמיכה** (קרוב ל-LOW 7527.25). `REACTIVE_LOCATION_GATE` (`5c7a4b2`, default-OFF: "blocks REACTIVE on wrong side of POC") **לא היה פעיל**. בנוסף: **duplicate id199≡200** (ר' D23).
**שלב-0 (verify — CC):** מצב-flag `REACTIVE_LOCATION_GATE`; ומיקום-POC בחלון 10:10–14:45 (האם ה-SHORTs באמת היו מתחת/בצד-השגוי של POC?).
**שינוי מוצע:** (א) הפעלת `REACTIVE_LOCATION_GATE` (אחרי אימות-POC); (ב) **de-dup 1-לכל-(system×pattern×direction)-בחלון-T** (משותף עם D23-ב).
**Regression:** replay 06-22 ⇒ REACTIVE מתחת-POC נחסמות; 197 (ראשון) נשמר, 199/200/202/205 נחסמות; וידוא 203 (זוכה, רצה 48 ברים) — לבדוק אם נשמרת תחת שער-המיקום.
**סיווג:** **trading-risk → Michael.** flag-enable = שינוי-משטח-סיכון.
**Sierra:** POC/key_levels intraday (Study) מול entries (Rule 2).

## D23 — מעודכן 🔴 (trading-adjacent → Michael): duplicate-fire-guard + כיול cluster_guard

**שורש (מאומת היום):** **id199≡id200 byte-זהים** (10:20, entry 7537.75, REACTIVE_SHORT B_RVOL, −$127.50 כ"א). ה-`cluster_guard` (D-037) **קיים ב-gateway** (`recent_attempts`/`cluster_guard_active`/`block_remaining_sec`) **אך לא מנע** את הכפילות. + 6 gap-ids (D9).
**🟢 התיקון כבר קיים (STATUS_BOARD 06-22):** `DEDUP_FIRE_GUARD` **נבנה היום במיוחד לתפוס 199/200** ("חוסם אותו-סיגנל-פעמיים-תוך-30ש'", 4 טסטים, flag-OFF). הודלק ב-SHADOW ~12:1x — **אחרי** הכפילות ב-10:20 ⇒ לא הגן. **⇒ אין צורך לבנות מחדש.**
**שלב-0 (verify — CC):** מצב-flag `DEDUP_FIRE_GUARD` עכשיו (restart→OFF?); חלון-30ש' מספיק ל-same-bar-dup? `cluster_guard`(D-037)/`ssv`(D-049)/`cooldown` קיימים גם — לאמת כיול.
**שינוי מוצע:** **אמת+הפעל `DEDUP_FIRE_GUARD`** (אחרי restart-מאמת) — לא לבנות חדש. אם חלון-30ש' לא תפס same-bar ⇒ להוסיף key `system+pattern+direction+entry±ε`.
**Regression:** replay 199/200 ⇒ השני `blocked_by="DUPLICATE"` (טסט קיים); 0 כפילויות פוסט-close.
**סיווג:** **trading-adjacent → Michael.** נספח ל-`feedback_full_decision_pipeline_wiring`.
**Sierra:** —

## D11 — מתמשך 🔴 (trading-risk → Michael): חיווט `sizing → contracts → pnl_usd` (I-34)

**שורש (מאומת שוב):** כל הפסד = 3-חוזים ללא תלות ב-sizing (id211 sizing=2 → −$191.25=×3, לא ×2; id195/196 sizing=1 → ×3). string `"half"` (S4) וגם numeric (S2) — שניהם 3.
**שלב-0 (verify, Rule 5 — CC):** האם נתיב-הביצוע (`daytype_playbook.decide()`/sizer → `trading_gateway.route_setup` → `trade_manager`) מצמצם חוזים, או רק נרשם ב-metadata? shadow-PnL נומינלי-3-בכוונה או dead-wire?
**שינוי מוצע:** חיווט מלא של `sizing`→`contracts`→`pnl_usd` (כולל מיפוי `"half"`→מספר); +איחוד type (D12/I-35).
**Regression:** עסקת-sizing=2 ⇒ `pnl = risk×5×2`; "half" ⇒ חוזים מצומצמים. anti-tautological (לא רק metadata).
**סיווג:** **trading-risk → Michael** (השער-לגודל לא מגן ב-LIVE). חוסם-LIVE.
**Sierra:** —

## D2 — מתמשך 🟡 (display-safe): ספירת-ירי אמיתית ב-`build/pattern-status` (I-31)

**שורש (מאומת):** `S2=21 + S4=8 = 29` נטען מול **19** ב-`v9_trades` (12/7). יחס S2 1.75× · S4 1.14×.
**שינוי מוצע:** מקור-ספירה יחיד = `COUNT(v9_trades WHERE session_date=today GROUP BY firing_system)`; להסיר את המונה-הכפול/buffer ב-pattern-status.
**Regression:** `pattern-status.fired_today_count == COUNT(v9_trades)` פר-מערכת.
**סיווג:** **display-safe** (אך מרעיל-כיול). → לתקן-לפני-מסקנות-כמותיות.
**Sierra:** —

## D1 — מתמשך 🟡 (display-safe): איחוד `pnl_r` (I-22)

**שורש:** id203 +$51.25→`41`, id195 +$94.38→`4.19`, id209 +$10.62→`8.5` — בסיסים שונים בין branches (T1/BE מול T2/trail); נתיב-הפסד תקין (−1.0).
**שינוי מוצע:** נוסחה אחת `R = pnl_usd / risk_$` בכל הנתיבים, `risk_$ = |entry−stop0|×$5×contracts`.
**Regression:** כל עסקה: `pnl_r == pnl_usd/risk_$` (±ε), זוכה-וגם-מפסידה.
**סיווג:** **display-safe** (ה-CF משתמש ב-netR-מקומי). → D1.
**Sierra:** —

## D9 — מתמשך 🟡 (integrity): gap-ids + sequencing (I-32, החמיר ל-6)

**שורש:** ids 189/192/198/204/207/210 חסרים (6, מול gap-יחיד 06-19). + duplicate 199/200.
**שלב-0 (verify — CC):** `SELECT id,entry_ts FROM v9_trades WHERE session_date='2026-06-22' ORDER BY id` + לוג-backend — insert-fail-שקט / rollback / triple-fire-מגולגל?
**שינוי מוצע:** תלוי-אבחון — או החלקת-sequence, או תיקון-insert-fail (+`logger.warning`, "no silent failures").
**Regression:** אין gap-ids ביום-בדיקה; כל-fire ⇒ row או reject-מתועד.
**סיווג:** **integrity** (חשד trading-adjacent אם fires אבדו). → CC.
**Sierra:** —

---

## נספח — סיווג מהיר

| D | בעיה | סיווג | פעולה |
|---|------|-------|-------|
| **D24** | I-40 פיצול-מקור-בבוקר (roll) | source-of-truth→trading-impact | **CC-verify-Sierra (עדיפות-על)** |
| **D26** | שער-כיוון counter-trend | trading-risk (**חסום מאחורי D24**) | **Michael sign-off** |
| **D27** | שער-מיקום REACTIVE | trading-risk | **Michael sign-off** |
| **D25** | I-41 all-short bias | detection-audit | CC-audit→Michael |
| **D23** | duplicate-fire (199/200) | trading-adjacent | **אמת+הפעל `DEDUP_FIRE_GUARD`** (קיים) |
| **D11** | I-34 sizing→PnL | trading-risk | **Michael** (חוסם-LIVE) |
| **D9** | I-32 gap-ids ×6 | integrity | CC-verify-DB |
| **D2** | I-31 fired-count | display-safe | תיקון-ישיר |
| **D1** | I-22 pnl_r | display-safe | תיקון-ישיר |

**עדיפות-יישום מומלצת:** D24 (אבחון-Sierra — קובע אם D26 בכלל רלוונטי) → D11 (חוסם-LIVE) → D26+D27 (שערי-מסחר, Michael) → D9/D23 (integrity) → D1/D2 (display).
