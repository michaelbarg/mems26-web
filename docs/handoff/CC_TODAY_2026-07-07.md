# CC work order — TODAY before RTH 16:30 IL (2026-07-07)

Owner: Claude Code (Sierra machine — can read the Message Log + rebuild the DLL; Cowork can't).
Discipline: Rule 5 (paste raw command+output, never relay "done"), Rule 2 (verify before trust),
snapshot before any .env/DLL/LaunchAgent change (`scripts/mems26_snapshot.sh "label"`).
Fix BUGS, KEEP SAFETY (−$400 halt · R:R gate · entry-confirm · caps · 2 contracts stay ON).

## STEP 1 — DLL rebuilt with the SendOrders fix + clean config  [CRITICAL — gates everything]
The fix `9d314d0` (auto-match `sc.SendOrdersToTradeService = !GlobalTradeSimulationModeIsOn`)
is committed 07-07 06:42 but the deployed binary is from 07-06 21:11 → the fix is NOT live.
1. `git log -1 sc_study/MES_AI_DataExport.cpp` → confirm HEAD is 9d314d0.
2. `./scripts/build_monolithic_cpp.sh --deploy` (auto-snapshots) → Remote Build → reload study.
3. Paste `ls -la ~/SierraChart*/ACS_Source/MES_AI_DataExport_64.dll` — the binary mtime MUST be
   AFTER 07-07 06:42. If not, it's the old binary — do not proceed.
4. **Config restore (Michael ruled 07-07):** snapshot (`scripts/mems26_snapshot.sh "cont_trend_restore"`)
   then set **`CONT_TREND_FILTER=1`** in `.env` (back to its standing default — Cowork disabled it
   07-06 on a mis-diagnosis; the real blocker was A7). Restart backend
   (`launchctl kickstart -k gui/$UID/com.mems26.backend`) and confirm via the `[env_loader]`
   boot-line that `CONT_TREND_FILTER=1` is live (not `ps eww`). Paste the boot-line.

## STEP 2 — SIM proof, 1 then 2 contracts  [Michael point 2, OPEN_ITEMS A1]
Sim Mode ON, 0 open positions. SendOrders auto-flips to 0 (sim) → orders must now submit.
1. Fire a **1-contract** SIM test BUY (command block in `CC_SIM_TEST_BUY_2026-07-06.md` §2).
   Expect: Message Log `ORDER_SUBMITTED` (NOT `GENERAL_ERROR_OR_NOT_ENABLED`) +
   `trade_fills.json` ENTRY fill. Paste raw both.
2. Fire a **2-contract** SIM test BUY (`contracts:2`) → both submit + fill. Paste raw.
   This is Michael's "2 successful contracts through the code, outside market hours."
3. Confirm the backend cancels/releases the gateway `live_slot` cleanly after the test closes
   (no stuck slot — yesterday's 293/296 phantom). Paste the slot state.

## STEP 3 — verify the routing bugs are actually gone (live path)
1. **A7 fire_setup** (task #23, OPEN_ITEMS A6): CC added the V2-risk_points stop fallback
   (90567fb, woodies_system.py:616). Confirm a ZLR fire now ROUTES — no
   `[Woodies] FIRE DROPPED ... failed_stages=['A7']`. Paste the routing log for one ZLR.
2. **Reconcile wiring** (A2): confirm `reconcile.py` runs in the poll loop (or say it doesn't).

## STEP 4 — refresh the index (2 days stale)  [Michael point 4]
`python3 scripts/gen_index.py` + `python3 scripts/gen_flag_index.py --check` → commit the
refreshed `SYSTEM_INDEX.md` / `_INDEX.md` / `FLAG_INDEX.md`. Report the diff summary.

## STEP 5 — day-type breakout diagnosis  [Michael point 1, task #22 — secondary, don't block Step 2]
Yesterday the day stayed "Normal" until ~18:00 while price broke IB and rallied 7567→7584,
auth-SKIPping 3 conf-0.80 INITIATIVE_LONG. Fix the RECOGNITION (not the safety):
- **B3** intra-bar/close-confirmed IB-break reclass — upgrade Normal→Variation/Trend on the
  confirming close, not one bar later.
- **B2** volume-acceptance: typical-price all-or-nothing under-counts straddle-bar above-IB
  volume (ignored 12,262 vol); full-session denominator too large → volume-at-price + rolling.
- Build flag-OFF, SHADOW/backtest first, report net-R before proposing enable. Do NOT loosen
  R:R or entry-confirm to force fires.

## Report back (Rule 5)
For each step: raw command + output. A NOT-DONE section for anything incomplete. Do NOT flip
Sim Mode OFF / send real money until Step 2 is green AND Michael signs off.
