# תור-הלילה 04.09 (אחרי 23:00) — השורש: **מחיר-הכניסה הרשום מוטה ⇒ כל מדידה שמעליו מוטה**

> נכתב ע"י `cowork-dev` ב-23:10 IL אחרי בדיקת-`git log` — **אפס פעילות-cc**:
> 15 הקומיטים האחרונים כולם `cowork`, האחרון `4fb1bc31` ב-`22:47`; אין שורת-claim
> ב-`LIVE_CHANNEL`; אין תהליך `claude`/`cc` חי (‏`ps` ⇒ רק Claude.app של ה-desktop).
> ⇒ `cowork` תופס claim ומבצע בעצמו, לפי פסיקת-מייקל 27.08.

## מצב-פתיחה (נמדד 23:08, לא מזיכרון)

```
/api/v9/health            http=200  t=0.0038s  {"status":"ok","version":"v9.0.0"}
sierra_state.json         age=0.84s  is_sim=0  last_price=7717.25
                          position_qty=0  orders=0  acct_daily_pl=-116.25
                          daily_total_qty_filled=50
/api/v9/system6/diagnose  active=false · slot_trade_id=null · "live slot is free"
backend PID 60439         up since 15:14:23 (7h53m)
DB                        waiting_locks=0 · idle_in_txn=1 (0.1s) · dup_ts=0
                          v9_bars_5min_woodies rows_today=266 · max_ts=23:05:00+03
```

⇒ **החשבון שטוח · אפס פקודות · הסלוט פנוי** ⇒ **ריסטארט מותר** (אחרי 23:00).

### ספרי-הלייב של היום (גולמי)

```
 998 | ZLR              | LONG  | 7745.75 → 7740.25 | STOP_FILL | -137.50 | r=-1.00
1008 | INITIATIVE_SHORT | SHORT | 7717.00 → 7724.75 | STOP_FILL |  +32.50 | r=+0.20
1069 | INITIATIVE_LONG  | LONG  | 7730.50 → 7730.75 | STOP_FILL |  +62.50 | r=+0.48
1073 | GB100            | SHORT | 7725.50 → 7725.25 | STOP_FILL |  +70.00 | r=+0.45
1076 | (SCALE_IN child) | SHORT | 7718.75 →   NULL  | PLACE_REFUSED:t3_missing | CANCELLED
1141 | INITIATIVE_LONG  | LONG  | 7724.25 → 7719.25 | STOP_FILL | -125.00 | r=-0.95
                                                    books_sum = -97.50
                                                    pnl_sierra NOT NULL = 0 (מתוך 6)
```

**מול הברוקר:** `TradeActivityLog_2026-09-04_UTC.37138283.data` ⇒ 22 רשומות
`Closed Trade Profit/Loss`, `SUM = -116.25` = `acct_daily_pl` בדיוק.
⇒ **הספרים מייפים ב-$18.75.** ו-`daily_total_qty_filled=50` = 5 עסקאות × 5 חוזים ×
(כניסה+יציאה) ⇒ **כל הפילויים היום שלנו — אתי לא סחרה** (אין לגלגל את הפער אליה).

---

## 🟡 סעיף-1 (עדיפות עליונה) · T-256 — **אין לנו מדידה פר-עסקה מול הברוקר**

**מה שכן מוכח, וזה מספיק כדי להצדיק את הסעיף:** `pnl_sierra` הוא `NULL` ב-**6/6**
עסקאות-הלייב של היום ⇒ **אין שום נתיב שמשווה עסקה-שלנו למה שהברוקר באמת שילם.**
כל מה שיש הוא בדיקת-אגרגט, ו-04.09 מדגים למה היא חסרת-ערך.

**מה ש*לא* מוכח — ואני מוריד אותו בעצמי מ-🔴:** ההסבר שנתתי ב-`18:58`
("מחיר-הכניסה הרשום מוטה"). ראה התיקון-העצמי מיד להלן **לפני** שנוגעים בקוד.

### ⚠️⚠️ תיקון-עצמי 23:12 — הראיה שעליה בניתי את T-256 **אינה קבילה**. לקרוא לפני הכל.

בהודעה למייקל ב-`18:58` הצגתי טבלת "ברוקר מול ספרים" שהסיקה היסט קבוע במחיר-הכניסה.
**חזרתי ליומן עכשיו וקראתי את הרשומה במלואה — והשדה שציטטתי הוא לא מה שחשבתי:**

```
Auto-trade: MESU26_FUT_CME[M]  5 Min  #8 | MES AI Data Export v9.4.3-chart5 |
SellEntry | Bar start date-time: 2026-09-04  10:05:00.000 | Last: 7716.25 |
AOE=true | AOU=true | Server side bracket order
```

`Last:` הוא **טקסט-ההגשה של הפקודה** — המחיר-האחרון-שנסחר **ברגע ששלחנו**, כלומר
**קלט**, לא **תוצאה**. **זה אינו מחיר-הפילוי.** ⇒ ההסבר המתחרה — "המחיר זז בין
ההגשה לפילוי" (החלקה רגילה, לא באג) — **מסביר את אותם המספרים בדיוק**, ולכן
הטענה "מחיר-הכניסה הרשום מוטה" **אינה מוכחת**.

**וגם המספרים עצמם שציטטתי היו שגויים.** הפירוק הנכון (‏`strings | grep -oE`, כל
5 העסקאות, כולל `#1141` שהשמטתי):

| # | כיוון | `Last:` בהגשה | ספרים `entry_price` | הפרש |
|---|---|---|---|---|
| 998 | LONG | 7745.00 | 7745.75 | +0.75 |
| 1008 | SHORT | 7716.25 | 7717.00 | +0.75 |
| 1069 | LONG | 7730.00 | 7730.50 | +0.50 |
| 1073 | SHORT | 7725.50 | 7725.50 | 0.00 |
| **1141** | LONG | 7724.50 | **7724.25** | **−0.25** |

**`#1141` הופך את הסיפור:** ההפרש **מחליף סימן**. היסט-רישום קבוע היה נותן סימן
אחיד; החלקה סביב מחיר-ההגשה נותנת בדיוק את זה. ⇒ **הנתונים תומכים בהחלקה, לא בבאג.**

⚠️ **וזו הפעם השלישית היום** שאריתמטיקה שהסתדרה הובילה אותי לשורש שגוי
(‏`[[feedback_arithmetic_fit_is_not_causation]]`). **T-256 יורד מ-🔴 ל-🟡 "לא-מוכח".**

### המשימה — לבנות את המדידה שתכריע, לא להניח שורש

1. **למצוא את מחיר-הפילוי האמיתי.** ביומן **אין** שדה `FillPrice` פשוט. מה שיש:
   `Fill of InternalOrderID: <id>` (42 רשומות, נותן רק מעבר-כמות),
   `Requested Price:` (27), ו-`Closed Trade Profit/Loss:` (22 — **הסמכותי**).
   ⇒ **המפתח היחיד הוא `InternalOrderID`.** לבנות join `InternalOrderID` ↔ `order=`
   שלנו — **לא** לפי קרבת-מחיר (זו בדיוק הייתה `T-229`).
2. **המבחן המכריע:** אחרי ה-join — האם `entry_price` שלנו שווה למחיר-הפילוי
   של פקודת-הכניסה? **אם כן ⇒ T-256 נסגר כלא-באג** וההפרש כולו החלקה.
   **אם לא ⇒ יש באג, ואז יש לו ראיה אמיתית.**
3. **מקור-האמת ל-P&L הוא `Closed Trade Profit/Loss`** — 22 רשומות,
   `SUM = -116.25` = `acct_daily_pl` **בדיוק**. לאכלס ממנו את `pnl_sierra`
   (היום `NULL` ב-**6/6** השורות) — **לא** לדרוס `pnl_usd`.
4. **וזה הממצא שכן עומד, והוא לא תלוי בשורש:** סך-היום מסכים **רק במקרה**.
   ספרים `-97.50` מול ברוקר `-116.25` ⇒ פער `$18.75` שקיים גם כשהאגרגט "מסתדר"
   בימים אחרים. ⇒ **בדיקת-אגרגט מול הברוקר היא בדיקה חסרת-ערך** — נדרשת
   השוואה **פר-עסקה** דרך ה-join של (1). זה התוצר המחייב של הסעיף.
5. ⛔ **`backfill` למפרע אסור** — דורש פסיקת-מייקל. לבנות כלי עם `--dry-run`
   שמדפיס דלתא לכל שורה, **לא להריץ**.

---

## 🟠 סעיף-2 · T-254 + T-257 — אותו שורש: מיפוי רגל→חוזים שבור

שני הפריטים חולקים באג אחד: **המערכת לא יודעת כמה חוזים כל רגל סגרה.**

**T-254 — הרקונסיילר מחסיר חוזה אחד לכל יעד-שנפגע**, בלי קשר לגודל-הרגל,
ו**רגל-T0 אינה בטאפל כלל**. ב-`#1073`: `T1` סגר **2** חוזים, הוא הוריד **1**.
⇒ הוא חושב 4, המציאות 2 ⇒ `DIVERGENCE: Records ≠ reality!` על **כל** עסקת-לייב,
כל יום (אותה חתימה על `#877 · #953 · #968 · #971 · #1008 · #1069 · #1073`).

⚠️ **ומה שאין להגזים בו:** הדגל **יכול** לחסום כניסות, אבל
`grep "T-43.*POSITION_MISMATCH"` ⇒ **0** בכל הלוג, אי-פעם. ⇒ **פער-מדידה, לא כסף** —
אבל הוא רעש שמטביע אזעקות אמיתיות, וזה בדיוק מה שגרם לשתי אזעקות-שווא היום.

**T-257 — הפאנל שמייקל מסתכל עליו מנופח פי 4–9.** השדה המוצג הוא **`total_pnl`**
(‏**לא** `upnl_usd` — זה fallback מת שלא מוצג לעולם). שתי תקלות בו:
(א) `mobile_monitor.py:260` ‏`_n = max(contracts, 1)` — הגודל **המקורי**, לא הנותר;
(ב) `_lvl_hit = [False, ...]` — **רגל-T0 מקודדת-קשיח כלא-מומשה** ומתומחרת כפתוחה.
נמדד: הפאנל `69.2 / 65.6 / 65.6` בזמן שסיירה `15.0 / 10.0 / 7.5`.
**תצוגה בלבד — אין צרכן במסלול-מסחר או סיכון** (שני מופעים בקוד, שניהם בפאנל).

### המשימה

מקור-אמת אחד לכמות-פר-רגל — ספר-`T-62` (`T1 2c` וכו') מול `position_qty` + `orders[]`.
לתקן את שניהם מאותו מקור. **טסט-מוטציה:** להחזיר את ההחסרה-הקבועה-1 ⇒ נופל.

---

## 🟠 סעיף-3 · T-252 — בורסטי-צל + עלות-מעבד-הבר (⚠️ סעיף שכבר הפלתי פעמיים)

**מה מוכח:** 167 עסקאות-צל היום, מהן **129 = אותו דפוס יחיד** (`S2_DELTA_DBL_LONG`)
שירה ב-**11 דקות בלבד**, בשלושה בורסטים (`18:42-18:44` · `21:22-21:34` · `22:27-22:28`),
עד **20 עסקאות כמעט-זהות ב-60 שניות**. ⇒ **שכפול, וחתימה זהה ל-31.08.**
⇒ **אסור לצטט את מספר-הצל של היום** בלי דדופ-לפי-תזה.

**⚠️ ומה שכן הפרכתי בעצמי — לא לבנות עליו:**
פירוק עלות-`on_bar` פר-שעה מול מספר-הפתוחות **מפריך** את "הצל הוא השורש":

| שעה | פתוח | ממוצע | שיא |
|---|---|---|---|
| 19 | **70** | **2,579ms** | 13,371ms |
| 20 | **70** | 1,217ms | 11,474ms |
| 21 | 22 | 570ms | 4,741ms |
| 22 | **47** | **426ms** | 4,313ms |

`19` ו-`20` עם **אותן 70** נבדלות **פי-2**; `22` עם **47** **זולה** מ-`21` עם **22**.
⇒ **מספר-הפתוחות אינו מנבא את העלות — קיים גורם שני שלא זוהה.**
וסייג על המדידה עצמה: הממוצע חושב **רק על ברים שחצו את סף-האזהרה** (התפלגות קטומה).

**המשימה:** (א) לתקן את שכפול-`S2_DELTA_DBL` (שומר-כפילות פר-תזה, לא פר-ביצוע);
(ב) **טיימר-בקוד** פר-עסקת-צל ב-`bar_level_detector` — **לא** הסקה-מהלוג;
(ג) הגנה שתמנע ממספר-הצל להשהות ירי-לייב.

---

## 🟡 סעיף-4 · רעש-לוג שמטביע אזעקות אמיתיות (T-255 + T-249)

- **T-255:** ה-AUTO-CORRECT של סטיית-יעדים ירה **39,639** פעם מאז 31.08 ו**נדחה
  ב-100%** — הוא שולח `op=MODIFY_TARGET` **שאינו קיים ב-DLL**. אפס סיכון (שום דבר
  לא מגיע לשוק), אבל 40 אלף שורות-אזהרה. **לחסום בשורש** (לא להנפיק op שאינו קיים).
- **T-249:** הודעת-החסימה `structural_targets_wrong_side` מדפיסה את היעדים
  ה**מתוקנים** במקום אלה שנפסלו ⇒ הלוג **משקר על מי נחסם ולמה**. באג-דיווח בלבד.

---

## סעיף-5 · תחזוקה שוטפת (הסדר מפסיקת-27.08)

1. **DB** — נעילות (`0` בפתיחה), `idle in transaction`, שלמות-ברים, חותמות כפולות
   (`0` בפתיחה), התאמת `UNIQUE` בכל טבלת-כתיבה חמה.
2. **מחזיק-שני `S1-BINARY`** — האם נותר מחזיק-מצב שני שמחזיר מצב-מומצא בלי סימון.
3. **שערים** — `flag_guard` + `task_log_guard` + שומרי-התנהגות; `RULED_FLAGS.yaml`
   תקין כ-YAML (‏`T-168` פתוח).
4. **ריסטארט-צל** — מותר: אומת `position_qty=0` + `orders=0` + סלוט-פנוי מ-`sierra_state`
   בן `0.84s`. `snapshot` לפני כל נגיעה ב-`.env`/LaunchAgent/DLL.
5. **`resolver` / `WHATIF`** — ⚠️ **לדדופ קודם** (סעיף-3). 6 עסקאות-צל עדיין פתוחות
   ב-23:08. אסור להסיק "לייב טוב/גרוע פי-N" ממדגם משוכפל.
6. **`fills` / Redis** — Redis לא מותקן על ה-MacBook, אין לו מנוי בקוד ⇒ **רעש,
   לא תקלה**. **לא להתקין.**
7. **`T-248`** — `docs/reports/OPS_LOG_<date>.md` הוא git-tracked ונכתב בזמן-ריצה
   ⇒ חוסם `git pull` בכל RTH. מעקף: `--rebase --autostash`. תיקון-שורש: `.gitignore`.
8. **דוח-EOD לטלפון-מייקל** — בעברית, קצר, עם מספרי סעיף-1 והסייג של החשבון-המשותף.

---

## כללים (מחייבים)

- **Rule-5: פלט-גולמי.** "תוקן"/"עובד" בלי `command + raw output` = **לא-בוצע**.
- **הסבר-מתחרה לפני שורש.** לפני שכותבים "השורש הוא X" — לנסח הסבר מתחרה
  ולהריץ עליו את הנתונים. אריתמטיקה שמסתדרת **אינה** מאמתת סיבה
  (‏`[[feedback_arithmetic_fit_is_not_causation]]` — הופעל היום פעמיים, הפיל שני שורשים).
- **קומיט-פר-סעיף.** `git pull --rebase --autostash` לפני, `commit`+`push` אחרי.
- **`snapshot` לפני כל שינוי out-of-git** (`scripts/mems26_snapshot.sh "why-label"`).
- **בלי הדלקות שלא נפסקו.** כל דגל חדש = `RULED_FLAGS.yaml` + פסיקה-קיימת, אחרת OFF.
  `STALL_EXIT`/`OPPOSITE_EXIT_V1` נשארים **OFF** (‏`op=EXIT` שבור מ-13.07).
- **`git stash` אסור על קובץ-לוג חי.**
- **`NOT-DONE` מפורש** בסוף — מה לא בוצע ולמה. סעיף בלי `NOT-DONE` = לא-מאומת.
- **אפס נגיעה בפוזיציות.** החשבון משותף עם אתי.
