# MEMS26 · טיוטות-עיצוב — 2026-06-15

עיצובים מוכנים-להפיכה-לפרומפט לבעיות שאומתו היום (`PATTERN_EOD_2026-06-15.md`).
**לא מומש דבר — design בלבד.** כל פריט: שורש · שינוי מוצע (קובץ/שכבה) · regression · סיווג trading-logic(→Michael)/display-safe · הצלבת-Sierra ל-CC.

> **תזכורת standing-decision (CLAUDE.md):** chop-gates (S2 `choppiness_ok`, Layer-0) ו-COT/AMT (S2⟂S3) **מנוטרלים בכוונה**. אף עיצוב כאן אינו מציע להחזירם. שערי-ה-regime/trend המוצעים (D3/D5) הם **חדשים ונפרדים** — לא re-enable של שער מנוטרל.

---

## D1 — I-22 🔴 (display-safe): `pnl_r` ÷$1.25 — שבור **רק בנתיב-T1/BE**

**שורש (8 דוגמאות היום):** ids 91,92,100,102,104,105,106,115 — כולן `pnl_r(API) == pnl_usd ÷ 1.25` בדיוק (יחס 0.8 קבוע, =1/tick_value-ish). נתיב-T2 תקין (id93 = +0.93R נקי). ⇒ שני branches במחשבון-R; נתיב-T1/BE-exit מחלק ב-tick_value במקום בסיכון-הכניסה.
**שינוי מוצע:** דרך `SYSTEM_INDEX` לאתר את ה-branch של T1/BE-close (כנראה trade-closer/gateway) וליישר לנוסחת נתיב-T2: `pnl_r = pnl_usd / (abs(entry − stop_initial) × 5.0 × contracts)`. backfill לשורות-T1 ההיסטוריות או סימון `r_known_bad=true`.
**Regression:** עסקה סינתטית T1+BE (entry 7618.5/stop 7623.25/3 חוזים/pnl_usd 26.25) ⇒ `pnl_r≈0.37`, **לא 21**; עסקת-T2 קיימת (id93) ⇒ ללא-שינוי; STOP מלא ⇒ −1.0.
**סיווג:** **display/analytics-safe.** scope: branch אחד. **חוזר מ-D1(06-12) — לא מומש עדיין.**
**Sierra:** לא נדרש.

## D2 — I-31 🔴 (display-safe, עדיפות-גבוהה): ספירת-ירי שקרית ~2× ב-`build/pattern-status`

**שורש (מאומת היום):** `S4 fired_today_count=39` + `S2=15` = 54, מול **26** ב-`v9_trades` (18+8). יחס ~2× ⇒ ה-count סופר פר-חוזה או פר-סיגנל-כולל-blocked, לא עסקאות-distinct.
**שינוי מוצע:** ב-build_status inspector — להחליף את מקור-הספירה ל-`SELECT firing_system, pattern_id, COUNT(*) FROM v9_trades WHERE date=today AND state='CLOSED'/'OPEN' GROUP BY 1,2`. אם המקור הוא טבלת-סיגנלים (כולל blocked) — להפריד `signals_count` מ-`fired_count` ולתייג בבירור.
**Regression:** ביום עם 18 S4 + 8 S2 ⇒ endpoint מחזיר 18/8; cross-check `Σfired == COUNT(v9_trades WHERE date=today)`.
**סיווג:** **display-safe** — אבל **חוסם-כיול**: כל מסקנת-תבניות כמותית מורעלת עד שזה מתוקן. **חוזר מ-D2(06-12) — לא מומש.**
**Sierra:** לא נדרש (DB בלבד).

## D3 — I-26/I-28 🔴 (trading-logic → Michael): veto-מגמה לקבוצת REVERSAL/counter-trend

**שורש (יום רביעי ברציפות):** נגד-המגמה −$581.65 (−4.79R) מול עם-המגמה +$345.95 (+2.07R). היום: HFE-SHORT ×4 (id93–96) לתוך דרייב-בוקר עולה (7599→7648). דפוס זהה 06-10 (HFE-LONG נגד-RED, I-26) · 06-11 (SHORT נגד-ראלי) · 06-12 (SHORT לתוך V-recovery).
**שינוי מוצע (ללא-שינוי מהותי מ-DESIGNS 06-11/06-12 §D3/D4):** ב-gateway — veto fire כש-`pattern_group ∈ {REVERSAL, counter-trend}` והכיוון **נגד** מגמה-יציבה. **תוספת קריטית היום:** "מגמה" = `woodies_trend` **או** `price_slope ≥ X pt על N ברים` — id93–96 ירו SHORT כש-trend עוד BLUE/GRAY אבל המחיר עלה 49pt; שער-צבע-בלבד יחמיץ. סף N≈6 ברים + slope ב-YAML.
**Regression:** replay 06-15 ⇒ id93–96/89/94/95 veto; id97/98 (ZLR-L עם-הדרייב) עוברים. + replays 06-10/11/12.
**סיווג:** **trading-logic — דורש הכרעת-Michael.** *זהו התיקון בעל יחס עלות/תועלת הגבוה ביותר; היה חוסך +$581.65 היום, ~$1,069 ב-06-12.*
**Sierra:** להצליב `woodies_trend`+CCI-14 בעת-הכניסה (id93–96) — האם Sierra הראה trend עולה שה-backend פספס.

## D4 — I-30 🔴 (trading-logic → Michael): cluster-guard

**שורש (חי היום):** HFE 94/95/96 על **stop משותף 7623.25** מתו יחד דקות אחרי id93 (−$525); TLB 108/109 (stop 7627.25); REACTIVE_S 110/112 (stop 7636). CF-cull: **+$694.05** (היום → +$299.60).
**שינוי מוצע (ללא-שינוי מ-§D3/D5 קודמים):** `MAX_CONCURRENT_PER_PATTERN=1` (YAML) + veto ב-gateway על כניסה-שנייה כש קיימת פוזיציה-פתוחה **אותה תבנית** או **אותו `stop_initial` (±tick)** תוך N דקות.
**Regression:** replay 06-15 ⇒ נשמרות id93/108/110, נחסמות 94/95/96/109/112; ימים קודמים (06-12 id69/70, 06-10 id45–49) ⇒ veto לשני+.
**סיווג:** **trading-logic — Michael.**
**Sierra:** לאמת את מקור ה-`stop_initial` המשותף (level/round-number) — דורש הצלבת רמות Sierra.

## D5 — חדש 🔴 (trading-logic → Michael): שער-regime/confidence לאחה"צ + הקשר-מיקום ל-REACTIVE

**שורש (חדש+חוזר):** range-אחה"צ 13:00–15:00 = −$595.65 (1W/7L). `day_type=Trend_Normal` נשאר נעול ב-`confidence=0.46`/`LOCKED_LOW_CONF` בעוד המחיר התכווץ ל-range 7615–7640 — והתבניות ירו reversal/reactive לשני הכיוונים לתוך רעש. בנוסף REACTIVE 3W/7L (גם ראש-מהלך id101/103).
**שינוי מוצע (שתי שכבות, שתיהן YAML):**
1. **regime/confidence gate (gateway):** כש-`day_type.confidence < C` (≈0.50) **וגם** טווח-ברים-אחרון מתכווץ (`range_N < ATR×k`) ⇒ half-size או skip לתבניות-reversal. *לא* chop-gate מנוטרל — מטריקה חדשה (confidence+range-compression), שער חדש.
2. **REACTIVE position-context (`five_min_system.py _detect_reactive`):** feature `pos_in_session_range` — REACTIVE_LONG ב->80% עליון של הרנג' / SHORT ב-<20% תחתון ⇒ skip/half. שמירה על עצמאות-S2 (Standing Decision S2⟂S3).
**Regression:** replay 06-15 ⇒ id108–114 (range, conf 0.46) skip/half; id101/103 (ראש-מהלך) skip; id97/98 (דרייב, conf בזמן-אמת) עוברים.
**סיווג:** **trading-logic — Michael** (מקטין ירי). חופף חלקית ל-D3 (range-אחה"צ כלל גם נגד-מגמה).
**Sierra:** לאמת ATR/range מ-Sierra ל-threshold.

## D6 — תפעולי/safe (יום רביעי ברציפות): סוכן-ה-DIAG-30-דק' לא רץ

**שורש:** אין `PATTERN_DIAG_` ל-06-11/06-12/06-15 — ה-trigger מת מאז 06-10. בלעדיו ה-EOD עיוור לנחסמים (אין reject_reason-intraday, אין CF-קלאסי).
**שינוי מוצע:** (א) לבדוק את ה-scheduled-task/LaunchAgent של ה-DIAG (נעצר אחרי 06-10? error? TZ?); (ב) guard-בוקר: אם עד 09:30 CT אין snapshot ראשון ⇒ נטיפיקציה ל-Michael מיידית (לא לחכות ל-EOD).
**Regression:** יום-המסחר-הבא מייצר ≥8 snapshots; היעדר-snapshot עד 09:30 ⇒ נטיפיקציה.
**סיווג:** **תפעולי/safe.** **חוזר מ-D6(06-12) — לא טופל; כעת חוסם 4 ימי-CF.**
**Sierra:** לא נדרש.

## D7 — I-33 חדש 🟡 (display-safe): רדאקציית-סוד רחבה-מדי בולעת שדות-דיאגנוסטיקה

**שורש (חדש מאז 06-12):** `day_type/state` + `pattern-status` מחזירים `session_min`/`session_date`/`rtb_session` כ-"[BLOCKED: Sensitive key]". matcher-הסוד תופס תת-מחרוזת (`session`/`date`) ובולע ערכים תמימים הדרושים לדיאגנוסטיקה (כולל הראיה ל-residual-I-1).
**שינוי מוצע:** לצמצם את ה-redactor מ-substring-match ל-**allowlist/denylist מדויקת** של מפתחות (password/token/secret/key בלבד), או להחריג מפורשות `session_min`/`session_date`/`rtb_session`.
**Regression:** קריאת `day_type/state` מחזירה `session_min` כמספר (לא "[BLOCKED…]"); מפתח-סוד אמיתי (אם קיים) נשאר חסום.
**סיווג:** **display-safe.**
**Sierra:** לא נדרש.

## D8 — I-3/I-13 🔴 (trading-logic → Michael): טבלת stop/target פר-תבנית×day-type — עדיפות-LIVE #1

**ללא-שינוי מ-DESIGNS 06-11/06-12 §D8.** ראיות-היום: **TLB negative-skew** (4W/3L אבל זכיות T1/BE זעירות +$23–37 מול הפסדים-מלאים −$67–101 ⇒ −$144.65) ⇒ ה-runner/BE לא מכויל; **HFE stops 12.75pt** ($191 risk) מול **ZLR/TLB 4–7pt**. `MEMS_MAX_RISK_POINTS` עדיין אחיד. **Michael מגדיר את הטבלה** (memory: `project_stop_target_placement_table`, `project_config_tunable_stop_exits_contracts`).
**Regression:** טבלה ב-YAML ⇒ כל תבנית×day-type מקבלת stop/T1–T5/contracts ייעודיים; replay מאמת ש-id89/94/95/96 לא היו מקבלים stop 12pt ביום-Trend.
**סיווג:** **trading-logic — Michael** (החלטת-מוצר, חוסם-LIVE).
**Sierra:** רמות-stop להצלבה מול Sierra.

## D9 — לאימות-CC (קטנים): I-32 · I-25 · readiness · I-1

- **I-32:** id 90 חסר ב-`v9_trades` (89→91). rollback/insert-fail שקט? sequence מול לוג ("no silent failures"). 🟡
- **I-25:** `limit=200`→422 (cap 100). enhancement: העלאת-cap או pagination ל-EOD; היום ללא-אובדן (26<100). 🟢
- **readiness=DEGRADED post-close:** reason `trend_state=GRAY` + check `dead: bars_5min` (severity info) — **post-close-benign** (RTH סגור 39 דק'). לאמת מחר תוך-RTH שאין stall-גשר אמיתי (אחרת I-21). 🟢
- **I-1 residual:** `vote_history=[]` נמשך; אך `opening_type=OPEN_DRIVE`+`day_type=Trend_Normal` **סווגו נכון** היום ⇒ S1-classification עובד, ה-residual הוא בעיקר vote_history+I-33-redaction. 🟡

---

**סדר-עדיפות מוצע ל-Michael:**
**D8** (טבלת stop/target — חוסם-LIVE) → **D3 (veto-מגמה — 4 ימי-ראיות, היה חוסך +$581.65 היום)** → **D5** (regime/REACTIVE — range-אחה"צ −$595.65) → **D4** (cluster-guard, +$694 CF) → **D2** (I-31 — מחזיר אמינות-ספירה לכיול) → **D1** (I-22 branch-T1) → **D7** (I-33 redaction) → **D6** (DIAG trigger — לפני יום-המסחר הבא!) → **D9**.

*שלושת השערים העצמאיים (D3/D4/D5) — כל אחד לבדו הופך את −$394.45 של היום לירוק (+$187 / +$300 / +$201 בהתאמה).*
