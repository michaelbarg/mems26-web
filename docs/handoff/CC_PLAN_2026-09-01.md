# תוכנית-ביצוע · 01.09 — בעיה → פתרון → אימות

**מאת:** cowork-dev · **אל:** cc-macbook
**שיטה (פסיקת-מייקל 01.09):** *"קודם זיהוי הבעיות, פתרון, ואז לתת לקלוד-קוד לתקן —
ואז תהיה לנו תוכנית עבודה שבאמת תצא לפועל, ואנחנו נמשיך לדבר על משימה חדשה."*

**‏🔴 הקובץ הזה מחליף** את `FIX_QUEUE.md` · `CC_WORKORDER_2026-09-01_EDGE.md` ·
`CC_WORKORDER_2026-09-01_NO_CHASE.md`. **מקור-האמת למשימות נשאר `TASK_LOG.md`** —
כאן יושב **מפרט-הביצוע** בלבד.

---

# מסלול א · **בצע עכשיו, אין צורך בפסיקה**

*הכל מדידה או תיקון-כשל. אפס שינוי במשטח-הסיכון. אפשר לעבוד ברצף בלי לחכות לאיש.*

| # | T | הבעיה — במשפט | הפתרון | אימות-סגירה |
|---|---|---|---|---|
| **א1** | **T-192** | `pnl_sierra` מולא **ידנית** היום. כלי שרץ פעם אחת ידנית יחזור להיות NULL | לחווט `sierra_pnl_reconcile` לריצה אוטומטית ב-EOD | יום חדש נסגר ⇒ `count(*) FILTER (WHERE pnl_sierra IS NULL AND mode='live')` = **0** בלי התערבות |
| **א2** | **T-193** | יציאות-FLATTEN אינן ביומן ⇒ **53 עסקאות בלי מספר-ברוקר**, וביניהן כל עסקאות ה-scratch | לגזור P&L מ**דלתת `sierra_state.daily_pnl`** סביב הסגירה. **רק** כשהדלתא מיוחסת לעסקה יחידה | יום עם FLATTEN ⇒ `sum(pnl_sierra) == daily_pnl` ±$0.01; **שתי סגירות באותו חלון ⇒ עדיין `incomplete`** |
| **א3** | **T-190** | `exit_fills` קיים ב-12 מתוך 413 ⇒ **אי-אפשר לדעת איך עסקאות-צל יצאו** | לרשום `exit_fills` גם על צל + `mae_pts`/`mfe_pts` על **כל** עסקה סגורה | צל שנגע ב-T1 ⇒ השדה מכיל T1 **וגם** `mfe_pts ≥ מרחק-T1`; ישר-לסטופ ⇒ STOP **וגם** `mae_pts ≥ מרחק-הסטופ` |
| **א4** | **T-191** | טסטי-`delta_dbl` ירוקים בלילה ואדומים בבוקר — תלויים במצב-שוק חי | לזייף את `awaiting_release`/`cold_start_guard`/`eod_entry_cutoff` כך שיבודדו **את השומר בלבד** | `17 passed` בשלוש הרצות בשעות שונות; **והסרת-השומר ⇒ עדיין נכשל** |
| **א5** | **T-152** | ה-scratch רץ רק על live/demo ⇒ הצל מנפח כל מפסידה ב-~$38 ⇒ **כל §D מוטה** | §D **מדמה** scratch על צל באותם ספים. **לדווח את גורם-ההטיה שנמדד** | §D על 31.08 מדפיס גורם-הטיה מפורש. **לא להפעיל scratch אמיתי על צל** |
| **א6** | **T-179ג** | 139 חסימות-כפילות מול 41 שורות-צל שנכתבו בכל-זאת ⇒ הלוח סופר דפוס×כפילויות | `quality.duplicate_of` — קיבוץ בחלון-30ש' `system+direction+pattern`, כמו הגייטוויי | ירי-חוזר תוך 30ש' ⇒ שורה אחת (או N עם `duplicate_of`); שני מועמדים **אמיתיים** >30ש' ⇒ **שתיים** |
| **א7** | **T-197** | `day_type=UNKNOWN` עד 10:00 ET ⇒ שתי עסקאות-פתיחה עיוורות | **אבחון בלבד:** מה השער עושה על `day_type` לא-מוכר — fail-open או fail-closed? | דוח שמצטט את הקוד. **אפס שינוי** |
| **א8** | **T-185** | ‏`market_ts`/`available_at` חסרים ⇒ סיבתיות-TPO אינה מדידה; `DALTON_EDGE` חי עליו | `ALTER` + backfill. **snapshot · `lock_timeout='3s'` · dry-run · לא בחלון-מסחר** | `count(*) WHERE market_ts IS NULL` = **0** ו-`WHERE created_at < market_ts` = **0** |

**סדר:** ‏א1 → א2 → א3 → א4 → א5 → א6 → א7 → א8.
**‏א1-א3 הם הבסיס** — בלעדיהם כל מספר-כסף שנצטט מחר אינו בר-הפרכה.

---

# מסלול ב · **בנה עם דגל כבוי · ההדלקה היא פסיקת-מייקל**

*כאן הכל שינוי משטח-סיכון. **בנה, בדוק, §D — ואל תדליק.***

## ב1 · `FADE_EDGE_RISK_V1` (‏T-194) — **התנאי לכל השאר**

**הבעיה:** ‏`SIZE_CAP_CUT` מגביל ל-3.1-4.0 נק'; עסקת-דהייה-בקצה דורשת 8-11.
⇒ **התקרה בוררת מבנית כניסות מאוחרות.**
```
31.08: risk 4.2>cap 3.1 · 16.8>3.3 · 25.0>3.9 · 25.2>4.0   → כולן 5→2
מנגד:  MAE של 3 הלונגים בשפל = 7.00 / 9.25 / 8.25 נק'
       סטופ מתחת לשפל-היום 7674.75 = 11 נק'
```

**הפתרון:** תקרה נפרדת כאשר **(א)** הכניסה בתוך סובלנות של VAL/VAH או של קצה-היום
**וגם (ב)** `family == "REV"` ⇒ תקרה `~1.15×(מרחק-לקצה)`, חסם עליון ~11 נק'.
בכל מקרה אחר — **התקרה הקיימת, ללא שינוי**.

**אימות דו-כיווני:** שחזור מועמדי 17:40/17:45 ⇒ סטופ ≥8 נק' וחוזים ≥2;
**ו-`#875`** (כניסה מאוחרת, סיכון-מבני 25.2 נק', **לא** בקצה) ⇒ **עדיין נחתך.**

**‏🔴 אם השני נכשל — עצור.** תקרה שמתירה 25 נק' על כניסה מאוחרת היא הרחבת-סיכון
עיוורת, לא תיקון.

## ב2 · `FAMIR`/`VEGAS` בפלייבוק (‏T-195) — **חסום ע"י ב1**

`FAMIR` ו-`VEGAS` הם `SKIP` **בכל שמונת סוגי-הימים**, בסתירה ל-`daytype_style.Normal`
של אותו קובץ (*"fade VA edges: SHORT only near VAH, LONG only near VAL"*).
עלות מדודה ליום אחד: **‎+$240.00 ו-+$217.50**.

**🔴 אל תשנה את ה-YAML לפני שמייקל פסק** — ייתכן שההשבתה מכוונת ואיני מכיר את הפסיקה.
**מה שכן לעשות עכשיו:** לחפש בהיסטוריית-git מתי ולמה הם הפכו SKIP-בכל-שמונה,
ולהביא את הממצא. **זה נתון שחסר לי כדי לשאול את מייקל נכון.**

## ב3 · סף-השחרור בקצה (‏T-196) — **חסום ע"י ב1**

`awaiting_release` דורש 2 שפלים-עולים ⇒ אי-אפשר לקנות שפל. חסם `REACTIVE_LONG`
(‏**+$88.13 לעסקה על כסף-ברוקר — הרווחי-ביותר במערכת**) ב-`7685.50`; MFE **+22.75 נק'**.

**הפתרון:** `1` שפל-עולה **רק** כאשר REV **וגם** בקצה. בכל שאר המקרים **נשאר 2**.

**אימות דו-כיווני:** ‏`REACTIVE_LONG @7685.50` בקצה ⇒ **עובר**;
‏`S2_DELTA_DBL_LONG` באמצע-טווח ⇒ **עדיין נחסם.** (אם הוא עובר — הסף רחב מדי.)

## ב4 · `NO_CHASE_V1` — **הורד בעדיפות, וזו הסיבה**

הרצתי אימות לפני שמסרתי: הגרסה שכוללת `Variation` **חוסמת דלי מרוויח**
(`n=15 · 53% · +$247.50`) **ומעבירה דלי מפסיד** (`n=12 · 33% · ‎-$398.75`).
שמונה המנצחות שהייתה הורגת — **כולן Variation**.
על `Normal` בלבד: `n=61 · 15% · ‎-$7,210` בספרים אך **`n=1` בכסף-ברוקר**.

⇒ **ב1-ב3 מטפלים באותו שורש מהצד החיובי** (לשחרר את מה שעובד) במקום מהצד השלילי
(לחסום את מה שלא), ויש להם ראיית-ברוקר. **מפרט מלא נשמר ב-`CC_WORKORDER_2026-09-01_NO_CHASE.md`
לעיון — אל תבנה אותו לפני שב1-ב3 נמדדו.**

---

# חוקים החלים על כל פריט במסלול ב

**דגל נפרד · ברירת-מחדל OFF **בקוד** (לא ב-`.env`) · flag-OFF זהה-בייט להיום ·
`RULED_FLAGS.yaml` באותו commit.**

**שלושה סוגי-טסטים, וכולם חובה:**
1. **פונקציונלי**
2. **בקרה (control)** — הוכח שאותו setup **כן** עובר כשהדגל OFF. יש **79 שערים**
   אחרים; בלי בקרה אינך יודע שהשער שלך עשה משהו. *(אמש הבקרה תפסה `cold_start_guard`
   ו-`eod_entry_cutoff`; הבוקר תפסה `awaiting_release`.)*
3. **מוטציה** — הסר את השומר ⇒ הסוויטה **חייבת** להיכשל. **צטט את שני הפלטים.**

**§D:** דלתא **וגם** חציון-יום. **‏🔴 לצטט `pnl_sierra` ולא `pnl_usd`** (‏T-192) —
ואם הוא NULL, לומר "לא-ניתן-להכרעה" ולא ליפול-אחורה בשקט.

# אסור

**להדליק דגל · לגעת ב-`.env` · לשנות `daytype_playbook.yaml` · ריסטארט 16:10-23:00 ·
לצטט "עבר" בלי פקודה+פלט · לדווח `sum(pnl_usd)` בלי לפצל `exit_price IS NULL` ·
להסיק מ-`exit_fills` בלי `IS NOT NULL` ובלי לדווח בסיס.**

# מסירה

דוח אחד: `docs/reports/CC_PLAN_2026-09-01.md` · פקודה+פלט לכל טענה ·
**סעיף `NOT-VERIFIED` מפורש** · `LIVE_CHANNEL` + `KIS` · `TASK_LOG` · `commit`+`push`.

**אחרי כל פריט במסלול א — `push`.** אל תצבור. cowork מאמת תוך כדי, לא בסוף.
