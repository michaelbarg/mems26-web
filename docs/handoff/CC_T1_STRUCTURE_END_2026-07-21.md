# CC — פסיקת-מייקל: T1 = סוף מבנה-הכניסה (ביטול החישוב הקיים) — עיצוב+בנייה הלילה

**פסיקת-מייקל (2026-07-21 ~18:15, בכתב, standing):**
> "הסיבה שמערכת 2 לא מאשרת עסקאות = חישוב הסיכון-סיכוי לא נכון. **T1 צריך להיות בסוף מבנה-הכניסה**,
> **לבטל את האופן שבו הוא מחושב היום**, ולחשוב על דרך נכונה יותר."

זו פסיקה חד-פעמית-וקבועה (נוהל 6920ef60): לבנות → לאמת → **להדליק בריסטארט-הבוקר בלי אישור-שני** +
רישום ב-RULED_FLAGS עם הפניה לפסיקה זו. **אפס-קוד תוך-מסחר — עבודה אחרי 23:00 IL בלבד.**

## ⛔ שער-ביקורת לפני ביצוע (מייקל 18:25): cursor עובר על המפרט ומאשר — רק אז cc מתחיל
**מייקל:** *"לפני — אני רוצה שקורסור יעבור על זה ויאשר. הוא עושה לנו סדר במערכת."*
**cursor:** בדוק את המפרט מול הקוד-בפועל והסדר שאתה בונה: (א) העיקרון structure_end_t1 עקבי עם עצי-ההחלטות,
טבלת-היעדים, ו-T2T3_NO_STOMP שהדלקת? (ב) מיפוי מקורות-ה-T1 שלם (S2 five_min + S4 patterns + rr_entry_gate)?
(ג) ה-fixtures מההחלטות-החיות נכונים? (ד) התנגשויות עם דגלים/פסיקות קיימים? (ה) משהו חסר/מסוכן?
**פורמט-אישור:** שורת-LOG `T1-SPEC: APPROVED` (או `CHANGES: ...` עם תיקונים קונקרטיים). רק אחרי האישור —
cc מבצע. אם שינית את המפרט — עדכן את הקובץ הזה באותו קומיט.

## הראיות החיות (היום, live) — אלה ה-fixtures
| החלטה | entry | stop_dist | T1_dist | R:R | תוצאה |
|---|---|---|---|---|---|
| 17:30:03 S4 GB100 LONG | 7532.25 | 23.25 | **0.25** | 0.01 | חסום rr |
| 17:45:05 S4 ZLR LONG | ~7531 | 7.50 | **3.00** | 0.40 | חסום rr |
| 17:45:07 S4 ZLR LONG | ~7531 | 8.25 | **2.25** | 0.27 | חסום rr |
ביום שהמסווג-הקנוני כבר קרא **Trend_Normal UP** — ZLR-עם-הטרנד נחסם על R:R מעוות. הסטופ מבני-נכון
(פסיקת-07-20, נשמר!); **ה-T1 הוא השבור** — ננעץ ברמה-סמוכה-זעירה במקום בסוף-המבנה.

## העיקרון הנפסק
- **סטופ** (קיים, לא נוגעים): קצה-מבנה-הכניסה בצד-הסיכון **+6T** (STRUCTURAL_STOP_ORIGIN_V1 ✅).
- **T1** (חדש): **הקצה הנגדי של אותו מבנה** — סוף מבנה-הכניסה בכיוון-הרווח.
  דוגמאות: pullback-continuation (ZLR/Flag/TLB) → T1 = קיצון-הסווינג שממנו ירד/עלה ה-pullback ·
  Double-Bottom/HnS → T1 = קו-הצוואר/גובה-המבנה · Reactive-fade בקצה-value → T1 = הקצה הנגדי של ה-value
  (POC/צד-שני) לפי טבלת-היעדים הקיימת. **אותו מבנה לסטופ ול-T1** — R:R הופך לגאומטריית-מבנה אמיתית.
- **כנות:** אם סוף-המבנה לא נותן מרחק (מבנה מוצה — T1_struct קרוב מדי) → **התבנית נפסלת בכנות**
  (reject "structure_exhausted"), לא נחסמת ב-R:R=0.01 מטעה. אין המצאת-יעד.
- T2/T3 נשארים לפי טבלת-היעדים פר-סוג-יום (T2T3_NO_STOMP קיים); רק ה-T1 מוחלף.

## המשימה (cc מבצע, cursor מאמת, cowork מאמת-סימטרית)
1. **מיפוי (קודם!):** כל מקורות-ה-T1 היום ב-S2 (`five_min_system.py`) + S4 (`woodies/patterns/*`,
   `TARGET1_TICKS` fallback-12, pattern_t1) + איפה rr_entry_gate קורא אותם. טבלת file:line ל-LOG.
2. **עיצוב:** כלל structure_end_t1 פר-משפחת-תבנית (CONT/REV/BREAKOUT) — טבלה קצרה ל-LOG (למבט-מייקל,
   לא לאישור-מחדש). כל סטייה מהעיקרון = שאלה למייקל לפני קוד.

### ✅ צ'קליסט-כיסוי-חובה (מייקל 18:35: "כל התבניות של וודי") — אסור לדלג על אף שורה
לכל תבנית: הגדרת "מבנה-הכניסה" שלה + מה סוף-המבנה (=T1) + fixture. תבנית בלי הגדרה = המפרט לא גמור.
| # | תבנית (מודול) | משפחה | "מבנה-הכניסה" להגדרה |
|---|---|---|---|
| S4-1 | **ZLR** (`zlr.py`) | CONT | pullback→קיצון-הסווינג שממנו |
| S4-2 | **TT** (`tt.py`) | CONT | להגדיר |
| S4-3 | **GB100** (`gb100.py`) | CONT/BREAKOUT | להגדיר (ה-fixture 17:30 קיים) |
| S4-4 | **VEGAS** (`vegas.py`) | להגדיר | להגדיר |
| S4-5 | **GHOST** (`ghost.py`) | REV | להגדיר |
| S4-6 | **FAMIR** (`famir.py`) | REV | להגדיר |
| S4-7 | **HTLB** (`htlb.py`) | להגדיר | להגדיר |
| S4-8 | **TLB** (`tlb.py`) | להגדיר | להגדיר |
| S4-9 | **HFE** (`hfe.py`) | מושתק (HFE_DISABLED=1) | **להגדיר בכל-זאת** — שיהיה מוכן ליום שיודלק |
| S2-1 | **FHB** | שעה-ראשונה | להגדיר |
| S2-2 | **REACTIVE_LONG/SHORT** | REV/fade | קצה-value→הקצה הנגדי (per targets_table) |
| S2-3 | **INITIATIVE_LONG/SHORT** | CONT | להגדיר |
| S2-4 | **DOUBLE_BOTTOM_EE / DOUBLE_TOP_AA** | REV | גובה-המבנה/קו-צוואר |
| S2-5 | **HNS_TOP** (+bottom אם קיים) | REV | קו-צוואר→גובה-הראש |
**שלמות:** cc מריץ grep על כל pattern-id שנפלט בקוד (S2+S4) ומוכיח שאין תבנית מחוץ לטבלה (חוק-5 — הדבק
את ה-grep). כל "להגדיר" מתמלא בשלב-העיצוב; cursor מאשר את הטבלה המלאה.
3. **בנייה:** דגל `T1_STRUCTURE_END_V1` (OFF עד ריסטארט-הבוקר). smallest-correct-change בנקודת-חישוב-ה-T1;
   בלי לגעת בסטופ/T2/T3/שערים.
4. **טסטים:** fixtures דטרמיניסטיים משלוש ההחלטות של היום (הברים האמיתיים סביב 17:30+17:45) —
   assert: ZLR-17:45 עם T1=סוף-מבנה עובר rr (או נפסל-בכנות אם המבנה באמת מוצה — לפי הדאטה, לא הנחה) ·
   GB100-17:30 לא מייצר R:R=0.01 · תבנית-ללא-מרחק → structure_exhausted.
5. **RULED_FLAGS:** `T1_STRUCTURE_END_V1: expected "1"` + ציטוט-הפסיקה. הדלקה בריסטארט-הבוקר (עם Task#5
   ו-TS-gate אם נפסק) — ריסטארט אחד לכולם.

## משימת-לילה 2 — פסיקת-מייקל 22:18: כניסה נגד-הרחבה ביום-רוטציה = בקצה בלבד, אחרי בדיקה
**הפסיקה (בכתב, standing):** *"הייתי מעדיף שכניסה בסוג יום כזה תהיה כמו ההגדרה שנתנו — זה כניסה
ב-VAH שורט לאחר בדיקה."* כלומר ביום Variation/רוטציה-עם-הרחבה: **שורט נגד-ההרחבה רק ב-VAH ואחרי
בדיקת-הרמה** (טסט שנכשל — מחיר בודק את הקצה ונדחה), סימטרית לונג ב-VAL. כניסות באמצע-value נגד
ההרחבה — אסורות.

**הראיות החיות (ה-fixtures):** 4 שורטים חיים 20:00–22:00 IL, כולם הפסד (‎-$138.75):
| trade | תבנית | entry | מיקום מול VAH 7553.25 / POC 7547.25 | פסק-הגייט הנדרש |
|---|---|---|---|---|
| #449 | ZLR SHORT | 7548.75 | על ה-POC, אמצע-value | **BLOCK** |
| #452 | ZLR SHORT | 7546.00 | על ה-POC | **BLOCK** |
| #456 | ZLR SHORT | 7545.50 | על ה-POC | **BLOCK** |
| #445 | REACTIVE_SHORT | 7550.25 | 3pt מתחת ל-VAH | cc יכריע מהדאטה: הייתה בדיקת-VAH לפני? אם לא — BLOCK |

**Fixture חיובי (חובה! מייקל 22:20 "במצב זה השורט היה מצליח" — אומת במתמטיקת-ברים):** הבדיקה קרתה
בפועל: בר 19:55 IL דחף מעל VAH (H 7554.25 > 7553.25) **ונסגר מתחת** (7550.25) = דחייה. שורט@VAH 7553.25
אחרי הדחייה: T0 ‎-4 נפגע 20:15 · סטופ-מבני ~7555.75 לא-נגוע · POC 7547.25 נפגע · המשך עד 7542.25 (+11).
**השער חייב לאשר את הכניסה הזו** (allow) — טסט חיובי על הברים האמיתיים 19:50–20:15, לא רק חסימות.
זה מגדיר את תבנית-"הבדיקה" המכנית: חריגה-מעל-הקצה + סגירה-חזרה-מתחתיו בבר ה-5-דקות.

**ביצוע (cc, אחרי משימת-ה-T1):**
1. שורש: למה `DAYTYPE_LOCATION_GATE` כובה ב-07-20 (git log + LOG) — החשד: תוויות-מזוהמות דאז, סיבה שכבר לא קיימת. לתעד.
2. עיצוב: `location_gate` v2 — (א) REV-fade רק בקצה-הנכון (קיים); (ב) **חדש: תנאי-"בדיקה"** — הגדרה
   מכנית מהברים (למשל: בר קודם נגע/חרג מ-VAH ונסגר מתחתיו) — טבלת-הגדרה ל-LOG למבט-מייקל; (ג) CONT
   ב-Variation רק עם כיוון-ההרחבה (קיים בקוד). כל סטייה = שאלה למייקל.
3. חיווט: לוודא שהשער קורא תווית-חיה (לא None — העסקאות היום נרשמו `day_type_at_entry=None`; מצטלב
   עם משימה-15 של cowork — אם השורש שם, לתקן שם ולא לעקוף).
4. טסטים: 4 ה-fixtures מהטבלה דרך `route_setup` האמיתי (כמו הטסטים של T2T3/T1) — ‎449/452/456 חסומים.
5. דגל: `DAYTYPE_LOCATION_GATE=1` + עדכון-RULED (פסיקת 22:18 מחליפה את כיבוי-07-20) — הדלקה בריסטארט-הבוקר,
   ריסטארט אחד עם T1_STRUCTURE_END_V1.

## משימת-לילה 3 — פסיקת-מייקל 22:22: הסטופ מאחורי קיצון-המבנה, לא מאחורי נר בודד
**הפסיקה (בכתב, standing):** *"העסקאות היו טובות — הסטופים היו קרובים מדי ולא במבנה, או מאחורי
המחיר הנמוך או הגבוה ביותר באותו מבנה."* כלומר: הסטופ = מאחורי **הקיצון של מבנה-הכניסה כולו**
(השיא-הגבוה-ביותר לשורט / השפל-הנמוך-ביותר ללונג) + אופסט, **לא** מאחורי נר-הפריצה הבודד.

**⚠ מחליפה את פסיקת 2026-06-30** (`stop_anchors.yaml:80` — ZLR `breakout_bar/window:1`, שנפסקה אז כי
`cluster_low/4` הגיע לשפלים-ישנים → 16.5pt SKIP). ההגנה מפני הבעיה ההיא נשארת: `max_risk_points: 15`
+ פסילת-כנות אם המבנה רחב מדי. לרשום ב-RULED_FLAGS: "supersedes 06-30 breakout_bar anchor".

**הראיות החיות (מ-v9_trades, אותם fixtures של משימה-2):** קצה-המבנה היה 7554.25 (שיא בדיקת-ה-VAH),
אבל הסטופים ישבו בתוך המבנה — ‎#449 שורט 7548.75/סטופ 7550.5 (1.75pt) · ‎#452 7546.0/7549.5 (3.5pt) ·
‎#456 7545.5/7547.25 (1.75pt) · וגם S2 ‎#444 7550.25/7552.0 (1.75pt). כולם נדחו בתוך הרעש בזמן שהמחיר
ירד בפועל עד 7542.25 — הכיוון היה נכון, הסטופ היה השבור.

**ביצוע (cc — יחד עם משימת-ה-T1, זה אותו מזהה-מבנה):**
1. אותו מבנה מגדיר את שני הקצוות: T1 = הקצה בכיוון-הרווח (משימה-1) · **סטופ = מאחורי הקצה בצד-הסיכון**
   (+6T כמו STRUCTURAL_STOP_ORIGIN_V1). טבלת-הכיסוי של §צ'קליסט חלה גם על הסטופ — לכל תבנית: מה
   קיצון-המבנה שלה.
2. עדכון `stop_anchors.yaml`: ZLR (וכל תבנית עם window:1) → עוגן-מבנה; `max_risk_points` נשאר.
   S2 (`adaptive_stop`) — לוודא שגם שם העוגן הוא קיצון-המבנה.
3. טסטים: fixture ‎#456 על הברים האמיתיים — סטופ נדרש ~7555.75 (מאחורי 7554.25), לא 7547.25;
   assert שהעסקה שורדת את הברים 21:55–22:20 · fixture-רוחב: מבנה >15pt → פסילה כנה, לא סטופ-15 שרירותי.
4. דגל `STOP_STRUCTURE_EXTREME_V1` (OFF עד ריסטארט-הבוקר) + RULED_FLAGS — ריסטארט אחד עם השאר.

**שים לב לזוגיות סטופ↔T1:** סטופ רחב יותר + T1 רחוק יותר (סוף-מבנה) → ה-R:R נשאר גאומטרי-אמיתי.
לבנות את שניהם יחד ולהריץ את טסטי-ה-rr על הצירוף, לא על כל אחד לבד.

## משימת-לילה 4 — הדלקת `LSMA_FLAT_GATE_V1` (פסיקת-מייקל 07-08 קיימת-ועומדת; אושררה 22:36 "המערכת צריכה להמתין כשהמחיר תקוע")
**הפסיקה המקורית (07-08, standing):** *"לא רוצה שיהיו עסקאות כאשר הקו הזה אופקי מדי ללא זווית של
מגמה ברורה."* השער נבנה אז flag-OFF (`trading_gateway.py:919`, טסטים ב-`test_lsma_flat_gate.py`)
ומעולם לא הודלק. מייקל 22:36 (עם צילום המחיר התקוע 7544–7548): *"המערכת צריכה להמתין שהמחיר תקוע
באזור מסוים ולא להסתכן לפני שיש לה ודאות."* — אותה התנהגות בדיוק. לפי נוהל-הפסיקות-הקבועות:
מימוש-פסיקה-קיימת → הדלקה בלי אישור-שני.

**אימות-קונטרפקטואלי על הערב (cursor, מ-lsma_value ב-woodies, lookback-4, סף 0.25):**
‎#449 ‏slope ‎+0.078 → BLOCK · ‎#452 ‏‎-0.082 → BLOCK · ‎#456 ‏‎+0.110 → BLOCK ·
‎#444 @20:00 ‏‎-0.438 → pass (ירידה אמיתית אחרי דחיית-VAH — השער לא הורג את הכניסה הטובה).

**ביצוע (cc):**
1. `.env`: ‏`LSMA_FLAT_GATE_V1=1` (ברירות-מחדל: lookback 4, סף 0.25, scope ALL — בלי כיול-יתר הלילה;
   כיול-סף = משימת-SHADOW נפרדת אם יידרש).
2. טסט-fixture: 3 העסקאות התקועות של הערב (lsma_value האמיתי מה-DB) → blocked_by=lsma_flat ·
   ‎#444 → עובר. דרך `route_setup` האמיתי.
3. RULED_FLAGS: ‏`LSMA_FLAT_GATE_V1: expected "1"` + ציטוט 07-08 + אשרור 07-21 22:36.
4. **לא** להדליק את `LAYER0_CHOP_GATE`/`S2_CHOPPINESS_GATE` — נשארים STANDING-OFF (פסיקת 06-08 בתוקף;
   ל-Layer0 גם סיכון-deadlock של קריאות-HTTP-עצמיות ו-`v9_chop_score` ריק — לא מוכן).

## גבולות
אפס שינוי תוך-מסחר · rr_entry_gate נשאר (הוא יקבל T1 וסטופ אמיתיים) · חוק-5 על הכל.
(הערה: "הסטופ-המבני לא נוגעים" מהנוסח המקורי בוטלה בפסיקת 22:22 — משימה-3 לעיל.)

## ✅ ביקורת-cursor (18:05 IL) — T1-SPEC: APPROVED עם 3 תוספות-חובה (נכללות במפרט, cc מבצע איתן)

**אומת:** ה-fixtures נכונים — שלוש החסימות קיימות ב-`gateway/decisions` בדיוק כמתואר (פלט גולמי ב-LOG):
GB100 14:30:03Z R:R=0.01 (T1_dist=0.25/stop=23.25) · ZLR 14:45:05Z R:R=0.40 · ZLR 14:45:07Z R:R=0.27,
כולן `min=0.65`. העיקרון עקבי עם: סטופ-מבני (לא נוגעים) ✓ · T2/T3 מטבלת-היעדים + T2T3_NO_STOMP ✓ ·
נוהל-הפסיקות-הקבועות (בנייה→אימות→הדלקת-בוקר בלי אישור-שני) ✓ · אפס-קוד-תוך-מסחר ✓.

**תוספת-חובה 1 — שני דורסי-T1 בגייטוויי חסרים במיפוי (אותה משפחת-באג כמו T2/T3-stomp):**
אחרי שהתבנית קובעת t1, **שני בלוקים בגייטוויי דורסים אותו** לפני rr_entry_gate:
(א) `DAYTYPE_TARGETS_STRUCTURAL=1` → `resolve_structural_targets().t1_price` דורס `setup["t1"]`
(trading_gateway ~1195-1250; זה כנראה המקור של GB100 T1=0.25 — C1 על shelf-סמוך, תקרית-333 חוזרת);
(ב) `PATTERN_T1_OVERRIDE` מ-targets.yaml (פסיקת-07-10, MFE-based pts) → דורס t1 שוב (~1253-1279).
**כלל-קדימות נפסק:** כש-`T1_STRUCTURE_END_V1=1` — T1-סוף-מבנה **מנצח**; שני הבלוקים מדלגים על t1
(t2/t3 המבניים נשארים כרגיל). בלי זה — ה-T1 החדש נדרס וכל הפרויקט מת בשקט. חובה טסט-route שמוכיח
שה-T1 שורד את שני הדורסים דרך `route_setup` האמיתי (כמו `test_t2t3_no_stomp_route.py`, לא unit בלבד).

**תוספת-חובה 2 — שמירת-מונוטוניות של הסולם:** אחרי T1=סוף-מבנה ייתכן `t1 >= t2` (סוף-מבנה רחוק מ-POC/‏T2
של הטבלה) → סולם-OCO הפוך (C2 קרוב מ-C1). כלל: אכוף `t1<t2<t3` בכיוון-העסקה; הפרה → t2/t3 נגזרים-מחדש
מהטבלה ביחס ל-T1 החדש **או** מוזזים החוצה — cc מציע בטבלת-העיצוב; סטייה מהעיקרון = שאלה למייקל לפני קוד.

**תוספת-חובה 3 — רישום-החלפה מפורש:** פסיקת-07-21 **מחליפה את פסיקת-07-10** (PATTERN_T1_OVERRIDE) לגבי T1
בלבד. לרשום ב-RULED_FLAGS בשדה-note של `T1_STRUCTURE_END_V1`: "supersedes 07-10 pattern-T1 (MFE pts) for T1;
t2/t3 policy unchanged" — אחרת ביקורת עתידית תראה סתירת-פסיקות ו"תתקן" חזרה (משפחת רגרסיות-מעגליות).

**הערת-אגב (לא חלק מה-fix, לתעד במיפוי):** ה-rr-gate היום רץ עם `min=0.65` (rotation) בזמן שהקנוני קרא
Trend_Normal — לציין במיפוי איזה ענף-min נבחר ולפי איזה מקור-תווית. אם זה G5/None — זה כבר במשימה-15 של cowork.
אחרי הבנייה: לעדכן את שני עצי-ההחלטות (שורה 22 rr_entry_gate + שלב-6) באותו קומיט.
