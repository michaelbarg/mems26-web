# פקודה · 08.09 09:40 — סוגרים פערים **היום, לפני הפתיחה**

**מאת:** cowork-dev · **אל:** cc-macbook · **‏`--re 184d6aa3`** ·
**פסיקת-מייקל 09:35: "אפשר לתקן את כל הפערים לפני יום מסחר היום."**

| שעה | מי | מה |
|---|---|---|
| עכשיו → **13:30** | cc | ‏§A · §B · §C · §D + טסטים + ריפליי-§A |
| ‏13:30–14:30 | cowork | אימות מול פלט גולמי · `guard_tests` · `flag_guard` |
| **‏15:00** | — | **הקפאה.** אין קוד אחרי זה |
| ‏15:45 | cowork | ריסטארט + `fire_drill` + `ps -o lstart` > זמן-הקומיט |
| **‏16:10** | — | שער. אדום ⇒ לא סוחרים |

**‏🔴 ההפרדה שקובעת מה נכנס היום:** ‏§A-§D **מיישמים פסיקה קיימת או שאינם משנים התנהגות בכלל**
⇒ נכנסים. ‏§E-§G **משנים כמה עסקאות ייכנסו** ⇒ הם **החלטה של מייקל**, לא תיקון, ולא היום.

---

## ‏§A · 🔴 חוזה 5 מקבל ראנר ביום שאינו-מגמה — היפוך ברירת-המחדל

**פסיקת-מייקל 06.09:** *"5 חוזים ללא ראנר — יש רק סוג-יום אחד שצריך ראנר וזה טרנד-דיי."*
הקוד מיישם אותה — **רק כשיש תווית:**

```
sierra_command.py:989    if _rbd_dt is not None:      ← אין else
                             Trend*   → ראנר · Neutral_Center/Nontrend → c4=t3
                             אחרת     → c4=struct_c3
sierra_command.py:1038   RUNNER_TRAIL_V2 (רץ אחרי) ⇒ _c4_target=None ⇒ סטופ-בלבד
```
**‏`day_type=None` ⇒ §2 לא נוגע ⇒ הכלל הישן לוקח את הרגל האחרונה לראנר.** מדידה (‏`v9_trades`, לייב, מ-24.08):

```
39 עסקאות-לייב · 14 בלי תווית (36%) · בכולן t4=NULL · 5 מהן אחרי 17:30
ימי-טרנד בפועל: 3   ⇒  3 היו אמורות לקבל ראנר, לפחות 14 קיבלו
```

**התיקון:** ‏`if _rbd_dt is None or not _rbd_dt.startswith("Trend")` ⇒ **מסלול הלא-מגמה**
(`c4 = struct_c3`, נפילה-לאחור `t3`) **וגם `setup["runner_by_daytype"] = True`** — כדי
ש-`RUNNER_TRAIL_V2` ידלג. הראנר נשאר **אך ורק** על תווית שמתחילה ב-`Trend`.
**זה מיישם פסיקה עומדת ⇒ בונים, מאמתים, מדליקים. בלי אישור שני** (CLAUDE.md §"רשומות עומדות").

**טסטים (התנהגותיים, דרך `command_from_setup` עם stub ל-`write_trade_command`):**
`day_type=None ⇒ t4 is not None` · `"Variation" ⇒ t4 == struct_c3` · `"Trend_Normal" ⇒ t4 is None` ·
`"Trend_DD" ⇒ t4 is None`. **מוטציה:** החזרת ה-`if _rbd_dt is not None` ⇒ הטסט הראשון נכשל.

**וריפליי (לדעת את הגודל, לא לאישור):** ‏14 עסקאות-הלייב חסרות-התווית — מה היה קורה עם
`c4 = t3` מול סטופ-בלבד בפועל. **דווח `Σ$` ושורת-`RULED_FLAGS`.**

## ‏§B · `OPENING_DRIVE_SKIP_V1` — השוואה למחרוזת שלא קיימת

```
five_min_system.py:2180   _trig.get("type") == "OPENING_DRIVE"
opening_entry.py:287      הקידומת OPENING_ נוספת רק ב-build_opening_setup — הטריגר הוא "DRIVE"
```
**הדגל `=0`, ולכן התיקון אינו משנה התנהגות.** לתקן ל-`"DRIVE"` **ולהשאיר `=0`** — כדי שהיום
שבו מייקל יפסוק להדליק אותו לא ייפול על באג-מחרוזת. טסט: הדגל `1` + טריגר `DRIVE` ⇒ נופל.

## ‏§C · פטור-רדיפה בימי-מגמה — `AttributeError` בשקט

```
trading_gateway.py:2427   str((_ecg_gldt() or {}).get("day_type") or "")
trade_context.py:565      get_live_day_type() → str  ⇒  .get על str ⇒ AttributeError
:2439                     except: _ecg_trend_exempt = False   ← נכשל-סגור, תמיד
```
**אפס שינוי-התנהגות היום** — אומת: המשתנה נצרך רק ב-`if (_ecg_bypass and _ecg_revoke_on ...)`
ו-`EXTREME_CHASE_TIP_REVOKE_V1` **אינו ב-`.env` ⇒ ברירת-מחדל `0`** ⇒ הבלוק כולו אינרטי.
**תיקון:** `_ecg_dt = str(_ecg_gldt() or "")`. טסט: הזרקת `Trend_DD` + רגל-חיה ⇒ `_ecg_trend_exempt=True`.

## ‏§D · `RUNNER_TRAIL_V1=1` — דגל דלוק על ענף בלתי-נגיש

‏`bar_level_detector.py:984` הוא `elif` אחרי `DYNAMIC_STRUCT_TRAIL`, והקוד **עצמו רושם "INERT"**.
`.env` אומר `1`. **לא להחיות אותו** — לתקן את **האמת**: `.env` ל-`0` + הערה,
או `RULED_FLAGS` `expected: "0"` עם `note: "אינרטי מאז RUNNER_TRAIL_V2 — ראה :964-978"`.
דגל שאומר `1` על קוד מת הוא בדיוק מה שגורם לי להאמין להגנה שאינה קיימת.

---

## ‏§E-§G · **לא היום** — אלה החלטות, לא תיקונים

| | מה | למה לא היום |
|---|---|---|
| **‏E** | וטו סוג-הפתיחה (תוויות `*_UP/_DOWN` שלא קיימות + שדה `UNKNOWN`) | תיקון **מוריד עסקאות**. צריך ריפליי: כמה נחסמות ומה ה-Σ. אחר-כך פסיקה |
| **‏F** | `Nonconviction` נכשל-פתוח ל-FULL | הפעלה **מדלגת ימים שלמים**. כמה ימים בכלל קיבלו את התווית? למדוד |
| **‏G** | ‏`RE_ACCEPTANCE` כ**פטור** מ-`awaiting_release` (במקום גלאי נוסף) | שבוע-צל תחילה. שחרור-כולל של 116 החסימות = **±0** — הערך הוא בהגדרה המדויקת |

---

## ‏13:30 — מה שאני בודק

`git status ⇒ ריק` · ‏4 טסטים התנהגותיים + מוטציה לכל אחד · `guard_tests` · `flag_guard` ·
**ריפליי-§A עם `Σ$`** · `RULED_FLAGS` מעודכן ל-§A ו-§D.

## אסור

לגעת ב-§E/§F/§G · ריסטארט (שלי, 15:45) · `.env` בלי `mems26_snapshot.sh` · להחיות `RUNNER_TRAIL_V1` ·
לגעת ב-`awaiting_release`/`entry_not_confirmed`/הסלוט · קוד אחרי 15:00.
