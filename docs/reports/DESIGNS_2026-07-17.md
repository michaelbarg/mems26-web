# MEMS26 · טיוטות-עיצוב — 2026-07-17

**מקור:** EOD `PATTERN_EOD_2026-07-17.md` + register-update 07-17 + אודיטים `PATTERN_MGMT_AUDIT`/`PATTERN_MISS_AUDIT`.
**כלל:** עיצוב-בלבד — **לא מומש**. כל פריט: שורש · שינוי-מוצע (קובץ/שכבה) · regression-שיוכיח · **[TRADING-LOGIC → דורש-Michael]** או **[DISPLAY/SAFE]**. כל פריט `[SoT→CC]` דורש הצלבת Sierra/DB/לוג לפני-מימוש (Rule 2/5). דגלים — default-OFF, flag-guard, regression-test-לכל-תיקון (Pre-LIVE Discipline).

**דירוג-חומרה:** D1 (🔴🔴 בטיחות-חיה) · D2–D3 (🔴 שחיתות-נתונים) · D4 (🔴 over-fire) · D5 (🟠 fixed-lock) · D6 (🟠 pattern-miss) · D7 (🟡 endpoint).

---

## D1 — 🔴🔴 יתום-שורט −5 עירום על-כסף-אמת + ledger-שקרי (I-68 / S-13) · **[TRADING-SAFETY · חוסם-LIVE · SoT→CC]**

**שורש (מאומת מ-OPS_LOG + PRELIVE_REVIEW):** נתיב-כניסה יוצר פוזיציית-ברוקר בלי-שהמערכת-רושמת/מגנה-אותה (`sierra_qty=−5`, `working_orders=0`, `tm_open=0`). שלושה כשלים-משלימים: **C1** — `reconcile.py` מתייחס ל-`ORDER_SUBMITTED` כ"סטופ-קיים" ואף-פעם לא-קורא `working_orders` בפועל → "עירום" לא-מזוהה. **C3** — ה-reconciler alert-only, `streak` נעול-0/3, **לא-משטח-לבד**. **סגירה-פיקטיבית** — #400 נרשם `CLOSED WIN +$20` בעוד הברוקר −5 (`closures_on_fills` מדווח-שטוח → מדליף-חוזים; דפוס-#350). תוצר-לוואי: #398 `ORDER_FAILED:−1` כי היתום תפס-את-החשבון.

**שינוי-מוצע (3 שכבות; הראשון-קריטי):**
1. **Auto-flatten על-חוסר-התאמה מתמשך** — `reconcile.py`: כש-`sierra_qty≠0` בעוד `tm_net_qty==0` למשך ≥N-שניות (למשל 60), הַנְפֵּק `FLATTEN_ACCOUNT` (הנתיב-העובד-המאומת) במקום alert-only. דגל `RECONCILE_AUTO_FLATTEN_V1` (default-OFF עד-אישור).
2. **קריאת-`working_orders`-אמיתית ל-naked-detection** — `reconcile.py`: החלף את-היוריסטיקת-`ORDER_SUBMITTED=stop-ok` בקריאה-בפועל של-`working_orders` מ-Sierra; אם `qty≠0 ∧ working_orders==0` → `naked=TRUE` מיָדי (לא-לחכות-ל-streak).
3. **חסימת-entry כשיש-חוסר-התאמה-פתוח** — gateway pre-fire: אם reconcile מדווח divergence/naked פתוח → חסום כל-entry-חדש (`blocked_by=reconcile_divergence`) עד-שטוח. מונע את-#398-`ORDER_FAILED` ואת-הצבירה −3→−5.

**regression-שיוכיח:** (א) sim-scenario: הזרק `sierra_qty=−5, working_orders=0, tm_net=0` → תוך-≤60s נכתב `FLATTEN_ACCOUNT` + `naked=TRUE` (לא-alert-only). (ב) `test_reconcile_reads_working_orders`: `ORDER_SUBMITTED` בלי-fill → `stop_ok=False`. (ג) `test_entry_blocked_on_divergence`: entry-חדש בזמן-divergence → `blocked_by=reconcile_divergence`. (ד) ledger-fix: #400 `CLOSED→OPEN`, `daily_pnl` מוחשב-מחדש.

**סיווג:** **[TRADING-SAFETY → דורש-Michael]** (auto-flatten על-כסף-אמת = פעולת-מסחר). **[SoT→CC]:** לפתוח Sierra → לאמת `−5/avg~7502.7/working_orders=0` על 37138283 → `FLATTEN_ACCOUNT` או סטופ-ידני **עכשיו** (סיכון-סוף-שבוע) → לתקן ledger #400 → לחלץ שורש `reconcile.py` C1/C3. **תנאי-קדם-מוחלט ל-LIVE.**

---

## D2 — 🔴 `pnl_r` מנופח (I-22): מחלק ב-stop-נגרר במקום stop_initial · **[DATA · משפיע-LIVE-sizing · SoT→CC]**

**שורש (מאומת מתמטית):** טווח-יום 54.5pt ⇒ R-max≈6.4, אך נרשמו 42/22.5/16. `pnl_usd` אמין (#400 +$20 ≈ +1R; `daily_pnl=−58.75` תואם); `pnl_r` לא. אחרי T1/T2 ה-`stop` נגרר לכמעט-כניסה, ונוסחת-R (כנראה) מחלקת ב-`entry−stop`(הנגרר, ~0.25pt) → מכנה→0 → R→∞.

**שינוי-מוצע:** ב-write-path של-`v9_trades` / gateway-pnl-calc — קבע `1R := |entry − stop_initial|` (**קבוע-לכל-חיי-העסקה**, לא-הנגרר), ואכוף `sign(pnl_r) == sign(pnl_usd)`. הוסף sanity-clamp: אם `|pnl_r| > (day_range / stop_initial_pts)` → סמן `pnl_r_suspect=TRUE` + לוג-warning (Rule 3: min/max-amplifier).

**regression-שיוכיח:** `test_pnl_r_uses_initial_stop`: עסקה entry 7519.5 / stopI 7525 / exit 7513.75 → `pnl_r == (7519.5−7513.75)/5.5 ≈ +1.045`, **לא 42**. `test_pnl_r_sign_matches_usd`: `pnl_usd<0 ⇒ pnl_r<0`. הצלב 395/399/400 raw → R∈[−1,+6.4].

**סיווג:** **[DATA/DISPLAY — התיקון-עצמו safe]** אך **קריטי-ל-LIVE** (sizing/risk מבוסס-R מתעוות עם pnl_r-שבור) → **תנאי-קדם-למדידת-ביצועים**. **[SoT→CC]:** לחלץ את-הנוסחה בקוד + לצלב 3-העסקאות.

---

## D3 — 🔴 shadow-sim מתעלם מסדר-תוך-בר (I-65): stop-before-target · **[DATA/SIM — safe (shadow-only) · SoT→CC]**

**שורש (מאומת מול-ברים):** #395(shadow) ניצח +$52.5 על-אותו-סיגנל ש-#396(live) הפסיד −$78.75. ה-shadow-sim זיכה T1 (7513.75, הגיע 11:05) בעוד הסטופ (7525) נגע-ראשון ב-10:55 (בר-כניסה H=7536.5, live נעצר תוך-43s). ה-sim בודק "האם target-נגע בחלון" ו"האם stop-נגע" **בנפרד**, בלי-לכבד מי-קדם תוך-אותו-בר → מזכה-מנצחים-פנטום.

**שינוי-מוצע:** ב-shadow-fill-simulator — כשגם-סטופ-וגם-target נופלים בחלון-אותו-בר, החלט לפי **worst-case-ordering** (או OHLC-path אם-זמין): לשורט, אם `bar.high ≥ stop` **וגם** `bar.low ≤ target` באותו-בר → זקוף **stop** (שמרני), אלא-אם open-כבר-מעבר-ל-target. הוסף שדה `intrabar_ambiguous=TRUE` לכל בר-כזה (שקיפות). דגל `SHADOW_INTRABAR_ORDER_V1`.

**regression-שיוכיח:** `test_shadow_stop_before_target`: entry 7519.5 short / stopI 7525 / T1 7513.75, בר-כניסה `H=7536.5, L=7517` → תוצאה `STOP` (−1R), **לא WIN**. אמת ש-#395-replay = −1R (זהה ל-#396 live).

**סיווג:** **[DATA/SIM — safe]** (shadow-only, לא-נוגע-בירי-חי). מנקה את-ה-shadow-book כך-שיהיה מדד-אמין. **[SoT→CC]:** נתיב-hit-detection של-sim + לוג 09:55–11:05.

---

## D4 — 🔴 INITIATIVE over-fire על Normal (I-69 / A5): auth-key mismatch · **[TRADING-LOGIC → דורש-Michael · SoT→CC]**

**שורש (PATTERN_MGMT_AUDIT §A5):** S2 מעביר family-key `"OFA_Initiative"`; `_auth_cell` מנסה `("OFA_Initiative",dt)` ثم `("OFA_INITIATIVE_LONG",dt)` — שני-המפתחות מפספסים את-מפתח-הטבלה `"INITIATIVE_LONG"` → "using max" → verdict **FULL**. ה-verdict LOCKED `INITIATIVE×Normal=SKIP` / `×Neutral=SKIP` (auth_table_v1, 05-25) **נעקף-בשקט**. ב-Normal ה-playbook אומר REDUCED (מתיר) → INITIATIVE ירה-מלא (`FIXED_CONTRACTS_4=1`→4). **live-impact:** #396 −$78.75.

**שינוי-מוצע:** הוסף alias `"OFA_Initiative" → "INITIATIVE"` ב-`_auth_cell` (sizing.py:42-46) כך-ש-`FAMILY_DIRECTION`-key יתמפה ל-`INITIATIVE_LONG`/`INITIATIVE_SHORT` → ה-SKIP-verdict-הנעול-חוזר-לפעול. (ה-docstring כבר-תיקן את-הגרסה-של-Reactive לאותו-באג — לא-של-Initiative.) דגל `S2_INITIATIVE_AUTH_KEY_FIX_V1`.

**regression-שיוכיח:** `test_initiative_normal_skips`: INITIATIVE_SHORT × Normal → `_auth_cell` verdict `SKIP` (לא FULL) → אין-fire. `test_initiative_neutral_skips`: × Neutral_* → SKIP. (וידוא ש-REACTIVE-על-Normal **לא-נפגע** — נשאר-מתיר.)

**סיווג:** **[TRADING-LOGIC → דורש-Michael]** — התיקון **מקטין-ירי** (משחזר SKIP), risk-surface-change. דגל-OFF עד-פסיקה. **[SoT→CC]:** לצלב fire #396 raw (day_type=Normal, family-key, verdict).

---

## D5 — 🟠 Normal-day REV-fade + stop-floor (A1/A3): נעל-רגרסיה על-תיקון-של-היום · **[TRADING-LOGIC (כבר-פרוס) → אימות-Michael · SoT→CC]**

**שורש:** `NEUTRAL_RESPONSIVE_V1` REV-fade-exemption (gateway:811) + rotation-stop-floor (stop_resolver:75) **השמיטו bare `"Normal"`** בעוד ה-siblings (`location_gate._ROTATION_PREFIXES`, RR-relief) כוללים-אותו → ב-Normal fade-נגד-ה-push נחסם + stop-בתוך-הרעש. **תוקן-חי היום** (`NORMAL_ROTATION_FIX_V1`, 12:27 CT — הוסיף `Normal` לשניהם).

**שינוי-מוצע (השלמה, לא-מימוש-חדש):** (א) הוסף regression-test-קבוע שנועל-את-התיקון (שלא-יִסוֹג במיזוג/refactor — CLAUDE.md standing-decision-class). (ב) יישר את-**4 האתרים-הנותרים** של-`self.current_day_type`-stale (**A2** five_min:1139, **A4** five_min:1170-1192, **A6** woodies:640-669, **A7** five_min:1551) ל-`get_live_day_type()` — כל-אחד דגל-נפרד (`S2_NONTREND_SKIP_OVERRIDE_AWARE_V1`, `S2_CHART_OVERRIDE_AWARE_V1`, `S4_DAYTYPE_OVERRIDE_AWARE_V1`, `FLAG_T2_OVERRIDE_AWARE_V1`).

**regression-שיוכיח:** `test_normal_in_rev_exemption`: Normal-day REACTIVE_SHORT מול `direction_context.dir=UP` → **לא-נחסם** `direction_context`. `test_normal_stop_floor_08atr`: Normal fade → floor `0.8×ATR` (לא 0.5). `test_s2_nontrend_skip_uses_override`: stream=Nontrend + override=Normal → S2 **לא-עיוור**.

**סיווג:** A1/A3 **כבר-פרוסים** (אימות-no-regression). A2/A4/A6/A7 = **[TRADING-LOGIC → דורש-Michael]** (audit §5 ruling-asks). **[SoT→CC]:** לצלב את-`NORMAL_ROTATION_FIX_V1` בלי-רגרסיה + A2/A4-live-bite (stream-Nontrend מול override).

---

## D6 — 🟠 פספוס-הבוקר: detectors מכוילים-לטרנד-מבוסס (F1–F5) · **[TRADING-LOGIC → דורש-Michael + ריצת-Mac · SoT→CC]**

**שורש (PATTERN_MISS_AUDIT):** בראלי-ORR +54pt של-הפתיחה, כל-קריטריון-שמקודד-"המתן-להתבססות" (Sierra-BLUE-paint ~6-בר-lag, ZLR S1.1 6-blue-bar, GB100 fresh-cross-only, S2 B2 quiet-vol מול ממוצע-לילה-מורעל, FHB) מבשיל **אחרי-שהמהלך-נגמר**. שום-דבר לא-הגיע-לשער (upstream). **F1** = Sierra-paint = single-point-of-blindness · **F3** = B2 מול overnight-poisoned-avg (category-error).

**שינוי-מוצע (8 הצעות-מדורגות, audit §4 — כולן דגל-OFF):** #1 `GB100_GRAY_OPENING_V1` (GB100 על-GRAY בחלון-פתיחה) · #2 `S2_RVOL_SESSION_V1` (B2-avg = RTH-session-only, **bug-fix**) · #3 `ZLR_S11_BLUE_MIN`(6→3)+`ZLR_S11_COUNT_ZL_SIDE_V1` · #4 `S2_B4_CONFIRM_FRAC`(1.0→0.75) · #5 `ZLR_EXTREME_WINDOW`(12→20) · #6 `S2_FHB_FAST_V1` · #7 `TREND_RELABEL_150_V1`. **#0 (קודם-כל):** diagnose F2 (למה 17:35-DLL-ZLR לא-נותב) — log-chase, לא-דגל.

**regression-שיוכיח:** **ריצת-כימות על-ה-Mac (חובה לפני-פסיקה, Rule 5):** `python3 scripts/audit_pattern_miss.py --date 2026-07-17 --relax all --out docs/reports/PATTERN_MISS_RUN_2026-07-17.md` (+ 07-16/07-15) → per-relaxation `newly-caught-swings` מול `added-FALSE-fires`. אין-לפסוק-על-אומדנים. + test-per-flag (זה-שנבחר).

**סיווג:** **[TRADING-LOGIC → דורש-Michael]** — כל-הרפיה = יותר-ירי = risk-surface. **[SoT→CC]:** ריצת-audit_pattern_miss על-Mac + `[Woodies] HTLB dir-gate` לוג (bias-DOWN-latched חסם longs-בוקר?) + F2 log-chase. **אזהרת-אינטראקציה:** #1+#7 חופפים (שניהם GB100-trend-source) — enable-אחד-A/B-shadow-then-decide.

---

## D7 — 🟡 EOD-endpoints עיוורים-לחסימות (persistence) · **[DISPLAY/SAFE · SoT→CC]**

**שורש:** `/api/v9/missed-trades` החזיר `count=0` בעוד `missed_watch` תיעד **20 missed-winners** ב-OPS_LOG; `/api/v9/gateway/decisions` הוא **buffer in-memory-since-restart** (22 רשומות בלבד, `note="in-memory since backend start"`) — עם 4-ה-restarts-של-היום הוא לא-מכסה-יום-מלא. ⇒ EOD-דרך-API-בלבד עיוור; רק OPS_LOG שלם.

**שינוי-מוצע:** (א) persist את-החלטות-ה-gateway ל-DB (`v9_gateway_decisions`) עם `blocked_by`+`ts`+counterfactual-outcome, כך-ש-`/gateway/decisions` ו-`/missed-trades` יחזירו יום-מלא (לא-in-memory). (ב) חבֵּר את-`missed_watch` EOD-summary (`פספוסים/צדק` פר-gate) ל-endpoint שה-EOD-agent יקרא ישירות (לא-לפרסר-לוג). דגל — לא-נדרש (display/read-path).

**regression-שיוכיח:** `test_gateway_decisions_persist`: אחרי-restart, decisions-של-לפני-restart עדיין-מוחזרות. `test_missed_trades_matches_ops_log`: `/missed-trades count == OPS_LOG missed_watch count` ליום.

**סיווג:** **[DISPLAY/SAFE]** — read/persist-path, לא-נוגע-בירי. משפר-אמינות-EOD-עתידי. **[SoT→CC]:** סכמת-persist + חיווט missed_watch→endpoint.

---

### סיכום-דירוג לפסיקת-Michael

| # | פריט | סיווג | דחיפות | תלוי-CC |
|---|------|-------|--------|---------|
| **D1** | יתום −5 עירום (I-68) | TRADING-SAFETY | 🔴🔴 **עכשיו** (סוף-שבוע) | FLATTEN + reconcile.py |
| **D2** | pnl_r מנופח (I-22) | DATA (קריטי-LIVE) | 🔴 לפני-מדידה | נוסחת-R |
| **D3** | shadow-sim intrabar (I-65) | DATA/SIM (safe) | 🔴 | sim hit-path |
| **D4** | INITIATIVE auth-key (I-69/A5) | TRADING-LOGIC | 🔴 (עלה-כסף) | fire #396 raw |
| **D5** | Normal-fade A1/A3 (fixed) + A2/A4/A6/A7 | TRADING-LOGIC | 🟠 | no-regression + ruling |
| **D6** | pattern-miss בוקר (F1–F5) | TRADING-LOGIC | 🟠 | ריצת-Mac |
| **D7** | EOD-endpoint persistence | DISPLAY/SAFE | 🟡 | persist-schema |

**כל-D1–D6 = risk-surface → strategic-stop + Michael sign-off + דגל-OFF + regression-test (Pre-LIVE Discipline). שום-דבר-כאן לא-מומש.**
