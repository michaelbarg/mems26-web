# פקודה · שלושה תיקונים לפני הפתיחה — הקפאה 15:50, ריסטארט 16:00

**מאת:** cowork-dev · **אל:** cc-macbook · **01.09 13:40**
**פסיקת-מייקל:** *"תעשה את הריסטארט ב-16:00 — העיקר שנמקסם תיקונים איכותיים
שהמערכת תסחר ותרוויח."*

## לוח-זמנים קשיח

| שעה | מה |
|---|---|
| **15:50** | **הקפאה.** מה שלא נדחף ונבדק — לא נכנס. אין חריגים |
| 16:00 | ריסטארט (`launchctl kickstart -k gui/$UID/com.mems26.backend`) |
| 16:05–16:10 | ‏`fire_drill` · `flag_guard` · פיד · `is_sim` |
| **16:10** | **שער קשיח.** משהו אדום ⇒ לא סוחרים את הפתיחה |
| 16:30 | פתיחה |

**‏🔴 פריט שלא הספיק לאימות מלא נוחת כ-`shadow`, לא כ-`live`.** חצי-בנוי בפתיחה
גרוע מלא-בנוי. **לומר מוקדם אם לא מספיקים — זו תשובה טובה, לא כישלון.**

---

# 🔴 1 · שורש הסטופ-הקרוב — ‏ATR מעופש בפתיחה

**הראיה, מהלוג של אתמול, אותו יום ואותו מכשיר:**
```
16:35 (פתיחה)     band [2.1,  5.0]     ← רצפה 2.1 · תקרה 5.0
אמצע-היום         band [7.6, 11.8]     ← רצפה 7.6 · תקרה 11.8
```
**⇒ השעה התנודתית ביותר ביום מקבלת את הרצועה הצרה ביותר — פי 2 מתחת לאמצע-היום.**

**וזו לא בעיית-תקרה. זו בעיית-רצפה.** ‏`stop_resolver` הולך מהרונג הקרוב לרחוק
ובוחר את **הראשון שבתוך הרצועה**. ב-#875 רונג-0 במרחק 4.50 **עבר רצפה של 2.1**
ונבחר — בעוד הסטופ המבני היה 14.25. **מול רצפה של 7.6 הוא היה נפסל.**

**⇒ רצפה ותקרה שתיהן `= k × ATR_5m`. שורש אחד, תיקון אחד.**

## מה לעשות — **אבחון לפני תיקון**

1. **לקרוא מאיפה `ATR_5m` מגיע** ב-`stop_resolver`/`sizing` ומה **חלון הברים**.
2. לחשב לכל 10 עסקאות-הפתיחה: ‏ATR-שבשימוש מול **ATR של ברי-ה-RTH של אותו יום**.
3. **רק אז** לתקן — ובדגל נפרד.

| התוצאה | המסקנה |
|---|---|
| החלון בולע ברי-לילה ⇒ ‏ATR נמוך פי ‎~2 | **מאומת** — התיקון הוא בחלון |
| החלון תקין וה-ATR באמת היה 4.2 | **ההשערה נופלת.** לומר זאת ולעצור |

## 🔴 ולמה זה בטוח דווקא היום

רצפה גבוהה יותר ⇒ סטופ רחב יותר ⇒ **ופחות חוזים, כי `RISK_BUDGET_SIZING_V1=225`
מחשב חוזים מהסטופ.** מעל 15 נק' הוא **דוחה**. ⇒ **מנגנון-הסיכון סופג את זה
כראוי; שני המנגנונים מתחברים במקום להילחם.**

**‏⚠️ ואל תרחיבו את התקרה כפיצוי.** זה שינוי-סיכון נפרד בלי פסיקה.

---

# 🟠 2 · לחווט `ENTRY_LOCATION_QUALITY_V1` — **פוסלים 1+2 בלבד**

`backend/v9/systems/entry_location_quality.py` — נבנה מפסיקת-מייקל 28.08
(*"אם היה נכנס במקום נכון היינו מרוויחים הרבה כסף"*) — **ויש לו אפס קוראים.**

**קראתי את הקוד: שלושת הפוסלים הם בלוקים נפרדים ומוגנים.**

| פוסל | קלט | תלוי VA? |
|---|---|---|
| 1 · `chaser` — `pos = \|entry−leg_base\| / L > 0.66` | ‏`leg_base`/`leg_extreme` | **לא** |
| 2 · `expensive_stop` — `stop/atr > 1.5` | ‏`stop_distance`/`atr` | **לא** |
| 3 · `beyond_value` — `(entry−VAH)/va_width > 0.25` | ‏`vah`/`val` | **כן** |

**⇒ להעביר `vah=None, val=None`. פוסל 3 נמנע בשקט — אפס שינוי-קוד במודול.**
פוסל 3 נדלק אחרי `T-203`, לא לפני.

**האנקורים כבר בדוקסטרינג וחייבים לעבור:**
```
17:00 @7749.75 → pos≈0.38 → PASS      18:15 @7774.00 → pos≈0.84 → FAIL
17:35 @7750.75 → pos≈0.40 → PASS      18:25 @7777.75 → pos≈0.91 → FAIL
```

**‏🔴 ומה שהשער הזה *אינו* מכסה — לומר זאת בדוח:** פוסל 2 דוחה סטופ **רחוק**
(`rr>1.5`). **הוא אינו תופס סטופ קרוב מדי** — זה פריט 1. **⇒ שני הפריטים אינם
חופפים, ושניהם צריכים לנחות.**

**אימות דו-כיווני:** ארבעת האנקורים · דגל OFF ⇒ **זהה-בייט** · ‏`vah=None` ⇒
פוסל 3 **אינו** יורה · טסט-מוטציה: הסרת הקריאה ⇒ **הטסט נכשל**.

---

# 🔵 3 · רפליי-המצפן — **מדידה, אפס קוד**

`scripts/f1_compass_replay.py` קיים. להריץ על 10 עסקאות-הפתיחה
(`CC_QUERY_OPENING_2026-09-01.md`) ולענות: **מה המצפן היה אומר בזמן-הכניסה?**

‏`DIRECTION_COMPASS_V1=1` דלוק אבל צרכניו הם `trading_gateway:1722/1792` בלבד;
`grep compass five_min_system.py` = **אפס**. **⇒ ‏OPENING_DRIVE, שהוא פריט 1
בדוקסטרינג של המצפן, הוא היחיד שהמצפן לא מכסה.**

**זו מדידה בלבד היום.** החיווט — רק אם הרפליי מראה שהמצפן היה מנוגד לכניסות
המפסידות. **אם הוא היה `NEUTRAL` — לומר זאת ולא לחווט.**

---

## סדר: **1 → 2 → 3**

‏1 מכסה קבוצה שלמה של הפסדים · 2 הוא פסיקה שכבר ניתנה ומחכה מ-28.08 ·
3 הוא מדידה שלא נוגעת בקוד ⇒ אין לה סיכון-פתיחה.

## מה שכבר נחת ואין לגעת בו

`OPENING_DRIVE_SKIP_V1=1` ב-`.env` (`flag_guard` **228 PASS**) ·
`RISK_BUDGET_SIZING_V1=1` / `225` / `min 3`.

## אסור

**‏🔴 להרחיב את תקרת-הרצועה** (שינוי-סיכון בלי פסיקה) · לחווט את **פוסל 3**
לפני `T-203` · לכבות תבנית נוספת (*"מתקנים ולא מבטלים"*) · לתקן לפני שהאבחון
בידיים · **לדחוף אחרי 15:50** · ריסטארט אחרי 16:10.
