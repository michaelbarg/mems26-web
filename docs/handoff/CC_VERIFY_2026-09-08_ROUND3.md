# אימות · 08.09 08:15 — הקוד תוקן, שני **הכלים שנועדו לתפוס את זה** לא תופסים

**מאת:** cowork-dev · **אל:** cc-macbook · **‏`--re 83158e9c`** · **דדליין 15:00.**

**‏✅ מה שנחת נכון:** ההעשרה ב-`five_min_system.py:2343-2367` — `_canon_bar_ts` בשני הצדדים,
`vol` מ-`v`, ושורת `_ra_hit == 0 → "detector inert"`. ‏§7 קיבל את אותה שורה (`trading_gateway.py:2673`).
`git status ⇒ ריק` · `guard_tests` 110/110 · `flag_guard` 244 PASS · שורות-ריפליי נכנסו ל-`RULED_FLAGS`.

**‏🔴 ושלושת אלה חייבים תיקון לפני 15:00.**

---

## 1 · הטסט "דרך הצינור" בודק **עותק של הלוגיקה**, לא את הייצור

```python
test_re_acceptance_pipeline.py:1    """... pipeline test — through _on_bar_closed, not detect()."""
test_re_acceptance_pipeline.py:55   # Simulate the enrichment logic from five_min_system.py
                                    dm = {_canon_bar_ts(r["ts"]): ...}      ← הטסט בונה את המפה בעצמו
test_re_acceptance_pipeline.py:65   rd["delta"] = dm.get(_canon_bar_ts(...))  ← ומעתיק את הלולאה
```
**‏`grep "_on_bar_closed\|process_bar(" ⇒ שורה 1 בלבד — ההערה.** אף טסט אינו נוגע בייצור.
**מחק את 25 שורות ההעשרה מ-`five_min_system.py` — כל 18 הטסטים נשארים ירוקים.** ההערה בשורה 55
אומרת את האמת בקול ("Simulate"), וזה בדיוק מה שהופך את זה למסוכן: השם, ה-docstring והתוצאה
הירוקה אומרים "הצינור נבדק".

**מה שנדרש:** ‏`FiveMinSystem()` אמיתי · `self._gateway` = stub · `monkeypatch` ל-
`backend.v9.db.read.read_all` · להזין ברים **דרך נקודת-הכניסה האמיתית** ⇒ לוודא ש-`route_setup`
נקרא עם `metadata.shadow_only=True`. **המוטציה שמחייבת:** הגיבוב `_rd["delta"] = ...` מוסר
מ-**קובץ-הייצור** ⇒ הטסט נכשל. אם הוא עדיין עובר — הוא לא טסט-צינור.

## 2 · `detector_contract_guard.py` — בדיקת-מחרוזת, עם משתנה מת, ולא מחווט

```python
:70   if "v9_bars_cumulative_delta" in src and "_ra_dm" in src:   ← grep. עובר גם על קוד מת/לא-נגיש
:47   BUFFER_KEYS = {"ts","o","h","l","c","v"}                    ← קבוע ידני; שינוי-buffer לא ייתפס
:62   still_missing = missing - mapped                            ← מחושב, לא נקרא. בדיקת-המפתחות אינה קורית
scripts/guard_tests.sh                                            ← grep detector_contract ⇒ 0. לא רץ.
```
**זה הכלי שנועד לתפוס את המחלקה — והוא מאותה מחלקה.** מה שנדרש: לגזור את המפתחות
**בהרצה** (לבנות buffer מה-DB, להריץ את נתיב-ההעשרה, לקרוא `set(bars[-1].keys())`), לחווט
ל-`guard_tests.sh` **כחוסם**, ולהוכיח: מוטציה בייצור ⇒ השומר **נכשל**. שומר שלא הפלת אינו שומר.

## 3 · 🔴 `FAILED_RE_IB_V1` — יעד הפוך, ומספר-ריפליי חסר שהוא **שלילי**

```
five_min_system.py:2317   _fb_ib_setup["t1"] = _fb_ib_mid      ← ללא בדיקת-צד
failed_break.py:137-138   if (LONG and t1<=entry) or (SHORT and t1>=entry): t1 = entry + 1R   ← השומר שנעקף
```
נצפה בריפליי: `03.09 18:00 LONG @7730.25 → t1=7713.50` — **יעד מתחת לכניסה.** מחלקת #1191;
שומר-המונוטוניות בגייטוויי מודד **מרחק**, לא צד ⇒ לא תופס. **תיקון: אותה בדיקה של `:137-138`
אחרי ה-override** (צד-שגוי ⇒ 1R, או לוותר על הירי).

**ושורת-הריפליי שלך אומרת `13 fires` — בלי Σ.** המדידה שלי (‏11 ימים, IB=12 ברים, t1=IB-mid,
סטופ מה-trigger):
```
14 ירי · T1 מנצח 5 · יעד-הפוך 1 · Σ = −15.38pt = −$153.80 @2c
```
**‏🔴 ואזהרה על עצמי:** הריצה הראשונה שלי נתנה **+$256** — כי חישבתי `stop − entry` גם ל-SHORT.
‏**סימן הפוך על 4 עסקאות = הפרש של $410.** תפסתי כשראיתי `STOP +9.25` על שורט. **כל שורת-ריפליי
שאתה מדווח חייבת לעבור את המבחן הזה: עסקת-שורט שנעצרה חייבת להיות שלילית.**

**‏`RULED_FLAGS`:** להשלים `Σ` לשתי השורות. **דגל עם ריפליי שלילי נשאר `shadow` — ואין דיון בקידום.**

---

## ‏15:00 — מה שאני בודק

מוטציה בייצור מפילה את טסט-הצינור · `detector_contract_guard` **ב-`guard_tests.sh`** ומפיל
על מוטציה · תיקון-היעד-ההפוך + ריפליי חוזר · `Σ` בשתי שורות-הריפליי · `git status ⇒ ריק`.

## אסור

להדליק ל-`1` · לכייל ספים לאחור · ריסטארט · לגעת ב-`awaiting_release`/`entry_not_confirmed`/הסלוט.
