# cursor — פרונטאנד: פאנל סוג-פתיחה + תווית-אחת (מייקל 2026-07-22, לפני פתיחה)

**חלוקה:** CC = backend (`CC_SMART_BUILD_2026-07-22.md` — B1/A/O/E/F). **אתה = פרונטאנד + אימות-CC.**
תצוגה בלבד — אפס לוגיקת-מסחר. חוק-5. דדליין ~09:00 ET.

## 1. OpeningTypePanel (הרחב את הקיים — אל תבנה חדש)
`OpeningTypePanel.tsx` (mounted V9Dashboard) + `opening_panel` endpoint. אחרי ש-CC מוסיף שדות:
- **stance** (המיפוי המאושר-דלטון): הצג ברור — 🎯 ודאות-כיוונית (Drive/Test-Drive, רק עם-הדרייב) ·
  🔄 עסקת-היפוך (Rejection-Reverse) · ⏸ אין-יתרון/המתן (Auction). זה הריבוע שמייקל ביקש:
  סיווג-פתיחה נפרד + מה סחיר עכשיו (היפוך או ודאות-כיוונית).
- **תבניות-מתאימות לסוג-הפתיחה** + סימון **מה ירה בפועל** (השדה החדש מה-join של CC).
- **תווית-יום אחת:** הפאנל מציג את `live` (מ-get_live_day_type) — אם CC טרם חיווט, אל תציג replay
  כ-live; `cross_check.match=false` → אינדיקטור-סטייה קטן (audit≠live), לא החלפת-תווית.
- None → **"—"/FORMING** בכנות. בלי fallback ל-Normal.

## 2. תווית-אחת בכל המסך
TopBar == OpeningTypePanel.live == `/api/v9/day_type/live` — צלם-הוכחה אחרי ריסטארט-הבוקר.
עדכן `FRONTEND_INDEX.md` (שורות הפאנל/TopBar) באותו קומיט — לפי פרוטוקול-העדכון שקבעת.

## 3. אימות-CC (התפקיד השני שלך)
לכל סעיף של CC (B1 v2 · A · O · E · F): הרץ את הטסטים **עם `.env` טעון**, אמת fixtures
(‎#449/452/456 BLOCK · 19:55 VAH ALLOW), הדבק פקודה+פלט-גולמי ל-LIVE_CHANNEL. אף סוכן לא סוגר של-עצמו.

## הכרעת "שכפול תבניות" (מייקל שאל; זו העמדה — cursor/CC אל תשכפלו בלי פסיקה)
**לא משכפלים תבניות עכשיו.** ADAPT לפני BUILD: התבניות הקיימות (REV: REACTIVE/GHOST/DBDT · CONT:
ZLR/INITIATIVE) + שער-ה-stance של הפתיחה = הכיסוי הראשון. אם הסים/צל יראה שהן לא מגיעות לטריגר בחלון-
הפתיחה (מבנה-5-דק' עוד לא קיים ב-09:35) — אז בונים **תבנית-פתיחה ייעודית אחת** (OPENING_REVERSAL,
פרמטרי-IB, דגל-OFF, סים לפני לייב) — לא שני שכפולים. שכפול-קונפיג מותר; שכפול-קוד אסור.
