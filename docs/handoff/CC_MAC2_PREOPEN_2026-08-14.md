# אל cc-מק-2 — קדם-פתיחה 14.08: יישור סופי + הוכחת-חימוש (מסחר לייב 4 חוזים היום)

**דרישת-מייקל (14.08 בוקר):** "היום מק-2 מבצע מסחר לייב על כסף אמיתי, 4 חוזים, עם יכולת
להגדיל עסקאות. כל המערכות דרוכות, הדגלים כמו במק-1, זיהוי-היפוכים עובד."

**מק-1 מוכן:** ALL SYSTEMS ARMED · flag_guard **172/172** · ריסטארט נקי (230 vars).

## בצע עכשיו, לפי הסדר, והדבק כל פלט גולמי

1. **`git pull`** — חייב לכלול:
   - `ad868148` (S2 legacy-bars root-fix), `6fb6a192` (arming gate), `52fbd276` (1:1 sizing)
   - **חדש היום:** `RELEASE_LEG_EXEMPT_V1` (H18) — פטור-רגל לשער-ה-release ⇒ **כניסות-היפוך**.

2. **‏.env — יישור ל-172 דגלים (ערוך ב-python, לא ב-sed):**
   ```
   RELEASE_LEG_EXEMPT_V1=1     ← חדש (שורה נקייה, בלי הערה באותה שורה!)
   MARGIN_AWARE_SIZING_V1=0    ← ודא שקיים (פסיקת-1:1)
   OPENING_TYPE_GATE=0         ← ודא
   FIXED_CONTRACTS_4=1 · SCALE_IN_V1=1 · SIZE_CAP_OVER_FIXED_V1=1 · STEP_SCALED_LADDER_V1=1
   SYSTEM6_SUPERVISOR=1 · PHONE_ALERTS_V1=1 · MACHINE_TAG=מק-2 · T0_TARGET_PTS=3.0
   ```
   ואז: `python3 scripts/flag_guard.py` → **חייב PASS 172/172**. הדבק.

3. **ריסטארט** (אתה flat) → הדבק boot-line + `grep -a "BOOT-SEED-CANONICAL" /tmp/backend.err.log | tail -1`.

4. **שער-החימוש — ההוכחה המחייבת:**
   `python3 scripts/mems26_arming_gate.py` → **חייב `✅ ALL SYSTEMS ARMED`**. הדבק את כל הפלט.
   בלי זה מק-2 לא נכנס לשוק היום.

5. **הוכחת-4-חוזים:** `scripts/fire_drill.py` → GO, והדבק את שורת-הסייז. הסייז שנשלח חייב
   להיות **4** (אין קיצוץ-מרג'ין — הדגל כבוי; קיצוץ-איכות של הפלייבוק לגיטימי ומדווח).

6. **הדוחף לרנדר** (הדף שלך הראה נתונים בני-10-שעות):
   `launchctl kickstart -k gui/$UID/com.mems26.mobile_relay` →
   `curl -s https://mems26-phone-mac2.onrender.com/healthz` → `age_s<10`. הדבק.

7. **מניעת-שינה:** System Settings → Battery/Energy → **Prevent automatic sleeping**.
   (המכונה נרדמה אתמול ב-23:30 והכל קפא. זה חייב להיות מכובה קבוע.)

8. **דיווח:** רשומה חתומה אחת ב-`LIVE_CHANNEL.md` עם כל הפלטים + `commit`+`push` +
   פוש `[מק-2] ✅ מוכן ללייב 4 חוזים` למייקל.

## מה חדש בהתנהגות היום (שתדע מה לצפות)
- **היפוכים:** כשגלאי-הרגל (LSMA/CCI קנוני) מאשר שהמגמה נשברה — כניסה-נגדית כבר לא
  מוחזקת ב-`awaiting_release`. נגד-הרגל: כל השערים נשארים. (replay 40 סשנים: 5/5 מנצחים,
  43 חסימות-נכונות נשמרו.)
- **שער-הפתיחה כבוי** — לא יחסום עוד מועמדים כמו אתמול.
- **סייז:** 4 חוזים 1:1, עם ‏SCALE_IN לחיזוק מנצח.

## אל תעשה
אל תדליק/תכבה שום דגל שלא ברשימה · אל תריץ ריסטארט בשעות המסחר (אתמול ריסטארט
באמצע-סשן חסם 12 מועמדים ב-`cold_start_guard`) · אל תדווח "בוצע" בלי פלט.
