# פקודה · יום שלישי 08.09 — 5 חוזים, ראנר רק ביום-מגמה, ניהול-דלתון לפי סוג-יום

**מאת:** cowork-dev · **אל:** cc-macbook · **06.09 19:50**
**מחליפה את `CC_DAYTYPE_MGMT_2026-09-06.md`** — שם תיקון 1 (משמעת-כיוון) **בוטל**:
על 10 ימים הוא חוסם 8 מנצחות (‎−$577.50) מול 3 מפסידות (‎+$300) ⇒ **נטו ‎−$121.**
תיקון 3א (‏`has_pullback`) **כבר בוצע** ב-`8514bf33` (‏T-235). הפקודה הזו היא הרשימה.

**פסיקת-מייקל 06.09 19:45:** *"‏5 חוזים ללא ראנר — יש רק סוג-יום אחד שצריך ראנר וזה
טרנד-דיי. שהמערכת תדע לזהות אפשרויות ולתת להן ניהול של דלתון."*

**הבסיס (‏5 סוכנים, 500+ קריאות, יומן-ברוקר):** המערכת ‎≈+$191 בשבועיים, תוחלת
**‎+$6/עסקה.** הראנר (רגל 5) ‎**−$161.25/14**, ‏2/14 חיובי. רגליים 4+5 יחד ‎−$317.50.
**המסווג קורא סנאפשוטים של 2 שניות** ⇒ תווית-השער ב-18:00 = הסופית **0/10 ימים.**

**יום שני 07.09 = חג. יום שלם.** מה שלא מאומת עד שני 23:00 — לא נדלק בשלישי.

---

# 🔴 P0 · תשתית — בלי זה שום דבר למטה לא רלוונטי

| | מצב 06.09 19:20 | ראיה | לתקן |
|---|---|---|---|
| **סיירה** | **לא רצה**, ייצוא מ-05.09 | `pgrep -i sierra` ריק · `fire_drill ✗ feed age=71,891,062ms` | מייקל מפעיל בבוקר שלישי **לפני 15:30** |
| **‏6/9 LaunchAgents** | לא רשומים | `launchctl list \| grep mems26` ⇒ 3 | ‏T-259: `activity_feed` (‏T-227 חוזר) · `mobile_relay` (**FLATTEN מהטלפון לא מגיע**) · `export_promoter` · 3 `BTM disallowed` ⇒ מתג Login Items, לא `bootstrap` |
| **‏`manual_position_ack.json`** | `date=2026-09-04` — **פג** | `entry_guard.py:72-76` ⇒ פוזיציה של אתי חוסמת 100% (‏24-25.08: 27/27) | לחדש בבוקר · **‏T-234** חידוש-אוטומטי — לבנות |
| ‏T-260 | דוח >2000 תווים ⇒ `400` נבלע | `render_mobile_relay/app.py:243` | לפצל |

---

# 1 · 🔴🔴 המסווג קורא ברים שגויים — **~5 שורות, ראשון**

**השרשרת:** ה-DLL מייצא את הדלי-הנבנה (`cpp:358`) → הברידג' דוחף כל 2ש' → `bars.py:764`
מנתב את האחרון → `main.py:258-261` דדופ לפי `ts` → **`main.py:343-347`
`_cls_rth_bars.append({...})` פעם אחת, בהופעה הראשונה, לעולם לא מתעדכן.**
‏S2 לעומתו: `five_min_system.py:1774` `self._bar_buffer[-1] = bar`.

**נמדד 04.09 17:30 (נעילת-IB):** `rib 0.8256` (אמת **1.000** — <1 בנעילה בלתי-אפשרי) ·
`vol_ratio 0.01` (אמת **0.26**). ⇒ `Normal` לא יכול להתקיים (`:534`), פרופיל-DD על נפח
של 2 שניות ⇒ **18 ההיפוכים.** אחרי ריסטארט (ברים מ-DB): `rib 1.8953` נכון.

**לבנות:** ב-`main.py:343-347` — כש-`ts` שווה לאחרון, **להחליף** את הכניסה האחרונה
(כמו S2), לא לדלג. **דגל:** `S1_BAR_REFRESH_V1`, default OFF.
**קבלה:** שורת-17:30 ב-`v9_day_type_state` ⇒ `rib==1.0`, `vol_ratio≈0.25`. **שחזור על
04.09:** היפוכים 18 ⇒ **≤4**. דגל OFF ⇒ זהה-בייט.

**ובאותו commit, שני מתים:** ‏(א) `_prev_neutral_subtype` נקרא ב-`daytype_classifier.py:379`
ואף אחד לא כותב ⇒ היסטרזיס מת ⇒ **31 מ-45** היפוכי-replay הם `Neutral_E⇄C`. לכתוב אותו.
‏(ב) `cumulative_delta` נזרק ב-`bars.py:662-669` ⇒ `cvd_pos=None` תמיד ⇒ "CVD-confirmed"
ופריט-הביטחון של S1 **לעולם לא מתיישרים.** להעביר מפתח אחד.

---

# 2 · 🔴 ראנר רק ביום-מגמה — **הפסיקה, ואפס לוגיקה חדשה**

**היום:** `sierra_command.py:977-994` `RUNNER_TRAIL_V2`: `contracts≥4 ⇒ c4=None` — **בלי
בדיקת סוג-יום.** ‏`c4_target_id=0` ב-4/4 הלא-ZLR ב-04.09. **הראנר ‎−$161.25/14.**

**הפלייבוק כבר אומר בדיוק מה מייקל פסק** (`config/daytype_playbook.yaml`):
```
Trend_Normal    runner: trail_chandelier   c3: hold_to_close           ← ראנר
Trend_DD        runner: trail              c3: trail_behind_structure  ← ראנר
Variation       runner: trail              c3: measured_move           ← יעד!
Normal          runner: trail              c3: opposite_IB_edge        ← יעד!
Neutral_Extreme runner: trail              c3: winning_extreme         ← יעד!
Neutral_Center  runner: none  · Nontrend  runner: none
```
**‏`runner` ו-`c3` — 0 צרכנים.** (`grep -rn "\.runner\|\"runner\"" backend/` ⇒ רק
`opening_playbook_engine.py:81` מקובץ אחר.)

**לבנות:** `RUNNER_BY_DAYTYPE_V1` (default OFF). ב-`sierra_command.py:977`, **לפני**
ההחלטה על `c4`:
```
day_type ∈ {Trend_Normal, Trend_DD}   ⇒ כמו היום: c4 = None (ראנר, טרייל)
אחרת                                  ⇒ c4 = struct_c3 (מ-spacing_levels, ראה §3)
                                        ואם struct_c3 אין ⇒ c4 = t3 (m×risk)
day_type ∈ {Neutral_Center, Nontrend}  ⇒ ראנר none: c4 = t3 תמיד
day_type None / conf<0.25             ⇒ כמו היום (כלל 1: אין החלטה ⇒ אין שינוי)
```
**מקור סוג-היום:** `get_live_day_type()` (‏ANTIFLAP) — **לא** `v9_day_type_state` הגולמי.
**קבלה:** ‏#1073 (Variation) ⇒ `c4_target_id ≠ 0` · ‏#942 (Trend_Normal בכניסה) ⇒ `c4=None`
· OFF ⇒ זהה-בייט · **מוטציה:** הסרת הבדיקה ⇒ נכשל. **ו-DLL:** `cpp:2967` בונה יעד רק אם
`t4>0` — **לאמת שהוא מקבל c4 כשנשלח**, בסים.

---

# 3 · 🔴 היעדים המבניים מחושבים ונדרסים — **זה "ניהול-דלתון"**

**נמדד:**
```
#987   struct c1/c2/c3 = 7735.00 / 7721.00 / 7715.25    נשלח 7744 / 7739.25 / 7734.5
#1073  struct c1/c2/c3 = 7724.00 / 7718.75 / 7715.00    נשלח 7720.25 / 7715 / 7709.75
```
‏`DAYTYPE_TARGETS_STRUCTURAL=1` (‏`.env:108`) ⇒ `structural_targets.py` **רץ**:
`_resolve_variation :203-257` (C1=½IB-ext · C2=nearest-structure · C3=VA-edge/measured) ·
`_resolve_trend_dd :371-402` · תקרות `:515-517` · `_fix_side :574-588`. התוצאה נכתבת
ל-`metadata.spacing_levels` — **והסולם הסופי הוא `m×risk`** (`woodies_system.py:1032-1065`,
`T1_STRUCTURE_END_V1`, `:1061` T1=1R). **מי דורס את מי, ואיפה — לא ניתן להכרעה מהחוץ.**

**לאבחן:** לעקוב אחרי `spacing_levels` מ-`structural_targets` עד `t1/t2/t3` ב-`accept_setup`.
**למצוא את השורה** שבה `m×risk` מחליף את המבני. **ואז:** `STRUCT_TARGETS_WIN_V1` (default OFF):
כש-`spacing_levels` קיים ו-`day_type` ידוע ⇒ `t1/t2/t3 = struct_c1/c2/c3` (אחרי `_fix_side`
ותקרות). **‏m×risk = fallback בלבד.**
**קבלה:** ‏#1073 ⇒ `t3=7715.0` (המבני; **השפל היה 7712.75 ⇒ היה מתמלא**) · OFF ⇒ m×risk.

---

# 4 · 🔴 גרירת-יעד ב-BE — **3/3, $91.25** — חלופת-בקאנד לשלישי

DLL `cpp:3202-3218` השחזור לא רץ (למה — סים בלבד). **לשלישי — בקאנד:**
`STOP_MOVE_TARGET_RESTORE_V1` (default OFF): אחרי כל `MODIFY_STOP` (‏`manager.py:296-345`),
**לקרוא `sierra_state.json → orders[]`** (‏1 שנייה אחרי), להשוות מחיר-יעד לכל רגל מול
`quality.c{n}_target_price` — **ואם זז ב-|Δ| ≥ 1 טיק ⇒ `MODIFY_TARGET` חזרה.** לוג `WARNING`
בכל שחזור (זה מדד לבאג-ה-DLL). **קריאה מהמצב-החי, לא הנחה.**
**קבלה (סים):** BE ⇒ יעד-c3 **לא זז** (או זז וחזר ≤2ש'). OFF ⇒ זהה-בייט.
**‏⚠️ `MODIFY_TARGET` בלי הגנת-סטופ** (`cpp:3227-3246`) — לשלוח **רק** את היעד; **לאמת
שהסטופ לא זז** אחרי השחזור (18:31:04 ב-04.09 הוא זז +8).

---

# 5 · 🟠 ‏ELQ — הרגל מהקצה השבור, לא מהסשן

‏`has_pullback` ✅ (‏T-235, `8514bf33`). **נשאר:** `trading_gateway.py:1789-1794`
`leg_base = session_low/high` ⇒ ביום-הרחבה הרגל מתארכת עם כל בר ⇒ **28 חסימות ב-61.5 נק'.**
**לבנות:** `ELQ_LEG_FROM_BREAK_V1` (OFF): כש-`broken_IB_edge` קיים (‏`v9_tpo_state`/הייצוא),
`leg_base` = הקצה השבור. הפלייבוק: `ref_points: [broken_IB_edge, ...]`.
**קבלה (03.09 שחזור):** DDBL @7722.5 (18:02, pos .86) ⇒ **pos≈0.1 ⇒ עובר** · ZLR @7757.5
(18:50) ⇒ עדיין נחסם · **לדווח כמה מ-28 עוברות + `t1_before_stop` שלהן.**

---

# 6 · 🟠 ‏`RISK_BUDGET` דורס את הקיצוץ-השיפוטי

`sierra_command.py:727` `return` מוקדם **לפני** `SIZE_CAP_OVER_FIXED :730` ⇒ 6/14 עסקאות
שהסייזר ביקש 2 קיבלו 5 (‏948, **953** `sizing: 2`, 963, 968, 998, 1073): ‎−$228.75 ב-5c מול
‎−$93.75 ב-2c. **לבנות:** `min(budget_n, sizing_contracts)` כשהשני קיים. **הזהיר גובר.**

---

# 7 · 🔴 כניסה בזמן — שחרור על דלתא, לא על מבנה (אישור-מייקל 20:05)

**הבר:** 03.09 18:00 — שבירת IBH 7726.5, סגירה 7730.25, ווליום 22,284, **דלתא +4,130 =
המקסימום של היום** (הבר לפני: −232, אחרי: −71). ‏46 מועמדי-DDBL ב-18:00-18:05 — **כולם
נחסמו.** הכניסה הראשונה-מול-השיא: 29.5 נק'.

**למה:** `release_gate.py:21-24, :115-135` — שחרור = `min_higher_lows` שפלים-עולים רצופים
**וגם** ווליום-פולבק < 0.75×. **"‏structure not turning (1/2 higher lows)"** ×98 ביום.
**על בר-דרייב אין שפל-עולה שני במשך 30 דקות.** השער בנוי ל-V-reversal, לא להמשך.

**לבנות:** `DELTA_BREAKOUT_RELEASE_V1` (default **shadow**) — מסלול-שחרור **שני** ב-`release_gate.py`:
```
accepted_break קיים (קצה-IB נשבר, relative_features.py:262-306)
AND דלתא-הבר בכיוון-השבירה ≥ מקסימום-הסשן-עד-כה (v9_bars_cumulative_delta.delta)
AND ווליום-הבר ≥ 0.7 × מקסימום-הסשן-עד-כה
⇒ ReleaseVerdict(True, "released (delta-breakout)") — בלי המתנה למבנה
```
**מקורות:** דלתא מ-`v9_bars_cumulative_delta` (78 שורות/יום, נכתב חי) · **לא** מ-`_bar_buffer`
של S2 (סנאפשוט). ‏`delta=None` ⇒ המסלול **לא מחליט** (כלל 1), נופל למבנה.
**שחזור חובה (יום שני):** 10 ימים, כל בר — כמה פעמים היה משחרר · `t1_before_stop` על
המועמדים ששוחררו · **חייב להראות 03.09 18:00 ו-04.09 17:40.** ‏≥60% על n≥10 ⇒ `live` לשלישי.
**מוטציה:** הסרת תנאי-הדלתא ⇒ נכשל.

# 8 · 🔴 ‏CVD חי ב-S2 — תנאי-מוקדם ל-7

`S2_CVD_DETECTION_V1=shadow` (‏`.env:81`) ⇒ **0 השפעה** (`five_min_system.py:1027` מגן).
**ובאג-הכיסוי:** `:794-796` `_window_min = int(window×0.9)` מול `window=20` **הנומינלי**, לא
מול הזמינים ⇒ `2/20` בפתיחה ⇒ **‏CVD לא זמין בדיוק כשהוא הכי נחוץ** (עד ‎~17:55).
**לבנות:** ‏(א) `window` = `min(20, len(available))`, מינימום 4 · ‏(ב) `1` אחרי שחזור.
**קבלה:** `[S2-CVD] insufficient coverage` **לא מופיע** ב-16:35 · דגל OFF ⇒ זהה-בייט.

---

# 9 · 🔴 שלא יחזור — שלושה שומרים, לפי הצורה

**כל מה שנמצא בשבועיים הוא "כתוב ואף אחד לא קורא" או "רץ ואף אחד לא מודד".**

| # | השומר | תופס | מה היה תופס |
|---|---|---|---|
| **9א** | **`config_consumer_guard.py`** — כל שדה ב-`daytype_playbook.yaml` (ו-`targets.yaml`, `stop_anchors.yaml`) חייב ‎≥1 אתר-קריאה ב-`backend/`, אחרת **נכשל**. אותה צורה כמו `flag_guard` liveness ("all ON flags have ≥1 read-site"), על YAML | כתוב-ולא-נקרא | `bias` · `runner` · `c3` · `contracts` · `ref_points` · `fade_edges` — **6 שדות, ביום הראשון** |
| **9ב** | **‏invariant בנעילת-IB:** ב-`state_persist.py:62` — אם `stage==B2` ו-`rib != 1.0±0.01` ⇒ `logger.error("[S1] rib=%.3f at IB lock — input is not a closed bar")` + שדה `input_suspect=true` בשורה | רץ-על-קלט-שגוי | הסנאפשוט **ביום הראשון**, לא אחרי חודשים |
| **9ג** | **‏T-204 שלבים 4-5 — `mechanism_verdict.py` + `DAILY_VERDICT.md`** (פקודה מ-01.09, **טרם נבנתה**): כל ערב, לכל שער — **מה הוא סירב** (`blocked_candidate_audit.py` קיים) ו-`t1_before_stop` על K מתוקן-סלוט. ‏`NOT_JUDGEABLE` על n<10. **פוש רק על `CANDIDATE_*`** | רץ-ולא-נמדד | ‏03.09 ב-**23:00 חמישי**: *"‏ELQ סירב ל-28, 46% t1, ‏A$ +$1,834 בבוקר"* — לא ביום ראשון |

**‏9ג הוא הלולאה שמייקל ביקש ב-01.09.** בלעדיה כל תיקון מהשבועיים היה מתגלה **רק** כשמישהו
במקרה ספר. **הסדר:** 9ב (3 שורות) · 9א (סקריפט, אותה תבנית כ-`flag_guard`) · 9ג (הבנייה).

---

# ❌ לא לשלישי — ולמה

| | |
|---|---|
| **משמעת-כיוון** (`DAYTYPE_DIR_DISCIPLINE_V1`) | **‎−$121 על 10 ימים.** ‏`shadow` בלבד, **אחרי** תיקון 1 |
| **‏`awaiting_release`** | 116 חסימות, 61% t1 — אבל תוחלת +$6 ⇒ נפח ≠ רווח. פסיקה (‏T-181) אחרי תיקון 1 |
| **‏`SCALE_IN`** | מת מ-01.09 (‏`t3=None` + `RISK_BUDGET` + `T3_REQUIRED`); שרשור 0/4 ‎−$265. **נשאר מת** עד שנבנה בלי שרשור |
| **‏DLL** | אפס deploy בלי סים. תיקון 4 = בקאנד |
| **חלון-פתיחה** 09:30–10:00 ET | ‎−$328.75, 0/4 — **פסיקת-מייקל** (‏T-176 קיימת) — לא שער-זמן בלי מילה ממנו |

---

## סדר: **‏P0 → 1 → 9ב → 2 → 3 → 4 → 8 → 7(shadow+שחזור) → 6 → 5 → 9א → 9ג**

‏1 הוא היסוד (הכול קורא תווית). ‏2+3 הם הפסיקה. ‏4 כסף מדיד. ‏6 שורה. ‏5 אחרון כי הוא
פותח נפח — ונפח בלי 1-4 = עוד הפסדים.

## חובה על כל פריט

דגל · OFF זהה-בייט · **מוטציה** · `RULED_FLAGS` באותו commit · **שחזור על 03+04.09 עם
לפני/אחרי** · `guard_tests.sh` ירוק · **דיווח אחרי כל פריט, `--re` לרשומה.**

## אסור

לגעת ב-DLL בלייב · להדליק דגל בלי הפסיקה · **`has_pullback=True` קשיח** · לכבות
`entry_location_quality` · לתת אחוז על n<10 · לגזור מ-MFE · לבנות משמעת-כיוון חוסמת.
