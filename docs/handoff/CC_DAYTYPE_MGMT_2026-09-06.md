# פקודה · 03-04.09 — למה המערכת לא סחרה את הבטן, וחמישה תיקונים

**מאת:** cowork-dev · **אל:** cc-macbook · **06.09 17:50** · **מחר (07.09) אין מסחר — יש יום שלם.**
**פסיקת-מייקל:** *"בשני הימים השוק עלה או ירד לכיוון מסוים כדי לבנות את הבטן,
והמערכת לא ניצלה הזדמנות. חייב להיות ניהול-עסקה שיאפשר למקסם רווח אבל גם לא
להחזיר כסף לשוק. אני לא מוכן להמשיך להפסיד סתם מדברים שהמערכת צריכה לדעת לבצע."*

**הבסיס:** שני ניתוחי בר-בר מיומן-הברוקר (לא מהספרים), 03.09 ו-04.09. הכול מצוטט.
**כלל-הפקודה:** כל משתנה שנוקבים בו — **`קובץ:שורה` שבו המנגנון-היעד *קורא* אותו.**
אין שורה — סרב.

---

## המספרים

| | 03.09 | 04.09 |
|---|---|---|
| הבטן | ‎+61.5 (17:55→18:55) | ‎−35.25 (17:00→18:30) |
| לייב עם-הכיוון | **0** | **1** (‏#1008, אחרי 83%) |
| נחסמו עם-הכיוון (ייחודי) | **28**, 100% `entry_location_quality` | 6, חמישה שערים + `fusion=None` ×5 |
| לייב נגד-הכיוון | ‏#987 ZLR-S ביום-UP: **‎−156.25** | ‏#1069, #1141 INIT-L ביום-DOWN: ‎−75.00 נטו |
| שיא-הון → ממומש | — | **$427.50 → $152.50 (36%)** |
| ברוקר (מערכת) | **‎−283.75** | **‎−116.25** |

---

# 🔴 1 · משמעת-כיוון לכל תבניות-ההמשך — **$231 בשני הימים**

**הפלייבוק** `config/daytype_playbook.yaml:81-92` (‏Variation):
`bias: "WITH the IB expansion only; enter on pullback to broken edge"`.

**הקוד:** `grep -rn '"bias"\|fade_edges\|ref_points' backend/ --include=*.py` ⇒ **0**.
משמעת-כיוון קיימת **רק** ב-`backend/v9/systems/daytype_playbook.py:194-199` תחת
`require_with_trend`, והמטריצה `daytype_playbook.yaml:164-170` נותנת אותו **רק**
ל-REACTIVE/HNS/CONFLUENCE. ‏**ZLR · INITIATIVE · GB100 — אין.**
`VARIATION_WITH_TREND_CONT_V1=1` (‏`.env:414`) חי רק בתוך אותו ענף
(`daytype_playbook.py:255-263`) ⇒ **אינרטי** לשלושתן.
`CONT_TREND_FILTER` (‏`trading_gateway.py:1937-2006`) משווה ל-`leg_dir` (רגל-חיה),
לא לכיוון-היום ⇒ **0 חסימות** ב-04.09.

**התוצאה:** ‏#987 (03.09 20:20) ZLR-SHORT בזמן ש-`day_dir=UP` · ‏#1069 (04.09 19:25)
ו-#1141 (22:00) INITIATIVE-LONG בזמן ש-34/34 שורות אמרו `DOWN`.

**לבנות:** `DAYTYPE_DIR_DISCIPLINE_V1` (default OFF) — ב-`daytype_playbook.py:199`,
כש-`day_type ∈ {Variation, Trend_DD, Trend_Normal}` ו-`direction_raw` נותן כיוון:
**כל** תבנית-CONT (ZLR/INITIATIVE/GB100/TREND_STEP/BULL_FLAG) נגד-הכיוון ⇒ `SKIP`,
עם `reason="against day_dir"`. **לא** REV (אלה נשארות במטריצה הקיימת).
**מקור-הכיוון:** `v9_day_type_state.direction_raw` (‏`with_extension(DOWN)`) — **לא**
`leg_dir`. ‏`conf < 0.25` או `direction_raw=None` ⇒ **אין החלטה** (כלל 1), לא חסימה.

**אימות:** ‏#987 ⇒ SKIP · ‏#1069/#1141 ⇒ SKIP · ‏#1008/#1073 (עם-הכיוון) ⇒ **עוברות** ·
דגל OFF ⇒ זהה-בייט · `conf=0` ⇒ עובר. **טסט-מוטציה:** הסרת הבדיקה ⇒ נכשל.

---

# 🔴 2 · גרירת-היעד ב-BE — **$91.25 מדיד, 3/3**

**הראיה (יומן-ברוקר 04.09):** בכל BE, יעד-c3 זז **בדיוק ב-Δ של הסטופ:**
```
#1008  18:31:01  BE stops→7716.75      18:31:04  target 10983  7699.25→7707.25  (+8.00)
#1069  19:52:33  BE→7730.75            19:52:39  target 10994  7739.25→7744.75  (+5.50)
#1073  20:29:41  BE→7725.25            20:29:41  target 11005  7715.00→7708.50  (−6.50)
```
‏#1073: השפל 7712.75 **עבר את 7715 ב-2.25** ⇒ היעד המקורי היה מתמלא (‏+$52.50).
הנגרר 7708.5 הוחמץ ב-4.25. **הרגל חזרה ל-BE.**

**איפה:** ‏`sc_study/MES_AI_DataExport_merged.cpp:3131-3224` (‏`MODIFY_STOP`): מצלם
יעדים לפני (‏`:3176-3186`, סלוטים 2/4/6/8) ומשחזר "רק אם סיירה הזיזה" (‏`:3202-3218`).
**ב-3/3 השחזור לא רץ.** למה — **לא ניתן להכרעה** בלי `sc.GetPersistentInt64` בסים.
ובנוסף `MODIFY_TARGET` (‏`:3227-3246`) **ללא הגנת-סטופ** ⇒ 18:31:04 גרר סטופ-c3
‏7716.75→7724.75 — **1.25 מעבר לסטופ-המקורי.**

**לאבחן (סים, אחרי 01:00):** ‏BE על פוזיציה-סים עם 4 קבוצות ⇒ לתעד את הסלוטים
לפני/אחרי ⇒ למה `:3202-3218` לא משחזר. **ואז** לתקן. **חלופה בקאנד** אם ה-DLL
לא ניתן לתיקון מהר: אחרי כל `MODIFY_STOP`, לקרוא `sierra_state.orders[]` ולשלוח
`MODIFY_TARGET` חזרה למחיר-הקודם — **קריאה מהמצב-החי, לא הנחה.**

**אימות:** BE בסים ⇒ **יעד-c3 לא זז** · `MODIFY_TARGET` ⇒ **סטופ לא זז** · 3/3 ⇒ 0/3.

---

# 🔴 3 · `entry_location_quality` חייב לדעת סוג-יום — **‏28 חסימות ב-61.5 נק'**

**הקוד:** `backend/v9/systems/entry_location_quality.py:90`
`pos > pos_max and not has_pullback` ⇒ chaser. **קלט סוג-יום: אין** (‏T-241 grep⇒0).
‏`has_pullback` **לא מועבר** מ-`trading_gateway.py:1803-1812` (‏T-235).
הרגל = `session_high` (‏`:1789-1794`, T-236/T-242) ⇒ ביום-מגמה **הרגל מתארכת עם
כל בר** ⇒ `pos` תמיד גבוה ⇒ **השער נסגר לצמיתות** בדיוק כשהיום זז.

**‏03.09:** ‏46×S2_DELTA_DBL @7722-7730 ב-18:00-18:05 (‏pos .86-.99), ואז @7741, 7746,
7755, 7757 — **כולם ELQ.** הראשון-מול-השיא: 29.5 נק'.

**לבנות (בסדר):**
‏(א) **להעביר `has_pullback`** — הוא כבר מחושב ב-`trading_gateway.py:~1740`, רק לא
מועבר לקריאה ב-`:1803`. שורה אחת. (‏T-235)
‏(ב) **`ELQ_DAYTYPE_AWARE_V1`** (default OFF): כש-`day_type ∈ {Variation, Trend_DD,
Trend_Normal}` **וכיוון-הכניסה = `direction_raw`** ⇒ `pos_max` עולה ל-**0.85** (מקובץ
‏`config/entry_location_quality.yaml`, לא קשיח). נגד-הכיוון ⇒ 0.66 נשאר. ‏`Normal` ⇒
0.66. **הרעיון:** ביום-הרחבה, "מאוחר בתוך המהלך" הוא **המקום הנכון** לפי הפלייבוק —
`enter on pullback to broken edge` — לא רדיפה.
‏(ג) **הרגל מהשבירה, לא מהסשן** (‏T-236): כשיש `broken_IB_edge`, `leg_base` = הקצה
השבור, לא `session_low/high`. הפלייבוק כבר אומר `ref_points: [broken_IB_edge, ...]`.

**אימות (03.09 בשחזור):** ‏DDBL @7722.5 (18:02, ‏pos .86) ⇒ **עובר** תחת (ב) ·
ZLR @7757.5 (18:50, .98) ⇒ **עדיין נחסם** (מעל .85) · דגל OFF ⇒ 28/28 נחסמות כמו
שהיה. **ולדווח: כמה מ-28 עוברות, ומה `t1_before_stop` שלהן** — לא MFE.

---

# 🟠 4 · טרייל-הראנר מת מבנית — **‏0 הזזות ב-88 דק'**

**הקוד:** `backend/v9/services/trade_manager/swing_trail.py:55-57,79-85`
`rev = clamp(ATR-סשן-קודם, 4, 12)` — 03.09 mean-TR 5.07 ⇒ **rev=5.0**.
‏#1073 (04.09): ה-swing-high המאושר האחרון **7734.25 (19:45)** ⇒ עוגן 7734.5 >
סטופ 7725.25 **בכל 19 הברים** 20:30-22:00 ⇒ `tightens=False`. תנודה אמיתית: 12.75.
**⇒ `SWING_TRAIL n=0` כל היום** ב-`v9_trade_management_log`.

**וגרוע מזה:** `bar_level_detector.py:961-962`
```python
if _f5 is not None: pass  # F5 owns the stop this bar (moved or deliberately held)
```
⇒ `DYNAMIC_STRUCT_TRAIL=1` · `STOP_PERBAR_STRUCT_V1=1` · `RUNNER_TRAIL_V1=1`
(‏`.env:175/274/97`) — **לא הורצו על אף בר-פוסט-T1.** שלושה דגלים דלוקים, מתים.

**לבנות:** ‏(א) `rev` מ-**ATR-RTH של הסשן הנוכחי** אחרי 14 ברי-RTH (אותה מחלקה
כ-FIX-8 — `trading_gateway.py:2595`), ולפני כן מהסשן-RTH הקודם. ‏(ב) **להחליט מי
הבעלים של הסטופ** — F5 או השלושה. **לא** להריץ את כולם (הם ילחמו). **המלצתי:** F5
נשאר בעלים, אבל כש-`tightens=False` **ורק אז** — `DYNAMIC_STRUCT_TRAIL` מקבל תור.
‏(ג) `runner_reversal` ×871: ‏`SYSTEM6_REVERSAL_TIGHTEN_V1` (‏`.env:361`, OFF) — **לא
להדליק** בלי פסיקה. לדווח מה הוא היה עושה ב-#1073 (מדידה, לא הדלקה).

**אימות:** ‏#1073 בשחזור ⇒ ‎≥1 הזזה בין 20:45 (‏MFE 12.75) ל-21:57 · דגל OFF ⇒ 0.

---

# 🟠 5 · `contracts: 3` — אפס צרכנים

`daytype_playbook.yaml:37` `max_contracts: 3` · `:92` Variation `contracts: 3` ·
ZLR `REDUCED` ⇒ `ceil(3/2)=2` (‏`daytype_playbook.py:419-420`).
**הגייטוויי קורא רק `_pb.allow`** (‏`trading_gateway.py:1481-1488, :1605`).
‏`_pb.contracts` — **0 צרכנים.** כל 5 עסקאות-04.09 = **5 חוזים**, גם כש-S4 ביקש
`sizing=half, sizing_contracts=2` (‏#998, #1073 `quality.metadata`).

**זה סותר את `RISK_BUDGET_SIZING_V1`?** לא — הוא ב-`sierra_command.py:694` (סיכון-לדולר).
הפלייבוק הוא ממד **שלישי** (סוג-יום). **`final_contracts` ב-`resolver.py:117` כבר
מגדיר `min(risk-ladder, auth, mode_cap)`** — להוסיף `_pb.contracts` כאיבר רביעי
ל-`min`. **הזהיר תמיד גובר.** ‏`RULED_FLAGS` — פסיקת-מייקל קיימת (הפלייבוק).

---

## סדר: **1 → 3א → 2 → 4 → 3ב+ג → 5**

‏1 ו-3א הם שורה-אחת כל אחד ומכסים $231 + פותחים את הבטן. ‏2 הוא כסף שנמדד 3/3.
‏4 מונע החזרה. ‏5 הוא היגיינה.

## חובה על כל פריט

דגל נפרד · default OFF · OFF זהה-בייט · **טסט-מוטציה** · `RULED_FLAGS` באותו
commit · **שחזור על 03.09 + 04.09 עם המספרים לפני/אחרי** (הסקריפטים:
`scripts/opening_atr_audit.py`, `scripts/blocked_candidate_audit.py`,
`scripts/sierra_activity_join.py --date`) · `guard_tests.sh` ירוק.

## אסור

להדליק דגל בלי הפסיקה · לגעת ב-`MODIFY_STOP` בלייב לפני סים · לכבות `entry_location_quality`
("מתקנים ולא מבטלים") · לגזור מ-MFE · לתת אחוז על n<10 · **`has_pullback=True` קשיח**
(‏T-244: זה הופך את השער לאינרטי — לא לתקן ככה).

## ומה שהסוכנים מצאו ולא בפקודה הזו (פתוח, לא היום)

‏T-251 ספרים סגרו עם חוזה חי · ‏T-31 `OPENING_DIR_FUSION` 0/5 (‏1,211× "‏opening_vol <
median") · ‏T-252 כפילות-צל (‏162→42) · ‏T-241 `Trend_DD` ב-1.675 · תווית-יום מתהפכת
‏18× ב-34 שורות עם `ANTIFLAP=1` · `/tmp/backend.err.log` **מאותחל ב-boot** — הלוג של
03-04.09 **אבד**; ‏`OPS_LOG_*.md` הציל. **לרוטציה, לא לאיפוס.**
