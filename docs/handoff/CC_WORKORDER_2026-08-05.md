# cc — פקודת-יום 05.08 (פסיקות-מייקל הבוקר). בזמן-מסחר: flag-OFF/אנליזה בלבד.

אתמול: לייב +$535 (0 מפסידות, T3 עבד), צל +$1,135, סיווג Trend_Normal מדויק. מייקל סגר
ידנית באמצע — הפער לייב/צל הוא בעיקר זה. המיקוד: דיוק, לא שינוי-כיוון.

## M1 — ביקורת-מימושי-מערכת-6 (השאלה של מייקל: "לא ברור אם נכון")
על כל עסקאות 03-04.08: כל התערבות-S6 (BE-אחרי-T1, MAE-scratch אם היו, autocorrect) —
טבלה: עסקה · ההתערבות · תוצאה-בפועל · מה-היה-קורה-בלעדיה (מהברים) · דלתא-$.
אתמול נמדד: לייב-ניהול מול צל עלה ~$72 על 3 עסקאות (620:−16 · 622:−20 · 625:−36 BE-סטופ).
מסקנה נדרשת: האם BE-אחרי-T1 מוקדם-מדי ביום-מגמה (קשור לרצפת-הסטופ) — המלצה+ראיות, בלי קוד.

## M2 — מערכת-אבחון-אחרי-הפסד (POST_MORTEM_V1, בנה — לא-flag, observability בלבד)
פסיקת-מייקל: "אחרי עסקה שלא הצליחה — לאבחן מה לא זוהה נכון". בכל סגירת-עסקה בהפסד
(לייב או צל): כתיבת דוח-אוטומטי ל-docs/reports/postmortem/PM_<trade_id>.md +שורת-סיכום
ל-DB: סוג-יום-בכניסה מול סוג-יום-EOD-אמת · רגל · מיקום-בטווח · ציון-S7 (מחושב גם כבוי) ·
השערים שעברו/כמעט-חסמו · MAE/MFE · מנגנון-היציאה · ו"פסק-דין-שורש" מתבנית סגורה
(סיווג-שגוי / כניסה-מאוחרת / סטופ-צר / ניהול / רעש-תקין). מצטבר שבועי = קלט-הכיול.
זה observability טהור — בלי שינוי-התנהגות; חייב טסטים שהוא לא חוסם את לולאת-המסחר.

## M3 — תור-הלילה שלא בוצע (מ-04.08 ערב)
(א) חירום-בטלפון (CC_TASK_2026-08-04_MOBILE_EMERGENCY.md) — אחרי-סגירה.
(ב) S7 replay-קבלה (14 ימים כולל 03-04.08) — חוסם-הדלקה לפסיקת-מייקל.
(ג) רצפת-סטופ replay + תיאום-MAE-scratch (רצפה+1) — חוסם-הדלקה; מתכתב עם ממצא-M1.
(ד) בידוד-טסטים · FLAG_REGISTRY חוב-78.

## M4 — פרופיל-שוק זמן-אמת (תוספת-מייקל 05.08: "לא היה פרופיל שוק זמן אמת")
עמודת-היום ב-TPO-panel חייבת להתעדכן חי לאורך כל הסשן: today_block נבנה טרי בכל קריאה —
לאמת שהוא מופיע מהבר-הראשון (16:35) ומתעדכן כל דקה (POC/VA/אותיות מתקדמות), כולל אחרי
ריסטארט (תלוי-rehydrate של סעיף-5 מ-04.08). בנוסף: לחשוף את הפרופיל-המתהווה גם למסווג
(S1 קורא POC/VA-של-עכשיו לאימות-סיווג תוך-יומי) — קריאה-בלבד, בלי שינוי-התנהגות, flag-OFF
אם נוגע בלוגיקה. קבלה: צילום-מצב ב-3 זמנים בסשן (פתיחה/אמצע/ערב) עם ערכים מתקדמים.

## M5 — פסיקת-מייקל 05.08 ערב: מצב-צל-3-ימים ל-S7 ולרצפת-הסטופ (מאושר)
שני ה-replays החזירו NO-GO מסיבת-דאטה (אין הקשר-חי בהיסטוריה) ⇒ אימות-קדימה:
1. **S7_SHADOW_LOG_V1** (בנה-ON — observability בלבד): על כל החלטת-גייטוויי חיה (עוברת
   או נחסמת) חשב את ציון-S7 המלא מההקשר-החי ורשום ל-gateway_decisions.jsonl:
   {s7_score, s7_components, s7_would_size}. אפס-השפעה על ההחלטה עצמה.
2. **TSF_SHADOW_LOG_V1** (בנה-ON — observability): בכל עסקה חיה עם-מגמה רשום:
   {stop_actual, stop_floor_would_be, floor_would_change: bool} + בסגירה: האם הרצפה
   הייתה משנה את התוצאה (מהברים).
3. אחרי 3 ימי-מסחר (מדידה: 05-06-07.08 או 06-09-10.08 לפי ההתחלה): דוח-הצטברות אחד —
   דליי-ציון מול תוצאות-בפועל + דלתא-$-רצפה ⇒ פסיקת-הדלקה סופית.
4. **תקלת-היום לחקירה:** כותב-v9_day_type_state מת ב-11:45 ("writer may be dead SYS-2",
   קם רק בריסטארט 17:0x) — שורש+תיקון+watchdog. וגם: M1+M2 עדיין לא נמסרו — בתור.
