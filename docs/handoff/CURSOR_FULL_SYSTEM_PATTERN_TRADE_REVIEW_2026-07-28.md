# CURSOR — סקירה מלאה: כל המערכות · כל התבניות · כל העסקאות

**מבקש:** מייקל, 2026-07-28 — *"סקירה לכל המערכות, לכל התבניות ולכל העסקאות של המערכת שבוצעו וייתן הערות"*
**בעלים:** `cursor-agent` · **מזמין:** `cowork-dev`
**מצב:** מייקל מחזיק **פוזיציה חיה** (לונג 6 @7430). **קריאה בלבד — אפס כתיבה ל-DB/סיירה.**

---

## 0. איך לעבוד כאן

1. **אינדקס תחילה** (CLAUDE.md): `SYSTEM_INDEX.md` · `_INDEX.md` פר-תיקייה · `docs/SOURCE_OF_TRUTH.md` · `docs/FLAG_INDEX.md`. אל תגרפ בעיוורון.
2. **חוק-5:** כל טענה = פקודה + פלט-גולמי. גם "תקין" צריך ראיה.
3. **הערה = החלטה.** לכל פריט תן פסק: `שמור · כייל · נטרל · חקור`. סקירה בלי פסק לא נחשבת.
4. **אל תסמוך על cowork.** אני טעיתי היום במספרי-P&L; מייקל תפס. כל מה שכתבתי = טענה לבדיקה.
5. **קדימות:** אם אתה מוצא סיכון-מסחר פעיל → **🔴 מיידי ב-`LIVE_CHANNEL.md`**, לא לחכות לדוח.

### התלות המחייבת ⚠️

`CURSOR_TRADE_LEDGER_AUDIT_2026-07-28.md` (משימה 20) קובע **האם הפנקס בכלל קביל**.
הממצא הפותח: **`pnl_sierra` ריק ב-36/36 עסקאות-לייב** — כל P&L הוא חישוב שלנו, ו-9 עסקאות
נושאות P&L **בלי מחיר-יציאה**. לכן:

> **אל תדרג תבנית לפי `pnl_usd` לפני שביקורת-הפנקס אמרה מאיזה תאריך הנתונים קבילים.**
> אם צריך — הרץ קודם את משימה 20. דירוג על נתונים פסולים גרוע מאי-דירוג.

**מה שכן קביל תמיד:** `~/SierraChart/TradeActivityLogs/TradeActivityLog_<יום>_UTC.<חשבון>.data`
(קובץ ליום, `strings`), ו-`sierra_state.json` להווה.

---

## 1. ציר A — כל המערכות

לכל אחת: **מה היא אמורה לעשות · מה היא עושה בפועל · האם היא מחווטת · מה שבור · פסק**.

| # | מערכת | קוד | שאלות-מפתח |
|---|---|---|---|
| S1 | סוג-יום + כיוון | `systems/day_type/`, `day_direction.py` | ‏7 הסוגים מסווגים בלי lookahead? ‏`classify_replay` מול החי — פער? ‏IB-lock/staging (15/30/60 דק') מכובד? |
| S2 | חמש-דקות Reactive/Initiative | `systems/five_min/` | ‏COT/AMT מנוטרל (‏S2⟂S3) — מה זה עושה לאיכות? ‏`choppiness_ok` כבוי — מוצדק? |
| S3 | פוטפרינט | `systems/footprint/` | **נדחה עד אחרי לייב.** רק ודא שהוא לא משפיע בשקט |
| S4 | טקטי | `systems/` (חפש באינדקס) | ‏`S4_OVERRIDE_AWARE_V1` · שער-מיקום-סוג-יום · ‏0 ירי-לייב היסטורית — למה |
| S6 | מפקח-עסקאות | `system6_supervisor.py`, `system6_journal.py`, `system6_exit_signals.py` | ‏9 האינווריאנטים — מי חי מי inert? ‏`AUTOCORRECT=protective` — מה בפועל נשלח? |
| L0 | Layer-0 chop | `systems/layer0/` | וטו כבוי בפסיקה — מחושב ונרשם עדיין? |
| GW | שער-המסחר | `gateway/trading_gateway.py` | סדר-השערים · `blocked_by` · האם יש **wiring מת** (ענף שלא נקרא) |
| — | פתיחה | `opening_entry.py`, `opening_type_gate.py`, `opening_detector_v2` | ‏5 הטריגרים · חלון-60-דק' · ‏`OPENING_DIR_FUSION` |
| — | ניהול-פוזיציה | `stop_anchors/`, `structural_targets.py`, `target_structure_clamp.py`, `trade_manager/` | ‏T1/T2/T3 · BE · ראנרים · האם היעדים מבניים או קבועים |

**חובה בציר הזה:**
- **מפת-חיווט:** לכל מערכת — מי קורא לה בזמן-ריצה. **סמן כל מה שלא נקרא כלל.** (‏W6 `higher_low` = 0 imports — דוגמה ידועה; חפש עוד.)
- **הצלבה מול `docs/FLAG_INDEX.md`:** דגל שדלוק אך הקוד שלו לא נקרא = חיווט-מת. דגל שכבוי אך ההתנהגות קיימת = דליפה.

---

## 2. ציר B — כל התבניות

התבניות שירו בפועל (מ-`v9_trades.pattern_id_at_entry`), עם ספירת לייב/צל:

```
ZLR 28/113 · REACTIVE_SHORT 14/60 · REACTIVE_LONG 7/38 · GB100 6/10 · GHOST 4/8
INITIATIVE_LONG 3/12 · INITIATIVE_SHORT 2/22 · BEAR_FLAG_SHORT 2/5 · FAMIR 2/9
HTLB 2/7 · CONFLUENCE_RI_ZLR 1/1 · TLB 0/50 · HFE 0/27 · BULL_FLAG_LONG 0/5 · VEGAS 0/4
```

לכל תבנית:

1. **הגדרה מול מימוש** — `docs/handoff/CURSOR_PATTERN_BIBLE_2026-07-19.md` (15 כרטיסים) מול הקוד. פער = ממצא.
2. **‏TLB 50 צל / 0 לייב · HFE 27/0 · BULL_FLAG 5/0 · VEGAS 4/0** — **למה אף אחת לא הגיעה ללייב?** שער? גודל? חיווט מת? זו השאלה החשובה בציר.
3. **‏ZLR** — הכי פעילה (141). פסיקת-כיול ממתינה (צל ‎−1,831 בוריאציה מול ‎+71 ב-Normal). אשש/הפרך **מנתונים**, והמלץ.
4. **‏GHOST** — הצעה לדלג בוריאציה. אשש/הפרך.
5. **‏INITIATIVE_SHORT** — חשד למיפוי-OFA שגוי. **קודם תקן את המיפוי, רק אחרי זה תשפוט.**
6. **מטריצה תבנית × סוג-יום × מיקום** — איפה כל תבנית עובדת ואיפה לא. השווה ל-`config/daytype_playbook.yaml`.
7. **פסק לכל תבנית:** `שמור · כייל · נטרל · חקור` + נימוק במשפט.

---

## 3. ציר C — כל העסקאות שהמערכת ביצעה

**היקף:** כל `v9_trades` (‏`live` · `demo` · `shadow`), לא רק לייב.

1. **טבלה מלאה** — כל עסקה: מזהה · מצב · mode · תבנית · סוג-יום בכניסה · כיוון · כניסה/סטופ/יעדים · יציאה · סיבה · `pnl_usd` · `pnl_sierra`.
2. **פילוח-סיבות-יציאה** — כמה `STOP_HIT` · `T1/T2/T3` · `SIERRA_FLAT` · `phantom_reconcile` · `ORDER_FAILED` · `CANCELLED`. **‏`phantom_reconcile` ו-`SIERRA_FLAT` הן סיבות-בקרה, לא תוצאות-מסחר** — הפרד אותן.
3. **צל מול לייב** — לכל תבנית: מה עשה הצל ומה עשה הלייב. פער גדול = שער חוסם דווקא את הטובות (או להפך).
4. **עסקאות שנחסמו** — מ-`blocked_by`/לוגים: אילו איתותים לא ירו ולמה. **אלה נתונים חסרים בכל דוח עד היום.**
5. **‏10 העסקאות הגרועות ו-10 הטובות** — לכל אחת שורה אחת: מה קרה, ומה הלקח.
6. **חשד לכפילות:** `544`/`546` — שני צללים ל-ZLR אחד (7424.5 ו-7424.75, 20 דק' הפרש). באג או איתות חוזר לגיטימי?

---

## 4. ציר D — הצטברות-סיכון

לא פר-עסקה אלא פר-יום: מקסימום פוזיציה בו-זמנית · חשיפה מצטברת · `RISK_HALT`/`SIZE_CAP_CUT` —
נורו? חסמו את הדבר הנכון? האם היו רגעים שבהם המערכת **ומייקל** היו בשוק יחד באותו כיוון?

---

## 5. תוצר

`docs/handoff/CURSOR_FULL_REVIEW_VERDICT_2026-07-28.md`:

1. **‏10 שורות בראש** — הדברים החשובים ביותר, לפי סדר. אם מייקל קורא רק את זה, זה מה שהוא צריך.
2. **טבלת-מערכות** — פסק לכל אחת + רשימת חיווט-מת.
3. **טבלת-תבניות** — פסק לכל אחת + המטריצה.
4. **טבלת-עסקאות** + הפילוחים.
5. **מה שבור עכשיו** — מסודר לפי סיכון-כספי, לא לפי קלות-תיקון.
6. **מה שנראה תקין** — במפורש. גם זה ממצא.
7. **פסיקות שמייקל צריך לתת** — כל אחת: השאלה · האפשרויות · ההמלצה שלך · הנתון התומך.

**כלל-אורך:** אם פריט אין לו הערה מעשית — שורה אחת. חסוך את המקום למה שכן.

בסיום: שורה חתומה ב-`LIVE_CHANNEL.md` (‏🔴 OPEN → ✅) + `commit`+`push`.
