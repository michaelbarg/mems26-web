# Work Plan — 2026-07-08 (Wed) · execute + VERIFY before trading

**Deadline:** everything below runs and is verified **before RTH open 08:30 CT / 16:30 IL.**
**Priority:** 🔴 LIVE only · ⚪ shadow = nice-to-have.
**Rules:** Rule 5 (paste raw command + output, never "done") · no backend restart / deploy while a
trade is OPEN — only when FLAT · snapshot before any `.env` / DLL / LaunchAgent change
(`scripts/mems26_snapshot.sh "label"`) · keep safety ON (−$400 halt, R:R gate, entry-confirm, 2c).
**Owners:** CC = live Mac (Sierra, restart, live DB/logs, DLL). Cowork = verify from repo/DB, build
code + tests, keep docs current. Live board of all this: **/board** + `docs/plans/LIVE_FIX_JOURNAL.md`.

> This plan covers **every topic raised 2026-07-07**. IDs match the task board (`task_board.json`) and
> the journal (L1–L8). Each item = **action → verify → gate**.

---

## Phase 0 — Pre-open readiness (do first, while FLAT) · CC + Cowork verify
- **0.1 Health** — `scripts/mems26_verify.sh` → services (:3000 **and** :8000), DLL↔repo, feed fresh,
  DB lag. Paste. (Frontend :3000 has been down — bring it up; the board + monitor need it.)
- **0.2 Flat + clean DB** — 0 open trades, both gateway slots `null`, reconcile `AGREED_FLAT`
  (yesterday's `MISMATCH_ORPHAN_DB` db_open=[401–405] must be resolved/explained). Paste.
- **0.3 Snapshot** — `scripts/mems26_snapshot.sh "pre-0708"` before any change.
- **0.4 Orderly index (A7 traceability — Michael)** — `python3 scripts/gen_index.py` +
  `python3 scripts/gen_flag_index.py --check` → commit refreshed `SYSTEM_INDEX.md` / `_INDEX.md` /
  `FLAG_INDEX.md`. This is the "where to turn" map the board links to.

## Phase 1 — ✅ BLOCKER-1 RESOLVED (2026-07-07 eve) — re-verify arming holds · CC
- **Status** — Michael: **2 live trades executed today, BOTH succeeded.** Real-money order path works
  (the 303 `GENERAL_ERROR_OR_NOT_ENABLED` was live-account arming, not our code — SIM passed on the
  same DLL). **No longer a blocker.**
- **Re-verify (Rule 5)** — after any restart, arming can drop: confirm the live account 37138283 is
  still armed (order-placement / Auto-Trading) with one small live/SIM order → `ORDER_SUBMITTED`
  error=0. Paste. Then confirm **both of today's live trades appear in the Live Ledger** (Phase 3.1)
  with correct fills/P&L — that closes the loop Michael cares about (frontend ↔ Sierra).

## Phase 2 — 🔴 Verify today's fixes on the live/SIM path (Rule 5) · CC
- **2.1 L2 live MODIFY** — fire → T1 hit → confirm the BE stop-move actually reaches Sierra
  (paste the `sc.ModifyOrder` line), not just the DB record. (fix `eb4bc6f`)
- **2.2 L2-residual** (build) — a trade captured via the I-58 fallback still gets its
  `sierra_order_id` so the MODIFY isn't silently dropped; convert both silent `return`s in
  `_emit_modify_stop` (manager.py:121-125) to rate-limited warnings. Shared root with L4.
- **2.3 L1 / A7** — one live ZLR/GHOST fire **routes** — no `[Woodies] FIRE DROPPED … failed_stages=['A7']`.
  Paste the routing log. (fallback woodies_system.py:668)
- **2.4 ea868cc regression** — add a guard/test so no `exit_reason` can overflow varchar(30) again
  (the 42-char GENERAL_ERROR crashed the DB session → 299 stuck PARTIAL).

## Phase 3 — 🔴 Builds owed (FLAT only · flag-OFF · tests + raw verify) · CC / CC+Cowork
- **3.1 L8 — Sierra ledger: ENGINE + UI BUILT (Cowork) → CC DEPLOY + VERIFY** 🔴 investor-critical.
  Built: `sierra_ledger.py` (reader/ledger/reconcile/manual-tag, **7/7 tests green**) ·
  `GET /api/v9/live_ledger` (wired app.py) · `/board` LiveLedgerPanel (flag `LIVE_LEDGER_V1`, OFF).
  **CC:** enable `LIVE_LEDGER_V1` → restart when FLAT → feed the Sierra **TradeActivityLog**
  (`sierra_stop` + stop-move history; fills alone cover entry/exits/P&L, the stop-MODIFY history needs
  the log) → **verify V1–V5 against the 2 real live trades** (round-trip to the cent, manual stop-move,
  manual close, reconcile MATCH↔mismatch, MANUAL vs SYSTEM). Paste raw Sierra evidence.
- **3.2 L4 — live fill capture** on a real fill: map the order_id (no fallback) → tracked trade +
  Sierra P&L. **CC.**
- **3.3 L7 — 2-contract symmetry**: bracket/targets + per-contract P&L + R + Sierra command + display
  all read contracts=2 (no phantom 3rd target). **CC.**
- **3.4 L3 — monitor** shows SHADOW as "live" + no P&L → fix mode/label + P&L wiring. **CC.**

## Phase 4 — 🟡 Secondary / backtest (non-blocking — must NOT delay live) · CC
- **4.1 L5 day-type lag** (task #22) — build flag-OFF, backtest net-R before enable; don't loosen R:R.
- **4.2 L6 T1/T2 P&L** from the Sierra fill (task #17).
- **4.3 L2(b) design** — after-T1 stop trails to **STRUCTURE** (nearest structural level), not BE;
  flag-OFF + backtest. (Separate from the L2 bug fix.)

## Phase 5 — Frontend (Cowork · no backend change) · built today
- **5.1** `/board` live — **Live Ledger** (real Sierra trades) + **System 6 supervisor** + the
  prioritized task board. Verify it renders once :3000 is up (`board_logic.test.mjs` green; page =
  `src/app/board`). The Live Ledger needs `LIVE_LEDGER_V1` on + a backend restart (3.1).
- **5.2** Decide with Michael: embed the board in the **main dashboard**, or keep the standalone route.

---

## FINAL GATE — before 16:30 IL (Michael sign-off)
1. **BLOCKER-1 green** (live order submits + fills) — else **no real money**, DEMO/SHADOW only.
2. Phase 2 verifications pasted (L2 live MODIFY, A7 route) — Rule 5.
3. Board + journal reflect reality; snapshot taken; safety flags still ON (−$400 halt · R:R · 2c).
4. Anything not finished → carried on the board with an owner + next action (nothing lost).

_Topics-raised-today index → board IDs: BLOCKER-1 · L1 A7 · L2 (+residual, +L2b design) · L3 · L4 ·
L5 · L6 · L7 · L8 · ea868cc regression · FE board/System6. Full detail per item: `/board`._
