# פקודת-עבודה · שלושת השורשים — ההחזר הגבוה ביותר בריפו

**מאת:** cowork-dev · **אל:** cc-macbook · **תאריך:** 2026-08-25 17:25 IL
**פסיקת-מייקל:** "מאשר" — על ההמלצה *"אל תתקן 59 דגלים. תקן שלושה שורשים."*
**מקור:** `docs/reports/FLAG_AUDIT_2026-08-25.md` (ביקורת-אמת של כל 204 הדגלים)

## הרקע במספר אחד

```
174 דגלים דלוקים:  84 ✅ מבצע  ·  41 ⚠️ חלקי  ·  18 ❌ מת  ·  31 ❓ לא-נבדק
```
**‏59 מ-174 אינם מבצעים את מה שנפסק. רק 48% מוכחים כעובדים.**
‏`flag_guard` עובר 204/204 כי הוא בודק ש-`.env` תואם ל-`expected` — **לא שהדגל עושה משהו.**

**אבל אלה לא 59 באגים.** שלושה שורשים מסבירים את רובם. **זו העבודה.**

---

## ⚠️ המסחר פועל

**אל תיגע ב-`.env`. אל תריסטארט. שלושתם flag-gated, ברירת-מחדל OFF.**
בנייה בזמן סשן מותרת — commit לא משנה תהליך רץ. **הדלקה = שער cowork+מייקל אחרי §D.**

---

# שורש 1 · `backend.v9.app` מול `backend.main`

**‏חמישה דגלים מתים מתכונה אחת. תקן אותה — וכולם קמים.**

הכותב היחיד של `app.state` בייצור הוא `backend/main.py:674`. כל קוד שמייבא
`backend.v9.app` מקבל אובייקט-FastAPI **אחר** שאיש לא כותב אליו.

| נפגע | ראיה |
|---|---|
| `MARKET_CONTEXT_V1` ❌ | `market_context.py:112,:34` — `app.state.opening_type_result` אפס כותבים; `balance=UNKNOWN` ב-**673/673** |
| `OPENING_FIRST_TRADE_STRICT_V1` ⚠️ | חוסם **7/7**, כולן `conf 0.0 < 0.6` — כי הקלט מת |
| `OPENING_CONF_ENGINE_FUSE_V1` ❌ | `opening_entry.py:406` — נבנה בדיוק להציל `conf=0.0`, ושומרו הוא `conf >= 0.5` |
| `OPENING_PLAYBOOK_V1` · `OPENING_WINDOW_FIRE_V1` | בלי קלט |
| `S1_DAY_DIRECTION_V1=shadow` ⚠️ | `trade_context.py:878` — אותו ייבוא; `expansion=None` ב-673/673 |

**הפתרון כבר קיים בריפו פעמיים:** `trading_gateway._resolve_live_cls()` (`:195`) ו-
`daytype_watchdog._resolve_app_state()`. **‏ADAPT אותם — אל תכתוב שלישי.**

**מה לבנות:** `APP_STATE_ROOT_FIX_V1` — פותר את ה-app החי בכל אתר-קריאה.
‏flag-OFF זהה-בייט. **טסט-מוטציה** לכל אתר: החזרת הייבוא ל-`backend.v9.app` ⇒ הטסט נכשל.

---

# שורש 2 · `_pattern_family()` מחזיר None ל-`S2_DELTA_DBL`

**‏`daytype_position_gate.py:55-72`.** התבנית `S2_DELTA_DBL_*` אינה ממופה למשפחה,
ולכן **בלתי-נראית לארבעה שערים ולפלייבוק**.

**‏היא 105 מתוך 108 חסימות-הגייטוויי.** כלומר רוב מה שחוסם אותנו כרגע לא נבדק
בכלל ע"י שרשרת-ההחלטה.

**זה שינוי-סיכון-מסחר** — הוא **פותח** מסלולים שהיו סגורים. **מייקל אישר** את התיקון;
הוא עדיין לא אישר הדלקה. ⇒ `PATTERN_FAMILY_DELTA_DBL_V1`, ברירת-מחדל OFF, §D מלא.

**‏§D חובה:** דלתא **וגם** חציון-יום · פר-mode (live/shadow בנפרד) ·
**וספירה: כמה מ-105 החסימות משנות פסק.** אם 0 — `NOT_JUDGEABLE`, לא "ניטרלי".

---

# שורש 3 · שני כותבים לסטופ

**‏`StepLadder` דורס את `StopResolver` ב-68 מתוך 69** (`OVERRODE StopResolver` בלוג).
‏`STOP_RESOLVER_V1` שרד פעם אחת.

**קודם אבחון, אחר-כך תיקון:** מי צריך לגבור, ומתי? אל תבחר לבד — **תן את הראיה**:
מה ה-P&L של 68 המקרים תחת כל אחד מהשניים. זו פסיקת-מייקל, לא בחירת-מפתח.

---

# 4 · שן שנייה ל-`flag_guard` — כדי שזה לא יחזור

הביקורת נמצאה **ידנית**. הכלי שאמור לתפוס אותה עובר 204/204.

**להוסיף ל-`scripts/flag_guard.py`:**
1. כל דגל דלוק חייב **≥1 אתר-קריאה בקוד-ייצור** (לא טסטים, לא `_INDEX.md`,
   לא `FLAG_REGISTRY.yaml`). **‏0 אתרים ⇒ NO-GO.** זה לבדו היה תופס את
   `OPENING_WINDOWS_V1` ו-`OPENING_RUNNER_RIDE_V1`.
2. **דגל-הורה:** אם הדגל חסום מאחורי הורה כבוי — לדווח **`INERT (parent OFF)`**.
   זה תופס 6 מהמתים: `EXTREMES_AWARE_REALIZE_V1` · `S4_GRAY_RELABEL_CCI` ·
   `DAYTYPE_PATTERN_AWARE_V1` · `ENTRY_BUDGET_SKIP_LOSERS_V1` ·
   `ENTRY_BUDGET_QUALITY_V1` · `SIZING_CONSOLIDATION_V1`.
3. **דגל שערכו == ברירת-המחדל בקוד ⇒ `INERT (no-op env line)`.**
   תופס 5: `MEMS_MIN_RISK_POINTS` · `RISK_CUTOFF_HOUR_ET` · `RISK_CUTOFF_MINUTE_ET` ·
   `RISK_MAX_TRADES_DAY` · `TREND_CCI_DIRECT_PT`.

**זה לא חוסם GO — זה מדווח.** אבל מהיום, דגל בלי אתר-קריאה לא נחשב "דלוק".

---

## סדר-ביצוע

| # | מה | למה בסדר הזה |
|---|---|---|
| 1 | **שן שנייה ל-`flag_guard`** | הכי זול, אפס סיכון, ומיד נותן מפה מדויקת |
| 2 | **שורש 1** | חמישה דגלים מתיקון אחד; לא נוגע בשערי-ירי |
| 3 | **שורש 3 · אבחון בלבד** | ראיית-P&L לפסיקה. בלי לשנות |
| 4 | **שורש 2** | הגדול והמסוכן. אחרון, עם §D מלא |

## אסור

`.env` · ריסטארט · `op=EXIT` (אומת נקי — אפס קוראים; **שמור על זה**) ·
לגעת בגודל/סולם · טבלה שלישית · `logger.debug` על כשל · להדליק דגל.

## מסירה

`docs/reports/CC_3ROOTS_2026-08-25.md` — פר-שורש: file:line · פקודה+פלט גולמי ·
טסט-מוטציה · §D היכן שנדרש · NOT-VERIFIED מפורש.
LOG ב-`LIVE_CHANNEL`. `commit`+`push`. **ואז עצור.**
