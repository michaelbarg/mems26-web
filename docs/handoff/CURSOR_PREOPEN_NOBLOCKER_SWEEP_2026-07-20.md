# קורסור — סריקת אין-מחסום לפני-פתיחה (2026-07-20, ~4ש' לפני 16:30)

**פסיקת-מייקל:** *"בדיקה של המערכת שאין שום מחסום לעבודה של כל המערכות."*
**מבצע: cursor (קריאה-בלבד).** מאמת: cowork. **is_sim=1 — אין PLACE, אין .env, אין שינוי-קוד.**
**מטרה: טבלת GO/NO-GO פר-מערכת עם ראיה — כל 🔴 = מחסום לסגור לפני הפתיחה.**

## מה לבדוק (כל שורה = פקודה + פלט-גולמי + 🟢/🔴)

### A · צינורות-נתונים (בלי זה שום מערכת לא רואה)
- Sierra export טרי: `ls -la ~/SierraChart_Data/v9_export/*.json` — mtime < כמה-דקות (בזמן-מסחר).
- bridge→localhost בלבד + רץ · export_promoter רץ · backend :8000 = 200.
- DB טרי: הבר-האחרון ב-`v9_bars_5min_woodies` — כמה-דקות (לא מעופש). CVD `cumulative_delta.json` זורם.
- `scripts/mems26_verify.sh` — deployed-DLL==repo · index · feed · DB-lag.

### B · כל מערכת יכולה לעבוד? (אין מחסום-לוגי)
- **S1 (סוג-יום):** `classify_replay` היום מחזיר סוג (לא FORMING-תקוע) · `get_live_day_type` עונה ·
  מקור-אחד (לא נסיגה-למת). ראיה: הרצה.
- **S2 (חמש-דקות):** מנוע נדרך + מקבל ברים (buffer מתמלא) · FHB לא-תקוע · `S2_VSA_VOLUME` לא חוסם-הכל.
- **S4 (woodies):** מקבל current_bar עם טרנד-עדכני (G1) · דטקטורים נטענים · A7 לא-חוסם-הכל.
- **S6 (מפקח):** רץ · `SYSTEM6_SUPERVISOR=1` · לא-פולט op=EXIT.
- **Gateway:** `/gateway/decisions` מגיב · שערים לא-כולם-חוסמים (אין חתימת-07-09 של 0-fire) ·
  `flag_guard` PASS.

### C · שערים שחסמו בעבר — לוודא שלא חוזרים (מחסומי-שישי)
- סף-כניסות = 15:30 ET (לא 14:30) · `entry_not_confirmed` לא על מחיר-מעופש (זיהום תוקן) ·
  `cont_trend`/location לא over-block · paint לא GRAY-דביק על הבר-החי.

### D · תצוגה + פלאפון (שהמשתמש רואה מצב-אמיתי)
- פרונטאנד :3000 = 200 · day-type/paint override-aware · פלאפון `10.1.118.147` מגיש local (לא iMac).

### E · בטיחות
- `is_sim` (עכשיו 1) · חשבון שטוח · שערי-בטיחות דלוקים (RISK_HALT/EOD_FLATTEN/FEED_WATCHDOG/LIVE_EXECUTION).
- **⛔ תזכורת-סגירה:** לפני 16:30 להחזיר סיירה ל-LIVE + לאמת `is_sim=0` (אחרת נפתח על סים).

## תוצר
`docs/handoff/PREOPEN_NOBLOCKER_2026-07-20.md` — טבלת מערכת × [בדיקה/פקודה/פלט/🟢🔴]. בסוף: **רשימת
כל ה-🔴 (מחסומים) ממוינת** + מה נדרש לסגור כל אחד לפני הפתיחה. חוק-5: אין 🟢 בלי פקודה+פלט.
