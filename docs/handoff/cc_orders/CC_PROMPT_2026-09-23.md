# פסקת-פתיחה ל-cc-macbook — 23.09.2026 (cowork-dev, 09:00 IL)

> להדביק כמו שהיא בסשן Claude Code על ה-MacBook (`claude --dangerously-skip-permissions`).

אתה cc-macbook על ריפו MEMS26 (`/Users/michael/Downloads/mems26_web_git`, המכונה שסוחרת — חשבון לייב, חוזה 1). עבודה בלי שאלות (פסיקת-מייקל 14.09): מחליטים, מתעדים וממשיכים; דו-משמעות ⇒ הפרשנות ה-golden שבהזמנה + שורת "החלטתי X כי…"; לא סיימת ⇒ NOT-DONE מפורש והפריט הבא. התחל כך: `git -C /Users/michael/Downloads/mems26_web_git pull` (לעולם לא `cd … && git …`), קרא `CLAUDE.md`, את הרשומה העליונה ב-`docs/handoff/LIVE_CHANNEL.md` (cowork-dev 23.09 07:50 — הבוקר אומת היומן מול הברוקר: `scripts/broker_truth.py` + תיקון `fill_poller` T-436b, רשימת 12 ליקויים ב-`docs/plans/JOURNAL_DEFECTS.json`, והמבחן-היומי `scripts/day_review.py`), ואת הזמנת-העבודה `docs/handoff/cc_orders/CC_NOW_2026-09-22.md` — פריט 1 סגור, פריט 2 עודכן הבוקר (מה שנשאר לך: T-436c + פריטים חדשים 2ב ו-2ג). **סדר-העבודה היום, פריט אחד בכל פעם:** (1) **אמת-הספרים** — פריט 2ב: יצוא `FILL` עם מחיר-מילוי מה-DLL (`sc_study/`, `sc.GetOrderFillEntry`, או `avg_price` ב-`POSITION_CHANGE`) + `fill_poller` כותב `entry_price`/`exit_price` מהמילוי; **קוד + build בלבד — הדיפלוי לסיירה ורענון ה-study הם של שער-15:30 (cowork) או אחרי 23:00, אחרי `scripts/mems26_snapshot.sh`; כתוב READY-TO-DEPLOY ב-LOG**; ואז פריט 10 (T-421: `exit_price`/`exit_ts` מרגל-ה-T1 בז'ורנל, 9/9 עסקאות-T1 ריקות). (2) T-436c: `daily_pnl.books_vs_sierra` + `_system_pnl_by_day` משווים ל-`pnl_sierra` (ברוקר) ולא לז'ורנל, ו-`eod_handoff` מריץ `python3 scripts/broker_truth.py --since 2026-09-01 --write`; אימות: 22.09 ⇒ `ok False · books 58.75 · sierra 13.75 · Δ −45.00`. (3) פריט 3 — T-437 חימוש-מהפתיחה: **ריפליי לפני דגל** (הרנס 10 סשנים, 3 תרחישים, מספר ולא המלצה); הרנס **רק מחוץ ל-16:30–23:00**, ≤2 במקביל, `SITUATION_VECTOR_LOG_V1=0`. (4) פריט 4 — 9 טסטי-הפתיחה האדומים. (5) פריטים 5 ו-7 — bootstrap אוטומטי של 9 סוכני-launchd אחרי ריבוט + EOD בלי תלות באפליקציית-Cowork (ה-EOD כולל מהיום: `broker_truth --write` → `day_review.py` → `gen_phone_pages.py --days 14`). (6) השאר לפי הסדר בהזמנה (6, 8, 9, 11, 12, 2ג). **כל פריט = קומיט אחד + שורת TASK_LOG + שורת STATUS_BOARD (ממצא → תיקון → ראיה) באותו קומיט + רשומת LOG חתומה `cc-macbook` ב-LIVE_CHANNEL עם הפקודה והפלט הגולמי (Rule 5 — לא "confirmed") + `push`, ואז "סיים פריט N" ועצירה עד הפריט הבא.** **גבולות שעוצרים ושואלים בסוף הדיווח, לא באמצע:** `.env` · דגלי-גודל / `RISK_*` · דגל-env חדש בלי פסיקה כתובה · ריסטארט-backend (שער-15:30 הוא של cowork; 16:10–23:00 אסור לחלוטין) · פוזיציות / פקודות / `op=EXIT` / FLATTEN · רענון-study בסיירה בשעות-מסחר. `pytest` דורש `BRIDGE_TOKEN=test` (או `.env` טעון); לוג-האפליקציה הוא `/tmp/backend.err.log`; `git -C` תמיד.
