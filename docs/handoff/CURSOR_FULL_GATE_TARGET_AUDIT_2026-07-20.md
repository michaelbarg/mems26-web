# cursor — ביקורת מקיפה: כל השערים + כל תקלות-היום + סולם-היעדים (מייקל 2026-07-20)

**מטרה:** לעבור על **כל** שער-החלטה ו**כל** תקלת-היום, לזהות איפה הם לא-תואמים-דלתון או חוסמים על סיגנל-שבור,
ולתקן שהמערכת תסחר **בדיוק לפי ההגדרות** ולא תישבר כל פעם. **עם סוכן-בדיקה** (spawn subagent לכל ממצא, חוק-5).
קריאה-בלבד לאבחון; תיקונים = דגל-OFF + טסט-דטרמיניסטי + אימות-cowork לפני הדלקה.

## חלק A — ביקורת כל שערי-ה-gateway (אחד-אחד)
מ-`backend/v9/gateway/trading_gateway.py` — לכל `blocked_by`: (1) מה מטרתו, (2) איזה סיגנל הוא קורא
(day_type/expansion/trend_state/POC/R:R), (3) **האם הסיגנל אמין** (get_live לא-null? הרחבה נרשמת? trend=כיוון-יום?),
(4) **האם הוא תואם דלתון** או over-block. השערים שכבר נמצאו שבורים היום (לאמת שתוקנו):
`daytype_playbook(require_with_trend)` · `location_gate` · `rr_entry_gate` · G6/G2/G3. סרוק גם את השאר:
cont_trend_filter · direction_lsma_veto · direction_context · htlb_direction_gate · opening_type_gate ·
nontrend_disable · entry_confirm · pre_fire · cluster_guard · dedup · suffering_side_veto · SSV.
פלט: טבלה שער×[סיגנל/אמין?/דלתון?/over-block?/תיקון].

## חלק B — שער-הסיכון:סיכוי של וודיס + מיקום-הסטופ (חשד-מייקל)
מייקל: *"שער-הסיכון של וודיס ממקם סטופ לא-נכון ואז חושב שיש סיכון גבוה מדי."* — אַמת:
- ב-`rr_entry_gate` (trading_gateway.py:1403+) ובנתיב-הסטופ של S4/woodies: איזה סטופ נכנס לחישוב-ה-R:R?
- אם הסטופ ממוקם-שגוי (Task#7 — לא בקצה-המבנה) → risk מנופח → R:R<min → חסימה-שגויה.
- הוכח #420: סטופ 5.25 נק' בתוך מבנה של 13. אַמת ש-cc-1 (סטופ-למבנה) מתקן גם את חישוב-ה-R:R של וודיס.

## חלק C — סולם-היעדים T0/T1/T2/T3/T4 (בחירה-חכמה — מייקל)
מייקל: *"יש t0 t1 t2 t3 t4 שצריך להיבחר בחוכמה."* — בקר:
- איפה נבחרים T0-T4 (targets_table / structural_targets / atr_caps / flag_relative_t1)? לפי מה?
- האם הם **מבניים** (רמות-דלתון: VAH/VAL/POC/IB-extension/measured-move) או R-based שרירותי?
- האם הבחירה מותאמת פר-**תבנית** ופר-**סוג-יום** (Variation-מטה → יעדים מטה עד VAL/הרחבה)?
- T0 (סקאלפ) מול T1-האמיתי — מתי כל אחד; BE-אחרי-T1-האמיתי (לא T0).
פלט: איך נבחר כל יעד היום, ומה צריך להשתנות כדי שייבחרו חכם (מבני, פר-תבנית×סוג-יום).

## חלק D — כל תקלות-היום (הצלב מול EOD_FIX_LIST_2026-07-20.md)
עבור על מפל-5-השערים + הסטופ + הרקונסיליאציה + גיוס-בבוט + מקור-יחיד. לכל אחת: מצב (פתוח/בתיקון), מי-מטפל.

## סוכן-הבדיקה
לכל ממצא בחלקים A-D: spawn subagent שמאמת עם **מקרה-אמת של היום** (פקודה+פלט-גולמי) — לא הצהרה.
פלט סופי: `docs/handoff/FULL_AUDIT_2026-07-20.md` — טבלאות + רשימת-תיקונים ממוינת + מה מאומת.
