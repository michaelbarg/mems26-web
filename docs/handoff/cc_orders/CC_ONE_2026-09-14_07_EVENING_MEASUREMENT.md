# פריט 7 מתוך 8 — T-364: ריצת-המדידה של הערב. שלוש הכרעות, ריצה אחת

**רץ רק אחרי 23:00** (סגירת RTH). **אפס שינוי-קוד, אפס דגל, אפס `.env`** — מדידה בלבד.
המספרים שיצאו מכאן הם שמכריעים שלושה דגלים שממתינים לפסיקת-מייקל.

## למה ריצה אחת
שלוש השאלות הפתוחות חולקות את אותו מנוע: הרנס → מסלולים → סימולציית-סלוט → תוצאה.
הרצה נפרדת לכל אחת היא בזבוז של שעה ומקור לאי-עקביות בין הטבלאות.

## הבסיס: להרחיב את המדגם מ-5 ל-כל מה שיש
`scripts/replay_admits.py` כבר קיים ומכיל את המודל (סלוט-אחד · `n=min(5,floor(225/(5×risk)))` ·
`n<3` ⇒ אין כניסה · `$5`/נק' × n · סטופ-או-T1 על ברי-5-דק' · **שניהם באותו בר ⇒ `AMBIG`
ולא מיוחס** · לא נסגר ⇒ סגירת-EOD).

**שלב 0:** לזהות כל הסשנים ב-`v9_bars_5min_woodies` עם **≥60 ברי-RTH** (יש ~30), ולהריץ
עליהם `fwd_harness.py` — **ברצף או בקבוצות של 5, לא 30 במקביל** (המכונה סוחרת בבוקר).
לשמור ל-`/tmp/oos/<session>.json`.

---

## שאלה א · האם `K=3` מחזיק מחוץ-למדגם? ובאיזה חלון?
**[[T-357]] + [[T-361]].** ב-5 הסשנים: `K=3` נתן `+$1,051` מול `+$336` ללא-סינון. אבל
T-361 הראה שהמימוש מודד על **חלון-120-ברים** (כולל לילי) ולא על ה-RTH, ו-**9/86** מועמדים
מקבלים החלטה שונה — כולם בשעה הראשונה.

**הפלט:** טבלה של `K ∈ {None,1,2,3,4,6,8,12}` × **שתי גרסאות-חלון** (`120-bar` מול
`RTH-only`), ולכל תא: סה"כ-$ · עסקאות · מפסידות · **ימים שליליים** · אחוז-מנצחות.
**ההכרעה של מייקל היא "לסיים כל יום חיובי עם מעט מפסידות"**, ולכן עמודת ימים-שליליים
היא העמודה הקובעת, לא הסכום.

## שאלה ב · 47 הכניסות ש-ELQ סימן כגרועות ועברו בכל זאת
זרוע `expensive_stop` ב-`entry_location_quality.py` **מושתקת** (‏`# reasons.append(...) —
SHADOWED, not blocking`, מ-10.09): ב-5 הסשנים היא כתבה `would block, logging only` **47**
פעמים בעוד ELQ חסם בפועל רק **28**.

**הפלט:** לכל מסלול שנשא `expensive_stop SHADOW` והורשה לעבור — תוצאה ו-$ לפי אותו מודל.
סיכום: כמה מנצחות, כמה מפסידות, סה"כ-$, וכמה **ימים** היו נהיים חיוביים אילו הזרוע הייתה
חוסמת. **אל תמליץ — תן את המספר.**

## שאלה ג · יעדי-הדוקטרינה, מופרדים מהסטופ
**זה השורש שזוהה ב-14.09 15:45 ולא היה ידוע קודם:** כשהודלק `EDGE_FADE_TARGETS_V1` בבוקר
הוא שינה **גם יעדים וגם סטופ** (10.09 17:50: סטופ `7624` → `7615.75`), ה-R:R עבר, ונולדה
עסקה שנעצרה. **ההפסד נזקף ליעדים בטעות — מקורו בסטופ.**

**הפלט (מדידה בלבד, בלי לשנות קוד):** לכל setup בשורת Normal/Neutral שכלל-המיקום אישר,
לחשב שלוש שרשראות ולהשוות תוצאה:
1. **סולם** (מה שרץ היום) — הבסיס.
2. **יעדי-דוקטרינה + הסטופ הקיים** — `T1=POC · T2=קצה-ערך-נגדי · T3=IB-נגדי`, הסטופ **לא נוגע**.
3. **יעדי-דוקטרינה + סטופ-מבני** (מה שהודלק ונכבה הבוקר).

טבלה: לכל אחת — סה"כ-$ · עסקאות · מפסידות · ימים שליליים. **אם (2) מנצח את (1), הדוקטרינה
נכונה והסטופ היה האשם**; אם גם (2) מפסיד — הדוקטרינה עצמה צריכה בדיקה מחדש.

---

## כללי-דיווח
- כל טבלה **מסתכמת במכנה** — מספר העסקאות בכל תא + ה-`AMBIG` שלא יוחסו, בנפרד.
- **אפס מבט-קדימה.** כל סטטיסטיקת-בסיס מחושבת מברים **שנסגרו לפני** רגע ההחלטה. חציון-יום,
  ממוצע-יום, ATR-יום או קיצון-יום כסף-החלטה = פסילה של הטבלה ([[T-357]] בוטלה בדיוק על זה).
- לציין לכל שאלה את **גודל-המדגם** ואת מספר הסשנים שנכנסו בפועל (ומי נפל ולמה).
- **בלי המלצה ובלי הדלקה.** הפלט הוא מספרים; מייקל פוסק.

## דיווח
קומיט אחד (סקריפטים+טבלאות, בלי שינוי בקוד-המנוע) · `TASK_LOG` (T-364) + `STATUS_BOARD`
באותו קומיט · הטבלאות ל-`LIVE_CHANNEL.md` · ואז **"סיים פריט 7"** ולעצור.
