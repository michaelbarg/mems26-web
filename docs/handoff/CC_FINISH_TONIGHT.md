# ⚡ להשלים הלילה — שלושת החוסרים + ריסטארט (פסיקת-מייקל 23.08 20:45)
**"תוכל להשלים עוד תיקונים ולהריץ בדיקה היום ב-1 בלילה שעון ישראל שהכל עובד,
וכמובן לעשות ריסטארט אחרי הכל."**
§0 חל. **היום ראשון — ריסטארט מותר בכל שעה.**

## מה שנמצא חסר באימות (23.08 20:41)
✅ בוצע: 12 עמודות-זמן → timestamptz (CVD 0%→65.3%) · HLST הוסרה (‎−$4,574) ·
desync→ANOMALY · מונה-בליעות · TREND_STEP→צל.
🔴 **לא בוצע: P3 · סגירת-T-10 · `pnl_reconcile`.** ואין ריסטארט — הבאקנד רץ מ-22.08
13:07 עם `commit=4083374d`, כלומר **שום תיקון לא באוויר**.

## 1 · P3 — הגדלה מחושבת (הפריט היקר, ‎+$27/יום)
דגל `SCALE_IN_P3_V1` (default OFF), ארבעה תנאים **יחד**:
- טריגר **1.5×ATR14** מהכניסה (**לא** 0.5 — נמדד ‎−$299 מול ‎+$683).
- **מרווח ‎≥1.5R** ליעד/לרמה המבנית הבאה — לא מחזקים לתוך קיר.
- **סטופ-מבני אחד לכל הפוזיציה** — לחשב את **מחיר-הכניסה הממוצע לאחר התוספת מראש**,
  ולהציב סטופ יחיד ברמה מבנית שמתאימה לממוצע החדש (רעיון-מייקל; win 45%→55%).
  היום הילד מקבל סטופ בכניסת-האם — שגוי אחרי מיצוע.
- **איסור-חיזוק-בקצה**: לא מחזקים כשהמחיר בתוך X×ATR מקיצון-הסשן (31/49 נכנסו
  בקיצון ומתו; ב-21.08 שני החיזוקים הפסידו ‎−$122.50).
טסטים: OFF זהה-בייט · שחזור 3 החיזוקים ההיסטוריים · חיזוק-בקצה נחסם · סטופ-הילד
לעולם ‎≤ סטופ-האם.

## 2 · סגירת T-10 (‎+$3.28/יום, מנטרל חשיפת-לילה)
`EOD_CLOSE_T10_V1` (default OFF): 10 דק' לפני סגירת-RTH — **סגירה** (הווריאנט שנמדד
חיובי; הידוק נמדד no-op). ‏FLATTEN בלבד (`op=EXIT` שבור).

## 3 · `pnl_reconcile` — הכלי שאמור לתפוס שקר ושיקר
משווה מחירי-**פקודה** ולא מילוי · מדלג על FLATTEN/BE · `incomplete` במקום לצעוק.
לתקן, להריץ על כל עידן-הלייב, **ולדווח את הסכום המתוקן** (הקודם: ספרים ‎−$107.50 מול
ברוקר ‎−$11.25 ב-21.08).

## 4 · פסיקת-מייקל נדרשת — לא להדליק לבד
`S2_CVD_DETECTION_V1=1` **דלוק**, ואחרי תיקון-הזמנים הוא **יתחיל באמת לרוץ** (0%→65.3%)
⇒ מסנן חדש שיחסום כניסות = שינוי-משטח-סיכון. **להריץ עליו `week_replay` (לפני/אחרי,
דלתא + חציון-יום), לרשום את המספר ב-LIVE_CHANNEL, ולהשאיר את ההחלטה למייקל.** אם
הרפליי שלילי — לכבות ולציין; אם חיובי — להשאיר דלוק ולציין שזו המלצה הממתינה לאישור.

## 5 · ריסטארט + בדיקה מלאה ב-01:00 IL
לתזמן/להריץ ב-**01:00 שעון ישראל**:
```
git pull && python3 scripts/flag_guard.py && \
launchctl kickstart -k gui/$UID/com.mems26.backend && sleep 40 && \
curl -s -o /dev/null -w "health=%{http_code}\n" http://localhost:8000/api/v9/health && \
grep -a "\[boot\] logging OK" /tmp/backend.err.log | tail -1 && \
python3 scripts/mems26_verify.sh ; python3 scripts/mems26_arming_gate.py ; \
python3 scripts/fire_drill.py ; python3 scripts/pnl_reconcile.py --mode live
```
**חובה לאמת:** שורת-הבוט נושאת את ה-commit **החדש** (לא 4083374d) · `flag_guard` PASS ·
`fire_drill` — פיד עשוי להיכשל בלילה (שוק סגור), זה תקין; כל כשל **אחר** = לעצור ולדווח.
לחזור על `fire_drill` בבוקר יום שני לפני 16:10 עם פיד חי.

## 6 · דיווח
`LIVE_CHANNEL` חתום + `TASK_LOG` + `STATUS_BOARD`, עם פקודה+פלט-גולמי לכל פריט
(חוק-5). מה שלא הספקת — **לרשום מפורשות כ"לא בוצע"**, לא להשמיט.

---

## ⚠️ תיקון-תפיסה לסעיף 4 (פסיקת-מייקל 23.08 20:55)
**ציטוט: "למה לחסום? הוא צריך לאשר תבניות, ונקודות סיום ושינוי מגמה."**

**‏CVD אינו שער.** להפסיק להתייחס אליו כמסנן-חוסם. שלושת התפקידים הנכונים שלו,
לפי סדר-הערך שנמדד:

1. **מחולל-איתות (הכי חזק) — ספיגה = נקודת-סיום/היפוך.**
   המחיר עושה קיצון חדש, ה-CVD **לא** ⇒ הצד השולט נגמר. נמדד: **85.3% היפוך**,
   ובימי-מגמה **87.5%** — שם `B` (קצה-אזור-ערך) נופלת ל-66.7%. זהו **הגלאי החזק
   ביותר שיש לנו**, והוא צריך **לייצר מועמדים**, לא לסנן אותם.
2. **מאשר-תבנית / קובע-איכות.** תבנית שהדלתא תומכת בכיוונה ⇒ ציון-איכות גבוה
   (ובהמשך: גודל גדול יותר). דלתא **מנוגדת** ⇒ ציון נמוך. **דירוג, לא וטו** —
   ובשום מקרה לא "אין דלתא ⇒ לא סוחרים" (הנתון פשוט חסר לעיתים; חוק-1).
3. **זיהוי שינוי-מגמה** — היפוך שיפוע-CVD מתמשך, כקלט למצפן-הכיוון ולמעבר
   BALANCE⇄DISCOVERY. **לא** כחוסם עצמאי.

**מה לעשות בפועל הלילה:**
- **לא להדליק את `S2_CVD_DETECTION_V1` כמסנן-חוסם.** להשאיר כבוי עד שיוגדר מחדש.
- **לבנות במקומו `S2_DELTA_ABSORPTION_V1`** (default OFF): הפרימיטיב `D` כ**מחולל
  איתות** — קיצון-מחיר בלי קיצון-CVD ⇒ מועמד-היפוך שעובר את שרשרת-השערים הרגילה
  ככל מועמד אחר. מותנה סוג-יום לפי המדידה (עובד גם ב-Trend).
- **לחווט את הדלתא כשדה-איכות** (`delta_confirms: bool/score`) על כל סטאפ של S2 —
  לרישום ולמדידה בלבד בשלב זה, בלי השפעה על החלטה. כך נוכל לכמת אחרי שבוע כמה
  שווה האישור לפני שנותנים לו כוח.
- רפליי לשלושתם (‏§D) → `docs/reports/REPLAY_DELTA_ROLES.md`.
