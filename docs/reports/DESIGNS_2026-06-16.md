# MEMS26 · טיוטות-עיצוב — 2026-06-16

עיצובים מוכנים-להפיכה-לפרומפט לבעיות שאומתו היום (`PATTERN_EOD_2026-06-16.md`).
**לא מומש דבר — design בלבד.** כל פריט: שורש · שינוי מוצע (קובץ/שכבה) · regression · סיווג trading-logic(→Michael)/display-safe · הצלבת-Sierra ל-CC.
**D-IDs עקביים עם 06-11/12/15** (עיצובים מתמשכים); עודכנו בראיות-היום.

> **תזכורת standing-decision (CLAUDE.md):** chop-gates (S2 `choppiness_ok`, Layer-0) ו-COT/AMT (S2⟂S3) **מנוטרלים בכוונה**. אף עיצוב כאן אינו מציע להחזירם. שערי-ה-trend/regime המוצעים (D3/D5) הם **חדשים ונפרדים** — לא re-enable של שער מנוטרל.

---

## D11 — I-34 🔴 (verify-then-fix; אם dead-wire → trading-risk): חיווט verdict ה-sizing של DAYTYPE_PLAYBOOK

**שורש (day-1 ב-SHADOW, מאומת):** `DAYTYPE_PLAYBOOK=1` הודלק היום 08:32 CT. `metadata.sizing="half"` נרשם ב-18 עסקאות, אך 6 שנעצרו ב-stop הפסידו **3 חוזים מלאים** (implied=3.0; id133 −$191.25=12.75×5×3) ⇒ −$1,076.25 במקום −$538.13. ה-verdict REDUCED **לא הגיע לנתיב-ה-PnL**.
**שלב-0 (verify, Rule 2 — לפני כל תיקון):** לקבוע מי משניים — (א) shadow-PnL מחושב נומינלית-3-חוזים בכוונה (ו-"half" יחול ב-LIVE) ⇒ **תמים, סגירה**; או (ב) `sizing→contracts→pnl_usd` מנותק ⇒ **trading-risk** (השער-הראשי לא מגן). נתיב לבדיקה: `daytype_playbook.decide()` → `trading_gateway.route_setup` → `services/trade_manager/manager.py` (חישוב contracts/pnl).
**שינוי מוצע (רק אם ב'):** ב-trade_manager — לקרוא `effective_contracts = base × sizing_factor` (`half→×0.5`) בנקודת-חישוב-ה-PnL וגודל-הפוזיציה, לא רק ב-metadata.
**Regression:** עסקת-half סינתטית עם stop-מלא ⇒ `pnl_usd == −(risk×5×contracts×0.5)`; replay 06-16 ids 123/127/129/131/133/141 ⇒ סך −$538.13, לא −$1,076.25.
**סיווג:** **verify-first.** אם (ב) → **trading-risk → Michael** (נוגע בגודל-פוזיציה). **נספח ל-`feedback_full_decision_pipeline_wiring` (אירוע-5-דגלים).**
**Sierra:** לא נדרש (נתיב-execution פנימי).

## D12 — I-35 🟡 (display-safe): collision ב-`metadata.sizing` בין שני sizers

**שורש (מאומת):** `sizing` מכיל גם `"half"`/`"full"` (DAYTYPE_PLAYBOOK) וגם `0/1/2/3` (sizer אחר — RISK_CAP/vol-adaptive?). type-inconsistent (string+number); אותה תבנית×יום מקבלת `"half,full"`/`"3,2"`. מקור-אמת מעורפל לגודל-המבוצע + מבלבל את I-34.
**שינוי מוצע:** להפריד שדות — `playbook_verdict` (FULL/REDUCED/SKIP) **נפרד** מ-`size_contracts` (numeric); או canonical-מספרי יחיד שכל ה-sizers כותבים אליו דרך reducer מסודר (precedence מוגדר). לתעד את ה-precedence בין playbook↔RISK_CAP↔vol-adaptive.
**Regression:** `type(metadata.size_contracts)` תמיד number; verdict תמיד מ-{FULL,REDUCED,SKIP}; אין ערך-כפול.
**סיווג:** **display/data-hygiene-safe** (אלא אם ה-precedence משנה גודל-מבוצע → אז עם D11).
**Sierra:** לא נדרש.

---

## D3 — I-26/I-28 🔴 (trading-logic → Michael): veto-מגמה · **עדיפות-עליונה, יום חמישי ברציפות**

**שורש (יום חמישי ברציפות, הקיצוני-ביותר):** היום LONG אל-תוך-ירידה = **3W/9L, −$1,285.03 (−7.52R)** מול SHORT עם-הירידה **18W/0L, +$627.40 (+6.43R)**. דילוג על ה-LONGs ⇒ **−$657.63 הופך ל-+$627.40**. אובדן-הזדמנות: 3 זכיות-LONG זעירות בלבד (+$106) מול מניעת −$1,391 הפסדים — **יחס עלות/תועלת 13:1**. דפוס זהה 06-10/11/12/15. הליבה: HFE-LONG ×3 + REACTIVE_LONG ×3 (0W/3L) + TLB-LONG ×2.
**שינוי מוצע (ללא-שינוי מהותי מ-§D3 קודמים):** ב-gateway — veto fire כש-`pattern_group ∈ {REVERSAL, counter-trend}` והכיוון **נגד** מגמה-יציבה. **תוספת קריטית (מתחזקת):** "מגמה" = **price_slope** (`price < opening − k·ATR` או EMA-slope יורד על N ברים), **לא רק `woodies_trend` צבעוני** — היום המחיר ירד מונוטונית מהבר-הראשון אבל woodies הראה **BLUE×4 + GRAY×10**; שער-צבע היה מתיר את id123 (TLB-L תחת BLUE) ומחמיץ שורטים-מרוויחים מוקדמים. סף N≈6 ברים + slope-k ב-YAML.
**Regression:** replay 06-16 ⇒ כל 12 ה-LONGs veto, כל 18 ה-SHORTs עוברים ⇒ +$627.40. *גרסה-מינימלית* ("אין LONG כש-woodies=RED") חוסכת −$839.25 בלבד (5 RED-LONGs) — מאשר שצריך slope ולא צבע. + replays 06-10/11/12/15.
**סיווג:** **trading-logic — דורש הכרעת-Michael.** *התיקון בעל יחס עלות/תועלת הגבוה ביותר; 5 ימי-ראיות זהים = מסקנה, לא תצפית.*
**Sierra:** להצליב `woodies_trend`+TCCI בעת-הבוקר (id119–122) — האם Sierra הראה BLUE/GRAY נכון למרות ירידת-מחיר, או lag.

## D8 — I-3/I-13 🔴 (trading-logic → Michael): טבלת stop/target פר-תבנית×day-type — **עדיפות-LIVE #1**

**ללא-שינוי מ-§D8 קודמים.** ראיות-היום: **TLB negative-skew נמשך גם ביום-מגמה** (3W/2L אבל זכיות T1→BE זעירות +$25–66 מול הפסדים-מלאים −$168/−$202 ⇒ −$247.18); **HFE risk 12.5–12.75pt** ($187–191) מול **ZLR/GB100 3–6pt** — אותו `MEMS_MAX_RISK_POINTS` אחיד. **ה-runner נחנק גם כשהמגמה נתנה 40pt** (כל ה-SHORT-wins T1→BE זעירות +$17–48, ΣR +6.43 אבל ב-$ זעיר). Michael מגדיר את הטבלה (memory: `project_stop_target_placement_table`, `project_config_tunable_stop_exits_contracts`).
**Regression:** טבלה ב-YAML ⇒ כל תבנית×day-type מקבלת stop/T1–T5/contracts ייעודיים; replay מאמת ש-(א) HFE-LONG לא היה מקבל stop 12.5pt ביום-יורד, (ב) ZLR-SHORT עם-המגמה היה מחזיק runner ל-T2/T3 במקום BE זעיר.
**סיווג:** **trading-logic — Michael** (החלטת-מוצר, חוסם-LIVE).
**Sierra:** רמות-stop להצלבה מול Sierra.

## D4 — I-30 🔴 (trading-logic → Michael): cluster-guard

**שורש (חי היום):** **5 LONGs נשענו על stop-זהה 7603.25** (id126/127/129/131/133) — רמת-תמיכה משותפת שנפרצה (low=7582.75, ~20pt מתחת). HFE 131/133 + TLB 127 מתו יחד כשהרמה קרסה. CF-cull (2nd+ באותו pat@stop@dir): **−$647.30** ⇒ היום ≈ breakeven (−$10.33). **חולשה:** השער מקפח גם 2 שורטים-זוכים (119/121, +$106) ⇒ **פחות-נקי מ-D3; D3 דומיננטי.**
**שינוי מוצע (ללא-שינוי):** `MAX_CONCURRENT_PER_PATTERN=1` (YAML) + veto ב-gateway על כניסה-נוספת כש קיימת פוזיציה **אותה תבנית** או **אותו `stop_initial` (±tick)** תוך N דקות.
**Regression:** replay 06-16 ⇒ נשמרות 126/129 (ראשונות@7603.25), נחסמות 127/131/133; ימים קודמים (06-15 94/95/96).
**סיווג:** **trading-logic — Michael.** (D3 מכסה את רוב הליבה; D4 משלים על clusters עם-מגמה.)
**Sierra:** מקור ה-`stop_initial` 7603.25 המשותף (pivot/level/round) + אישור-פריצה.

## D2 — I-31 🔴 (display-safe, עדיפות-גבוהה): ספירת-ירי שקרית ~2.4× ב-`build/pattern-status`

**שורש (מאומת היום, גדל):** `woodies.fired_today_count=52` + `five_min=19` = **71**, מול **30** ב-`v9_trades` (21+9). יחס ~2.5×/~2.1× — גדל מ-54/26 (06-15). ⇒ ה-count סופר פר-detection/פר-חוזה/כולל-blocked, לא עסקאות-distinct.
**שינוי מוצע:** ב-build_status inspector — מקור-הספירה ל-`SELECT firing_system, COUNT(*) FROM v9_trades WHERE session_date=today GROUP BY 1`. אם המקור טבלת-סיגנלים — להפריד `signals_count` מ-`fired_count` ולתייג.
**Regression:** ביום עם 21 S4 + 9 S2 ⇒ endpoint מחזיר 21/9; `Σfired == COUNT(v9_trades WHERE date=today)`.
**סיווג:** **display-safe** — אבל **חוסם-כיול** (כל מסקנת-תבניות כמותית מורעלת). **חוזר מ-D2(06-12/15) — לא מומש.**
**Sierra:** לא נדרש (DB בלבד).

## D1 — I-22 🔴 (display-safe): `pnl_r` ÷$1.25 — שבור בנתיב-T1/BE

**שורש (20 דוגמאות היום, גדל מ-8):** כל יציאת-WIN דרך T1/BE → `pnl_r == pnl_usd ÷ $1.25` (tick-value): id116 +$32.5→"26R", id122 +$48→"38.4R", id146 +$35.8→"28.64R" (בלתי-אפשרי). נתיב-T2 = `÷(risk×5)` (id120=2.8, id126=3.5 — בסיס-פר-חוזה, **לא-עקבי** עם בסיס-ההפסד); נתיב-הפסד-מלא תקין (−1.0).
**שינוי מוצע:** דרך `SYSTEM_INDEX` לאתר את branch ה-T1/BE-close (trade-closer/gateway) וליישר לנוסחה אחידה: `pnl_r = pnl_usd / (abs(entry − stop_initial) × 5.0 × contracts)` בכל הנתיבים (גם T2, להחזיר עקביות). backfill או `r_known_bad=true` לשורות היסטוריות.
**Regression:** עסקה סינתטית T1+BE (entry 7629.25/stop 7637.25/3 חוזים/pnl 32.5) ⇒ `pnl_r≈0.27`, **לא 26**; T2 ⇒ נוסחה זהה; STOP מלא ⇒ −1.0.
**סיווג:** **display/analytics-safe.** scope: מחשבון-R. **חוזר מ-D1(06-12/15) — לא מומש.**
**Sierra:** לא נדרש.

## D6 — תפעולי/safe (יום חמישי ברציפות): סוכן-ה-DIAG-30-דק' לא רץ

**שורש:** אין `PATTERN_DIAG_` ל-06-11/12/15/16 — ה-trigger מת מאז 06-10. בלעדיו ה-EOD עיוור לנחסמים (אין reject_reason-intraday, אין CF-קלאסי). **חמישה ימי-CF אבודים.**
**שינוי מוצע:** (א) לבדוק את ה-scheduled-task/LaunchAgent של ה-DIAG (נעצר אחרי 06-10? error? TZ?); (ב) guard-בוקר: אם עד 09:30 CT אין snapshot ראשון ⇒ נטיפיקציה ל-Michael מיידית.
**Regression:** יום-המסחר-הבא מייצר ≥8 snapshots; היעדר עד 09:30 ⇒ נטיפיקציה.
**סיווג:** **תפעולי/safe.** **חוזר מ-D6(06-12/15) — לא טופל; חוסם 5 ימי-CF.**
**Sierra:** לא נדרש.

## D10 — חדש 🟡 (display/diagnostic-safe): dead-wrapper read ב-`/api/v9/day_type/state`

**שורש (חדש היום):** ה-endpoint מציג `day_type=Trend_Normal 0.46 LOCKED_LOW_CONF` post-close, בעוד **19/30 העסקאות תויגו `Variation`** ב-live (UNKNOWN×6→Trend_Normal×5→Variation×19). זה **בדיוק** ה-dead-wrapper שמזהיר עליו CLAUDE.md §Codebase-Index: ה-route קורא instance-wrapper מת, הסיווג-האמיתי ב-`app.state.day_type_machine` + טבלת `v9_day_type_state`.
**שינוי מוצע:** ב-route `/api/v9/day_type/state` — להחליף את מקור-הקריאה ל-`app.state.day_type_machine` (כמו שאר-המערכת), או ל-`SELECT … FROM v9_day_type_state ORDER BY ts DESC LIMIT 1`. נספח ל-I-1.
**Regression:** `day_type/state.day_type` == ה-`day_type` של העסקה-האחרונה-של-היום (== `v9_day_type_state` האחרון). היום: שניהם Variation, לא Trend_Normal.
**סיווג:** **display/diagnostic-safe** (לא חוסם ירי — העסקאות כבר קראו את ה-machine הנכון).
**Sierra:** לא נדרש (state-instance בלבד).

## D5 — 🟡 (trading-logic → Michael): שער-regime/confidence + הקשר-מיקום ל-REACTIVE — **פחות-רלוונטי היום**

**שורש (חוזר, לא-מודגם היום):** 06-16 היה יום-מגמה-יורד **נקי** (לא range-אחה"צ) ⇒ ההפסד היה **כיוון** (D3), לא רעש-range. D5 נשאר פתוח ל-ימי-range (כמו 06-15, −$595.65). מרכיב REACTIVE-position עדיין תקף: REACTIVE_LONG 0W/3L היום — אך כולן counter-trend (מכוסה ב-D3).
**שינוי מוצע (ללא-שינוי, YAML):** (1) regime-gate: `confidence<0.50` **וגם** `range_N<ATR×k` ⇒ half/skip ל-reversal. (2) REACTIVE `pos_in_session_range` ב-`five_min_system.py _detect_reactive` — LONG ב->80% עליון / SHORT ב-<20% תחתון ⇒ skip/half (שמירה על S2⟂S3).
**Regression:** replay יום-range (06-15 id108–114) skip/half; replay יום-מגמה (06-16) **לא** חוסם את ה-SHORTs-המרוויחים.
**סיווג:** **trading-logic — Michael.** משני ל-D3 השנה.
**Sierra:** ATR/range מ-Sierra ל-threshold.

## D7 — I-33 🟢 (display-safe): רדאקציית-סוד — **כנראה נסגר, לאימות**

**שורש (נראה תוקן מאז 06-15):** `day_type/state` מחזיר כעת `session_min=0`/`session_date`/`rtb_session` כ-**ערכים גלויים** (סריקה: 0 שדות-redacted), לא עוד "[BLOCKED: Sensitive key]".
**פעולה:** **לא נדרש עיצוב-תיקון** — לאמת-CC שהשינוי מכוון (redactor צומצם ל-allowlist מדויקת) ולא רגרסיה לכיוון-ההפוך (חשיפת-סוד אמיתי). אם מכוון ⇒ **לסגור I-33**.
**Regression:** `day_type/state.session_min` מספר; מפתח-סוד אמיתי (אם קיים) נשאר חסום.
**סיווג:** **display-safe.**
**Sierra:** לא נדרש.

## D9 — לאימות-CC (קטנים): I-32 · I-25 · readiness · I-1

- **I-32 🟡:** id **117** חסר (116→118). gap-id 3 ימים ברציפות (06-12: 64/72/76 · 06-15: 90 · 06-16: 117). rollback/insert-fail שקט? sequence מול לוג ("no silent failures").
- **I-25 🟢:** trades cap=100; bars החזיר 165. enhancement (cap/pagination); היום 30<100 ⇒ ללא-אובדן.
- **readiness 🟢:** היום **READY** post-close (לא DEGRADED כמו 06-15) — bridge fresh, s1 classified, s4 trend=RED. תקין.
- **I-1 🟢/🟡:** `vote_history=[]` + `session_min=0` post-close נמשכים; הסיווג עצמו (opening_type/day_type) **נכון** בפרקטיקה. נספח: D10 (dead-wrapper).

---

**סדר-עדיפות מוצע ל-Michael:**
**D11 (verify חיווט-sizing — day-1 של השער שהדלקת; לפני יום-2 ב-SHADOW)** → **D8** (טבלת stop/target — חוסם-LIVE) → **D3 (veto-מגמה price-slope — 5 ימי-ראיות, +$1,285 היום, יחס 13:1)** → **D4** (cluster-guard, −$647 CF) → **D2** (I-31 — אמינות-ספירה) → **D1** (I-22 branch-T1, 20 דוגמאות) → **D6** (DIAG trigger — לפני יום-המסחר הבא!) → **D10** (dead-wrapper day_type — משפיע על קריאת-day_type של ה-playbook) → **D12** (sizing collision) → **D5** (regime — לימי-range) → **D7** (I-33 — אימות-סגירה) → **D9**.

*היום, **D3 לבדו** (veto-מגמה price-slope) הופך את −$657.63 ל-+$627.40 — השער הדומיננטי. **אך הסיפור-המרכזי הוא day-1 של DAYTYPE_PLAYBOOK:** השער שהדלקת היה פעיל, הקצה verdicts — ובכל-זאת לא מנע את הדימום, כי (א) ה-"half" לא הופעל על ה-PnL (D11/I-34) ו-(ב) המיפוי לא עשה SKIP ל-counter-trend ביום-יורד. **לפני יום-2 בשאדו: D11 + הידוק-מיפוי.***
