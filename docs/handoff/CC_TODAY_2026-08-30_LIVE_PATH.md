# פקודה ל-cc · **פסיקת-מייקל אושרה** — מסלול-הלייב המהיר

**שתי פסיקות שאושרו 30.08:**
1. *"יום אחד של צל וממשיכים ללייב"* — שער-ערב בן 4 בדיקות; אין מדגמים ארוכים.
2. *"תיקוני-נכונות לא ממתינים אפילו ליום"* — באג ידוע עולה אחרי טסט-שחזור ודריל-סים.
המדיניות המלאה: `docs/plans/SHADOW_TO_LIVE_POLICY.md`. **זו פקודת-הביצוע.**

## מסלול א' — היום · שלושת תיקוני-הנכונות (הכי דחוף)

אלה **באגים** שכבר עלו כסף. הנדרש מכל אחד — ואז בקשת-הדלקה אליי:
| # | התיקון | טסט-השחזור המחייב |
|---|---|---|
| 1 | **מודל-פוזיציה** — סטופ מהממוצע | `sierra: pos=-3 avg=7740.83` + `TM: #851@7750, #853@7736` ⇒ שלושת הסטופים מ-**7740.83** |
| 2 | **נרמול-CVD** | 28.08: ‏26 חורים ⇒ **0-1**; קריאה מחזירה ערך במקום `None` |
| 3 | **מקור-הכיוון** | תרחיש-28.08: ‏ZLR-17:21 אינו נחסם ע"י מקור-כיוון (הפלייבוק — נושא נפרד) |

**דריל-סים אחד לשלושתם** — `MODIFY_STOP` · `MODIFY_TARGET` · `FLATTEN_ACCOUNT`, כל אחד
עם **ACK מסיירה** ופלט-גולמי. **‏`op=EXIT` — אסור.** בלי הדריל אין הדלקה, גם לא לבאג.

## מסלול ב' — עד 16:30 מחר · שיהיה מה למדוד ביום-הצל היחיד

**האבחון שהוביל לפסיקה: 2 אירועי-צל בלבד מאז 26.08.** אם מחר ייצא כך — היום-אחד יתבזבז.
1. **‏`scripts/shadow_promotion_board.py`** — טבלה יומית פר-דגל: אירועים-היום · מצטבר ·
   ‏P&L-מדומה עם עמלות · החטאות · 🟢/🟡/🔴. ל-`docs/reports/SHADOW_BOARD.md`, לתיק ולטלפון.
2. **התרעת-צל-ריק:** אפס אירועים ביום-מסחר ⇒ הודעה לטלפון באותו ערב. **אפס אירועים =
   תקלת-גלאי לתיקון, לא סיבה להאריך צל** (פסיקה).
3. **לוודא שכל דגל-צל רושם ללִיגר** בכל הפעלה — בלי זה הלוח ריק והשער עיוור.
4. **להשלים חיווט:** `CEILING_LONG_LOCK_V1` (שער-הנעילה טרם נבנה) ·
   `ENTRY_LOCATION_QUALITY_V1` לגייטוויי (הגלאי קיים, חסר חיבור).

## שער-הערב של מחר — 4 בדיקות, זה הכל
1. הצל רץ? אירועים > 0 · 2. מה עשה — אירוע-אירוע: שעה · מחיר · החלטה · תוצאה, **עם עמלות** ·
3. אין נזק ואין סתירה לדוקטרינה · 4. מסלול-כבוי זהה-בייט + כיבוי מהטלפון בשנייה.
**עובר ⇒ לייב בשלישי. נכשל ⇒ תיקון ועוד יום אחד.**

## הרשת שמחליפה את המדגם
מנגנון שעולה לייב עולה **מנוטר**: כל הפעלה ביום-הראשון מדווחת לטלפון-מייקל, וכפתור-כיבוי
זמין שם. **המדידה נמשכת בלייב** — לא לפניו.

## כללים
טסטים עם `.env` טעון · רגרסיה מול בסיס (HEAD=235, שני מספרים) · **הדלקה: cowork+מייקל** ·
`op=EXIT` אסור · שורת-STATUS_BOARD מאומתת · `NOT-DONE` מפורש ·
**`ast.parse` על כל קובץ-ייצור לפני commit** (תו שגוי חסם אתמול את כל הסוויטה).
