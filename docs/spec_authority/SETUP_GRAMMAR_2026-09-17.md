# דקדוק-הסטאפים — תבנית = מיקום × רצף × טריגר × ווליום (מייקל 17.09 19:05)

**מקור:** מייקל 17.09 19:00 — *"זה שילוב של מיקום, מספר ברים והתנהגותם, עם בר-טריגר שמאשר, והתנהגות הווליום של כל בר"* + הצ'ארט של 17.09 09:25-11:05 ET — *"רצפה כפולה, רואים את התנהגות הווליום, הגיע ל-VAL, לא הצליח להמשיך, ואז עלה למעלה"*.
**מעמד:** מסמך-סמכות לתבניות. תבנית מוגדרת **רק** בדקדוק הזה; מה שלא ניתן לכתוב בו — אינו תבנית של המערכת. כל תבנית נמדדת ב-`oracle_validate` (עשרת המבחנים) לפני צל, ובצל לפני לייב.

---

## 1 · הדקדוק — חמישה חלקים, כולם סיבתיים (ברים סגורים בלבד)

| חלק | מה נבדק | מהווקטור / מהברים |
|---|---|---|
| **מיקום** | איפה הרצף יושב מול מבנה: VAL/VAH/POC של אתמול, VA מתפתח, קצה-IB, קיצון-הסשן, LSMA, VWAP — ובמרחק ATR | `zone`, `prior_zone`, `extension`, `dist_to_level_atr` |
| **הקשר** | שלב · סוג-יום · כיוון ההרחבה/הדרייב · ברים מהקיצון · מהלך מהפתיחה ב-ATR | `phase`, `day_type`, `dir_hint`, `bars_since_*`, `move_from_open_atr` |
| **רצף** | N ברים (3-12) והתנהגותם: שפלים/שיאים, התכווצות-טווחים, זנבות-דחייה, מבנה (רצפה/תקרה כפולה, ראש-כתפיים, ספל-ידית) | מחושב על חלון-הברים |
| **טריגר** | בר אחד שמאשר: סוגר מעבר לקו (צוואר / קצה-הרצף / רמה), סגירה ב-30% הקיצוניים, גוף ≥ 50% מהטווח, טווח ≥ 0.8×ATR | הבר האחרון בחלון |
| **ווליום** | לאורך הרצף ובטריגר: ווליום-יחסי לכל בר, דלתא לכל בר, "מאמץ מול תוצאה" (דלתא כבדה בלי התקדמות-מחיר = ספיגה), היפוך-דלתא בטריגר | `v9_bars_cumulative_delta` (ייחודי לפי ts), `vol_ratio` |

**כניסה:** פתיחת הבר שאחרי הטריגר. **סטופ:** מעבר לקיצון הרצף (+1 טיק). **יעדים:** T1 = הרמה הבאה (POC/VAH/IB-mid), T2 = מהלך-נמדד של הרצף, ראנר לפי סוג-יום. **מדידה:** אותו מודל קבוע.

---

## 2 · סטאפ #1 — "רצפה כפולה בקצה-הערך עם ספיגה" (הדוגמה של מייקל, 17.09)

**הברים (IL):**
```
16:30  7713.5→7695.75  v=30k  δ=−1,348     הדרייב למטה (Open-Drive)
16:40  low 7688.25     v=21k  δ=+144   ┐   נגיעה ראשונה — ליד VAH-של-אתמול 7680.75 / VAL-של-היום 7687.5
16:45  low 7688.25     v=21k  δ=+834   │   דלתא חיובית על שפל = קונים סופגים
16:55  low 7688.25     v=13k  δ=−333   ┘
17:00  low 7683.75     v=19k  δ=−2,855 ┐   דחיפה שנייה: דלתא כבדה מאוד —
17:05  low 7680.5      v=15k  δ=−1,372 ┘   — והמחיר זז רק 7.75 נק' מתחת לשפל הראשון ולא המשיך ("מאמץ מול תוצאה")
17:10  7683.25→7692.25 v=17k  δ=+3,319     ← הטריגר: סוגר מעל שיא-הרצף (7687.5 של 17:05), הדלתא החיובית הגדולה בסשן
17:15  →7695.75        δ=+2,159 · 17:20 →7700.75 · 17:25 high 7704.0 = TPO VAH 7703.75   (+12 מהטריגר · +23.5 מהשפל)
```
**מה המערכת עשתה באותו רגע:** 17:10:04 ‏INITIATIVE_SHORT @7683 (לייב, −$102.50) — הכיוון ההפוך, בשפל; הלונגים ב-16:50 / 17:15 / 17:20 נחסמו `dalton_intent:bias` (‏OPEN_DRIVE ⇒ bias SHORT). **בפלייבוק אין "הדרייב נכשל".**

**ההגדרה (פרמטרים ראשונים — מייקל מתקן):**
- **מיקום:** השפל הראשון בתוך `0.5×ATR` מרמת-ערך (VAL של היום / VAH-או-VAL של אתמול / קצה-IB / POC של אתמול).
- **הקשר:** שלב B או C; מותר גם נגד דרייב-הפתיחה (זה "drive failed"); `move_from_open_atr ≥ 2` (היה מהלך שאפשר להפוך).
- **רצף:** שני שפלים (או שלושה) במרחק `3-12` ברים, השני בתוך `[−1.0×ATR, +0.3×ATR]` מהראשון (מותר spring מתחת, לא רחוק); בין השפלים שיא-ביניים = **קו-הצוואר**; **ספיגה:** בדחיפה השנייה `|δ| ≥ 1.5× חציון-|δ|` של הסשן **ואילו** ההתקדמות מתחת לשפל הראשון `≤ 1×ATR`; זנב-דחייה בבר-השפל השני (סגירה ב-40% העליונים של הבר) — רצוי, לא חובה.
- **טריגר:** בר שסוגר **מעל** שיא-הבר-הקודם או מעל קו-הצוואר (המחמיר מבין השניים כאשר הצוואר ≤ 1.5×ATR מהשפל; אחרת — מעל שיא הבר הקודם), סגירה ב-30% העליונים, `δ_trigger ≥ +1.0× חציון-|δ|`, `vol_ratio ≥ 1.0`.
- **כניסה:** פתיחת הבר הבא · **סטופ:** מתחת לשפל השני −1 טיק · **T1:** IB-mid / POC · **T2:** VAH / מהלך-נמדד (גובה הרצף) · ראנר ביום-Trend בלבד.
- **סימטרי לשורט:** תקרה כפולה בקצה-הערך העליון.

---

## 3 · סטאפ #2 — "שבירת רוטציה אחרי דרייב" (17.09 17:45)

**הברים:** 17:20-17:40 חמישה ברים ברוטציה 7696.25-7704.0, טווחים מתכווצים, ווליום 16k→10k (מתייבש); **17:45** סוגר 7693.25 מתחת לשפל-הרוטציה 7696.25, סגירה ב-25% התחתונים, v=16.8k (×1.5 מחציון-הרצף), δ=−3,093 (הגדולה בסשן) ⇒ 17:50 low 7685.5 (+8 מהטריגר). המערכת: אפס מפיק-שבירה; VA_FADE/ZLR בצל, חסומים על מיקום.
**ההגדרה:** **מיקום** — הרוטציה בתוך/ליד אזור-ערך, אחרי דרייע ≥ 2×ATR באותו כיוון (המשך-מגמה) **או** נגד הדרייב אחרי סטאפ #1 (זה המקרה כאן: היפוך ⇒ רוטציה ⇒ שבירה חזרה למטה); **רצף** — `N=4-8` ברים שכולם חופפים (high_i < high_max+0.2×ATR, low_i > low_min−0.2×ATR), טווח-הרוטציה ≤ 1.2×ATR, ווליום יורד (חציון 3 האחרונים < חציון 3 הראשונים); **טריגר** — סגירה מעבר לקצה-הרוטציה, ב-30% הקיצוניים, `vol_ratio_trigger/median_seq ≥ 1.3`, `|δ| ≥ 2× חציון`; **כניסה/סטופ:** הבר הבא / הקצה הנגדי של הרוטציה; **יעד:** T1 = 1×גובה-הרוטציה מנקודת-השבירה, T2 = הרמה הבאה.

---

## 4 · מה מייקל מוסיף (ריק בכוונה — שלושת הסטאפים שהוא סוחר בעיניים)

| # | שם | מיקום | הקשר | רצף | טריגר | ווליום |
|---|---|---|---|---|---|---|
| 3 | | | | | | |
| 4 | | | | | | |
| 5 | | | | | | |

---

## 5 · המדידה (ללא שינוי מהפרוטוקול)

`oracle_validate` על כל סטאפ: N · good% [CI] · lift-גילוי/אימות · $/עסקה (החלקה+עמלות) · plateau על פרמטרי-הסטאפ · walk-forward. **סף לצל:** N ≥ 30 על ההיסטוריה, CI-תחתון > בסיס, חיובי בשני חצאי-ההיסטוריה. **סף ללייב:** שבוע בצל עם `[TREE-DIFF]` + פסיקת-מייקל.
