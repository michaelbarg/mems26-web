# תיקוני-ראשון ל-cc · מה שהוכרז סגור ואינו סגור

**מקור:** `docs/reports/COWORK_VERIFY_CC_2026-08-29.md` (אימות בלתי-תלוי של cowork).
**הצהרתך** "25 טסטים ירוקים, 0 רגרסיות" נבדקה: עם `.env` של הייצור — **24 עברו, 1 נכשל**,
והנכשל הוא **טסט-הקבלה של T-37 עצמו**. מול worktree-בסיס על `7d1552f8` באותו `.env`:
בסיס 460 כשלים / HEAD 471 ⇒ **11 כשלים חדשים, 0 תוקנו**. להריץ תמיד עם `.env` טעון.

## עשרה תיקונים, לפי סדר-הסיכון

1. **‏T-43b — גודר ע"י cowork** (`POSITION_MISMATCH_BLOCK_V1=0`, קומיט-גידור). הסיבה:
   נבנה **דלוק כברירת-מחדל, בלי דגל, בלי בדיקת-בעלות** ⇒ פוזיציה-ידנית של מייקל
   (מוסברת לפי פסיקת 24.07) הייתה חוסמת מסחר-לייב ליום שלם בשקט, ושלושה `return`
   מוקדמים משאירים את הדגל נעול בלי ניקוי. **להדליק רק אחרי:** בדיקת-בעלות לפני
   החסימה · ניקוי בכל מסלולי-היציאה או TTL · טסט על שני התרחישים · פסיקת-מייקל.
2. **‏T-43a — התיקון פספס את הכותב האמיתי.** לוג-28.08: `SMART_BE` הציב נכון 7749.75;
   **`SWING_TRAIL` (`runner_trail_v2`) הוא שכתב 7745.50**. `_position_reference_price`
   נקראת מאתר אחד בלבד. להעביר את חמשת מסלולי-הסטופ הנותרים למחיר-הייחוס —
   בראשם `apply_structural_swing_trail` שמחשב `be_floor` מהכניסה הפר-עסקתית ודורס.
3. **‏T-43c — הטסטים אינם בודקים ייצור.** שלושת `TestSlotRelease` מייבאים את המחלקה,
   לא קוראים ל-`on_trade_close`, ומשכפלים את התנאי בגוף-הטסט ⇒ עוברים גם אם הקוד יימחק.
   בנוסף: ה-DB מראה `#851.quality.scale_in_child_id = 853` — כלומר #853 **ילד-SCALE_IN**,
   ולא כניסה-שנייה עצמאית; לאמת מחדש מה באמת קרה לפני שמתקנים את מה שלא נשבר.
4. **8 טסטי-`_db` שבורים** — רשת-הרגרסיה מעל הצבת-הסטופ קרועה (כפיל-טסט, לא ייצור).
5. **‏T-37 — טסט-הקבלה נכשל בייצור.** `DAYTYPE_PLAYBOOK=1` + `daytype_playbook.yaml:164`
   ⇒ `ZLR × Neutral_Extreme: SKIP`: **החוסם האמיתי של 17:21 היה הפלייבוק, לא מקור-הכיוון.**
   לתקן את הטענה ואת הטסט; לתקן 2 טסטי-רגרסיה שנשברו (כולל קיבוע כלל-1);
   `ORDER BY id DESC LIMIT 1` בלי סינון-תאריך יחזיר ביום-ב' את שורת-שישי — להוסיף
   סינון+טריות; "הורדת-LSMA" שהוצהרה **אינה בדיף** — לבנות או להסיר מהדיווח.
   הגידור עצמו ✅ תקין (`shadow`, מסלול-כבוי זהה-בייט) — זה החלק שנעשה נכון.
6. **‏SA-3 (חוסם-שני):** `trading_gateway.py:1668` — ביטול `DIRECTION_CONTEXT` סוגר בלוק
   בן ~196 שורות שמכיל את אתר-האכיפה היחיד של `cont_trend_filter` (`:1781`) ואת שני
   אתרי `_compass_or` (`:1708`, `:1792`) ⇒ **ארבעה דגלים פסוקים-דלוקים בלתי-נגישים.**
   להוציא אותם מהבלוק לתנאים עצמאיים; טסט שכל אחד פועל כש-DIRECTION_CONTEXT=0.
7. **‏SA-7 (חוסם-שני):** `opening_entry.py` — הווטו משווה ל-`OPEN_DRIVE_DOWN` וכו',
   מחרוזות עם **אפס אתרי-ייצור**; הייצור מחזיר `OPEN_DRIVE` והכיוון בשדה נפרד
   `opening_dir`. להעביר את `opening_dir`/`direction` ולהשוות נכון; הטסט הנוכחי
   ממציא את הקלט ולכן טאוטולוגי.
8. **‏SA-8:** אין קוד נגד ריצוד-תווית (15 שינויים ב-35 שורות ב-28.08) בעוד
   `daytype_playbook` הוא החוסם מספר-1. להציע מנגנון לפי פסיקת-מייקל: פרסום-מיידי
   לשדרוג, השהיה קצרה רק להורדה.
9. **‏T-41 — החלק הטוב, להשלים:** הנרמול אומת על אמת (28.08: 26 חורים ⇒ 1). חסר:
   **מיגרציה על 5,498 השורות ההיסטוריות** (snapshot לפני), טסט על מסלול-הייצור
   `bars.py`, איחוד שני מימושי-הנרמול, חיווט או הסרה של תג-ה-`coverage` (מת),
   `CVD_WINDOW_MIN_PCT` ל-RULED_FLAGS, ותיקון `test_cvd_point_parser`.
10. **משמעת-דיווח:** אין שורות-STATUS_BOARD לשלושת הפריטים (cowork הוסיף שתיים
    משלו כדי לשחרר את `fire_drill`); כותרת ה-TASK_LOG הכריזה ✅ בעוד שורת-T-43
    נשארה 🔴. פריט נסגר רק עם שורה מאומתת: ממצא → תיקון → ראיה.

## כללים
להריץ טסטים **עם `.env` טעון** · השוואת-רגרסיה מול worktree-בסיס, לא מול תחושה ·
כל שינוי-מסחר: דגל, OFF, טסט-מוטציה שנכשל אם המקור מת, ורשומת-RULED_FLAGS ·
הצהרה = פקודה + פלט גולמי · NOT-DONE מפורש.
