# פרומפט לצ'אט מבצע — משימות המערכות (S2/S4/S6/UI) · 2026-07-08 ערב

**להדביק לצ'אט חדש:**

---
קרא קודם `docs/handoff/START_HERE.md` בריפו mems26_web_git ופעל לפי כלליו במלואם (‏Rule 5 —
פלט גולמי לכל טענה; טסטים אנטי-טאוטולוגיים שנופלים על קוד ישן; ‏smallest-correct-change;
עדכון `task_board.json`+יומן אחרי כל פריט). ספר-החוקים המחייב: `config/TRADING_SPEC.yaml`.
פסיקות דגלים נעולות: `config/RULED_FLAGS.yaml` — אסור לגעת בלי מייקל; ‏`scripts/flag_guard.py`
חייב PASS אחרי כל שינוי שלך.

**תחום שלך (בסדר הזה, מהלוח ב-`frontend/v9/public/task_board.json`):**
1. **‏S6 שלב 2 — התרעות תוך-עסקה בזמן אמת:** מערכת 6 כבר שופטת EOD ‏(`scripts/s6_eod_report.py`)
   וסורקת אינווריאנטים (`system6_supervisor.py`). הרחב: בזמן עסקה פעילה, בדיקה פר-בר של חוקי
   ‏TRADING_SPEC (סטופ בתוך רצועת ‏0.5–1.5×ATR · יעד לא מעבר ל-IB ביום לא-נייטרלי · ‏BE/מבנה
   אחרי T1 בוצע בפועל · כיוון מול סוג-יום) → התרעה ב-event bus + באנר בפרונט. ‏ALERT בלבד —
   שום פעולה אוטומטית (AUTOCORRECT = פסיקת מייקל, לא שלך).
2. **‏S4-RUNNER-T2-SIGN:** ‏T2 של runner בשורטים מחושב בצד הלא-נכון ונמחק ע"י sanitize
   (לוג 07-08 ‏17:30: ‏ZLR SHORT ‏t2=7506.25 מעל t1) → השורט מאבד יעד-runner. תקן את הסימן
   ב-`woodies_system.py` ‏(RUNNER_T2) + את באג-הפורמט בלוג + טסט שנופל על הישן.
3. **‏PLAN-PANEL:** בטאב התכנון (S2/S4) להציג **כל** תבנית-בהתהוות עם: מה חסר לה (התנאי
   האדום הראשון), מיון לפי קרבה-לירי, והסברים בעברית ברורה (מייקל: "כרגע מעורפל").
   ‏frontend בלבד, אל תיגע בלוגיקת מסחר.
4. **‏PAGES-AUDIT:** לעבור על `/`, ‏`/trades`, ‏`/build` מול `pages_index.json` — לעדכן סטטוס
   והערה לכל עמוד (פעיל/לבדיקה/מועמד-לארכיון) עם ממצאים קונקרטיים למייקל.

**גבולות קשיחים:** אל תעבוד על מערכת 1 (צ'אט אחר מבצע את `PROMPT_S1_DALTON_P0_2026-07-08.md`) ·
אל תבצע ריסטארטים/פעולות על המערכת החיה או סיירה (CC בלבד; הקוד שלך מוחל בריסטארט המתוזמן
הבא) · אל תדליק/תכבה דגלים · כל שינוי משטח-סיכון = עצירה ושאלה למייקל. בסיום כל פריט: קומיט
עם ראיות, עדכון הלוח, ודוח קצר מה בוצע/מה נשאר.
---
