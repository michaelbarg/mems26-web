# CC Prompt — חבילת ערב 2026-06-12: ניהול-נגד · כניסת-retrace · באג-struct · רפאים · EOD

**Contract:** `CC_HANDOFF_CONTRACT.md` — Rule 5, אנטי-טאוטולוגי RED-on-revert מוכח, NOT-DONE.
כל לוגיקת-מסחר flag-gated default-OFF + שער-Michael. קרא קודם: STATUS_BOARD שורות 06-12.

## T1 — ניהול-עסקה מגיב לתבנית-נגד (בקשת Michael, 19:45)

> "אם מתגלה תבנית שסותרת את כיוון העסקה — לשנות את הניהול. מנסים להמשיך ל-T2 בלונג,
> ותבנית מעידה על כיוון אחר."

עדות-בסיס: ניתוח 06-11 — 13 זוגות ירי-נגדי בתוך 15 דק', כמעט בכל זוג צד אחד ספג הפסד
גדול (‑$255…‑$1,042). עיצוב, דגל `COUNTER_PATTERN_MGMT` (default OFF):
1. **שיתוף-זיהויים**: כל מערכת (S2+S4) מפרסמת ל-`app.state.latest_detections` את הזיהוי
   האחרון {system, pattern, direction, conf, bar_ts} — גם זיהוי שנחסם (caps/pre_fire),
   כי המידע הכיווני תקף גם בלי כניסה.
2. **תגובת-ניהול** (ב-trade_manager, על עסקה פתוחה): זיהוי-נגד טרי (≤2 ברים) בכיוון
   ההפוך לעסקה ⇒ מדורג לפי שלב: לפני-T1 → הידוק סטופ לקצה בר-האות; אחרי-T1 (ראנר
   ל-T2) → סגירת הראנר במחיר-שוק או הידוק ל-BE+0.25R, לפי דגל-משנה
   `COUNTER_MGMT_MODE=tighten|exit` (ברירת-מחדל tighten). רישום ל-mgmt_log
   (`COUNTER_TIGHTEN`/`COUNTER_EXIT` + הזיהוי שגרם).
3. **סינון רעש**: רק זיהוי בעוצמה (conf ≥ 0.7) ורק מתבנית שאינה אותה-משפחה
   (ZLR-נגד-ZLR בכיוון הפוך = כן; אותו כיוון = לא).
4. רגרסיות: עסקת-LONG פתוחה + DOUBLE_TOP SHORT conf 0.8 ⇒ עם דגל=הידוק נרשם,
   בלי=כלום; RED-on-revert מוכח. **שער-Michael לפני הדלקה.**

## T2 — כניסת-retrace על בר-ענק (ההנחיה מהצהריים על #69)

כשה-fire נולד על בר-ענק (range ≥ 2×ATR): במקום כניסת-market בסגירה, הצב כניסת-limit
ברטרייסמנט (ברירת-מחדל 38% לתוך הבר, env) עם תוקף N=2 ברים; לא מולא ⇒ ביטול. עם
GIANT_BAR_STOP_V1 הקיים זה משלים את ה"כניסה ב-7407 עם סטופ בתוך הבר". דגל
`GIANT_BAR_RETRACE_ENTRY` (OFF). SHADOW: סימולציית-fill מול ברים (כמו T1-detection).

## T3 — באג `struct=None` ב-RUNNER_T2 (הוכח בלוג — כל ה-T2 היום R-mult בלבד)

`compute_targets_for_day_type` לא מחזיר `t2_price` אף פעם (exception נבלע או חוזה-API
שונה). אבחן (הדפס פעם אחת את החזרה המלאה), תקן את החיבור, רגרסיה: ביום Normal עם
POC/VA זמינים ⇒ T2=min(R-mult, רמה) ולא R-mult עירום.

## T4 — מופעי-רפאים (status.py:250)

`FiveMinSystem()+hydrate()` בכל קריאת-סטטוס: ① הוכח האם המופע-הזמני מסוגל לפלוט
setups (אם כן — חומרה גבוהה) ② תקן לשימוש ב-app.state instance ③ ודא שהלוג הכפול
(פורמט-ts עם רווח) נעלם.

## T5 — דוח EOD ליום-הניסוי (חוסר מאתמול — חובה הערב)

`scripts/eod_anchor_trial_report.py`: כל עסקה/דחייה היום × עוגן, סיכון, CAP-SKIP/
SIZE_DOWN/GIANT_BAR_STOP, T2 שהוצב מול שהושג, קאונטרפקטואל בלי-תקרות/בלי-giant-bar,
היסטוגרמת [S2-DL] (איזה תנאי חוסם הכי הרבה אחרי האדפטיב), והשוואה ל-06-11. הפלט =
בסיס החלטת-העוגנים של Michael + הכיול מול קובץ-הסימונים שלו מהכלי החדש.

## T6 — קווי POC/VAH/VAL שגויים בצ'ארט (אבחון Cowork 21:00 — צילום Michael)

עדות: Sierra tpo.json (מקור-האמת) אומר היום POC=7414.00 VAH=7437.00 VAL=7383.50;
`/api/v9/key_levels` מחזיר נכון ✓. אבל בצ'ארט מצוירים **ורודים POC=7416.75 VAH=7438.00**
— ערכים שלא קיימים בשום מקור-אמת ⇒ מחושבים מקומית (הפרת Rule 1). בנוסף:
`v9_bars_volume_profile` — **val==poc ב-92/92 שורות היום** (חישוב VAL שבור באינג'סט),
והתוויות מתערבבות: VAL-של-היום (7383.50) מוצג בקבוצה הלבנה של אתמול.
תיקון: ① הצ'ארט מצייר רמות-סשן אך ורק מ-key_levels (Sierra) — להסיר/לתקן את המקור
הוורוד המקומי ② שורש val==poc באינג'סט volume_profile ③ הפרדת תוויות היום/אתמול.
מצא איזה קומפוננט מצייר את הוורודים (ChartArea/levels overlay) והדבק את המקור.

## T7 — קווי עסקה בפועל על צ'ארט-המערכת (בקשת Michael)

על הצ'ארט הראשי: לכל עסקה פתוחה/סגורה-היום — קווי entry/stop-נוכחי/T1/T2 + סימוני
▲כניסה/⊗יציאה (כמו בכלי-הסימון של Cowork, `TRADE_PLACEMENT_MARKER`). בדוק קודם:
`TradeMarkerOverlay.tsx` קיים ב-frontend/chart — ADAPT אותו, אל תבנה כפול. עדכון על
ה-polling הקיים (לא להוריד intervals — §Polling Floors).

## T8 — Chop-veto שלב-1: צבירת דאטה בלבד (Michael 22:30 — תכנון משותף, אין הדלקת gate!)

ההחלטה העומדת (gate כבוי) בתוקף — שלב-1 הוא log-only: ① תקן את ההתמדה — `v9_chop_score`
ריקה (0 שורות; הציון מחושב ב-`layer0/chop_score.py` אבל לא נשמר) — שמירה כל בר-5דק'
(composite + 6 האינדיקטורים + state). ② הוסף אינדיקטור-7 לפי ספק-Michael: **ווליום-חלש**
(ווליום-בר מול ממוצע-20 מותאם-שעת-סשן) — מחושב+נשמר, עדיין לא במשקולות (weight=0 עד
שמייקל יקבע ב-4 שאלות-העיצוב). ③ העיצוב המדורג המוצע (RESPECTING⇒SIZE-DOWN,
SEARCHING⇒veto) מתועד — **לא לממש את ה-gate עצמו**.

## NOT-DONE / מחוץ לתחום
אין הדלקת דגלים חדשים בלי Michael · Standing Decisions OFF · §7a · אין שינוי ספי
b2_vsa/expansion (ממתין למחקר החיצוני + סימוני-הכלי).

## דו"ח
`docs/reports/EVENING_2026-06-12.md` + לוחות. Cowork מאמת.
