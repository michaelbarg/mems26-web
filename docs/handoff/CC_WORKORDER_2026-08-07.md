# cc — פקודת-בוקר 07.08 (פסיקות-מייקל ליל-06.08). לפני-פתיחה; Rule-5 בכל סעיף.

## P0 — לפני הסשן (שלושת אלה = תנאי-GO)
1. **ערוץ-הפקודות single-slot (שורש-השורשים):** MODIFY_STOP נדרס ע"י MODIFY_TARGET באותו
   קובץ (פגע ב-#633 וב-#643). תקן: תור-פקודות (קבצים ממוספרים או תור-ב-DB שה-DLL מרוקן)
   + ACK פר-פקודה + טסט-מקביליות. עד-אז: אסור לשלוח שתי פקודות באותה שנייה.
2. **חשבונאות-אמת:** #640 נרשם $0 בעוד ה-fills אומרים −$131.25 (FillPoller זרק CLOSED→CLOSED).
   תקן את ה-poller לקבל fills על רשומה סגורה (עדכון-P&L בלבד) + תקן רטרואקטיבית את
   03-06.08 מול fills-journal + טסט. אמת-הכסף קודמת לכל.
3. **לוגי-הצל כתבו 0 שורות** (DDL-גולמי נבלע תחת SA-2.0): תקן, אמת שורה-נכתבת בסים,
   וספירת-3-הימים מתחילה מחדש מחר.
## P1 — זיהוי-עסקה בשלבים-נכונים (הפסיקה המרכזית של מייקל)
4. **שחרור-קצה ביום-BALANCE:** direction_context חסם 2 שורטים בקצה-הרוטציה המדויק (7758-59).
   כלל: regime=BALANCE + מחיר בקצה-רוטציה/VA + חתימת-דחייה ⇒ פטור-direction_context
   (flag-OFF ⇒ replay על 06.08+14-ימים ⇒ פסיקה).
5. **קונסיומר-כניסה ל-EXCESS:** לואו-EXCESS ביום-מאזן ⇒ סטאפ-לונג-מגיב (ולהיפך). המנוע
   כבר מזהה — לחווט כניסה flag-OFF ⇒ replay. (הרוטציה 7724→7745 עברה בלי עסקה.)
6. **gateway_routes.py:84** נכשל כל-סייקל (bigint=text) — תקן.
## P2 — יציבות ואמינות
7. **כותב-סוג-היום נתקע שוב** (19:00, 3.5ש') למרות ה-watchdog — תקן את הכותב עצמו
   (thread-death root) או self-heal: watchdog מזהה ⇒ מפעיל-מחדש את הכותב, לא רק מתריע.
8. אוסצילציית Neutral_Extreme↔Center (#647) — היסטרזיס לסיווג.
9. סעיף-6-הישן (יעדים-מעוגני-מבנה) — ה-T3 שפספס בטיק את הלואו = המקרה המחייב.
## שאלת-S6/S7 של מייקל — תשובה בקוד
S6: המכניקה עבדה (scratch ירה, BE הוצב) אבל (א) חלון-scratch↔סטופ 0.5נק' = אין-חלון —
פתח gap (scratch=סטופ−2 לפחות) פר-תבנית; (ב) החשבונאות שיקרה (P0-2). S7: לא-נמדד היום
(P0-3) — אחרי התיקון, 3 ימי-איסוף אמיתיים.

## P1.5 — כרטיס-עסקה-חיה בטלפון עם אימות-סיירה (פסיקת-מייקל ליל-06.08)
בשני דפי-הפלאפון (מקומי + Render), כשיש פוזיציה פתוחה — כרטיס "עסקה בפועל":
1. כיוון · חוזים · כניסה · P&L-חי · מנגנון-פעיל (BE?/scratch-סף).
2. **אימות-מיקום מול סיירה (הלב):** פר-רמה (סטופ, T1, T2, T3) — המחיר-שהמערכת-מתכוונת מול
   המחיר-שיושב-בפועל-בפקודות-סיירה (מ-sierra_state.orders + נתוני-אינווריאנט-10 שכבר קיימים):
   ✅ תואם / 🔴 פער (עם שני המחירים). זה בדיוק מה שהיה תופס את #633/#643 מהטלפון.
3. **מד-התקדמות פר-יעד** כמו בפרונט: פס כניסה→יעד עם מיקום-המחיר-הנוכחי ו-% דרך.
מקור-נתונים: הרחבת ה-snapshot שכבר נדחף כל 5ש' (radar+account) — אפס-endpoints חדשים ברנדר.
קבלה: סים עם בראקט חי — הכרטיס מראה תואם; הזזת-פקודה-ידנית בסיירה ⇒ 🔴 תוך ≤10ש'.

## P0.5 — ביקורת-סיווג-06.08 (פסיקת-מייקל: "היה Variation-down, לא Neutral")
מייקל קרא מהנרות: פריצה-מטה + חזרה + **בטן באזור-הנמוך** = Variation-down בוודאות — והמסווג
אמר Neutral (ואף התנדנד Extreme↔Center, #647). אבחן-לפני-תיקון (Rule-2):
1. הדפס מברי-06.08: ext_up_pts/ext_dn_pts + כמה-ברים-החזיקו + סגירות-מעבר, מיקום-POC/VA
   בסוף-יום (הבטן-בנמוך), ומה ערכי-sides שחושבו בכל שלב.
2. **השערה-לבדיקה:** רצפת-הרעש 0.5 (תיקון-04.08) נמוכה-מדי בלי דרישת-החזקה — דקירת-0.5-1נק'
   מעל-IB נספרה "הרחבה-למעלה" ⇒ sides=2 ⇒ Neutral. התיקון הנכון כנראה: הרחבה = פריצה
   **עם-החזקה** (סגירה-מעבר / K-ברים / delta-backed R5 שכבר קיים) ולא נגיעה; ואז 16.07/30.07
   חייבים להישאר Neutral (הרגרסיה של 04.08) ו-06.08 להפוך Variation-down.
3. הוסף למסווג משקל-בטן: POC/VA שנבנו באזור-ההרחבה = קבלה ⇒ מחזק Variation לאותו כיוון.
4. replay מלא 15 הימים: מטרה 15/15 מול קריאות-מייקל (06.08 כלול). כל שינוי flag-OFF⇒פסיקה.

## P1.6 — דיווח לשעון-אפל (פסיקת-מייקל 07.08) + השלמת האפליקציה
**שעון:** בלי אפליקציית-watchOS (פיתוח-Xcode לא-משתלם) — התראות-דחיפה שמשתקפות אוטומטית
לשעון: mobile_relay שולח POST ל-ntfy (topic פרטי ארוך-אקראי + Authorization, ב-.env:
NTFY_TOPIC/NTFY_TOKEN) על אירועים בלבד: כניסה (עם ✅/🔴-אימות-סיירה מ-P1.5) · T1/T2/T3 ·
סטופ/scratch (עם ה-P&L-האמיתי מה-fills) · PAUSE/RESUME · חירום (עירום/פער-סטופ) · סיכום-EOD.
Rate-limit (אין-ספאם), fail-open, טסטים. מייקל: מתקין אפליקציית ntfy באייפון, מנוי-topic —
ההתראות קופצות בשעון אוטומטית.
**אפליקציה:** לוודא שכל התור הקודם נמסר — P1.5 כרטיס-עסקה-חיה+אימות, כפתורי-FLATTEN/PAUSE
(CC_TASK_2026-08-04_MOBILE_EMERGENCY), כרטיסי-רדאר-מורחבים — ומה שחסר, להשלים היום.
