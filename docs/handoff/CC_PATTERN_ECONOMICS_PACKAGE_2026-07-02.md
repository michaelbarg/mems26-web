# CC — Pattern Economics Package · 5 פריטים מאושרי-Michael (2026-07-02)

**Owner:** Michael · **Prepared by:** Cowork · **אישור:** Michael 2026-07-02 (~14:00 IL, צ'אט Cowork): "כן" על חמשת הפריטים כמקשה.
**Contract:** `docs/handoff/CC_HANDOFF_CONTRACT.md` — טסטים אנטי-טאוטולוגיים · פלט גולמי · NOT-DONE חובה.
**רקע ואסמכתאות:** `docs/spec_authority/PATTERN_RECONCILIATION_2026-07-02.md` (F-1..F-8, D-1..D-9) · `RESOLVER_TARGETS_BY_DAYTYPE.html` (ספק-07-01) · `S1_TRADE_MANAGEMENT_3CONTRACTS.md` (06-20) · נתוני מימון-סטופים שנמדדו היום (בגוף המסמך).
**עקרונות-על:** הכל flag-gated default-OFF (למעט עריכות-תאים בפריט 1 — ראה אזהרה שם) · SHADOW-first · לפני כל enable: חישוב-אחורה גולמי לפני/אחרי · בנייה ואקטיבציה אחרי-שעות בלבד · אף רמה סינתטית — כל מדרגה = קצה בר/מבנה אמיתי ±3 טיקים (Michael).

---

## פריט 1 · שמונת פסקי-התאים (D-1) — `config/daytype_playbook.yaml` + קוד נקודתי

⚠️ **ה-playbook פעיל ברנטיים עכשיו** (short-circuit ל-FULL רק כש-`DAYTYPE_POSITION_GATE=1`, והוא =0) ⇒ עריכת-תאים = שינוי-התנהגות-חי בריסטארט הבא. לבצע אחרי-שעות, ליידע את Michael לפני הריסטארט.

1. **REACTIVE×Variation = עם-הכיוון בלבד** — קוד: ב-`daytype_playbook.py:117` הבדיקה `require_with_trend` חלה רק על `_TREND_DAYS`; להרחיב ל-Variation (עבור תבניות עם `require_with_trend`). מקור-כיוון: `trend_state` (RED/BLUE); None → fail-open + log.
2. **HNS = כמו-REACTIVE** — `require_with_trend` נשאר, תאי TN/TDD: REDUCED→**SKIP** (יישור ל-auth), Variation נשאר FULL אך תחת ההרחבה מסעיף 1.
3. **INITIATIVE×Normal = SKIP** — תא playbook: REDUCED→SKIP (יישור ל-auth-code שכבר חוסם בפליטה). + לתקן את כרטיס-INITIATIVE ב-`PATTERN_PLAYBOOK_CANDLES.html` (סולם-Normal מוצג שם — להסיר/לסמן SKIP).
4. **HTLB = עם-המגמה בלבד** — להוסיף `require_with_trend: true` ל-HTLB (תאי TN/TDD נשארים FULL).
5. **TT+GB100×TN/TDD: REDUCED→FULL** ("לא מוקטן — לתקן את ההגדרה שלהן").
6. **DBDT×TDD: REDUCED→SKIP** (תמיכה בסוג-היום; יישור ל-auth).
7. **ZLR×NeuC: נשאר SKIP** (אשרור — אין שינוי).
8. **TT — אובחן + נפסק "נתקן" (Michael 07-02 ~17:10) → `TT_SPEC_V2` (דגל, default OFF):** האבחון (Cowork, קריאת-קוד): הקוד מזהה תבנית אחרת מהמקור — "נשיקת-TCCI" תלת-ברית (`tt.py:80-85`: was_above>10 → touch≤+5 → bounce>+5 באותו בר, בתוך BLUE+cci14>0) — רצף כמעט-בלתי-אפשרי ⇒ 0 ירי אי-פעם. **התיקון לפי Table A שורה 3:** Stage-1 trend → פאוזת-CCI מתחת ל-ZL **3–9 ברים** (רצפה −100; לא-חצה −50 = tag-איכות) → בר-כניסה: CCI-14 **וגם** TCCI סוגרים מעל ZL + סגירת-מחיר מעל EMA-34 (כניסה=close לפי D-5) → סטופ: שפל-בר-האות −3T (מחליף `zl_excursion` w9). טסט נכשל-על-הישן: פאוזה-4-ברים-מתחת-ZL + חזרה-כפולה ⇒ הישן לא מזהה, החדש כן. עדכון-כרטיס: "דחיסה 1-2 נרות" → "פאוזה 3-9 מתחת ל-ZL" (ממתין לאישור-ספק סופי של Michael על הניסוח).

9. **באג-מיפוי דאבלים (ביקורת-נתיבים 07-02, Cowork):** `_norm` ב-`daytype_playbook.py:73-78` לא ממפה את ה-IDs האמיתיים — `DOUBLE_BOTTOM_EE_LONG`→`DOUBLE_BOTTOM_EE` ו-`DOUBLE_TOP_AA_SHORT`→`DOUBLE_TOP_AA` **אינם ב-aliases** (יש רק `DOUBLE_BOTTOM`/`DB_EE`/`DOUBLE_TOP`/`DT_AA`) ⇒ unmapped ⇒ FULL fail-open ⇒ תאי-DBDT (כולל D-1 TDD=SKIP) לא חלים. **מיטיגציה חיה (מאומת):** auth-V1 חוסם דאבלים בפליטה על TN/TDD/NT (SKIP בשלושתם) ⇒ אין חשיפה בפועל היום; אבל D-1 הכריז על ה-playbook כטבלה הקנונית — התיקון חובה יחד עם עריכת-התאים. תיקון: שני aliases + טסט `decide("DOUBLE_BOTTOM_EE_LONG","Trend_Normal",...)==SKIP` (נכשל-על-הישן).

הערת-scope: verdict=REDUCED אינרטי בגייטוויי (רק `.allow` נבדק, `trading_gateway.py:281`) — **לא** לחווט את `Decision.contracts` במסגרת החבילה הזו.

## פריט 2 · D-3 — יישור הרזולבר לספק-07-01 (`structural_targets.py`)

חוקי-הבסיס לכל סוגי-היום (היום קיימים רק ב-`_resolve_variation`):
- **C1 = ה-swing המבני הראשון** בכיוון (Williams K=2, close-confirm) — להרחיב את `_find_swing_t1` ל-Normal/NeuE/NeuC/Trend_Normal/Trend_DD. ב-Trend: ה-checkpoint הרחוק (IBH+2×IBw) עובר להיות חומר ל-C2/C3 — ספק-07-01 גובר על 06-20 (פסק D-3).
- **C2 = מבנה קרוב** (POC / IB-center / ½ext / swing-ביניים) הקרוב מקצה-הערך; **C3 = קצה-ערך/extension (רנר)**.
- **קאפים לפי הספק (min, לא max):** C1 ≤ `min(2×ATR₅ₘ, 0.30×dATR)` ≈14 · רצפת-C1 `0.5×ATR₅ₘ` ≈3.5 · רנר ≤ `min(1.5×dATR, 3×IBw)`. להחליף את `max(2×ATR, 2×risk)` הנוכחי (`:417-419`).
- **ATR חי** — לא 7.0 קשיח (`:411`); מקור: ATR₅ₘ שוטף + dATR-RTH נמדד.
- **אכיפת-מונוטוניות:** אחרי הרזולוציה — `|C1−entry| < |C2−entry| < |C3−entry|`, אחרת מיון/תיקון + log. לתקן את ה-fallback שבו `swing_t1=None` ⇒ C1=half_ext העמוק (`:225,239`) — ה-fallback בוחר את המדרגה הקרובה בתוך הרצועה. **טסט-רגרסיה חובה על הדגימה מ-07-02 13:00:** HTLB SHORT C1=7560.5 C2=7569.5 C3=7568.0 (הפוך) חייב להפוך מונוטוני.
- fail-safe ל-R-based נשאר. שדות `contracts`/`time_stop` שהרזולבר מחזיר נשארים לא-נצרכים (מחוץ ל-scope).
- **אינווריאנט-BE (Michael 07-02 ~21:1x: "אם T1 קרוב לכניסה זה לא שווה כלום"):** ה-BE-אחרי-T1 (smart_be) נדרך **רק** כשמרחק-ה-T1-שנפגע ≥ רצפת-C1 (0.5×ATR-חי). T1 מנוון מכל מסלול-קצה עתידי לא מזיז את הסטופ ל-BE — ההגנה לא הופכת למכונת-שריטות. טסט: T1 במרחק 1.4pt נפגע ⇒ smart_be לא רץ + WARNING; T1 במרחק ≥רצפה ⇒ BE רגיל.
- **מלאי-רמות ליעדים (Michael 07-02 ~19:4x: "קווי-תמיכה של היום ושל אתמול לצורך מימוש"):** מועמדי-C2/C3 יורחבו לכלול גם את **רמות-אתמול שכבר מחושבות** — prior_POC/prior_VAH/prior_VAL (קיימים ב-classify_replay, לא-נצרכים היום) + PDH/PDL (היום רק במסלולי-Trend → לכל סוגי-היום). כלל-חדש: **עצור-לפני-רמה-חזקה** — יעד שנופל ≤2T מעבר לרמה-נגדית משמעותית (POC-אתמול / קצה-VA) נצמד אליה מהצד הקרוב. ‏HVN/LVN-proxy מפרופיל-אתמול = שלב-ב' (workstream, דורש מנוע-TPO-count).
- **ראיות-חיות 07-02 18:16 (עסקאות 277/278, חובה בטסטים):** ‎(א) **אין רצפת-C1** — ‏T1 יצא ‎1.375 נק' מ-entry ("המימוש קרוב מדי", מילא C1 תוך שניות והשאיר 2 חוזים); הרצפה ‎0.5×ATR≈3.5 מהספק חייבת להיאכף. ‎(ב) **t1==t2 זהים** (7530.625 בשתיהן, וגם ב-275/276) — ‏C2 חייב להיות שונה-ורחוק-מ-C1. ‎(ג) **מחיר off-grid** — ‏7530.625 אינו על גריד-הטיק (0.25); כל יעד/סטופ מה-resolver חייב עיגול-לגריד לפני שליחה.

## פריט 3 · שער-R:R בכניסה — דגל חדש `RR_ENTRY_GATE_V1` (default OFF)

בגייטוויי, **אחרי** הצבת-היעדים המבניים (סדר קריטי — צריך C1 סופי וסטופ סופי): אם `|C1−entry| < |entry−stop|` → block (`blocked_by="rr_entry_gate"`) + log של שני המרחקים. חל על כל 16 התבניות, S2+S4. fail-open אם C1/סטופ חסרים. זהו כלל-06-20 של Michael ("מרחק ל-C1 קטן ממרחק-הסטופ → דלג") — הבלם הישיר של מימון-סטופים. ראיה מהיום: מאז 06-20 רק 8/55 עסקאות פגעו ב-T1; יחסי C1:סטופ נעו מ-0.77 (BULL_FLAG/ZLR — מימון ישיר) עד 17.6 (HTLB — יעד שלא נפגע).

## פריט 4 · Stop Resolver V1 — דגל חדש `STOP_RESOLVER_V1` (default OFF, SHADOW-first)

**רצועה:** רצפה `0.5×ATR₅ₘ` (≈3.5) · תקרה `1.2×ATR` ל-CONT / `1.5×ATR` ל-REV (מקדמי Table C — היום size-gate בלבד, כאן הם בוחרי-מדרגה) · hard-cap 25pt נשאר.
**כלל:** הולכים על סולם-המדרגות מהקרובה לרחוקה ובוחרים את הראשונה שמרחקה (קצה-מבנה ±3T) בתוך הרצועה. קרובה-מדי → מדרגה הבאה החוצה (משחרר את ההדוקים — I-55). כולן מעל התקרה → **אין עסקה** (`no_stop_in_band`, log). אף רמה סינתטית.

**סולמות ראשוניים (אושרו עקרונית; אשרור-סופי פר-תבנית בהמשך ההליכה):**

| תבנית | קרובה → רחוקה |
|---|---|
| REACTIVE | שפל-b3 → שפל-b2 (שפל-ההיפוך) → אזור-w4 |
| INITIATIVE | שפל-בר-הפריצה (w1) → שפל-b2 → הרמה-השבורה ∓3T |
| BULL/BEAR_FLAG | שפל-בר-הפריצה → **השפל של הבר-הנמוך-ביותר בחלק הצובר** (תיקון-Michael: לא "חצי-דגל") — אין מדרגה שלישית; מעבר=skip · **✅ אושרר-סופית בהליכה 07-02 (D-10)** |
| VEGAS | **✅ אושרר-סופית 07-02 (D-11):** קצה-בר-הכניסה → קצה-הידית (במחיר) → תחתית/פסגת-הספל (=swing_extreme הנוכחי) |
| GHOST | **✅ אושרר-סופית 07-02 (D-12):** קצה-בר-הכניסה → קצה-כתף-ימין (במחיר) → קצה-הראש (במחיר; =העוגן הנוכחי — מתויג "shoulder" ב-YAML, לתקן תיוג) |
| ZLR/TLB/TT/GB100 | העוגנים הנוכחיים (כבר ברצועה) — עוברים דרך הרזולבר כמדרגה-יחידה |
| FAMIR | **✅ אושרר-סופית 07-02 (D-13, נוסף — היה חסר בטבלה):** קצה-בר-הכשל → קצה-שלב-3 של ה-ZLR-הכושל → קיצון-המהלך (שלב-1) — תחת הגאומטריה החדשה של פריט 12 |
| HTLB | consolidation_extreme הנוכחי |
| DBDT/HNS | second_bottom_top / shoulder הנוכחיים — לוודא התאמה לרצועה ולדווח |

**לפני enable (חובה):** חישוב-אחורה על כל הירי מאז 06-20 — טבלה גולמית תבנית × (סטופ-ישן, סטופ-רזולבר, מדרגה שנבחרה, %-בתוך-רצועה). בסיס-המדידה מהיום (חציוני, אחרי סינון BE-artifacts): REACTIVE 10.8-12 · FLAGS 15.6 · GHOST 13 · FAMIR 11.3 · VEGAS 11 · ZLR 8.5 · INITIATIVE 6.5-8.3 · HTLB 5.3.

⚠️ **מתודולוגיית-מדידה (הליכה 07-02, D-10):** סיכון-פתיחה אמיתי נקרא מ-`v9_trade_management_log` (`SMART_BE.from` / `TRAIL.from`), **לא** מעמודת `stop` של `v9_trades` שכוללת BE/trail. דוגמה מהדגלים: עמודת-stop → חציון 15.6, אבל initial אמיתי = **10.5** (n=6: 6.0/8.5/10.5/10.5/15.5/15.75, נמשך 07-02).

⚠️ **באג-מקור לתקן בתוך הפריט (S2):** ענף-V2 גוזר את חלון-העוגן מ-`self._bar_buffer[-window:]` (`five_min_system.py:1220`) — כולל את **הבר-החלקי** החדש, בעוד הכניסה נגזרת מ-`_det_buf[-1]` (הבר שהושלם). מדרגות ה-Stop-Resolver חייבות להיגזר מ-`_det_buf`. לאבחן קודם איזה מסלול חי בפועל: הסטופים המוקלטים תואמים detector-anchor/legacy (חשד ל-fallback `cfg=None` ב-`:1245-1251`) — Rule 5: פלט גולמי.

## פריט 5 · ווליום-b4 ל-REACTIVE — דגל חדש `S2_B4_VOL_V1` (default OFF)

הכרטיס דורש "b4 עם ווליום עולה" — הקוד לא בודק (`five_min_system.py:690-693` — אין תנאי-ווליום על b4). תנאי: `b4_vol > b3_vol`, אחרת reject + log (עקבי עם וריאנטי-ה-VSA הקיימים). לשקול log-only שבוע ראשון.

---

## פריט 6 · אישור-כניסה ל-S4 — דגל חדש `S4_ENTRY_CONFIRM_V1` (default OFF) — אושר ~15:40

**ממצא:** אף תבנית-woodies לא קוראת ווליום/דלתא (grep ריק על `backend/v9/systems/woodies/patterns/`); `v9_bars_5min_woodies` מכילה `volume`, דלתא-פר-בר זמינה מ-`v9_bars_5min`. **דרישה (Michael):** נר-הכניסה של כל תבנית-S4 חייב (א) שינוי-קומולטיב מאשר — דלתא-פר-בר בכיוון העסקה, ובהיפוכים לחלופין דיברגנס-CVD; (ב) ווליום נר-הכניסה > הנר הקודם. במראה של `S2_CVD_DETECTION_V1` הקיים (fail-open על CVD מעופש). log-only שבוע ראשון → ואז enable per-Michael.

## פריט 7 · חלון דינמי "החל מ-4 נרות" — מחקר `S2_PHASE_DETECTOR_V1` — אושר ~15:40

**מוטיבציה כמותית (ניתוח 07-02):** כל 12 ה-INITIATIVE מאז 06-20 = STOP_HIT; בארבע (237/240/262/263) ה-MAE עבר את הסטופ ב-≤2 נק' ואז MFE ‎27-52 נק' לכיוון — סטופ-בר-הפריצה לבד נשטף מרעש (I-55 מאושש, ‎−$267). **שלב-מחקר בלבד (לא קוד-חי):** פרוטוטייפ אופליין שמריץ על ברי 2-3 השבועות האחרונים זיהוי-לפי-שלבים (קליימקס → התייבשות 1-3 ברים → ריקליים → אישור; INITIATIVE: התפשטות → בדיקה 1-3 → הצטרפות → אישור) מול ה-4-הקבוע, ומדווח גולמי: תבניות שנוספו/נעלמו, MFE/MAE של כל אחת, שיעור-זבל. שפל-שלב-ההתייבשות = מדרגת-מ2 טבעית. חיווט-חי רק אחרי פסק-Michael על התוצאות (זו סטיית-ספק מחוקת-V3).

## פריט 8 · זיהוי פולבק-ריטסט — מחקר+ספק (Michael ~15:45: "יש גם פולבק שצריך לזהות אותו")

**החור:** ספק-06-20 ל-Variation קובע "כניסה בכיוון ההרחבה; **עדיף בפולבק לקצה-ה-IB שנשבר** (הפך S/R)" — ואין היום אף תבנית שמזהה פולבק-ריטסט: INITIATIVE תופס את הפריצה עצמה בלבד. **ספק ראשוני לזיהוי:** (1) פריצה/הרחבה מאושרת של רמה (IB-edge / VAH-VAL / שיא-שפל-קודם); (2) פולבק של 1-4 ברים אל הרמה-השבורה בווליום דועך (לא סוגר מעבר לה); (3) בר-חידוש בכיוון-ההרחבה עם ווליום עולה = כניסה בסגירתו; סטופ = מדרגות: קצה-בר-החידוש → קצה-הפולבק (מעבר לרמה) — מבני, בתוך הרצועה; יעדים מהרזולבר (CONT). דיוק-ההגדרה מול Michael לפני-בנייה; משתלב טבעית ב-phase-detector של פריט-7 (הפולבק = שלב). דגל עתידי: `S2_PULLBACK_RETEST_V1`.

## פריט 9 · אימות-ווליום+פריצה לדגלים — דגל חדש `S2_FLAG_VOL_V1` (default OFF) — ✅ Michael 07-02 (הליכת-תבניות, D-10)

הכרטיס דורש מוט-ווליום-גבוה / דגל-מתייבש / פריצה-בווליום — `patterns/flags.py` גאומטרי-בלבד (אפס תנאי-ווליום; גם העזרים ה-ATR-יחסיים `flags.py:49-60` ללא callers). ארבעה תנאים (AND), מראה בשורט:

1. **ייבוש-צבירה:** `avg(vol דגל) ≤ 0.8 × avg(vol מוט)`
2. **פריצה-בווליום:** `vol בר-הפריצה ≥ 1.3 × avg(vol דגל)`
3. **באפר-מחיר:** סגירה ≥ ראש-הדגל + **3T** (היום +1T, `flags.py:175`)
4. **סגירה-חזקה:** close בשליש-העליון של טווח בר-הפריצה

אופציונלי: CVD נטו-בכיוון על חלון-התבנית (תשתית `_compute_setup_cvd` קיימת). המספרים (0.8 / 1.3 / 3T / שליש) = נקודת-כיול — עדיף YAML-tunable. לפני enable: הרצה על ההיסטוריה כולל 6 הירי (לדווח האם שני מפסידי-06-24, ‎−15.5/−15.75, היו נחסמים) + שבוע log-only. ידוע ומקובל: דגלים ירו 6× בחודש והתנאים יורידו תדירות (איכות>כמות — Michael). נלווה docs-only: בכרטיס-FLAGS ב-`PATTERN_PLAYBOOK_CANDLES.html` לתקן שורת-סטופ+SVG (flag-low → בר-הפריצה; מדרגה-2 = שפל-הבר-הנמוך-בדגל).

## פריט 10 · יישור-VEGAS למקור (ספל-וידית) — קוד נקודתי + docs — ✅ Michael 07-02 (הליכה, D-11)

⚠️ `VEGAS_SPEC_V2=1` חי ⇒ שינוי-הקוד כאן = שינוי-התנהגות בריסטארט הבא — אחרי-שעות + יידוע Michael.

1. `vegas.py:79` — `_HANDLE_MIN_BARS: 2 → 3` (המקור: ידית "לפחות 3 יחידות-זמן"). טסט אנטי-טאוטולוגי: ידית 2-ברים שעוברת על הישן חייבת להידחות על החדש.
2. `_HANDLE_MAX_RETRACE = 0.5` **נשאר** (אושרר 07-02 — שער-איכות, אינו במקור אך משרת את כוונתו).
3. docs-only: כרטיס-VEGAS ב-`PATTERN_PLAYBOOK_CANDLES.html` → ספל-וידית (4 צעדי-המקור + "אסטרטגית לא טקטית") · פוטר `RESOLVER_TARGETS_BY_DAYTYPE.html` — להסיר "Cup&Handle — לא במערכת" (שגוי; VEGAS הוא בדיוק זה).

## פריט 11 · יישור-GHOST למקור (CCI-H&S + קו-בחינה) — דגל חדש `GHOST_SPEC_V2` (default OFF) — ✅ Michael 07-02 (הליכה, D-12)

המקור (סקרינשוטים 07-02): קו-בחינה בין הכתפיים ששבירתו = הפריצה · רצוי בשיפוע לכיוון ZL · כתף-2 לכל-היותר בגובה כתף-1 · **אין חשיבות לגודל הראש** · כל התבנית מעל ZL לשורט / מתחת ללונג. הקוד היום (`ghost.py:80-146`): אין נקליין (טריגר = ירידה מתחת לערך כתף-ימין) · דורש ראש-הכי-גבוה · אין בדיקת-אזור.

תחת הדגל (OFF = התנהגות-היום בדיוק):
1. **נקליין:** קו דרך שני השפלים שבין שלוש הדחיפות; כניסה על חצייה טרייה שלו (close-confirm) בכיוון האיתות.
2. להסיר `p2>p1 ∧ p2>p3`; להשאיר `p3 ≤ p1` (≤ במקום <, יישור למקור).
3. **צד-אפס קשיח:** כל ערכי-הסגמנט (מכתף-1 עד בר-הכניסה) > 0 לשורט / < 0 ללונג.
4. שיפוע-נקליין לכיוון ZL: לא חוסם — שדה log (`neckline_slope_toward_zl`) לניתוח.
5. docs: כרטיס-GHOST ב-`PATTERN_PLAYBOOK_CANDLES.html` ← לפי דף-המקור (קו-בחינה · חוק-אפס · "רוח-רפאים") במקום failed-high; `PATTERN_ACCESS_MAP.md` שורת-GHOST ("שבירת neckline") תהיה נכונה רק אחרי היישור — לסמן.

טסטים אנטי-טאוטולוגיים: (א) ראש קטן מהכתפיים — נדחה על הישן, עובר על החדש (ב) תבנית חוצת-אפס — עוברת על הישן, נדחית על החדש (ג) ירידה-מתחת-לכתף בלי שבירת-נקליין — יורה על הישן, שקט על החדש. שבוע log-only → enable רק ע"י Michael.

## פריט 12 · יישור-FAMIR למקור (ZLR-שנכשל) — דגל חדש `FAMIR_SPEC_V2` (default OFF) — ✅ Michael 07-02 (הליכה, D-13)

המקור (דף-קורס 07-02): "תבנית ZLR שנכשלה **באופן מיידי** — מייד אחרי שלב-3 ה-CCI שובר חד לכיוון המנוגד · חיזוק כשהכשל בתוך ±50 · **ה-LSMA צריך לתמוך ברגע הכניסה** (ZL ירוק ללונג / אדום לשורט) · שלב-1 של ה-ZLR הכושל עבר את ה-200". הקוד היום (`famir.py:72-73,135-136`) = failed-200-approach (שיא-5-ברים ב-170-209 + ירידת 20 נק'-CCI) — ללא שום קשר ל-ZLR.

תחת הדגל (OFF = התנהגות-היום בדיוק):
1. חיבור למכונת-השלבים של `zlr.py` (`ZLR_SPEC_V2`): שלב-3 הושלם → חלון-כשל **1-2 ברים**: חציית-ZL חדה נגד כיוון-ה-ZLR ⇒ FAMIR בכיוון המנוגד, כניסה בסגירת בר-החצייה.
2. תנאי: שלב-1 של ה-ZLR הכושל עבר ±200.
3. LSMA-תומך בכיוון-החדש = קשיח (מיפוי ל-AP9 הקיים — לוודא שהבדיקה = צבע-ZL כמו במקור).
4. כשל בתוך ±50 = שדה-ביטחון/log (`fail_within_50`), לא חוסם.
5. ה"פליפ" על פוזיציית-ZLR פתוחה (יציאה בהפסד + היפוך) — **מחוץ ל-scope**; לרשום ב-NOT-DONE כשאלה פתוחה ל-Michael.
6. docs: כרטיס-FAMIR ← דף-המקור (כולל שורת הקושי-המנטלי) · תיוג `failed_bar` ב-YAML (בפועל קיצון-5-ברים) · שורת-ACCESS_MAP.

טסטים אנטי-טאוטולוגיים: (א) failed-200-approach בלי ZLR — יורה על הישן, שקט על החדש (ב) ZLR-שלב-3 שנכשל בחציית-ZL תוך 1-2 ברים — שקט על הישן, יורה על החדש (ג) אותו כשל עם LSMA-נגד — נחסם בשניהם.

## פריט 10 · ירי-בחלון-הפתיחה לפי סוג-הפתיחה — דגל חדש `OPENING_WINDOW_FIRE_V1` (Michael 07-02 ~16:55, הוראה קבועה)

**הפסיקה (Michael, מילולית):** "לצורך עסקה ראשונה — ברגע שהמערכת מזהה לפי סוג-הפתיחה עסקה, עליה לבצע רכישה גם בחצי-השעה הראשונה."

**הראיה החיה (07-02 08:45 CT):** S2 זיהה `INITIATIVE LONG` בדרייב-פתיחה עולה ונחסם בפליטה:
`T1Setup skipped: pattern=INITIATIVE_LONG day_type=UNKNOWN tier=HIGH · Auth Table SKIP` — השורש:
עד סיווג-S1 (~30 דק'), `day_type=UNKNOWN` נופל ל-fallback `Neutral_Center` (`auth_table_v1.py:143-147`)
שבו INITIATIVE=SKIP ⇒ תבניות-המשך כבויות בדיוק בחלון-הדרייב.

**ספק:** בחלון 08:30–09:00 CT (עד נעילת day_type), כאשר `opening_type` כבר ננעל (15-דק', `S1_OPEN_DRIVE_TREND`/סטייג'ינג):
1. אם `opening_type ∈ {OPEN_DRIVE, OPEN_TEST_DRIVE}` וכיוון-ה-setup == כיוון-הדרייב → **פליטת-S2 משתמשת במיפוי-פתיחה** במקום fallback-NeuC: OPEN_DRIVE→שורת `Trend_Normal` בטבלת-ה-auth (INITIATIVE/FLAGS=FULL עם-הדרייב). S4 — אותו עיקרון בגייטוויי.
2. נגד-הדרייב — נשאר חסום (`OPENING_TYPE_GATE=1` הקיים כבר עושה זאת) · `OPENING_AUCTION/AMBIGUOUS` → נשאר fallback שמרני.
3. **תלות:** תיקון I-53 (`OPENING_FIRE_CVD_V1` אינרטי — `ts::timestamptz` + fail-loud) חובה באותו קומיט — ירי-פתיחה בלי בדיקת-ה-CVD שלו = חצי-שער.
4. כל שאר הגייטים ללא-שינוי (R:R, direction, dedup...). דגל default-OFF → הפעלה בסימן-Michael, יעד: הפתיחה של מחר.
5. טסטים: UNKNOWN+דרייב-עם-כיוון ⇒ עובר · UNKNOWN+נגד-דרייב ⇒ נחסם · אחרי-סיווג ⇒ התנהגות רגילה (נכשל-על-הישן בתרחיש-1).

## פריט 11 · 🔴 HOTFIX — שחרור-סלוט וספירת-סטופים רק על T3 (נמצא חי 07-02 17:1x IL)

**הראיה החיה:** עסקאות 271/272 (REACTIVE_SHORT, נכנסו 17:10, STOP_HIT 17:13) — אחרי הסגירה:
`GET /api/v9/gateway/status` → `demo_slot:"272"` (תפוס על עסקה סגורה!) · `cooldown.consecutive_stops:0` ·
אין `[Gateway] DEMO slot freed` ואין `[Cooldown]` בלוג (ה-Cooldown האחרון: 07-01 21:15).
**שורש (קריאת-קוד):** `bar_level_detector.py:158` — `_notify_trade_close` נקרא **רק** כש-`target_name=="T3"`;
נתיב-הסטופ (`:133` → `on_stop_hit`) לא קורא לו לעולם ⇒ `gateway.on_trade_close` לא רץ ⇒ demo_slot נשאר תפוס,
cooldown/SSV/consecutive_losses עיוורים לסטופים. **זה גם השורש של I-48/D-guards-bus מ-06-29** ("cooldown=0 מול 4 stops").
**תיקון:** `self._notify_trade_close(trade, "STOP")` בענף-הסטופ (אחרי `:133`) + לוודא כיסוי בכל מסלולי-סגירה-מלאה
(TIME_STOP ב-W-10 · opposite-exit · EOD-flatten) — או ריכוז ההודעה ב-`TradeManager` על כל מעבר-ל-CLOSED (עדיף, נקודה-אחת).
**טסט אנטי-טאוטולוגי:** עסקת-דמו בסלוט → סטופ על בר → assert `gateway.demo_slot is None` **וגם** `cooldown.consecutive_stops==1` (נכשל-על-הישן).
**Impact עד-התיקון:** demo-slot תקוע עד ריסטארט (היום: shadow ממשיך לתעד; הדמו קיבל את המחזור שלו ב-17:10 ✓) · cooldown-2-סטופים לא מגן.

## פריט 12 · TT_SPEC_V2 — עדכון לספק-המקור המלא (מסמך Tony-Trade שמסר Michael, 07-02 ~17:2x)

מחליף את ניסוח-הביניים בפריט 1.8. **המקור (וודי/טוני-הולדן) — שלושה שלבים:**
- **שלב 1 (כמו-ZLR), לונג:** CCI-14 ≥6 יחידות-זמן מעל קו-האפס (כחול) · CCI הגיע מעל +100 · SWI צהוב ≥בר-אחד · ≥3 ברים אחרונים מעל EMA-34 · רצוי-לא-חובה: לא עבר +200 (tag-איכות). שורט = מראה.
- **שלב 2 (העצירה):** CCI-14 משנה כיוון ויורד מתחת לקו-האפס **בלי לחצות −100**, שוהה שם **3–9 יחידות-זמן**; לא-עבר-−50 ⇒ תבנית חזקה יותר (tag). שורט = מראה (מעל האפס, לא +100/+50). הערת-סיירה מהמקור: אזור-הטרנד **לא מתהפך** בזמן העצירה גם אחרי 6 נרות ⇒ **אסור לדרוש `trend_state==BLUE/RED` בבר-הכניסה** (הדרישה הזו בקוד הנוכחי, `tt.py:81/141`, היא חוסם-ירי נוסף) — דרישת-הטרנד שייכת לשלב-1 בלבד.
- **שלב 3 (הטריגר):** המומנטום חוזר לכיוון שלב-1 — **CCI-14 וגם TCCI סוגרים מעל קו-האפס** (לונג; שורט=מתחת) + **בר-המחיר נסגר מעל/מתחת EMA-34**. כניסה=close (D-5). "במהותו דומה לשלב-3 של ZLR".
- סטופ: שפל/שיא בר-האות ∓3T (Table A) · יעדים מהרזולבר · טסט נכשל-על-הישן כבפריט 1.8.

## פריט 13 · פילטר-P/b נגד initiative-מזויף — דגל חדש `PB_SHAPE_FILTER_V1` (default OFF) — אושר 07-02 ~18:40

**הזול-ביותר בחבילה — הנתון כבר קיים:** `profile_shape` (P/b/D) מחושב חי כל יום (tpo_history + classify_replay; נצפה P הבוקר) — ואף gate לא צורך אותו. לפי דלטון (MoM, נשמר ב-`MOM_SUPPLEMENT_DETECTORS_2026-07-02.md` טבלה-6): ראלי-short-covering בצורת-P נראה כמו קניית-OTF אבל הוא old-business — **חוסם initiative-long מזויף** (ו-b = מראה נגד initiative-short).
**תנאי-חסימה (4 תנאי-הספר, כולם):** (1) כיוון הימים האחרונים הפוך לתנועה (pd_close/direction-context) (2) open ליד הקצה הנגדי (`open_location` קיים) (3) אין המשכיות אחרי ה-drive (4) ה-auctions מאבדים קרקע בהדרגה (רצף שיאי-30-דק' יורד). **מבחן-ההבחנה מהספר:** auctions שמחזיקים שיאים = pause-לפני-המשך, לא P — אין חסימה.
**חיווט:** בגייטוויי, לפני שיבוץ, כשהתבנית CONT/INITIATIVE בכיוון-הצורה — block (`blocked_by="pb_shape_filter"`) + log 4-התנאים. fail-open על נתון חסר. גזירה מ-gap-analysis: `MOM_GAP_ANALYSIS_2026-07-02.md` שכבה-A. טסטים: תרחיש-P מלא ⇒ נחסם · bracket-pause (שיאים מחזיקים) ⇒ עובר · flag-off ⇒ אפס-שינוי.

**שאר פערי-MoM (אושר סדר 07-02):** דאבל-פרינט-transition → Value-Area-Rule → NONCONVICTION — נפתחו כ-workstream נפרד: `docs/handoff/S1_MOM_DETECTORS_WORKSTREAM_2026-07-02.md` (לא בחבילה הזו).

## פריט 16 · משטר-תנודתיות קנוני + מדיניות-יום-אלים — `VOL_REGIME_V1` (default OFF) — פסק-Michael 07-02 ~19:15

**הפסיקה (מילולית):** "ביום אלים אפשר לבצע רכישה של 2 חוזים, ולהרחיק סטופים, ולבקש עוד ודאות בכניסה — כמו עוד בדיקה, או אולי עוד בדיקה וכניסה מוקדמת."

**א · אות-משטר קנוני אחד:** שירות משותף שמחשב CALM / NORMAL / VOLATILE מ: avg-bar-range-14 (הקיים ב-`vol_adaptive_active`, סף 8pt — env) + dATR-חי + ib_pctile. חשיפה: שדה ב-state/cockpit + צ'יפ בטופ-בר. כל הצרכנים קוראים ממנו (S2-detection הקיים מוסב אליו; אין יותר חישובים מקומיים). ראיה מהיום: avg-14=16.6pt (פי-2 מהסף) ביום שבו סטופים-קבועים נשטפו ו-C1-צמוד מולא בשניות.

**ב · צרכני-המדיניות כש-VOLATILE (כל אחד בדיקה עצמאית בקוד):**
1. **גודל: 3→2 חוזים** — תיקון-סטנדינג ל-FIXED_CONTRACTS_3 ("קבוע-3, חוץ מ-VOLATILE⇒2"); נאכף באותן 3 נקודות-חנק של 6ec3209.
2. **סטופים רחוקים יותר:** רצועת-ה-Stop-Resolver (פריט-4) נמתחת — תקרה ×1.5 גם ל-CONT (במקום 1.2), עדיפות למדרגה החיצונית כשה-MAE-הצפוי גבוה.
3. **עוד ודאות בכניסה:** ‏(i) אישור-כניסה (פריט-6 CVD+ווליום) הופך **חובה** ב-VOLATILE גם אם הדגל הכללי שלו OFF-רך; ‏(ii) כשגלאי-הריטסט (פריט-8) ייבנה — ב-VOLATILE מצב-הכניסה המועדף = **retest-entry**: המתנה לבדיקה-חוזרת של הרמה וכניסה עליה (קרובה-למבנה = "מוקדמת" במחיר), במקום רדיפת בר-אישור רחוק.
4. S1: המשטר מוזן כ-input (workstream — משפיע-סיווג/ביטחון).

**טסטים:** רגיל⇒אפס-שינוי · VOLATILE⇒2-חוזים בפקודה בפועל (עד-הסוף, לא רק בפליטה — לקח-GAP-1) + רצועה-מורחבת + דחיית-כניסה-בלי-אישור. fail-on-old לכל צרכן.

## פריט 17 · יומן-החלטות בזמן-אמת — "למה לא בוצעה עסקה ומה המערכת רואה" (Michael 07-02 ~19:3x)

**הבקשה (מילולית):** "כלי שיבצע רישום לוג אוטומטי בזמן אמת שאפשר לבדוק כל הזמן — למה לא בוצעה עסקה ומה היא רואה."

**בסיס קיים (ADAPT, לא לבנות מאפס):** `missed_trade_detector.py` (Should-Have-Fired, blocked_signal/uncaptured_move) + `GET /api/v9/missed-trades` (status.py:339) — **בזיכרון בלבד** (ריסטארט מוחק — היום 3 ריסטארטים ⇒ count:0) וניזון חלקית. + נקודות-הצעקה שנוספו היום (I-59/I-60): gateway blocked_by (נקודה-אחת!), FIRE-DROPPED, S2-DL וקטורים, A7-messages, T1Setup-skipped.

**הבנייה:**
1. **טבלת `v9_decision_journal` (PG):** ts · system · pattern · direction · entry_price · stage (detection/emission/gateway/prefire) · verdict (FIRED/BLOCKED/DROPPED/NO_MATCH) · reason · details-JSON (וקטור-תנאים/failed_stages/gate) · **context-snapshot: day_type · vol_regime · dir_sustained · price**. כותב יחיד fail-safe (`journal()`), נקרא מכל נקודות-החנק הקיימות — בלי לוגיקת-מסחר חדשה.
2. **API:** ‏GET `/api/v9/decision_journal?limit&verdict&system` (מחליף/עוטף את missed-trades; תאימות-אחורה נשמרת).
3. **UI — טאב "יומן-החלטות":** כותרת "מה המערכת רואה עכשיו" (day-type, משטר, כיוון, גייטים חמושים — רכיבי-הסטריפ הקיימים) + טבלה חיה של ההחלטות (רענון 15s, רצפת-diagnostic): שעה · תבנית · עד-איזה-שלב הגיע · למה-נפל / ✅ירה. פילטרים: הכל/רק-חסומות/רק-S4.
4. אימות: כל חסימת-gateway מופיעה בטבלה תוך שנייה (טסט אינטגרציה על route_setup חסום) · שורד-ריסטארט (PG) · ה-EOD קורא ממנו את ה-missed-trades (מחליף את החישוב-בדיעבד).

## פריט 18 · דוקטרינת-יום-כיווני — פסק-Michael 07-02 ~20:3x (סוגר את שאלת-#20)

**הפסק (מילולית):** "יום טרנדי שמחפש מקום חדש — העסקאות היו צריכות להיות עם כיוון המגמה, עד שיש עצירה שמוכיחה שנעצרה. היינו צריכים לבצע עסקאות עם הכיוון שלה."

**הכלל:** כשהיום כיווני (Variation / Trend_Normal / Trend_DD) ואין **הוכחת-עצירה** — מותרות כניסות **עם-כיוון-ההרחבה בלבד**, לכל המשפחות (גם REV — HTLB/REACTIVE עם-הכיוון כן; נגד — לא). הרפרנס הכיווני = **כיוון-ההרחבה של היום** (שבירת-ה-IB המאושרת), **לא** ה-LSMA-המקומי (K=3) שמתהפך על כל באונס — הראיה: 18:50, פילטר-ההמשך וטא שורט-עם-היום על "sustained UP" רגעי, ובמקביל הלונג-נגד עבר.

**הוכחת-עצירה (המפתח לפתיחת הצד-הנגדי):** ‎v1 (זמין מייד, אותה לוגיקת-acceptance שאומתה היום על שבירת-ה-IB): **2 סגירות-5-דק' רצופות חזרה מעבר לרמת-ההרחבה** (ביום-יורד: מעל IB-low) **או** נעילת-סיווג חדשה. ‎v2 (workstream): גלאי הדאבל-פרינט-transition — ההגדרה הדלטונית המלאה. עד הוכחה — counter=SKIP; אחריה — הצד-הנגדי נפתח לפי הטבלה הרגילה.

**יישום:** מחליף/מכליל את הרחבת-require_with_trend של פריט-1 (REACTIVE/HNS = מקרים פרטיים); נבנה כבדיקה אחת בגייטוויי (עם day-direction מ-S1 + halt-proof-v1), דגל `DAY_DIRECTION_DOCTRINE_V1` default-OFF → SHADOW → הפעלת-Michael. טסטים: יום-כיווני+נגד-בלי-עצירה⇒חסום · עם-כיוון⇒עובר · אחרי-הוכחת-עצירה⇒נגד נפתח · יום-מאוזן⇒אפס-שינוי.

**תוספת (Michael 07-02 ~21:0x — "הוא Trend מובהק אבל מסווג Variation"): מצב-טרנד-בתוך-Variation.** התווית נשארת לפי הכללים המכניים (07-02: IB=42.25 רחב + שיא-בתוך-IB + ext=34.75<1×IB ⇒ Variation תקין), אבל **הניהול עוקב אחרי ההתנהגות**: כשה-one-timeframe נמשך ≥N תקופות-30-דק' אחרי ההרחבה (בוליאני-1TF מה-workstream; N=2 ברירת-מחדל) — ניהול-הראנר (C3) עובר לסגנון-Trend: hold-to-close + trail מבני במקום יעד-2×ext. חוזר לסגנון-Variation אם ה-1TF נשבר. flag-gated יחד עם הדוקטרינה.

## פריט 19 · מסגרת-LIVE: בלם-יומי + קריטריון-מעבר (ממתין למספרי-Michael)

`DAILY_LOSS_HALT_V1`: הפסד-יומי ≥ סף-Michael (הצעה: −$450 = 3 סטופים-מלאים) ⇒ עצירת-כניסות + flatten + נעילה-עד-מחר; נאכף בגייטוויי (daily_pnl חייב לספור demo+live per-mode — היום סופר live בלבד, `on_trade_close:626-634`). קריטריון DEMO→LIVE (הצעה: 5 ימי-דמו רצופים: אפס-תקלות-מכניקה ביומן · ניהול-מלא-תקין · R-נטו≥+2 · בלם-לא-נדרך · פריט-20 בנוי) — נבדק אוטומטית בבדיקת-הבוקר. **המספרים הסופיים = פסיקת-Michael (פתוחה).**

## פריט 20 · Sierra Truth Reconcile — "מה סיארה מחזיקה בפועל" (בלוקר-LIVE; Michael 07-02 ~20:5x)

**קיים:** אירועי-פילים במחירי-אמת + ACKs (מוכח 07-02) + מיפוי-מזהים (I-58). **חסר:** תמונת-מצב-אמת — פוזיציה-נוכחית + פקודות-עובדות מסיארה, מושווית לרשומות שלנו. **הבנייה:** ‎(א) DLL: ייצוא תקופתי `position_state.json` (פוזיציה, כמות, ממוצע, פקודות-חיות עם מזהים) — עבודת-sc_study + Remote-Build (אחרי-שעות בלבד, snapshot לפני) ‎(ב) backend: comparator כל 30ש' — mismatch ⇒ 🔴 התרעה + (ב-LIVE) halt-חדשות ‎(ג) גלאי-סטופ-עירום: עסקה פתוחה בלי פקודת-סטופ עובדת בסיארה ⇒ אזעקה מיידית. בסיס: `CC_SIERRA_FILL_LOOP_2026-07-01.md` + `CC_DEMO_SLOT_RECONCILE_2026-07-01.md`. תנאי-מעבר-ל-LIVE (סעיף ה' בקריטריון).

## פריט 21 · חלון-סיכון-EOD — פסק-Michael 07-02 ~22:50 IL — דגל `EOD_RISK_WINDOW_V1` (default OFF)

**הפסק (מילולית):** "סגירה של עסקאות פתוחות ב-15 הדקות האחרונות של המסחר, וב-45 דקות לפני סיום המסחר לא ייכנסו עסקאות."

**ADAPT — לא לבנות מאפס:** מנוע-Killzone (System 6, `backend/v9/systems/killzone/`) כבר מגדיר אזורי-זמן וכולל `block_last_15min` — אבל **מנותק מנתיב-הירי** (advisory; A4 מראה "killzone:missing"). עץ-B7 ("EOD flatten") = תווית ללא מימוש.

1. **חסם-כניסות (14:15 CT → פעמון 15:00 CT):** אכיפה בגייטוויי דרך gate-הקילזון הקיים — `blocked_by="eod_entry_cutoff"` + רישום ליומן-ההחלטות. חל על S2+S4, כל המצבים.
2. **‏Flatten אוטומטי (14:45 CT):** scheduler (כמו eod_archive) → כל עסקה פתוחה: `close_trade(reason="EOD_FLATTEN")` + סגירה בצד-סיארה לדמו/לייב (מסלול-הסגירה של OPPOSITE_2X; אם אין פקודת-CLOSE ב-DLL — זו תלות בפריט-20, לציין ב-NOT-DONE) + notify-gateway (שחרור-סלוט וספירה — המסלול המתוקן של היום).
3. **קונפיג מפורש-TZ (Rule 4):** `EOD_ENTRY_CUTOFF_MIN=45`, `EOD_FLATTEN_MIN=15`, עוגן=15:00 CT (RTH close, America/Chicago) — לא שעות-קיר ללא-TZ.
4. טסטים: setup ב-14:20 CT ⇒ blocked · ב-14:10 ⇒ עובר · עסקה פתוחה ב-14:45 ⇒ נסגרת+slot-freed+נספרת · יום-רגיל-באמצע ⇒ אפס-שינוי.

## פריט 22 · מנוע אזורי-מימוש (Target-Zones) — Michael 07-02 ~23:0x: "שהמערכת תזהה אזורים לקביעת מימושים חוץ מה-T1"

**הרעיון:** ‏C2/C3 לא נצמדים לרמה בודדת אלא ל**אזור-קונפלואנס** — צביר רמות קרובות = אזור חזק.
1. **איסוף** (בונה על מלאי-פריט-2): IB-edges/center · POC/VAH/VAL היום · prior-POC/VAH/VAL · PDH/PDL · swings · (שלב-ב': HVN/LVN-proxy, spike-edges, gap-edges מה-workstream).
2. **צבירור:** רמות במרחק ≤3T מתמזגות לאזור; **עוצמת-אזור = מספר-הרמות** (משוקלל: prior-POC/PDH כבדים יותר). אזור-חזק = ≥2 רמות.
3. **שימוש במימוש:** C2 = הקצה-הקרוב של האזור-החזק-הבא בכיוון (לא מעבר לו — הכלל מפריט-2) · C3/runner = האזור-החזק-שאחריו; ה-trail מהדק כשנכנסים לאזור-חזק (time-at-extreme ישתלב כאן) · אזור-חלש = הראנר ממשיך דרכו.
4. **תצוגה:** האזורים על ה-marker/צ'ארט + ביומן-ההחלטות (details: אילו רמות מרכיבות כל אזור).
5. טסטים: שתי רמות ב-2T ⇒ אזור אחד עוצמה-2 · C2 נצמד לקצה-הקרוב · רמה בודדת רחוקה ⇒ לא-אזור. flag `TARGET_ZONES_V1` default-OFF; תלוי-בפריט-2 (נבנה אחריו; אם הלילה קצר — ספק+שלד ו-NOT-DONE).

## סדר-בנייה מומלץ + אימות
2 (רזולבר) → 1 (תאים) → 3 (שער-R:R, תלוי ב-2) → 4 (Stop Resolver) → 5 (b4-vol). כל פריט: טסט שנכשל-על-הישן ועובר-על-החדש · אין tautology · פלט גולמי. הפעלת דגלים: Michael בלבד, אחד-אחד, אחרי SHADOW.

## NOT-DONE (למלא בכנות)
- [ ] מה לא הושלם + סיבה
