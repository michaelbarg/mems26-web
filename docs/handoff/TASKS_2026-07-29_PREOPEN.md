# משימות 2026-07-29 — הכנה ליום מסחר-מערכת-בלבד (מייקל)

**הפסיקה של היום:** "לדאוג שהיום יהיו עסקאות של המערכת בלבד … שהמערכת תשפר ותבצע
עסקאות איכותיות ותדע לממש בנקודות." מייקל לא סוחר ידנית היום; בכל סקירה —
**מתעלמים לחלוטין מעסקאות ידניות שלו.**

**הנתון המרכזי מאתמול (28.07):** מהפתיחה — **28 איתותים, 0 עברו את מחסנית-השערים.**
ביקורת-נגד (סיכון 12 נק', חלון שעתיים, מדוגם ייחודי):

| שער | חסך | עלה | פסק |
|---|---|---|---|
| awaiting_release | 8 | 5 | ✅ צדק 8/13 |
| extreme_chase_guard | 5 | 1 | ✅ 5/6 |
| cont_trend_filter | 4 | 0 | ✅ 4/4 |
| eod_entry_cutoff | 3 | 0 | ✅ 3/3 |
| daytype_playbook | 3 | 3 | — 50/50 |
| lsma_flat | 1 | 1 | — |
| **location_gate** | **0** | **2** | 🔴 עלה-בלבד |

**האבחנה:** אף שער אינו שבור קשות — אבל **שבעה שערים ב-AND טורי מכפילים את
שיעורי-החסימה זה בזה ומגיעים לאפס עסקאות.** הבעיה היא הארכיטקטורה, לא שער בודד.

**סוגי-פתיחה (למידה):** ‏27.07 = `OPEN_AUCTION_OUT` → ‏Variation ‏UP (סופי) ·
‏28.07 = `OPEN_DRIVE` → ‏Variation ‏UP (סופי). **בשני הימים הסיווג הסופי תאם את מייקל** —
אבל תוך-יומית הרגל (leg) התנדנדה ‏DOWN↔UP (‏8×DOWN מול 17×UP אתמול). היציבות
התוך-יומית של הכיוון היא הפער, לא הסיווג.

---

## 🔴 cursor-agent — לפי סדר

1. **חוב פתוח:** משימות **20** (קבילות-הפנקס) ו-**21** (סקירה מלאה) מ-28.07 —
   **לא נמסרו**. השלם היום; משימה 21 בוטלה-חלקית: **דלג לגמרי על ניתוח עסקאות
   ידניות** (פסיקת 29.07), התמקד בעסקאות-מערכת ובאיתותים חסומים.
2. **כיול השער היחיד שעלה-בלבד:** `location_gate` ‏0/2 — קרא את הקוד, מצא למה
   שתי חסימותיו אתמול היו שגויות, והצע כיול (או נטרול) עם ראיות.
3. **‏`daytype_playbook` ‏50/50** — פרק לפי סוג-תבנית: איפה הוא מוסיף ואיפה מזיק.
4. **אימות-חוק-5 להדלקות של cowork מ-28.07:** ‏release-gate · margin-sizing ·
   bracket-slot-cap · conftest-isolation · ביטול-auto-flatten. כולן חיות בלייב.

## 🔴 cc-macbook — לפי סדר

1. **יציבות רגל-הכיוון בוריאציה** — ‏`with_extension(DOWN)` נרשם אתמול בעודף
   (‏8 פעמים) כשכל הלחץ היה מעלה; הרגל מתנדנדת תוך-יומית. מצא את מקור הרגל
   ב-`daytype_classifier` (שורה ~135) והוסף היסטרזיס: רגל מתחלפת רק אחרי N
   דגימות עקביות או שינוי-קיצון ממשי. טסטים + סים. **זה משפיע ישירות על
   `RESPONSIVE_WITH_DAY_TREND_V1`.**
2. **‏S6 — שני שיפורים קונקרטיים:**
   א. **אינווריאנט כיסוי-בראקט:** כל עסקת-מערכת פתוחה — ‏sum(stop qty) חייב
   להשוות ל-position qty; פער → ‏CRITICAL מיידי. (הפער הזה עלה כסף אמש.)
   ב. **אוטומציית ביקורת-שערים ב-EOD:** הרץ `scripts/release_gate_review.py`
   אוטומטית בסוף-סשן וכתוב את הפסק ל-ops_log — השערים נותנים דין-וחשבון כל יום.
3. **רישום הסטופ-המבני של release-gate ליומן-ההחלטות** — כרגע ה-hold לא נושא
   את הסטופ שהשער חישב, וביקורת-הנגד נאלצת להניח 12 נק'. שדה אחד.

## cowork-dev (אני)

1. ‏T-30 לפתיחה: ‏verify מלא + flag_guard + מעקב ראשון-ירי end-to-end.
2. ‏EOD: ‏release_gate_review + ביקורת כל-השערים כמו אתמול + דוח למייקל.
3. אימות מסירות cursor/cc לפי חוק-5.

## הצעת-החלטה למייקל (לא מבוצע בלי פסיקה)

**בעיית ה-AND הטורי:** להעביר שערים "רכים" (‏location_gate, ‏lsma_flat,
‏daytype_playbook במקרי-גבול) ממעצור-קשיח ל-**ניקוד** — איתות עובר אם ציון-מצרפי
עובר סף, במקום להיפסל על שער בודד. ‏awaiting_release / extreme_chase / margin
נשארים קשיחים. זה שינוי-סיכון → דורש פסיקה נפרדת שלך.


---

## עדכון 14:5x (cowork ביצע את חלק-קורסור; ממצאים חדשים)

1. **טבלת-השערים מהבוקר בטלה** — היה בה באג-TZ שלי (UTC מול IL). המתוקנת, מאומתת-כניסה-בתוך-בר:
   release-gate **7/0 חסך** · chase 3/1 · cont_trend 3/1 · playbook 1/1 · location_gate לא-הוכרע.
   ההצעה-לניקוד ירדה (מייקל דחה; והנתון המתוקן ממילא לא תומך בה).
2. **🔴 שורש-העל: זיהום-ברים.** ברי-אתמול השתנו בין ערב לבוקר + תפר 31.5 נק' (17:20→17:25).
   הרגל המתנדנדת, האיתותים-על-מחיר-לא-נכון, וה-8 הבלתי-ניתנים-ליישור — כולם תוצר שלו.
   גלאי חדש: `scripts/bar_integrity_check.py` (אתמול 🔴 · היום ✅).
3. **cc — משימה חדשה בראש-הסדר (לפני היסטרזיס-הרגל):** שומר-קליטה דוחה-תפרים —
   בר נכנס שיוצר אי-רציפות >15 נק' מול השכן שלו נדחה/מסומן (`BAR_SEAM_REJECT_V1`, OFF→סים→ON),
   ‏+ חקירת מקור הכתיבה-מחדש הלילית (מי כתב ברים של 07-28 אחרי 18:10). ההיסטרזיס אחריו —
   ייתכן שהרגל תתייצב מעצמה על זרם נקי.
4. פסק-הפנקס (משימה 20) נמסר: `CURSOR_LEDGER_VERDICT_2026-07-29_BY_COWORK.md` —
   קביל מ-07-27 20:20 בלבד (3 עסקאות); 07-13/25 = סים-שתויג-לייב; pnl_sierra ריק בכולן.
