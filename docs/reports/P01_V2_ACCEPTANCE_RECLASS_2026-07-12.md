# P0-1 v2 — reclass-בקבלה בכל רמות-הייחוס (DEV-1) · 2026-07-12

**סטטוס: בנוי, flag-OFF, ממתין לפסיקת-הדלקה של מייקל.** דגל: `S1_ACCEPTANCE_RECLASS_V1`.

## מה נבנה

1. **`level_acceptance()`** (`relative_features.py`) — מבחן-קבלה גנרי לכל רמת-ייחוס:
   ‏≥2 סגירות רצופות מעבר לרמה (+2 טיקים) **וגם** ‏≥8% מנפח-הסשן נסחר מעבר לה
   (אותה מכניקה בדיוק כמו מבחן-ה-IB הקיים). מזהה גם **קבלה-ואז-דחייה** (2 הסגירות
   האחרונות חזרו פנימה) — מה שה-v1 לא ידע להביע.
2. **מיזוג-רפרנסים** (`classifier_core.py`) — ‏accepted_break נבחן מול **PDH/PDL
   (טווח-אתמול=bracket), קצה-VA-אתמול, וקצה-IB** — החיצוני-ביותר מנצח בשם; קבלה
   דו-כיוונית ⇒ אין reclass; שום רפרנס לא מוחזק + הייתה קבלה שנדחתה ⇒
   ‏`failed_break` → תג `failed_breakout` בפלט (הענף ב-classify היה קיים מ-07-08
   וקורא לזה — עכשיו יש מי שמזין אותו).
3. **הרג פיצול-המקורות** (`daytype_classify_routes.py`) — ‏classify_replay בנה ‏feat
   ידנית ופספס בשקט כל פיצ'ר חדש (מדרגות-P1-5, ‏neck-P1-7, ‏open_dir-P0-2,
   ‏accepted_break). עכשיו הוא קורא ל-`classifier_core.classify_session` — נתיב-קוד
   אחד ל-UI/replay/מנוע, כנדרש ב-ACCEPT של P0-1 ("‏UI==gate==engine one value").

## ראיות (Rule 5)

**טסטים** — ‏`tests/v9/regression/test_p01_v2_acceptance_all_refs.py`:
```
10 passed in 0.14s
```
כולל אנטי-טאוטולוגיים: אותו מסלול-מחיר עם נפח-דק נחסם ע"י שער-הנפח; אותם ברים
בדיוק עם דגל-OFF חוזרים ‏FORMING (זהות-בייט); קבלה→חזרה-פנימה מתייגת ‏failed_breakout
בלי קידום; קבלה דו-צדדית לא מקדמת.

**רגרסיה**: ‏127 עוברים; ‏11 נופלים — **זהים לחלוטין לפני ואחרי השינוי** (נבדק עם
‏git stash: ‏`11 failed` בשני הריצים — משפחת פיקסטורות-parity ישנה, כבר בטבלה אצל CC).
‏3 קבצי-הטסט שקוראים ל-classify_replay ישירות: ‏`25 passed`.

**רפליי** (offline, ‏PG, כל דגלי-הדוקטרינה החיים ON):

| יום | דגל-OFF | דגל-ON | ‏EOD |
|-----|---------|--------|------|
| ‏07-08 (גאפ-דאון) | כיוון ראשון ב-09:55 (פרוביזורי-פתיחה) | **DOWN ב-09:35 — בר 2** (‏PDL-acceptance) | ‏Neutral_Extreme — זהה |
| ‏06-29 | ‏Normal@10:25 | ‏NV-UP ב-09:35 (‏PDH), המשך זהה | ‏Normal_Variation — זהה |
| ‏06-30 (+44.75 מגמה שנחסמה) | ‏NV כל היום; ‏Trend רק ב-15:05 | **Trend_Normal-UP ב-10:00** (‏PDH+one_tf, ‏rib-floor waived) | ‏Normal_Variation — זהה |

קריטריון-ה-ACCEPT מהדוקטרינה: היפוך ≤2 ברים מסגירת-הקבלה ✓ (07-08: אותו בר);
רגרסיות 06-29/06-30 ללא שינוי-EOD ✓; נתיב-אחד UI==engine ✓.

## הדלקה (פסיקת מייקל)

`S1_ACCEPTANCE_RECLASS_V1=1` ב-.env + ‏RULED_FLAGS + ריסטארט. המשמעות המסחרית:
ביום-פריצה המערכת מקבלת כיוון דקות-עד-שעות מוקדם יותר (06-30: ‏Trend ב-10:00 במקום
15:05) → שערי-התבניות (family-gate/playbook) מיישרים כיוון מוקדם. הסיכון: ‏reclass
מוקדם עלול להתהפך (הוגן — ‏failed_breakout מתייג ומחזיר).
