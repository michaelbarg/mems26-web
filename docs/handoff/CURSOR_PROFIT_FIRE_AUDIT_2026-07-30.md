# Cursor — ביקורת עצמאית: "המערכת מזהה עסקאות רווחיות וגם יורה" (מייקל 30.07 בוקר)

אתה המבקר העצמאי. **אסור לשנות קוד/קונפיג/דגלים** — תוצר = דוח-ביקורת + המלצות
מדורגות-לפי-כסף, כל אחת עם ראיה גולמית + הטסט שיוכיח אותה. חוק-5: פקודה+פלט, לא
טענות. עבוד על ה-repo העדכני (`git pull`) — קרא קודם:
`docs/reports/CC_NIGHT_2026-07-29_REPORT.md` · `docs/reports/E2E_FIRE_PROOF_2026-07-2{7,9}.md`
· ‏`docs/handoff/LIVE_CHANNEL.md` (ערכי 29-30.07) · ‏journal: `~/SierraChart_Data/v9_export/gateway_decisions.jsonl`.

## הקשר — שני מצבי-הכשל שאתה שופט

המטרה העסקית (מייקל): עסקאות רווחיות כל יום ⇒ הכנסה חודשית. לכן שני צדדים, שקולים:
- **מצב-כשל א' — לא יורה:** ‏07-29: ‏60 החלטות-שער/0 עברו + ‏12 זיהויי-תבנית שמתו
  לפני-הגייטוויי; ‏29 מנצחות-מאומתות נחסמו.
- **מצב-כשל ב' — יורה ומפסיד:** ‏07-27 בבייסליין: ‏3 עסקאות-לייב ⇒ **‎−$90**.
  ירי בלי איכות-כניסה = גרוע מאי-ירי.

## המשימות

### 1 · אימות-נגד של אבחנת-ה-TZ (הבסיס לכל השאר)
‏CC קבע: צ'ארט = ‏America/Chicago (לא NY כמתועד), הברידג' מתקן נכון
(‏V9_CHART_TZ), ‏TS_WHOLE_HOUR_NORMALIZE_V1 היה תיקון-כפול-משכתב (כובה 30.07 בוקר).
אמת עצמאית: (א) בר-ייצוא טרי ⇒ ‏ts יחיד נכון בכל הצנרת עד ה-DB; (ב) אפס כתיבות
מוזזות-שעה מאז הכיבוי (שאילתת-continuity על היום); (ג) חפש תיקוני-ts נוספים
ששרדו (`WOODIES_TS_HOUR_FIX` וכל grep על tz/hour/shift בנתיב-הקליטה) — כל אחד
שנשאר = המלצת-מחיקה עם ראיה.

### 2 · צד-ההפסד: נתיחת שלוש עסקאות-07-27 (−$90)
לכל אחת: כניסה/סטופ/יעדים מול ברי-האמת — האם הכניסה מוקדמת-בזון (המחלה ש-release-gate
נבנה לתקן)? סטופ-בתוך-רעש? יעד קרוב-מדי? האם השערים-החדשים (trend_bypass/מסלול-V)
היו משנים אותן, לטובה או לרעה? זו הבדיקה שקובעת אם הכיול החדש מרוויח כסף או רק יורה יותר.

### 3 · סקירת כל הכיולים החדשים מול הדאטה
‏`RELEASE_TREND_BYPASS_PTS=15` · ‏`DAYTYPE_PLAYBOOK_MIN_CONF=0.4` · ‏chase ‏6נק' ·
‏lsma_flat ‏0.25 (חסם לונג-29נק' על שיפוע 0.213) · ‏cooldown. על כל יומן-ההחלטות
הזמין: לכל סף — כמה חסך/עלה בכל ערך-סף חלופי (טבלת-רגישות, לא ערך-בודד). המלצה
רק איפה שהדאטה חד-משמעית; אחרת "אין ראיה מספקת" (אל תמציא).

### 4 · חוסמי-S2 הפנימיים (P5 של CC — טרם בוצע)
בקר את ההצעות לפני שנבנות: (א) ‏R:R-breakout (reward=measured-move במקום מרחק-לשיא-ישן)
— בדוק על כל ה-REJECTED ביומן: כמה היו זוכים/מפסידים תחת המודל המוצע? (ב)
‏auth-table בביטחון-נמוך ⇒ ‏REDUCED-2 — סמלץ על 07-29: כמה עסקאות היה משחרר ומה
התוצאה שלהן? תן GO/NO-GO מנומק לכל אחת.

### 5 · פערי-זיהוי פתוחים
‏Neutral-round-trip (NOT-DONE של CC) + ‏DD_BIMODAL_RELAX_V1 (בנוי-OFF): הרץ את שניהם
על 07-28+07-29 (‏replay, ‏flag בסביבת-טסט בלבד!) ותן המלצת-הדלקה מגובת-פלט.

## תוצר

`docs/reports/CURSOR_PROFIT_FIRE_AUDIT_2026-07-30.md`: (א) טבלת-המלצות מדורגת לפי
‏$-מושפע — המלצה · ראיה · טסט-מוכיח · סיכון; (ב) פסקת-שורה-תחתונה: "עם ההמלצות,
ביום כמו X המערכת הייתה מרוויחה Y" מגובה-חישוב; (ג) NOT-VERIFIED מפורש לכל מה
שלא הספקת. עדכן LIVE_CHANNEL בסיום. **אסור:** שינוי קוד · הדלקת דגלים · op=EXIT ·
נגיעה בחשבון. מייקל פוסק על כל המלצה.
