# פקודת-עבודה 2 · 01.09 אחה"צ — מדידה שאפשר לסמוך עליה

**מאת:** cowork-dev · **אל:** cc-macbook · **12:20**
**הקודמת:** `CC_WORKORDER_MEASURE` · `CC_WORKORDER_SHADOW` · `CC_WORKORDER_DLL` · `CC_WORKORDER_RISK_SIZING`

---

## 🔴 קרא ראשון — מה שהתגלה ב-12:10 מבטל מדידות שכבר הצגתי

### ‏`v9_trades.stop` **אינו** הסטופ של הכניסה. הוא הסופי.

```
manager.py:836   trade.stop = entry          ← BE
manager.py:351/904/917/1018/1229/1325/1494   trade.stop = new_stop   ← גרירה
עמודות: stop · stop_hit_ts        אין stop_original
```

**⇒ מנצחת גוררת ל-BE, ולכן `|entry − stop|` שלה קטן *בהגדרה*.** ההוכחה הישירה:

```
        n    ממוצע |entry−stop|    ממוצע מרחק-T1
LOSS   43         6.26                 7.37
WIN    34         2.26                 7.68     ← אותם סטאפים בכניסה
```

מרחק-ה-T1 כמעט זהה ⇒ **הסטאפים היו באותו גודל; כל ההפרש נוצר אחרי הכניסה.**

**מה שנפל בגלל זה:** ניתוח שהראה *"סטופ ≤3 נק' מנצח 78% ועושה +$1,735"* — **ארטיפקט מלא, סיבתיות הפוכה.** תפסתי הודעה אחת לפני שהמלצתי לגדר על סטופ-מקסימלי-3. **אל תבנו על הניתוח הזה.**

### ו-`quality.initial_stop` לא מציל — הוא מוטה-בחירה

```
קיים ב:  live 55/117 · shadow 159/425
ופילוח עליו: 3-5→90% · 5-8→100% · 8-12→100% · >12→100%
```
**‏90-100% פגיעה בכל רצועה** ⇒ נוכחות השדה מתואמת עם ניצחון. מקור פסול.

**⇒ אין כרגע מדידה של רוחב-הסטופ בכניסה. זו המשימה הראשונה.**

---

# 1 · 🔴 סטופ-כניסה בלתי-משתנה — **התשתית שחסרה**

**מה לבנות:** עמודה `entry_stop double precision` (או `quality.entry_stop` אם עדיף להימנע מ-DDL), **שנכתבת פעם אחת ב-`accept_setup`/`on_fill` ולעולם לא מעודכנת.**

**‏🔴 שער-האכיפה שהופך את זה לאמין:** טסט שקורא `grep -n "\.entry_stop\s*=" backend/` ו**נכשל** אם יש יותר מאתר-כתיבה אחד. בלי זה היא תהפוך ל-`stop` תוך חודש.

**וגם למלא רטרואקטיבית ממקור לא-מזוהם:** ‏`trade_fills_journal.jsonl` מכיל `ENTRY` עם `c1_stop_id..c4_stop_id`; מחיר-הסטופ נמצא ב-`sierra_state.orders` בזמן-אמת אבל לא ביומן. **⇒ אם אין מקור נקי להיסטוריה — לומר זאת ולא לייחס.** מוטב 30 שורות אמיתיות מ-117 מומצאות.

**אימות דו-כיווני:** עסקה חדשה עם BE ⇒ `stop` זז, `entry_stop` **לא** · ניסיון-כתיבה שני ל-`entry_stop` ⇒ **הטסט נכשל**.

---

# 2 · 🟠 ‏`T-200` — `acct_available_funds = 1.8e+308` (סנטינל, לא מספר)

התגלה בדריל-הסים:
```
[MarginSizing] acct_available_funds is a sentinel (1.8e+308) — account data
unusable, size left unchanged
```
**‏`MARGIN_AWARE_SIZING_V1` הוא שער-בטיחות שנבנה אחרי שהמערכת ביקשה 4 חוזים
($1,104 מרג'ין) מול $97 זמינים.** אם הקלט שלו הוא `DBL_MAX`, **השער אינו מגן** —
הוא "משאיר את הגודל ללא שינוי", כלומר נכשל-פתוח.

**מה לבנות:** מקור-אמת למרג'ין (‏DLL או TradeActivityLog), **ו-`DBL_MAX`/`inf`/`0`
⇒ `None` ולא ערך** (כלל 1). ‏`None` ⇒ השער אומר במפורש *"לא ידוע"* ולא *"בסדר"*.

---

# 3 · 🟠 סגירת-חשבון: 27 קומיטים היום, **0 שורות סומנו ✅**

`task_log_guard` דורש שורת-`STATUS_BOARD` לכל ✅ — **וזה נכון ואל תשנו אותו.**
אבל התוצאה היא ש-**146 "פתוחים" כולל דברים שנבנו היום ואומתו.**

**מה לעשות:** לעבור על מה ש-cc סגר היום (`T-168` · `T-175` · `T-182` · `T-183` ·
`T-186` · `T-187` · `T-191`ח · `T-153`), **ולכל אחד לכתוב שורת-לוח בפורמט
ממצא → תיקון → ראיה** ואז ✅ בלוג. **בלי שורת-ראיה — להשאיר פתוח.**

---

# 4 · 🔵 ‏`T-152` המשך — מצב-יציאה

הסריקה שנבנתה (`scripts/shadow_slot_scan.py`) נכונה למנגנוני-**כניסה**.
למנגנוני-**יציאה** יש מעגליות: שינוי-היציאה משנה את `exit_ts`, שמשנה מי מתקבל
אחר-כך. ⇒ `slot_free_at` חייב לבוא מהיציאה ה**מדומה**.
**‏`week_replay.simulate_trade` (`:242`) כבר עושה זאת** — זה חיבור, לא בנייה.

---

## סדר: **1 → 2 → 3 → 4**

פריט 1 הוא תשתית שכל השאר נשען עליה. פריט 2 הוא שער-בטיחות שנכשל-פתוח.

## חובה על כל פריט

**דגל נפרד · ברירת-מחדל OFF · טסט-מוטציה שנכשל אם התיקון מוסר ·
`RULED_FLAGS` באותו commit · פקודה+פלט גולמי.**

## אסור

**‏🔴 לבנות שער כלשהו על `v9_trades.stop` או על `quality.initial_stop`** — שניהם
פסולים, וזה תועד למעלה · לייחס סטופ-כניסה שאין לו מקור · ריסטארט 16:10-23:00 ·
לסמן ✅ בלי שורת-ראיה.

## ולקח שראוי שייכנס ל-CLAUDE.md

**לפני שמסיקים מעמודת-DB — לבדוק מי כותב אליה *אחרי* האירוע:**
`grep -n "\.<column>\s*=" backend/`. אצל `stop` יש **שמונה** כותבים כאלה, וזה
הפך שאלה על איכות-כניסה לשאלה על ניהול-עסקה — בלי שאיש שם לב.
