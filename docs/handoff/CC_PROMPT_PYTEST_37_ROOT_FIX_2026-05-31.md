# מגה-פרומפט CC — תיקון 37 כשלי pytest מהשורש

> להדבקה ב-Claude Code. **קרא קודם `CLAUDE.md` ו-`.cursor/rules/mems26-pre-live-protocol.mdc`.**
> מטרה: pytest **all-green** ע"י תיקון **שורש** של 37 הכשלים הקיימים — לא ע"י
> החלשת/דילוג טסטים ולא ע"י שינוי לוגיקת-מסחר כדי "לעבור".
>
> ⚠️ עיקרון‑על: **diagnose-first.** חלק מהכשלים עלולים לחשוף **באג אמיתי בלוגיקת
> מסחר** (trade_manager, time_stop). אסור לשנות התנהגות מסחר כדי לרצות טסט בלי
> אישור Michael — strategic stop.

---

## רקע
אחרי תיקון 1.12 נותרו **37 כשלים קיימים** (אומתו `git stash` כלא-קשורים לשינויים
האחרונים). אשכולות שזוהו: trade_manager/bar_level_detector · five_min/day_type_wiring ·
tpo/session_id · blocker_sweep/time_stop. ייתכנו נוספים.

## שלב 1 · Triage מלא (אבחון, לא תיקון)
1. רוץ `python3 -m pytest -q` והדבק את רשימת 37 הכשלים המלאה (raw).
2. קבץ ל**אשכולות לפי שורש משותף** (לא לפי קובץ). לכל אשכול: traceback מייצג + שורש.
3. **סווג כל אשכול** לאחד משלושה:
   - **(A) טסט מיושן** — הקוד נכון, הטסט בודק התנהגות ישנה/שגויה. → לעדכן את הטסט
     להתנהגות הנכונה הנוכחית (עם הסבר למה ההתנהגות הנוכחית נכונה).
   - **(B) פער fixture/infra** — כמו 1.12 (טבלה/fixture/seed חסר ב-scope). → לתקן
     fixture/conftest, קוד טסטים בלבד.
   - **(C) באג אמיתי בקוד** — הטסט נכון, הקוד שבור. אם זה **לוגיקת מסחר/risk** →
     **לא לתקן עכשיו**: לתעד שורש + הצעת תיקון ו**לעצור לאישור Michael**. אם זה באג
     לא-מסחרי ברור (למשל None-guard, import) → לתקן עם רגרסיה.

## שלב 2 · תיקון הבטוחים בלבד
- תקן (A) טסטים מיושנים + (B) fixtures + (C) באגים לא-מסחריים.
- לכל תיקון: רגרסיה שמראה את הכשל לפני וה-PASS אחרי (raw). אסור לגעת בקוד שמשנה
  התנהגות מסחר/סיווג/ירי כדי לעבור.

## שלב 3 · לוגיקת מסחר → עצירה
- אשכולות שסווגו (C) לוגיקת-מסחר (trade_manager pnl, time_stop, day_type_wiring,
  bar_level_detector): **לתעד שורש + הצעת תיקון + השפעה אפשרית** ולהשאיר את הטסט
  failing עם `xfail`/הערה זמנית מסומנת — **לא** לשנות את הלוגיקה. להציג ל-Michael.

## Forbidden
- להחליש/למחוק/לדלג טסט רק כדי לקבל ירוק.
- לשנות לוגיקת מסחר/risk/sizing/סיווג כדי שטסט יעבור (בלי אישור).
- לגעת ב-order routing / sc_study / polling.
- "all green" מזויף — עדיף 37→N עם N מתועדים מאשר ירוק שמסתיר באג.

## תוצרים
דוח `docs/reports/PYTEST_37_ROOT_FIX_2026-05-31.md`:
- טבלת אשכולות: שורש · סיווג (A/B/C) · פעולה · raw before/after.
- כמה תוקנו, כמה נותרו, ולמה (כל נותר = (C) מסחר עם הצעה, ממתין ל-Michael).
- פלט `pytest -q` סופי.
דוח אוטומטי, פורמט קבוע.

## שער
תקן את כל ה-(A)+(B)+(C-לא-מסחרי). עצור על (C-מסחר) ודווח — אלה דורשים אישור Michael
לפני שנוגעים בלוגיקה.
