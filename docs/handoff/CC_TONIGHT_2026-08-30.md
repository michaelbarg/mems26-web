# פקודת-עבודה · לילה 30.08 → פתיחה 31.08

**מאת:** cowork-dev · **אל:** cc-macbook · **תאריך:** 2026-08-30
**פסיקת-מייקל:** *"תוציא עכשיו שיבוצע."*
**יום ראשון, השוק סגור.** בנייה חופשית. **`.env` וריסטארט = שער cowork+מייקל בבוקר.**

---

# משימה 1 · 🔴🔴 T-160 — הספרים מזכים רווח על עסקאות-רפאים

**זה קודם לכל דבר אחר. כל עוד הוא פתוח, כל החלטה מבוססת על מספר שאינו קיים.**

```sql
-- mode='live', 20 יום אחרונים:
הכל:              n=46   נטו = +$360.00
בלי phantom:      n=35   נטו =  -$46.25
phantom_reconcile: n=11  exit_price ריק = 11/11   +$406.25
```

**‏11 מתוך 11 עסקאות-הפאנטום חסרות מחיר-יציאה — וכולן מנצחות.**
‏`sierra_position_reconciler.py:881` קורא `tm.close_trade(tid, reason="phantom_reconcile")`
**בלי `exit_price`**, ו-`manager.py:1658` נופל למחיר-היעד. כלומר: סיירה אמרה
"הפוזיציה לא קיימת", והספרים זיכו רווח-יעד מלא.

**‏והתופעה רחבה מהפאנטום:** `T2_HIT` 3/3 · `T3_HIT` 1/1 · `MAE_SCRATCH` 7/7 ·
`SIERRA_FLAT` 3/3 — כולם בלי `exit_price`.

## מה לבנות

1. **`PNL_REQUIRES_EXIT_PRICE_V1`** — סגירה בלי `exit_price` **לא מייצרת P&L**.
   הערך הוא `NULL` + `pnl_status='UNPRICED'`, לא מספר. **כלל-1: כישלון-כן > ערך-סינתטי.**
2. **‏`phantom_reconcile` חייב מחיר** — מהמקור הנכון: יומן-המילויים, או
   `sierra_state.json` בסגירה, או **`NULL` בכנות** אם אין. **לא מחיר-היעד.**
3. **‏backfill היסטורי — לא לגעת בשורות.** להוסיף עמודה `pnl_trusted BOOLEAN`
   ולסמן `false` על 11 הפאנטומים + כל שורה חסרת-`exit_price`.
   **‏snapshot + `lock_timeout='3s'` + dry-run.**
4. **דוח-אמת:** סקריפט שמדפיס נטו **עם ובלי** שורות לא-נאמנות. זה המספר שנצטט מכאן.

**‏🔴 אסור:** למחוק שורות · לשנות `pnl_usd` קיים · לנחש מחיר.

---

# משימה 2 · 🔴 T-152 + T-153 — בלעדיהם יום-הצל של מחר ייצא ריק

**‏T-153:** אין טבלת-ליגר מאוחדת. **‏T-152:** מדידת-§D על הצל **מוטה מבנית** —
לצל אין הגבלת-סלוטים, ולכן הוא מנפח כל מנגנון שנמדד עליו.

**‏⚠️ זה חוסם את שמונת הדגלים שבצל:** `S1_DAY_DIRECTION_V1` · `APP_STATE_ROOT_FIX_V1` ·
`S1_STRUCTURAL_BINARY_V1` · `S2_DELTA_DBL_V1` · `TARGET_MIN_SPACING_V1` ·
`FAILED_BREAK_VA_V1` · `S2_CVD_DETECTION_V1` · `TREND_STEP_ENTRY_V1`.
**אף אחד מהם לא יכול לעלות** כל עוד המדידה מנופחת.

## מה לבנות
1. **תצוגת-ליגר מאוחדת** מעל שלוש הטבלאות הקיימות — **‏VIEW, לא טבלה רביעית.**
   מקור-אמת שלישי הוא תנאי-עצירה בתוכנית.
2. **‏§D מודע-סלוטים:** מדידה על הצל חייבת **לדמות את מגבלת-הסלוטים של הלייב**.
   בלי זה כל דלתא מנופחת. **תן את גורם-הניפוח שמדדת** — כמה עסקאות-צל היו נחסמות
   תחת מגבלת-סלוט אמיתית.
3. אימות: להריץ §D על מנגנון **ידוע** לפני ואחרי מודעות-הסלוטים ולהראות את הפער.

---

# משימה 3 · באג-ה-TZ של TPO — **עדיין לא בוצע**

**נמדד עכשיו:** `728 מ-729 שורות` עם `created_at > ts`, פיגור ממוצע **182 דק'**,
ו-**אפס** עמודות `market_ts`/`available_at`.

הכותב מייצר מחרוזת-UTC **נאיבית** ו-PG מתייג ‎+03. אימות אמפירי:
`ts=14:30+03` ב-27.08 מחזיק `ib=7730.5/7702.75` = קיצוני 09:30–10:25 ET.
אחרי `+3h` הבּיָשנוּת החציונית **15 דק'**.

**‏🔴 דחוף במיוחד כי `DALTON_EDGE=live` כבר רץ על הקלט הזה.**

1. הכותב → `timestamptz` מודע · 2. `market_ts` + `available_at` מפורשים ·
3. dry-run על ההיסטוריה, snapshot + `lock_timeout` · 4. טסט שנכשל על מחרוזת נאיבית.

---

# משימה 4 · `lib.atr_from` — באג-שמות שמחכה לקורבן הבא

המחקר של היום מצא: `lib.atr_from` מחשב **ATR יומי** (טווח-יום + פער-לילה) = **91.18 נק'**,
ונצרך בקוד בשם `ATR14` כאילו הוא של ברי-5-דקות. **גורם 12.7.** ‏ATR אמיתי = **7.20 נק'**
(אומת מול `v9_bars_5min_woodies`, **0/45 אי-התאמות**).

זו אותה מחלקה של **כלל-4 ב-CLAUDE.md** (עמימות-יחידה בקלט-ספק).
**לתקן: יחידה בשם** (`atr_daily_from` / `atr_5min_from`) · לאתר כל צרכן ולוודא
שהוא מקבל את היחידה הנכונה · טסט שנכשל על ערבוב-יחידות.

**‏⚠️ ייתכן שצרכנים בייצור השתמשו ב-ATR מנופח פי-12.7.** לספור אותם ולדווח —
**זה עלול לשנות ספי-סטופ ותקרות-סיכון חיים.**

---

# משימה 5 · T-155 — `_position_reference_price` בלי killswitch

רץ בייצור (`manager.py:792` · `bar_level_detector`) **בלי דגל ובלי מעצור.**
**לעטוף בדגל** `POSITION_REF_PRICE_V1` ברירת-מחדל **ON** (שימור התנהגות) —
כך שאם הוא טועה, אפשר לכבות בלי deploy.

---

## סדר

| # | משימה | למה |
|---|---|---|
| 1 | **T-160** | בלעדיו כל מספר שנצטט הוא בדיה |
| 2 | **T-152/153** | בלעדיהם יום-הצל ריק ו-8 דגלים תקועים |
| 3 | **TZ של TPO** | `DALTON_EDGE` כבר חי על הקלט הזה |
| 4 | **`atr_from`** | ייתכן שספי-סיכון חיים מנופחים פי-12.7 |
| 5 | **T-155** | מעצור לרכיב שרץ בלי אחד |

## אסור

`.env` · ריסטארט · `op=EXIT` · לגעת בגודל/סולם · **טבלה רביעית לליגר** ·
למחוק/לשנות שורות-P&L קיימות · לנחש מחיר-יציאה · לתקן היסטוריה בלי
snapshot + `lock_timeout` + dry-run · להדליק דגל.

## מסירה

`docs/reports/CC_TONIGHT_2026-08-30.md` — פר-משימה: `file:line` · פקודה+פלט גולמי ·
טסט-מוטציה · NOT-VERIFIED מפורש. LOG ב-`LIVE_CHANNEL`. `commit`+`push`.
**ואז עצור** — כל הדלקה היא שער cowork+מייקל בבוקר.
