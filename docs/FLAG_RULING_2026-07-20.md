# הכרעת-דגלים + מקרא למייקל — מה חייב להיות פעיל כדי שהמערכת תסחר נכון
**2026-07-20 · אומת ע"י `flag_guard.py` (PASS, 101/101 מוסדרים = `.env`) · המקור-המלא: `docs/FLAG_INDEX.md` (175 דגלים)**

> **הבהרת §0:** cursor הריץ טסטים **בלי `.env`** (ברירות-מחדל-קוד) → מערכת פיקטיבית → "10 passed".
> cowork הריץ **עם `.env`** (הדגלים החיים) → 5 נכשלו (סדר-שערים שונה). **כל הטסטים חייבים לטעון את הסט
> המוסדר למטה** — אחרת בודקים מערכת שלא קיימת. זה השורש של הפער, לא באג-מסחר.

## ✅ קבוצה 1 — לייב (חובה ON, אחרת אין מסחר כסף-אמת)
| דגל | מצב | למה |
|---|---|---|
| `MEMS26_MODE` | **live** | מצב-חי (לא סים) |
| `LIVE_EXECUTION_V1` | **1** | שולח פקודות אמת ל-Sierra |
| `LIVE_TRADING_ARMED` | **1** | חימוש — אתה מחמש, לא cowork |
| `is_sim` | **0** (חובה) | כלל-סוחר-יחיד: iMac חייב סים לפני חימוש MacBook |

## ✅ קבוצה 2 — נכונות Dalton (תיקוני-היום, חובה ON)
| דגל | מצב | למה |
|---|---|---|
| `STRUCTURAL_STOP_ORIGIN_V1` | **1** ✅נצרך | סטופ על קצה-המבנה, לא מרחק-ATR (תיקון #420) |
| `STOP_WIDEN_TO_STRUCTURE_V1` | **1** ✅נצרך | מקבל סטופ רחב-מ-ATR במקום לדחות |
| `REQUIRE_WITH_TREND_DAY_DIRECTION_V1` | **1** ✅נצרך | כיוון מ**סוג-היום**, לא ממומנטום רגעי |
| ⚠️ `STOP_WINDOW_COMPLETED_V1` | **1** — אבל **"not built" באינדקס** | **cursor לאמת: נצרך או מת-חיווט?** אם אינרטי — או לבנות או להוריד מ-`.env` (לא להשאיר אשליה) |

## קבוצה 3 — שערי סוג-יום (המצב המוכרע — הכבויים נכבו בכוונה)
| דגל | מצב | למה כבוי |
|---|---|---|
| `DAYTYPE_PLAYBOOK` | **1** ON | הפלייבוק (מקור-יחיד לכיוון-פר-תבנית) פעיל |
| `DAYTYPE_LOCATION_GATE` | **0** | חסם על מיקום-שבור → כיבינו היום |
| `DAYTYPE_POSITION_GATE` | **0** | כנ"ל |
| `S2_DETECTION_LIVE_DAYTYPE_V1` | **0** | קרא day_type=null → חסם כל ירי S2 |
| `S4_HONEST_DAYTYPE_FALLBACK_V1` | **0** | יצר dead-zone בוקר (null לפני נעילת-IB) |

## קבוצה 4 — גבולות-סיכון (גב-בטיחות)
| דגל | מצב | למה |
|---|---|---|
| `RISK_CONSECUTIVE_LOSS_LIMIT` | **0** (כבוי) | החוק שעצר את היום על 2 הפסדי-בוקר; הגב האמיתי = ה-cap |
| `RISK_DAILY_LOSS_CAP` | **800** | עצירת-יום על −$800 (הגב האמיתי) |
| `RISK_CUTOFF_HOUR_ET` | **15** | אין כניסה חדשה אחרי 15:00 ET |
| `RISK_MAX_TRADES_DAY` | **999** | למעשה ללא הגבלת-מספר |

## 🔴 קבוצה 5 — שבור, חובה שיישאר OFF (אל תדליק)
| דגל | מצב | למה |
|---|---|---|
| `STALL_EXIT` / `OPPOSITE_EXIT_V1` | OFF | נתיב op=EXIT שבור (r=-1) — עד EXIT-v2 |
| `LAYER0_CHOP_GATE` / `S2_CHOPPINESS_GATE` | OFF | שערי-chop מבוטלים (פסיקת 06-08) |
| `S2_REQUIRE_COT_AMT` | OFF | S2⟂S3 — COT/AMT לא נדרש |

## קבוצה 6 — פתוח / למחר (דורש הכרעתך)
| דגל | מצב | הכרעה נדרשת |
|---|---|---|
| `IB_BREAK_ANY_EXPANSION_V1` | **0** | **להדליק מחר** — מסווג-יום-אוטומטי, מייתר את ה-override הידני (שפג בחצות) |
| `ORPHAN_AUTO_STOP_V1` | **0** | **לולאת-האורפן** — חסום על is_sim=0. זו התקלה-המעגלית #1. להכריע: ריפוי-אוטו או חסימה-קשיחה |
| `CONT_TREND_FILTER` | **1** | דוקטרינה: מומנטום-רגעי מול כיוון-יום (האם שורט-עם-היום יורה תוך באונס) |
| `SYSTEM6_AUTOCORRECT` | **protective** | מצב-הגנתי בלבד (מותר, פסיקת 07-15) |

---
**סיכום:** הקבוצות 1-4 = מה שחייב להיות כדי שהמערכת תסחר נכון, וכולן במצב הנכון (flag_guard PASS). קבוצה 5 =
אל-תיגע. קבוצה 6 = 4 הכרעות פתוחות שלך (מודגש = דחוף למחר). **פעולה מיידית:** cursor מאמת את
`STOP_WINDOW_COMPLETED_V1` (אינרטי?) ומריץ מחדש את טסטי-§0 **עם הסט הזה טעון**.
