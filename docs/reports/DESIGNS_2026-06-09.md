# MEMS26 · טיוטות-עיצוב — 2026-06-09

לכל בעיה מאומתת היום: שורש · השינוי המוצע (קובץ/שכבה) · regression שיוכיח · סיווג **trading-logic (→אישור Michael)** או **display/safe**. **לא ממומש — מוכן-להפיכה-לפרומפט-CC.** מקור-אמת מסומן להצלבה מול Sierra v9_export.

מקרא סיווג: 🟢 **display/safe** (תצוגה/דיווח/דיאגנוסטיקה, לא בנתיב-ירי) · 🔴 **trading-logic** (משנה מה/מתי יורה או risk — דורש strategic-stop + אישור Michael).

---

## D1 · I-22 — `pnl_r` מנופח ×10 (🟢 display/safe)

**שורש (מאומת raw):** R פר-חוזה מחושב `pnl_usd ÷ $1.25` (ערך-טיק = 0.25pt×$5) במקום `pnl_usd ÷ risk_$` (מרחק-הסטופ פר-חוזה). הוכחה id=20: C1 `$17.5 ÷ 14R = $1.25` (טיק), בעוד risk אמיתי = 2.5pt×$5 = `$12.50` ⇒ ניפוח ×10. סך-העסקה `pnl_r=233` מול ≈+15.5R אמיתי (blended) / +42.4R על הרץ.

**השינוי המוצע (שכבה: trades/PnL-calc):** באתר חישוב ה-`pnl_r` (נתיב `v9_trades` write + serialization ב-`/trades/recent`) — להחליף את המכנה מ-`tick_value` ל-`risk_per_contract_$ = |entry − stop_initial| × point_value`. לחשב פר-חוזה ב-`contracts_pnl[*].pnl_r` ולסכום נכון ל-`pnl_r` הכולל (סכום R פר-חוזה, או R-blended מוגדר-מפורש). **לאתר את הקובץ דרך `SYSTEM_INDEX.md`** (חישוב-R) לפני נגיעה — לא מהזיכרון.

**regression שיוכיח:** טסט עם trade קבוע — entry 7489.25, stop_initial 7491.75 (risk 2.5pt), exit 7383.25 ⇒ `assert pnl_r ≈ 42.4` (לא 233); וכן C1 exit 7485.75 ⇒ `assert contract.pnl_r ≈ 1.4` (לא 14). anti-tautological: לכלול גם trade עם risk≠2.5pt כדי לוודא שהמכנה הוא risk ולא קבוע.

**סיווג:** 🟢 **display/safe** — `pnl_r` שדה-דיווח, אינו gate בנתיב-ירי (אומת: אף gate לא קורא pnl_r). אך הוא **חוסם את ה-EOD-counterfactual** ⇒ עדיפות גבוהה.
**מקור-אמת ל-CC:** הצלב entry/stop_initial/exit-fills של id=20 מול `risk_$` פר-חוזה. (עיצוב קודם: `DESIGNS_2026-06-08.md`.)

---

## D2 · I-3 — ZLR/S4 לא יורה: target מנוון / היעדר טבלת stop/target (🔴 trading-logic)

**שורש (מאומת על 3 signals היום):** ZLR מגיע ל-A7 (A1–A6 PASS) אך נופל ב-`A7 "missing fire_setup"`: ה-`targets_stop` מייצר **T1 מנוון של 1pt** מול stop 17–37pt ⇒ R:R 0.03–0.06 ⇒ `r_t1_gate`/`exit_rules.ready_to_route` נכשלים ⇒ `fire_setup` לא נבנה ⇒ **ZLR לעולם לא יורה**. אותו דפוס ב-HFE (R:R 0.11–0.31). חסימת ה-R:R **נכונה** בהינתן ה-target המנוון — ה**שורש** הוא היעדר טבלת stop/target אמיתית.

**השינוי המוצע (שכבה: S4 targets_stop / config):** להזין **טבלת stop/target פר-תבנית×day-type** (YAML-tunable, חופף ל-project `stop/target placement table` של Michael) ל-builder של `fire_setup`. ה-T1/T2/stop ייגזרו מהטבלה (לפי pattern×day_type×anchor) במקום ה-1pt המנוון. **קריטי — wire-the-full-pipeline (לקח 5-flag):** הטבלה חייבת להגיע ל**כל** הענפים הצורכים — `targets_stop.r_t1_gate`, `targets_stop.targets`, `exit_rules.ready_to_route`, וה-sizing — בלי dead-wiring חלקי.

**regression שיוכיח:** signal ZLR קבוע (entry 7302, day_type=Trend_Normal) → ב-counterfactual של היום עם targets שפויים (T1=1R, T2=2R) הגיע T1 ⇒ `assert fire_setup is not None` ו-`assert R:R ≥ 1.0`. anti-tautological: signal עם targets מנוונים (1pt) חייב **עדיין** להיחסם (לוודא שה-R:R-gate לא בוטל, רק ה-target תוקן).

**סיווג:** 🔴 **trading-logic — דורש strategic-stop + אישור Michael.** משנה מה יורה ב-S4 (פותח את ZLR/TLB/TT/וכו' לירי). תלוי בהגדרת הטבלה ע"י Michael (ראה project `stop/target placement table`). **לא לממש בלי אישור + הטבלה.**
**מקור-אמת ל-CC:** האם ה-target המנוון (1pt) מקורו ב-builder הבקנד או ב-Sierra export? הצלב מול `~/SierraChart_Data/v9_export/`.

---

## D3 · I-18 — ts עתידי (06-10) בערוץ woodies_5min/bars5min (🟢 display/safe, נוגע ב-data-integrity)

**שורש (אומת היום ברמת DATA):** הבר האחרון ב-`/api/v9/chart/bars5min` נושא ts=`2026-06-10 22:40:00+03:00` — **יום קדימה** (~+30h), לא רק ב-gate `woodies_5min`. שאר הערוצים (CVD/VP/imbalance) ב-UTC תקין. מפר Rule 4 (TZ-ambiguity) + Rule 1. חשוד-שורש לפער-UI ב-I-15 ול-lag-שלילי ב-I-20.

**השינוי המוצע (שכבה: bridge ingest boundary):** בנקודת-הגבול שבה ה-bridge קורא את ה-Sierra export של woodies_5min/bars_5min — **לנרמל ts ל-UTC מפורשות** (לא "assumed IL"), ולהוסיף **guard נגד ts עתידי** (`if bar_ts > now_utc + ε: reject/clamp + logger.warning rate-limited`, לא debug — לפי "no silent failures"). אסור לסנתז ts; אם Sierra מייצא ts עתידי — לדגל, לא לתקן בשקט.

**regression שיוכיח:** הזרקת בר עם ts עתידי ⇒ `assert` שהוא נדחה/נחסם + `logger.warning` נרשם; בר עם ts תקין ⇒ עובר. בדיקת freshness: `assert lag_seconds ≥ 0` עבור ערוץ תקין.

**סיווג:** 🟢 **display/safe** ברובו (freshness-display/gating; readiness משתמש ב-crit-gates אחרים). אך זו **data-integrity** בערוץ הזנה ⇒ עדיפות בינונית-גבוהה.
**מקור-אמת ל-CC (חובה):** הצלב mtime + last-bar-ts בקובץ `~/SierraChart_Data/v9_export/woodies_5min` — האם Sierra מייצא ts עתידי, או שה-bridge ממיר שגוי? זו ההכרעה.

---

## D4 · I-20 — lag שלילי עם fresh=true (🟢 display/safe)

**שורש:** ה-predicate של `data_freshness` לא אוכף `|lag| ≤ threshold` — מקבל `lag_seconds=-97160` (~-27h) כ-`fresh=true`. קשור ל-ts-העתידי (D3) שמייצר last_bar_ts עתידי/null.

**השינוי המוצע (שכבה: bridge freshness predicate):** לאכוף `0 ≤ lag_seconds ≤ threshold` (לדחות lag שלילי כ-stale, לא fresh). תלוי-חלקית ב-D3 (נרמול-TZ פותר את מקור ה-lag השלילי).

**regression שיוכיח:** `lag=-100 ⇒ fresh=false`; `lag=50, threshold=90 ⇒ fresh=true`; `lag=200, threshold=90 ⇒ fresh=false`.

**סיווג:** 🟢 **display/safe** — readiness אינו תלוי ב-aggregate הזה (משתמש ב-crit-gates). דיאגנוסטי.

---

## D5 · I-23 — gateway counters לא סופרים shadow fires (🟢 display/safe)

**שורש:** `gateway.trades_today` / `shadow_active_count` / `daily_pnl` נשארו 0 למרות 2 עסקאות-shadow היום (id=20+id=22) ו-board "S2×1 S4×1". המונים לא מחווטים ל-shadow fires (רק ל-live?).

**השינוי המוצע (שכבה: gateway counters):** לחווט את מוני-היום לספור גם shadow fires (`trades_today`++ ב-shadow-fire, `daily_pnl` += contract pnl, `shadow_active_count` = פתוחים). **wire-the-full-pipeline:** לוודא שכל שלושת המונים מתעדכנים באותו event, לא רק אחד.

**regression שיוכיח:** shadow-fire אחד ⇒ `assert trades_today == 1` ו-`shadow_active_count == 1`; אחרי close ⇒ `shadow_active_count == 0`, `trades_today` נשאר 1, `daily_pnl` מעודכן.

**סיווג:** 🟢 **display/safe** — מוני-דיאגנוסטיקה, אינם בנתיב-ירי (ה-WR/Trades בכותרת נגזרים מ-`/trades/recent`, לא מהמונים).

---

## D6 · I-25 — `/trades/recent?limit=200` → 422 (🟢 doc-fix)

**שורש:** ה-route מגביל `limit ≤ 100`; מפרט סוכן-ה-EOD (`SKILL.md`) מבקש `limit=200`.
**השינוי המוצע:** תיקון-מסמך — עדכן את `SKILL.md` ל-`limit=100` (או, אם רוצים 200, להעלות את ה-`le` ב-validator). **לא חוסם** (5 עסקאות < 100).
**סיווג:** 🟢 doc-fix.

---

## נדחים מעיצוב היום (טרם בשלים)

- **I-1** (day_type split + vote_history=[]) — דורש אבחון feed-instance/wrapper ע"י CC + הצלבת `Y IB dll_missing`/atr_daily מול DLL **לפני** עיצוב. לא לעצב מהזיכרון.
- **I-11** (footprint 0 ברים) — מושתק S3_MUTE, S3 deferred עד אחרי-LIVE. לא blocker; עיצוב-ingest כשתיפתח S3.
- **I-15 / I-19** — דורשים הצלבת Sierra (CCI גולמי) / פרופיל-latency של `build/pattern-status` לפני עיצוב-שורש.

**הערת-פייפליין (לקח 5-flag, מהזיכרון):** כל שינוי-החלטה/flag (במיוחד D2) חייב להגיע ל**כל** ענף מושפע — אסור dead-wiring חלקי. **כל ה-trading-logic (D2) דורש strategic-stop + אישור Michael בכתב לפני מימוש.**
