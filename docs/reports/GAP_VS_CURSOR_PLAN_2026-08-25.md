# מה יש בפועל · מה הפערים מול תוכנית קורסור

**תאריך:** 2026-08-25 14:15 IL · **מאת:** cowork-dev
**נמדד מול:** `docs/plans/MAXIMIZATION_INFRA_EXECUTION_PLAN_2026-08-24.md` — **פר קריטריון-קבלה**
**מעמד:** קריאה-בלבד. לא שונה קוד, לא הודלק דגל, לא בוצע ריסטארט.

---

## הפסק בשורה אחת

**התוכנית נכונה. הביצוע בערך 15%. ושניים משישה תנאי-העצירה של התוכנית עצמה — מתקיימים כרגע.**

---

## 🔴 הממצא החמור ביותר — שער-העצירה פתוח

התוכנית מגדירה שישה תנאים שבהם **עוצרים**. שניים מהם נכונים היום:

**1 · "replay/live משתמשים בפונקציות שונות" — מתקיים.**
`scripts/good_pattern_fix.py:189` מצהיר על עצמו: *"live-faithful **mirror** of S2"* → `react_mirror` (`:234`) · `init_mirror` (`:287`).
‏`replay_maximized_opportunity.py:56` טוען את הקובץ הזה ומריץ עליו את כל הזרועות.
⇒ **כל מספר-רפליי שהפקנו מודד את המראה, לא את הקוד החי.** יש selfcheck שמדפיס השוואה — אבל זו הדפסה, לא שער נאכף.

**2 · "מספר-$ נבנה מ-populations שונות" — מתקיים.**
המספרים שהתוכנית נשענת עליהם הופקו מ-DB שהקרנל — שנבנה בדיוק כדי לתפוס את זה — מכריז עליו
**NOT_JUDGEABLE ב-25 מתוך 34 סשנים**. ‏`TASK_LOG.md:229` כבר מתעד שמספר אחד הפך
מ-**+$339 ל-−$558.76** אחרי תיקון-סיבתיות ל-TPO.

---

## הטבלה — Task אחר Task

| Task | מה יש בפועל | מה חסר | חוסם? |
|---|---|---|---|
| **0 · מנוע-רפליי אחד** | ‏6 מודולים · 29 טסטים · manifest נאכף · hash יציב (אומת ×2) · לא מיובא ב-runtime ✅ | **הקרנל לא מייצר candidates בכלל** (`candidate_engine_id="none-stage0b"`). אין `ReplayCandidateEngine`/`Policy`/`ExecutionModel` — 2 מ-5 רכיבים נבנו. **31 סקריפטי-replay עדיין קיימים, אפס adapters.** אין `feature_schema` ב-manifest | 🔴 |
| **1 · Candidate Ledger** | כותב + ווים ב-S2/S4/gateway · dedup דטרמיניסטי · pytest-isolation · source/pid/commit ✅ | **אינרטי בייצור** (ר' למטה). אין `context` (day label/phase/direction) · אין levels/`level_available_at` · אין CVD · אין size-intent · **אין `RESOLVED` ואין MFE/MAE בכלל** · אין טסט anti-tautology · 024 לא הורצה ואין stamping ל-ORM | 🔴 |
| **2 · TPO סיבתיות + CVD** | הקרנל מיישם collapse דטרמיניסטי + coverage כן ✅ | **אפס עבודה על השורש.** ‏`v9_tpo_history` — **אין `available_at`, אין `market_ts`**; `676 מ-677 שורות` עם `created_at > ts`, פיגור ממוצע **183 דק'**. ‏`historical_backfill` — לא קיים בקוד. CVD: המפתח הוא `cvd_{idx}` (`bars.py:1096`) ולא `(symbol,bar_ts,source_version)`; **246 חותמות סותרות** | 🔴 |
| **3 · DB↔SCID** | validator קיים ורץ ✅ | **התוצאה: PASS=9 · FAIL=25 מתוך 34.** ‏78-ברים ב-28/34 · 0-תפרים ב-30/34 · 07-17: כל 78 הברים שונים מ-SCID ב-OHLC ובווליום. **אין diff report, אין RCA, אין snapshot, אין dry-run** — כלומר שלב 1 של מסלול-הריפייר לא התחיל | 🔴 |
| **4 · S1 בינארי (shadow)** | הקוד קיים · בלי confidence לצרכנים ✅ · אפס שינוי-התנהגות ✅ | **shadow = סשן חי אחד** (08-24), נדרשים ≥10. אירועי **EOD ו-DD-refill לא מומשו**. אין restart-parity. **ובאג:** `bar_count == 12` לעולם לא מתקיים (ה-harness מתחיל ב-12) ⇒ `determined` היה False בכל 34 הסשנים. וה-PASS ניתן על מדד **BROAD 91.2%** בעוד **EXACT = 38.2%**, ומול reference שהתוכנית לא קבעה | ⚠️ |
| **5 · `CONTEXT_ENTRY_V1`** | — | **אפס קוד.** ‏`grep CONTEXT_ENTRY --include=*.py` → 0. כל 24 האזכורים ב-`docs/` בלבד | ⚪ חסום בכוונה |
| **6 · Ranker** | אפס קוד — **וזה נכון** ✅ | השער דורש **≥300 outcomes** מהליגר. יש **0**, והמונה לא זז | ⚪ חסום בכוונה |

---

## 🔴 חוסם מספר 1 — והוא נסגר הערב

**הליגר אינרטי בייצור.** הראיה:

```
$ grep -n CANDIDATE_LEDGER_V1 .env
547:CANDIDATE_LEDGER_V1=1                    ← נוסף היום 13:15

$ ps -p 71390 -o lstart=
Sun Aug 23 21:24:32 2026                     ← התהליך החי לא מכיר את הדגל

$ (ספירת gateway_decisions.jsonl)
שורות=196 · עם event_type=0                  ← יום-מסחר מלא, אפס אירועי-ליגר
```

‏08-24 היה יום-מסחר מלא עם 196 החלטות — **ואפס אירועי-ליגר.** המונה ל-Task 6 עומד על 0
ולא מתקדם. **הריסטארט של 15:45 מפעיל אותו** — זה הדבר היחיד היום שמזיז את התוכנית קדימה.

**אבל גם אחרי הריסטארט הוא יאסוף חצי:** הוא ירשום **מועמדים** ולא **תוצאות**.
`grep RESOLVED\|mfe_3` בקוד-הייצור → אפס. בלי outcome אין מה למדוד ואין מה לדרג.

---

## חמשת החוסמים למקסום — לפי סדר

| # | החוסם | למה הוא חוסם |
|---|---|---|
| **1** | **הליגר אינרטי** | 0 אירועים. שעון-האיסוף ל-≥300 לא זז. **נסגר בריסטארט הערב** |
| **2** | **אין `RESOLVED`/MFE/MAE** | גם אחרי הריסטארט — מועמדים בלי תוצאות. אי-אפשר למדוד איכות |
| **3** | **25/34 סשנים NOT_JUDGEABLE** | כל מספר-$ שנפיק מ-`v9_bars_5min_woodies` בלתי-קביל. חוסם 0/3/5/6 יחד |
| **4** | **TPO לא-סיבתי (676/677) + 246 CVD סותרים** | שני הפיצ'רים ש-`CONTEXT_ENTRY` אמור להישען עליהם אינם דטרמיניסטיים ואינם סיבתיים |
| **5** | **הרפליי רץ על mirrors** | כל parity שנמדוד מודד את המראה, לא את הקוד החי |

**ניקיון (חשוב, לא חוסם):** 024 + stamping · טולרנס-SCID ל-manifest · EOD+DD-refill ב-S1 ·
`bar_count>=12` · restart-parity · 31 ה-adapters (ערך גדול — אך חסר-משמעות לפני שהקרנל
מייצר candidates).

---

## מה זה אומר

התוכנית של קורסור מזהה נכון את מחלקות-השורש. מה שקרה בפועל הוא ש**נבנו הכלים למדוד —
ולא הופעלו.** הקרנל מאמת נתונים אך לא מייצר מועמדים; הליגר כתוב אך כבוי; ה-validator רץ
ומחזיר 25 כשלים שאיש עוד לא טיפל בהם.

**ולכן שני תנאי-העצירה מתקיימים:** אנחנו ממשיכים להפיק מספרי-$ ממראות ומאוכלוסייה
שנכשלת באימות שלנו עצמנו.
