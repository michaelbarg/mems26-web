# MEMS26 · טיוטות-עיצוב — 2026-07-06

מקור: `PATTERN_EOD_2026-07-06.md` (EOD 15:12 CT). **קריאה-בלבד — לא-מומש.** כל design מוכן-להפיכה-לפרומפט.
מקרא: **[TRADING-LOGIC]** = משנה risk/fire-surface → **דורש אישור-Michael + סטופ-אסטרטגי** · **[DISPLAY/SAFE]** = תצוגה/instrumentation, ללא-שינוי-מסחר. **[SoT→CC]** = דורש הצלבת Sierra v9_export/DB/לוג ע"י CC לפני-מימוש.

---

## §1 · I-62 — הנתיב-הקובע (DEMO) כבוי · **[TRADING-LOGIC] · [SoT→CC]**

**שורש (חשד):** `gateway.demo_enabled_systems=[]` post-close 07-06 (בעוד 07-03 = דמו-פעיל, id290). `live_enabled_systems=[2,4]` אך `live_slot=null`. ⇒ demo-track דומם, רק shadow-sim רץ. **לא-ידוע** אם זה קידום-מכוון (S2/S4 הועברו demo→live-enabled לקראת LIVE) או השבתה-בשוגג.

**הצלבה-נדרשת לפני-כל-מהלך (CC):** `git log`/`.env`/`docs/FLAG_INDEX.md`/`FLAG_REGISTRY.yaml` — **מתי `demo_enabled_systems` רוקן, ובאיזה commit/הוראה.** להצליב מול `live_enabled=[2,4]` — האם נרשמה החלטת-Michael לקדם S2/S4 ללייב-enabled.

**שינוי מוצע (מותנה-בממצא):**
- אם **מכוון** → **לא-לגעת.** רק לתעד ב-`docs/FLAG_INDEX.md` + `SOURCE_OF_TRUTH.md`: "demo כבוי במכוון מ-<date>, S2/S4=live-enabled" ולעדכן את סוכן-ה-EOD שלא-לצפות ל-demo. **[DISPLAY/SAFE]**
- אם **שוגג** → החזרת `demo_enabled_systems` לערכו-הקודם. **[TRADING-LOGIC] — Michael בלבד.**

**Regression שיוכיח:** `test_demo_enabled_matches_intended_config` — טוען gateway-config ומוודא `demo_enabled_systems` תואם ערך-מוצהר ב-`FLAG_REGISTRY.yaml`; `--check` נכשל על drift לא-מתועד (כמו gen_flag_index).

**עדיפות:** 🔴 סטופ-אסטרטגי — הנתיב-הקובע היה-דומם יום-שלם; חובה לאשר-כוונה לפני עוד-EOD.

---

## §2 · I-61 — flatten-ידני של שורטי-shadow 12:01 CT · **[SoT→CC] → [DISPLAY/SAFE] + אולי [TRADING-LOGIC]**

**שורש (חשד):** 292 (entry 11:55) + 294 (entry 12:00) נסגרו שניהם `exit_reason=manual / outcome=BE` **באותה-דקה 12:01**, במחיר≈כניסה. עבור **שורט** שנכנס-לתוך-ראלי (בר-12:00 עלה ל-7589, אדוורסרי מיָדִי), BE-stop אמיתי לא-ייתכן (סטופ-ל-BE נקבע רק אחרי מהלך-חיובי). ⇒ זו **סגירת-flatten**, לא BE-management. מנגנון-לא-ברור: התערבות-ידנית / חוק-shadow-sim / באג.

**הצלבה-נדרשת (CC):** לחלץ מ-`fill_poller.py` / `on_trade_close` / shadow-sim-exit-path מי-כתב `exit_reason=manual` ל-292/294; להצליב `/tmp/*.log` סביב 12:01 CT. **לקבוע: נתיב-אוטומטי (חוק) או ידני (Michael).**

**שינוי מוצע:**
- **instrumentation תמידי (מיָדי):** להחליף `exit_reason=manual` בתווית-מבחינה — `manual_ui` (Michael לחץ flatten) מול `auto_flatten_<rule>` (חוק-סיסטמי). כך כל-EOD-עתידי יידע מקור-הסגירה. **[DISPLAY/SAFE]**
- אם מתגלה **חוק-flatten-אוטומטי** על שורטי-shadow → זה **נתיב-החלטה-סמוי** שמשפיע-על-CF (הפך −2R→0R היום); לתעד-ולהחליט אם רצוי. **[TRADING-LOGIC] — Michael.**

**Regression שיוכיח:** `test_exit_reason_source_labeled` — סגירת-flatten סיסטמית מקבלת `auto_flatten_*`, סגירת-UI מקבלת `manual_ui`; אף-סגירה לא-מקבלת `manual` גנרי.

**עדיפות:** 🟡 — מהותי-ל-CF (מנגנון-לא-מובן שמעצב תוצאות), אך התיוג עצמו safe.

---

## §3 · I-54 / D-targets-monotonic — short-targets מבניים-רחוקים + היפוך t2>t3 · **[TRADING-LOGIC] · [SoT→CC]**

**שורש:** 292: t1 7578.5 / **t2 7558 (25.75pt) / t3 7557.25**. 294: t1 7578.25 / **t2 7558 (25.75pt) / t3 7569.75 (14pt)** ⇒ **t2 רחוק-מ-t3** (היפוך-מונוטוניות: T2 אמור-להיות-בין-T1-ל-T3). ערך-7558 חוזר על שני-השורטים = חשד **רמה-מבנית-קבועה** מוזרקת-כ-target ללא-בדיקת-סדר/מרחק. על יום-טווח-50pt, targets של 25+pt לא-בני-השגה; שורט-מנצח נחנק (כמו I-54 ב-07-01/02: 267/268 נסגרו-שטוח, +11R אבודים).

**הצלבה-נדרשת (CC):** לחלץ את חישוב-ה-targets ב-`five_min_system` (REACTIVE) + woodies-target-calc (ZLR) — מאיפה 7558? (רמת-מבנה/POC/swing?) ולמה לא-נאכף `t1<t3<t2` (short) / מרחק-מקסימלי מ-ATR.

**שינוי מוצע (`backend/v9/systems/.../target_calc`):**
1. **אכיפת-מונוטוניות:** לשורט `entry > t1 > t2 > t3`; ל-long הפוך. לזרוק/לתקן target שמפר-סדר.
2. **תקרת-מרחק ATR-מודעת:** `|target − entry| ≤ k·ATR_daily` (k מ-config) — target מבני-רחוק-מדי → clamp ל-ATR-cap או להשמיטו (T2/T3 אופציונליים).
3. לתעד ב-trade-record את **מקור** כל-target (structural/atr/fixed) ל-CF-עתידי.

**Regression שיוכיח:** `test_targets_monotonic_and_bounded` — עבור fixture-שורט (entry 7583.75, ATR-יומי נתון): מוודא `t1>t2>t3`, כל `|t−entry| ≤ k·ATR`, ואין target=רמה-מבנית-מעבר-לתקרה. Anti-regression מול 294 (t2>t3) ו-07-01 (targets-88pt).

**עדיפות:** 🔴 — משפיע-ישירות-על-runner-economics; היום מוסתר ע"י flatten, אבל ביום-בלי-flatten זו עלות-אמיתית.

---

## §4 · D-missed-buffer — `/missed-trades` חסר-ערך-אינטרה-דיי · **[DISPLAY/SAFE] · [SoT→CC]** (מאחד I-60)

**שורש:** count=50, כולם ZLR-LONG `ready_to_route=False`, מרוכזים ~14:47 CT, `hypothetical_r=null`. שני-פגמים: (א) **אין-dedup** — Sierra ריבוי-UPDATE/בר → אותו-מועמד עד תקרת-50; (ב) **`hypothetical_r` תמיד null** — CF לא-מחושב-בכתיבה. ⇒ ה-store לוכד רק פרץ-מאוחר, מפספס חסימות-אמצע-יום (שם ה-R-האמיתי). חוזר 06-16→07-03 + היום.

**הצלבה-נדרשת (CC):** לוג `missed_trade_detector` — לאשר שה-50=דופ' של ~1 סיגנל (ריבוי-UPDATE), ושה-null נכתב-במקור.

**שינוי מוצע (`missed_trade_detector` / `/missed-trades` route):**
1. **dedup** על `(pattern, direction, entry_price±tick, bar_ts)` — רשומה-אחת-לסיגנל, שדה `dupe_count`.
2. **מילוי `hypothetical_r`** בכתיבה: replay-קדימה על bars5min (entry/stop/T1 לפי ספק-התבנית) → hit_T1/stop/timeout → R. (בדיוק ה-CF שהסוכן עושה ידנית — להעביר-לקוד.)
3. **persistence** → PG (לא in-memory; מונע מחיקת-ריסטארט, I-60) + retention=RTH-מלא.
4. לחשוף `failed_stages` (ראו §5) פר-רשומה.

**Regression שיוכיח:** `test_missed_trades_dedup_and_r` — הזרקת 50-UPDATE של סיגנל-אחד → endpoint מחזיר רשומה-אחת `dupe_count=50`, `hypothetical_r` מספרי (לא-null), שורד ריסטארט (PG).

**עדיפות:** 🟡 — כלי-אבחון-מרכזי חסר-ערך; מימושו מייתר CF-ידני.

---

## §5 · D-ready-route-instrument — חשיפת `failed_stages` פר-חסימה · **[DISPLAY/SAFE] · [SoT→CC]** (תנאי-מוקדם ל-I-3)

**שורש:** `ready_to_route=False` (I-3) הוא-החוסם-המוביל (היום ~50 ZLR-LONG; חסם-נכון — CF −1R), אבל **חסר instrumentation איזה A-stage הפיל.** הקוד **כבר-רושם** `current_state.last_route.failed_stages` (`woodies_system.py:~1035–1039`) אך `/missed-trades` לא-חושף. בלי-זה אי-אפשר לכייל את I-3 (להבחין חסימה-מוצדקת מ-שמרנית-מדי).

**שינוי מוצע:** להוסיף `failed_stages: [...]` (רשימת A1–A7 שנכשלו + ערך-הקלט-שחתך) לכל-רשומת-`/missed-trades` ול-build_status. **קריאה-בלבד מ-state קיים — אפס-שינוי-לוגיקה.**

**הצלבה (CC):** לבלוקי-14:47 היום — לאשר `A7:TOO_CLOSE_TO_EOD` (chase 8–13 דק' לפני-סגירה, מול `MIN_EOD_DISTANCE_MINUTES`) או stage-אחר.

**Regression שיוכיח:** `test_missed_exposes_failed_stages` — חסימת-ZLR מזריקה `failed_stages` לא-ריק ל-endpoint; ערך-הקלט-החותך נוכח.

**עדיפות:** 🟡 — **תנאי-הכרחי לפני כל-כיול-של-I-3** (אסור לגעת-בשער לפני-שרואים-למה).

---

## §6 · I-44 — day_type wrapper-קפוא · **[DISPLAY/SAFE] · [SoT→CC]** (handoff קיים)

**שורש:** `/day_type/state` = `session_min=0 / vote_history=[]` (קפוא) — endpoint קורא wrapper-instance-מת במקום `app.state.day_type_machine`+`v9_day_type_state` (ראו CLAUDE.md § Codebase Index). היום ההתוויות **סבירות** (OPEN_REJECTION_REVERSE/WIDE תואמים צורת-יום-האמת) — ניגוד ל-07-03 (OPEN_DRIVE/EXTREME על 12.5pt, בלתי-סביר) — כלומר ה-**wrapper** הוא-הבעיה, לא-הסיווג-עצמו.

**שינוי מוצע (עומד מ-`CC_DAYTYPE_SOURCE_CONSISTENCY_2026-06-30.md`):** לאחד את ה-endpoint למקור-החי (`app.state.day_type_machine`) + דגל-`stale` כשה-wrapper לא-מתעדכן (`session_min` לא-עולה). display-only.

**Regression:** `test_daytype_endpoint_live_source` — `session_min>0` ו-`vote_history` לא-ריק כש-RTH-פעיל; דגל-stale=true כשקפוא.

**עדיפות:** 🔴 (מבלבל-אבחון) אך display-safe. handoff עומד — לא-חדש.

---

## §7 · I-23 — gateway counters לא-סופרים shadow · **[DISPLAY/SAFE]** (עומד)

**שורש:** `trades_today=0/daily_pnl=0` מול 3-fires-shadow היום. מוני-היום לא-מחווטים לעסקאות-shadow.

**שינוי מוצע:** לחווט `trades_today`/`daily_pnl`/`shadow_active_count` לספור shadow (עם פילוח mode). display-only.
**Regression:** `test_gateway_counts_shadow` — 3 shadow-fires → `trades_today=3`.
**עדיפות:** 🟡 display-safe. design עומד מ-06-09.

---

## סיכום-עדיפויות

| # | פריט | class | דחיפות | חוסם-מימוש |
|---|------|-------|--------|-----------|
| §1 | I-62 demo כבוי | TRADING/SoT | 🔴 סטופ | אישור-Michael (כוונה?) |
| §3 | I-54 targets t2>t3 | TRADING | 🔴 | אישור-Michael + SoT |
| §2 | I-61 flatten 12:01 | DISPLAY→(TRADING?) | 🟡 | SoT (מנגנון?) |
| §4 | D-missed-buffer | DISPLAY/SAFE | 🟡 | — (safe) |
| §5 | D-ready-route-instrument | DISPLAY/SAFE | 🟡 | — (תנאי ל-I-3) |
| §6 | I-44 wrapper | DISPLAY/SAFE | 🔴-בלבול | handoff עומד |
| §7 | I-23 counters | DISPLAY/SAFE | 🟡 | — (safe) |

**3 החמורים (לנוטיפיקציית-Michael):** §1 I-62 (demo כבוי — אשר-כוונה) · §3 I-54 (short-targets שבורים — trading-logic) · §2 I-61 (flatten-12:01 מנגנון-לא-מובן שהציל +2R).
**כולם דורשים אישור/SoT** לפני-מימוש פרט §4/§5/§7 (display/safe — ניתן-לממש-מיָדית).
