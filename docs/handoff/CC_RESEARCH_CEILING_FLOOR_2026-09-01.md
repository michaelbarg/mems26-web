# פקודה · אזור-הערך שבור → לתקן, ואז לחקור תקרה/רצפה מול הברים

**מאת:** cowork-dev · **אל:** cc-macbook · **01.09 13:15**
**פסיקת-מייקל:** *"תחקור את התקרה ואת הרצפה כדי שכן ניכנס במיקום המתאים · לבצע
בדיקות מול ברים ודלתון ולהפיק לקחים אמיתיים לאיפה צריך להיכנס · הסטטיסטיקה גם
לא נכונה · ומערכת 2 ו-4 צריכות לעבוד ביחד, יש חיבור שבנית."*
**ואישור 13:10:** *"לתקן קודם את חישוב אזור-הערך, ואז לחקור."*

---

# 🔴 שלב 1 · אזור-הערך שגוי — **חוסם את כל השאר**

אזור-ערך הוא **70% מהנפח**, בהגדרה. אצלנו:

```
תאריך   צורה  רוחב-VA  טווח-RTH  %מהטווח    IB
31.08     b     3.50     33.50      10%     29.50   ← VA קטן פי 8 מה-IB
28.08     b    23.25     70.75      33%     34.00
27.08     D     9.75     53.00      18%     27.75
26.08     P     5.75     34.50      17%     23.25
25.08    NA    27.00     38.50      70%  ←  21.50   התקין היחיד
24.08     b    10.25     31.50      33%     27.75
21.08     D     2.75     33.50       8%     19.75
20.08     b    10.50     60.75      17%     24.75
```

**‏7 מתוך 8 בטווח 8-33%. אחד — 25.08 — נותן בדיוק 70%.**
⇒ **החישוב יודע לעבוד ובדרך-כלל לא עובד.** זה לא פרמטר לא-מכויל; זה באג
שנופל לרוב ולא תמיד. **מצאו למה אותו יום אחד הצליח** — שם התשובה.

**‏🔴 ו-31.08 הוא הבלתי-אפשרי מבנית:** ‏`VA = 3.5` בעוד `IB = 29.5`.
ה-IB הוא שעת-המסחר הראשונה בלבד; **אזור-הערך של יום שלם אינו יכול להיות
קטן פי 8 ממנו.**

## מי צורך את זה — וכולם חיים עכשיו

| צרכן | מה הוא עושה עם VA שגוי |
|---|---|
| `location_gate` | ‏`near_val`/`near_vah` על VA של 3.5 נק' ⇒ **כמעט כל מחיר "מחוץ לערך"** |
| `DALTON_EDGE_V1` | ‏`live` מ-28.08, רץ על אותו קלט |
| `daytype_playbook` | ‏`Normal: ref_points [VAH,POC,VAL]` + `fade_edges: true` ⇒ **דוהה קצוות שאינם הקצוות** |
| `target_zones` | בונה אזורי-קונפלואנס מאותן רמות |

**⇒ כל פסיקות-הדלתון של מייקל — *"לדהות את הקצוות"*, *"VAL→לונג · VAH→שורט"* —
מיושמות על אזור-ערך שגוי.** זה גם ההסבר הסביר לכך שמדידה של היום הראתה
שכניסה "קרוב לקיצון" מנצחת **הכי פחות** (22%): **המערכת לא יודעת איפה הקצוות.**

## מה לבנות

1. **לאתר את השורש** — `v9_tpo_sessions` נכתב מ-? (‏`tpo_*` בברידג' / DLL).
   ‏**‏🔴 ולבדוק ראשון:** האם ה-VA מחושב מ-**TPO** (אותיות) או מ-**נפח**, והאם
   הקלט הוא הסשן המלא או חלון חלקי. ‏`letter_count=17` על 31.08 מול 78 ברי-RTH
   מרמז שהפרופיל נבנה על **חלק מהיום**.
2. **שער-שפיות שנכשל** — `0.5 ≤ VA_width / RTH_range ≤ 0.9`, אחרת
   ‏`profile_quality='SUSPECT'` **והצרכנים מקבלים `None`, לא ערך שגוי** (כלל 1).
   **זה הפריט החשוב ביותר בשלב הזה** — הוא הופך כשל-שקט לכשל-רועש.
3. **‏`GLOBEX` מול `CASH`** — `31.08` הופיע בשתי השורות עם **אותם** VAH/POC/VAL
   (7702/7701/7701). **פרופיל-לילה ופרופיל-יום אינם יכולים להיות זהים.**
   לבדוק אם אחד דורס את השני.

**אימות דו-כיווני:** יום עם VA=70% ⇒ עובר · יום עם VA=10% ⇒ **`SUSPECT`,
והצרכנים מקבלים `None`** · ו-`location_gate` עם `None` ⇒ **אינו מחליט**, לא
"מחוץ-לערך".

---

# 🔵 שלב 2 · תקרה/רצפה מול הברים — **רק אחרי שלב 1**

## מה שיש היום — כמעט כלום

```
DOUBLE_BOTTOM_EE_LONG   live n=2 · shadow n=3
DOUBLE_TOP_AA_SHORT     shadow n=2 · live n=0
```
**‏7 עסקאות.** ⇒ מחקר, לא כיול. **אסור לתת אחוז על n<10.**

## המדידה — פר **זיהוי**, מהגלאי, לא מ-`v9_trades`

רוב הזיהויים לא הפכו לעסקה, וזה בדיוק המדגם שחסר. ‏`five_min/patterns/double_bt.py`.

| שדה | מקור |
|---|---|
| `extreme_price` | השפל/השיא של הצלע השנייה |
| `neckline` · `pattern_height` | הגלאי |
| `entry_actual` | ‏`bar.close` של בר-האישור (`five_min_system.py:2351`) |
| **`lateness = |entry − extreme|`** ו-**`lateness / pattern_height`** | הגודל המרכזי — **שתי הגרסאות** |
| **מיקום-דלתוני של ה-`extreme`** | מרחק ל-VAH/POC/VAL/IBH/IBL **המתוקנים** ולרמות-אתמול |
| `t1_before_stop` | הליכה קדימה על ברי-5-דק' |

**השאלות, מנוסחות כך שיהיו בנות-הפרכה:**
1. האם `t1_before_stop` יורד ככל ש-`lateness` עולה?
2. **האם קיצון שנוגע ברמה דלתונית מנצח יותר מקיצון שאינו?** ← *זו שאלת-מייקל*
3. איזו רמה הכי חזקה: `VAL` · `IBL` · שפל-אתמול · `POC`?

**‏⚠️ בקרה חובה:** ‏`lateness` מתואם עם גובה-התבנית ⇒ **בלי הנרמול זו מדידה של
גודל-תבנית ולא של איחור.** לדווח את שתי הגרסאות תמיד.

## מה שכבר ידוע — אל תבזבזו זמן

`double_bt.py:236-238` — הכניסה מותנית בסגירה מעבר לקו-הצוואר ⇒ **בהגדרה
גובה-תבנית שלם מהקיצון.** זה לא באג; **השאלה היא אם המחיר הזה מוצדק.**

---

# 🔵 שלב 3 · ‏S2 × S4 — החיבור קיים ולא נמדד

```
backend/v9/systems/confluence/confluence_ri_zlr.py
  "S2 (REACTIVE/INITIATIVE) × S4 (ZLR) same-bar coincidence detector"
  CONFLUENCE_RI_ZLR_V1    default OFF
  CONFLUENCE_RI_ZLR_LIVE  default OFF — "is NOT implemented"
ירי בפועל:  live n=1 (−$42.50) · shadow n=2 (+$78.75)
```

**בסדר הזה:**
1. **מה ערך הדגל ב-`.env` בפועל?** אם OFF — 3 הירי הם ממסלול אחר או מלפני שכובה.
2. **כמה קונפלואנס היו *צריכים* לקרות?** לסרוק היסטוריה: ברים שבהם S2 ו-S4
   זיהו **באותו בר ובאותו כיוון**. ‏**אם התשובה 3 — נדיר, אין מה למדוד. אם 50 —
   כבוי ומפספסים.** *זו השאלה שמכריעה.*
3. ‏`t1_before_stop` על אותם ברים מול הבסיס של כל מערכת בנפרד.

**‏🔴 אל תדליקו את `CONFLUENCE_RI_ZLR_LIVE`** — הקוד אומר *"is NOT implemented"*
⇒ דגל קיים, מסלול לא. הדלקה = דגל-מת שנראה חי (מחלקת `S2_CVD_DETECTION_V1`, T-96).

---

# 🔴 שלוש מלכודות-סטטיסטיקה שכל מספר חייב לעקוף

**א · כפילויות — פי 10.** ‏`468 חסימות → 46 ייחודיים`. הופיע היום **שלוש
פעמים** (`S2_DELTA_DBL` 37× · ניפוח-הצל 2.42× · חסימות 10×).
⇒ **ייחודי לצד גולמי. תמיד.**

**ב · `v9_trades.stop` הוא הסופי.** ‏8 כותבים אחרי-כניסה.
‏`WIN` ‏2.26 מול `LOSS` ‏6.26, ומרחק-T1 זהה (7.68/7.37) ⇒ ההפרש נוצר **אחרי**.
`quality.initial_stop` פסול גם (‏90-100% בכל רצועה). **השתמשו ב-`quality.entry_stop`
החדש** (`T-200a`) — **קדימה בלבד.**

**ג · ‏MFE אינו רווח.** הטעה **פעמיים היום**, פי 4 ופי 7.
⇒ **‏`t1_before_stop` בלבד.** בר שנוגע בשניהם ⇒ **STOP** (פסימי).
כלי: `scripts/blocked_candidate_audit.py`.

---

## מסירה · `docs/reports/CC_CEILING_FLOOR_2026-09-01.md`

שורש ה-VA + השער · טבלת `lateness` (גולמי **ומנורמל**) מול `t1_before_stop` ·
מיקום-דלתוני של הקיצונים · ספירת-קונפלואנס · **`n` לכל מספר** ·
**`NOT_JUDGEABLE` היכן ש-`n<10`** · סעיף **NOT-DONE** מפורש.

## אסור

להדליק דגל · לשנות פלייבוק · לגזור איכות-כניסה מ-`stop`/`initial_stop` ·
לדווח MFE כרווח · ספירה גולמית בלי ייחודיים · אחוז על n חד-ספרתי ·
**לחקור מיקום-דלתוני לפני ששלב 1 ירוק** · ריסטארט 16:10-23:00.

---

## ובמקביל — שתי פסיקות-מייקל שנכנסות לביצוע

**‏(א) `OPENING_DRIVE` → `SKIP` בכל 8 סוגי-הימים.** ראיה: **6 עסקאות, אפס
ניצחונות** (`live n=3 −$383.75` · `shadow n=3 −$366.25`).
`daytype_playbook.yaml` + `RULED_FLAGS` באותו commit.

**‏(ב) כל מונה-חסימות מדווח ייחודיים לצד גולמי** — לוג, לוח, וכל דוח.
אותו טיפול שקיבל N מול K. **‏468 מול 46 היא הראיה.**
