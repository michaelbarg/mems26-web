# פקודת-סוף-שבוע ל-cc · מוכנות ליום שני 31.08

**מקור-האמת המלא:** `docs/plans/MONDAY_READINESS.html` (תיק-המוכנות — לקרוא ראשון).
**פקודות-הרקע:** `CC_WORKORDER_CEILING_FLIP_2026-08-28.md` · `CC_EVENING_PROMPT_2026-08-28.md`.

## שבת — שלושת חוסמי-המסחר (בסדר הזה, כל אחד עם טסט שמשחזר את 28.08)

**1 · T-43 מודל-הפוזיציה (הכי דחוף — כסף אמיתי היה בסיכון שגוי).**
סיירה מנהלת פוזיציה נטו אחת בממוצע; ה-TM חושב שיש שתי עסקאות נפרדות (5 חוזים מול 3).
תיקון: (א) כשיותר מעסקה-חיה-אחת באותו כיוון — מחיר-הייחוס לכל חישוב-סטופ הוא
`sierra.avg_price`, לא `trade.entry_price`; (ב) אימות רציף `Σ TM.contracts == |position_qty|`
עם אזעקה + חסימת-כניסות עד יישוב; (ג) `live_slot` משתחרר רק ב-`position_qty==0` **וגם**
אפס הוראות-הגנה — לא על מימוש-חלקי. טסטים: שתי עסקאות-לייב ⇒ סטופים מהממוצע · T1-חלקי
⇒ הסלוט נשאר תפוס ⇒ הכניסה השנייה של 19:01 נחסמת · דריל-סים ל-MODIFY_STOP לפי ממוצע.

**2 · T-41 הזנת-הוולום-המצטבר.** חותמת-שנייה=00 · מפתח ייחודי `(ts,symbol,source)` ·
מילוי-חורים מ-SCID · חסינות-חלון (כיסוי חלקי מתויג במקום None-על-חור-בודד).
קבלה: אפס חורים ב-20-הברים האחרונים לאורך סשן מלא.

**3 · T-37 מקור-הכיוון.** §D על צל-S1DayDir 25-28.08 + שני מבחני-הסיכון ⇒ אם עובר:
הכיוון ממערכת-1/IB, LSMA לשובר-שוויון בלבד, `None` מוקדם = לא-נקבע (בלי וטו-כיווני).
טסט: תרחיש-28.08 — ה-ZLR של 17:21 עובר; ותרחיש-נגד עם כיוון-DOWN חיובי נחסם.

## ראשון — הבנייה-לצל (כבוי, נמדד, לא נדלק בלי אישור)

**4 · T-39 עצירת-כיוון מוקדמת** (תקרה/רצפה כפולה ⇒ בנק · נעילת-קצה · היפוך-צד) —
לפי סעיף-2 בפקודת-התקרה. **5 · T-40 איכות-מיקום** — לפי סעיף-4א (יחסי בלבד).
**6 · T-42 המרת-ספים** ליחס-תנודתיות + פרמטרים ב-YAML. **7 · T-44 חסימת-דגל-ליום** —
day-scoped עם פקיעה אוטומטית בסגירת-הסשן, אותם כפתורים בפאנל-המחשב, כיסוי כל השערים
החוסמים (למעט קבוצת-הבטיחות), ויומן: מי חסם · מתי · מה זה שינה. **8 · T-88/T-123**
יומן-המילויים + התאמת-ברוקר אוטומטית בסגירה.

## כללים
קומיט-פר-סעיף · Rule-5 פקודה+פלט-גולמי · טסט-מוטציה לכל דגל · `RULED_FLAGS` עם ציטוט ·
snapshot לפני כל שינוי מחוץ-לגיט · **אפס הדלקות בלי cowork+מייקל** · NOT-DONE מפורש ·
LOG חתום ב-`LIVE_CHANNEL` בסוף כל סעיף · עדכון-טלפון קצר בכל סעיף שנסגר.
