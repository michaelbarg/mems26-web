# פקודת-עבודה ל-cc-macbook — כיול-אדפטיבי מלא + אוטונומיה למחר
**פסיקת-מייקל 20.08 ~21:00:** "איך המערכת תדע לבצע התאמות בהתאם לסוג היום, הנרות
והנפחים כדי כן להיכנס לעסקאות... תתקן כל תבנית בהתאם... שמחר היום יהיה אוטונומי לחלוטין."

**עקרון-על (פסיקות עומדות):** מתקנים תבנית — לא מבטלים · אפס הגבלת-שעות · כל סף
מבוטא **יחסית** (×ATR / ×IB / אחוזון-נפח), לא מספר קבוע.

## A · כיול-אדפטיבי פר-תבנית (הליבה)
מקור-האמת למספרים: `docs/reports/TODAY_TRADER_TABLE_2026-08-20.md` (היום),
`DAILY_EXTREMES_PLAYBOOK_2026-08-20.md` (פר-סוג-יום), `ORACLE_STUDY_2026-08-20.md`.
דגל אחד לכל פריט, default-OFF, RULED_FLAGS באותו קומיט, טסטים + רפליי-הוכחה.

1. **A1 · REACTIVE — כיול מלא מותנה-סוג-יום (הפריט הכי חשוב; מייקל הדגיש)**
   היום `vsa=0 rvol=0` הרגה **8 מ-8** גיאומטריות מושלמות 4/4 — כלומר התבנית זיהתה
   הכל ולא ירתה כלום. לכייל את **שלושת** הצירים, כל אחד **פר-סוג-יום**:
   - **נפח:** להחליף את הסף המוחלט באחוזון של אותו יום (`b2_vol` מול אחוזון-20/30 של
     20 הברים האחרונים). מדוד פר-סוג-יום מה האחוזון שהיה עובר: ביום-Neutral/Normal
     הנפח דועך ולכן דרישת-דעיכה נוקשה בלתי-אפשרית; ביום-Trend היא הגיונית.
   - **גיאומטריה (b1s/b2d/b3b/b4c):** סובלנות יחסית ל-ATR — נר של 2 נק' ונר של 12
     לא יכולים להימדד באותה אמת-מידה.
   - **מיקום/הקשר:** ב-Neutral (מבחן-דלטון, A5) REACTIVE מהקצה הוא **התבנית הראשית**
     של היום — שם להקל; ב-Trend_DD להחמיר (fade נגד דרייב).
   **דרישה:** טבלת-כיול פר-סוג-יום ב-YAML (לא מספרים בקוד), עם עמודת "מה היה קורה
   היום ובכל אחד מ-32 הסשנים" מטבלאות סעיף B. בלי המספר — לא לשנות סף.
2. **A2 · `b3_join` של INITIATIVE** — 6/7 בשתי נקודות-מפנה; נפל על 7.00 מול 12.60.
   להחליף בבר-המשכיות יחסי. **+$241.50/4c** היום.
3. **A3 · רצפת-סטופ `≥0.35×IB`** — #756 נכנס עם סטופ 3.5 נק' ושלושת היעדים בתוך
   נקודה; רץ 27.5 נק' והוחזר ב-$0. **+$366/4c** היום. גם רצפת-מרווח-יעדים.
4. **A4 · כיול `lsma_flat`** — חסם על 0.05 מול 0.25 את ה-REACTIVE היחיד בנקודה
   הנכונה (+$70.50). הסף חייב להיות יחסי ל-ATR ומודע-רגל.
5. **A5 · מבחן-דלטון של מייקל** — פריצת **שני** קצוות ה-IB ⇒ `Neutral`. חסר במסווג;
   היום היה מסווג נכון ב-11:40 במקום 13:30. להזין ל-F6.
6. **A6 · פלייבוק מותנה-סוג-יום** — לחווט את `RUNNER_TRAIL_V2` ואת מדיניות-הכניסות
   לפי הטבלה בדוח-הפלייבוק (Variation=כניסה-אחת+trail · Trend_Normal=N∞ · Trend_DD=סולם
   · Normal=סלקטיבי · Neutral=סולם+3 כניסות). **+$53/יום, מונע −$724.**

## B · טבלאות פר-יום (בקשה מפורשת של מייקל)
להרחיב את `scripts/daily_extremes_playbook.py` לפלט **טבלה פר-יום** בפורמט של
`TODAY_TRADER_TABLE` (שעה·כיוון·תבנית·כניסה·סטופ·יעדים·גודל·הגדלה/הקטנה·יציאה·$4c·$6c·
למה-לא-קרה) לכל 32 סשני-הלייב → `docs/reports/DAILY_TRADE_TABLES/`. משם נגזרים
הכיולים של סעיף A — כל שינוי חייב מספר מהטבלאות.

## C · אוטונומיה למחר
- להשלים את `docs/plans/READY_FOR_2026-08-21.md`; בוקר: pull→flag_guard→fire_drill 🟢.
- **T-74 (חוסם-אמת):** מסחר-ידני של מייקל באותו חשבון חסם **3 מ-4 יריות-לייב היום**.
  להביא למייקל שתי חלופות ממומשות-חלקית: (א) חשבון נפרד למערכת; (ב) `entry_guard`
  מודע-בעלות שמתעלם מפוזיציה-ידנית מסומנת. לא להדליק בלי פסיקה.
- אחרי הסגירה: `pnl_reconcile.py --mode live` + EOD חתום ב-LIVE_CHANNEL.

**סדר:** B (טבלאות) → A3+A1+A2 (הכי יקרים) → A4+A5 → A6 → C.
