# MEMS26 · טיוטות-עיצוב — 2026-06-25

לכל בעיה-מאומתת: **שורש · שינוי-מוצע (קובץ/שכבה) · regression-שיוכיח · trading-logic (→Michael) או display/safe**. לא-ממומש — מוכן-להפיכה-לפרומפט. **מאפיין-היום:** יום של **נפילת-feed @09:40 CT** (I-45) ⇒ העיצובים נסובים סביב **חוסן-תשתית** (זיהוי-מוות-feed, force-flatten, alert) ולא לוגיקת-תבניות. מספור D ממשיך מ-06-24 (D1–D33).

> ⚠️ **כל העיצובים-להלן חוסמים אבחון של 06-25 עצמו:** שורש מוות-ה-feed (D34) טרם-אומת מעבר-ל-API — דורש גישת-Mac ל-`~/SierraChart_Data/v9_export/` + `bridge.err.log` + `launchctl` (CC). **אבחן-קודם-תקֵן-אח"כ** (CLAUDE.md §Pre-LIVE Discipline).

---

## D34 — חדש 🔴🔴 (infra → CC לאבחן · trading-safety): I-45 — מוות-feed אמצע-RTH @09:40 CT
> **⚠️ זו החייאת D22 (06-19), לא עיצוב-חדש.** I-45 = הישנות-ישירה של I-38 (feed-death 06-19, פוזיציות-יתומות 186/187), שעבורו כבר-נקבע **blocker-LIVE #0 = D22 (feed-watchdog + halt-on-death)** — ש**מעולם-לא-נבנה**. D34+D35 = מימוש-D22 שנדחה. **המסקנה-העיקרית: לבנות-את-ה-watchdog לפני-LIVE** (blocker-#0 חזר-לנשוך).

**שורש (אומת-API; שורש-מדויק טרם-מאומת):** ערוץ ה-market-data עצר @~09:40 CT. ראיות: `/chart/bars5min` בר-אחרון 09:40 (vol-חלקי 6,254); `pattern-status` Bridge-feed `fresh:false/lag=9558/last_bar_ts=null`; S2+S4 `last_bar_ts=09:40/lag=20358`; **S4·Woodies (`v9_bars_5min_woodies`, SoT-חי) קפא גם-הוא** ⇒ מוות-מקור אמיתי (לא SoT-split). שני חשדות-שורש: **(א) מוות-גשר** (LaunchAgent `KeepAlive=SuccessfulExit:false` → יציאה-נקייה לא-מקימה; CLAUDE.md §LaunchAgent) · **(ב) עצירת-Sierra-export/DLL** (study הפסיק לכתוב JSON).
**שינוי-מוצע (אבחון-קודם, ללא-קוד):** CC לרוץ §הצלבה למטה ⇒ להכריע (א)/(ב). **אם (א) מוות-גשר:** שקול KeepAlive-מותנה-מתוחכם-יותר (restart-on-feed-silence) **בלי** לחזור ל-`KeepAlive=true` הגורף (אסור — CLAUDE.md). **אם (ב) Sierra:** runbook `docs/runbooks/SIERRA_DLL_OPS.md` — reload-study + Input-4 path. **שכבה:** bridge / Sierra-export (תשתית).
**regression-שיוכיח:** (אחרי-שורש) test/probe ש-`MAX(ts)` בשתי-הטבלאות מתקדם-תוך-RTH; watchdog-alert מופעל כש-`now − MAX(ts) > 660s` in-session. anti-tautology: הזרקת-fixture עם bar-gap → alert-fires.
**סיווג:** **infra-אבחון → CC**; אם-תיקון-נוגע-ל-KeepAlive/Sierra-reload → **trading-surface → Michael** (CLAUDE.md §LaunchAgent/§Service Bring-Up — אל-תפעיל שירותים בלי-אישור). **CC לאמת (Rule 5):** paste של §הצלבה (`ls -la ~/SierraChart_Data/v9_export/` mtime + `tail bridge.err.log` + `launchctl list | grep mems26` + `SELECT MAX(ts) FROM v9_bars_5min_woodies`).

**§הצלבה (CC, על-ה-Mac):**
```
ls -la ~/SierraChart_Data/v9_export/*.json        # mtime > 09:40 CT? ⇒ Sierra חי ⇒ מוות-גשר
tail -120 /tmp/bridge.err.log                       # "API push FAILED" / exit / traceback @~09:40
tail -40 /tmp/bridge.out.log ; launchctl list | grep com.mems26.bridge   # PID/last-exit-code
psql postgresql://localhost/mems26 -c "SELECT MAX(ts) FROM v9_bars_5min_woodies; SELECT MAX(ts) FROM v9_bars_5min;"
```

## D35 — חדש 🔴 (trading-safety → Michael): orphaned-open trade — אין feed-loss/EOD flatten
**שורש (תצפית-היום):** id245 (REACTIVE_LONG @09:35) נשארה `state=FILLED`/`exit_ts=null`/`pnl=0`/`bars_count=1` **6 שעות** אחרי-entry — כי ה-feed מת ב-09:40 ואין-ברים לקדם trail/stop/exit. אין מנגנון feed-loss-flatten ואין EOD-reconcile. ב-SHADOW = זיהום-נתונים; **ב-LIVE = פוזיציה-אמיתית-פתוחה שהמערכת עיוורת-אליה** (ללא-stop-management) — סיכון-חמור-ל-LIVE.
**שינוי-מוצע:** (א) **feed-loss-watchdog:** כש-`now − last_bar_ts > N×bar_interval` (N≈2–3) **ופוזיציה-פתוחה** → alert + (LIVE) force-flatten-at-market / (SHADOW) mark `outcome=INDETERMINATE,exit_reason=FEED_LOSS`. (ב) **EOD-reconcile:** ב-15:00 CT כל-trade `state=FILLED` ללא-exit → סגירה-לוגית (mark-to-last-known / INDETERMINATE). **שכבה:** `trade_manager` / gateway-lifecycle.
**regression-שיוכיח:** fixture trade-פתוח + הזרקת bar-gap>threshold → (LIVE) flatten-issued / (SHADOW) `outcome=INDETERMINATE`; EOD-tick → אין trade `FILLED`-תלוי. anti-tautology: feed-רציף → אין flatten.
**סיווג:** **trading-logic/safety → Michael** (force-flatten = פעולת-risk). **CC:** האם קיים flatten-on-disconnect כלשהו ב-`trade_manager`; נתיב-ה-lifecycle.

## D36 — חדש 🟡 (display/observability → CC, safe): I-46 — דגל-freshness משקר post-close
**שורש:** S2+S4 `data_freshness.fresh=true` למרות `lag=20358s ≫ threshold=660s`; הגשר (threshold=90) נכון `false`. סביר: freshness-S2/S4 מותנה-`in_session` ⇒ post-close "טרי-by-exception". התוצאה: ה-stall מוסווה בכל תצוגת-post/pre-session.
**שינוי-מוצע:** הפרד 2 שדות — `fresh_for_trading` (in-session gate, נשאר) **+** `data_current` (תמיד `lag≤threshold`, ללא-תלות-session). הצג שניהם; alert על `!data_current` in-session. **שכבה:** `build/pattern-status` data_freshness (display/observability).
**regression-שיוכיח:** post-close עם lag-גדול → `data_current=false` (לא-עוד `fresh:true` בלעדי); in-session-gate ללא-שינוי. **סיווג:** **display-safe** (תצוגה; לא-נוגע-gating). **CC לאמת:** ש-in-session 09:40–15:00 ה-`fresh` של S2/S4 **כן** היה false (⇒ חסם-ירי-על-קיפאון).

---

## עיצובים-נמשכים (status-delta — ר' 06-24 לפירוט-מלא)

## D9 — מעודכן 🔴 (verify→DB): I-32 gap-ids — **id 244 חסר היום**
**נמשך (יום-N):** 244 חסר (yesterday 243; today 245). דפוס-קבוע (229/234 ב-06-23/24). עיצוב: לוג-INSERT-fail מפורש (no-silent-fail) + `SELECT id FROM v9_trades WHERE id BETWEEN 243 AND 246`. **verify→DB.** CC: מה-קרה ל-244.

## D-gateway-counters — נמשך 🟡 (display-safe): I-23
`gateway.trades_today=0`/`daily_pnl=0` מול 1-ירי (`shadow_active_count=1` **כן**-תקין — סופר-פתוחות). counter-ירי מנותק-מ-shadow. עיצוב: ספור shadow ב-`trades_today`/`daily_pnl` או סמן "live-only". **display-safe.**

## D30/D31/D32 — נמשכים (06-24, **לא-נבדקו היום** — feed מת)
**D30 (I-43 CVD per-bar-delta)** · **D31 (I-44 day-type source-unify)** · **D32 (S4-reversal-bleed)** — שלושתם trading-surface→Michael, נדחפו-ב-`c98b808`, **דורשים replay-אימות**. **היום בלתי-נבדקים** (1 ירי, feed-frozen, אין S4-fires). נשארים-פתוחים לאימות ביום-feed-תקין הבא. **אל-תסמן closed בלי replay** (Rule 5).

## D11 — נמשך 🔴 (verify→trading): I-34 sizing-dead — **לא-נבדק היום**
id245 sizing=2 אך `pnl=0` (לא-נסגרה) ⇒ אי-אפשר-לאמת sizing→contracts→PnL היום. נשאר חוסם-LIVE. ר' 06-24.

## D1/D2/D24/D29/D33 — ללא-ראיה-חדשה היום (feed-frozen)
**D1 (I-22 pnl_r)** · **D2 (I-31 fire-count)** — *הערה:* `fired_today_count` תאם-DB היום (1/0, ≠over-count); **לא-סגירה** (n=1). · **D24 (I-40 corrupt-bars)** — 0 ברי-זבל-נראים (אך feed-frozen ⇒ מבחן-לא-תקף). · **D29 (gate-state per-fire)** · **D33 (HFE/Nontrend-disable verify)** — בלתי-נבדקים (יום-קטוע).

---

**טבלת-עדיפות לעיצובי-היום:**

| D | בעיה | סיווג | בעלים | דחיפות |
|---|------|-------|-------|--------|
| **D34** | I-45 מוות-feed @09:40 (root: גשר מול Sierra) | infra-אבחון | **CC** → Michael | 🔴🔴 #1 (חוסם-הכל) |
| **D35** | orphaned-open trade (אין feed-loss/EOD flatten) | trading-safety | **Michael** | 🔴 #2 (חוסם-LIVE) |
| **D36** | I-46 freshness-flag-post-close | display-safe | CC | 🟡 #3 |
| D9 | I-32 gap 244 | verify→DB | CC | 🔴 נמשך |
| D-cnt | I-23 gateway counters | display-safe | CC | 🟡 |
| D30/D31/D32/D11/D33 | 06-24 carry — **לא-נבדקו** (feed מת) | trading/verify | Michael+CC | ⏸️ ממתינים ליום-feed-תקין |
