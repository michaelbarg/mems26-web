# פקודה · 08.09 07:15 — לפני הפתיחה: עץ נקי · דלתא מתחברת · 2 גלאי-צל

**מאת:** cowork-dev · **אל:** cc-macbook · **דדליין: 15:00** (‏15:00–16:10 = אימות-cowork + ריסטארט).

**מצב מאומת 07:09 (‏`git log` + `ps` + DB):** HEAD `ac420f24`. מהלילה שלך **נכנסו ל-HEAD**: ZLR→צל
בנקודת-החנק `route_setup` (‏`trading_gateway.py:759-776`, `716c7dfe`) · §3 (`1d4f9ae9`) · §9ב (`27bbf1fd`) ·
T-251 (`71e47cd1` + טסטים `ac420f24`). **שום דבר מזה לא רץ:** הבקאנד PID 58492 עלה **07.09 15:36:45**.
הריסטארט שלי (לפני 16:10) הוא מה שיטען אותם — **מהדיסק, לא מ-HEAD**. ומכאן פריט 1.

---

## 1 · P0 — עץ-עבודה נקי עד 15:00

```
 M backend/v9/gateway/trading_gateway.py     ← #1191 monotonic guard, :3430-3450, בלי טסט, לא-committed
 M config/news_calendar.yaml                 ← 14 שורות, בלי הסבר
?? docs/reports/postmortem/PM_1176..1183.md  ← 6 קבצים
```
**הריסטארט טוען את הדיסק.** שומר לא-מבודק ולא-committed נכנס ללייב בשקט — מחלקת T-263 הפוכה.
**לכל אחד: או טסט+קומיט, או `git checkout --`.** לשומר-#1191: טסט התנהגותי — סולם הפוך
(‏t1=7702, t2=7702) ⇒ `t2 is None` + warning · סולם מונוטוני ⇒ ללא שינוי · **מוטציה:** הסרת
`_mdist <= _prev_dist` ⇒ נכשל. `news_calendar.yaml` — קומיט עם הסיבה בהודעה, או revert.
**קבלה:** `git status --short ⇒ ריק` ב-15:00. אני בודק את זה ראשון.

## 2 · §7 `DELTA_BREAKOUT_RELEASE_V1` — מת: הדלתא לעולם לא מתחברת. עמודה אחת.

```
trading_gateway.py:2599   SELECT high, low, close, volume FROM v9_bars_5min_woodies   ← אין ts
release_gate.py:256       if delta_map and r.get("ts"):                              ← קורא ts ⇒ None ⇒ delta=None תמיד
trading_gateway.py:2658   SELECT ts, delta FROM v9_bars_cumulative_delta             ← המפה נבנית, ואין למה להצמיד
```
**‏§7 מעולם לא העריך בר עם דלתא.** ה-`shadow` מ-06.09 מדד אפס. **וזה בדיוק גלאי-"בר-הקבלה"**
מ-`docs/reports/DALTON_EARLY_ENTRY_2026-09-07.md` — הרעיון נכון, הצנרת שבורה.

**תיקון:** `ts` ב-SELECT של :2599. **אומת:** שתי הטבלאות `timestamp with time zone`, אותו driver ⇒
מפתחות `str(ts)` תואמים; דלתא 07.09 = 42 שורות/42 ייחודיות 16:30–19:55 (בלי כפילות-3ש' ביום הזה;
בימים קודמים יש — ראה הדוח). **טסט:** `test_delta_breakout_release.py` בודק `check_release` עם מפה
סינתטית — **לכן ירוק על צנרת מתה.** להוסיף אחד: `_rg_read` מזויף שמחזיר שורות לשתי השאילתות ⇒
`_rg_bars[i].delta is not None` **דרך `route_setup`**, לא דרך `check_release`. **מוטציה:** הסרת `ts` ⇒ נכשל.
הדגל נשאר `shadow`. **קבלה בלייב מחר:** לוג `delta-breakout` עם ערך-דלתא לא-None לפחות פעם.

## 3 · שני גלאי-צל — מדידה, לא סיכון (‏`shadow_only=True` דרך `route_setup:776`, אפס `op=PLACE`)

**הבסיס:** `DALTON_EARLY_ENTRY_2026-09-07.md`. ‏11 ימים: **‏7/10 גלאי ראה את בר-הכסף ונחסם.** הכניסה
"המוקדמת" של דלתון (בר-2 של הדרייב) **הפסידה 3/4**; הכסף בבר-הקבלה 20–90 דק' אחרי הפתיחה.
**‏n=4/5, ספים בדיעבד ⇒ צל 20 יום, ואז מספר, ואז פסיקה.** לא לייב.

| דגל | תנאי-ירי (על בר **סגור**, `ts ≥ 16:30 IL` — הדלתא ב-DB כפולה 3ש' מוקדם בימים ישנים) | n | איפה |
|---|---|---|---|
| **‏`RE_ACCEPTANCE_V1`** | ‏\|delta\| ≥0.7×מקס-סשן **∧** vol ≥0.7×מקס **∧** close ברבע-הקיצוני **∧** חוצה קצה (IB/VA/open) **∧** בכיוון-הגאפ | ‏4/4 t1 · דוחה 6/6 | גלאי חדש, לא נתיב-שחרור. הצמדת דלתא = פריט 2 |
| **‏`FAILED_RE_IB_V1`** | `failed_break.py` הקיים (‏VA בלבד, צל) — **וריאנט על קצות-IB** + תנאי-גאפ | ‏5/5 t1 | הרחבה, לא קובץ חדש. יעד = IB-mid |

**‏RULED_FLAGS:** `expected: "shadow"`, `ruled_by: מייקל`, `date: 2026-09-06/07`, note: *"06.09: 'נעזרים
בקולמטיב/ווליום כדי להיכנס בזמן' · 07.09: 'תבניות ושלבי-כניסה לפי דלתון' · DALTON_EARLY_ENTRY n=4/5 ⇒
צל 20 יום"*. **default OFF בקוד.** טסט לכל אחד: בר-שעומד-בתנאים ⇒ setup עם `shadow_only=True` ·
כמעט-בר (‏delta 0.6×) ⇒ אין · **מוטציה** לכל תנאי. **‏`PULLBACK_REARM` — לא לבנות** (‏EV≈0).

## 4 · לא היום — של מייקל, לא שלך

‏§4 `STOP_MOVE_TARGET_RESTORE_V1=0` נשאר: השורש הוא הגדרת-סיירה *Maintain Same Offset* (מייקל, לפני 15:30) ·
‏`manual_position_ack.json` → `2026-09-08` (מייקל) · DLL — אפס · `CONFLUENCE_RI_ZLR` — פסיקה.

---

## סדר: **‏1 → 2 → 3** · דיווח אחרי כל פריט עם **פלט גולמי** (‏`pytest -q` + `git log -1`)

**‏15:00:** `git status ⇒ ריק` · `scripts/guard_tests.sh` ירוק · `flag_guard` PASS · `--re` לכל id פתוח
ב-`LIVE_CHANNEL`. **אם 1+2 לא ירוקים ב-15:00 — תגיד. לא לדחוף חצי.** ‏3 יכול להיכנס אחרי הפתיחה
(צל, ריסטארט-ערב) — **‏1+2 לא.**

## אסור

ריסטארט (שלי) · `.env` בלי `mems26_snapshot.sh` · לגעת ב-`awaiting_release` / `entry_not_confirmed` / הסלוט
(מגנים: −$416 / −$776) · להדליק §7 או גלאי-צל ל-`1` · טסט דרך `check_release` כהוכחה ל-`route_setup` ·
טסט-grep · `git stash` אחרי 16:10.
