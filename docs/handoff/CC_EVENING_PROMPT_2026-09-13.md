# תור-הלילה 13.09 (אחרי 23:00) — **ליל יום-ראשון: השוק סגור, ונפתח בעוד ~1:45**

**נכתב ע"י `cowork-scheduled`, נמדד 23:06-23:15 (‏`date` נקרא ברגע-הכתיבה).**
לא היה קובץ-ערב ל-13.09 — נכתב כאן, כמו ב-09.09. הסדר להלן הוא הסדר הפסוק
(‏DB → מחזיק-`S1-BINARY` → שערים → ריסטארט-צל → resolver/WHATIF → fills/Redis → דוח-EOD),
אבל **ליל-סופ"ש חותך ממנו את כל מה שדורש נתוני-RTH** — ולכן סעיף שאין בו מה למדוד
נרשם כ**"אין מה למדוד, וזו הסיבה"**, ולא ממולא בהמצאה.

---

## מצב-פתיחה (נמדד, לא מזיכרון)

```
23:06-23:15  health 200 / 2.9ms · backend PID 39331 (מ-11.09 18:29:18, uptime 189,590s)
             sierra_state age 0.4s          ⇒ הכותב חי גם בשוק סגור (המפריד מול פיד-מת)
             position_qty=0 · working_orders=0 · tm_open_trades=0 · flat בשני הצדדים
             is_sim=0 · armed=1 · closures_today=0 · day_pnl $0.00
flag_guard      rc=0  (252 דגלים, כל דגל-ON עם ≥1 אתר-קריאה)
task_log_guard  rc=0  (339 פריטים)            ← שני ה-rc נלכדו ישירות, לא דרך `| tail`
```

**שעון-השוק:** ‏MES נפתח א' `18:00 ET` = ב' `01:00 IL`. בזמן כתיבת הקובץ נותרו `~1:45`.
זה **אילוץ-קשיח על הלילה:** כל פעולה שנוגעת בתהליך-הרץ חייבת להסתיים הרבה לפני הפתיחה,
ולכן §4 (ריסטארט) **מבוטל הלילה** — ראו שם.

---

## 🔴 §1 · DB — **[[T-350]]: להפריד בין שתי ההשערות, בקריאת-קוד. זו משימה מספר 1.**

T-350 נפתח הערב (‏22:49) עם המדידה: `pnl_sierra` קיים ב-**6/6** מעסקאות-הלייב של
`09-07..09-09` ו-**0/3** של `09-10..09-11` — ולשלוש הלא-מתומחרות **יש** רשומות-מילוי-יציאה
(‏4/2/2) בדיוק באותה צורה כמו לקבוצת-הביקורת המתומחרת (2,2,2,2,4). כלומר **המילויים הגיעו
והשדה-הנגזר פשוט לא נכתב**. זה לא קוסמטי: בקבוצת-הביקורת `pnl_sierra` **חולק** על `pnl_usd`
ב-3 מ-5, כולל `103.75` מול `86.25` בעסקה בודדת ו-`377.50` מול `358.75` בסכום — ולכן היעדרו
משאיר את P&L של `09-10`/`09-11` נשען על הרישום-שלנו, המקור המוטה של [[T-256]].

**מה שכבר ידוע ואין לחזור עליו** (‏cowork 22:49 + איתור-נתיב 23:14):

```
נתיב-הכתיבה:  backend/v9/services/sierra_pnl_reconcile.py
docstring:    "T-10 — fill `v9_trades.pnl_sierra` from the Sierra fills journal."
נגיעות נוספות: services/daily_pnl.py (קורא) · services/exit_verifier.py:300 (מזכיר)
              db/models/trades.py:51 (עמודה) · migrations/023_pnl_sierra_column.py
grep pnl_sierra --include=*.py backend/ scripts/  ⇒  103 התאמות
```

**⚠️ תיקון-עצמי של cowork, 23:15 — לקרוא לפני שמצטטים את T-350:**
ניסחתי את הראיה כ"יש להן `exit_fills` בשורת-ה-DB". **אין עמודה כזו.** מפקד-סכימה:
```
$ information_schema.columns WHERE table_name='v9_trades' AND (…'%fill%'|'%exit%'|'%pnl%')
  ⇒  exit_price  exit_ts  exit_reason  pnl_r  pnl_sierra  pnl_usd      ← אפס עמודת-fills
$ information_schema.tables  WHERE …'%fill%' OR …'%activity%'          ⇒  אפס טבלאות
```
**🔴 תיקון-לתיקון (‏cowork, 23:42) — הראשון שלי היה חצי-שגוי, והחצי השגוי הוא המסקנה.**
מ-"אין עמודה שטוחה" הסקתי *"המילויים אינם ב-Postgres בכלל, הם ב-JSONL"*. **לא נכון.**
המפקד שלי רץ על `information_schema.columns` בלבד והוא **עיוור לקינון** — המילויים **כן**
ב-DB, בתוך `quality` (‏JSONB):
```
SELECT id, jsonb_array_length(quality->'exit_fills') FROM v9_trades … ⇒ 2·2·4·4·2·2·2·2
```
בדיוק [[feedback_verify_json_structure_before_claiming]] — **מפקד-עמודות-שטוח אינו מפקד-סכימה.**
הטענה "המילויים הגיעו" נכונה, אבל **המקור לצטט הוא `quality->'exit_fills'`**, לא הקובץ.

**האימות העצמאי של הפער (‏23:15, גולמי):**
```
 id  |     d      | state  | outcome  | pnl_usd | pnl_sierra
1512 | 2026-09-11 | CLOSED | WIN      |   38.75 |   (NULL)
1498 | 2026-09-11 | CLOSED | UNPRICED |  (NULL) |   (NULL)
1409 | 2026-09-10 | CLOSED | LOSS     |  -33.75 |   (NULL)
1343 | 2026-09-09 | CLOSED | LOSS     | -131.25 |   -137.50   ← חולק על pnl_usd
1328 | 2026-09-09 | CLOSED | LOSS     |  -40.00 |    -40.00
1262 | 2026-09-08 | CLOSED | LOSS     |  -52.50 |    -52.50
1231 | 2026-09-08 | CLOSED | LOSS     | -103.75 |    -86.25   ← חולק
1224 | 2026-09-08 | CLOSED | LOSS     |  -50.00 |    -42.50   ← חולק
1191 | 2026-09-07 | CLOSED | WIN      |    2.50 |      2.50
```
‏**0/3 מול 6/6, ו-3 מ-6 חולקות** — הפער אינו תיאורטי.

**השאלה היחידה של הסעיף — שתי השערות, מפריד אחד:**

| # | השערה | מה יפריך אותה |
|---|-------|----------------|
| א | הכתיבה היא **אוטומטית בסגירת-עסקה**, והיא נשברה ב-`09-10` | מפקד אתרי-קריאה: מי קורא ל-`sierra_pnl_reconcile`? אם יש קורא על מסלול-הסגירה — לקרוא אותו ולמצוא את הענף שמחזיר בלי לכתוב |
| ב | הכתיבה היא **כלי-אצווה** שפשוט לא הורץ (הערת [[T-192]]: "הכלי-הממלא מעולם לא הורץ") | אם אין ולו קורא-ייצור אחד — ההשערה (א) מתה, והשאלה הופכת ל"מי היה אמור להריץ, ומתי הורץ לאחרונה" |

**הפלט הנדרש (‏Rule-5 — פקודה + פלט גולמי, לא "אומת"):**
1. **מפקד אתרי-קריאה** `file:line` לכל יבוא/קריאה של המודול ושל הפונקציה הראשית שבו —
   כולל `scripts/`, LaunchAgents, ו-`crontab`. **"נסרק, אפס קוראי-ייצור" היא תשובה קבילה**
   אם היא מגובה בפקודה ובפלט.
2. **הכרעה בין א ל-ב**, או אמירה מפורשת שהיא לא הוכרעה ולמה.
3. אם (א) — **אל תתקנו הלילה.** לתעד את `file:line` של הענף ולעצור: זהו קוד על מסלול-סגירה
   של עסקאות-לייב, והשוק נפתח ב-`01:00`. תיקון-בלילה בלי ריפליי הוא בדיוק
   [[LEARNING_DOCTRINE]] §"הוראה חדשה ⇒ קודם ריפליי".
4. **`NOT-DONE` מפורש** לכל מה שלא נסגר.

**סגירת T-350 אסורה הלילה.** המקסימום הוא הורדתו ל-🟠 עם מנגנון-מוכח.

---

## 🟠 §2 · מחזיק-ה-`S1-BINARY` — **צפוי אפס, וזו תשובה, לא כישלון**

ב-09.09 הסעיף היה מפקד `[S1-BINARY] HELD` מ-`/tmp/backend.err.log` (‏35 שורות, 31 `HELD`).
**הלילה השוק סגור ואין ברים חדשים מאז בר-סגירת-שישי**, ולכן מפקד-היום אמור להיות `0`.

**המשימה היא המפריד, לא המספר:** מפקד מעוגן-תאריך על `^2026-09-13` **עם ביקורת-חיובית**
דרך אותו פרסר על `^2026-09-11` (יום-מסחר). אם `09-11` מחזיר `>0` ו-`09-13` מחזיר `0` —
**האפס אמיתי** ולא עיוורון-פרסר. אם גם `09-11` מחזיר `0` — **הפרסר שבור, וזה הממצא**.
(‏הלוג הוא `backend.err.log` ולא `backend.log`.)

---

## 🟠 §3 · שערים — **[[T-345]]: להפוך "קוד-מסחר לא-מאומת" למדוד. בלי לקמט.**

עבודת-`T-328` של cc יושבת לא-מקומטת: `M five_min_system.py` (+83) + `?? ceiling_touch2.py`
+ `?? tests/test_ceiling_touch2.py`. הסיכון הרשום הוא לשער-קדם-הפתיחה של מחר.

**מה שכבר מוכח (‏cowork 18:44, ניסוי מבוקר ב-clone ב-`/tmp`, אפס נגיעה בריפו החי):**
`git pull --ff-only` (זו הפקודה שהשער באמת מריץ — `MEMS26_CONTROL.command:128`) **נכשל רק אם**
הקומיט-הנכנס נוגע ב-`five_min_system.py`; ‏`--rebase` נכשל תמיד. `HEAD...@{upstream} = 0 0`
כלומר origin לא זז — **העלות עדיין אפס והכשל סמוי**.

**מה שנמדד הערב ומשנה את תמונת-הסיכון:** הקוד הלא-מקומט **אינרטי** —
```
grep CEILING_FLIP_TOUCH2 .env   ⇒  לא קיים
הקוד:  os.getenv("CEILING_FLIP_TOUCH2_V1", "0")  ⇒  ברירת-מחדל כבויה
```
כלומר הוא אינו יכול לירות גם אם ייטען. **זה מקטין את סיכון-הטעינה, לא את סיכון-ה-pull.**

**המשימה — צרה ומדידה:** להריץ את `backend/v9/tests/test_ceiling_touch2.py` (קיים, `2,995` בתים,
mtime `13.09 14:13`) ולדווח **פלט-גולמי** של pytest: עבר/נפל, כמה, ואילו.

**🚫 איסור מפורש:** **לא לקמט** את העבודה הזו ולא לייצר קומיט על `five_min_system.py`.
מבחן ירוק הופך "לא-מאומת" ל"מאומת-ביחידה" — הוא **אינו** אישור-לייב, ו-T-328 §3/§4 עדיין
פתוחים. ההחלטה לקמט היא של cc/מייקל, לא של תור-הלילה. אם המבחן נופל — **זו הידיעה**,
ולדווח אותה כפי שהיא.

---

## ⛔ §4 · ריסטארט-צל — **מבוטל הלילה. הסיבה, לא רק ההחלטה.**

הכלל אוסר ריסטארט `16:10-23:00`; עכשיו אחרי `23:00`, ולכן הוא **מותר טכנית**. הוא **מבוטל
בכל זאת** משתי סיבות נמדדות, וזה תיעוד-שיקול ולא עצלות:

1. **התהליך הרץ (‏PID 39331, מ-11.09 18:29:18) מחזיק קוד שאינו עץ-העבודה.** ריסטארט יטען את
   `+83` השורות הלא-מאומתות של `T-328` לתוך התהליך שסוחר. הן אמנם אינרטיות (‏§3), אבל
   "אינרטי לפי דגל" אינו "אומת", ו-**השוק נפתח ב-`01:00`**.
2. **אין מה להרוויח:** אפס פוזיציות, אפס עסקאות היום, `health 200@2.9ms`, `uptime 189,590s`
   בלי תקלה. ריסטארט הוא כאן סיכון בלי תמורה.

**מי שבכל זאת סבור שצריך ריסטארט — עוצר ושואל את מייקל.** אל תריץ.

---

## 🟠 §5-6 · resolver/WHATIF · fills/Redis — **כבר נמדדו הערב; לא לחזור**

* **[[T-347]]** (‏`FillPoller` מכריז `BLIND`) — כבר מאופיין במלואו ב-`19:45` ו-`22:55`, כולל
  המפקד-לפי-שעה שמוכיח **אפס הכרזות-`BLIND` בכל חלון-המסחר של שישי**. השורש הוא
  **היעדר-מקור** (סיירה לא יצרה `TradeActivityLog_2026-09-13`), לא כותב-שבור.
  **אל תכבו את הדגל** (פסיקת-מייקל 27.07). הצעד שייך ל**שער-הבוקר** של מחר, לא ללילה:
  לאמת שהקובץ נוצר ושה-JSONL מתקדם **בתוכן** (‏`scan_ts` חדש בפנים) ולא רק ב-`mtime`.
* **[[T-349]]** (ארכיון-הליגר לא-אחיד) ו-**[[T-348]]** (‏`21.4%` מהפסדי-הצל מוקצים בכלל-סדר)
  נמדדו הערב ואינם צריכים ריצה שנייה.

---

## §7 · דוח-EOD לטלפון

**מטופל ע"י `cowork-scheduled` עצמו** — לא משימת סוכן-המשנה.

---

## כללי-הכנות — מחייבים כל מי שעובד מהקובץ הזה

* **Rule-5:** פקודה + פלט-גולמי. "אומת" / "עובד" / "תוקן" בלי פלט = לא נספר.
* **קומיט לכל סעיף** (‏למעט §3, שבו קומיט **אסור**), ו-**`NOT-DONE` מפורש** לכל שארית.
* **snapshot לפני כל נגיעה מחוץ-ל-git** (‏DLL / `.env` / LaunchAgents): `scripts/mems26_snapshot.sh`.
* **בלי הדלקות שלא נפסקו.** דגל חדש ⇒ עצירה-אסטרטגית ושאלה למייקל. גם דגל שנראה "ברור".
* **אפס נגיעה** בפוזיציות · בסלוט · בתור-הפקודות · ב-`.env` · ב-`~/SierraChart*` (קריאה-בלבד).
* **אל תמציאו ערך חסר.** מקור שותק ⇒ `None`/"חסר" עד הצרכן ([[Rule-1 כשל-כן > ערך-סינתטי]]).
* **המכנה חייב להסתכם.** כל פילוח מדווח רק אם החלקים מסתכמים לדנומינטור שנמדד.
