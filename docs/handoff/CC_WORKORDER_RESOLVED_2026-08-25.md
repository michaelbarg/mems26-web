# פקודת-עבודה · `RESOLVED` + MFE/MAE — עכשיו

**מאת:** cowork-dev · **אל:** cc-macbook · **תאריך:** 2026-08-25 14:20 IL
**פסיקת-מייקל:** *"למה לא עכשיו? למה לא לסדר את זה."*

## למה זה בטוח לבנות בזמן שהמערכת רצה

לפי `CANDIDATE_LEDGER_CONTRACT.md` §9, ה-resolver הוא **סקריפט EOD מחוץ לנתיב-הירי** —
*"מחוץ לנתיב-הירי (סקריפט/EOD), לא ב-`route_setup`."*
הוא לא נוגע בזיהוי, בשער, בהזמנות, בסטופים, ביעדים או בגודל. **אפס סיכון-מסחר.**

**אל תיגע ב-`.env`. אל תריסטארט. אל תיגע ב-`trading_gateway`/`five_min_system`/`woodies_system`.**

## למה זה דחוף

הליגר נדלק הערב בריסטארט של 15:45 — **הסשן הראשון שבו הוא כותב.** בלי resolver הוא
ירשום **מועמדים בלי תוצאות**. ‏`grep RESOLVED\|mfe_3` בקוד-הייצור היום → **אפס**.
ושער-ה-300 של Task 6 נמדד ב-**outcomes**, לא במועמדים — כלומר בלי זה המונה נשאר על 0
גם אחרי שהליגר ידלק.

*(הנתונים לא אובדים — ה-JSONL נשמר ואפשר לפתור רטרואקטיבית. אבל אין שום סיבה לחכות.)*

---

## מה לבנות — `scripts/candidate_resolver.py`

### קלט
1. אירועי `DETECTED` של הסשן מ-`gateway_decisions.jsonl` + `decisions_archive/`.
2. **ברים קנוניים** — `v9_bars_5min_woodies`. **לא** `v9_bars_5min` (מזוהם; ‏SoT).

### חישוב
לכל candidate, מרגע ה-decision, בכיוון ובמחיר-הכניסה שלו:
- `mfe_3` / `mae_3` · `mfe_6` / `mae_6` · `mfe_12` / `mae_12` (בברי-5-דק')
- `t1_before_stop` — האם ה-t1 נגע לפני הסטופ
- `resolved_at`

**‏ADAPT, אל תבנה מנוע חדש.** `scripts/good_pattern_gates.py` כבר מחשב MFE/MAE מברים —
השתמש בו. אם לא מתאים, נמק בשורה אחת ו-ADAPT את `replay_f5_runner_trail`.

### פלט
אירוע **`RESOLVED` אחד** לכל candidate, **אידמפוטנטי**, לאותו `gateway_decisions.jsonl`.
אותו `candidate_id`. **אסור** לשנות שורות `DETECTED`/`GATE_DECISION` קיימות.

### איכות — כלל-1 חל במלואו
סשן שנכשל באימות-האיכות של `backend/v9/replay/` (‏**25 מ-34 נכשלים היום**) מקבל:
```
outcome_status = "NOT_JUDGEABLE"
reason_codes   = [...]
```
**והוא לא נזרק.** אל תמלא MFE/MAE מברים שנכשלו באימות — זו סינתזה של ערך חסר.
זו הנקודה הכי חשובה בפקודה הזו: resolver ש"פותר" 34 סשנים בעוד 25 מהם פגומים
ייצר 300 outcomes מזויפים ויפתח את שער-ה-Ranker על שקר.

---

## מבחני-קבלה (חוזה §10 · 12)

1. **הרצה חוזרת = אפס שורות חדשות** (אידמפוטנטי).
2. סשן `NOT_JUDGEABLE` **נרשם** ולא נזרק, עם `reason_codes`.
3. אפס שינוי בשורות קיימות — `diff` על הקובץ לפני/אחרי מראה **רק הוספות**.
4. ‏pytest כותב **0** שורות ל-JSONL החי.
5. אינו מיובא מ-`route_setup` ואינו רץ בתוך הבאקנד — `grep` שמוכיח.
6. ‏`candidate_id` תואם בדיוק ל-`DETECTED` המקורי.

## הרצה ראשונה — על מה שכבר קיים

הרץ אותו על הארכיון הקיים (‏08-19 · 08-20 · 08-21 · 08-24) ודווח:
כמה candidates · כמה נפתרו · כמה `NOT_JUDGEABLE` ולמה · והתפלגות MFE/MAE.
**זה גם המבחן וגם הנתון הראשון שלנו.**

*(שים לב: ב-4 הימים האלה הדגל היה כבוי, ולכן אין אירועי `DETECTED` — רק שורות-החלטה
legacy. אם אין מה לפתור, אמור זאת במפורש והרץ במקום זאת על שורות-ההחלטה הקיימות
כדי להוכיח את הצינור. אל תדווח "עבר" על קלט ריק.)*

---

## מסירה

`docs/reports/CC_RESOLVER_2026-08-25.md` — פקודה+פלט גולמי לכל מבחן · file:line ·
NOT-VERIFIED מפורש. LOG ב-`LIVE_CHANNEL`. `commit`+`push`.

**אסור:** `.env` · ריסטארט · שינוי בנתיב-הירי · טבלה שלישית · JSONL שני ·
מילוי ערך חסר · `logger.debug` על כשל.
