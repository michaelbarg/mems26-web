# CC_NOW 22.09.2026 — הזמנת-עבודה ל-cc-macbook (cowork-dev, 11:10 IL)

**מייקל 22.09 10:55:** *"תיק-המוכנות מלא משימות לא רלוונטיות ומלא משימות לקלוד קוד — תעבור עליהם היטב ובחוכמה, ואם יש לקלוד קוד משימות לביצוע תתקן לו."* ⇒ 263 פריטים ישנים הועברו לארכיון (⚪) ב-TASK_LOG; מה שנשאר ל-cc הוא הרשימה הזו — **12 פריטים, לפי סדר-הכסף**, כל אחד עם גולדן ואימות. כללי-cc כרגיל: בלי שאלות · `git -C` · פריט = קומיט + TASK_LOG + STATUS_BOARD + LOG חתום עם פלט גולמי (Rule 5) · **אפס נגיעה ב-.env, בדגלי-גודל, ב-RISK_*, בפוזיציות, בריסטארט** · הרנס רק מחוץ ל-RTH (16:30–23:00 IL) · `SITUATION_VECTOR_LOG_V1=0` בכל הרנס.

## פריט 1/12 · T-438 — ירייה חיה נדחתה על סולם לא-מונוטוני ולא נרשמה (🔴)
21.09 18:30:03: `T-335 LADDER INVALID: SHORT targets [7782.5, 7786.0, 7779.0]` ⇒ PLACE נחסם, `#2038` UNPRICED. שלוש שורות קודם `STRUCT_TARGETS_WIN` נתן סולם תקין `7782.50/7768.50/7754.50`; `[TargetSpacing] SHADOW would-be: t2 7786.00 dropped`.
1. **מקרה-ריפליי** (דוקטרינה 09.09: תקרית ⇒ מקרה): טסט שמשחזר את הסולם שהגיע ל-`sierra_command.py:920` ב-21.09 11:30 ET ונכשל, ומוסיף אותו ל-`guard_tests.sh` כך ש-`T-335=0` נשאר תנאי-שער.
2. **לקרוא את שרשרת-המפיקים** בין `§3 STRUCT_TARGETS_WIN` ל-PLACE ולזהות מי כותב `t2=7786.00` (קריאת-קוד, לא ניחוש). לספור `LADDER INVALID` בלוגים מאז 20.09 (‏`docs/handoff/eod/*/OPS_LOG_*.md` — הלוגים החיים נמחקים בריבוט).
3. **תיקון:** `target_spacing` (או המפיק) **אוכף צד-נכון** לפני PLACE — יעד מהצד הלא-נכון נזרק, לא נשלח; אם אחרי הזריקה נשאר רק T1 — PLACE ממשיך עם T1 בלבד (חוזה-1 ממילא צריך רק T1). **דחיית-PLACE נרשמת ב-`v9_trades`** (`exit_reason=ladder_invalid` כבר קיים — לוודא שגם `blocked_by` בפיד-ההחלטות אינו `None`).
**אימות:** הטסט ירוק · הרנס 21.09 ⇒ `#2038`-המקביל מקבל PLACE ok עם T1 7782.50 · guard_tests ירוק.

## פריט 2/12 · T-436 — הספרים והברוקר מעולם לא הושוו: `pnl_sierra` ריק בכל שורה (🔴)
1. `scripts/reconcile_broker.py` (חדש, קריאה-בלבד ב-DB חוץ מכתיבת `pnl_sierra`): לכל עסקת-לייב סגורה — למצוא את הפילויים ב-`~/SierraChart_Data/v9_export/trade_activity_events.jsonl` + `trade_fills_journal.jsonl` לפי `sierra_order_id`/`c1_target_id`/`c1_stop_id` ולחשב `pnl_sierra` (נטו אחרי עמלות אם קיימות באירועים); אי-התאמה > $5 ⇒ שורת WARNING.
2. להריץ אחורה על 09.09→היום; להדפיס טבלה: `id · pnl_usd · pnl_sierra · Δ`. **אפס המצאה:** אין פילוי ⇒ NULL ונשאר NULL.
3. לחבר ל-`eod_handoff` (LaunchAgent 23:05) כך שכל ערב הספרים מתיישבים.
**אימות:** `select count(*) from v9_trades where mode='live' and exit_ts >= '2026-09-15' and pnl_sierra is null` ⇒ 0 (למעט עסקאות בלי פילוי, מודפסות בשם).

> **🔴 תיקון להזמנה — cowork-dev 22.09 23:15 (מדידת-EOD, פלט גולמי ב-LIVE_CHANNEL):** סעיף 1 כפי שנכתב **ייכשל בדיוק על השורה הכי חשובה.** ‏`trade_fills_journal.jsonl` **אינו** מכיל את רגל-היציאה של סקרצ'-MAE: היום יש בו 5 שורות בלבד — `ENTRY 11318 · ENTRY 11322 · T1 11323 · ENTRY 11325 · T1 11326` — ואילו הזמנת-הסגירה של `#2106` (‏`11321`) **חסרה** (`grep -c 11321 ⇒ 0`). ⇒ רקונסיילר שמסתמך על הז'ורנל בלבד יחזיר `NULL` לכל סקרצ'-MAE (3/3 בלייב ב-14 יום), יעבור את תנאי-האימות ("עסקאות בלי פילוי, מודפסות בשם") — **וישאיר את הפער הגדול ביותר בלי הסבר.**
> **המקור הקנוני הוא סיירה עצמה** (כמו ב-[[T-227]]): `~/SierraChart/TradeActivityLogs/TradeActivityLog_<date>_UTC.37138283.data`, שדה `Closed Trade Profit/Loss`. היום הוא נותן **שלוש** רשומות — `-36.25 · +26.25 · +23.75` — שסכומן `+13.75`, **זהה בדיוק** ל-`sierra.daily_pnl`, מול `+58.75` בספרים. הפער `45.00` מתפרק במלואו: `-36.25` (הסקרצ' שחסר לגמרי) `-2.50` `-6.25` (עמלות/החלקה על שתי המנצחות). ⇒ **סעיף 1 קורא קודם את ה-TradeActivityLog, והז'ורנל הוא רק השלמה לזיהוי-ההזמנה.** התוצאה הזו היא גולדן לריצה-אחורה: `2026-09-22` חייב לצאת `-36.25/+26.25/+23.75`.

## פריט 3/12 · T-437 — חימוש מערכת-2 מהפתיחה (פסיקת-מייקל 21.09 14:47 + 22.09 10:05) — **ריפליי ואז יישום**
המדידה: הדפוסים תלויי-CCI-14 מעולם לא ירו לפני 10:30 ET ב-9 ימים. **השער בפועל אינו האינספקטור** — לקרוא ולקבוע מי חוסם: FHB (`first_hour_buffer.py` — ברים 1-3 כלום, 4-6 REACTIVE בלבד, דפוסי-צ׳ארט לא בשעה הראשונה) · הפלייבוק (שלב B: EDGE_FADE רק תחת AUCTION) · ELQ/T-355 · באפר-CCI (14 ברים).
1. **הרנס 10 סשנים** (09.09→21.09) בשלושה תרחישים: בסיס · חימוש מבר 6 (FHB: REACTIVE+INITIATIVE מבר 6, דפוסי-צ׳ארט מבר 10) · חימוש מבר 4. המוצר: `N` ירי בחלון 16:45–17:40 · `Σ$` · win% · max-DD, לכל תרחיש — **מספר, לא המלצה**.
2. אם התרחיש "מבר 6" אינו שלילי ⇒ ליישם כפרמטר `S2_ARM_MIN_BARS` (ברירת-מחדל 14 בקוד, `.env` **לא** נוגעים — cowork-dev ידליק אחרי הפסיקה ב-`RULED_FLAGS` עם `measured:`).
**אימות:** דוח `docs/reports/S2_ARMING_REPLAY_2026-09-22.md` עם הטבלה הגולמית.

## פריט 4/12 · T-432 — 9 מבחני-רגרסיה של נתיב-הפתיחה אדומים על הקוד החי
`pytest tests/v9/regression/*opening*` ⇒ 9 failed (גם על a04a81c5). לכל מבחן: האם המבחן מיושן (כלל שנפסק אחרת — לתקן את המבחן ולצטט את הפסיקה) או הקוד שבור (לתקן קוד). `test_opening_fire.py::test_pullback_cont_catches_0723_short` נופל כי `OPENING_MAX_RISK_PTS=25` מ-.env (הסטופ המבני 07-23 > 25) — המבחן צריך לקבע את הסביבה שלו, לא להניח.
**אימות:** `set -a; . ./.env; set +a; pytest -q tests/v9/regression/*opening* tests/v9/regression/test_opening_provisional.py` ⇒ 0 failed.

## פריט 5/12 · T-442-בן — bootstrap אוטומטי של 9 סוכני-launchd אחרי ריבוט
ריבוט 22.09 09:34 החזיר 3/9 (גם 15.09, T-259). `startup_check` (שכן חוזר) מריץ `scripts/mems26_bootstrap_agents.sh --bootstrap` **רק** כשתנאי-הבטיחות שהסקריפט כבר מודד מתקיימים (cmd/pending null · position 0 · working 0), ורושם ל-LIVE_CHANNEL. **אפס** kickstart ל-backend/bridge (הם חוזרים לבד).
**אימות:** סימולציה: `launchctl bootout` לסוכן-לא-מסחרי אחד (mobile_relay) ⇒ הריצה הבאה של startup_check מחזירה אותו; `--check` 9/9.

## פריט 6/12 · T-435 — `start_all.sh` מרים backend על screen במקום ה-LaunchAgent
לוג-עיוור, 3 דגלים חסרים, לולאת-רספאון. `start_all.sh`/`restart_all.sh` ⇒ `launchctl kickstart -k gui/$UID/com.mems26.backend` (ו-bridge) בלבד; screen נשאר רק ל-`trade_activity_feed` אם אין לו LaunchAgent (יש — `activity_feed`, אז גם הוא ל-launchctl).
**אימות:** אחרי `restart_all.sh`: `ps -o command -p $(lsof -ti :8000)` מראה את פקודת ה-plist; `/tmp/backend.err.log` מתמלא; `mems26_bootstrap_agents.sh --check` 9/9.

## פריט 7/12 · T-393 — דוח-EOD + עמודי-הטלפון בלי תלות באפליקציית-Cowork
`eod_handoff` (LaunchAgent 23:05) מריץ אחרי החבילה: `python3 scripts/missed_trades_study.py --sessions 10` · `python3 scripts/gen_phone_pages.py --days 14` · commit+push (`git -C`) · `scripts/phone_reply.py` שורה אחת עם קישור `https://mems26-mobile.onrender.com/doc/days/<היום>.html` (≤300 תווים, מקרה ב מורחב — זו הבקשה של מייקל 22.09). כישלון ⇒ שורה ב-LIVE_CHANNEL, לא לטלפון.
**אימות:** הרצה ידנית של ה-plist מחוץ ל-RTH ⇒ 3 הקבצים מתעדכנים, push, GET /chat מראה את השורה.

## פריט 8/12 · T-427 — `close_stale_shadow.py` סוגר בלי `exit_ts`/`exit_price`
לכתוב `exit_ts` = ts של הבר האחרון שנבדק (לא now()) ו-`exit_price` = סגירת אותו בר, `exit_reason=STALE_UNRESOLVED`. **אפס כתיבה רטרואקטיבית** ל-94 השורות הישנות בלי פסיקה.
**אימות:** dry-run ⇒ apply על שורה אחת בסביבת-טסט ⇒ שני השדות מלאים.

## פריט 9/12 · T-419 + T-420 — הליגר: `trade_id` בוליאני ו-`ts` חסר בשני שלבי-המשפך
`trade_id=true` במקום מזהה; DETECTED/EMIT_DECISION בלי `ts`. תיקון במקור-הכתיבה (`_s2_ledger`), טסט-יחידה לכל שדה.
**אימות:** `grep '"trade_id": true' gateway_decisions.jsonl` ⇒ 0 בשורות חדשות; כל שורה נושאת `ts`.

## פריט 10/12 · T-421 — ה-TM מסיק T1 על מחיר שאין בו הזמנה חיה (141 שורות "awaiting Sierra fill" ב-5 דק')
לקרוא `trade_manager` (היסק-רגל): היסק-T1 רק כשיש `c1_target_id` חי; אחרת — לא להסיק ולא לכתוב לוג בלולאה (rate-limit 1/דק').
**אימות:** הרנס 18.09 ⇒ אפס "awaiting Sierra fill" על עסקה בלי יעד חי.

## פריט 11/12 · `v9_decision_vectors` — 11,596 שורות `BAR` ביום (~1 לכל דחיפה)
(שורש כמו T-414) — שורת-BAR אחת לכל בר סגור, לא לכל דחיפה: dedupe על `date_trunc('5 min', ts)` בכותב (`bar_level_detector` hook). **לא למחוק** שורות קיימות.
**אימות:** אחרי הטעינה: `select count(*) from v9_decision_vectors where kind='BAR' and ts >= <היום 16:30>` ≈ מספר הברים (≤ 80 ל-RTH).

## פריט 12/12 · T-164 / T-380 — רעש `TS-OFFSET-GATE` (~20 שורות ERROR בדקה כשהזרם-הרציף תקוע)
(א) rate-limit ל-1/דק' + WARNING במקום ERROR כשהמצב ידוע (סוף-שבוע / קובץ ישן); (ב) שורש-הזרם: `export_promoter` אינו רשום אחרי ריבוט (פריט 5 סוגר) — לאמת ש-`v9_bars_5min` (הרציף) מתקדם אחרי bootstrap; אם לא — לדווח NOT-DONE עם הראיה.
**אימות:** `grep -c TS-OFFSET-GATE /tmp/backend.err.log` בשעה ⇒ ≤ 60.

---
**לא ל-cc (נשאר אצל cowork-dev / מייקל):** T-441 אימות-הטעינה · T-389–T-394 עץ-v2 (מדידה מהיום) · T-408/409/411/412/413 מחקר-הסטאפים · T-279/T-319a/T-394/T-411 פסיקות-מייקל · T-385/T-386/T-396/T-423 מדידות.
