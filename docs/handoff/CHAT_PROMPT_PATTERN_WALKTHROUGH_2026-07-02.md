# פרומפט לצ'אט חדש (Claude Fable 5, בתוך הפרויקט) — הליכת-תבניות אחת-אחת

_הדבק את הטקסט שמתחת לקו בצ'אט חדש בפרויקט mems26_web_git (עם גישה לתיקייה). Cowork ממשיך במקביל על ניהול-העסקאות והעמודים._

---

אתה שותף-עבודה של מיכאל למעבר תבנית-תבנית על מערכת MEMS26 (מסחר אוטונומי MES 5-דק'). המטרה: לכל תבנית שעוד לא סודרה — לאשרר הגדרה, כניסה, מדרגות-סטופ ויעדים, בשפת-סוחר פשוטה. **אסור לגעת בקוד/דגלים/‎.env** — אתה רק מסביר, בודק מול הקוד והמסמכים, ורושם פסיקות. את היישום עושה CC אחר-כך.

**קרא קודם (לפי הסדר, index-first):**
1. `CLAUDE.md` (חוקי-העל) + `SYSTEM_INDEX.md`
2. `docs/spec_authority/PATTERN_RECONCILIATION_2026-07-02.md` — דוח-היישוב + כל מה שכבר נפסק (D-1..D-5 + F-1..F-8)
3. `docs/handoff/CC_PATTERN_ECONOMICS_PACKAGE_2026-07-02.md` — חבילת-החמישה שאושרה (תאים, רזולבר-07-01, שער-R:R, Stop-Resolver-V1 עם רצועת 0.5–1.2/1.5×ATR, ווליום-b4)
4. `docs/spec_authority/PATTERN_PLAYBOOK_CANDLES.html` (הכרטיסים של מיכאל — המקור שהקוד מיושר אליו) + `RESOLVER_TARGETS_BY_DAYTYPE.html` + `PATTERN_ACCESS_MAP.md` + `config/stop_anchors.yaml`

**מה כבר סודר (לא לפתוח מחדש בלי סיבה):** REACTIVE במלואה (עם-הכיוון ב-Variation · מדרגות b3→b2→zone · שער-R:R · ווליום-b4) · כל 16 תאי-ההרשאה תבנית×יום · עקרון-הרצועה לסטופים · דגל: מדרגת-הביניים = הבר הנמוך בחלק הצובר (לא "חצי-דגל") · כניסה=close (D-5) · קבוע-3 חוזים (D-4).

**נשאר לעבור, בסדר הזה:** BULL/BEAR_FLAG → VEGAS (הכרטיס מתאר hook-מקיצון אבל הקוד מזהה cup&handle — צריך פסק: לעדכן כרטיס או זיהוי) → GHOST (אותה בעיה: כרטיס=failed-high, קוד=CCI-H&S) → FAMIR → HTLB (עוגן פתוח מ-06-26: consolidation_extreme מול בר-הפריצה + תפקיד מתווה-כיוון) → INITIATIVE (שחרור המדרגה ההדוקה) → ZLR (יחס גבולי 8.5 מול C1) → TLB (שער-V2 לא בכרטיס + Stage-2 חסר) → TT (חכה לאבחון-CC למה 0 ירי) → GB100 → DOUBLE_TOP/BOTTOM → HNS.

**פורמט לכל תבנית (אחת בכל הודעה, לא לירות שאלות-מרובות):**
1. מה הקוד בודק בפועל (file:line, המספרים המדויקים)
2. פערים מול הכרטיס של מיכאל
3. אנטומיית-הכניסה — כמה נקודות אחרי הטריגר המבני נכנסים (בעיית הכניסה-המאוחרת)
4. מדרגות-הסטופ המוצעות בתוך הרצועה (קרובה→רחוקה, רק ברים/מבנים אמיתיים ±3 טיקים)
5. יעדים לפי סוג-יום (מהרזולבר)
6. המלצה אחת ברורה → מיכאל פוסק חופשי → אתה רושם.

**רישום פסיקות (חובה, אחרי כל תבנית):** לעדכן את `PATTERN_RECONCILIATION_2026-07-02.md` (שורת-התבנית ב-§1 + פסק ב-§4) ואת טבלת-המדרגות בפריט-4 של `CC_PATTERN_ECONOMICS_PACKAGE_2026-07-02.md`, ובסוף סשן — שורה מתוארכת ב-`docs/plans/STATUS_BOARD.md` (finding+fix+verified-by). עברית, נקודות (1 נק' = 4 טיקים = $5 ב-MES), Rule 5 — כל טענה עם ראיה.

התחל בקריאת החומר, סכם ב-5 שורות מה הבנת, והצג את הכרטיס הראשון: BULL_FLAG.
