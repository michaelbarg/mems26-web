# MEMS26 · טיוטות-עיצוב — 2026-06-12

עיצובים מוכנים-להפיכה-לפרומפט לבעיות שאומתו היום (`PATTERN_EOD_2026-06-12.md`).
**לא מומש דבר — design בלבד.** כל פריט: שורש · שינוי מוצע · regression · סיווג trading-logic/display.

---

## D1 — I-22 (מיקוד חדש): `pnl_r` ÷$1.25 — שבור **רק בנתיב-T1/BE**

**שורש (חודד היום):** עסקאות שפגעו T2 מחזירות `pnl_r` **נכון** (id65=3.15 · id66=3.0 · id78=3.15 · id85=2.6 — אומת אריתמטית מול risk_pt×$5×3), בעוד כל WIN-של-T1-בלבד עדיין `pnl_usd÷1.25` (id87: 39.9÷31.92=1.25 · id63: 99.5÷79.6=1.25 · id79: 129÷103.2=1.25). ⇒ קיימים **שני branches** במחשבון: נתיב-T2 מתוקן, נתיב-T1/BE-exit עדיין מחלק ב-tick_value.
**שינוי מוצע:** לאתר את ה-branch של T1/BE-close (כנראה trade-closer/gateway, דרך SYSTEM_INDEX) וליישר לאותה נוסחה של נתיב-T2: `pnl_r = pnl_usd / (abs(entry−stop_initial) × 5.0 × contracts)`. backfill לשורות-T1 ההיסטוריות (או סימון known-bad).
**Regression:** עסקה סינתטית T1+BE: entry 7429.25/stop 7417.75/3 חוזים/pnl_usd 39.9 ⇒ `pnl_r≈0.231`; עסקת-T2 קיימת ⇒ ללא-שינוי (3.15); STOP מלא ⇒ −1.0.
**סיווג:** **display/analytics-safe**. ה-scope הצטמצם משמעותית מול D1 של אתמול — branch אחד.

## D2 — I-31 (חדש): ספירת-ירי שקרית ב-`build/pattern-status`

**שורש (מאומת post-close):** `FaMir fired count 5` + `HFE fired count 4` בעוד `v9_trades` של היום מכיל **אפס** FAMIR/HFE; ה-ts של FaMir (`19:55:05Z`) זהה-לשנייה ל-TLB (=id88), HFE≈ZLR. Σcounts-S4=26 מול 15 בפועל. כנראה ה-match בשאילתת-הספירה רופף — LIKE/pattern-group/או join על `firing_system` בלבד בלי `pattern_id`.
**שינוי מוצע:** ב-build_status (inspector של S4): ספירה במדויק — `WHERE firing_system=4 AND pattern_id=:pid AND date=today`. אם המקור הוא mapping של קבוצות (FaMir⊂TLB-group), להציג את שם-התבנית האמיתי בלבד.
**Regression:** ביום עם ירי TLB-בלבד: count(TLB)=N ו-count(FaMir)=count(HFE)=0; cross-check `Σcounts == COUNT(v9_trades WHERE firing_system=4 AND date=today)`.
**סיווג:** **display-safe** — אבל עדיפות גבוהה: כל כיול-תבניות שמסתמך על ה-endpoint מקבל נתוני-ירי שקריים.

## D3 — I-28 (יום שלישי ברציפות): veto-מגמה לקבוצת-REVERSAL

**שורש (התחזק):** ZLR-SHORT×2 (id69/70) לתוך V-recovery בן-שעה; id70 נכנס 09:50 ומת על אותו בר (בר-הטיל +43.5pt, mfe=0). −$1,068.75. שלושה ימים: 06-10 LONG-נגד-RED · 06-11 SHORT-נגד-ראלי (−$1,559) · 06-12 SHORT-לתוך-recovery.
**שינוי מוצע:** ללא-שינוי מ-DESIGNS_2026-06-11 §D4 (A1/A4: veto כש-`group=REVERSAL` נגד trend יציב ≥N ברים, N≈6, YAML) — **הראיות הצטברו, ההחלטה אצל Michael.** תוספת-היום: גם slope-של-מחיר (recovery מ-LOW) צריך להיחשב "מגמה", לא רק trend_state צבעוני — id69/70 ירו כש-trend אולי עוד RED אבל המחיר עלה 30pt בשעה.
**Regression:** replay 06-12: id69/70 ⇒ veto; id65/66 (TLB-L עם-ה-recovery) ⇒ עוברים. + replays של 06-10/06-11 מ-§D4 אתמול.
**סיווג:** **trading-logic — דורש הכרעת-Michael.**

## D4 — חדש: הקשר-מיקום ל-S2 REACTIVE (כניסות ראש-רנג')

**שורש:** id80/81 REACTIVE_LONG (12:05/12:20 CT) נכנסו ב-7437/7442.5 כשהשיא-היומי 7461.5 נקבע שעתיים קודם והמחיר ברנג' 7420–7450; mfe 7.75/0.5pt ⇒ הכניסה בקצה-התנועה; T2 דרש פריצת-שיא ביום-Variation. 0/3 ל-REACTIVE היום (−$806.25).
**שינוי מוצע (כיול, לא חסימה):** ב-`five_min_system.py` `_detect_reactive` — feature חדש `pos_in_session_range` (מרחק-מ-HIGH/LOW-סשן ב-ATR או % מהרנג'): REACTIVE_LONG כשהמחיר ב->80% עליון של הרנג' ⇒ skip/half-size. סף ב-YAML. שים לב לא לדרוס את עצמאות-S2 (Standing Decision S2⟂S3).
**Regression:** replay 06-12: id80/81 ⇒ skip; id-71/73/75/82 INITIATIVE (שעבדו) ⇒ לא מושפעים.
**סיווג:** **trading-logic — דורש הכרעת-Michael** (מקטין ירי-REACTIVE).

## D5 — I-30 (נמשך): cluster-guard

ללא-שינוי מ-DESIGNS_2026-06-11 §D3 (`MAX_CONCURRENT_PER_PATTERN=1`, YAML, veto ב-gateway). ראיות-היום: id69/70 stop-משותף מתו יחד (חיסכון-CF +$581.25); id87/88 stop-משותף פתוחים יחד כעת. **trading-logic — Michael.**

## D6 — תפעולי (יום שני ברציפות): סוכן-ה-DIAG-30-דק' לא רץ

**שורש:** אין `PATTERN_DIAG_2026-06-11.md` וגם לא `_2026-06-12.md` — ה-trigger מת מאז 06-10. בלעדיו ה-EOD עיוור לנחסמים (אין reject_reason-intraday, אין CF-קלאסי).
**שינוי מוצע:** (א) לבדוק את ה-scheduled-task/LaunchAgent של ה-DIAG (האם נעצר אחרי 06-10?); (ב) guard-בוקר: אם עד 09:30 CT אין snapshot ראשון ⇒ נטיפיקציה ל-Michael מיידית (לא לחכות ל-EOD).
**Regression:** יום-המסחר-הבא מייצר ≥8 snapshots.
**סיווג:** **תפעולי/safe.**

## D7 — I-29 (ממוסגר-מחדש 🔴→🟡): ה-runner עובד עם-המגמה — הכיול מתכנס ל-D3+stop/target

**ממצא-היום:** 5 פגיעות-T2 (Σ≈+14.65R), כולן עם-המגמה — `expected_t2_r_mult=2.0` מתממש כשהכניסה נכונה. ⇒ אין צורך ב-revert של `319e303`; הכיול הנדרש = veto-מגמה (D3) + טבלת stop/target (D8). **לא נדרש עיצוב נפרד — נבלע ב-D3/D8.**

## D8 — I-3/I-13 (נמשך): טבלת stop/target פר-תבנית×day-type — עדיפות-LIVE #1

ללא-שינוי מ-DESIGNS_2026-06-11 §D5. ראיות-היום: S4 יורה stops 32–48.5pt ($487–727 risk, GB100 id63=48.5pt) מול S2-INITIATIVE 4–11pt; `MEMS_MAX_RISK_POINTS=60` רחב-מדי ליום-Variation. **Michael מגדיר את הטבלה** (memory: `project_stop_target_placement_table`).

## D9 — לאימות-CC (קטנים): I-32 gap-ids · `data.mode_context` · readiness=DEGRADED

- **I-32:** ids 64/72/76 חסרים ב-v9_trades — rollback שקט? לבדוק sequence מול לוג backend ("no silent failures").
- **`data.mode_context`:** blocker חדש-בשם על 6 תבניות-S2 (HNS/DOUBLE/FLAGS) — לא הופיע בימים קודמים. לאמת שזה לא re-enable מוסווה של chop-gate (Standing Decision!) אלא mode-context לגיטימי (DAY_TYPE mode).
- **readiness=DEGRADED post-close:** לפרק אילו gates; צפוי footprint/tpo non-critical — לתעד שאין gate חדש.

---

**סדר-עדיפות מוצע ל-Michael:** D8 (טבלת stop/target — חוסם-LIVE) → **D3 (veto-מגמה — 3 ימי-ראיות, היה חוסך ~$1,069 היום)** → D5 (cluster-guard) → D2 (I-31 — מחזיר אמינות-נתונים לכיול) → D1 (I-22 branch-T1) → D4 (REACTIVE position-context) → D6 (DIAG trigger — לפני יום-המסחר הבא!) → D9.
