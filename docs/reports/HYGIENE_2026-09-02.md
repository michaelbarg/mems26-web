# ביקורת-ניקיון 2026-09-02 (cowork, קריאה-בלבד + גנרטורים + commit יחיד)

בוצע דרך Desktop Commander על המק (`/Users/michael/Downloads/mems26_web_git`,
branch `stabilize/mems26-local-truth-2026-05-16`). כל פקודה עם הפלט הגולמי שלה.

---

## א. מה CC דחף בשעה האחרונה (מ-17:00 היום)

```
git log --since="2026-09-02 17:00" --oneline
```
→ **ריק. אפס קומיטים** מ-CC (או מכל סוכן) מ-17:00 ועד רגע-הביקורת (~17:30).
היחיד שנוצר בחלון הזה הוא קומיט-הרג'ן שהביקורת עצמה הפיקה (§ב, 17:37).

בדיקת "קומיט-נרטור":
```
grep -n -i "narrator" render_mobile_relay/app.py
```
→ **0 matches.** אין מחרוזת/פונקציה בשם narrator בקובץ. כלומר: אין קומיט-נרטור
לבדוק (לא קיים, ולכן השאלות "עטוף-try?" / "קורא שדות-קיימים?" לא רלוונטיות —
אין מה לאמת).

**מסקנה: CC לא דחף כלום בשעה האחרונה.**

---

## ב. אינדקס וניקיון

### B1. `gen_index.py --check` — היה drift אמיתי, תוקן + נדחף לקומיט

לא הצלחתי לכידה נקייה של exit-code (Desktop Commander הרג את התהליך פעמיים
תוך ~90-100ש' לפני flush — ראה "מגבלת-כלי" למטה) אבל ה-`git diff` נותן הוכחה
חזקה יותר מ-exit-code:

```diff
- *Auto-generated by `python3 scripts/gen_index.py` · 2026-09-01.*
+ *Auto-generated by `python3 scripts/gen_index.py` · 2026-09-02.*
- **Scope:** [...] · 1042 files · 119 directories
+ **Scope:** [...] · 1069 files · 119 directories
- `backend/` — 647 files        →  661 files
- `scripts/`  — 192 files        →  205 files
- ## Suspected orphans (44)      →  (45)
```

כלומר: האינדקס המחויב-ל-git היה **מיושן מ-2026-09-01** בעוד שהריפו כבר זז
(scripts +13 קבצים, backend +14, וכו'). **הרצת `--check` בפועל כותבת-מחדש את
כל ה-`_INDEX.md`/`SYSTEM_INDEX.md` (זו לא בדיקה-בלבד — תיעוד לתשומת-לב לסבב
הבא)** — כך שהריצה עצמה כבר תיקנה את התוכן.

**בוצע commit יחיד, בדיוק כמבוקש, מנוקה משאר הבלגן (ראה B3):**
```
git add -- <120 קבצי _INDEX.md/SYSTEM_INDEX.md בלבד>
git commit -m "index regen (gen_index.py) - stale since 2026-09-01 ..."
→ 1a9e34d061cb628078b2b1a3eaf66a2cc4d0db64
   120 files changed, 183 insertions(+), 155 deletions(-)
```
**לא נדחף (`push`) — רק commit מקומי**, כי המשימה ביקשה "commit יחיד" ולא
push, וזה branch לא-main; אם רוצים שהסוכנים האחרים (cc-macbook/cc-imac) יראו
את זה דרך `git pull`, צריך push מפורש בנפרד.

**מגבלת-כלי שנתקלתי בה (שקיפות, לא ניחוש):** שלוש קריאות נפרדות ל-
`python3 scripts/gen_index.py --check` דרך Desktop Commander (ריצה עצמאית,
לא בשרשרת) נהרגו על-ידי הסביבה בערך ב-90-100 שניות לפני flush של הפלט
(`/tmp/gidx.txt` נשאר 0 בייט פעמיים). ריצה **ראשונה בלבד**, בתוך שרשרת פקודות
ארוכה יותר (~113ש' סה"כ), כן הסתיימה ונתנה את ה-JSON `{"files": 1069,
"dirs_indexed": 119, "orphans": 45}` — התוכן הזה תואם בדיוק את מה שנכתב
בפועל ל-`SYSTEM_INDEX.md`, ומאמת את ה-diff. לא נמדד exit-code ישיר; ההוכחה
היא תוכן+diff, לא הנחה.

### B2. `gen_flag_index.py --check` — 🔴 EXIT=1, דריפט גדול (לא תוקן — דורש עבודת-תוכן)

```
python3 scripts/gen_flag_index.py --check
EXIT=1        (99 שורות פלט)
```
שורת-כותרת: `UNDOCUMENTED behavior flags (add to docs/FLAG_REGISTRY.yaml):`
ואחריה **כ-97 שמות-דגלים** בקוד שאינם ב-registry. דוגמאות מהראש ומהזנב:
`APP_STATE_ROOT_FIX_V1, BLOCKED_TWIN_V1, CEILING_FLIP_SHORT_V1,
DALTON_EDGE_LOOKBACK_N, DALTON_EDGE_SKIP_OPEN_BARS, ENTRY_BUDGET_QUALITY_V1,
ENTRY_GUARD_OWNERSHIP_V1, ... STRUCTURE_EXIT_FAILBREAK_V1,
TREND_UPGRADE_ADD_V1, VA_FADE_V1`.

**לא תיקנתי** — בניגוד ל-`gen_index.py`, כאן אין "regen מכני": כל דגל
צריך שורה ידנית ב-`docs/FLAG_REGISTRY.yaml` (סמנטיקה + הסבר), וזה תוכן
מסחרי-רגיש שלא הוסמך במשימה הזו. **זה הממצא הגדול ביותר של הביקורת** —
`docs/FLAG_INDEX.md` (המרשם שה-CLAUDE.md אומר "לקרוא לפני טענת מצב-דגל")
פיגר משמעותית מהקוד.

### B3. `sot_map_guard.py` — EXIT=0 (לא נכשל), אבל הפלט עצמו מסמן 2× 🔴

```
python3 scripts/sot_map_guard.py
EXIT=0        (22 שורות פלט)
```
אבל התוכן:
```
🔴 docs/SOURCE_OF_TRUTH.md — cites only 1 source file(s). ...
🔴 docs/SYSTEM_MANIFEST.md — 3 of 7 cited files changed after the map was
   last updated:
   scripts/gen_flag_index.py          — changed 37d after the map
   sc_study/MES_AI_DataExport_merged.cpp — changed 36d after the map
   scripts/trade_activity_feed.py     — changed 16d after the map
```
כלומר הכלי לא כושל את התהליך (exit 0 תמיד?), אבל **מדווח בעצמו** ש-
`docs/SYSTEM_MANIFEST.md` (המסמך ש-CLAUDE.md מגדיר כ"מפת כל משטח... לשמור
מעודכן") מצביע על 3 קבצים שזזו אחרי שהמפה עודכנה לאחרונה. **לא תוקן** —
דורש קריאה אנושית/סוכן של אותם 3 קבצים והחלטה אם השורה עדיין נכונה, לא regen
מכני.

### B4. בלגן — מדדים

| מדד | ערך | פירוט |
|---|---|---|
| `git status --short \| wc -l` **לפני** ה-commit | **121** | 120 מתוכם = `_INDEX.md`/`SYSTEM_INDEX.md` (תוקנו ל-commit `1a9e34d0`, ראה B1); הנותר האחד: `docs/reports/OPS_LOG_2026-09-02.md` (בעריכה היום ע"י גורם אחר — **לא נגעתי**, לא שלי לגעת בו) |
| `git status --short \| wc -l` **אחרי** | **1** | רק `OPS_LOG_2026-09-02.md` |
| קבצי `.md` זרוקים בשורש (`ls *.md`) | 4, מתוכם 2 חריגים | `CLAUDE.md` + `SYSTEM_INDEX.md` צפויים; **`CC_W10_SELF_REPORT.md`** ו-**`PEER_REVIEW_2026-09-01.md`** יושבים בשורש במקום ב-`docs/reports/`־`docs/handoff/` |
| `docs/handoff/*.md` סה"כ | **662** | |
| מתוכם `CC_*.md` | **387** | |
| מתוכם `CC_*.md` בני 3+ ימים | **366** (94.6%) | ערימה גדולה של פרומפטים/וורקאורדרים היסטוריים של CC שלא פונו/אורכבו |

**כפילות סקריפטים — `t211_t0_backfill.py` מול `t211_backfill_apply.py`:**
שניהם נכתבו/רצו **היום** (`git log -1` על כל אחד):
- `t211_t0_backfill.py` — 12:14:14, קומיט לא-קשור ("Risk defaults...").
- `t211_backfill_apply.py` — 12:47:15, קומיט "T-211 backfill APPLIED:
  journal-truth criterion, 15 trades, anchors matched exactly".

ה-docstring של `t211_backfill_apply.py` פותר את זה בפירוש (לא ניחוש):
> "Michael approved the apply on 02.09 12:45 after seeing the dry-run. **CC's
> script turned out to list-only (its --apply branch was empty — verified:
> zero rows changed, no t0_backfilled marker), and its scope filter (t1_hit
> required) missed #948**, the sharpest known case."

**⇒ `t211_t0_backfill.py` הוא המיושן/שבור** (מצב ה-`--apply` שלו לא עשה
כלום בפועל + פספס עסקה קריטית). `t211_backfill_apply.py` הוא הגרסה
הנכונה שרצה בפועל, עם עוגני-קבלה עצמאיים (#942→+55.00, #948→−135.00) ואישור
מייקל מתועד. המלצה: לארכב/למחוק את `t211_t0_backfill.py` או לפחות לסמן
בכותרתו "SUPERSEDED — see t211_backfill_apply.py" כדי שאף אחד לא ירוץ עליו
בטעות.

**ממצא-לוואי (לא התבקש, עלה תוך כדי הבדיקה):** ה-backfill בפועל **רץ
ואושר היום** (12:45-12:47) אך `docs/plans/TASK_LOG.md` שורת T-211 עדיין
מסתיימת (מ-01.09 בלילה) ב"**הצעד הבא: backfill היסטורי... לא בוצע, דורש
פסיקה**" — וגם `STATUS_BOARD.md` לא מכיל שורה מ-02.09 על כך. זה בדיוק
הפער ש-CLAUDE.md אוסר ("התקדמת? לעדכן את הצעד הבא"; "סגרת? גם שורה
מאומתת ב-STATUS_BOARD"). לא תיקנתי (לא בסמכות/scope של ביקורת-הניקיון
הזו) — מומלץ שמי שסגר את ה-T-211 בפועל ישלים את שתי השורות.

---

## ג. סנכרון שלושת הסוכנים (cowork · cc · phone)

**המצב היום: אין מקור-תיאום אחד.**
- `cowork-dev`+`cc-macbook`+`cc-imac` → `docs/handoff/LIVE_CHANNEL.md`
  (עכשיו עם `id` לכל רשומה דרך `scripts/channel_post.py`, ונאכף ע"י
  `scripts/channel_guard.py` — "האם זה נענה?").
- הטלפון → `docs/handoff/PHONE_THREAD.jsonl` נפרד (`phone_reply.py`), עם
  שכבת-דיספוזיציה משלו: `scripts/phone_request_guard.py` +
  `docs/handoff/PHONE_DISPOSITION.md` (מוודא שכל בקשת-מייקל מהטלפון
  מצוטטת ב-TASK_LOG **או** ב-PHONE_DISPOSITION — לא נעלמת).

**אבל המנגנון לאיחוד כבר קיים בחצי-דרך — לא צריך לבנות כלום חדש:**
`scripts/channel_post.py` שורה 40:
```python
AGENTS = ("cowork-dev", "cc-macbook", "cc-imac", "phone-claude", "מייקל")
```
**"phone-claude" הוא כבר זהות חוקית** ל-`--to`/`--frm` ב-LIVE_CHANNEL.
ה-docstring של הקובץ מצטט את מייקל במפורש (01.09):
> "אני רוצה שתשפר את התקשורת שאכן יהיה תקשורת בינך, קלוד קוד והפלאפון."

כלומר זו בדיוק הבקשה שהמנגנון נבנה לענות עליה — התשתית קיימת, רק לא
מחוברת בפרוטוקול.

**הפער האמיתי:** ב-`CLAUDE.md` עצמו, בסעיף "ערוץ-עדכונים משותף — קרא
LIVE_CHANNEL.md FIRST" (המקום שכל סוכן, כולל כנראה phone-claude, אמור
לקרוא בתחילת סשן), רשומים **רק שלושה** סוכנים — "cowork-dev · cc-macbook ·
cc-imac" — **phone-claude לא מוזכר שם בכלל.** לכן אין ל-phone-claude שום
הנחיה לקרוא את LIVE_CHANNEL.md בתחילת סשן, או לענות בו דרך `channel_post.py
--frm phone-claude`. `grep -l phone docs/runbooks/*.md` נתן רק
`MAC2_CLONE_24_7.md` ו-`PRE_OPEN_PROTOCOL_2026-08-16.md` — אף אחד מהם
אינו קובץ-פרוטוקול ל-phone-claude עצמו; `docs/handoff/PHONE_DISPOSITION.md`
הוא יומן-דיספוזיציה (T-208), לא הנחיית-סשן.

**המלצה — הדרך הפשוטה ביותר, בלי מערכת חדשה, בדיוק כפי שהוצע:**
להוסיף שורה אחת לסעיף "ערוץ-עדכונים משותף" ב-**`CLAUDE.md`** (לא ערכתי
אותו — זה קובץ-הפרוטוקול הראשי והשינוי נוגע לתיאום-רב-סוכנים, ראוי
לאישור מייקל ולא לעריכה שקטה בביקורת-ניקיון):

> "**סוכן-phone-claude:** בתחילת סשן לקרוא גם את `docs/handoff/LIVE_CHANNEL.md`;
> לענות/לעדכן דרך `python3 scripts/channel_post.py --frm phone-claude --to
> <סוכן> --subject "..." --re <id>` (התשתית כבר תומכת בזהות הזו)."

`PHONE_THREAD.jsonl`+`phone_request_guard.py` נשארים כמות שהם — הם עונים
על שאלה שונה ("האם בקשת-מייקל נרשמה?"), לא מתחרים ב-LIVE_CHANNEL.

**הנחה אחת שלא אימתתי:** של-phone-claude יש בכלל גישת git/קבצים לריפו הזה
כדי להריץ `channel_post.py` ולקרוא `LIVE_CHANNEL.md`. שורת T-208 ב-
`PHONE_DISPOSITION.md` ("קלוד-הפלאפון בונה בחלון-הלילה") מרמזת שכן, אבל
כדאי שמייקל יאשר את זה במפורש לפני שמוסיפים את השורה ל-CLAUDE.md.

---

## נספח — פקודות מלאות שהורצו (לשחזור)

```bash
cd /Users/michael/Downloads/mems26_web_git
git log --since="2026-09-02 17:00" --oneline
grep -n -i "narrator" render_mobile_relay/app.py
python3 scripts/gen_index.py --check          # ראה B1 — כתב 120 קבצים
python3 scripts/gen_flag_index.py --check     # EXIT=1
python3 scripts/sot_map_guard.py              # EXIT=0, 2× 🔴 בפלט
git status --short | wc -l
ls *.md
ls docs/handoff/*.md | wc -l
find docs/handoff -maxdepth 1 -iname "CC_*.md" | wc -l
find docs/handoff -maxdepth 1 -iname "CC_*.md" -mtime +3 | wc -l
git log -1 --format="%ai %s" -- scripts/t211_t0_backfill.py
git log -1 --format="%ai %s" -- scripts/t211_backfill_apply.py
grep -n "T-211\|T211" docs/plans/TASK_LOG.md
grep -n -i "backfill\|T-211" docs/plans/STATUS_BOARD.md
grep -n "channel_post\|phone_request_guard" -r scripts/
# commit היחיד שבוצע:
git add -- <120 index files>
git commit -m "index regen (gen_index.py) - stale since 2026-09-01 ..."
# → 1a9e34d061cb628078b2b1a3eaf66a2cc4d0db64  (לא נדחף)
```
