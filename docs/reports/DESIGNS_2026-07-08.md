# MEMS26 · טיוטות-עיצוב — 2026-07-08

מקור: `PATTERN_EOD_2026-07-08.md` (EOD ~15:15 CT). **קריאה-בלבד — לא-מומש.** כל design מוכן-להפיכה-לפרומפט.
מקרא: **[TRADING-LOGIC]** = משנה risk/fire/exit-surface → **דורש אישור-Michael + סטופ-אסטרטגי** · **[DISPLAY/SAFE]** = תצוגה/instrumentation, ללא-שינוי-מסחר · **[SoT→CC]** = דורש הצלבת Sierra v9_export/DB/לוג ע"י CC לפני-מימוש.

---

## §1 · I-63 (חדש) — יציאה-מוקדמת חותכת מנצחי-T1 · **[SoT→CC] → [DISPLAY/SAFE]+אולי[TRADING-LOGIC]**

**שורש (נצפה):** 6/7 עסקאות-היום נסגרו ~12:02 CT, **17–22 דק' לפני-שה-T1 שלהן נגע** (12:20–12:30). מנגנונים: `stale_shadow_cleanup` (311/312 — אוטומטי, "מנקה" shadow-ישן) · `manual_sierra_close_michael` (310/313 — Michael ידני) · `manual` (307/309/314 — flatten-BE). תוצאה: +0.94R בפועל מול +7.49R hold-to-T1 ⇒ **~+6.5R נחתכו** (PATTERN_EOD §3). ל-**shadow** זו בעיה-קריטית: אם ה-sim לא-מחזיק-עד-יעד, כלכלת-ה-shadow חסרת-ערך (רק חלקי-R זעירים) וה-CF-הידני של הסוכן הוא-המקור-היחיד ל-R-האמיתי.

**הצלבה-נדרשת לפני-כל-מהלך (CC):** לחלץ מ-shadow-sim-exit / `fill_poller.py` / scheduler את **חוק/טיימר ה-`stale_shadow_cleanup`** — מאיפה ~17דק'? מהו התנאי (age? bars? end-of-cluster?)? האם ה-shadow-sim **אמור** להחזיק עד T1/T2/T3/stop/timeout(EOD)? לצלב `/tmp/*.log` סביב 12:02 CT מול id 309/311/312.

**שינוי מוצע (מותנה-בממצא):**
1. **אם ה-shadow-sim אמור-להחזיק-עד-יעד** ו-`stale_shadow_cleanup` סוגר-מוקדם בטעות → לתקן את ה-exit-path כך שסגירת-shadow תקרה **רק** על T1/T2/T3-hit / stop-hit / EOD-timeout, לא על טיימר-ניקוי. **[TRADING-LOGIC-shadow]** (משנה כלכלת-shadow; לא-כסף-אמת, אך משנה את ה-oracle) → אישור-Michael.
2. **instrumentation (מיָדי, safe):** לרשום בכל-trade את `exit_trigger` מפורש (`t1_hit`/`stop_hit`/`eod_timeout`/`stale_cleanup`/`manual_ui`) + `bars_held` + `mfe_at_exit` — כדי שכל-EOD יבחין סגירת-יעד מסגירת-ניקוי. **[DISPLAY/SAFE]**
3. **live/Michael:** אין-שינוי-אוטומטי (דיסקרֶציה); רק לתעד ל-CF שהחיתוך-הידני עלה +0.82R (id310) — מידע-ל-Michael, לא-חוק.

**Regression שיוכיח:** `test_shadow_holds_to_target` — shadow-trade מוזרק עם T1 שנגע ב-bar+4 ו-stop שלא-נגע → ה-sim סוגר ב-T1 (+1R), **לא** ב-cleanup-timer; `exit_trigger='t1_hit'`. Anti-regression מול היום (309/311/312 שנסגרו-מוקדם).

**עדיפות:** 🔴 — חוסם כל-הערכת-shadow; היום הסתיר ~+6.5R. **3-החמורים.**

---

## §2 · I-54 / D-targets-monotonic — `t2=7564.5` קבוע (2-כיוונים) + היפוך t2>t3 · **[TRADING-LOGIC] · [SoT→CC]**

**שורש:** **כל 7 העסקאות** קיבלו `t2=7564.5` **זהה** (entries 7484–7522 ⇒ 42–80pt מעל, בלתי-בר-השגה; שיא-יום 7536.5). ב-6/7 `t2 (7564.5) > t3` (למשל 307:t3=7498.25, 314:t3=7538.75) = **היפוך-מונוטוניות** (ל-LONG אמור `t1<t2<t3`). זהו אותו-דפוס מ-07-06 (t2=7558 קבוע על-**שורטים**) — עכשיו מאושר גם על **LONG** ⇒ **רמת-מבנה-גלובלית-קבועה מוזרקת כ-t2 בשני-הכיוונים, ללא-בדיקת-סדר/מרחק/ATR.**

**הצלבה-נדרשת (CC):** לחלץ את חישוב-ה-targets ב-`backend/v9/systems/five_min/five_min_system.py` (REACTIVE/INITIATIVE) + woodies-target-calc (ZLR) — **מקור 7564.5** (POC? VAH? swing-יומי? רמה-קבועה-בקונפיג?), ולמה `t1<t2<t3` לא-נאכף.

**שינוי מוצע (`.../target_calc`):**
1. **אכיפת-מונוטוניות:** LONG `entry<t1<t2<t3`; SHORT `entry>t1>t2>t3`. target שמפר-סדר → לתקן/להשמיט.
2. **תקרת-מרחק ATR-מודעת:** `|target−entry| ≤ k·ATR_daily`; רמה-מבנית-רחוקה → clamp ל-ATR-cap או להשמיטה (T2/T3 אופציונליים). מונע "7564.5 קבוע על יום-50pt".
3. **מקור-target ב-record:** לתעד `t2_source` (structural/atr/fixed) ל-CF-עתידי.

**Regression שיוכיח:** `test_targets_monotonic_and_bounded` — fixture-LONG (entry 7515.5, ATR נתון): מוודא `t1<t2<t3`, כל `|t−entry|≤k·ATR`, אין `t2=רמה-קבועה-מעבר-לתקרה`. Anti-regression מול היום (7×t2=7564.5) ו-07-06 (t2=7558 שורטים).

**עדיפות:** 🔴 — משפיע-ישירות-על-runner; היום מוסתר-ע"י-I-63, אך ביום-שמחזיק-runner זו עלות-אמיתית. **3-החמורים.**

---

## §3 · I-62 — DEMO כבוי / LIVE כעת-פעיל · **[TRADING-LOGIC] · [SoT→CC]**

**שורש (נצפה):** `demo_enabled_systems=[]` (נמשך מ-07-06) + `live_enabled_systems=[2,4]` שכעת **ירתה עסקת-אמת (id310 S4 ZLR, +$22.5, נסגרה ידנית ע"י Michael)**. ⇒ הנתיב-הקובע עבר **demo→live** — ראיה-חזקה שהריקון של demo היה **הַעֲלָאָה-מכוונת-pre-LIVE**, לא-שוגג (07-06 השאיר-זאת-פתוח).

**הצלבה-נדרשת (CC):** `git log`/`.env`/`FLAG_INDEX.md`/`FLAG_REGISTRY.yaml` — לאשר `demo=[]`+`live=[2,4]` = החלטת-Michael מתועדת, מתי/איזה-commit.

**שינוי מוצע (מותנה):**
- אם **מכוון** (הסביר) → **לא-לגעת.** לתעד ב-`FLAG_INDEX.md`+`SOURCE_OF_TRUTH.md`: "demo כבוי-במכוון מ-07-06, S2/S4=live-enabled, LIVE=הנתיב-הקובע" + **לעדכן סוכן-EOD** להצליב live-fills כנתיב-הקובע (לא demo). **[DISPLAY/SAFE]**
- אם **שוגג** → החזרת `demo_enabled_systems`. **[TRADING-LOGIC] — Michael בלבד.**

**Regression שיוכיח:** `test_enabled_systems_matches_intended` — טוען gateway-config, מוודא `demo_enabled`/`live_enabled` תואמים ערך-מוצהר ב-`FLAG_REGISTRY.yaml`; `--check` נכשל על drift-לא-מתועד.

**עדיפות:** 🟡 (הופחת מ-🔴-07-06 — עכשיו יש-נתיב-live-פעיל, לא-יום-דומם) — אך עדיין דורש **אישור-כוונה** לפני-הסקה. **3-החמורים.**

---

## §4 · I-61 — השלמת-תיוג exit_reason · **[DISPLAY/SAFE]** (מומש-חלקית)

**שורש:** בקשת-07-06 (§2) — לתייג את מקור-הסגירה — **מומשה-חלקית**: היום `manual_sierra_close_michael` / `stale_shadow_cleanup` כבר-מבחינים-מקור. **נותר:** `manual` גנרי עדיין-על 307/309/314 (flatten-BE) — לא-ברור אם UI/Michael או auto-flatten.

**שינוי מוצע:** למפות את ה-`manual` הגנרי-הנותר לתווית-מבחינה (`manual_ui` / `auto_flatten_<rule>` / `manual_sierra_close_michael`). משתלב עם §1.2 (`exit_trigger`).

**Regression:** `test_no_bare_manual_exit_reason` — אף-סגירה לא-מקבלת `exit_reason='manual'` גנרי; כולן מְתֻיָּגוֹת-מקור.

**עדיפות:** 🟡 — התקדמות-טובה מ-07-06; נותר זנב-קטן.

---

## §5 · chop_state="FOUND" (חדש-watch) — מיפוי-תווית-מצב חשוד · **[DISPLAY/SAFE] · [SoT→CC]**

**שורש:** `/gateway.chop_state` ו-`/chop_score.state` = `"FOUND"` (score=14). בימים-קודמים: `EXPANDING`/`SEARCHING`/`CONTRACTING`. `"FOUND"` לא-ערך-מוכר-בסקאלה ⇒ חשד **מיפוי score→state שגוי** או **מצב-חדש לא-מתועד**.

**הצלבה (CC):** ב-Layer-0 chop-composite (`trading_gateway.py`/chop-module) — לאמת את אֶנוּם-המצבים; האם "FOUND" לגיטימי (חדש) או באג-מיפוי.

**שינוי מוצע:** אם באג — לתקן מיפוי לתווית-הנכונה; אם מצב-חדש — לתעד ב-`FLAG_INDEX.md`/מקרא-chop. שערי-chop כבויים ממילא (CLAUDE.md) ⇒ display-only, לא-חוסם.

**Regression:** `test_chop_state_enum_valid` — `state` תמיד ∈ {SEARCHING,EXPANDING,CONTRACTING,...המורשים}; "FOUND" נכשל אם-לא-מתועד.

**עדיפות:** 🟡 watch — לא-חוסם (chop-off), אך תווית-חריגה שווה-בירור.

---

## §6 · I-44 — day_type wrapper-קפוא · **[DISPLAY/SAFE] · [SoT→CC]** (handoff עומד)

**שורש:** `/day_type/state` = `session_min=0 / vote_history=[]` (קפוא) — endpoint קורא wrapper-מת במקום `app.state.day_type_machine`+`v9_day_type_state`. ההתוויות היום (Variation/OPEN_AUCTION_IN/MEDIUM) סבירות-בערך.

**שינוי מוצע (עומד מ-`CC_DAYTYPE_SOURCE_CONSISTENCY_2026-06-30.md`):** לאחד endpoint למקור-החי + דגל-`stale` כש-`session_min` לא-עולה. display-only.
**Regression:** `test_daytype_endpoint_live_source` — `session_min>0` ו-`vote_history` לא-ריק כש-RTH-פעיל; `stale=true` כשקפוא.
**עדיפות:** 🔴 (מבלבל-אבחון) אך display-safe. handoff עומד — לא-חדש.

---

## §7 · I-23 — gateway counters לא-סופרים fires · **[DISPLAY/SAFE]** (עומד)

**שורש:** `trades_today=0/daily_pnl=0` מול 7-fires-היום (1 live +$22.5). לא-מחווט (shadow **וגם** live-שנסגר).
**שינוי מוצע:** לחווט `trades_today`/`daily_pnl`/`shadow_active_count` לספור fires (פילוח mode: live/shadow). display-only.
**Regression:** `test_gateway_counts_fires` — 1 live + 6 shadow → `trades_today` משקף (עם פילוח).
**עדיפות:** 🟡 display-safe. design עומד מ-06-09.

---

## §8 · תחזוקת-סוכן — `limit=100` + פער-לוג 07-07 · **[DISPLAY/SAFE]**

**שורש:** (א) `/trades/recent?limit=200`→**HTTP 422** (`le:100`) — I-25; ספֵּק-ה-API מגביל ל-100. מפרט-הסוכן (SKILL.md) מבקש `limit=200`. (ב) **אין EOD/log ל-07-07** (ג׳) — הסוכן לא-רץ אתמול.

**שינוי מוצע:**
1. עדכון-מפרט-הסוכן: `limit=100` (או pagination אם-צריך >100). **[DISPLAY/SAFE]**
2. לברר פער-07-07: cron/schedule לא-נורה, או ריצה-שגתה (לצלב מול scheduler-runner-log). אם-מבני — לתקן-תזמון.

**Regression:** לא-נדרש (תחזוקה). אימות: הריצה-הבאה מפיקה log-line יומי.
**עדיפות:** 🟢 safe — תחזוקה.

---

## סיכום-עדיפויות

| # | פריט | class | דחיפות | חוסם-מימוש |
|---|------|-------|--------|-----------|
| §1 | I-63 יציאה-מוקדמת חותכת-מנצחים | SoT→(TRADING-shadow/DISPLAY) | 🔴 | SoT (חוק-cleanup?) |
| §2 | I-54 t2=7564.5 קבוע + t2>t3 | TRADING | 🔴 | אישור-Michael + SoT |
| §3 | I-62 demo-off/live-active | TRADING/SoT | 🟡 | אישור-Michael (כוונה?) |
| §4 | I-61 תיוג exit_reason (זנב) | DISPLAY/SAFE | 🟡 | — (safe) |
| §5 | chop_state="FOUND" | DISPLAY/SAFE | 🟡 | SoT (enum?) |
| §6 | I-44 wrapper | DISPLAY/SAFE | 🔴-בלבול | handoff עומד |
| §7 | I-23 counters | DISPLAY/SAFE | 🟡 | — (safe) |
| §8 | limit=100 + פער-07-07 | DISPLAY/SAFE | 🟢 | — (תחזוקה) |

**3 החמורים (לנוטיפיקציית-Michael):** §1 I-63 (יציאה-מוקדמת חתכה ~+6.5R — כניסות-נכונות, יציאה-הרסנית) · §2 I-54 (t2=7564.5 קבוע בשני-כיוונים + t2>t3) · §3 I-62 (demo-off/**live-כעת-פעיל** id310 — אשר-כוונה).
**מיָדית-safe (ללא-אישור):** §4 (תיוג-זנב) · §5 (enum-chop) · §7 (counters) · §8 (limit/log). **§1/§2/§3/§6 דורשים SoT/אישור.**
