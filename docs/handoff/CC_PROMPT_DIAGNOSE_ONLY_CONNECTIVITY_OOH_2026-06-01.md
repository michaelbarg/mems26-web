# CC PROMPT — אבחון בלבד (READ-ONLY) · חיבור + גשר + נרות מחוץ ל-RTH · מול האפיון

**תאריך:** 2026-06-01 · **מקור:** Cowork (Michael) · **מצב:** SHADOW
**⛔ READ-ONLY — אפס שינויי קוד / config / הפעלת שירותים שמשנה מצב.** המטרה: אבחון שורש איכותי מול האפיון, **לא** תיקון. כל "תיקון" מוצע = הצעה לאישור Michael, לא לביצוע.
**משמעת:** diagnose-first · Rule 5 (פקודה + פלט גולמי לכל קביעה) · אסור פלסטרים — חתור לשורש, לא לסימפטום.

## למה זה ככה (לא לתקן עכשיו)
Michael רוצה תיקון איכותי מבוסס-שורש ומבוסס-אפיון, לא band-aid. לכן השלב הזה רק **מאבחן ומשווה לאפיון**, ומסיים בהמלצת-תיקון מנומקת. הביצוע — בפרומפט נפרד אחרי אישור.

## ראיות מהדאשבורד (Cowork צילם 2026-06-01 ~03:58 ET)
DISCONNECTED (WS, שני המקומות) · Woodies "Disconnected — retrying" · **Y IB dll_missing** · CLOSED/pre-open · SHADOW 0t · 0 trades · badge "1 Issue".

---

## מה לאבחן — לכל פריט: ראיה גולמית → שורש → מול האפיון → סיווג → תיקון מומלץ (לא לבצע)

לכל סימפטום החזר את 5 השדות: (a) **ראיה** (פקודה+פלט), (b) **שורש** מאומת, (c) **מה האפיון אומר שאמור לקרות** (ציטוט + נתיב מסמך — CLAUDE.md / D-089/091/092/093 / specs / `MEMS26_PIPELINE_*`), (d) **סיווג** באג מול התנהגות-צפויה, (e) **תיקון איכותי מומלץ** (שורש, לא פלסטר) — להצעה בלבד.

1. **DISCONNECTED (frontend↔backend).** האם backend רץ (`curl -s localhost:8000/health`, `lsof -i :8000`/`:3000`)? אם רץ — איפה ה-WS נשבר (endpoint/URL/CORS/handler)? אם לא רץ — מדוע לא עלה (לוג `/tmp/backend.log`)? **שורש, לא "תריץ מחדש".** מול האפיון: מה אמור להיות מצב החיבור ומה ה-WS contract.
2. **Y IB dll_missing.** מקור הדגל הזה בקוד; האם DLL לא טעון/לא מייצא? יש קבצי JSON טריים ב-`~/SierraChart_Data/v9_export/` ומתי עודכנו? מול האפיון (D-092 §SoT, SIERRA_DLL_OPS) — מה אמור להתקבל ומאיפה.
3. **"1 Issue".** מה ה-issue (פאנל/קונסול/לוג)? שורש + סיווג.
4. **אין נרות מחוץ ל-RTH.** כאן הליבה של בקשת Michael — **לאבחן מול האפיון**, לא להניח: 
   - מה האפיון אומר על זרימת נתונים מחוץ ל-RTH? האם המערכת אמורה לקבל Globex overnight bars, או רק RTH? מה ההגדרה של "CLOSED" בקוד מול האפיון (RTH-only? Globex?).
   - אילו טבלאות DB מכילות נרות שמורים (`v9_bars_5min`, woodies, footprint, tick_reversal) — טווח, טריות, מבנה. האם קיים כבר מנגנון fallback/replay היסטורי, ומה האפיון אומר עליו.
   - **מה התיקון האיכותי הנכון** (לא פלסטר): מקור הנתונים הנכון מחוץ ל-RTH לפי האפיון, איך להבחין בין חי להיסטורי, ומה ההשלכה על בטיחות (ירי אסור על נתון לא-חי). **רק להמליץ.**

5. **גשר (read-only).** אילו streams מגיעים בפועל ל-backend ואילו לא (`sot_health.py --strict` / bridge inspector / לוג) — FRESH/STALE/DEAD per-stream. שורש לכל אחד שחסר. ודא local-only.

## עקביות מול האפיון (חובה)
- לכל ממצא, ציין אם הוא **באג** (קוד סוטה מהאפיון), **גַּף-אפיון** (האפיון לא מכסה), או **התנהגות-צפויה** (לא באג — לדוגמה pre-open שקט). זה מה שמבדיל תיקון איכותי מפלסטר.
- אם האבחון נוגע בנקודה שכבר מתועדת בעצי ההחלטה/פערים (`MEMS26_PIPELINE_FLOW.html`), הצלב.

## כיוון מוצע ע"י Michael — להעריך מול קוד/אפיון (לא לבצע)
Michael הציע את הכיוון הבא לתיקון האיכותי. **אל תבנה** — אמת היתכנות ונכונות מול הקוד/האפיון, וציין לכל סעיף: האם בר-ביצוע, מול מה צריך להיזהר, ומה הנתיב הנכון.

1. **מקור נרות מחוץ ל-RTH = טבלת ה-Woodies 5-דק' (פעילה כל הזמן).** Michael: הטבלה הזו מתעדכנת רציף (גם מחוץ ל-RTH), בניגוד ל-RTH-only. **אמת:** מהי הטבלה הקנונית (`v9_bars_5min_woodies`? `v9_woodies_signals`?), האם היא באמת מתעדכנת **24/6** (טווח + טריות + מקור ה-DLL), ומבנה השדות. האם מתאימה כמקור ל-5-דק' + history מחוץ ל-RTH. אם כן — זה המקור המומלץ (לא v9_bars_5min שעלול להיות RTH-only).
2. **עומק היסטוריה = 200 נרות אחורה.** אמת שזה זמין בטבלה ומה ה-latency של שליפה כזו.
3. **באתחול: לטעון מחדש את 200 הנרות + הגשר ידווח על הכל.** אמת מה קורה היום באתחול (`bars_5min_stream.py` backfill מ-MAX(ts), `day_type_seed.py`) — האם 200 נרות נטענים? האם הגשר מדווח health/load per-stream באתחול? מה חסר.
4. **רשימת ה-state המלא ל-hydrate בכל הפעלה (התחלה רציפה).** Michael רוצה שבכל restart המערכת תתחיל רציף — לטעון מחדש את ההיסטוריה של: **cumulative_delta (CVD), Woodies studies, נרות 5-דק', טווח יומי, POC/VAH/VAL**, ועוד. **אבחן:** מה כבר נטען היום (restart-recovery R2-9 — opening_type/day_type/ib_locked), ומה **לא** נטען (CVD cumulative, woodies tail, daily range, POC/VA). החזר **רשימה מלאה ומדורגת** של כל ה-state שצריך hydrate באתחול + מהיכן (איזו טבלה), כולל מה שכרגע נאבד ב-restart. ⚠️ ציין מה מהם reset-aware (CVD מתאפס בגבול session — לא לטעון חוצה-reset).

## פלט מצופה
`docs/reports/DIAGNOSE_ONLY_CONNECTIVITY_OOH_2026-06-01.md`: טבלה לכל סימפטום עם 5 השדות + ציטוטי-אפיון + המלצת תיקון איכותי. בסוף: רשימת תיקונים מומלצים מדורגת (root-cause, לא band-aid) **לאישור Michael**. **אפס commits, אפס שינויי קוד/config.**

**שער:** זהו אבחון בלבד. אל תתקן, אל תפעיל שירותים באופן שמשנה מצב, אל תיגע בשום קובץ. אם משהו מזמין "תיקון מהיר" — רשום אותו כהמלצה ועצור.
