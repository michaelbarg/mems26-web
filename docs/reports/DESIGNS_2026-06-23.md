# MEMS26 · טיוטות-עיצוב — 2026-06-23

עיצובים מוכנים-להפיכה-לפרומפט לבעיות שאומתו היום (`PATTERN_EOD_2026-06-23.md`).
**לא מומש דבר — design בלבד.** כל פריט: שורש · שינוי מוצע (קובץ/שכבה) · regression · סיווג trading-logic(→Michael)/display-safe · הצלבת-Sierra.
**D-ID חדש היום: D28 (playbook-מת — R1/R2/R3), D29 (.env-פוסט-סשן / gate-state-בזמן-ירי).** מתמשכים מעודכנים: D24 (I-40 — חצי-נסגר), D25 (I-41 — שוכך), D2/D9/D11/D19/D23.

> **תזכורת standing-decision (CLAUDE.md, 2026-06-08):** chop-gates (S2 `choppiness_ok`, Layer-0 `chop_state`) ו-COT/AMT (S2⟂S3) **מנוטרלים בכוונה**. **אף עיצוב כאן אינו מציע לאפס flag ללא sign-off של Michael.** שער-הצ'ופ לא-רלוונטי היום (`chop_state=FOUND`, range_atr 1.479).

> **🔴 הקשר-קריטי:** היום היה **יום-היפוך** (ראלי-בוקר +76pt ל-7491 → מכירה ל-7435), הפסד-קטן −$137.86 ב-13 עסקאות (≠06-22's −$1,311). **השורש-היחיד-המהותי:** 4 שורטים-מוקדמים נגד-הראלי (−$266), מתוכם 3 (HFE) הן **תאי-SKIP-מתים** במטריצת-ה-playbook. **עדיפות-העל: D28** — מאחד את R1–R3 של ה-CASCADE_AUDIT (05:00) ומאומת-בקוד היום.

---

## D28 — חדש 🔴🔴 (trading-logic → Michael, strategic-stop): החייאת מטריצת playbook (תבנית×יום-מסחר) — R1/R2/R3

**שורש (מאומת-בקוד + תוצאה-בפועל היום — לא תיאוריה):**
ה-CASCADE_AUDIT_S1_S2_S4_2026-06-23 (05:00) קבע 3 חורי-שורש; **אימתי את שלושתם היום ב-read-only:**
- **R1 — המטריצה מתה בנתיב-החי.** `backend/v9/systems/daytype_playbook.py:104-106`:
  ```python
  # #68: position gate supersedes pattern suppression
  if os.environ.get("DAYTYPE_POSITION_GATE", "0").lower() in ("1","true","yes"):
      return Decision("FULL", cap, "position-gate-active (all-patterns-fire)")
  ```
  `DAYTYPE_POSITION_GATE=1` ב-`.env` ⇒ `decide()` מחזיר **FULL לכל-תבנית×יום** לפני קריאת ה-YAML. כל `config/daytype_playbook.yaml` `patterns:` (SKIP/REDUCED + `require_with_trend`) = **קוד-מת**.
- **R2 — שער-המיקום עיוור-לתבנית.** `grep pattern backend/v9/systems/daytype_position_gate.py` → ה-arg `pattern` ב-**חתימה בלבד (שורה 36)**, אפס-שימוש-בלוגיקה ⇒ לא מבדיל continuation (ZLR/TLB/INITIATIVE — לירות עם-המגמה) מ-reversal (HFE/REACTIVE — לחנוק ביום-מגמה).
- **R3 — Nontrend-SKIP בבעלות-אף-אחד.** שער-המיקום מאציל Nontrend ל-playbook (`daytype_position_gate.py:57`), וה-playbook no-op ⇒ Nontrend מאשר-הכל.
**הוכחה-חיה היום:** 215/216/217 = **HFE×Trend_Normal** (תא **SKIP** ב-YAML) ירו `blocked_by=null`, **−$142.50**. mfe=0/0/4.25 ⇒ חיסכון-נקי אם-נחסם.
**שינוי מוצע (שתי-אופציות — Michael בוחר):**
  (א) **הסר את ה-short-circuit (`:104-106`)** והרץ את שתי-השכבות בשרשרת (AND): שער-המיקום מטפל ב-**כיוון×מיקום**, ה-playbook מטפל ב-**תבנית×יום (SKIP/REDUCED)** — שתיהן-חייבות-לאשר. זה משחזר את כוונת-#68 ("position handles direction; playbook handles pattern-suppression") שנבנתה-חצי.
  (ב) **הזרק `pattern.group` (CONT/REV) לשער-המיקום** — חסום reversal על Trend_Normal/Trend_DD; אפשר continuation. פחות-נקי (מכפיל-לוגיקה).
  **המלצה: (א)** — שכבות-מורכבות, ה-YAML כבר קיים ומלא.
**Regression (חובה):** (1) replay 06-23 ⇒ 215/216/217 (HFE×Trend_Normal) `blocked_by="PLAYBOOK_SKIP"`; (2) 227 FAMIR_LONG×Normal (תא FULL) **לא** נחסם; 225 ZLR×Normal (RED/reduced) לא-SKIP; (3) replay 06-22 ⇒ HFE 191/193/194 נחסמות, INITIATIVE 195/196 (CONT) עוברות; (4) anti-tautology: טסט שמוודא שכש-`DAYTYPE_POSITION_GATE=1` המטריצה **כן** רצה (לא חוזרת ל-FULL).
**סיווג:** **🔴 trading-logic → Michael (strategic-stop).** שינוי משטח-סיכון-מסחרי (פחות-ירי, חוסם-תבניות) — **אין לממש ללא sign-off** + backend restart. נספח ל-`feedback_full_decision_pipeline_wiring` (זהו בדיוק dead-wiring של החלטה).
**Sierra/לוג:** CC לאשר ב-**trace-חי של gateway** ש-`daytype_playbook.decide()` מוחזר FULL-תמיד היום (קראתי קוד-סטטי + תוצאה; trace מאשר runtime). + לוודא ש-`pattern.group` (CONT/REV) ממופה-נכון לכל תבנית ב-YAML.

## D29 — חדש 🟡 (ops/observability → CC): חשיפת gate-state פר-ירי (כיסוי מתפתח תוך-סשן)

**שורש (תפעולי — *מתוקן* אחרי-קריאת-STATUS_BOARD; **לא** "flip-שקט-פוסט-סגירה"):** היום הודלקו flags **תוך-הסשן, מתועד, עם restart-לכל-אחד** — `DIRECTION_CONTEXT` ~08:30 (אינרטי-בפתיחה), `HTLB_DIRECTION_GATE`+`TLB_SPEC_V2` ~10:xx (אישור-Michael). ⇒ **4 השורטים-המוקדמים (213/215/216/217 @08:55–09:15) ירו לפני** ש-HTLB-dir+TLB-v2 עלו (כיסוי-חלקי). `.env` mtime 15:46 עקבי עם פעילות-היום. כל 13 הירי `blocked_by=null`. **הבעיה האמיתית:** ה-EOD **לא יכול לדעת מ-API** איזה gate היה חי בכל-ירי — `blocked_by` מראה רק מה-שחסם, לא מה-שהיה-דלוק-ולא-חסם.
**שלב-0 (verify — CC):** מ-לוג-gateway פר-trade: אילו gates היו דלוקים ב-08:55–14:00? ספציפית — אילו `HTLB_DIRECTION_GATE` (שמסנן נגד-ביאס) היה חי ב-09:05 וביאס=UP, **האם היה חוסם את 215/216/217**? (חופף-חלקית ל-D28 עבור S4).
**שינוי מוצע (observability, safe):** חשוף `active_gates` (+ ערך-flag-בזמן-route) ב-`gateway/status` ו/או חותמת פר-trade ב-`metadata.gates_active[]` ⇒ ה-EOD מאמת ללא-CC, ו-CF-של-שער הופך-מדיד.
**Regression:** trade-record חדש נושא `metadata.gates_active`; `gateway/status.active_gates` == env-בפועל ב-route-time.
**סיווג:** **ops/display-safe** (חשיפת-מצב, לא-משנה-ירי). מאפשר אימות-עצמי של D28/D29.
**Sierra/לוג:** לוג-gateway 08:55–14:00 (CC).

## D24 — מעודכן 🟡 (I-40 חצי-נסגר): exec↔signal לא-חזר · display-leak נמשך

**עדכון-היום:** **החצי-המסוכן (פיצול-ביצוע↔סיגנל) לא-חזר** — entries-היום (7458–7484) **תואמים** את `chart/bars5min` (08:55 H7466 → 09:15 H7487) ⇒ הגלגול יוני→ספטמבר התייצב (06-22 היו ~7590 trades מול ~7540 woodies). **החצי-ה-display נמשך:** 2 ברי-זבל ב-`/chart/bars5min` (**09:30 c4214 · 14:30 c3745**) מ-`v9_bars_5min_continuous` היתום.
**שינוי מוצע (display/safe — החצי שנשאר):** `/chart/bars5min` יקרא רק מהמקור-הקנוני (`v9_bars_5min_woodies`, fallback `v9_bars_5min`); **לא** לאחד/ליפול-ל-`v9_bars_5min_continuous`. (זהה ל-D24-ב של 06-22.)
**Regression:** `/chart/bars5min` `bad_count=0` (אין 4214/3745/6274); כל בר במגדר [7000,8000] בסשן.
**סיווג:** **display/safe** (החצי-שנשאר). **🔴 verify-CC נדרש** לאשר ש-exec↔signal **באמת** מיושרים פוסט-גלגול (לא רק-במקרה-היום) — מול `~/SierraChart_Data/v9_export/` contract-id.
**Sierra:** `v9_export` 5min + contract-id היום (ספטמבר בכל-המקורות?).

## D25 — מעודכן 🟢→🟡 (I-41 שוכך): הטיה-כיוונית הצטמצמה ל-11S/2L

**עדכון-היום:** חזרו **2 LONGs** (226 REACTIVE_LONG, 227 FAMIR_LONG) מול **0** ב-06-22. **אנומליית-ה-all-short לא-חזרה** ⇒ ה-detectors **לא** עיוורים-מבנית ל-LONG. הנטייה-לשורט (11/2) תואמת יום-שמכר-מהפיק.
**שינוי מוצע:** **אין — הורד-ל-monitor.** D25 (audit-סימטריה) **לא-נדרש כפעולה** כל-עוד LONGs מופיעים. שמור פתוח-לניטור: אם יום-עם-ראלי-מתמשך שוב-מניב 0-LONG ⇒ להחיות.
**Regression:** המשך-מעקב יומי ביחס long/short.
**סיווג:** monitor (לא-פעיל).
**Sierra:** —

## D2 — מעודכן 🔴 (display-safe): ספירת-ירי שקרית ב-`build/pattern-status` (I-31, undercount חדש)

**שורש (החמיר):** `systems` היום: key2(five_min)=**13** (בפועל 7) · key4(woodies)=**0** (בפועל 6!). **S4=0 הוא undercount-מוחלט חדש** (06-22 היה overcount S4=8). לא-יציב בשני-הכיוונים.
**שינוי מוצע:** `fired_today_count` ייגזר מ-`SELECT COUNT(*) FROM v9_trades WHERE session_date=today GROUP BY firing_system` — מקור-אחד-אמין, לא counter-פנימי.
**Regression:** `pattern-status` fired-counts == `v9_trades` GROUP BY (S4=6, S2=7 היום).
**סיווג:** **display-safe** (לא-משפיע-ירי, אך מרעיל כיול).
**Sierra:** —

## D11 — מעודכן 🔴 (verify→trading): `sizing` נרשם אך לא-מיושם (I-34)

**שורש (מאומת-מדויק היום):** כל הפסד = `risk_pt×5×3` **בדיוק**; id213 `sizing="half"` הפסיד −$123.75 (=8.25×5×**3**, לא ×1.5); 220 `sizing=2`→×3; 215/216/217 `sizing="half"`→×3. **half/2/3 כולם 3-חוזים בפועל.**
**שלב-0 (verify — CC):** האם shadow מריץ **נומינלי-3-בכוונה** (תמים) או ש-`sizing→contracts` dead-wire בנתיב-הביצוע? נתיב: `daytype_playbook.decide()`/sizer → `trading_gateway.route_setup` → `trade_manager`.
**שינוי מוצע (אחרי-אבחון):** אם dead-wire — חבר `sizing→contracts→pnl_usd`; אם shadow-נומינלי-מכוון — תֵעד מפורשות (`pnl_mode="nominal_3"`) שלא-יוטעה.
**Regression:** id עם sizing=1 ⇒ |pnl|=risk×5×**1**; sizing=2 ⇒ ×2.
**סיווג:** **trading-risk → Michael** (אם dead-wire, חוסם position-sizing ל-LIVE). נספח ל-`feedback_full_decision_pipeline_wiring`.
**Sierra:** —

## D9 — מעודכן 🔴 (verify→DB): gap-ids ב-`v9_trades` (I-32, מתון) + cluster same-bar (I-39-adj)

**שורש:** ids חסרים **214, 222** (2, שיפור מ-6 ב-06-22). + **218≈219** same-system(S2)-same-bar(10:20)-same-entry(7446.75)-pattern-שונה (BEAR_FLAG/REACTIVE), 4ש' הפרש — `DEDUP_FIRE_GUARD` (key …+pattern+…) **לא-תפס** (pattern שונה); `cluster_guard` `recent_attempts=1` לא-הופעל.
**שלב-0 (verify — CC):** `SELECT id,entry_ts,pattern_id,entry_price FROM v9_trades WHERE <06-23> ORDER BY id` — 214/222 = insert-fail/rollback? 218/219 = 2-INSERTs נפרדים-לגיטימיים או cluster-לא-רצוי?
**שינוי מוצע:** (א) gap — לוג-WARNING על id-gap (no-silent-failures); (ב) cluster — הרחב `cluster_guard`/`DEDUP` למפתח `system+direction+entry±ε` (ללא-pattern) לתפוס same-bar-cross-pattern.
**Regression:** replay 218/219 ⇒ השני `blocked_by="CLUSTER"`; אין gap-id ללא-לוג.
**סיווג:** (א) display/safe; (ב) **trading-adjacent → Michael** (חוסם ירי-לגיטימי-לכאורה). משותף עם D23.
**Sierra:** —

## D19 — מעודכן 🟡 (verify→trading): day_type low-conf + UNKNOWN-בפתיחה (I-1/I-36)

**שורש:** `confidence=0.48 LOCKED_LOW_CONF` (עלה מ-0.18 ב-06-22), `vote_history=[]` (I-1, יום-N), `playbook=null`; **dt=UNKNOWN @08:55** (id213) — opening-lag שהשאיר TLB-ראשון ללא-שער. **השרשרת קריטית ל-D28:** שערי-תבנית×יום **חסרי-ערך** כל-עוד dt=UNKNOWN ב-30-הדק'-הראשונות.
**שלב-0 (verify — CC):** מדוע `vote_history` ריק (I-1, חוזר יום-N)? מתי dt עובר מ-UNKNOWN ל-Normal/Trend (זמן-סיווג-פתיחה)?
**שינוי מוצע:** (א) זירוז-סיווג-פתיחה (opening_type@15min / day_type@30min — ר' `project_s1_staging_spec`); (ב) **תנאי-קדם ל-D28:** עד-שיש-dt-ודאי, שער-ה-playbook משתמש ב-`woodies_trend`/opening_type כ-fallback (לא מאשר-הכל על UNKNOWN).
**Regression:** replay ⇒ dt≠UNKNOWN עד בר-6 (09:00); `vote_history` לא-ריק.
**סיווג:** **trading-logic → Michael** (משפיע אילו-שערים-חיים-בפתיחה). תלוי-הדדית עם D28.
**Sierra:** opening_type/IB מ-`sierra_tpo` (Rule 2).

## D23 — מעודכן 🟢 (trading-adjacent → Michael): DEDUP + cluster — אמת+כייל

**עדכון-היום:** **אין כפילות-byte היום** (≠199/200 ב-06-22). `DEDUP_FIRE_GUARD=1` חי. אך **cluster same-bar-cross-pattern (218/219) לא-נתפס** — ה-DEDUP keys על pattern, ושתי-התבניות שונות.
**שינוי מוצע:** הרחב כיסוי — key נוסף `system+direction+entry±ε` (ללא-pattern), חלון same-bar. (משולב D9-ב.)
**Regression:** replay 218/219 ⇒ השני `blocked_by="CLUSTER"`; 199/200 (06-22) ⇒ `DUPLICATE`.
**סיווג:** **trading-adjacent → Michael.**
**Sierra:** —

---

### עדיפויות (לסבב-CC הבא)
1. **D28 🔴🔴 (Michael) — playbook חי.** המנוף-היחיד-המהותי היום (+$142.50 ודאי). מאחד R1/R2/R3. **strategic-stop — אישור-Michael + restart.**
2. **D29 🟡 (CC) — gate-state-בזמן-ירי.** מכריע אם שאר-השערים בכלל-היו-חיים (מכריע גם את D28-3b ו-06-22's D26).
3. **D24 🟡 (CC) — אשר exec↔signal פוסט-גלגול** + נקה display-leak (4214/3745).
4. **D11/D9/D2 🔴/display** — sizing(אבחון), gaps+cluster, fired-count.
