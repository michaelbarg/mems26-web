## T-328 — **חבילת-הפעלה** (מייקל 18:00: "למה אין תיקונים שמאפשרים ירי — כל הרעיון היה שהמערכת תעבוד ותירה"). P0 עכשיו, מחליפה בסדר-העדיפויות את T-327a (שנבלע כאן). קומיט עד 19:00, הרנס ×5 (כולל 11.09), ריסטארט רק על מילת-מייקל.
כל התיקונים עד עכשיו היו שערים (חוסמים מפסידים). זו החבילה שמאפשרת את העסקאות הנכונות — ומבוססת על פסיקות **קיימות**, לא על מדידות חדשות:

### 1. העוגן של התבנית מגיע לשער (T-327a §3) — מאפשר כפולות ביום Normal
האמיטרים של `DOUBLE_TOP_AA`/`DOUBLE_BOTTOM_EE` (`info["structural_anchor"]`), `CEILING_FLIP` (P1/P2), `FAILED_BREAK` (הקיצון שנכשל) כותבים `setup["structural_anchor"]` (וגם ב-`metadata`). השער כבר קורא אותו ראשון (T-319b-lite). golden: `11.09 17:40:07 DOUBLE_TOP_AA_SHORT 7660.25` ⇒ anchor 7679 ⇒ `zone=near_vah` ⇒ **admitted** בדלתון (ואז R:R/גודל יכריעו — סטופ 7682.5 = 22 נק' ⇒ ייחסם בגודל, וזה נכון לכניסה-בצוואר; ראה §2).

### 2. `CEILING_FLIP` → **live** (פסיקת-מייקל 28.08: "…ולאחר מכן, בהינתן הטריגר, אני רוצה עסקה של שורט") — קוד שמממש פסיקה קיימת
- מצב: המפיק קיים ורץ ב-SHADOW (`[CeilingFlip] … (SHADOW)`), ירה נכון פעמיים ביומיים: 10.09 17:20 FLOOR_FAILED→LONG 7599.75 (→7620 תוך 5 דק'), 11.09 17:45 CEILING_FAILED→SHORT 7657.25.
- להעלות ל-live (הדגל הקיים של המפיק; RULED_FLAGS עם הציטוט של 28.08 + `measured:` = שני המקרים + הריפליי מ-08-28). כניסה = סגירת בר-האישור (כמו היום). סטופ = מעבר ל-P1/P2 + טיק (`BEYOND_REJECTED_EXTREME`). יעדים ביום Normal לפי הדוקטרינה של 11.09: T1=POC, T2=הקצה-הנגדי של הערך, T3=קצה-ה-IB הנגדי (דרך `trade_economics` — סמכותי **לתבנית זו בלבד**, לא גלובלי). גודל: לפי הסטופ המבני (פסיקת 09.09 10:20) — אם n<3 ⇒ לא נכנסים (ולא מקטינים סטופ).
- golden: 10.09 17:20 ⇒ LONG 7599.75 stop 7585.25 T1 7602.75 T2 7609.25 ⇒ **live write**; 11.09 17:45 ⇒ SHORT 7657.25 stop 7679 (22 נק' ⇒ n=2 ⇒ **לא נכנסים** — הכניסה בצוואר מאוחרת מדי; זה בדיוק למה §4).

### 3. אירוע-מבני מפרסם תווית מיד (T-286/היום 17:45) — מאפשר שורט-עם-ההרחבה בזמן
`DAYTYPE_RECLASS_STABILITY` (פסיקת 19-20.08, נגד ריצוד) נשאר — אבל **אירועים בינאריים של דלתון** (`acceptance_*`, הרחבה מעבר ל-IB — מה ש-`S1-BINARY` כבר מזהה) מפרסמים מיד, כמו T-313 בנעילה. ריצוד = שינוי בלי אירוע; אירוע = לא ריצוד. golden: 11.09 17:45:02 ⇒ `Variation` מפורסם ב-17:45 (לא 17:50); 09-09 18:15 ⇒ אותו דבר. הרנס: לספור כמה routes משתנים ולהדביק.

### 4. הכניסה של דלתון — בדחייה השנייה, לא בצוואר (T-327 §3) — **הלילה**, לא עכשיו
`CEILING_TOUCH2_REJECT`: בר-הנגיעה-השנייה נסגר מתחת לסגירת בר-הנגיעה-הראשונה (מראה לרצפה) ⇒ SHORT בסגירה, סטופ מעל השיאים+טיק, יעדים POC→VAL→IB-low. golden: 11.09 17:25 @7674.75 (stop 7679, T1 7667.5 ✓17:30, T2 7659.75 ✓17:35). shadow-twin ראשון, ואז מייקל פוסק לפי המספר — **אבל הקוד נבנה הלילה**, לא "בהמשך".

### כללים
הרנס ×5 — הימים הישנים: שינויים **רק** במה שהגולדנים מצפים (10.09 17:20 כתיבה חדשה); 0 Traceback. TASK_LOG+STATUS_BOARD באותו קומיט. LOG חתום עם השורות הגולמיות. אפס `.env` בלי snapshot; הדגל של CEILING_FLIP הוא הדגל הקיים של המפיק. **ריסטארט — מילת-מייקל בלבד** (אני מבקש ממנו כשההרנס ירוק).
