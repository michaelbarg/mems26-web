# Diagnose C5/C6/C7 — Sizing, Opening→Entry, Decision Tree · 2026-06-05

No code changes. Reports only. Michael decides.

## C5 · I-13: sizing=reject blocking patterns

### Finding
S2 `calculate_size()` (five_min_system.py:665-705) rejected **every detected pattern today** on a single gate: `location_vs_poc_vol = "far"`.

### The sizing function logic
```
bars_formed >= 3?      → ✅ (3-bar pattern)
COT/AMT aligned?       → ✅ (COT=-19652 < AMT=-16065 for SHORT)
location_vs_poc_vol?   → ❌ "far" (POC=7526, price=7490, 36pts away)
  "at" = ≤1.0pt from POC → full (3 contracts)
  "near" = ≤3.0pt → half (2 contracts)
  "far" = >3.0pt → REJECT
```

### Impact
- **100% of candidates blocked** — all on location=far
- On a trend day where price runs 60+ pts from POC, `location=far` blocks everything
- Auth table would ALLOW: DOUBLE_TOP_AA_SHORT × Variation = FULL (3,2,2)
- The sizing function is **more restrictive than the auth table** on trend days

### Proposal (Michael to decide)
| Option | Change | Risk |
|--------|--------|------|
| A: Relax "near" threshold | 3.0 → 10.0pt | More trades, some may be low-quality far from POC |
| B: Skip location check on trend days | location gate OFF when trend_state=RED/GREEN | Trades in trend direction fire regardless of POC distance |
| C: Downgrade location to advisory | location=far → half (not reject) | Every pattern with flow support fires at reduced size |
| D: Keep as-is | No change | 0 trades on trend days (current behavior) |

## C6 · I-14: opening→entry chain

### Full chain trace (verified against code + DB)
```
1. opening_type = OPEN_REJECTION_REVERSE    ✅ (detect_opening_type_cvd, 3+ bars)
2. IB = 7552.75 / 7505.75 = 47pts = WIDE   ✅ (Sierra TPO export)
3. day_type = Variation, conf=0.68          ✅ (B1 matrix: ORR × WIDE)
4. Auth table: REACTIVE_SHORT × Variation   ✅ = FULL (3,2,2)
5. Pattern: DOUBLE_TOP_AA_SHORT detected    ✅ (S2 live state)
6. calculate_size()                         ❌ = REJECT (location=far)
   └→ Gateway never called. No trade record.
```

**The chain is fully wired and classified correctly.** The break is at step 6 — sizing rejects on location, which is the same root as C5/I-13.

### DB evidence
```
v9_day_type_history: Variation, prob=0.68, WIDE, OPEN_REJECTION_REVERSE, PENDING
v9_trades: 0 rows for 2026-06-05
S2 last_reasoning: "DOUBLE_TOP_AA SHORT size=reject: 3-bar pattern, COT=-19652 vs AMT=-16065, location=far"
```

## C7 · I-10: Decision Tree design for S2/S3

### Current state
- **S4 (Woodies):** Has A1-A7 decision tree (`decision_tree.py`). 7 pre-fire stages, 14 active-trade stages. `evaluate_bar()` returns `ready_to_route`, `failed_stages`, `pending_stages`.
- **S2 (5-min):** No decision tree. Uses direct flow: `_detect_reactive` → `_detect_initiative` → `calculate_size` → `emit_t1_setup` → `gateway.route_setup`. Gates are inline code, not staged.
- **S3 (Footprint):** No decision tree. Direct signal→fire flow. Currently muted (S3_MUTE=1).

### Proposed design: S2 staged decision tree (A1-A7 equivalent)

| Stage | Name | Check | Current Code Location | Blocking? |
|-------|------|-------|-----------------------|-----------|
| A1 | Day Type Gate | day_type != Nontrend | L839 (NT early skip) | Yes |
| A2 | Pattern Detection | _detect_reactive / _detect_initiative | L852-857 | Yes |
| A3 | Sizing (Auth + Flow + Location) | calculate_size() | L665-705 | Yes |
| A4 | Choppiness Gate | choppiness_score < 70 | L836 (now continuous) | Advisory |
| A5 | R:R Validation | emit_t1_setup pre_fire | setup_emitter.py | Yes |
| A6 | Session Gate | is_within_firing_window() | gateway.route_setup | Yes |
| A7 | Gateway Risk | cooldown + SSV + cluster + chop_state | gateway.route_setup | Yes |

### Exposure in build_status
Each stage maps to a build_status component with:
```json
{
  "stage": "A3",
  "name": "Sizing (Auth + Flow + Location)",
  "status": "FAIL",
  "reason": "location=far (36pts from POC, threshold=3pt)",
  "blocking": true
}
```

This would replace the current `s2_inspector` pattern-status (which shows individual fields like `choppiness_ok`) with a staged view that matches S4's decision tree.

### NOT building this now — design only for Michael's review.

## NOT-DONE
- **No code changes** — all three items are diagnose/design per prompt
- **S4 Sierra DLL flags** (zlr_detected/hfe_detected) not set despite CCI matching — DLL investigation needed
- **sizing thresholds** — proposals above, pending Michael's decision
- **S2 decision tree implementation** — design only, needs approval
