# V6.5.4 Checkpoint

## Summary
Sprint focused on diagnosis + surgical fixes. No rewrites.

## Commits

### Commit 0+1+2+3+4+5 (combined — all surgical, no rewrites)

**Sierra Dispatch (Commit 1):**
- Status: VERIFIED (code path intact)
- Root cause: NOT a code regression. Chain is correct since V6.5.3.
  Most likely: Bridge wasn't running when EXECUTE was clicked, or Render was cold-starting.
- Added: `POST /trade/test-dispatch` diagnostic endpoint for Michael's local verification
- Added: `docs/V6.5.4_SIERRA_VERIFY.md` with 3-step local test

**Traffic Light (Commit 2):**
- Status: VERIFIED (already renders correctly)
- Root cause: not missing — the panel content was long and user had to scroll.
  The scroll-reset fix (Commit 5) ensures Signal tab always starts at top.

**Setups Tab Action Card (Commit 3):**
- Status: FIXED
- Root cause: card only renders when `selectedSweep` is set, but nothing auto-selected.
- Fix: auto-select first sweep event when entering Setups tab

**DayTypeBar overflow (Commit 4):**
- Status: FIXED
- Fix: added `minWidth:0, flexWrap:'wrap'` to the day type pills container

**Scroll Reset (Commit 5):**
- Status: FIXED
- Fix: `scrollRef` + `useEffect` resets scrollTop to 0 on tab change

## Open Items
- Pattern Scanner stop validation (Commit 6) — deferred to next sprint
- DEMO gates inline in TopBar (Commit 7) — already implemented in V6.5.2
- /analytics/by-segment (Commit 8) — already implemented in V6.5.2
- setup_attempts table (Commit 9) — already implemented in earlier sprint

## For Michael
1. Start Bridge: `export MEMS26_ENTRY_MODE=DEMO && python3 json_bridge.py`
2. Run test: `curl -X POST https://mems26-web.onrender.com/trade/test-dispatch`
3. Check Bridge log for `[C4]` output
4. Check `/Users/michael/SierraChart2/Data/trade_command.json` exists
5. See `docs/V6.5.4_SIERRA_VERIFY.md` for detailed steps
