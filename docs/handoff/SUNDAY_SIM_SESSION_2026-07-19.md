# יום ראשון 19/07 בבוקר — סשן-סים מלא: להביא את המערכת מוכנה ליום שני

**פסיקת-מייקל 2026-07-18 ערב:** *"נבצע את זה מחר, בסים, שהמסחר ייפתח לעבודה. חוץ לשעות מסחר.
תביא לקלוד-קוד ולשני לעבוד."*

**מבצעים:** `cc-macbook` (קוד+סים על מכונת-המסחר) · `cc-imac` (סים מקביל/גיבוי) · `cursor-agent` (מעקב).
**מאמת:** `cowork-dev`. **שערים-אנושיים:** מייקל (Sim Mode, Remote Build אם יידרש, פסיקות).
**מטרה יחידה: יום שני 20/07 ב-16:30 נפתח עם מערכת שיורה — לא עם מערכת שחוסמת.**

## הרקע בשורה
יום-לייב-1 (07-17): לייב **−$58.75**, **מערכת-4 ירתה 0 לייב**, אבל ספר-הצללים עשה **+$277**
(שורטי-S4 4/4 מנצחים). **התבניות עבדו — הצינורות והשערים חסמו.** 5 באגי-חסימה-שקטה תוקנו חי.
מה שנשאר = רשימה סגורה, וזה מה שסוגרים היום.

## ⚠️ חוקי-בטיחות לסשן (השוק סגור — ובכל זאת)
1. **הכל על סים.** לפני **כל** פקודה: אמת `is_sim=1` מ-`sierra_state.json`. `is_sim=0` → **עצור מיד**.
2. **אפס PLACE/PLACE_STOP על לייב.** גם לא "רק לבדוק".
3. **דגלי-סיכון נשארים כפי-שהם ב-.env** — ניסויים בסביבת-הסים בלבד. הדלקה קבועה = פסיקת-מייקל.
4. **snapshot לפני כל נגיעה ב-DLL/.env/LaunchAgent** (`scripts/mems26_snapshot.sh`).
5. **בסוף הסשן: להחזיר את סיירה ללייב** + לאמת `is_sim=0` — אחרת יום שני נפתח על סים ולא נסחר.
6. חוק-5: כל "בוצע" = פקודה + פלט-גולמי ב-`LIVE_CHANNEL.md`.

---

## 🔵 cc-macbook — הסדר שלך (עצור בכל שער-אנושי)

### S1 · השלמת משימה 1ב — אימות-סים ל-PLACE_STOP  ⏸ שער: מייקל מעביר ל-Sim Mode
לפי `CC_PROMPT_DLL_PLACE_STOP_2026-07-18.md` §אימות-סים. ה-DLL כבר נבנה (RB 17:11, אומת).
צור יתום ‎-2 בסים → הדלק `ORPHAN_AUTO_STOP_V1=1` **בסביבת-הסים בלבד** → **הוכח:** הסטופ נח בצד
ובמחיר הנכונים, `working_orders` 0→1, **הפוזיציה לא גדלה**. מקרה-מראה LONG. חד-פעמיות.
אם `r=-1` גם ליתום-נקי (`working=0`) — **עצור, אל תעקוף, דווח.** זו תוצאה לגיטימית.

### S2 · מטריצת-הדמיה מלאה — הלב של היום
**זה מה שיקבע אם יום שני יורה.** הרץ את מה שכבר בנוי:
```
python3 scripts/sim_matrix.py                          # 104 תאים — לוגיקה
python3 scripts/audit_pattern_miss.py --date 2026-07-17 --relax all
python3 scripts/audit_pattern_miss.py --date 2026-07-16 --relax all
python3 scripts/audit_pattern_miss.py --date 2026-07-15 --relax all
```
ואז **E2E על סיירה-סים**: לכל סוג-יום (`DAY_TYPE_MANUAL_OVERRIDE=<תאריך>:<סוג>` → ריסטארט-קל),
לכל תבנית שה-playbook לא מסמן SKIP — עסקת-סים מלאה: כניסה→סולם→MODIFY_STOP→מילוי-יעד→BE→S6→סגירה.
**תוצר: טבלת 7×תבניות עם ✅/❌ + הראיה בכל תא.** כל ❌ = תיקון + ריצה-חוזרת.

### S2b · אימות STOP_WIDEN_TO_FLOOR_ON_REJECT_V1 (פסיקת-מייקל 07-19)
בסביבת-הסים בלבד: `STOP_WIDEN_TO_FLOOR_ON_REJECT_V1=1`. צור תרחיש שבו StopResolver **דוחה**
(אין שלב-מבני בטווח) והסטופ-המקורי צר מרצפת-ה-ATR. **הוכח:** הסטופ נדחף לרצפה, על הצד-המגן,
לא-מעבר-לתקרה, והעסקה נורית. בדוק את האינטראקציה עם `SIZE_CAP_CUT` (סטופ-רחב עלול לחתוך חוזים).
✅ → דווח ל-cowork להדלקה קבועה (RULED→1). ❌/לא-צפוי → השאר OFF ודווח.

### S2c · סניטי S4_OVERRIDE_AWARE_V1 (פסיקת-מייקל 07-19, כבר דלוק)
הצב `DAY_TYPE_MANUAL_OVERRIDE=<היום>:Variation` בסים → ירה תבנית-S4 → **אמת:** הסיזינג
(V2Sizing) **וגם** היעדים (compute_targets_for_day_type) חושבו לפי **Variation**, לא Normal.
זהו סניטי (הדגל כבר חי) — אם הסיזינג/יעדים עדיין Normal, זו רגרסיה → דווח מיד.

### S3 · 2 כשלי-הסימולציה הידועים
`Neutral_Center × HTLB` · `Neutral_Extreme × TLB` — למצוא שורש ולתקן (או לתעד כ-SKIP מכוון).

### S4 · הרחבת כיסוי-הביקורת
`audit_pattern_miss.py` מכסה היום רק ZLR/GB100/TT + REACTIVE/INITIATIVE.
הוסף: **TLB · HTLB · VEGAS · GHOST · FAMIR · DBDT**. בלי זה אנחנו עיוורים לחצי מהתבניות.

### S5 · 6 הכשלים בסיווג-יום (קיימים-מראש — אבל זה הלב של המערכת)
`test_daytype_gate_live`(2) · `test_classifier_core_parity`(2) · `test_opening_fire_cvd`(1) ·
`test_s2_detect_on_completed_bar`(1). אומת שהם **לא רגרסיה** (נכשלים גם בקומיט-ייחוס), אבל התוכן
מדאיג: `expected Neutral_Center, got Normal_Variation`. **קבע:** האם הטסטים מיושנים מול דוקטרינה
מאושרת חדשה, או שהמסווג באמת נסוג? זו בדיוק התלונה של מייקל ("המערכת לא מזהה שהיום הפך לנייטרלי").

---

## 🟠 cc-imac — במקביל (סים בלבד, לא לחמש)
1. **שכפול-אימות S2:** הרץ את אותה מטריצת-הדמיה על ה-iMac כגיבוי-הצלבה — אם תוצאה שונה מ-cc-macbook,
   זה ממצא חשוב בפני-עצמו (הבדל-סביבה) → דווח.
2. **S6-EOD 07-17** (פריט S-11 שנשאר פתוח): `python3 scripts/s6_eod_report.py --date 2026-07-17`
   + ממצאי-CRITICAL ל-`task_board.json`.
3. **ריפליי-סיווג 15/16/17-07** — לתמוך ב-S5 של cc-macbook: האם המסווג מייצר את הקריאות של מייקל
   (16/07: Normal→Neutral_Center→Neutral_Extreme בזמנים שקרא).
4. **חוק סוחר-יחיד:** ה-iMac נשאר סים. אל תחמש. אל תיגע ב-.env של הלייב.

---

## 🟣 cursor-agent — מעקב
עדכן `MONDAY_CHECKLIST_2026-07-20.md`: הוסף **שער S** (סשן-ראשון) עם S1–S5 + משימות-iMac,
כל אחת עם בעלים/תלות/קריטריון-סיום. סמן את **שערי-האנוש** (Sim Mode בהתחלה, החזרה-ללייב בסוף).
⛔ **הכלל הקריטי:** הצ'קליסט לא נסגר בלי הפריט האחרון — **"סיירה הוחזרה ללייב + `is_sim=0` אומת"**.
זו התקלה הקלאסית: לסיים סשן-סים ולשכוח להחזיר.

---

## 🟢 מייקל — 3 דברים שרק אתה
1. **בהתחלה:** סיירה → **Sim Mode ON** (משחרר את S1 ואת כל ה-E2E).
2. **במהלך:** להוסיף **סטאדי-CVD לצ'ארטבוק** — `cumulative_delta.json` ריק, ואישור-הזרימה של S2
   מנוון בלעדיו. זה חוסם-איכות אמיתי ליום שני.
3. **6 הפסיקות** (סף-14:30 · entry_not_confirmed · StopResolver · A5 · A6 · הדלקת-ORPHAN) —
   **אלה החוסם האמיתי**, לא הקוד. בלעדיהן יום שני ייפתח עם אותם שערים שחסמו ביום שישי.
4. **בסוף:** סיירה → **חזרה ללייב** (אני אזכיר ואאמת).

---

## תוצר הסשן
שורות חתומות ב-`LIVE_CHANNEL.md` פר-משימה (פקודה+פלט), טבלת-המטריצה המלאה, ו-`MONDAY_CHECKLIST`
מעודכן. **קריטריון-הצלחה: בסוף ראשון אנחנו יודעים — בראיות — שכל תבנית מאושרת יורה בסוג-היום שלה,
ושאין חוסם-שקט שנשאר.**
