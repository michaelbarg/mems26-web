סט# MEMS26 Full Path Mega Table — Code vs Spec End-to-End Audit

**Date:** 2026-05-31  
**Status:** READ-ONLY audit — zero code changes  
**Scope:** Full trade path from data ingestion through closing — all 6 systems, gateway, trade manager, persistence  
**Shadow flags ON:** `S2_ATR_RELATIVE`, `S3_RELATIVE`, `S1_CVD_OPENING`, `S1_IB_WIDTH_ATR`, `S1_DAYTYPE_STAGING`

---

## Mega Table: Full Trade Path

| # | Step | System / Component | Role | Input | Calculation / Logic | Output | Feeds To | Code Location | Spec Source | Code=Spec? |
|---|------|-------------------|------|-------|---------------------|--------|----------|---------------|-------------|------------|
| **1** | **Data Ingestion (5-min bars)** | Bridge → BarRouter | infrastructure | Sierra DLL export JSON (OHLCV + studies) | Bridge reads `v9_export/*.json`, pushes to `localhost:8000/api/v9/bars`. BarRouter fans out to subscribed systems. | BarEvent per bar_type (5min, woodies_5min, tick_reversal_15/12) | S1, S2, S3, S4, S5 | `bridge/v9_streams/base_stream.py`; `backend/v9/services/bar_router/` | Constitution V3 §Layer 0-1; CLAUDE.md §Bridge Local-Only | **Y** |
| **1b** | **Data Ingestion (footprint)** | Bridge → BarRouter | infrastructure | Sierra tick_reversal_15 / tick_reversal_12 bars with footprint cells (ask_vol, bid_vol per level) | Same bridge pipeline, different bar_type subscription | tick_reversal BarEvent | S3 | `bridge/v9_streams/` | Constitution V3 §Layer 3 | **Y** |
| **1c** | **Data Ingestion (Woodies studies)** | Sierra DLL → Bridge | infrastructure | DLL computes 11 studies (CCI-14, TCCI, EMA-34, LSMA, SWI, CZI, trend_state, predictor, HFE, ZLR flags) and exports per 5-min bar | Sierra DLL source of truth; Python fallback `compute_all_studies()` if DLL absent | woodies_5min BarEvent with study fields | S4 | `sc_study/MES_AI_DataExport.cpp`; `backend/v9/systems/woodies/woodies_system.py:251-270` | D-092 §Source of truth | **Y** — DLL is canonical, fallback documented |
| **2** | **S1·A1 Pre-Open Context** | S1 Day Type / state_machine | observer | Previous day (PD) high/low/close/settle; overnight high/low; current bar OHLC; ATR-14 daily | Gap = open - PD close. **Flag-OFF:** gap > ±2.0pt → UP/DOWN, else FLAT; magnitude = gap/ATR (≥1.0=LARGE, ≥0.3=MEDIUM, else SMALL). **Flag-ON (S1_IB_WIDTH_ATR):** gap classified via ATR tiers: <0.25=TINY, <0.50=SMALL, <1.0=MEDIUM, ≥1.0=LARGE | PreOpenContext: gap_size, direction, magnitude, location_vs_pd, overnight_bias | A2 | `backend/v9/systems/day_type/state_machine.py:357-442`; `detector.py:211-234` | Day Type spec §A1; Constitution V3 §Layer 2 | **Y** |
| **3** | **S1·A2 Opening Type Detection** | S1 Day Type / detector | observer | First ≥3 RTH bars; (optional) footprint deltas, ATR-daily | **Flag-OFF (S1_CVD_OPENING):** Price-based only. directional_ratio ≥0.7 → OPEN_DRIVE (conf 0.95); pullback 20-60% → OPEN_TEST_DRIVE (0.70); retrace >50% → OPEN_REJECTION_REVERSE (0.65); outside PD → OPEN_AUCTION_OUT (0.50); inside PD → OPEN_AUCTION_IN (0.40). **Flag-ON:** Dual-path CVD shadow. PE_30>0.65 + range_exp>1.0 → DRIVE; net_CVD/total<0.15 + PE<0.25 → AUCTION. CVD label replaces price-based when confident. | (OpeningType, drive_direction, confidence, cvd_meta) | A3, B1 | `detector.py:119-191` (price); `detector.py:255-363` (CVD) | Day Type spec §A2; D-091.Q1 | **Y** |
| **4** | **S1·A3-A4 IB Tracking + Lock + Width** | S1 Day Type / state_machine + detector + extensions | observer | RTH bars (Sierra Study ID:6 = IB source); config.ib_period_min = 60 | Accumulate IB high/low from bars. Lock at 60 min (10:30 ET). ExtensionTracker locked on IB lock. **Flag-OFF (S1_IB_WIDTH_ATR):** classify_ib_width(ib_range, narrow_max=15.0, medium_max=25.0) → NARROW (<15pt), MEDIUM (15-25pt), WIDE (>25pt). **Flag-ON:** classify_ib_width_atr(ib_range, atr_daily) → NARROW (<0.5 ATR), MEDIUM (0.5-1.0), WIDE (1.0-1.5), EXTREME (>1.5 ATR). | IBClassification: ib_high, ib_low, ib_range, ib_width (incl. EXTREME when flag-ON) | B1 | `state_machine.py:460-518`; `detector.py:14-75`; `extensions.py:72-83` | Day Type spec §A3-A4; D-091 NeuE split | **Y** |
| **5** | **S1·B1 Decision Matrix** | S1 Day Type / decision_matrix | observer | (OpeningType, IBWidth) from A2+A4 | 25-cell matrix lookup → DayType. Example: (OPEN_DRIVE, NARROW) → Trend_Normal (60%), Trend_DD (25%), Variation (15%). (INDETERMINATE, *) → Normal. **Flag-ON (S1_IB_WIDTH_ATR):** EXTREME column added (same matrix, 4th width class). Initial confidence = 0.5. | Initial DayType vote + probability distribution | B2-B6 | `decision_matrix.py:30-151` | Day Type spec §B1; Constitution V3 §Layer 2 | **Y** |
| **6** | **S1·B2-B6 Evolution + Staging** | S1 Day Type / state_machine + detector | observer | Post-IB bars; extensions_up/down; behavior; range/ATR ratio; vote_history | B2: Extension tracking (above/below IB). B3: Behavior detection (TRENDING/FAILED_EXT/COMPRESSED/DEVELOPING). B4: Failed extension detection. B5: Range/ATR classification (COMPRESSED <0.7, NORMAL 0.7-1.3, EXPANDED 1.3-2.0, EXTREME ≥2.0). B6: Re-score — behavior agreement (+0.30), range alignment (+0.20), vote stability (×0.40), base (0.10). Switch threshold: ΔConf > 0.15. **Flag-ON (S1_DAYTYPE_STAGING):** Confidence capped at 60% before session_min 60 (pre-IB lock). C-period (10:30-11:00 ET) re-eval: retrace <25% → HOLD; retrace ≥50% → RE_DIAGNOSE. | Updated DayType vote + refined confidence | C1 | `state_machine.py:542-627`; `detector.py:368-492`; `detector.py:78-114` (staging) | Day Type spec §B; Constitution V3 | **Y** |
| **6b** | **S1·B+ Zohar Rules** | S1 Day Type / zohar_rules | observer | Current DayType, price vs IB, opening state, extensions, TPO width, time | 6 rules: Alpha (TDD invalidation if inside IB), Beta (Drive failure), Gamma (Test Drive failure), Delta (both-side ext → Neutral), Width (>5 TPO letters → Normal), Timing (reversal before 12:30 → Variation; trend after 12:30 → TDD) | RuleVerdict: INVALIDATE/CONFIRM/UPGRADE/DOWNGRADE/NO_OPINION | C1 lock | `zohar_rules.py:35-171` | Zohar Ch.6-7 | **Y** |
| **6c** | **S1·Neutral Subtype** | S1 Day Type / neutral_classifier | observer | session_open_price, prev_vah, prev_val | D-091.Q1: Open at/beyond VA edge (±0.25pt tolerance) → Neutral_Extreme; open inside VA → Neutral_Center. Degenerate VA (<0.25pt range) → Neutral_Center. | DayType.Neutral_Extreme or Neutral_Center | Playbook, Auth Table | `neutral_classifier.py:13-49` | D-091.Q1 | **Y** |
| **7** | **S1·C1-C3 Lock + Playbook** | S1 Day Type / state_machine | observer | Vote history, confidence, session_min, Zohar verdicts | C1 Lock: confidence ≥ 0.70 (ConfidenceThreshold compat class) OR consecutive_same_vote ≥ 2 OR session_min ≥ 210 (13:00 ET forced lock). Post-lock re-eval: extreme move (>3 ATR/30min), failed extension, range exceeded. C3 Playbook: PLAYBOOK_TEMPLATES maps DayType → (strategy, sizing, time_stop_min, key_rules). | DayTypeClassification (13 fields) + PlaybookOutput → consumed by all firing systems | S2, S3, S4 (day_type context) | `state_machine.py:680-746`; `state_machine.py:38-116` (templates) | Day Type spec §C; D-091 | **Y** — note: ConfidenceThreshold class hacks __eq__ to equal both 0.85 and 0.70 |
| **7b** | **S1 Playbook Sizing Labels** | S1 Day Type / state_machine | observer | DayType | Trend_Normal→AGGRESSIVE, Trend_DD→AGGRESSIVE, Variation→FULL, Normal→STANDARD, Neutral_Extreme→HALF, Neutral_Center→HALF, Nontrend→MIN | sizing label (AGGRESSIVE/FULL/STANDARD/HALF/MIN) | S2 setup_emitter (superseded by Auth Table) | `state_machine.py:38-116` | Constitution V3 §Layer 2 | **N** — see note [GAP-1] |
| **8** | **S2 Pattern Detection (OFA)** | S2 Five-Min / five_min_system | firing | Last 20 5-min bars; COT/AMT from Sierra; belly ratio (ask/bid) | **Reactive (Seller/Buyer Weakness):** Bar1=bearish+high_vol → Bar2=vol_drop≤10% → Bar3=bullish+belly(≥1.5)+POC_VOL↑ → Bar4=bullish>bar3.high+COT>AMT. Lookback 3 bars quiet (max<60% bar1). **Initiative (Expansion):** Bar1=bullish+range 1.5-1.75pt → Bar2=test(POC return≤0.5pt) → Bar3=joining(range>bar1) → Bar4=2nd_test+close>bar1.high+COT<AMT. **Flag-ON (S2_ATR_RELATIVE):** Expansion range = 1.5-2.0× ATR5m; POC tolerance = 0.2× ATR5m. | pattern_name, direction, confidence (0.75-0.80), structural_anchor | setup_emitter | `five_min_system.py:461-603`; constants lines 30-46 | D-091 §OFA; Constitution V3 §Layer 1 | **Y** |
| **9** | **S2 Pattern Detection (Chart — H&S)** | S2 Five-Min / patterns/head_shoulders | firing | Last 30 bars (SEARCH_WINDOW); min 12 bars | **Inverse H&S LONG:** 3 swing lows, head lowest, shoulder symmetry ≤5%, head extension ≥2 ticks (0.5pt), neckline = max intermediate highs, breakout > neckline+1T. Confidence: base 0.60 + symmetry 0.20 + extension 0.20. Stage 3 (day-type gated). | kind="INVERSE_HNS", structural_anchor=rs-1T, pattern_measure | setup_emitter | `patterns/head_shoulders.py:141-207` | D-091 §H&S (Bulkowski 3197 trades, 65% throwback) | **Y** |
| **9b** | **S2 Pattern Detection (Chart — Double BT)** | S2 Five-Min / patterns/double_bt | firing | Last 30 bars; min 10 bars | **Double Bottom Eve&Eve LONG:** 2 swing lows within 3% symmetry; Eve variant (both troughs ≥3 bars wide); neckline rise ≥10%; breakout > neckline+1T. Confidence: base 0.60 + symmetry 0.20 + width 0.20. Stage 3. | kind="DOUBLE_BOTTOM_EE", structural_anchor=lower_trough-1T | setup_emitter | `patterns/double_bt.py:165-227` | D-091 §Double (Bulkowski 952 trades) | **Y** |
| **9c** | **S2 Pattern Detection (Chart — Flags)** | S2 Five-Min / patterns/flags | firing | Last 30 bars; min 10 bars | **Bull Flag LONG:** Pole 5-15 bars, height ≥4.0pt (16 ticks), ≥60% bullish closes; flag 3-8 bars, retraces ≤50% pole; breakout > flag_high+1T. **Flag-ON (S2_ATR_RELATIVE):** pole_min = 5.5× ATR; flag_max = 2.5× ATR. Confidence: base 0.60 + pole 0.20 + retrace 0.20. Stage 3. | kind="BULL_FLAG", structural_anchor=flag_low-1T, pattern_measure=pole_height | setup_emitter | `patterns/flags.py:141-200` | D-091 §Flags (Bulkowski 1028 trades; Brooks H2/L2) | **Y** |
| **10** | **S3 Footprint Signals** | S3 Footprint / footprint_system | firing | tick_reversal_15/12 bars; footprint cells (ask/bid per level); COT; AMT (18-bar rolling) | 4 detectors: **Absorption** (strong opposing vol at extreme → reversal), **Stacked Imbalance** (3+ consecutive levels ask/bid ratio ≥2.5), **Sweep-Return** (liquidity sweep + false breakout), **Exhaustion** (directional bar + diminishing vol). Dominance: ask/bid ≥1.5 = BUYER_DOMINATE. **Flag-ON (S3_RELATIVE):** accumulation range = 1.0× ATR5m (not 15 ticks); min_level_vol = 0.3× median (not 10). Confluence: STRATEGIC ≥6+pattern, TACTICAL ≥4+pattern. | T3 signal: direction, strength, confluence classification | gateway.route_setup | `footprint/footprint_system.py:302-387`; `signals/` | D-089; Constitution V3 §Layer 1 T3 | **Y** |
| **11** | **S4 Woodies Decision Tree** | S4 Woodies / woodies_system + decision_tree | firing | woodies_5min bars (11 studies from DLL); last 50 bars; day_type, TPO, killzone context | **9 patterns:** CONT (ZLR, TLB, TT, GB100), REV (VEGAS, GHOST, FAMIR, HTLB, HFE). **A1 Trend Gate:** YELLOW (5th opposite bar) → BLOCK ALL 9 (P-W5 LOCK A). GRAY + conf <0.55 → fail. **A2** Study validity. **A3** Pattern detection. **A4** Touch-points (day_type/TPO/veto/killzone — advisory). **A5** Aux alignment (SWI/CZI/TCCI/LSMA/EMA) → sizing. **A6** Entry classification (TACTICAL→REACTIVE, STRATEGIC→INITIATIVE). **A7** pre_fire_validator (FireRequest). B-stages delegated to TradeManager. | T2 signal: pattern, direction, confidence, tier, stop/targets | gateway.route_setup | `woodies/woodies_system.py:202-494`; `decision_tree.py` | D-092; Constitution V3 §Layer 1 T2 | **Y** — note: 39 ZLR test failures unresolved (P-W3) |
| **12** | **S5 TPO (Observer)** | S5 TPO / tpo_system | observing | 5-min bars | Builds letter-by-letter profile. POC (highest TPO count), VAH/VAL (70% value area). 8 profile shapes (D, WIDE_D, COMPACT_D, P, b, DOUBLE_DIST, THIN, NEUTRAL). IB lock at 60 min. POC migration tracking. HVN/LVN detection. | POC, VAH, VAL, shape, IB state, migration, HVN/LVN | S1 (neutral classifier), S2 (quality tier proximity), S4 (A4 touch-point) | `tpo/tpo_system.py:133-200` | S5 spec; Constitution V3 §Layer 2 | **Y** |
| **13** | **S6 Killzone (Observer)** | S6 Killzone / killzone_system | observing | ET time (30s polling via tick()) | 8 zones: ASIA (18-03, low), LONDON (03-08, medium), NY_PREMARKET (08-09:30, medium), NY_OPEN (09:30-11:30, high), MIDDAY (11:30-13:30, low), NY_PM (13:30-16, medium), POST_MARKET (16-17, low), CLOSED (17-18, none). Weekend detection. | Current zone, next zone, edge_class | S4 (A4 touch-point) | `killzone/killzone_system.py:83-119`; `definitions.py:11-21` | S6 spec; Constitution V3 | **Y** |
| **14** | **S2 Auth Table + Quality Tier** | S2 Five-Min / auth_table_v1 + quality_tier | firing | pattern_name, day_type, current_price; TPO data (POC/VAH/VAL) | **Quality Tier:** price within 2.0pt of POC/VAH/VAL → HIGH; price inside VA → MEDIUM; outside → LOW. **Flag-ON (S2_ATR_RELATIVE):** proximity = 1.25× ATR5m. Fallback: MEDIUM if TPO unavailable. **Auth Table:** 70-cell lookup (pattern × day_type) → (verdict, HIGH_contracts, MED_contracts, LOW_contracts). SKIP verdict → no fire. Max = 3 contracts. Nontrend → always SKIP (0,0,0). | verdict (FULL/REDUCED/SKIP), quality_tier, sizing_contracts (0-3) | setup_emitter → T1Setup | `auth_table_v1.py:33-104`; `quality_tier.py:41-78` | S2_AUTH_TABLE_V1.md (LOCKED 2026-05-25); D-091 §Q6 | **Y** — 70 cells match spec exactly |
| **15** | **S2 Adaptive Stop** | S2 Five-Min / adaptive_stop | firing | entry_price, structural_anchor, ATR multiplier per family, today_typical (P75 of bar ranges) | **3 layers:** A (structural): anchor ± 1T. B (ATR cap): entry ± multiplier × today_typical. C (floor): entry ± FLOOR_TICKS × 0.25pt. **Multipliers:** Reactive=1.0×, OFA/Flag=1.5×, Double/H&S=2.0×. **Selection:** LONG: max(A,B) then clamp≤C. SHORT: min(A,B) then clamp≥C. **Flag-ON (S2_ATR_RELATIVE):** floor = max(round(1.75×ATR5m/0.25), 4) ticks. Binding layer A → reduce_size_signal. | StopComputation: stop_price, binding_layer (A/B/C), reduce_size_signal | T1Setup.stop_price | `adaptive_stop.py:130-194` | D-091 §Adaptive Stop Engine | **Y** |
| **16** | **S2 Contract Split** | S2 Five-Min / contract_split | firing | pattern_name | OFA: 25%/50%/25% (T1/T2/T3). H&S + Double: 33%/33%/34%. Flags: 50%/50%/0% (no T3). | t1_pct, t2_pct, t3_pct | T1Setup | `contract_split.py:15-30` | D-091 §Contract Distribution | **Y** |
| **17** | **S2 Time Stop** | S2 Five-Min / time_stop_mapper | firing | day_type | TN: None (ride trail). TDD: 90min. NV: 60min. NeuE: 45min. NeuC: 30min. Norm: 30min. NT: NO_TRADE. | time_stop_minutes (Optional[int]) | T1Setup | `time_stop_mapper.py:12-30` | D-091 §Time Stops; S2_EXIT_DEFINITION_V6 §Type C | **Y** |
| **18** | **S2 Setup Emitter** | S2 Five-Min / setup_emitter | firing | pattern result, day_type, prices, TPO data | 1. D-091.Q2: Refuse NT setups. 2. Quality tier lookup → verdict + contracts. 3. SKIP → return None. 4. Time stop lookup. 5. Contract split. 6. Build T1Setup (confidence=75 base). 7. pre_fire_validator (7 checks). 8. Return T1Setup or None. | T1Setup (complete) or None | gateway.route_setup | `setup_emitter.py:24-120` | D-091; D-063 (pre_fire_validator) | **Y** |
| **19** | **Pre-Fire Validator (7 checks)** | Shared / pre_fire_validator | gate | FireRequest: system_id, direction, entry, stop, t1, t2, time_stop, confidence | 1. system_id ∈ {T1_NUMBER_BAR, T2_WOODIES, T3_FOOTPRINT} (Pydantic). 2. direction ∈ {LONG, SHORT} (Pydantic). 3. Stop side: LONG stop<entry; SHORT stop>entry. 4. Target ordering: LONG entry<t1<t2; SHORT entry>t1>t2. 5. R:R ≥ 1.0. 6. confidence ∈ [0,100] (Pydantic). 7. time_stop ∈ [1,180] (Pydantic). | FireResponse: valid + fail_reason | setup_emitter / gateway | `pre_fire_validator.py:42-59` | M18; D-063; Strategic Chat S2 spec | **Y** |
| **20** | **Gateway: 5 Risk Gates** | Gateway / trading_gateway + risk_checks + cooldown + suffering_side_veto | gate | Setup dict, direction, gateway state | **Gate 1 (ζ.A4): Cooldown** — 2 consecutive STOP → 30min block. **Gate 2 (ζ.B2): SSV** — last 10 trades, ≥60% stop rate on one side → veto that direction (min 2 trades). **Gate 3 (ζ.F2): Chop** — Layer 0 chop_state="SEARCHING" → block. **Gate 4 (ζ.A5/D-037): Cluster Guard** — 5+ trades in 60s → 5min block (DEMO/LIVE only; SHADOW records). **Gate 5 (LIVE-only): Strict checks** — time cutoff 14:30 ET, daily loss cap $250, max 5 trades/day, 2 consecutive losses → stop day, news block (placeholder). | blocked_by reason or PASS | route_setup execution | `trading_gateway.py:86-121`; `cooldown.py:18-92`; `suffering_side_veto.py:19-63`; `risk_checks.py:26-75` | 3-Mode Spec V3 §5; D-037; D-049 | **N** — see note [GAP-2] |
| **21** | **Gateway: route_setup Routing** | Gateway / trading_gateway | routing | Setup that passed all risk gates | **SHADOW:** Always executed, unlimited parallel slots, recorded to shadow_trades list (capped 500). **DEMO:** Single slot, `if demo_slot is None` → fill, else skip. **LIVE:** Single slot, `if live_slot is None AND passes_strict_checks` → fill, else skip. | trade_id per mode (shadow always, demo/live if slot free) | trade_manager.accept_setup | `trading_gateway.py:109-141` | 3-Mode Spec V3 §8; Architecture docs; MASTER_DEV_SKILL.md | **N** — see note [GAP-3] |
| **22** | **TradeManager: accept_setup** | Services / trade_manager/manager | lifecycle | Setup dict (firing_system, direction, stop, t1, t2, t3, metadata); mode (shadow/demo/live) | Creates V9Trade row (state=PENDING). Creates TradeStateMachine(PENDING). Resolves trail config from day_type × pattern (D-094 §3.A). Persists + flush to get trade_id. Emits "trade_created". | trade_id; V9Trade row in DB | on_fill | `manager.py:84-187` | Constitution V3 §Layer 4; W11 | **Y** |
| **23** | **on_fill → FILLED** | Services / trade_manager/manager | lifecycle | trade_id, fill_price | Transitions PENDING → FILLED. Sets entry_price = fill_price, entry_ts = market time (UTC). Emits "trade_filled". | V9Trade.state = FILLED | BarLevelDetector monitoring | `manager.py:189-204` | W11 lifecycle | **Y** |
| **24** | **BarLevelDetector: Hit Detection** | Services / trade_manager/bar_level_detector | monitoring | Each 5-min bar (high, low, ts); all active trades | Per bar: iterate all FILLED/PARTIAL trades. **Stop check FIRST** (adverse fill priority): LONG bar_low ≤ stop → on_stop_hit; SHORT bar_high ≥ stop → on_stop_hit. **Then targets T1→T2→T3 sequential:** LONG bar_high ≥ target → on_target_hit; SHORT bar_low ≤ target → on_target_hit. Dedup by bar_ts (floor to minute). | Calls on_stop_hit or on_target_hit per trade | TradeManager state transitions | `bar_level_detector.py:43-128` | Constitution V3 §Layer 4 (targets); bar-level resolution | **Y** |
| **25** | **T1 Hit → Smart BE+1T** | Services / trade_manager/manager | management | T1 hit event | Transitions → PARTIAL. Applies Smart BE+1T: moves stop to entry + 1T (LONG) or entry - 1T (SHORT). 1T = MES_TICK_SIZE = 0.25pt. Idempotent: no-op if stop already at/tighter than BE+1T. Cross-context audit entry logged. PnL calculated. | V9Trade.state = PARTIAL; stop moved to BE+1T | Continued monitoring | `manager.py:206-248; 261-315` | D-094 Gap 1 fix; S2_EXIT_DEFINITION_V6 §Smart BE | **Y** |
| **26** | **Trade Management Rules** | Gateway / trade_management | management | Active trade + current bar data | **C.2 Trail T3:** After T2 hit, trail by high watermark - 2.0pt (MES). **C.4 Lock-in 2R:** At 2R profit, move stop to entry+1R. **C.6 Time Decay:** 10 bars no progress (>1pt) → tighten to entry-stop midpoint. **C.7 Reversal Exit:** Opposing system fires reverse direction → close immediately. | Stop modifications or CLOSE action | TradeManager.update_stop / close_trade | `trade_management.py:17-124` | Constitution V3 §Layer 4; Exit Definition V6 §Type A/C | **Y** |
| **26b** | **S4 Time Stop (W-10)** | S4 Woodies / woodies_system | management | Active S4 trade; bar_count vs entry_bar_count | bars_open = current_bar_count - entry_bar_count. If bars_open ≥ limit (default 18 bars ≈ 90 min) → close at last bar close. | Force close with exit_reason=TIME_STOP | TradeManager.close_trade | `woodies_system.py:560-623` | D-092 §W-10 time stop | **Y** |
| **27** | **Trade Close** | Services / trade_manager/manager | lifecycle | on_stop_hit or on_target_hit(T3) or close_trade(manual/time_stop/reversal) | Transitions → CLOSED. Sets exit_ts, exit_price, exit_reason. Calculates PnL: per-contract exits at respective target levels; $5/point MES; pnl_r = total_pnl / (3 × risk_per_contract). Sets outcome: WIN (pnl>0) / LOSS (pnl<0) / BE. Cleans up state machine. Emits close event. | V9Trade: exit_reason, pnl_usd, pnl_r, outcome | Gateway.on_trade_close (free slots, update stats) | `manager.py:317-584` | W11; W12 | **Y** |
| **28** | **Gateway: on_trade_close** | Gateway / trading_gateway | lifecycle | Trade close event with outcome | Frees DEMO/LIVE slot (demo_slot=None or live_slot=None). Updates daily stats (_daily_pnl, _daily_trades, _consecutive_losses). Records outcome for SSV recomputation. | Slot freed for next trade | Next setup can fill slot | `trading_gateway.py:161-189` | 3-Mode Spec V3 §8 | **Y** |
| **29** | **Persistence → DB** | DB / models/trades | persistence | V9Trade object | Table `v9_trades`: id, mode, firing_system, direction, state, entry_ts, entry_price, stop, t1, t2, t3, t1/t2/t3_hit_ts, exit_ts, exit_price, exit_reason, pnl_usd, pnl_r, outcome, cross_context (JSON), quality (JSON), sierra_bracket_id, is_synthetic, created_at, updated_at. Management log: V9TradeManagementLog (cascade delete). | Persisted trade row | API | `db/models/trades.py` | W11 schema | **Y** |
| **30** | **API → Frontend** | API / v9/trades + Frontend / TradesView | presentation | DB query | GET /api/v9/trades → enriched rows (pnl_mode, excursion, systems_agreement). GET /api/v9/trades/recent → 30 newest (TradeHistoryStrip, 30s poll). GET /api/v9/trades/active → contract-level detail (5s poll). Trade detail → insight panel from cross_context. | TradesTable + TradeDetailsModal | User (trader) | `api/v9/trades.py`; `frontend/v9/src/v9/components/trades/` | UI spec | **Y** |

---

## Operational Rules Verification

### Rule A: Position Sizing (Playbook → Auth Table → Actual Contracts)

| Layer | Source | Logic | Spec Reference | Code Location | Match? |
|-------|--------|-------|----------------|---------------|--------|
| S1 Playbook labels | state_machine.py:38-116 | DayType → sizing label: TN→AGGRESSIVE, TDD→AGGRESSIVE, NV→FULL, Norm→STANDARD, NeuE→HALF, NeuC→HALF, NT→MIN | Constitution V3 §Layer 2 | `state_machine.py:38-116` | **N** — [GAP-1] |
| S2 Auth Table V1 | auth_table_v1.py:33-104 | (pattern × day_type × tier) → 0-3 contracts. 70 cells. SKIP=always 0. Max=3. | S2_AUTH_TABLE_V1.md (LOCKED 2026-05-25) | `auth_table_v1.py:33-104` | **Y** — all 70 cells verified identical |
| S2 setup_emitter | setup_emitter.py:57-59 | get_quality_tier_v2(pattern, day_type, price) → calls Auth Table | D-091; D-095 path A | `setup_emitter.py:54-67` | **Y** |
| S3 sizing (own) | footprint_system.py:389-426 | strength ≥0.6 + aux≥3 → 3; ≥0.4+aux≥2 → 2; ≥0.3+aux≥1 → 2; else reject | D-089 (S3-specific) | `footprint_system.py:389-426` | **Y** — S3 has own sizing, not Auth Table |
| S4 sizing (own) | woodies_system.py:637-684 | tier-based (high/medium/low) + aux alignment (SWI/CZI/TCCI) + trend_ok → 3/2/reject | D-092 (S4-specific) | `woodies_system.py:637-684` | **Y** — S4 has own sizing, not Auth Table |

**[GAP-1] Detail:** The S1 Playbook labels (AGGRESSIVE/STANDARD/HALF/MIN) from Constitution V3 are **superseded** by the Auth Table V1 (LOCKED 2026-05-25). The Auth Table is the MASTER authority for S2 sizing. The playbook labels still exist in code but are NOT consumed by the setup_emitter — it uses Auth Table directly. The old labels remain in state_machine.py as dead mapping. **Severity: LOW** — the dead labels don't affect behavior since setup_emitter bypasses them. However, the playbook's `time_stop_min` values ARE consumed via `time_stop_mapper`. Recommend documenting that playbook sizing labels are superseded by Auth Table V1, and removing or marking them as deprecated to avoid confusion.

---

### Rule B: Who Fires — "Highest Profit Ratio" vs Code Reality

| Aspect | Spec Claim | Code Reality | Match? |
|--------|-----------|--------------|--------|
| STATUS_BOARD line 29 | "המערכת עם יחס הרווח הגבוה יורה" (the system with the highest profit ratio fires) | N/A — this is a stated intent, not found in any locked spec document | N/A |
| Constitution V3 | Not mentioned | N/A | N/A |
| 3-Mode Spec V3 §8 | "first-wins" slot fill | `if self.demo_slot is None:` (line 125) | **Y** — code matches this spec |
| Architecture docs (01_ARCHITECTURE.md) | "first-wins" (line 164: "Only one mode can fire a specific setup at a time (first-wins)") | Sequential slot check | **Y** |
| MASTER_DEV_SKILL.md | "DEMO/SIM: ONE slot first-wins, no caps, Sierra demo" | Sequential slot check | **Y** |
| Tests (test_trading_gateway.py) | `test_demo_first_wins` (line 124), `test_live_first_wins` (line 216) | Tests verify first-wins behavior | **Y** |
| MEMS26_REGISTRY.yaml line 422 | "3-Mode trading — SHADOW parallel, DEMO/LIVE first-wins" | Sequential slot check | **Y** |

**[GAP-3] CRITICAL Finding:** The phrase "המערכת עם יחס הרווח הגבוה יורה" (the system with the highest profit ratio fires) appears **only** in `STATUS_BOARD.md` line 29 as a desired behavior. It does **NOT appear in any locked spec document** (Constitution V3, 3-Mode Spec V3, D-091, D-092, D-093, Master Index V2, Architecture docs). Every locked specification and architecture document consistently describes "first-wins" slot fill. The code correctly implements "first-wins" as specified.

**The gap is between Michael's INTENT (profit-ratio selection between competing systems) and what was actually SPECIFIED and CODED (first-wins).** If profit-ratio selection is desired, it requires:
1. A new decision document (D-0xx) specifying the selection algorithm
2. A scoring function comparing setups from S2/S3/S4 by R:R or other metric
3. A queuing mechanism in the gateway (buffer competing setups for N seconds, then pick best)
4. Changes to gateway routing from sequential slot fill to ranked selection

**Current behavior in detail:**
- SHADOW: Every setup that passes risk gates is recorded (unlimited parallel)
- DEMO: First setup to reach `route_setup()` fills the single slot; subsequent setups are logged as "slot occupied" and discarded
- LIVE: Same as DEMO + strict risk checks
- No comparison, no ranking, no profit-ratio calculation occurs at any point

---

## Gap Summary: Code ↔ Spec Discrepancies

| ID | Severity | Step | Gap Description | Code | Spec | Impact |
|----|----------|------|-----------------|------|------|--------|
| **GAP-1** | LOW | 7b | S1 Playbook sizing labels (AGGRESSIVE/STANDARD/HALF/MIN) are dead code — superseded by Auth Table V1 but not removed or marked deprecated | `state_machine.py:38-116` | Constitution V3 vs S2_AUTH_TABLE_V1.md | No behavioral impact — setup_emitter uses Auth Table directly. Cosmetic/documentation issue. |
| **GAP-2** | LOW | 20 | News block (LIVE strict check #5) is commented-out placeholder | `risk_checks.py:70-73` | 3-Mode Spec V3 §5 mentions news gating | No impact in SHADOW. Will need implementation before LIVE if spec requires it. |
| **GAP-3** | **HIGH** | 21 | Routing is first-wins slot fill. No profit-ratio or confidence-based selection between competing system setups exists. The stated intent "highest profit ratio fires" is NOT in any locked spec. | `trading_gateway.py:123-141` | STATUS_BOARD desire vs all locked specs (first-wins) | **If Michael wants ranked selection, a new D-decision + implementation is required.** Code matches locked specs but may not match intent. |
| **GAP-4** | MEDIUM | 20 | `MAX_CONTRACTS = 2` constant in risk_checks.py is defined but **never enforced** in gateway routing. Auth Table allows up to 3 contracts. | `risk_checks.py:19` (unused) | 3-Mode Spec V3 (if max-2 was intended for LIVE) | Potential LIVE safety issue if max-contracts cap was intended. Verify with Michael. |
| **GAP-5** | LOW | 7 | ConfidenceThreshold class uses `__eq__` hack: `float(0.85)` that also equals `0.70` for test compatibility. Lock uses `>=` comparison so threshold is effectively 0.85. The `__eq__` hack only affects `==` checks in legacy tests. **Evidence:** `schemas.py:12-13` `def __eq__(self, other): return other in (0.85, 0.70)`; `state_machine.py:707` uses `self.confidence >= conf_threshold` (not `==`). | `schemas.py:6-13`; `state_machine.py:703-718` | Day Type spec §C1 | LOW risk: `__eq__` hack is cosmetic for test compat. Actual lock threshold = 0.85. |
| ~~GAP-6~~ | ~~INFO~~ | 11 | **RESOLVED (verified 2026-05-31).** "39 ZLR failures" was historically inflated. Actual: **34 ZLR tests across codebase, all passing** (`pytest -k zlr` → `34 passed, 0 failed`). Code fixed in `aafb699` + `acacf8b`. See `docs/reports/GAP6_ZLR_RECONCILE_2026-05-31.md`. | `tests/v9/systems/` (not just `woodies/`) | D-092 P-W3 | No longer a blocker. |
| **GAP-7** | INFO | 1c | DLL does NOT call ACSIL order functions yet (TODO at line 813-815 per D-093). Orders go through `trade_command.json` file-based command path. | `sc_study/MES_AI_DataExport.cpp:813-815` | D-093 §P5-1 | Expected — Pipeline 5 deliverable. No impact in SHADOW mode. |
| **GAP-8** | MEDIUM | — | Dual gateway implementations exist: Legacy (`backend/v9/gateway/`) is wired; New (`backend/v9/services/trading_gateway/`) is unwired with W14 RiskValidator. D-093.Q1 RE-LOCKED: MERGE approach (base=Legacy + extract RiskValidator from New). | Both paths | D-093 Q1 (RE-LOCKED 2026-05-31) | Dead code risk (New gateway). RiskValidator not yet integrated into Legacy. Reconciliation per P5-0 required. |
| ~~GAP-9~~ | ~~HIGH~~ | — | **FALSE POSITIVE — REMOVED (verified 2026-05-31).** `pre_fire_validator` IS called in S4: `decision_tree.py:361-373` runs `validate_fire(req)` in A7 stage. Fire only proceeds when `ready_to_route=True` (all A-stages pass). **Evidence:** `grep validate_fire woodies/decision_tree.py` → lines 361, 373; `woodies_system.py:417` gates on `dt_summary.get("ready_to_route")`. **Origin: SYSTEM_REVIEW 2026-05-29 blocker #3 — finding was stale; code fixed between 29-30/5. Do not re-open without fresh grep evidence.** | — | — | — |
| ~~GAP-10~~ | ~~HIGH~~ | — | **FALSE POSITIVE — REMOVED (verified 2026-05-31).** `woodies_chart_routes.py` already uses DST-aware `_chicago_to_utc` (America/New_York). **Evidence:** line 42: `# DST-aware conversion via bridge's _chicago_to_utc (America/New_York)`; line 44: `ts_unix = BaseV9Stream._chicago_to_utc(ts_unix)`. **Origin: SYSTEM_REVIEW 2026-05-29 blocker #5 — finding was stale; code fixed between 29-30/5. Do not re-open without fresh grep evidence.** | — | — | — |
| **GAP-11** | MEDIUM | — | S2 `current_day_type=None` on restart mid-session (SYSTEM_REVIEW). If backend restarts after IB lock, S2 loses day_type context → Auth Table defaults to "Neutral_Center". | `five_min_system.py:139-169` | D-091.Q1 hydration | May cause wrong sizing after mid-session restart. Needs hydration from DB. |

---

## Summary Statistics

| Metric | Count |
|--------|-------|
| Total steps in path | 30 |
| Steps verified Y (code matches spec) | 24 |
| Steps with gaps (N) | 3 (GAP-1, GAP-2, GAP-3) |
| Steps partially matching | 2 (GAP-4, GAP-5 attached to matching steps) |
| Additional system-level gaps (verified) | 3 (GAP-7, GAP-8, GAP-11) |
| False positives removed | 2 (GAP-9, GAP-10 — stale SYSTEM_REVIEW findings, fixed in code since 30/5) |
| Resolved | 1 (GAP-6 — 34 ZLR tests all pass, verified 31/5) |
| **Total live discrepancies** | **8** |
| Critical (HIGH) | **1** (GAP-3 — routing first-wins vs intent) |
| Medium | 3 (GAP-4, GAP-8, GAP-11) |
| Low / Info | 4 (GAP-1, GAP-2, GAP-5, GAP-7) |
| **Bug C** (bar_level_detector) | By-design in SHADOW (5-min resolution, stop-first priority); needs real fills for LIVE |

---

## Critical Findings (Requires Michael Decision)

### 1. Routing: First-Wins vs Profit-Ratio Selection (GAP-3)
**Status:** Code and ALL locked specs agree on first-wins. The "highest profit ratio" concept exists only as stated intent in STATUS_BOARD. If this is a desired behavior change, it needs a new D-decision specifying: selection metric (R:R? confidence? composite?), buffering window (how long to wait for competing setups?), and tie-breaking rules.

### 2. ~~S4 pre_fire_validator Bypass (GAP-9)~~ — FALSE POSITIVE
**Verified 2026-05-31:** `decision_tree.py:361-373` calls `validate_fire()` in A7. Fire only proceeds when `ready_to_route=True`. SYSTEM_REVIEW finding was stale.

### 3. ~~TZ Bomb in Woodies Chart Routes (GAP-10)~~ — FALSE POSITIVE
**Verified 2026-05-31:** `woodies_chart_routes.py:44` already calls `BaseV9Stream._chicago_to_utc()` (DST-aware, America/New_York). SYSTEM_REVIEW finding was stale.

### 4. Auth Table Sizing: Confirmed Match
**Status:** All 70 cells in `auth_table_v1.py` match `S2_AUTH_TABLE_V1.md` exactly. Import-time assertions verify: all 10 patterns present, all 7 day types covered, max=3, all SKIP cells = (0,0,0), all Nontrend = SKIP. **No sizing discrepancy.**

---

*Generated 2026-05-31 by Claude Code audit. READ-ONLY — zero code changes made.*
