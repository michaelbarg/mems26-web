# אינדקס 01.09 — כל מה שנגע בהתנהגות, ומה לבחון מחר

**נכתב 14:45 · 76 קומיטים · לקריאה קרה בתחילת הסשן של 02.09**
**בקשת-מייקל:** *"אינדקס של כל התיקונים שבוצעו היום ויבחן אותם, ומה עוד אפשר לתקן."*

**‏🔴 קרא ראשון:** ‏5 מ-13 השורות בטבלה הראשונה **מסומנות לבחינה מחדש** — לא
כי הן חשודות, אלא כי הן **טרם ראו יום-מסחר אחד.** אל תתייחס אליהן כאל מוכחות.

---

# 🅰 שינה התנהגות-מסחר — 6 שורות

| # | מה | מצב | אימות | לבחון מחר |
|---|---|---|---|---|
| 1 | **‏`RISK_BUDGET_SIZING_V1=1`** · `USD=225` · `MIN=3` | **חי** | ‏17+9 טסטים · **2 מוטציות נתפסו** (`floor→round` · `דחייה→clamp`) · דריל-סים | **‏🔴 טרם ראה יום-מסחר.** לספור כמה עסקאות נדחו על `n<3`, וכמה חוזים יצאו בפועל מול הצפי |
| 2 | **‏`ENTRY_LOCATION_QUALITY_V1=shadow`** | **חי (לוג)** | ‏CC חיווט לגייטוויי · 4 אנקורים | **הפעם הראשונה אי-פעם.** `grep 'ENTRY_LOCATION_QUALITY SHADOW-BLOCK'` → כמה נחסמו, ומה קרה להן ב-`t1_before_stop` |
| 3 | **‏T-178** שחרור-סלוט אחרי phantom | **חי** | 6 טסטים **+ מוטציה** · שני שורשים (השער **וגם** import שלא היה קיים) | לוודא שאין `slot occupied` בלוג-הערב |
| 4 | **‏T-177** ‏`flush` לפני `return` | **חי** | הוכח על #939 | — |
| 5 | **‏`OPENING_DRIVE_SKIP_V1`** ‏1 → **0** | **כבוי** | — | פסיקת *"לפני שתכבה קודם תתקן"*. **להדליק רק אם התיקונים ייכשלו במדידה** |
| 6 | **‏`OPENING_PATTERN_SKIP_V1`** | **כבוי, נבנה** | 12 טסטים | מנגנון ליום שנחליט. **אינו החלטה** |

---

# 🅱 מדידה וכלים — 7 שורות, אפס סיכון-מסחר

| מה | מה הוא נותן |
|---|---|
| **‏`guard_tests.sh`** | **‏110 שומרים** משני שורשי-הטסטים. **הגילוי: `fire_drill` לא מריץ `pytest` כלל** ⇒ הטסטים היו קיימים ומעולם לא רצו |
| **‏`sot_map_guard.py`** | נכשל כשקובץ שהמפה **מצטטת** השתנה אחריה. **מייעץ, לא חוסם** |
| **‏`opening_atr_audit.py`** | ‏13/13 סשנים · חציון **‎1.97×** — ‏ATR-פתיחה חצי מהסשן |
| **‏`shadow_slot_scan.py`** | ניפוח-הצל **‎2.42×** · הלוח **‎6.37×** |
| **‏`blocked_candidate_audit.py`** | שופט מה שהשערים **סירבו**, לפי `t1_before_stop` |
| **‏`va_sanity.py`** · `shadow_recorder.py` · מיגרציה 025 | ‏CC |
| **‏`task_log_guard`** | שער-כפילויות (אני) + `T-206` קריאת-✅ מעמודה (‏CC תיקן **אותי**) |

---

# 🅲 🔴 ארבע מסקנות שהתהפכו היום — **קרא לפני שתסמוך על משהו מאתמול**

### 1 · `T-203` אזור-הערך **אינו** חוסם — מדדתי מקור שגוי
```
tpo.json (ייצוא חי)  previous_session VA=49.5 / טווח 70.75 = 70%   ✅
v9_tpo_sessions      8-33% מהטווח                                   ❌
location_gate:1649   קורא מהייצוא, לא מהטבלה
```
**הטבלה שבורה; החיים קוראים מהייצוא.** ⇒ מחקר-דלתון **אינו** חסום. מחלקת 22.06.

### 2 · 🔴 המפרט שלי ל-ATR הצביע על האובייקט הלא-נכון
כתבתי לתקן את `five_min._current_atr_5m`. **הרזולבר לעולם אינו קורא אותו.**
הרצועה קוראת `_sr_atr` שהגייטוויי מחשב בעצמו (`:2570-2612`).
**מה ש-`eff80e1f` כן משנה:** ארבעה ספי-**זיהוי** (`:921 :1177 :1192 :2343`),
**כולם מרפים.** ⇒ **`eff80e1f` חייב להישאר כבוי. הוא אינו התיקון.**

### 3 · והתיקון כבר קיים — `FIX-8` / `EARLY_ATR_FLOOR_V1=1` (נפסק 10.07)
`LIMIT 12` ⇒ `len<14` **תמיד אמת** ⇒ רץ על **כל** עסקה, לא "early session".
**שבור ב"אתמול":**
```
08-31 Mon  אתמול=08-30 Sun |  84 ברים  ← 7 שעות globex. יום #875
09-01 Tue  אתמול=08-31 Mon | 276 ברים
```

### 4 · חמישה פריטים בסיכום-הבוקר היו מיושנים
`DBL_MAX` · `is_sim=1` · `T-171 ack פג` · `T-189` · `T-195` — **כולם נסתרו
ע"י המצב-החי.** ‏`sierra_state.json` נכתב **כל שנייה**: כל טענה על `is_sim`,
מרג'ין או פוזיציה נקראת **ברגע-האמירה**.

---

# 🅳 מה עוד אפשר לתקן — לפי החזר

| # | מה | למה | מי |
|---|---|---|---|
| **1** | **‏`FIX-8`** — "אתמול" ⇒ הסשן האחרון עם ‎≥14 ברי-RTH | **התיקון היחיד שנוגע בסטופ.** SQL מוכן בערוץ 14:32 | cc |
| **2** | **‏`guard_tests.sh` → `fire_drill`** | ‏110 שומרים שאיש אינו מריץ אוטומטית | cc |
| **3** | **‏`flag_guard`: `BUDGET×MIN ≤ CAP`** | תקרה חיה **800**, לא 450 | cc |
| **4** | **‏`T-103`** הכרעה→טבלה → `mechanism_verdict` → `DAILY_VERDICT` | **בלעדיו אין לולאת-למידה** | cc |
| **5** | **‏`SYSTEM_MANIFEST.md`** — 51 יום, מצטט DLL שזז 36 יום אחריו | ‏`sot_map_guard` תופס | cowork |
| **6** | **‏`T-199`** 5 פריטי-DLL — **אפס בוצעו** · `T-193` · `T-190` | לילה, סים בלבד | cc |
| **7** | **רפליי-מצפן** על 10 עסקאות-הפתיחה | ‏`DIRECTION_COMPASS_V1=1` דלוק ומחובר לגייטוויי בלבד; `grep compass five_min` = **אפס** | cc |
| **8** | **‏T-195** ‏`FAMIR`/`VEGAS` — בוטלו על ראיה | **סותר את "מתקנים ולא מבטלים"** ⇒ פסיקת-מייקל | מייקל |

---

# 🅴 שני כללים שנקנו היום — לאכוף עליי

**‏1 · מפרט שנוקב במשתנה חייב להדביק `קובץ:שורה` שבו המנגנון-היעד *קורא* אותו.**
אין שורה — **אין מפרט, ו-CC רשאי לסרב.** זה בדיוק מה שהיה עוצר את מסקנה 2 למעלה
במשפט הראשון.

**‏2 · שני שורשי-טסטים בריפו** (`tests/` · `backend/v9/tests/`). חיפוש באחד
בלבד הוליד "אפס טסטים" על ארבעה פריטים של CC. **‏`guard_tests.sh` מכסה את שניהם.**

---

## מצב ב-14:45
```
health 200 (1.8ms) · is_sim=0 · qty=0 · avail=$2,716
flag_guard 228 PASS · guard_tests 110 ירוקים
פתוחות: 132 (54 🔴) — cc 45 · cowork 26 · מייקל 8
```
**‏🔴 והריסטארט ב-16:00 הוא מה שמפעיל את הכול.** הבקאנד רץ מ-11:22; כל שינוי
ב-`.env` מהיום **אינו חי עד אז.**
