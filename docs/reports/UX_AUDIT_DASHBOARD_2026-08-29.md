# ביקורת-UX / פרונטאנד — דשבורד MEMS26

**תאריך:** 2026-08-29 (שבת, השוק סגור) · **מבקר:** cowork-dev
**קומיט-התיקונים:** `da0bcd09` · **דוח זה מתעד ממצא → ראיה → תיקון.**

---

## שיטה, ומה **לא** ניתן היה לבדוק

| | |
|---|---|
| **דפדפן חי** | `http://localhost:3000` (dev, hot-reload). מדידות-DOM אמיתיות, לא הערכות. |
| **אמת-ה-API** | נמשכה ב-`curl` מה-Mac ישירות מ-`localhost:8000`. |
| **מגבלה 1 — השוק סגור** | הפיד קפוא (`age_s≈39,832` = 11.1 שע'). **לא** נבדקו: ריצודי-רינדור בזמן-אמת, התנהגות ה-WS תחת עומס, מרוץ-פולינג ב-16:30. צריך בדיקה חוזרת ביום-מסחר. |
| **מגבלה 2 — הסנדבוקס חסום ל-8000** | ה-pane מגיע ל-:3000 אך **חסום** מול :8000 (`ERR_BLOCKED_BY_CLIENT` + כשלי-WS). |
| **איך הפכתי את מגבלה 2 ליתרון** | זה בדיוק מצב **"ה-backend לא זמין"** — המצב שהכי מייצר שקר-תצוגה. ואז הזרקתי `fetch`-shim עם **המטענים האמיתיים** שמשכתי ב-curl (+ פוזיציה פתוחה) כדי לראות גם את המסך המאוכלס. שני המצבים נבדקו. |

**אמת-הבסיס בזמן הביקורת** (`/api/v9/account/state`, `/api/v9/cockpit/heartbeat`):
`mode="live"` · `MEMS26_MODE=live` · `is_sim=0` · חשבון `37138283` · `armed=1` ·
`contracts_allowed=6` · `stale=true` · `age_s=39832` · `bar_integrity="no_data"`.

---

## 🔴 מטעה-בזמן-מסחר

### 🔴 1 — תג-המצב הכריז **SHADOW** בזמן מסחר-אמת ✅ תוקן
`layout/TopBar.tsx` — לשעבר שורות 31 · 44 · 118

```js
const [mode, setMode] = useState('SHADOW');          // ערך-פתיחה
setMode(MK[(d.mode||'shadow').toLowerCase()] || 'SHADOW');  // ערך לא-מוכר => SHADOW
}).catch(() => {})                                    // כשל-רשת נבלע בשקט
const ms = MODE_STYLES[mode] || MODE_STYLES.SHADOW;   // fallback שלישי => SHADOW
```

**ראיה:** ה-backend מחזיר `{"mode":"live"}` ו-`is_sim=0` (כסף אמיתי, חשבון 37138283).
בדפדפן שבו ה-heartbeat לא נגיש — המסך הציג תג **צהוב "SHADOW"**. שלוש נפילות
נפרדות ל-SHADOW, ו-`.catch` **ריק** — אין שום סימן שמשהו נכשל.

**למה זה החמור מכולם:** זו העובדה הבטיחותית היחידה שכל שיקול-דעת נשען עליה.
"צל" אומר *מותר לי לטעות*. תקלת-רשת של 15 שניות הופכת מסך-לייב למסך-צל.

**תוקן:** ברירת-מחדל `UNKNOWN` → תג אפור **"מצב ?"**; ערך-לא-מוכר ⇒ UNKNOWN;
`.catch` מחזיר UNKNOWN. **לעולם לא SHADOW מתוך חוסר-ידיעה.**

---

### 🔴 2 — הרדאר טען טענות-אמת מתוך חוסר-נתונים ✅ תוקן
`layout/ContextRadar.tsx` — לשעבר שורות 124 · 130-132 · 145-146

```js
{rg?.state==='holding' ? ... : rg?.state==='released' ? 'שוחרר' : 'פנוי'}   // null => "פנוי"
{gl?.passed ?? 0} עברו / {gl?.blocked ?? 0} נחסמו                            // null => "0 / 0"
{tr?.stale ? 'נתונים לא טריים' : tr?.armed !== 1 ? 'לא חמוש' : ...}          // undefined => "לא חמוש"
```

**ראיה:** ה-API מחזיר `trading.armed = 1`. המסך ללא-API הציג **"מסחר: לא חמוש"** באדום.
זו בדיוק המחלקה שמייקל נכווה ממנה — פאנל שהציג "לא-חמושה" בזמן שהמנוע עבד.
במקביל: **"שער שחרור: פנוי"** (= שום דבר לא מחזיק ירי) ו-**"0 עברו / 0 נחסמו"** —
שלוש טענות-אמת חיוביות שנולדו מ-`null`. מחוון-השגיאה היה טקסט **8px** בקצה
שורה עם `flexWrap`, כלומר נודד לשורה אחרת ואובד.

**תוקן:** `noData` מפורש. אין-מטען ⇒ `—` / **"לא ידוע"** (צהוב), ותיבת-שגיאה
אדומה **"🔴 לא זמין"** בראש השורה. `idle` האמיתי נשאר "פנוי" — הבחנה בין
*אין-נתונים* ל-*אין-חסימה* נשמרת.

---

### 🔴 3 — 5 מ-9 תיבות-הרדאר מחוץ-למסך, כולל "האם אפשר לסחור" ✅ תוקן חלקית
`layout/V9Dashboard.tsx:115` (מיכל-הגלילה) + `layout/ContextRadar.tsx` (הסדר)

**מדידה — 1440px, פוזיציה פתוחה של 4 חוזים:**

| רוחב-חלון | פיקסלים חתוכים |
|---|---|
| 1280 | **668px** |
| **1440** (MacBook) | **508px** |
| 1728 | 220px |
| 1920 | 28px |
| 2560 | 0 |

השורה דורשת **1948px** כדי להיכנס. ב-1440px נמדדו **מחוץ-למסך לגמרי**:
`איזון/קבלה` · `שער שחרור` · `שערים/שעה` · **`מסחר`** · **`שלמות ברים`**
(ו-`פתיחה` חתוכה חלקית). רק **17.9%** מהרדאר גלוי.

**החומרה:** שתי התיבות שנחתכות **אחרונות בקוד ולכן ראשונות להיעלם** הן
"האם המערכת יכולה לסחור" ו"האם זרם-הברים אמין" — בדיוק שתי שאלות-ההכרעה.
הגלילה-האופקית קיימת (החלטת-מייקל 05.08, לא לשנות) אך פס-הגלילה **6px**
מתחת לשורה של 64px — אין שום רמז שמעבר לקצה מסתתר "מסחר".

**תוקן (חלקית):** `מסחר` ו`שלמות ברים` הועברו **לראש השורה**. **נותר ל-cc:**
סימן-גלילה ויזואלי (הצללה/חץ "◀ עוד") — ראה טבלת-הנותרים.

---

### 🔴 4 — פאנל מערכת-6 מציג "אין נתוני פיקוח" בזמן שה-endpoint חי ❌ נותר ל-cc
`sidepanel/ActiveTradeCard.tsx:390` · `:407` · `:452`

```js
// :390  הערה מיושנת: "(currently 404)"
// :407  const list = Array.isArray(d) ? d : (d.checks ?? d.invariants ?? d.diagnostics ?? []);
```

**ראיה — אי-התאמת-חוזה מוכחת:**
```
$ curl -s localhost:8000/api/v9/s6/diagnose/851
{"trade_id":851,"state":"CLOSED","outcome":"WIN","healthy":true,"issues":[]}
    -> HTTP 200 (לא 404)
    -> מפתחות: trade_id, state, outcome, healthy, issues
    -> הפרונט מחפש: checks | invariants | diagnostics  =>  אף אחד  =>  list = []
```
`status='ok'` אך `checks.length===0` ⇒ נופל ל-`:452` ומציג **"אין נתוני פיקוח"**.

**המשמעות:** מייקל ביקש במפורש (07-10) "שמערכת 6 תהיה שם ורואים מה היא חושבת".
המפקח **רץ ומדווח `healthy:true`** — והכרטיס אומר שאין נתונים. פאנל מת בשקט.

**לא תיקנתי בכוונה:** יש לי רק דגימת-`healthy`. מיפוי `issues` למצב-כשל בלי
לראות מטען לא-תקין הוא בדיוק סינתזה-מנוחשת (כלל-1: כישלון-כן > ערך-מסונתז).
צריך דגימה אחת של עסקה לא-בריאה. **מאמץ ל-cc: 20 דק'.**

---

## 🟡 חיכוך

### 🟡 5 — גיל-הנתונים הוסתר בדיוק כשהוא קריטי ✅ תוקן
`layout/SierraTruthStrip.tsx` — לשעבר שורה 181: `{s?.age_s != null && !stale && (...)}`

הגיל הוצג **רק כשהנתונים טריים**. כשהם לא-טריים — רק "נתוני-סיירה לא טריים",
בלי מספר. **נמדד `age_s=39,832` = 11.1 שעות** והמסך לא רמז על כך.
"לא טרי" ב-40 שניות (גמגום-פיד, המספרים בסדר) ו"לא טרי" ב-11 שעות (נתוני-אתמול)
הם שני עולמות. **תוקן:** הגיל מוצג תמיד, בתג מודגש: `נתונים לא טריים · לפני 11.1 שע׳`.

### 🟡 6 — מחרוזות-קוד גולמיות בממשק עברי
| מקום | מוצג | מקור |
|---|---|---|
| `ContextRadar` שלמות-ברים | `no_data` | ✅ תוקן ⇒ "אין ברים" |
| `SierraTruthStrip:164` בעלות | `unknown` | ❌ נותר — `verdict` מרונדר גולמי |
| `systems/KeyLevelsCard.tsx:121` + `strips/KeyLevelsStrip.tsx:245` | `dll_missing` | ❌ נותר — מחרוזת קשיחה בקוד, לא מה-API |

### 🟡 7 — RTL: מספר-פותח נזרק לקצה הלא-נכון ✅ תוקן
`systems/DayTypeLensContent.tsx:120` — הכותרת `7 סוגי-יום — משמעות למסחר` בלי `dir="rtl"`.

**מדידה:** ב-`ltr` — `x(7)=0` מול `x(ס)=184` ⇒ ה-7 **משמאל** לעברית = בסוף
המשפט לקורא עברית. ב-`rtl` — `x(7)=591` מול `x(ס)=576` ⇒ נכון. **תוקן.**

> **תיקנתי גם באג שאני עצמי יצרתי:** תג-הגיל שכתבתי הציג `11.1h`, ומנוע ה-bidi
> **הפריד את ה-`h` מהמספר** והעיף אותו לקצה הנגדי (`11.1 … h`, נמדד). היחידות
> הוחלפו לעברית (`שע׳`) + `dir="rtl"`. לקח כללי: **סיומת לטינית על מספר בתוך
> משפט עברי נשברת** — להשתמש ביחידות עבריות.

### 🟡 8 — ניגודיות התוויות שמזהות את המספרים ✅ תוקן חלקית
תוויות 7px היו `textDim #404040` על רקע כמעט-שחור:

| צבע | יחס | תקן AA |
|---|---|---|
| `textDim #404040` | **1.91:1** | ✗ (דרוש 4.5) |
| `textDisabled #525252` | 2.53:1 | ✗ |
| `textTertiary #737373` | 4.10:1 | ✗ (גבולי) |
| `bear #dc2626` | 3.81:1 | ✗ — **מספרי-P&L שליליים** |
| `textSecondary #a3a3a3` | 7.70:1 | ✓ |

חמישה תאי-כסף **צמודים** (`P&L פתוח` / `P&L יומי` / `שווי חשבון` / `מזומן` /
`פנוי למסחר`) — המספרים בהירים, התוויות כמעט בלתי-נראות ⇒ סיכון לקרוא את
המספר הלא-נכון. **תוקן** ב-`SierraTruthStrip` + `ContextRadar` (⇒ 7.70:1).
**נותר:** `bear` 3.81:1 גלובלי, ו-`textDim` בשאר הרכיבים.

### 🟡 9 — "הכי קרוב לירי" נעלם בשקט בכשל-רשת ✅ תוקן
`sidepanel/NearestFireStrip.tsx:52` — היה `if (err || !near) return null;`

כשל-רשת ו"אין תבנית קרובה" נראו **זהים**: השורה נעלמת. מייקל ביקש אותה
במפורש (27.08) *כדי לדעת* — היעלמות שקטה = "שום דבר לא קרוב לירי".
**תוקן:** כשל ⇒ שורה צהובה `⚠ הכי-קרוב-לירי: אין נתונים (שגיאת-קריאה)`.

### 🟡 10 — כפתור "Move Stop" מת על כרטיס-העסקה ❌ נותר ל-cc
`sidepanel/ActiveTradeCard.tsx:358` — `alert('Move Stop is not wired to the API yet')`

הכפתור נראה פעיל, מבקש קלט (`prompt`), ורק **אחרי** שמייקל הקליד מספר מודיע
שהוא לא מחובר. בלחץ-עסקה זה שניות מבוזבזות על פעולה שלא תקרה. **הצעה:**
`disabled` + `title` מסביר, עד שהחיווט קיים. **מאמץ: 5 דק'.**

### 🟡 11 — שתי מוסכמות שונות למספר שלילי על אותו מסך ❌ נותר
`ActiveTradeCard:259,321` מציג `$-13` · `SierraTruthStrip` מציג `-$137.50`.
לאחד את שתיהן ל-`money()` הקיים. **מאמץ: 10 דק'.**

---

## 🟢 שיפור / היגיינה

- **🟢 12 — אפס מצבי-פוקוס בכל הפרונטאנד.** `grep ':focus|focus-visible' = 0 תוצאות`.
  כולל כפתור **Exit** ההרסני. ניווט-מקלדת עיוור לחלוטין. **מאמץ: 15 דק'** (כלל CSS גלובלי).
- **🟢 13 — `useState` בתפקיד `useEffect`** — `layout/SidePanel.tsx:91`: `useState(() => {... setOpen(true) ...})`
  קורא ל-`setState` בתוך אתחול-עצל = עדכון-בזמן-רינדור. עובד היום, שביר. ⇒ `useEffect`. **5 דק'.**
- **🟢 14 — `err` לא נדלק על 200-ריק.** `ContextRadar` מדליק `err` רק על `throw`/`!res.ok`.
  מטען חלקי ⇒ `err=false`. `noData` מכסה חלק; ולידציה של שדות-מפתח תסגור לגמרי.
- **🟢 15 — הערות מיושנות בקוד** — `ActiveTradeCard:390` טוענת ש-s6 מחזיר 404; הוא מחזיר **200**.

### מה נבדק ונמצא **תקין** (אנטי-רגרסיה)
- ✅ **גלילת פאנל-הצד** (תיקון 27.08) — `overflowY:auto` קיים ופועל; אפס חיתוך.
- ✅ **אפס גלישה אופקית** בפאנל-הצד ברוחב 248px גם עם עסקה מלאה (C1/C2/C3 + חיזוק + מערכת-6).
- ✅ **הבאנר "פוזיציה ללא הגנה"** גלוי ב-1440px עם פוזיציה עירומה.
- ✅ **סימני-מינוס בכסף תקינים** — `-$137.50` ו-`-4` נמדדו ב-`dir:ltr`, סדר ויזואלי = סדר-מקור.
- ✅ **"אין מחיר חי"** במקום מרחק-מומצא כשה-WS מת — כלל-1 מיושם נכון.
- ✅ **אפס שגיאות-קונסול** מהאפליקציה (כל השגיאות = חסימת-הסנדבוקס מול :8000).
- ✅ **המסלול-התקין לא נשבר** אחרי התיקונים: `✓ מוכן · עד 6 חוזים` · `✓ נקי` · `מחזיק 3 דק'` · `5 עברו / 2 נחסמו`.

---

## מה תיקנתי בעצמי (קומיט `da0bcd09`)

אפס נגיעה ב-`backend/` · `sc_study/` · `config/` · דגלים · **אינטרוולי-פולינג**.

| # | קובץ | תיקון |
|---|---|---|
| 1 | `layout/TopBar.tsx` | מצב-לא-ידוע ⇒ `UNKNOWN` ("מצב ?"), **לעולם לא SHADOW** |
| 2 | `layout/ContextRadar.tsx` | `noData` — `—`/"לא ידוע" במקום "פנוי"/"0/0"/"לא חמוש"; תיבת-שגיאה אדומה; `no_data`⇒"אין ברים" |
| 3 | `layout/ContextRadar.tsx` | `מסחר`+`שלמות ברים` הועברו לראש-השורה (הצלת-חיתוך) |
| 4 | `layout/SierraTruthStrip.tsx` | גיל-הנתונים מוצג **תמיד** + יחידות עבריות (תיקון-bidi) |
| 5 | `layout/SierraTruthStrip.tsx` + `ContextRadar.tsx` | ניגודיות-תוויות 1.91:1 ⇒ 7.70:1 |
| 6 | `sidepanel/NearestFireStrip.tsx` | כשל-קריאה ⇒ שורה צהובה במקום היעלמות |
| 7 | `systems/DayTypeLensContent.tsx` | `dir="rtl"` לכותרת — ה-7 חוזר למקומו |

**אימות:** כל תיקון נצפה בדפדפן חי בשני המצבים (backend-חסום ו-backend-מאוכלס
דרך shim עם המטענים האמיתיים). אפס שגיאות-קונסול חדשות.

## מה נשאר ל-cc

| # | פריט | חומרה | מאמץ | הערה |
|---|---|---|---|---|
| 4 | מיפוי `s6/diagnose` ⇒ `{healthy, issues, outcome}` | 🔴 | 20 דק' | דורש דגימת עסקה **לא**-בריאה לפני מיפוי |
| 3 | סימן-גלילה לשורת אמת-סיירה+רדאר | 🔴 | 30 דק' | הצללה/חץ; **לא** לבטל את הגלילה (פסיקת-מייקל 05.08) |
| 10 | `Move Stop` ⇒ `disabled`+`title` | 🟡 | 5 דק' | |
| 6 | `verdict`/`dll_missing` ⇒ עברית | 🟡 | 10 דק' | |
| 11 | איחוד פורמט-כסף שלילי | 🟡 | 10 דק' | |
| 8 | `bear` 3.81:1 + `textDim` גלובלי | 🟡 | 20 דק' | נוגע בטוקנים — לאמת בכל המסכים |
| 12 | כלל `:focus-visible` גלובלי | 🟢 | 15 דק' | |
| 13 | `useState`⇒`useEffect` ב-`SidePanel:91` | 🟢 | 5 דק' | |
| 14 | ולידציית-שדות ל-`err` ברדאר | 🟢 | 15 דק' | |

## בדיקה חוזרת נדרשת ביום-מסחר
הנקודות שהשוק-הסגור חסם: ריצודי-רינדור ב-16:30 · התנהגות WS תחת עומס ·
האם 508px החתוכים מורגשים בפועל · האם תג-הגיל החדש קופץ בין ערכים.
