# MEMS26 · טיוטות-עיצוב — 2026-07-15

מקור: `PATTERN_EOD_2026-07-15.md` (EOD, יום-ה-LIVE-הראשון). **קריאה-בלבד — לא-מומש.** כל design מוכן-להפיכה-לפרומפט.
מקרא: **[TRADING-LOGIC]** = משנה risk/fire/exit-surface → **דורש אישור-Michael + סטופ-אסטרטגי** · **[TRADING-SAFETY]** = הגנתי (halt/reject) אך נוגע-בנתיב-הירי → סקירה לפני-סמיכה-מלאה · **[DISPLAY/SAFE]**/**[TOOLING]** = ללא-שינוי-מסחר · **[SoT→CC]** = דורש הצלבת Sierra v9_export/DB/לוג ע"י CC.

**הבדל מ-07-13:** ביום-הקודם הבעיה הייתה R-חשבונאות/תבנית-לא-מתועדת בתוך מערכת-שפעלה. **היום — יום-ה-LIVE-הראשון — שלוש בעיות-תשתית עצרו סיגנלים-שכבר-עברו-את-כל-שערי-הדוקטרינה מלהגיע-לברוקר**: באג-סכמת-validation (S-10), קיפאון-פיד-בתוכן (מ-mtime מטעה), ושלושה שערי-כיול-גנריים לא-מכוילים-להקשר. רוב-התיקונים-כבר-נדחפו-חי היום ע"י cc-imac; העיצובים למטה הם בעיקר **הקשחה-רטרוספקטיבית** (regression + observability) כדי שהמחלקה-הזו-של-כשל לא-תחזור בשקט, ולא הצעות-net-new.

---

## §1 · S-10 — `sizing_contracts le=3` בלע כל ירי-S2-מלא (4c) כל-היום עד 20:47 · **[TRADING-LOGIC — כבר-בפרודקשן]**

**שורש (מתועד):** `backend/v9/.../output_schema.py:27/48` הגדיר `sizing_contracts: Field(ge=0, le=3)` מלפני חבילת-4-החוזים (`FIXED_CONTRACTS_4`). כל ירי-S2 בגודל-מלא (4) → Pydantic `ValidationError` בתוך `emit_t1_setup` → נתפס ב-`except` **שקט** → `route_setup` לא-נקרא → 0 עסקאות. S4 לא-הושפע (סכמה-נפרדת). אכל ≥4 סיגנלים מתועדים (18:25 + 3× `DOUBLE_BOTTOM conf=1.00` ב-19:40/19:45/19:50).

**כבר-בוצע (cc-imac, 20:47, `851f4a6`):** `le=3→le=4` + טסט יחידה (4-עובר/5-נדחה) + סריקת-`caps=3`-נוספים (0 נמצאו ב-`manager.py:486`, כבר 4c-aware).

**הפער שנשאר (זה מה שדורש-עיצוב, לא-רק-אישור):**
1. **בליעה-שקטה של ValidationError.** ה-`except` הגורף ב-`emit_t1_setup` הפך שגיאת-סכמה ל"אין-ירי" בלי-CRITICAL-log ובלי-חשיפה ב-`/api/v9/gateway/decisions`. כל באג-סכמה-דומה-עתידי (למשל אם `FIXED_CONTRACTS` יעלה שוב) יחזור-על-אותו-דפוס בלי-שיזוהה תוך-יום.
2. **אין בדיקת-CI שמקשרת `FIXED_CONTRACTS_*` לגבולות-הסכמה.** השינוי-הבא-ברוחב-חוזים יכול לפגוע-שוב באותה נקודה.

**שינוי מוצע:**
1. **הסרת ה-`except` השקט:** כל `ValidationError` ב-`emit_t1_setup`/`route_setup` נכתב כ-`logger.critical` (לא `debug`/`warning`) + נרשם לרינג-ה-decisions הקיים (`GET /gateway/decisions`) כ-`blocked_by="schema_validation_error"` — כדי שהוא-יופיע בפאנל "למה-לא-ירה" שנבנה היום (18:50) ולא-יתחפש ל"0-מועמדים".
2. **בדיקת-invariant בין `FIXED_CONTRACTS_*` לסכמות:** טסט (או check ב-`flag_guard`) שסורק את-כל ה-`Field(le=N)` הרלוונטיים ומוודא `N >= max(FIXED_CONTRACTS_*)`.

**Regression שיוכיח:** `test_sizing_contracts_matches_fixed_contracts` — פרמטרי על `FIXED_CONTRACTS_2/3/4`, מוודא ש-`emit_t1_setup` **לא-זורק** ValidationError בגודל-המלא (anti-regression מדויק מול S-10). `test_schema_validation_error_is_critical_and_visible` — מזריק ValidationError מלאכותי → מוודא `logger.critical` + רשומה ב-decisions-ring (לא-רק-שקט).

**עדיפות:** 🔴🔴 — כבר-חי, אבל הבליעה-השקטה היא-עצמה-הבעיה: היא-שמנעה-גילוי תוך-שעות ולא-תוך-שבוע. **החמור-ביותר-היום.**

---

## §2 · קיפאון-פיד-Woodies → 0-זיהוי 21:10→23:04 · **[TRADING-SAFETY]**

**שורש (מתועד):** שלושת קובצי-ה-5min-export (Sierra DLL) קפאו **בתוכן** (`history[-1].ts`) מ-≈16:20, בעוד ה-mtime המשיך-להתעדכן כל-שנייה (ראיה מטעה — "הקובץ נכתב" ≠ "הקובץ מכיל בר-חדש"). `_route_bar` דחה נכון על-בסיס-ts (stale-guard עבד), אך אין-אף-מנגנון שהופך "נדחה-שוב-ושוב" ל-HALT-גלוי — כך ש-`v9_day_type_state` פשוט **קפא** 21:10:39 ו-0 שורות-זיהוי נכתבו 2 שעות, בלי-אזעקה. שורש-ה-DLL עצמו (למה ה-history הפסיק-להתגלגל) חשוד ב-build-של-היום — טרם-אושר-קודי.

**כבר-בוצע (cc-imac, 23:14, `85cb05a`):** `feed_watchdog.py`/`frozen_tail_watchdog.py` — HALT על-בסיס DB-canonical-ts (TZ-safe), לא-על-mtime.

**הפער שנשאר:**
1. **טרם-מאומת-חי.** נדחף דקה-לפני שער-הריצה-הזו; אין-עדיין ריצת-RTH שעברה-דרכו.
2. **שורש-ה-DLL (למה ה-history הפסיק-להתגלגל אחרי-reload) לא-אובחן קודית** — רק חשד. אם-זו-קריסה-פר-אירוע ב-DLL, ה-HALT-החדש מונע-נזק אבל **לא-פותר** — תקרית-דומה תישנה כל-פעם שה-DLL קורס.
3. **HALT הוא by-definition שינוי-בנתיב-הירי** (מפסיק-מסחר במצב-מסוים) — גם-אם-הגנתי, נופל תחת "trading-risk-surface change" לפי-פרוטוקול-הפרה-LIVE ודורש-בדיקת-false-positive (לא-להיתקע-דלוק כמו NAKED_STOP-לוק ב-07-10).

**שינוי מוצע (ל-CC, לא-מומש כאן):**
1. **פקודת-פתיחה מחר (כבר-כתובה ב-AGENT_SYNC 22:30) לפני 16:30 CT, שתי-המכונות:** לוודא שלושת-ה-exports `<7 min` age-בתוכן + `_route_bar BLOCKED stale` לא-גדל תוך-דקה + `/gateway/decisions` עונה. **תנאי-סף מפורש: בלי-ירוק-אין-מסחר.**
2. **בדיקת false-positive ל-HALT-החדש:** לוודא שה-HALT **לא** נתקע-דלוק אחרי-שהפיד-מתאושש (בניגוד ל-NAKED_STOP-לוק ב-07-10/07-13 — reconciler שם לא-איפס-אמונה-ישנה). auto-clear כש-ts חדש מגיע.
3. **אבחון-DLL נפרד (P-ID עצמאי):** האם ה-history-freeze קורה שוב אחרי-ה-reload-הבא; אם-כן — regression-DLL, לא-רק-watchdog.

**Regression שיוכיח:** `test_feed_watchdog_content_freeze` (כבר-קיים לפי `git status` — לאשר-שהוא-ירוק ומכסה בדיוק את-התרחיש: mtime-מתעדכן + content-ts-קפוא). להוסיף `test_halt_auto_clears_on_fresh_bar` (anti-stuck-alarm, לקח-07-10).

**עדיפות:** 🔴 — הגנתי-וחיוני, אך "טרם-מאומת-חי" + "שורש-DLL-לא-נפתר" אומרים שזה **לא-סגור עד-מחר-בבוקר**. **[SoT→CC: אימות-RTH-הבא.]**

---

## §3 · שלושה שערי-כיול-גנריים (LSMA/RR/PATTERN_LOSS) — דפוס-אחד: סף-אחיד מול הקשר-משתנה · **[TRADING-LOGIC — כבר-בפרודקשן]**

**שורש (מתועד, שלוש-מופעים של-אותה-מחלה):**
- **(א) `cont_trend_filter`:** `LSMA_SUSTAIN_BARS=3` = פיגור-3-ברים אחרי-היפוך-מגמה → חסם שורט-מיצוי-מלא 18:35 (7604→7580 מדווח, "4/4 יעדים"). תוקן ל-2 (פסיקת-מייקל).
- **(ב) `RR_MIN_ROTATION`:** סף-R:R **אחיד** לא-הבחין בין-יום-רוטציה (רצפת-סטופ מנופחת מציבה R:R<1 כמעט-תמיד) לבין-יום-מגמה → כמעט-חסם מנצח 18:15 (R:R=0.65). ערך RR_MIN_ROTATION=0.65 ruled, מדורג לפי-סוג-יום.
- **(ג) `pattern_loss_breaker`:** ספר הפסדי-**shadow** (mode=shadow) כאילו-הפסדי-אמת → חסם ZLR 20:17 (`pattern_loss_breaker:2>=2`, מהם shadow). תוקן `mode!='shadow'`.

**המשותף:** בכל-שלושת-המקרים השער **לגיטימי-בעיקרון** (מגן-מפני-ריצה-בשוק-שהתהפך / R:R-גרוע / רצף-הפסדים) אבל **הקבוע/הספירה שלו לא-מכויל להקשר** (מהירות-מגמה, סוג-יום, mode-אמת-מול-סימולציה). זו-הפעם-השלישית-החודש שדפוס-כזה נתפס-רק-בדיעבד אחרי-שחסם-מנצח בפועל.

**שינוי מוצע (מעבר-לתיקוני-הנקודה שכבר-בוצעו):**
1. **סקירה-שיטתית של כל שער-דומה** (סף-קבוע/ספירה-לא-מסוננת) — רשימה ב-`FLAG_INDEX.md`/`FLAG_REGISTRY.yaml`: לכל-שער, לשאול "האם הפרמטר-שלו תלוי-הקשר (סוג-יום/מהירות-מגמה/mode) בעוד שהוא-מיושם-כקבוע-אחיד?"
2. **טלמטריה משותפת:** לכל-שער-מה-משפחה — מונה `blocked_would_have_won` (כבר-קיים חלקית ב-CF/missed-trades) שמצטבר-שבועית, כדי-שדפוס-כזה יתפס **לפני** שהוא-חוסם-מנצח-חי, לא-אחרי.

**Regression שיוכיח (לכל-אחד מ-3, אם-לא-קיים-כבר):** `test_lsma_sustain_bars_2_catches_18_35_short` (anti-regression מדויק) · `test_rr_min_rotation_graded_by_daytype` (Trend=1.0, Variation=0.65, לא-חוזר-ל-0.28-הישן) · `test_pattern_loss_breaker_excludes_shadow` (mode=shadow לא-נספר). כל-השלושה כבר-מוזכרים כ"5 טסטים משחזרים 18:15" ב-STATUS_BOARD 19:15 — **לאמת-שהם-אכן-קיימים-וירוקים**, לא-להניח.

**עדיפות:** 🟠 — כל-שלושת-התיקונים-כבר-חיים; העיצוב כאן הוא-מניעתי (לתפוס-את-הרביעי לפני-שהוא-קורה).

---

## §4 · S-9 — יתום-הפוך (Sierra מחזיק/TM ריק), reconciler מזהה-ולא-מרפא · **[TRADING-LOGIC] · [SoT→CC]**

**שורש (מתועד, נמשך ממשפחת-07-10/#350 + 07-14):** שורט-2 יתום @7576.5 (19:37+), `TM=0 / Sierra=−2` (src=state), `SYS-3 DIVERGENCE` כל-30ש' רצוף, **`phantom-heal streak 0/3`** — לא-מתקדם. זו **הכיוון-ההפוך** מ-#350-המקורי (שם TM-החזיק-פאנטום מול Sierra-flat); כאן Sierra-מחזיק-אמת מול TM-ריק — ולכן ה-phantom-heal (שנבנה-למקרה-ההפוך) לא-רלוונטי/לא-מטפל. מייקל פסק **"תן-לסטופ-לנהל"** (לא-op=EXIT — עדיין-broken לפי-CLAUDE.md — ולא-אימוץ-אוטומטי-היום).

**שינוי מוצע (design-בלבד, לא-למימוש-מיידי — ממתין ל-EXIT-v2):**
1. **adopt-or-alert סימטרי:** ה-reconciler היום מטפל רק-בכיוון TM>Sierra (phantom-heal). להוסיף-נתיב-סימטרי ל-Sierra>TM/הפוך: אימוץ-הפוזיציה ל-TM (מיפוי order-ids מ-Sierra events, כך שהסטופ-הקיים-בפועל ינוהל ע"י ה-backend, לא-רק "יתקווה שהוא-שם") — **או**, אם-אימוץ-מסוכן-מדי-בלי-בדיקה, לפחות alert-אקטיבי (לא-רק-log-שקט) אחרי-N-דקות-של-DIVERGENCE-בכיוון-הזה.
2. **תלוי op=EXIT-v2:** כל-נתיב-שדורש-לסגור-חלקית (לא-full-flatten) חסום עד-שה-EXIT-v2 (CC_PROMPT_2026-07-14) ייבנה+ייבדק-בסים — per CLAUDE.md standing-instruction. adopt (בלי-לסגור, רק-לשייך) לא-נוגע-ב-op=EXIT ולכן-אינו-חסום-על-ידו, אבל **כן** משנה-risk-surface (TM יתחיל-לנהל-כניסה/יציאה על-פוזיציה-שלא-הוא-פתח) → Michael sign-off.

**Regression שיוכיח:** `test_reconciler_adopts_sierra_only_position` — sierra_state qty≠0 / TM=0 רצוף N-מחזורים → TM מאמץ (או לפחות מסמן ALERT-פעיל, לפי-החלטת-מייקל) במקום-להישאר-ב-`DIVERGENCE`-שקט.

**עדיפות:** 🟡 — פתוח, אך מייקל כבר-נתן-הוראת-ניהול-זמנית ("תן-לסטופ"). לא-דחוף-לתיקון-הלילה; דורש-הכרעה-איתו לפני-בנייה.

---

## §5 · עיוורון-sandbox-EOD/session-watch — 8 אזהרות-רצופות בלי-פתרון · **[TOOLING]**

**שורש (מתועד):** ה-sandbox של-ריצות-מתוזמנות (session-watch + EOD-זה) לא-מגיע ל-iMac (מכונת-המסחר-בפועל מ-cutover 15:46) ולא-למק-הפיתוח (מנותק-בכוונה). 8 ריצות session-watch נפרדות (17:26→22:14) דיווחו-בדיוק-את-אותה-מגבלה וכל-אחת-המליצה להסב-ניטור ל-iMac או-להשבית. **ריצת-ה-EOD-הזו (הנוכחית) סבלה מ-אותה-מגבלה** — אין-API/DB, אין-`PATTERN_DIAG`, ולכן-טבלת-arm/fire-של-הדוח-הראשי חלקית-בהכרח.

**שינוי מוצע:**
1. **הסבת-היעד:** מוניטור-הסשן/EOD-המתוזמן מכוון ל-`http://192.168.1.95:8000` (כתובת-LAN-של-ה-iMac שכבר-נפתחה עבור S-8/מוניטור-הנייד) במקום localhost — אם-ה-sandbox-הזה-מסוגל-לגשת-ל-LAN-ביתי (לבדוק-קודם: ל-Chrome-MCP-כן יש-גישה-רשת של-המשתמש; ל-Bash-sandbox-הזה כנראה-לא — צריך-לבדוק-בפועל).
2. **חלופה אם-LAN לא-אפשרי מה-sandbox:** ה-EOD/session-watch עצמם ירוצו **על ה-iMac** (Claude Code, לא-Cowork) — תואם ל-`AGENT_SYNC.md`-פרוטוקול (cc-imac כבר-כותב-ל-git); cowork-dev יכול-לצרוך את-הפלט מה-git במקום-לנסות-API בעצמו.
3. **מזעור-נזק בינתיים:** כל-ריצה-עתידית-מה-sandbox-הזה תבדוק-קודם `Chrome MCP connected? + curl-ready?` ותוותר-מהר על-נתיב-ה-API (במקום-לחזור-על-8-ניסיונות-זהים) ותעבור-ישר לסינתזה-מ-git-tracked-docs (כמו-בדוח-הזה) — חוסך-זמן, לא-פותר-את-הפער-עצמו.

**Regression/אימות:** לא-קוד — זו-החלטה-תפעולית. "הוכחה" = ריצת-EOD-הבאה מצליחה-לקרוא `/gateway/decisions` או `/trades/recent` אמיתיים.

**עדיפות:** 🟡 — לא-סיכון-מסחר, אבל **8 אזהרות-זהות בלי-פעולה** הוא-עצמו-דפוס-שראוי-להפסיק. החלטה-של-מייקל, לא-טכנית-גרידא.

---

## §6 · DAYTYPE_LOCATION_GATE — חצי-בנוי, מפרט-השלמה נכתב היום · **[TRADING-LOGIC] · [SoT→CC]**

**שורש (מתועד, ביקורת-דלתון של-צ'אט-מערכות היום):** `daytype_playbook.py:decide()` אוכף **רק** (א) תא תבנית×יום FULL/REDUCED/SKIP, (ב) `require_with_trend` — חוסם-רק-counter-trend. **אין שום בדיקת-מיקום** (VAH/VAL/POC) — הבלוק `daytype_style` ב-YAML הוא-תיעוד-בלבד, `decide()` לא-קורא-אותו. זה-שורש-משותף לשלוש-תופעות: (1) #372-אתמול (REACTIVE_LONG בקצה-VAH ביום-Variation לא-נחסם — לפי-דלתון fade-שם-הוא-SHORT), (2) תאי-playbook הפוכים-מהספר (REACTIVE/HNS×Variation=FULL כשהם-אמורים fade וה-יום-הוא-continuation), (3) ריצת-הפתיחה (עקיפת-`OPENING_WINDOW_FIRE_V1` בלי-בדיקת-מיקום כלל).

**מפרט קיים (נכתב היום ב-`EXECUTION_REPORT_2026-07-15.md`, לא-חוזר-כאן במלואו):** flag `DAYTYPE_LOCATION_GATE` (default OFF), קלט=entry/direction/day_type/VAH/VAL/POC/IBH/IBL חיים, סיווג-אזור (near-VAH/near-VAL/mid-value/מתוח-מעל/מתוח-מתחת/near-POC, סובלנות ≤0.25×IB), כיוון-מותר-לפי-אזור-ולפי-סוג-יום. **תלוי-הכרעת-מייקל:** Variation = "with-expansion" (yaml) מול "fade-edges-בלבד" (הנחיית-מייקל) — סתירה-לא-פתורה.

**שינוי מוצע (design, לא-למימוש כאן):**
1. לפני-בנייה: להכריע-עם-מייקל את-הסתירה Variation-with-expansion/fade-edges (חוסם-את-כל-שאר-המפרט).
2. לממש כ-flag-OFF-כברירת-מחדל, רפליי N-ימים לפני-הדלקה (per Pre-LIVE Discipline — אין-loosening-שקט).
3. לתקן-במקביל את-תאי-ה-playbook-ההפוכים-מהדלתון (REACTIVE/HNS×Variation, VEGAS/GHOST/FAMIR/DBDT×Variation) — SKIP או REDUCED+location-gate, לפי-הטבלה-שכבר-נכתבה ב-EXECUTION_REPORT.

**Regression שיוכיח:** `test_location_gate_blocks_372_class` (#372-class → SKIP) · `test_location_gate_allows_fade_at_edge` (fade-בקצה-הנכון → allow) · `test_daytype_style_yaml_matches_decide_enforcement` (anti-tautological: לוודא שהיום-decide() באמת-לא-קורא yaml, כדי-שהתיקון-לא-יהיה-no-op).

**עדיפות:** 🟡 — לא-דחוף-להערב (S-10/קיפאון-הפיד קודמים), אבל **שורש-מבני-אמיתי** שחוזר-ומשפיע (#372, playbook, פתיחה) — מומלץ P1-הבא-בתור אחרי-שהתשתית-הבסיסית-יציבה.
