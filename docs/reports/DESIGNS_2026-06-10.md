# MEMS26 · טיוטות-עיצוב — 2026-06-10

לכל בעיה מאומתת היום: שורש · השינוי המוצע (קובץ/שכבה) · regression שיוכיח · סיווג **trading-logic (→אישור Michael)** או **display/safe**. **לא ממומש — מוכן-להפיכה-לפרומפט-CC.** מקור-אמת מסומן להצלבה מול Sierra v9_export.

מקרא סיווג: 🟢 **display/safe** (תצוגה/דיווח/דיאגנוסטיקה, לא בנתיב-ירי) · 🔴 **trading-logic** (משנה מה/מתי יורה או risk — דורש strategic-stop + אישור Michael).

> **הוכחת-היום (counterfactual):** ה-bottleneck היחיד-החד-משמעי הוא **D1 (טבלת stop/target)**. §3 ב-`PATTERN_EOD_2026-06-10.md` הראה ש-(א) ה-target המנוון חסם ~+3R של SHORTים עם-המגמה, ו-(ב) ה-stop-הצמוד שרף מנצח (id27, MFE +16.75pt). שני הקצוות = אותו רכיב חסר.

---

## D1 · I-13 / I-3 — היעדר טבלת stop/target: חוסם-העל של S4 משני הקצוות (🔴 trading-logic)

**שורש (מאומת על trades חיים + 4 signals היום):**
- **קצה "צמוד-מדי" (עסקאות שירו):** 3 HFE LONG (id24/26/27) נורו עם stop **1.0/2.0/1.75pt** ⇒ 3/3 נעצרו ברעש-בר (MAE 6.75–23.75pt ≫ stop). id27 הגיע ל-**MFE +16.75pt** — היה מנצח גדול עם stop סביר.
- **קצה "מנוון/רחב" (signals שנחסמו):** ZLR/TLB/GB100/GHOST/HTLB SHORT הגיעו ל-A7 (A1–A6 PASS) אך נפלו ב-`A7 R:R<1.0` כי ה-`targets_stop` מייצר **T1 מנוון 3pt** מול stop 12–20pt (R:R 0.18–0.65). ⇒ `r_t1_gate`/`exit_rules.ready_to_route` נכשלים ⇒ `fire_setup` לא נבנה ⇒ **שום תבנית-S4 לא יכולה לנתב כש-trend מוכרע.**
- חסימת ה-R:R **נכונה מתמטית** — ה**שורש** הוא היעדר טבלת stop/target אמיתית.

**השינוי המוצע (שכבה: S4 `targets_stop` / config-table):** להזין **טבלת stop/target פר-תבנית×day-type×anchor** (YAML-tunable, = project `stop/target placement table` של Michael) ל-builder של `fire_setup`. T1–T5 ו-stop ייגזרו מהטבלה (לא 3pt מנוון; לא 1pt stop). **wire-the-full-pipeline (לקח 5-flag):** הטבלה חייבת להגיע ל**כל** הענפים — `targets_stop.r_t1_gate`, `targets_stop.targets`, `targets_stop.stop_price`, `exit_rules.ready_to_route`, ו-sizing — בלי dead-wiring חלקי. **אתר את הקובץ דרך `SYSTEM_INDEX.md`** לפני נגיעה — לא מהזיכרון.

**regression שיוכיח:**
1. signal ZLR קבוע (entry 7317.25, stop מטבלה, day_type=Normal) → T1/T2 מהטבלה ⇒ `assert fire_setup is not None` ו-`assert R:R ≥ 1.0`; ב-replay של ברי-היום (ירידה ל-7276) ⇒ `assert hit_T1`.
2. **anti-tautological:** signal עם target מנוון 3pt חייב **עדיין** להיחסם (R:R-gate לא בוטל — רק ה-target תוקן ע"י הטבלה).
3. **קצה-stop:** HFE עם stop-מטבלה ≥ ATR-בר ⇒ `assert stop_distance ≥ min_stop`; trade עם MAE < stop ⇒ לא-נעצר (id27-style).

**סיווג:** 🔴 **trading-logic — strategic-stop + אישור Michael.** פותח את ZLR/TLB/TT/GB100/GHOST/HTLB/HFE לירי ומשנה את ה-risk. **תלוי בהגדרת הטבלה ע"י Michael** (project `stop/target placement table`). **לא לממש בלי הטבלה + אישור בכתב.**
**מקור-אמת ל-CC (חובה):** מקור ה-stop (1–2pt) וה-target (3pt) — builder-בקנד או Sierra? הצלב ATR/מבנה מול `~/SierraChart_Data/v9_export/`. (ממשיך `DESIGNS_2026-06-09.md` §D2.)

---

## D2 · I-28 (חדש) — HFE/reversal יורה נגד-מגמה מבוססת (🔴 trading-logic)

**שורש (מאומת):** 3 ה-fires היחידים היום היו **HFE LONG (reversal)** בתוך **trend RED יציב** (cci_14 −75→−154). HFE מותר לירות נגד-מגמה לפי-עיצוב (A1 מאשר reversal), אך התוצאה: id24/26 מפסידים-אמיתיים (MAE 11.75/23.75, MFE 2.5/0) — תנועה-נגדית מיד. גם עם stop סביר (D1) ה-CF של ה-HFE ≈ **−0.5R** (id27 בלבד מנצח). ⇒ D1 נחוץ אך **לא מספיק** ל-reversal-LONG-נגד-RED.

**השינוי המוצע (שכבה: S4 reversal-pattern gating / confidence):** **טעון-החלטת-Michael** — אחת מ:
(א) דרישת-confirmation חזקה יותר ל-reversal (HFE/GHOST) כש-trend מנוגד-וחזק (למשל cci_14 מעבר לסף + WSI), או
(ב) הקטנת-size ל-reversal-נגד-מגמה (כבר sizing=half — אולי quarter/veto מתחת ל-cci-סף), או
(ג) להשאיר כפי-שהוא ולתת ל-D1 (stop רחב) לנהל — בהנחה ש-id27-style מנצח לאורך-זמן מכסה.
**לא לבחור בלי Michael** — זו שאלת-אסטרטגיה, לא באג חד-משמעי.

**regression שיוכיח:** תלוי-בחירה. אם (א): HFE LONG עם cci_14 < −150 ⇒ `assert blocked` (או size מוקטן); HFE LONG עם cci_14 > −50 ⇒ עובר. anti-tautological: HFE בכיוון-המגמה לא מושפע.

**סיווג:** 🔴 **trading-logic — strategic-stop + אישור Michael.** משנה מה יורה. **דרוש input מ-Michael על מדיניות reversal-נגד-מגמה לפני עיצוב סופי.**
**מקור-אמת ל-CC:** הצלב cci_14/WSI/trend גולמי מ-Sierra לברי 10:09–10:20 CT (כניסות id24/26/27).

---

## D3 · I-22 — `pnl_r` מנופח ×10 (🟢 display/safe — עדיין פתוח)

**שורש (מאומת raw):** R מחושב `pnl_usd ÷ $1.25` (ערך-טיק) במקום `÷ risk_$ פר-חוזה`. id20 (06-09) `233R`, id13 (06-05) `+26.75R` על STOP_HIT (סימן+סדר-גודל שגויים). **היום:** 3 stop-outs טריים = `−1R` **נכון** (id27 C1 `−8.75÷(1.75pt×$5)=−1.0R`) — מחלק-stop-out לא-מושפע. **win-path עדיין לא-מאומת-תיקון** (אין WIN טרי 06-10).

**השינוי המוצע (שכבה: trades/PnL-calc):** להחליף מכנה מ-`tick_value` ל-`risk_per_contract_$ = |entry − stop_initial| × point_value`; לחשב פר-חוזה ב-`contracts_pnl[*].pnl_r` ולסכם נכון. אתר דרך `SYSTEM_INDEX.md`.

**regression שיוכיח:** trade entry 7489.25/stop 7491.75 (risk 2.5pt)/exit 7383.25 ⇒ `assert pnl_r ≈ 42.4` (לא 233). **anti-tautological:** trade עם risk≠2.5pt ⇒ לוודא שהמכנה=risk, לא קבוע. **+ regression-WIN:** trade מנצח עם risk ידוע ⇒ `pnl_r` חיובי-נכון (סוגר את הפער שהיום לא ניתן לאמת).

**סיווג:** 🟢 **display/safe** (אף gate לא קורא `pnl_r`). חוסם EOD-counterfactual ⇒ עדיפות גבוהה.
**מקור-אמת ל-CC:** הצלב entry/stop_initial/exit-fills על **WIN/partial טרי** מול `risk_$`. (ממשיך `DESIGNS_2026-06-08/09.md` §D1.)

---

## D4 · I-23 — 4 ספירות-ירי סותרות (gateway/shadow לא מחווטים) (🟢 display/safe)

**שורש (מאומת חי):** 3 fires (id24/26/27) אך `woodies.fired_today=3` · `gateway.trades_today=0` · board UI "**S4 ×1**" · Dashboard "**0f $0**" — **4 משטחים, 4 תשובות**. `daily_pnl` אמור −$71.25, מוצג 0. מוני-gateway + `shadow_active_count` לא מחווטים ל-shadow-fires; ה-board-counter ("×1") חלוק על ה-endpoint ("3").

**השינוי המוצע (שכבה: gateway counters + board aggregation):** לחווט את `trades_today`/`shadow_active_count`/`daily_pnl` לספור **shadow-fires** באותו event (++ ב-fire, pnl += contract pnl, active=פתוחים). ליישר את ה-board-counter למקור-אמת יחיד (`fired_today_count`/DB). **wire-the-full-pipeline:** כל 4 המשטחים מאותו מקור.

**regression שיוכיח:** shadow-fire יחיד ⇒ `assert trades_today==1 ו-shadow_active_count==1 ו-board==1`; אחרי close ⇒ `shadow_active_count==0`, `trades_today` נשאר, `daily_pnl` מעודכן. anti-tautological: 3 fires ⇒ כל-4-המשטחים==3 (לא 0/1/3/0 של היום).

**סיווג:** 🟢 **display/safe** — מוני-דיאגנוסטיקה (WR/Trades בכותרת נגזרים מ-`/trades/recent`). (ממשיך `DESIGNS_2026-06-09.md` §D5.)

---

## D5 · I-10 — `pre_fire_validator` + `risk_checks` לא נפלטים ל-endpoint (🟢 display/safe)

**שורש (מאומת UI + payload):** חיפוש-מחרוזת על כל 88–89KB של `build/pattern-status`: `risk_checks` **absent**, `pre_fire_validator` **absent** כ-top-level. R:R≥1.0 חשוף **רק** כ-`targets_stop.r_t1_gate` per-pattern — אין שורת pre-fire-gate גלובלית. בלוח: 6 risk_checks + 7 pre_fire_validator מסומנים "✗ ממתין — לא נפלט ל-endpoint".

**השינוי המוצע (שכבה: build/pattern-status serializer):** לפלוט `pre_fire_validator` (7: side==direction, R:R≥1.0, time_stop, no-duplicate-fire, entry/stop-ordering, confidence≥threshold, entry/stop≠provisional) ו-`risk_checks` (6 caps: daily-loss/concurrent-contracts/stop-after-losses/trades-per-day/cutoff-time/news-block) כ-**שורת-gate גלובלית** ב-payload. **קריאה-בלבד מהמנוע** — לא לשנות את הבדיקות עצמן, רק לחשוף.

**regression שיוכיח:** `assert 'pre_fire_validator' in payload` ו-`assert 'risk_checks' in payload`; כל בדיקה עם `{name, pass/fail, value}`. anti-tautological: כשל R:R אמיתי ⇒ `pre_fire_validator.rr.pass==false` (לא absent).

**סיווג:** 🟢 **display/safe** — חשיפה-בלבד (הבדיקות רצות במנוע; רק לא נראות). נתיב-הירי לא משתנה.

---

## D6 · I-18 / I-20 — freshness TZ-mix + ts-עתידי + stale-עובר-READY (🟢 display/safe, data-integrity)

**שורש (מאומת):** (א) `woodies_5min` gate ts מתויג-עתידי/`lag=null`; `bars_5min` IL-local; CVD/VP/imbalance UTC תקין — TZ-mix. (ב) **כשל-שקט:** `imbalance` crit:true `[FRESH]` אך lag עד **~111דק' > 90s req** עובר `bridge_streams_fresh` ⇒ readiness READY. (ג) `lag_seconds` שלילי/null עם `fresh=true` (predicate לא אוכף `0≤lag≤threshold`). מפר Rule 4.

**השינוי המוצע (שכבה: bridge ingest boundary + freshness predicate):** (1) לנרמל כל ts ל-UTC מפורשות בגבול-הקריאה (לא "assumed IL") + guard נגד ts-עתידי (`reject/clamp + logger.warning rate-limited`, לא debug). (2) לאכוף `0 ≤ lag ≤ threshold` ⇒ lag-שלילי/null = stale. (3) `Present` לא מספיק ל-`bridge_streams_fresh` — לאכוף את הסף גם על present-streams.

**regression שיוכיח:** `lag=-100 ⇒ fresh=false`; `lag=200,thr=90 ⇒ fresh=false`; imbalance lag 6640s ⇒ `bridge_streams_fresh==false` (לא READY); בר ts-עתידי ⇒ נדחה + warning.

**סיווג:** 🟢 **display/safe** ברובו (readiness משתמש ב-crit-gates נפרדים), אך **data-integrity** בערוץ-הזנה ⇒ עדיפות בינונית-גבוהה.
**מקור-אמת ל-CC (חובה):** mtime + last-bar-ts ב-`~/SierraChart_Data/v9_export/woodies_5min` — Sierra מייצא ts-עתידי או bridge ממיר שגוי? (ממשיך `DESIGNS_2026-06-09.md` §D3/D4.)

---

## D7 · I-26 (חדש) — אי-עקביות A7 reward ↔ target מוצג (🟢 display/safe — diagnose-first)

**שורש (מאומת #3):** ה-active-pattern הציג `target 7328.75` = **1pt** מ-entry 7327.75, בעוד `A7 reward` מחושב = **6.00**. שני מספרים שונים לאותו reward ⇒ או ה-target המוצג שגוי, או ה-reward של A7 מגיע ממקור-אחר (provisional/ATR פנימי).

**השינוי המוצע:** **קודם אבחון (CC), לא עיצוב-עיוור.** לאתר מאיפה A7 שואב `reward` מול מאיפה `targets_stop.targets` שואב את ה-display. אם נפרדים — לאחד למקור-יחיד (חופף ל-D1: הטבלה היא המקור). תיעוד reject-context ב-`details{}` (I-12, ריק היום).

**regression שיוכיח:** signal יחיד ⇒ `assert A7.reward == (entry − displayed_T1) × point_value` (אותו מקור). anti-tautological: target שונה ⇒ reward משתנה בהתאם.

**סיווג:** 🟢 **display/safe** (חשיפה/עקביות-דיווח). אם יתברר שה-reward השגוי משפיע על r_t1_gate ⇒ מתמזג ל-D1 (trading-logic). **diagnose-first.**
**מקור-אמת ל-CC:** מקור ה-reward (builder/Sierra).

---

## נדחים מעיצוב היום (טרם בשלים — דורשים אבחון/הצלבה לפני עיצוב)

- **I-1** (day_type split Normal↔Variation + `session_min=0` + `vote_history=[]`) — feed-instance/wrapper + הצלבת `Y IB dll_missing`/atr_daily מול DLL. **לא לעצב מהזיכרון.** — CC.
- **I-11** (footprint 0 ברים) — מושתק S3_MUTE, S3 deferred עד אחרי-LIVE. עיצוב-ingest כשתיפתח S3. לא blocker.
- **I-15 / I-17 / I-27 (חדש)** — flicker-CCI תוך-בר (חציות-אפס +113↔−134) + **flicker-detection per-bar** (#8: TLB↔NEUTRAL ב-~40s על cci כמעט-זהה). engine↔board מסכימים same-instant (I-15 לא-משחזר) ⇒ נדרש **CCI-14/TCCI גולמי מ-Sierra** לברי-הגבול לפני הכרעה אם artifact-בקנד. — CC.
- **I-19** — `build/pattern-status` 116–122ms היום (נקי); נטר אם חוזר ל-2898ms (06-09).

**הערת-פייפליין (לקח 5-flag, מהזיכרון):** כל שינוי-החלטה/flag (D1, D2) חייב להגיע ל**כל** ענף מושפע — אסור dead-wiring חלקי. **כל ה-trading-logic (D1, D2) דורש strategic-stop + אישור Michael בכתב לפני מימוש.** D1 תלוי גם בהגדרת טבלת ה-stop/target ע"י Michael.
