# פרוטוקול פתיחת-מסחר-לייב + משימות פתוחות · 2026-07-14

מכונת-המסחר: iMac · חוזים: 3 · עצירת-הפסד: −$400 · חלון-חדשות: דלוק (רק 🔴).

---

## חלק 1 — פרוטוקול פתיחת-לייב (שלב-אחר-שלב)

### שלב A · T-30 (16:00 IL) — תשתית עולה ובריאה
- [ ] **שירותים:** ‏backend · ‏bridge · ‏export_promoter · ‏frontend — כולם ● רץ
      (‏MEMS26_CONTROL → סטטוס, או `scripts/mems26_verify.sh`).
- [ ] **‏flag_guard PASS** (62 דגלים) · **‏fire_drill 🟢 GO**.
- [ ] **פיד טרי:** ‏woodies_5min.json < 5ש' · ‏DB last-bar lag < 2 דק'.
- [ ] **גשר local-only:** אין `push FAILED to https://...` בלוג.

### שלב B · T-15 — סנכרון מול סיירה (source-of-truth)
- [ ] **מחיר עליון זז** ותואם סיירה (±tick).
- [ ] ‏IB · POC/VAH/VAL · ‏Woodies-studies = סיירה (לא תקוע על "Last RTH").
- [ ] **עין-סיירה חיה:** `sierra_state.json` age < 3ש'.

### שלב C · T-5 — 🔴 המעבר ל-LIVE (ידני, אתה בלבד)
- [ ] **הפוך את סיירה ל-LIVE:** כרגע `is_sim=1`. עד שתכבה את Trade-Simulation-Mode
      בסיירה — **אתה על סים, לא לייב.** אחרי הכיבוי ודא `is_sim=0` ב-sierra_state.
- [ ] **חשבון-אמת ממומן** ומחובר (Teton).
- [ ] **חימוש:** ‏Input 21 (`EnableOrderPlacement`) = 1 → ‏`order_placement_armed:1`
      ב-state. (re-add של הסטאדי מאפס ל-0 בשקט — לוודא!)

### שלב D · T-0 (16:30 IL) — פתיחה
- [ ] **בריפינג-בוקר** נקרא (`scripts/morning_briefing.py`): סוג-יום, סטופים,
      הקשר-דלתון, ה-bracket השבועי.
- [ ] **חלונות-חדשות היום:** ‏CPI 15:20–15:35 (לפני-פתיחה) · **וורש 16:50–17:05
      — בתוך המסחר, המערכת תחסום כניסות שם אוטומטית.**
- [ ] העסקה-הראשונה: אמת בעין ‏entry+3-חוזים+ברקט-OCO תקין (working=6).

### שלב E · השמשת מערכת-6 (הפיקוח על העסקה החיה)
מערכת-6 = השומר על העסקה הפתוחה. **שתי שכבות, ומצבן היום:**
- ✅ **SYSTEM6_SUPERVISOR = דלוק** — סורק כל עסקה חיה בכל מחזור, מאבחן 9
      אינווריאנטים (reconcile slot/DB/סיירה · סטופ-עירום · צד-סטופ · BE-אחרי-T1 ·
      צד-יעד · רצועת-סטופ · שווי-T1 · גודל · EOD), ומרעיש **ALERT** + ממליץ תיקון.
      **זה מה שאתה רוצה דלוק בלייב** — הוא הפעמון שלך.
- ⏸️ **SYSTEM6_AUTOCORRECT = כבוי** (פסיקתך — שבוע-צפייה נקי לפני הדרגה). המשמעות:
      מערכת-6 **מזהה ומתריעה** אבל **לא מתקנת לבד** — אתה מקבל את ה-ALERT ופועל ידנית
      (הזזת-סטופ / **FLATTEN**). זו ההתנהגות הבטוחה ליום-לייב ראשון.
- **פעולה:** ודא ש-`SYSTEM6_SUPERVISOR=1` ב-.env (הוא כבר), ושהפאנל ב-/board מציג
      "מערכת-6 · read-only" חי. אל תדליק AUTOCORRECT היום.

### שלב F · שער-GO הסופי (15:55, אוטומטי + אתה)
- [ ] ‏fire_drill 🟢 + ‏flag_guard PASS + 6 השערים בפרוטוקול-הפרה-טרייד.
- [ ] **התראות-לטלפון** (מומלץ מאוד ללייב): אם תיתן טוקן-Pushover — תקבל 🔴
      סטייה/עצירת-הפסד/עסקה גם רחוק מהמסך. 5 דקות הקמה.

### רשת-הביטחון (לפני שאתה נכנס)
- **FLATTEN עובד ומאומת** — בכל רגע כפתור-מילוט לפוזיציה מלאה.
- **עצירת −$400** דלוקה — היום נעצר אוטומטית בהפסד-יומי.
- **חלון-חדשות** חוסם כניסות סביב 🔴 אוטומטית.

---

## חלק 2 — כל המשימות הפתוחות

### לפיתוח (טבלה 1) — אף אחת לא חוסמת-לייב
| עדיפות | משימה | אחראי | הערה |
|--------|--------|-------|------|
| **P1** | ‏op=EXIT v2 (יציאה-חלקית מבורקטת) | 🟣 CC | **לא היום** — DLL בלייב = סיכון; ממתין לחלון-סים. אין קורא חי. פרומפט מוכן. |
| P2 | הידרציה-מלאה בבוט | 🟣 CC | נמסר |
| P2 | ‏LaunchAgent לפרונטאנד | 🟣 CC | נמסר |
| P3 | ‏7 פיקסטורות-טסט ישנות | 🟣 CC | נמסר |

### בנוי, ממתין לפסיקת-הדלקה שלך (טבלה 2)
| משימה | מה צריך |
|--------|----------|
| חלון-חדשות | ✅ כבר דלוק |
| **התראות-לטלפון** | **טוקן-Pushover ממך** (מומלץ ללייב) |
| ‏P2-9 ‏TPO-count לביטחון | מילה שלך |
| ‏STALL_EXIT · ‏S6-AUTOCORRECT · כיול-יעדים-3c | שבוע-נתוני-לייב (~19/07) |

### פעולות-מייקל (לא-קוד)
| פעולה | דחיפות |
|--------|---------|
| **הפיכת סיירה ל-LIVE + חשבון ממומן** | 🔴 קריטי לפני פתיחה |
| רוטציית-טוקן GitHub | גבוהה (אבטחה) |
| ‏`brew install redis` | בינונית |
| ‏cutover ל-iMac (צ'אט-ההעברה) | לפי קצבך |

---

**שורה-תחתונה:** המערכת ‏GO. אין תיקון-קוד חסר לפתיחה. מה שחוסם אותך מלייב זה
2 פעולות ידניות שלך: (1) הפיכת סיירה ל-live + חשבון, (2) חימוש Input 21.
מערכת-6 כבר משגיחה (supervisor דלוק); AUTOCORRECT נשאר כבוי בכוונה.
