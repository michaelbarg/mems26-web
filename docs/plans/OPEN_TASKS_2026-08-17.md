# רשימת המשימות הפתוחות — 2026-08-17

נכתב 11:00 IL · cowork-dev · אחרי סוף-שבוע של תיקוני-שורש + שתי ביקורות-סוכן.
מיון לפי **מה עוצר כסף**, לא לפי מה קל.

**מצב המערכת ברגע הכתיבה:** `flag_guard` 175 PASS · ALL SYSTEMS ARMED ·
`fire_drill` GO · `wire_guard` 40/40 · פוזיציה 0 · 4 חוזים · הפיד טרי.

---

## 🔴 חוסמים — משמעותם שיכולת-מסחר חסרה

### O1 · ה-DLL לא הודר מאז 28.07 — 5/6 חוזים חסומים
הסולם (1/2/2/1) נכתב ל-`.cpp` ונפרס לשני ה-installs, אבל **הבינארי לא נבנה**.
`mems26_verify` משווה `.cpp` ולא `.dll` ⇒ הוא מדווח ירוק על מצב שאינו נכון.
- **מה חוסם:** כל סייז מעל 4. הדלקת `FIXED_CONTRACTS_5/6` בלי בנייה = 1-2 חוזים **עירומים**.
- **מי:** מייקל — Remote Build בסיירה + reload study. אחר-כך דריל-סים שמוכיח
  ב-`sierra_state.orders` שכל החוזים מוגנים.
- **תיקון-נלווה:** `mems26_verify.sh` חייב להשוות את **הבינארי**, אחרת הירוק-השקר יחזור.

### O2 · אין `op=MODIFY_TARGET` ב-DLL — "לממש יותר רחוק" בלתי-אפשרי
ה-executor (`bar_level_detector`) מכיר **שתי** פקודות: `MODIFY_STOP`, `DROP_TARGET`.
- **מה חוסם:** `target_divergence` (אינווריאנט 13, הלקח מ-#633) ואת חצי-הפסיקה
  של `reversal_tighten` — שניהם מסומנים AUTO ונופלים ל"טיפול ידני".
- **מי:** משימת-DLL. לבדוק אם ה-op קיים; אם לא — לבנות, לפרוס, לאמת בסים.
- ⚠️ **לא** לבנות על משפחת `op=EXIT` — שבורה (`r=-1`).

### O3 · מק-2 לא נגיש
`ping 10.1.118.70` = 100% packet loss. לא נכתבה שם עסקה מאז 17.07.
- **החשוד:** `InFailedSqlTransaction` — Session משותף בלי `rollback`. התיקון
  (`session_guard`) כבר ב-origin, מק-2 רק צריך למשוך.
- **ערכת-אבחון עצמי מוכנה לו:** `docs/handoff/CC_MAC2_GAP_PROBE_2026-08-17.md`
- **מי:** מייקל מדליק את המכונה → cc-mac2 מריץ את הערכה.

---

## 🟠 פערי-נכונות — המערכת סוחרת, אבל מודדת או מדווחת לא-נכון

### O4 · 5 מתוך 13 האינווריאנטים של S6 — חוּוטו הבוקר, ALERT בלבד
cc חיווט את הקלטים (`3dd3b8c8`). עכשיו הם **מדברים**, לא מבצעים.
- **מה נדרש:** 20 סשנים של ראיות ⇒ ואז פסיקה פר-אינווריאנט.
- ⚠️ **אזהרה מדודה:** כלל-הסטגנציה נבדק על 40 סשנים / 497 עסקאות →
  **+$254 בסך-הכל אך שלילי ב-20 הסשנים האחרונים, 55% בוטסטרפ = רעש.** NO-GO
  לאוטומציה. המועמד היחיד ששרד הוא החזר-33%-מה-MFE, והוא עולה $1,632 ב-21.07
  ו-$434 ב-13.08 — ימי-מגמה. גם הוא SHADOW-קודם.

### O5 · `sierra_targets` במטמון-זיכרון — נאבד בריסטארט
אינווריאנט 13 לא יכול לעבוד אחרי restart. חסום ממילא מאחורי O2.

### O6 · סולם-היעדים פגום ב-10 מתוך 42 עסקאות-לייב (24%)
6 הפוכות (T2 קרוב מ-T1 ⇒ חוזה-2 מממש ראשון ובפחות) · 4 קרוסות (T1==T2 ⇒
**אין ראנר בכלל**, כולן ZLR). זה פוגע ישירות ב"סיכוי-ההצלחה של עסקה".

### O7 · `StopResolver` אינו המעצב האחרון של הסטופ
`STEP_SCALED_LADDER_V1` רץ 363 שורות אחריו ודורס בשקט — מבטל מבנה, תקרות-תבנית
ורצפת-רוטציה. **אין לוג-ארביטרז'** שאומר מי ניצח. בנוסף שלושה ריווחי-עוגן
במקביל: 16T בגלאים · 6T ברזולבר · 3T בטרייל.

### O8 · חריגות מתקרת-25-הנק': 3 עסקאות
#581 (43 נק') · #575 (39.75) · #536 (37.25) — כולן במסלול S2/OPENING, כי
**S2 לא קורא את תקרות-התבנית של עצמו**.

### O9 · `pnl_sierra` מאוכלס ב-1 מתוך 42
אין הצלבה אוטומטית מול הברוקר. פער 14.08 (−$135 בספרים מול +$120 בקובץ-החשבון)
לא נסגר במנגנון. `trade_activity_events.jsonl` בלי `trade_id`.

### O10 · `gate_profit_audit.py` שבור — הפרש 3 שעות
ברים חוזרים מ-PG ב-`+03`, החלטות ב-`+00`, ושניהם עוברים `.replace(tzinfo=None)`.
**כל ניתוח-שערים שנשען עליו חשוד** — כולל הנימוק שכיבה את `ZONE_LIMIT` ב-14.08.

### O11 · `ZONE_LIMIT_ENTRY_V1` הוא מתג, לא מסנן
381 "חסימות" ביומן → **1 אמיתית ב-4 שבועות**. בימים שהיה דלוק עברו 16 כניסות
מאוחרות בעלות **−$1,727** והוא עצר **0**. השורש: הוא משווה entry מול המחיר-החי,
שהם אותו דבר בכניסת-market. **צריך תיקון-שורש, לא הדלקה.**

---

## 🟡 החלטות שממתינות לך

### O12 · תקרת-הפסד יומית מותנית-סוג-יום
cc מצא ש-14.08 (Trend_DD) אחראי לרוב הנזק, ושתקרת −$150 הייתה חוסכת $338.
**המספר נמדד על יום שלא קרה** — הסימולציה לקחה 7 עסקאות במקום 5 בפועל
(−$389 מדומה מול −$135 אמיתי). המדידה הנכונה: **מותנית-סוג-יום, 20+ סשנים,
שני חצאים.** `RISK_DAILY_LOSS_CAP=800` נשאר.

### O13 · `OPENING_FIRST_TRADE_STRICT_V1`
לפי EOD 14.08 עלה **$227.50**. מועמד #1 לפסיקה — אחרי שנראה איך הפתיחה
מתנהגת עם שער-הפיוז'ן המתוקן.

### O14 · `OPENING_CONF_ENGINE_FUSE_V1` — ה-catch-22
הפיוז מרים ציון רק כשהגלאי כבר ≥0.5, ו-`OPENING_MIN_CONF=0.6`. ביום שהגלאי
אומר 0.3 העסקה הראשונה נחסמת. **זו פסיקתך מ-12.08** ("כשהגלאי כיווני") —
שינוי דורש פסיקה חדשה.

### O15 · `SYSTEM7_SCORE_V1`
כבויה ונשארת. הדלקה = חיתוך כל עסקה ל-3 חוזים לכל היותר (`min()` ב-`:1866`).
25 שורות-הצל נמדדו על פונקציה חסרת 5 רכיבים ⇒ **הראיות לא תקפות**.
לפני פסיקה: להעביר `market_context` ו-`bar_ts` ב-`:3277`, ואז 3 ימי-צל.

---

## 🔵 חוב-תחזוקה

| # | מה | למה חשוב |
|---|---|---|
| O16 | `MOBILE_ACCESS_KEY` היה גלוי בקובץ ב-git | מוסך, אבל **בהיסטוריה** — להחליף מפתח |
| O17 | `SOURCE_OF_TRUTH.md` מיושן 54 יום · `SYSTEM_MANIFEST.md` 32 יום, חסרים 4 LaunchAgents | סוכן שסומך עליהם יטעה |
| O18 | `FLAG_INDEX --check` נכשל — 22 דגלים לא-מתועדים | כולל `EXIT_VERIFY_V1` של אתמול |
| O19 | 57 קבצי-טסט משתמשים ב-`inspect.getsource()` | זו המחלקה שאישרה תיקון שבור ל-#682 |
| O20 | ~490 כשלי-סוויטה פרה-קיימים | הופכים "ירוק" לחסר-משמעות; חייבים worktree-בסיס |
| O21 | 5 עסקאות-צל תקועות `PARTIAL` מ-14.08 | לא חוסם (`live_slot=None`) |
| O22 | `v9_bars_5min_continuous` — 929 שורות מזוהמות (`min(close)=1`) | הזיהום נפסק 20.07, השורות נשארו |

---

## מה **לא** לעשות היום

**אל תיגע באף דגל.** חמישה שינויים נחתו ב-36 השעות האחרונות ומעולם לא רצו
בשוק חי: T4 (אימות-יציאה) · T6 (בר-סגור) · T7 (יחידות-נפח) · resolver-הגודל ·
שומרי-scale-in. היום מבחן-האמת הראשון שלהם. משתנה נוסף הורס את הייחוס —
וזה בדיוק מה שקרה ב-13.08, כשריסטארט תוך-סשן עלה 12 מועמדים.
