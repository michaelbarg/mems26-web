# פרומפט לצ'אט מבצע — מערכת 1 לפי דלתון: שלושת ה-P0 (2026-07-08)

**להדביק לצ'אט חדש (Cowork/CC):**

---
קרא קודם `docs/handoff/START_HERE.md` ופעל לפי כלליו (Rule 5, anti-tautological tests,
smallest-correct-change, אין הדלקת דגלים בלי מייקל). המשימה שלך: לבצע את שלושת פריטי ה-P0
של ה-backlog בדוקטרינה `docs/spec_authority/DALTON_DOCTRINE.md` (התורה של מערכת 1 —
כל החלטה עיצובית מוצדקת בהפניית-עמוד מהספר). בנה הכל **flag-OFF**, אמת על replay של
2026-07-08 (יום מדרגות-ירידה Trend/DD שהמערכת סיווגה באיחור ובביטחון 0.46→0.18), ועצור
לפסיקת מייקל לפני כל הדלקה.

## P0-1 — רה-סיווג מיידי על פריצה+קבלה (דגל `S1_ACCEPTANCE_RECLASS_V1`)
פסיקת מייקל (07-08, חיה): "ברגע שיש פריצה באחד הצדדים הסיווג חייב להשתנות; מדרגות = לזהות
ולעדכן". מימוש לפי הדוקטרינה: כל פריצת רפרנס (IB / טווח-אתמול / אזור-איזון) שמלווה
ב-acceptance (2 ברים סגורים מעבר + ≥8% נפח-יחסי — יש תשתית ב-`relative_features.py:157-173`)
→ רה-סיווג באותו בר, בשני הכיוונים; חזרה-פנימה → revert. עמ' 37-38, 288-292 בספר.
**קבלה:** ‏replay של 07-08 מתהפך ל-Trend/DD-DOWN תוך ≤2 ברים מהקבלה; מנוע==UI==gate
(סגירת פיצול-המקורות I-44); טסט נופל על הקוד הישן.

## P0-2 — סיווג מוקדם מחויב ב-30 דק' (דגל `S1_COMMITTED_PROVISIONAL_V1`)
אין יותר FORMING עירום ב-30 דק' (`daytype_classifier.py:86`): ‏opening-type × מיקום-הפתיחה-
מול-value-אתמול × רוחב-IB → סיווג-שלב-1 מחויב (Open-Drive→Trend/NV · OA-out-of-range→
DD-watch, עמ' 63-74, 84). **קבלה:** ‏0% בר-FORMING ב-30 דק' על replays של 10 הימים
האחרונים + דוח hit-rate מול הסיווג הסופי.

## P0-3 — ציון ביטחון קנוני אחד (דגל `S1_CONFIDENCE_V2`)
ביטחון = שיעור הראיות המיושרות (opening type, one-timeframe, התמדת-extension, זנבות,
ספירת-TPO, ‏CVD, ‏close, ‏elongation) מחושב ב-`classify()`; ה-0.46/LOCKED_LOW_CONF הישן
(`state_machine.py:26`) לא מוצג יותר בשום surface. עמ' 29, 273-275. **קבלה:** מספר אחד
בכל ה-surfaces; עולה מונוטונית לאורך מדרגות 07-08; ‏UI מציג את אותו ערך כמו המנוע.

## כללי עבודה
- כל פריט: קוד + טסטים (נופלים-על-ישן) + replay-evidence → שורת STATUS_BOARD עם
  ממצא+תיקון+אימות → עדכון `task_board.json` (משימת S1-DALTON) והיומן.
- אל תיגע ב-S2/S4/gateway — רק מערכת 1 וה-surfaces שמציגים את הסיווג.
- בסוף: דוח קצר למייקל — מה נבנה, ראיות ה-replay, ואילו דגלים ממתינים להדלקה שלו.
---
