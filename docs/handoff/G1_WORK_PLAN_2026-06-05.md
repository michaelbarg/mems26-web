# G1 — Work Plan (what we do + how) · 2026-06-05

**Scope:** G1 only — promote 3 calibration cuts from `cross_context` JSON into
queryable columns. NO G2–G7, NO risk/polling/frontend. localhost-PG only. Every
"DONE" = paste command + raw output (Rule 5) + NOT-DONE section.

**§0 gate result (verified via API, 7/7 trades):** `has_killzone = has_daytype =
has_woodies = 7/7`. `cross_context` = 1-element array →
`cross_context[0].systems.{killzone_system, day_type_machine, woodies_system}`.
⇒ **G1 = promote + BACKFILL** (data exists in JSON; not a strategic-stop).

**Field contract (fixed names):** `day_type_at_entry` · `pattern_id_at_entry` ·
`session_at_entry`. Same-snapshot extraction (no second source). Silent/error →
`NULL` (not `""`). Zero synthesis.

---

## Division of labor (my execution boundary)
- **Phase A — AUTHOR (me, sandbox):** write all files into the repo.
- **Phase B — RUN (Mac, CC/Michael):** execute migration / backfill / pytest
  against `localhost/mems26` — sandbox can't reach the Mac PG. I provide exact
  commands; runner pastes raw output.
- **Phase C — VERIFY + CLOSE (me):** independent cross-check via API/code/git +
  board updates.

---

## Phase A — Author (sandbox)
1. **Model** — `backend/v9/db/models/trades.py`, add to `V9Trade` (no change to
   existing columns):
   ```python
   day_type_at_entry   = Column(String(20), nullable=True, index=True)
   pattern_id_at_entry = Column(String(40), nullable=True, index=True)
   session_at_entry    = Column(String(20), nullable=True, index=True)
   ```
2. **Migration** — new `alembic/versions/` file: add the 3 nullable columns +
   `CREATE INDEX` on each; `downgrade` drops the 3. Nothing else.
3. **Extraction helper** — reuse `trade_context.py` (`_systems_blob_at_entry` /
   `_snapshot_hint`, the same path `extract_trade_display` uses). One function:
   `cross_context → (day_type, pattern_id, session)` from
   `cross_context[0].systems.{day_type_machine, woodies_system.active_patterns,
   killzone_system}`. Value MUST equal what the UI shows. Silent → `None`.
4. **Populate-at-entry** — `trading_gateway.py`, between
   `_capture_cross_context()` and `_persist_trade()`: derive the 3 fields from
   the SAME captured `cross_context`; add the 3 columns to the `_persist_trade`
   INSERT.
5. **Backfill script** — ❌ **SKIPPED (Michael 2026-06-05: trades wiped, start
   from 0 today).** The 7 existing trades are truncated in the reset, so
   backfilling them is throwaway. No backfill script.
6. **Tests (anti-tautological)** — `backend/v9/.../tests/`:
   - WITH killzone in snapshot → `session_at_entry` == `extract_trade_display`
     zone.
   - **Litmus:** WITHOUT killzone in snapshot → `session_at_entry IS NULL`
     (not "", not synthesized) — RED if any fallback is added.
   - `GROUP BY day_type_at_entry` == manual JSON count on same rows.
   - Do NOT edit existing tests to green.

## Phase B — Run (Mac; paste raw output per Rule 5)
1. `alembic upgrade head` → paste output + `\d v9_trades` (3 cols nullable+indexed).
2. Restart backend so the new INSERT path is live (per CLAUDE.md: only with your
   ok).
3. Run backfill → paste `count(*) FILTER (... IS NOT NULL)` before/after + 3 rows.
4. `pytest <new tests>` → paste raw pass/fail; prove litmus RED on a revert.
5. `SELECT id, day_type_at_entry, pattern_id_at_entry, session_at_entry FROM
   v9_trades ORDER BY id;` → all 7 populated, matching JSON. Confirm one NEW
   trade after restart populates via the live path.

## Phase C — Verify + close (me)
1. Cross-check populated columns vs `/api/v9/trades/{id}.context_json`
   (UI-derived day_type/woodies/killzone) — must match, independently.
2. `STATUS_BOARD.md` + `ROADMAP_TO_LIVE.html`: finding + fix + raw verification.
3. NOT-DONE: G2–G7 deferred; frontend is a separate agent; any silent-source
   field stays NULL; backfill caveat (see cross-thread note).

---

## ✅ Cross-thread (RESOLVED — Michael 2026-06-05: reset to 0 today)
- **Backfill SKIPPED** — trades are wiped.
- **Columns + populate-at-entry (A1–A4) + tests (A6) MUST land BEFORE the reset
  + restart**, so the fresh shadow run captures the 3 columns from trade #1.
- G1 is now part of the single coordinated "start-from-0 today" cutover (see
  the unified cutover order alongside B-13 D1/D2/D3/D5).

## Guards
No touching existing columns / risk / polling / frontend. Same-snapshot only,
zero synthesis, NULL on silent. localhost-PG only. No G2–G7.
