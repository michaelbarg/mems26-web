# רצף מאושר (מייקל "מאשר" 2026-07-22 ~23:30) — תיקון-זיהום → S1=מוח / S2+S4=זרועות

**פסיקה חד-פעמית-וקבועה (נוהל-הקבע):** הרצף למטה מאושר. כל שלב: בנייה→אימות-סים→cowork מאמת→הבא.
אפס-בנייה עד ששלב-קדם ירוק. cc-macbook מבצע backend; cursor מאמת+פרונט; cowork מאמת-סימטרי+מדליק-בסים.

## 🔴 תיקון לתמונה (cowork מדד ~23:25, מחליף הנחה בדוח-cursor)
- **ה-VA=3.5pt לא מהברים המזוהמים.** ייצוא-Sierra הגולמי `tpo.json` **בריא**: VAH 7555.0/VAL 7535.5 = **19.5pt,
  `va_ok:True`**. שורת-ה-DB `v9_tpo_sessions` id 1523 = 3.5pt (7561.75/7558.25) — **לא תואם את Sierra.**
  ⇒ **שתי תקלות-נפרדות:** (1) ברים כפולים +1h (ts-offset בקליטה). (2) TPO/VA שגוי ב-DB (קליטת-TPO, לא ברים).
  **מסקנה שמתקנת את cursor:** "rebuild TPO מהברים" **שגוי** — ה-TPO לא נגזר מהברים אלא מייצוא-Sierra-הבריא.

## הרצף (מתוקן)
**שלב 0 — חוסם-על:** cc סוגר 12 הרגרסיות → parent-parity 145/145 בחבילה + מריץ migration 023. (cowork מאמת.)

**שלב 1 — תיקון-TPO/VA (מהיר, נפרד, בסים):** מצא למה `v9_tpo_sessions` id 1523 = 3.5pt בעוד Sierra = 19.5pt
(שורה-מעופשת / קליטה-מ-mapped). תקן קליטה/רענון → ה-VA ב-DB = Sierra. **קבלה:** DB VA == export VA (±tick).
זה לבדו מחזיר רמות-מיקום אמיתיות — לא תלוי בבר-contamination.

**שלב 2 — תיקון-offset הברים (השורש):** ts-הייצוא הגולמי ‎−5h → יישר את הקליטה/bridge שבר-טרי ינחת ב-ts-נכון
(≈0), **ופרוש את `WOODIES_TS_HOUR_FIX` לגמרי** (=0 לבד משאיר ‎−1h — לא מספיק). ואז נקה 12+12 רפאים (גיבוי;
ודא בר-אמיתי בסלוט). **קבלה בסים:** בר-טרי diff<בר · 0 זוגות-חדשים · ATR/day-type סבירים.

**שלב 3 — זרוע ראשונה (הכי-קטנה שסוגרת את הכאב):** זיהוי-reactive-מודע-מיקום ב-S2 — מזהה fade **רק** ליד
VAH/VAL המתאים לסוג-היום (אחרי probe), לא "בכל-מקום-ואז-reject". דגל-OFF+טסט+סים. **קבלה:** setup נפלט רק בקצה;
`day_type_at_fire` מלא.

**שלב 4 — הרחבה:** S4-continuation-עם-הכיוון באזור-פולבק · ואז opening-driven (S1 opening_type מניע זרוע).
opening→**נשאר shadow** עד הוכחה-קדימה (מחקר-31-סשן). זרוע-אחת-בכל-פעם.

## ⚠️ אזהרת-ארכיטקטורה (חובה, לא-להשמיט)
גיוט-זיהוי-על-S1 = S1-תקוע/שגוי מעוור את הזרועות לגמרי (קרה 3× היום → אפס-זיהוי → אין-מה-לאבחן). לכן שלב-3/4
**חייבים** להיות צמודים ל: (א) `DAYTYPE_ACCEPTANCE_DEMOTION` + `BOOT_SEED_CANONICAL` של S1 חיים ומאומתים,
(ב) נתיב **fail-open/observability**: הגלאי מזהה-ומתייג גם מחוץ-להקשר (לצל בלבד) → לעולם לא עיוורון-מלא.

## חזרה-ללייב
כל השלבים ירוק בסים → חזרה מיידית (מייקל פסק: בלי-soak). התנאי היחיד = ירוק-מלא + אימות-cowork פר-שלב.
