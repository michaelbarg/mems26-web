# תוכנית-פיתוח 02.08 — לקראת מסחר מחר (פסיקות-מייקל, בוקר-ראשון)

> הבסיס: ‏DALTON_GAP_AUDIT_2026-08-02.md (ביקורת-סוכן על 13 ימי-מסחר, כל מספר עם
> שאילתה) + מחקר-חוץ (14 מועמדי-חוקים עם מקורות: דלתון, ‏Sweeney, ‏Kaminski&Lo,
> פרקטיקני-order-flow). ‏cowork מתאם ומאמת · ‏cc מבצע · מייקל פוסק-ומדליק.

## התשובה לשאלת-היסוד "למה לא היו עסקאות איכותיות" — במספר אחד

‏93% מההפסד (‏−$880 על 31 עסקאות) מוסבר בשתי מחלות: **רדיפת-קצה** (8 עסקאות שנכנסו
בדיוק במיצוי — ‏MFE≤2 נק' — ‏−$631) ו**מכירה-נגד-ערך-נודד**. ומצד-ההחמצה: פער-הזיהוי
הגדול ביותר (‏≈$1,550) הוא **תבנית שאין לנו** — פייד-קצוות לימי-איזון. כל תבניות-המערכת
הן מחלקת-המשך/מגמה, בעוד רוב הימים (‏42%+ ‏Normal-Variation, ‏Neutral נפוץ) הם ימי-איזון
שהמשחק בהם הפוך. לא חסמים — חוסר-כלי.

## P1 · תבנית EDGE_FADE (סוגר את פער-ה-$1,550) — cc, היום

‏RESPONSIVE_EDGE_FADE: בימי ‏Normal/Neutral/NV-רוטציוני — דחיית-קצה (‏IB-edge/קצה-יום/VA-edge)
עם בר-דחייה ⇒ כניסה ריספונסיבית, יעד ‏POC/אמצע, סטופ מעבר-לקצה (מוגבל-תקרה).
אישור-order-flow כשזמין (‏P2): דיברגנציית-דלתא בקצה (‏R3) או ספיגה (‏R4) מעלים דרגה.
מקרי-בוחן: ‏15/17/27/31.07 (ארבעת ימי-הדחייה מהביקורת). ‏flag-OFF ⇒ ‏replay ⇒ מייקל.

## P2 · תחיית-Footprint שלב-1 (פסיקת-מייקל: "להוסיף את פוטפרינט שחסר") — cc

**פסיקת-הדחייה של S3 בוטלה בהוראת-מייקל 02.08.** שלב-1 לא דורש ‏footprint-מלא —
הייצוא כבר זורם: ‏cumulative_delta.json · ‏imbalance_flags.json · ‏stacked_imbalances.json.
1. ‏`delta_confirms_extension` (‏R5) — פיצ'ר למסווג-היום: הרחבה-עם-דלתא=אמיתית,
   הרחבה-בלי-דלתא=מועמדת-Neutral/פייד. מדייק בדיוק את נקודת-הכשל של סיווג-מחיר-בלבד.
2. ‏`cvd_directionality` (‏R6) — מוביל-סיווג ב-30-60 הדקות הראשונות.
3. אזורי-‏stacked-imbalance (‏R1/R2, סף 300% לייב) כרמות-S/R חיות ל-location_gate
   ול-EDGE_FADE (האות = הריטסט, לא ההיווצרות).
4. דיברגנציית-דלתא + ספיגה בקצוות (‏R3/R4) — וטו-מרדף + אישור-פייד.
‏SoT: ערכי-DLL בלבד, שום סינתזה (חוק-1). כל פיצ'ר ‏flag-OFF עד replay.

## P3 · מערכת-6 חכמה (פסיקת-מייקל: "לתקן עסקה לא-טובה") — cc

בתוך המגבלות הקיימות (‏MODIFY_STOP + FLATTEN; ‏scratch-מלא=FLATTEN; ‏op=EXIT אסור עד v2):
1. **‏MAE-scratch (‏R13, ‏Sweeney):** כיול על v9_trades+צל — גבול-MAE אמפירי פר-תבנית;
   חצייה לפני-התקדמות ⇒ ‏scratch. הנתון מהביקורת: 8 עסקאות-המיצוי היו נחתכות ב-BE±.
2. **‏Time-stop מפוצל (‏R14, ‏Kaminski&Lo):** ‏initiative שלא-עבד תוך N ברים ⇒ ‏scratch;
   ‏responsive מקבל סבלנות + סטופ-מבני (המחקר: יציאה-מוקדמת מזיקה ל-mean-reversion).
3. **‏Failed-breakout scratch (‏R5-נגזרת):** נכנסת-על-הרחבה + חזרה-לטווח + דלתא-התהפכה
   ⇒ יציאה מיידית, לא המתנה-לסטופ.
4. **ביטול-מבנה:** הרמה שבגללה נכנסנו נחתכה-בנקיון ⇒ יציאה על-האישור.

## P4 · תיקוני-ניהול מהביקורת — cc

‏(א) סטופ-מינימום מותאם-יום: ביום-IB-40-90 סטופ 1.75-6 נק' = רעש (‏#579 נעצר ‎−$45
ואז ‏MFE 43.5) — רצפת-סטופ נגזרת-IB/ATR. ‏(ב) ‏re-eval-ערבי של סוג-יום (‏31.07 הרחיב
ב-20:45 אחרי-שהסקירה ננעלה). ‏(ג) בנקאות-ראנרים: 5 מנצחות עם ‏125 נק' ‏MFE בנקו $102 —
מדרגות-‏R9 (ריספונסיב=מימוש-מלא ב-80%-extension; איניציאטיב=50%+ראנר). ‏(ד) רעש-שידור-חוזר
ביומן-ההחלטות (‏ZLR@7589.5 עשרות-פעמים) — דדופ. ‏(ה) חקירת 07-16 (יום-Trend נקי, אפס-רישום).

## P5 · מעבר-מסחר למחשב-השני (פסיקת-מייקל: "המחשב הזה = פיתוח")

מסמכי-הבסיס קיימים (‏SECOND_MACHINE_*). שער-GO קשיח לפני העברה:
‏(1) ‏clone עדכני + ‏.env מלא + ‏DLL פרוס ‏== ‏repo · ‏(2) פיד-סיירה חי במכונה-השנייה
(‏Chicago-TZ! הלקח) · ‏(3) ‏E2E רמה-B בסים שם · ‏(4) ‏flag_guard ירוק שם · ‏(5) ‏cutover
בפועל. **המלצת-cowork:** מחר לסחור עוד מהמק-הזה (יציב, כל התיקונים חיים) ולהעביר
באמצע-שבוע אחרי יום-ירוק במקביל-סים במכונה-השנייה — החלפת-מכונה ביום-של-תבניות-חדשות
= שני-סיכונים-בבת-אחת. מייקל פוסק. נדרשות ידי-מייקל: התקנת/כניסת-סיירה שם.

## P6 · סדר-עדיפויות-ביצוע (היום ומחר-בוקר)

‏cc היום: ‏P1 (EDGE_FADE) ⇒ ‏P2.1-2 (דלתא-למסווג) ⇒ ‏P3.1-2 (MAE+time-stop, כיול-בלבד)
⇒ ‏P4.א,ג. ‏cc-לילה: ‏P2.3-4, ‏P3.3-4, ‏P4.ב,ד,ה + ה-NOT-DONE הקיים (‏scid/רמה-D,
סדר-פעולות-יעדים, תזמון-נפח-release, ‏LEG_RIDE→MarketContext).
‏cursor מחר-בוקר: ביקורת-עצמאית על כל מה שנבנה + ‏GO/NO-GO פר-דגל.
‏cowork: אימות-חוק-5 על הכל + הדלקות-בפסיקה + הכנת-בוקר מחר (פיד/ברים/flag_guard).
**מחר נפתח עם:** מסווג-מתוקן + רגל + תקרות + first-trade-strict (כבר חיים) ± מה
שמייקל ידליק מה-P-ים אחרי replay. יעד-השבוע: יום ירוק ⇒ רצף-ירוקים ⇒ ‏cutover.
