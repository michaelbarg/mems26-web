# אל cc-מק-2 — עבודת-לילה 13→14.08: הוכחת-ירי מלאה + פערים

**רקע (מייקל, 23:50):** "המסחר היה צריך לבצע עסקת לונג ואז שורט — מק-2 לא זיהתה אף אחת
מהן… אני רוצה בדיקה ואישור שהמערכת יודעת לירות במק-2, לאפשר הרחבת-עסקה וניהול-עסקה,
ולדעת אם יש פערים. מחר חייב להיות מסחר על כסף אמיתי."

**מצב נוכחי (אימות-cw מרחוק, 23:45):** `mems26_arming_gate.py --host 10.1.118.70` →
**✅ ALL SYSTEMS ARMED** (S1/S2/S3/S4). ארבעת השורשים של היום תוקנו. מה שחסר עכשיו
הוא **הוכחה** שהשרשרת המלאה עובדת אצלך — לא עוד "ירוק" של שירותים.

---

## 1. הוכחת-זיהוי על הטייפ של היום (חובה, ראשון)
הרץ replay של ברי-היום (RTH 09:30-16:00 ET, מה-DB שלך) דרך **המנועים שלך** ואמת ששני
המקרים של מייקל מיוצרים כמועמדים:
- **הלונג:** ‏16:55 IL (09:55 ET) @~7816 — במק-1 זה נורה כ-`INITIATIVE_LONG` (טרייד #660,
  ‎+$12.50 ואז ילד-חיזוק #661 ‎+$46.25).
- **השורט:** ‏18:04 IL @~7818 `GB100 SHORT` + ‏18:25 @7812 `ZLR SHORT` (שניהם נחסמו היום
  ב-opening_type_gate שכבוי מאז).
**הפלט הנדרש:** לכל אחד — האם המנוע מייצר setup, ואיזה שער (אם בכלל) חוסם אותו **עכשיו**
בקונפיג הנוכחי. אם משהו עדיין חוסם — זה הפער, דווח אותו עם שם-השער והסיבה. אל תשנה דגלים.

## 2. הוכחת-ביצוע: PLACE → ניהול → חיזוק (sim-drill)
מייקל דורש אישור שהמערכת יודעת **לירות, להרחיב ולנהל** אצלך. עשה drill שלא נוגע בכסף:
`scripts/fire_drill.py` בלבד לא מספיק (הוא בודק מסלול, לא ביצוע). בצע:
- PLACE-drill דרך תור-הפקודות עם `is_sim` של סיירה (או `MEMS26_MODE=demo` זמנית **רק אם
  אתה flat ומחוץ לשעות**) — ואמת ACK תוך <2ש ואפס `ORDER_FAILED:-1`.
- **חיזוק:** אמת ש-`SCALE_IN_V1=1` אצלך (flag_guard) ושה-child-PLACE נבנה נכון: +2 חוזים,
  סטופ ב-BE-של-ההורה, לא-נוגע-בכניסה. הדבק את שורות-הלוג.
- **ניהול:** אמת ש-S6 (trade supervisor) רץ אצלך — לופ פעיל, `v9_exit_decisions` מקבל שורות
  על עסקה פתוחה (או הוכח שהלופ חי בלוג). זו הדרישה "ניהול-עסקה".
**החזר למצב לייב בסוף (`MEMS26_MODE=live`) ואמת `is_sim=0` + arming gate ירוק.**

## 3. פערים — רשימה כתובה
לאחר 1+2, כתוב בערוץ **טבלת-פערים של מק-2**: מה עובד, מה חסר, ומה דורש פסיקת-מייקל.
כלול לפחות: PG-over-ZT (עדיין חסום — דיאלוג Postgres.app), צ'ארטבוק/TPO מול מק-1
(ה-IB היה חסר עד 18:45 — האם עוד משהו בצ'ארט שונה?), ‏S3/footprint (0 pushes בשתי המכונות),
וכל דבר שראית היום ולא דיווחת.

## 4. שער-הבוקר (חובה לפני 16:30)
`git pull` → `python3 scripts/mems26_arming_gate.py` → **חייב `✅ ALL SYSTEMS ARMED`**.
אם לא — תקן לפני הפתיחה ודווח. בלי הפלט הזה מק-2 לא נכנס לשוק.

## 5. דיווח
רשומה חתומה אחת ב-`LIVE_CHANNEL.md` עם כל הפלטים הגולמיים + commit+push, ופוש
`[מק-2]` למייקל עם השורה התחתונה. **הכלל: פלט גולמי, לא "בוצע".**
