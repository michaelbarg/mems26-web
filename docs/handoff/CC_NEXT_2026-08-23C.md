# המשך C — דלתון V2 **מעל הגלאים הקיימים** (לא מעל סווינגים)
**פסיקת-מייקל 23.08: "כן".** §0 חל. **קריאה-בלבד** בשלב הזה — סימולציה, לא קוד-ייצור.
קודם: `git add -A && commit && push` את מה שכבר בעץ (classifier_core, replay_dalton, test_binary_convergence).

## מה למדנו (מהדוח שלך עצמו)
- V2 עם שלושת האלמנטים: **התכנסות 30% → 90%** ✓ · L2 ‎−$868 → ‎−$478.
- המסננת חותכת **90%** מהפסד-הירי-הגולמי (‎−$4,707 → ‎−$478).
- **המשפט המכריע בדוח:** *"ערכו של דלתון הוא ב-SELECTION (איפה להסתכל), לא ב-ENTRY
  (איך להיכנס)"* — הוא הורץ עם טריגרי-סווינג גולמיים שהם ~40% הצלחה בלבד.

## המשימה — צירוף נכון
הרץ את **דלתון-V2 כשכבת-בחירה מעל הגלאים האמיתיים** במקום מעל סווינגים:
1. **בחירה (דלתון V2):** מצב BALANCE/DISCOVERY + מיקום מול ערך/קצה + סוג-יום בינארי
   ⇒ מגדיר את **התזה המותרת** באותו רגע (כיוון + סוג-עסקה + "לא סוחרים").
2. **כניסה (הגלאים הקיימים):** רק תבנית שמתאימה לתזה מבצעת —
   BALANCE+קצה-תחתון ⇒ `REACTIVE_LONG` / `DOUBLE_BOTTOM_EE` / דחיית-קיצון ·
   BALANCE+קצה-עליון ⇒ `REACTIVE_SHORT` / `DOUBLE_TOP_AA` (אחרי תיקון-Adam) ·
   DISCOVERY ⇒ `INITIATIVE` / `GB100` / מדרגה — **עם** הכיוון בלבד ·
   אמצע-הערך ⇒ אף אחת.
   הכניסה/סטופ/יעדים לפי **מכניקת-הגלאי הקיימת** (זו שיש לה מכניקה טובה), לא סווינג גולמי.
3. **ניהול לפי משטר:** BALANCE ⇒ מימוש-רוטציה ליעד-הערך הנגדי · DISCOVERY ⇒ החזקה
   + P3 הגדלה. מעבר-משטר מעדכן עסקה פתוחה.

## ההרצה
34 סשני-הלייב **וגם** תת-החתך 08-10..08-21; 4 ו-6 חוזים; 3 רמות סליפג'; עמלות;
**סלוט-חי אחד**. ארבע שכבות להשוואה: בפועל · הגלאים-בלי-דלתון · **הגלאים+דלתון-V2** ·
אורקל. פר-סוג-יום ופר-תבנית: כמה עסקאות, שיעור-הצלחה, ‎$, חציון-יום, IS/OOS.

## שאלות שהדוח חייב לענות
1. האם דלתון **מעל הגלאים** חיובי? בכמה, ובאיזה חתך.
2. אילו צירופי **מיקום×תבנית** הם המרוויחים — טבלה מדורגת. זו הרשימה שנבנה ממנה.
3. כמה עסקאות ליום זה נותן (מייקל מצפה ל-2-3 ממוקדות, לא 30 מפוזרות).
4. מה קורה לשיעור-ההצלחה מול היום (53%).
5. אם עדיין שלילי — **לומר זאת מפורשות** ולציין מה החוליה החלשה. תוצאה תקפה.

→ `docs/reports/SIM_DALTON_OVER_DETECTORS.md` · commit+push · שורה ב-LIVE_CHANNEL.
**לא לגעת בקוד-ייצור, לא ב-.env, לא בריסטארט.** יום שני נשאר כפי שהוגדר ב-`CC_NEXT_2026-08-23B.md`.
