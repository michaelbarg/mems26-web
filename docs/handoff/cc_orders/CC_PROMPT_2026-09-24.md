# פסקת-פתיחה ל-cc-macbook — 24.09.2026 (cowork-dev, 08:55 IL) — גרסה 3, ענפי-יום-וריאציה (T-458)

> להדביק כמו שהיא בסשן Claude Code על ה-MacBook (`claude --dangerously-skip-permissions`). מחליפה את גרסה 2 מ-23.09 רק לגבי סדר-העבודה; הכללים זהים.

אתה cc-macbook על ריפו MEMS26 (`/Users/michael/Downloads/mems26_web_git`, המכונה שסוחרת — חשבון לייב, חוזה 1). עבודה בלי שאלות (פסיקת-מייקל 14.09): מחליטים, מתעדים וממשיכים; דו-משמעות ⇒ הפרשנות ה-golden שבהזמנה + שורת "החלטתי X כי…"; לא סיימת ⇒ NOT-DONE מפורש והפריט הבא. התחל כך: `git -C /Users/michael/Downloads/mems26_web_git pull` (לעולם לא `cd … && git …`), קרא `CLAUDE.md`, את הרשומה העליונה ב-`docs/handoff/LIVE_CHANNEL.md` (cowork-dev 24.09 08:50 — ענף-דרייב-הפתיחה `OPENING_DRIVE_BRANCH_V1` נבנה ונמדד, דגל כבוי ממתין לפסיקה; המספרים והראיות שם), את `docs/reports/VARIATION_PLAYBOOK_2026-09-24.md` ואת שורת T-458 ב-`docs/plans/TASK_LOG.md`. **הזמנה: T-458, ארבעה פריטים, פריט אחד בכל פעם, קומיט לכל פריט, מספר לפני דגל (דוקטרינת-הלמידה 09.09).** **(ג) קודם — הרנס-אישור של ענף-הדרייב:** על 56 הסשנים הנקיים של מחקר T-426 (15.06–18.09 בלי EXC — הרשימה והבחירה ב-`scripts/open_drive_branch_study.py` + `docs/reports/OPEN_DRIVE_BRANCH_2026-09-20.md`) הרץ `scripts/fwd_harness.py --session D --out harness_out/t458/branch{0,1}_D.json` פעמיים לכל סשן — `OPENING_DRIVE_BRANCH_V1=0` ו-`=1` (משתני-סביבה בשורת-הפקודה דורסים את `.env`), ובנה טבלה: דרייב-מאושר זוהה / נחסם-ב-ELQ / עבר / t1 לפני-ואחרי / win% / Σ$ במודל-ההערכה הקבוע (סטופ מבני, מגע-ראשון על 5-דק׳, EOD, 5$/נק׳, 1.30$/צד) — golden: אותו מודל כמו `scripts/variation_playbook_test.py::simulate`. זה המספר שמכריע את פסיקת-מייקל; רשום אותו ב-LIVE_CHANNEL עם הפלט הגולמי. **(א) `VAR_CONT` — מפיק-צל של התיקון-העצמי:** ביום וריאציה (או `day_dir_so_far` עם דרייב-בוקר באותו כיוון), שלב C/D, כניסה-עם-כיוון-הרגל אחרי חזרה: התנאים בדיוק כמו `CONT_1S` ב-`scripts/variation_playbook_test.py` (`one_sided` = הרחבת-IB בכיוון בלי הרחבה בצד השני, `pullback_before`, `trigger_ok`, `range_ge_08atr` — ה-features מ-`scripts/review_lib.py::features`), סטופ = קיצון 4 הברים האחרונים + טיק (תקרה 1.5×ATR, מינימום 1 נק׳), T1 = 1.5R. **`shadow_only=True` בלבד — בלי דגל-לייב, בלי `_emit_exit`, בלי פקודות**; מפיק בגייטוויי/עץ (לא בדטקטור), שורת-v2 `t457_cont_pullback_with_extension` ב-`config/dalton_tree_v2_draft.yaml` היא הבית שלו; טסט-רגרסיה על 23.09 20:20/21:00 (שתי הכניסות מהפלייבוק). **(ב) שער `BREAK`:** פריצת-טווח על סוג-יום שאינו טרנד מפסידה (N=142) — כלל-גייטוויי דרך שורת-v2 `t457_break_blocked_off_trend` (צל, TREE_SHADOW diff), לא שינוי-דטקטור. **(ד) ריפליי-יציאות:** `TARGET_REALISM_V1` מול T1-מבני מול time-stop (bar+6, MFE<0.5×ATR) על עסקאות-ההמשך בלבד, לפי סוג-יום, על 60 הסשנים — טבלה, בלי דגל. **כללי-ברזל:** הרנסים רק מחוץ ל-RTH (לא 16:30–23:00), ≤2 במקביל, `SITUATION_VECTOR_LOG_V1=0`; אין ריסטארט, אין `.env`, אין `RISK_*`, אין נגיעה בפוזיציות; דגל-לייב חדש = פסיקה בכתב של מייקל בלבד. כל פריט: קומיט אחד + TASK_LOG (T-458 "הצעד הבא") + STATUS_BOARD באותו קומיט + LOG חתום ב-LIVE_CHANNEL עם פלט גולמי (Rule 5), ואז "סיים פריט N" ולעצור עד הפריט הבא מ-cowork.
