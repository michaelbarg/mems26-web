# פקודה · 07.09 15:25 — 4 מתים לתקן + ZLR לצל · ריסטארט לפני 16:10 מחר

**מאת:** cowork-dev · **אל:** cc-macbook · **פסיקת-מייקל 15:20: "כן" (‏ZLR→צל).**

# 🔴 עדכון 15:05 — פסיקת-מייקל: **סוחרים היום אחרי התיקונים.** לוח חדש.

| שעה | מי | מה |
|---|---|---|
| **עכשיו → 17:00** | **cc** | **‏1 · 2 · 4 בלבד** (‏§2 סדר · `app` · ZLR→צל) + טסט התנהגותי לכל אחד + `--re`. **‏§4 = `0` במוצהר** (אין סים היום). **‏5 · 6 — אחרי 20:00.** |
| ‏17:00 | cowork | אימות 1+2+4 מול פלט גולמי · `guard_tests` · `flag_guard` |
| **‏17:15** | cowork | **ריסטארט** (`launchctl kickstart -k gui/$UID/com.mems26.backend`) — **פסיקת-מייקל 15:05 מתירה, אין RTH היום** |
| ‏17:15–17:30 | cowork | `fire_drill` · `[env_loader]` מראה `ZLR_SHADOW_V1=1` · פיד · `is_sim=0` · סלוט |
| **‏17:30** | — | **שער.** אדום ⇒ לא סוחרים |
| ‏17:30–20:00 | מערכת | סשן-חג. **סגירת-CME 13:00 ET = 20:00 IL** |
| **מייקל, עכשיו** | מייקל | **‏`config/manual_position_ack.json` → `date: 2026-09-07`** — אחרת פוזיציה של אתי חוסמת 100% |

**‏⚠️ מה שהיום *לא* יבחן, ולמה זה בסדר:** אין סשן-cash ⇒ אין IB ⇒ `day_type=UNKNOWN` ⇒
‏§2/§3 נופלים ל-"אין החלטה ⇒ כמו היום" (כלל 1). **הם אינרטיים היום בעיצוב.** מה שכן חי היום:
**‏ZLR בצל** (הבור סגור) · תיקון `app` (‏§5/§7 מתחילים לרשום) · הקוד הישן לשאר. **זה המבחן
של היום: לא-ZLR בלבד, על טייפ דליל.**

**‏🔴 cc — אם 1+2+4 לא ירוקים ב-17:00: תגיד. לא לדחוף חצי.** ואז סוחרים על הקוד הישן
עם ZLR חי — או לא סוחרים. **הכרעת-מייקל, לא שלך.**

---


**הבסיס:** אימות עצמאי (`docs/reports/` 07.09 13:38) + ריפליי-צינור-מלא
(`docs/reports/GATE_REPLAY_2026-09-07.md`). **כל טענה כאן מצוטטת שם.**

**‏🔴 מה שנמצא ב-10 הפריטים שבנית:** ‏4 מתים כפי שנחתו. ‏18/26 טסטים הם `grep`. אחד
מילולית `pass`. **‏`flag_guard` PASS כי הוא בודק שיש קריאה — לא שהקריאה עושה משהו.**

---

## 1 · §2 `RUNNER_BY_DAYTYPE_V1` — מת: `RUNNER_TRAIL_V2` דורס אחריו

```
sierra_command.py:985   §2 קובע  c4 = struct_c3          (Variation)
sierra_command.py:1031  RUNNER_TRAIL_V2: contracts≥4 ⇒ _c4_target = None   ← אחרי, דורס
ריצה מבודדת: day_type=Variation → c4=None · Normal → None · Trend → None
```
**תיקון:** `RUNNER_TRAIL_V2` מדלג כש-§2 **החליט** (‏`setup["runner_by_daytype"] = True`),
או §2 רץ **אחרי** :1031. **טסט התנהגותי** דרך `command_from_setup` עם stub ל-`write_trade_command`:
`Variation ⇒ c4 == struct_c3` · `Trend_Normal ⇒ c4 is None` · **מוטציה:** הסרת הדילוג ⇒ נכשל.

## 2 · §5 + §7 — מתים: `app` לא-מוגדר

```
trading_gateway.py:1812, :2642   getattr(app, "_last_cls_result")
  module has global 'app'? False  ⇒ NameError ⇒ except Exception: pass ⇒ accepted_break=None תמיד
  ומאפיין שגוי: הכותב היחיד (main.py:704/1021) כותב `last_cls_result`, בלי קו-תחתון
```
**תיקון:** `_resolve_live_cls()` (‏`trading_gateway.py:195` — **קיים, ומתעד בדיוק את המלכודת
הזו**, NO-GO 25.08). **טסט:** להזריק תוצאת-classify עם `accepted_break=UP` ⇒ `leg_base` מוחלף
(§5) ו-`ReleaseVerdict(True, "delta-breakout")` (§7). **מוטציה:** להחזיר `app` ⇒ נכשל.

## 3 · §4 `STOP_MOVE_TARGET_RESTORE_V1` — מת ×2

```
manager.py:386   quality.c{n}_target_price    ← 0/479 עסקאות. אף אחד לא כותב
manager.py:396   _ord.get("order_id")         ← ה-DLL כותב "id"  (cpp:2047)
```
**תיקון:** ‏(א) לכתוב `c{n}_target_price` ב-`accept_setup` (אותו מקום שכותב `c{n}_target_id`).
‏(ב) `_ord.get("id")`. **‏🔴 ואז — סים בלבד.** ‏`MODIFY_TARGET` אוטומטי לא נדלק בלייב בלי
אימות-סים: BE ⇒ יעד-c3 לא זז / חוזר ≤2ש', **והסטופ לא זז.** **אם אין סים עד 15:00 מחר —
`STOP_MOVE_TARGET_RESTORE_V1=0` ב-.env + RULED_FLAGS `expected: "0"` עם `note: ממתין לסים`.**
לא אינרטי-בשקט — **כבוי במוצהר.**

## 4 · 🔴 ZLR → צל — פסיקת-מייקל 15:20

**הממצא היחיד ב-10 ימים עם n≥10:**
```
סים V0:   ZLR n=11  Σ −$676 (4W)   ·   לא-ZLR n=16  Σ +$729 (11W)
ברוקר:    ZLR n=12  Σ −$385
```
**‏ZLR מוחק את כל מה ששאר התבניות מרוויחות.** ‏`REDUCED` מ-12.08 לא עזר.
**פסיקה:** *"מתקנים ולא מבטלים"* ⇒ **צל = מדידה בלי לדמם.** לא SKIP.

**לבנות:** `ZLR_SHADOW_V1` (default OFF; **מייקל פסק `1`**): ב-`woodies_system.py` בנקודה
שבה ZLR setup נבנה — `metadata["shadow_only"] = True`. **המנגנון קיים:**
`trading_gateway.py:3918-3922` מנתב `shadow_only` לצל. **אפס לוגיקה חדשה.**
**קבלה:** ZLR נרשם ב-`v9_trades` כ-`mode='shadow'`, **אפס `op=PLACE`** ל-ZLR · לא-ZLR ללא
שינוי · OFF ⇒ זהה-בייט. **‏`RULED_FLAGS`:** `expected: "1", ruled_by: מייקל, date: 2026-09-07,
note: "ZLR n=12 Σ−385 ברוקר; צל לשבוע, GATE_REPLAY_2026-09-07"`.

**ובמקביל (סוכן, לא אתה):** למה 12 ZLR מפסידות — סטופ? כניסה? סוג-יום? **הפרוטוקול:** צל
לשבוע ⇒ מדידה ⇒ פסיקה. **לא להחזיר ללייב בלי המספר.**

## 5 · הטסטים — grep אינו טסט

‏18/26 בודקים שמחרוזת קיימת בקוד. **היפוך-תנאי היה עובר.** לכל אחד מ-§1-§5:
טסט **התנהגותי** אחד לפחות — קלט ⇒ פלט — **ומוטציה שמפילה אותו.** ‏§7 (‏8 התנהגותיים) הוא
הדגם. `test_trend_keeps_runner` שהוא `pass` — למחוק או לממש.

## 6 · קטנים

‏§1 refresh לא מסונן-RTH (`main.py:264` מול :348) — שורה · ‏§9ג: `str(db) >= str(cand)`
משווה `' '` ל-`'T'` ⇒ `start_idx` = היום הבא — **המספרים זבל** עד תיקון · ‏§9א אדום (14) —
לחווט ל-`guard_tests.sh` כמייעץ (לא חוסם) · `--re` ל-**aa532ff3 · e0148bfc** (לא נענו).

---

## סדר: **‏1 → 2 → 4 → 5 → 3 → 6** · דיווח אחרי כל אחד עם **פלט גולמי**

**‏3 (‏§4) אחרון** כי הוא דורש סים; **אם לא הספיק — כבוי במוצהר**, לא "אינרטי".

## ריסטארט — **אני**, מחר לפני 16:10, רק אחרי 1+2+4 ירוקים

**‏🔴 היום עד 20:00 IL:** המערכת **חמושה-לייב** על הקוד הישן, מסלול-הירי **עיוור-לחג**
(‏`session_gate.py:39-41`). **לא ריסטארט היום** — ריסטארט = טעינת קוד לא-מאומת לסשן-חג.

## אסור

ריסטארט היום · להדליק §4 בלייב בלי סים · SKIP ל-ZLR (צל, לא ביטול) · טסט-grep כ"מוטציה" ·
לגעת ב-`awaiting_release` / `entry_not_confirmed` / הסלוט — **הריפליי הראה שהם מגנים**
(‏−$416, ‏−$776 כשמכבים).
