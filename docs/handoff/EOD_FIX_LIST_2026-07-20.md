# רשימת-תיקונים לסוף-המסחר — 2026-07-20 (יום-לייב)

כל הפריטים עלו/הוכחו חי היום. סדר לפי עדיפות. סים-אימות רק אחרי סגירה (is_sim=1) או iMac.

## P0 — יסוד (בלי זה אי-אפשר לבטוח במספרים / בטיחות)

**1. רקונסיליאציה מול סיירה (Task #6).** `trade_fills.json` **ריק** (size=0) — אין מילויי-אמת. `v9_trades.pnl_usd` **מחושב** ולא אמיתי (#420 נרשם −$82.50, במציאות ~$15). `backend_trade_id` לא מקושר, אין טבלת-מילויים.
→ לתפוס כל מילוי (כניסה/סטופ/יציאה) עם מחיר+qty+order-id, לקשר ל-backend_trade_id, לרשום P&L אמיתי + Divergence כשמחושב≠אמת. **עד שזה עובד — כל סטופ/P&L במערכת לא-מאומת.**

**2. סטופ בקצה-המבנה, כל התבניות (Task #7).** מוכח: #420 סטופ 7514 היה **7–13 נק' בתוך המבנה** (שיא 7521–7527; בר-הכניסה עצמו נגע 7521.25) → נתפס ב-8 שניות. `structural_stop_always_wins` **לא נאכף ב-S2** (הסטופ = כניסה+רצפת-ATR, לא השיא).
→ הסטופ **מעבר לקצה-המבנה + 6T** (שורט=מעל השיא, לונג=מתחת לשפל); המבנה **גובר** על רצפת-ATR כשהוא רחב יותר; פר-תבנית (S2+S4). טסט דטרמיניסטי עם ברי-#420. (הבנד-אייד 8pt כבר הוחזר.)

## P1 — נכונות (ירי + סוג-יום)

**3. מקור-אחד לסוג-יום + כיול-S1 (Task #5).** classify_replay="Normal" (כפוי-EOD) ≠ מכונה ≠ get_live=Variation(override) — הדאשבורד מפוצל. והמסווג **פספס פריצת-IB מטה** (5 נק' מ-IB של 46 → "0-sided/Normal"; לפי דלתון = Variation).
→ (א) מקור-אחד קנוני למנוע **וגם** לכל פאנלי-הדאשבורד. (ב) **כיול-סף-ההרחבה** — כל קבלה מעבר ל-IB = הרחבה (דלתון), לא רק פריצה גדולה. טסט: UI==מנוע==שער.

**4. גיוס-היסטוריה בבוט (Task #8).** ריסטארט מאפס את buffer-S2 → S2 עיוור ~15-20 דק' (זו הסיבה שלא ירה אחרי-הצהריים). BOOT_DAYTYPE_REPLAY משחזר סוג-יום אבל **לא את ה-buffer**.
→ בבוט לשחזר את ברי-ה-RTH דרך process_bar (בלי persist/orders) → S2 מוכן-מיד. + לתקן תצוגת-cooldown ("active" בזמן שלא-חוסם).

**5. ירי לאורך כל השעה-הראשונה אחרי סיווג-פתיחה (Task #3).** OPENING_WINDOW_FIRE רק 09:30–10:00 + **דוחה test-drive** (drive_failed=חזר דרך הפתיחה) — בדיוק סוג-הפתיחה של היום, שסווג ב-10:00 כשהחלון נסגר → אזור-מת 10:00→נעילת-IB.
→ לירות לפי סוג-הפתיחה המסווג לכל השעה-הראשונה; לקבל test-drive ככיווני; לסגור את אזור-המת.

## P2 — שיפורים

**6. ניהול-עסקה לפי סוג-פתיחה (Task #4).** סטופ/יעד/BE/טרייל מוגדרים פר-סוג-פתיחה (test-drive=הדוק יותר וכו'), config-tunable.

**7. פאנל דאשבורד: סוג-פתיחה + התבניות שיורות בו (Task #2).** סיווג חי + playbook-פר-סוג + מה-ירה-ולמה.

## החלטות / הדלקות-מחדש (אחרי הנ"ל)

- **G2/G3/G6** — הוחזרו OFF היום (כולם null-block על get_live לפני-נעילה). להדליק **רק אחרי #5** (כשget_live אמין).
- **override-Variation** — אוטו-פג בסוף-היום ET; אחרי #5 המסווג יזהה לבד.
- **ORPHAN auto (מבני+$200)** — עדיין OFF; דורש re-sim + פסיקת-הדלקה.

## סים-אימות דחוי (is_sim=1 אחרי-סגירה / iMac)

- T17 ירי-4-חוזים E2E · ORPHAN re-sim · STOP_WIDEN.
- מיון 92 טסטי-רגרסיה נכשלים (cc: מיושן-פסוק מול באג-אמת).

## למה לא היו עסקאות היום — מפל-החוסמים (כרונולוגי)

1. **09:30–10:45:** ירו 2 בלבד — #420 (S2 REACTIVE_SHORT 10:25) + #424 (S4 ZLR 10:45). **שניהם הפסידו על סטופ-לא-במבנה** (P0-2/Task#7): #420 סטופ 5.25 נק' בתוך המבנה → נתפס ב-8 שניות.
2. **הפתיחה המסווגת ("Open Test Drive" מטה) לא הניבה ירי** — חלון-הפתיחה נסגר ב-10:00 כשהסוג סווג + דוחה test-drive (Task#3).
3. **G6 חסם את S4** (get_live_day_type=null לפני-נעילה) → הוחזר OFF (ריסטארט).
4. **G2/G3 חסמו את S2** (אותו null) → הוחזרו OFF (ריסטארט).
5. **location_gate חסם CONT-מטה** (get_live_expansion=None — הגלאי פספס את פריצת-ה-IB) → הוחזר OFF (ריסטארט).
6. **מעגל-הריסטארטים** (~6, אחד לכל תיקון-דגל) **איפס את buffer-S2 כל פעם** → S2 עיוור ~15-20 דק' אחרי כל אחד (P1-4/Task#8).
7. **11:47 — buffer התאושש (12), השערים-השבורים כבויים, אבל `rr_entry_gate` חוסם** (R:R<0.65). זה **שער-סיכון לגיטימי** — כנראה חוסם כי **הסטופ ממוקם-שגוי (Task#7) → R:R מחושב שגוי**. לא לכבות אותו; לתקן את הסטופ.

**השורש-העמוק המאחד: הסטופ הממוקם-שגוי (P0-2/Task#7).** הוא (א) הפסיד את 2 העסקאות, ו-(ב) עכשיו גורם ל-rr_entry_gate לדחות setups חדשים (R:R שגוי מסטופ שגוי). תיקון-הסטופ פותר את שניהם. cc בונה אותו.

**דפוס-על שחזר 4 פעמים היום:** שער חוסם כי גלאי-upstream (day_type/expansion) פספס את פריצת-ה-IB. → כל השערים האלה תלויים במקור-אחד-אמין (Task#5) + גלאי-הרחבה תקין. עד אז הם OFF.

---
**סיכום-יום קצר:** לייב-אמת · סוג-יום נכון = **Variation** (override, דלתון; המסווג טעה Normal) · ירו #420(S2)+#424(S4), שניהם הפסידו על סטופ-שגוי · 4 שערים הוחזרו OFF (חסמו על סיגנל-שבור) · מעגל-ריסטארטים סימא את S2 · עכשיו rr_entry_gate חוסם = סימפטום של הסטופ. **שורש מאחד: מיקום-הסטופ (Task#7) + מקור-יחיד (Task#5).**
