# CURSOR — אימות בלתי-תלוי: הפסד-יומי ‑$269 (2026-07-23)

**אל:** cursor · **מאת:** cowork-dev · **דחיפות:** כסף-אמת חי · **פעל לפי** `docs/handoff/CC_HANDOFF_CONTRACT.md`
**רקע:** מייקל ראה "הפסד יומי ‑$269" שלא ציפה לו וביקש אימות בלתי-תלוי. cowork כבר חקר; **המשימה שלך = לאמת/להפריך את הממצאים שלי מול המקורות** (חוק-5: command+raw output לכל AC). אל תסמוך על הטבלה שלי — שחזר.

---

## הממצאים שלי (לאימות)

**F1 · ה‑$269 אמיתי ומדויק — סכום 3 עסקאות-לייב-סגורות של היום:**
| id | pattern | mode | pnl_usd | exit (ET) |
|----|---------|------|---------|-----------|
| 466 | (בוקר) | live | **+31.25** | 04:05 ET (לילי/pre-RTH) |
| 479 | INITIATIVE_SHORT (S2) | live | **‑161.25** | 11:38 ET |
| 481 | ZLR (S4) | live | **‑138.75** | 11:55 ET |
| **סה"כ** | | | **‑268.75 ≈ ‑$269** | |
(475/477/483 = CANCELLED, pnl 0. כל ה-shadow לא נספרים.)

**F2 · יש שני מספרים שונים — חוסר-עקביות-חלונות (הליבה לאימות):**
- **תצוגת-נייד ‑$269** — `mobile_monitor.py:108-113`: `SUM(pnl_usd) WHERE mode!='shadow' AND state='CLOSED' AND entry_ts::date(IL)=today(IL)` → **כולל** את 466 הלילי (+31.25).
- **מונה-RISK_HALT ‑$300** — `trading_gateway.py:343-348`: `SUM(pnl_usd) WHERE mode='live' AND state='CLOSED' AND exit_ts >= today 09:30 ET` → **מוציא** את 466 (יצא 04:05 ET, טרם-סשן).
- שניהם "נכונים" להגדרתם אך **סותרים**. השאלה: מי הסמכותי לתצוגה/להחלטה, והאם הפיצול מכוון?

**F3 · ‑$269/‑$300 מחושב, לא מאומת-Sierra.** `pnl_sierra` **ריק (SUM=0)** לכל עסקאות-היום — עמודת-ground-truth (migration 023) לא מאוכלסת. המספר הוא `pnl_usd` (חישוב backend), לא P&L-חשבון-אמת מ-Sierra.

**F4 · שתי המפסידות אינן מ-RESPONSIVE_WITH_DAY_TREND_V1** (הדגל שהודלק היום). 479=INITIATIVE_SHORT, 481=ZLR — אף אחת אינה REACTIVE/HNS, ולכן **לא עברו בענף שנערך** (`_RESPONSIVE_REV`). שתיהן שורט-עם-הטרנד (כיוון-נכון ביום-יורד) שנעצרו כשהמחיר קפץ 7411→7434; אותו פער-ניהול (סטופ לא ל-BE אחרי T1).

**F5 · תוך תקרת-הסיכון-המאושרת.** `.env`: `RISK_HALT_V1=1`, `RISK_DAILY_LOSS_CAP=800`, `LIVE_TRADING_ARMED=1`. ‑$269/‑$300 « ‑$800 → המערכת ממשיכה כמתוכנן. הפוזיציה **FLAT** כרגע.

---

## מה לאמת (AC בינארי + פקודה מדויקת)

**AC-1 · המספר ‑$269 vs ‑$300.** הרץ את **שתי** השאילתות המדויקות (מהקבצים לעיל) מול PG והדבק raw. אשר: תצוגה=‑268.75, halt=‑300.00. הכרע: איזה חלון נכון למה שמייקל רואה, והאם ההבדל (עסקה לילית 04:05 ET) מכוון או באג-הגדרה. **✓/✗**

**AC-2 · Sierra ground-truth.** משוך את ה-P&L האמיתי של חשבון 37138283 מ-Sierra (trade_activity / fills-journal / דוח-חשבון) והצלב מול ‑$269/‑$300. האם ה-`pnl_usd` המחושב תואם את המציאות? אם `pnl_sierra` ריק — **זה פער-אמת** (Rule 1): דווח כמה הפער והאם ‑$269 הוא ההפסד-האמיתי או הערכה. **✓/✗**

**AC-3 · שתי המפסידות לגיטימיות (לא באג).** ל-479 ו-481: אשר entry/stop/fills מול fills-journal; אשר שהכיוון (SHORT) היה עם-הטרנד; אשר שהסטופ נעצר על קפיצת-מחיר אמיתית (לא stop שגוי/רוחב-שגוי). אשר עצמאית ש**אף אחת לא עברה ב-`daytype_playbook` ענף-`_RESPONSIVE_REV`** (grep הלוג + קרא את הקוד). **✓/✗**

**AC-4 · RISK_HALT תקין.** אשר `RISK_HALT_V1=1` + `RISK_DAILY_LOSS_CAP=800` פעילים בתהליך-החי (boot-line, לא ps eww), ושהמונה (‑$300) מוזן נכון ל-`_daily_pnl`. אשר שהמערכת **תעצור** ב-‑$800 (קרא `trading_gateway.py:1839-1841`). **✓/✗**

**AC-5 · hydration/carryover.** אשר שאין carryover מיום-אתמול (‑$125 שהוזכר) מזוהם לתוך מונה-היום. session_start=09:30 ET היום → אמור לאפס. **✓/✗**

---

## NOT-DONE / הסתייגויות (cowork)
- לא תיקנתי את חוסר-עקביות-החלונות (F2) ולא את פער-ה-pnl_sierra (F3) — **מדווח בלבד**, ממתין להכרעתך + מייקל.
- לא עצרתי/פירקתי את המערכת — היא חמושה (מייקל חימש), תוך התקרה. אם מייקל ירצה עצירה זו פעולת-סיכון שלו.
- #479/#481 = כיוון-נכון, הפסד מניהול-ראנר (BE-אחרי-T1 לא הוחל ב-S2/S4) — פתוח נפרד, לא נגרם מהדגל.

## הכרעה מבוקשת
סמן כל AC ✓/✗ עם raw output. אם AC-2 חושף ש-‑$269 אינו ההפסד-האמיתי (פער-Sierra), או AC-3 חושף באג — **🔴 ל-LIVE_CHANNEL §OPEN + strategic-stop**. אחרת אשר את המספר ונדון בתיקון חוסר-העקביות (F2/F3) בנפרד.
