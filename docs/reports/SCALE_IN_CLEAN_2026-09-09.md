# SCALE_IN — מדידה נקייה · 09.09.2026

**השאלה:** ‏`SCALE_IN_V1=1` (פסיקת 13.08, "אם הכיוון ממשיך אפשר לחזק בעוד חוזים"). שני מספרים סותרים:
`DAY_AUDIT_2026-08-10_to_08-28.md` §2 — **6 שורות-ברוקר, ‎−$256.25**; שאילתת-הבוקר של 09.09 — **n=20, ‎+$628.75**.
מייקל רוצה "לנקות את מה שמזיק" וזה המועמד הראשון ⇒ למדוד לפני שנוגעים.

**בסיס-הכסף:** ‏`v9_trades` בלבד · `mode='live'` · **‏`pnl_sierra` בלבד** (`pnl_usd` מוצג רק כ-`usd*` ולעולם לא נסכם — `scripts/live_pnl.py:19-24`).
**איך נמדד:** ‏3 סקריפטים קריאה-בלבד על ה-Mac (`si_probe.py` / `si_probe2.py` / `si_probe3.py` בתיקיית-ה-outputs של הסשן), `.env` נטען, `backend.v9.db.read.read_all`, ‏`DATABASE_URL=postgresql://localhost/mems26`. כל מספר כאן הוא פלט-שאילתה; ה-SQL מצוטט.

---

## 1. איך הגדלה נרשמת — שורה חדשה, לא תוספת להורה

| מה | איפה | ציטוט |
|---|---|---|
| ההגדלה היא **עסקת-ילד חדשה** ב-`v9_trades` | `backend/v9/services/trade_manager/bar_level_detector.py:1512-1520` | `child = {"firing_system": …, "direction": dec.direction, "entry_price": dec.entry, "stop": dec.stop, "t1": …, "t2": None, "t3": None, "contracts": dec.add_contracts, "classification": "SCALE_IN", "metadata": {"scale_in_parent": <parent id>, "reason": …}}` ⇒ `child_id = self._tm.accept_setup(child, _mode)` |
| הסימון על **הילד** | `backend/v9/services/trade_manager/manager.py:503,517,520,593` | `classification = setup.get("classification")` ⇒ `quality.classification='SCALE_IN'`, `quality.trigger='SCALE_IN'`, `quality.pattern_name='SCALE_IN'`; הקישור להורה ב-`quality.metadata.scale_in_parent` |
| **‏`pattern_id_at_entry` על הילד = NULL** | `manager.py:584` | `pattern_id_at_entry=setup.get("pattern_id_at_entry")` — המילון של הילד לא נושא את המפתח ⇒ **‏0 מ-17 הילדים** יש להם `pattern_id_at_entry` (שאילתה §A להלן: `n_pid=0`) |
| הסימון על **ההורה** | `bar_level_detector.py:1503` · `:1528-1530` · `:1589-1591` | `q2["scaled_in"]=True; q2["scale_in_child_pending"]=True` לפני ה-PLACE; אחרי ה-PLACE `scale_in_child_id` + `scale_in_added` (מ-18.08 בלבד — לפני-כן ההורה נשאר `scale_in_child_pending` בלי מזהה-ילד); ילד שנדחה ⇒ `scale_in_last_refused` |
| הילד לא עובר בגייטוויי — `accept_setup` ישירות | `bar_level_detector.py:1520` | לכן אין `day_type_at_entry`/`pattern_id_at_entry` על הילד |
| רץ רק על demo/live | `bar_level_detector.py:917-919` | `if _is_demo_live: self._maybe_scale_in(trade, bar_high, bar_low)` |
| ה-`exit_reason` של הילד הוא **סיבת-היציאה הרגילה** (`STOP_FILL`/`phantom_reconcile`/…) — לעולם לא "SCALE" | שאילתה §A | `pattern_id_at_entry ILIKE '%SCALE%' OR exit_reason ILIKE '%SCALE%'` ⇒ **‏0 שורות** |

**המסקנה למדידה:** האוכלוסייה הנכונה ל"רגל-הגדלה" היא `quality->>'classification' = 'SCALE_IN'` — ‏17 שורות, כולן `mode='live'`, ‏`is_synthetic=0`, ‏#661…#1076, ‏13.08→04.09.
ההורה מזוהה דרך `quality->'metadata'->>'scale_in_parent'` של הילד (הקישור-מההורה חסר על 4 ההורים שלפני 18.08).

```sql
-- §A  inventory of the marker (si_probe.py)
SELECT mode, COUNT(*) n, COUNT(*) FILTER (WHERE pnl_sierra IS NOT NULL) n_sierra,
       COUNT(*) FILTER (WHERE pattern_id_at_entry IS NOT NULL) n_pid, MIN(id), MAX(id)
FROM v9_trades WHERE quality->>'classification'='SCALE_IN' GROUP BY mode;
-- live | 17 | 10 | 0 | 661 | 1076
```

---

## 2. שני המספרים הסותרים — איזו אוכלוסייה כל אחד תיאר

### 2.1 שאילתת-הבוקר (n=20, ‎+$628.75) — **מערבבת ילדים עם הורים**
```sql
SELECT … FROM v9_trades
WHERE (pattern_id_at_entry ILIKE '%SCALE%' OR exit_reason ILIKE '%SCALE%' OR quality::text ILIKE '%scale_in%')
  AND pnl_sierra IS NOT NULL;
-- n=20, Σ=+628.75  (משוחזר 1:1)
-- לפי תפקיד:  CHILD  n=10  Σ=−157.50   |   PARENT  n=10  Σ=+786.25
```
שני התנאים הראשונים תופסים **אפס** שורות (§1). כל ה-20 באים מ-`quality::text ILIKE '%scale_in%'`, שתופס את **הילד** (דרך `metadata.scale_in_parent`) **וגם את ההורה** (דרך `scale_in_child_id` / `scale_in_added` / `scale_in_child_pending` / `scale_in_last_refused`). ‏+$786.25 מתוכם הם רווח של **עסקאות-הורה** — עסקאות שכבר בנקו T1 לפני שההגדלה נולדה (תנאי-הכניסה: `bar_level_detector.py:1383` `trade.t1_hit_ts is None → return`). ובתוך ה-10 האלה, **3 הורים (#953/#971/#1073, ‎+$308.75) שההגדלה שלהם מעולם לא הגיעה לשוק** (T-226 / `PLACE_REFUSED:t3_missing`). זה מספר על "הורים שזכו לתוספת", לא על התוספת.

### 2.2 ‏DAY_AUDIT §2 (6 שורות, ‎−$256.25) — **ילדים בלבד, חלון 10–28.08, לפני backfill-הברוקר של ליל-08.09**
ההרכב משוחזר מהטקסט של הדוח עצמו: ‏#662 ‎−130 · ‏#770 ‎−57.50 · ‏#775 ‎−62.50 · ‏#841 ‎**−77.50** · ‏#673 ‎+11.25 · ‏#853 ‎+60 ⇒ ‎−256.25 ✓ (‏#831/#840 היו אז `usd*` בלי מחיר-ברוקר).
**אותו חלון היום** (אחרי `sierra_activity_join --write` על 39 הימים, MEASUREMENTS 08.09):
```sql
SELECT COUNT(*) n_rows, COUNT(*) FILTER (WHERE pnl_sierra IS NOT NULL) n_sierra, SUM(pnl_sierra)
FROM v9_trades WHERE mode='live' AND quality->>'classification'='SCALE_IN'
  AND (entry_ts AT TIME ZONE 'Asia/Jerusalem')::date BETWEEN '2026-08-10' AND '2026-08-28';
-- 12 | 8 | −140.00      (usd*-only: 4 rows, +87.50 — לא נסכם)
```
‏−256.25 → ‎−140.00 = ‏#831 NULL→+52.50 · ‏#840 NULL→+56.25 · ‏#841 ‎−77.50→‎−70.00 (‏+116.25 בדיוק). **הכיוון של הביקורת נכון (הרגליים עלו כסף בחלון), הגודל התיישן.**
וטענת-ההמשך *"בלעדיהן 11 הימים חיוביים"* התיישנה גם היא: החלון היום הוא **‏+$361.25 עם הרגליים** (31 שורות-ברוקר) ו-**‏+$501.25 בלעדיהן** — חיובי בשני המקרים; ה-‎−$142.50 של הביקורת היה 23 שורות-ברוקר לפני ה-backfill.

**אין למצע בין ‎+$628.75 ל-‎−$256.25 — הראשון אינו מדידה של הגדלה, השני מדידה ישנה של האוכלוסייה הנכונה.**

---

## 3. שלוש האוכלוסיות — ‏`pnl_sierra` בלבד

```sql
-- si_probe3.py §1 — role assignment (live only)
WITH legs AS (
  SELECT id, (quality->'metadata'->>'scale_in_parent')::int AS parent_id,
         (state <> 'CANCELLED' AND entry_ts IS NOT NULL) AS reached_market
  FROM v9_trades WHERE quality->>'classification'='SCALE_IN'),
roles AS (
  SELECT t.id, CASE WHEN t.quality->>'classification'='SCALE_IN' THEN 'A_LEG'
                    WHEN t.id IN (SELECT parent_id FROM legs WHERE reached_market) THEN 'B_PARENT'
                    WHEN t.id IN (SELECT parent_id FROM legs WHERE NOT reached_market) THEN 'B2_PARENT_ADD_NEVER_TRADED'
                    ELSE 'C_OTHER' END AS role
  FROM v9_trades t WHERE t.mode='live')
SELECT r.role, COUNT(*) n_rows, COUNT(*) FILTER (WHERE pnl_sierra IS NOT NULL) n_broker, SUM(pnl_sierra) …
FROM v9_trades t JOIN roles r ON r.id=t.id GROUP BY 1;
```

| אוכלוסייה | הגדרה | שורות | **‏n ברוקר** | **‏Σ ‏`pnl_sierra`** | ‏W/L | לא-נסכם |
|---|---|---:|---:|---:|---|---|
| **‏(a) רגלי-ההגדלה** | `quality->>'classification'='SCALE_IN'` | 17 | **10** | **‎−$157.50** | 5/5 · ממוצע-זכייה +$44.00 · ממוצע-הפסד ‎−$75.50 | ‏4 `usd*` (+$87.50: #661 +46.25 · #708 0 · #818 +41.25 · #820 0) · 2 רפאים-T-226 בלי מחיר (#955/#979) · 1 `CANCELLED` (#1076) |
| **‏(b) הורים שקיבלו הגדלה** | שורה שאינה-רגל ש-`metadata.scale_in_parent` של רגל-שהגיעה-לשוק מצביע עליה | 10 | **7** | **‎+$477.50** | 7/0 — **ברירה מובנית**: הורה חייב לבנק T1 כדי להיות זכאי (`:1383`) | ‏3 `usd*` (+$93.75: #660 · #830 · #851) |
| ‏(b2) הורים שהתוספת שלהם **לא נסחרה** | כנ"ל, אך הרגל `CANCELLED`/`entry_ts IS NULL` | 3 | 3 | ‎+$308.75 | 3/0 | ‏#953 (ילד #955 רפאים) · #971 (ילד #979 רפאים) · #1073 (ילד #1076 נדחה) |
| **‏(c) כל שאר הלייב** | כל השאר | 146 | **86** | **‎−$942.50** | 37/48 | ‏21 `usd*` (+$315) · 35 `CANCELLED` · 4 בלי מחיר |
| **סה"כ לייב** | | 176 | **106** | **‎−$313.75** | | = בדיוק שורת-MEASUREMENTS 08.09 ("Σ מאומת = ‎−$313.75") ✓ |

ילד יכול להיות גם הורה (שרשרת): ‏#661, ‏#818, ‏#840, ‏#881 הם רגליים שקיבלו רגל משלהן — הם נספרים **פעם אחת, כרגל** (a).

**סימן-שורט אומת** על כל רגל שנעצרה (מ-`quality.exit_fills`): ‏#770 SHORT ‏7689.50→STOP 7695.25 ×2c = ‎−5.75×2×$5 = ‎−$57.50 ✓ · ‏#885 SHORT ‏7676.50→7682.50 ×2c = ‎−$60 ספרים / ‎−$57.50 ברוקר ✓ · ‏#841 SHORT ‏7731→7738.75 ×2c = ‎−$77.50 ספרים / ‎−$70 ברוקר ✓ · ‏#775 LONG ‏7702.25→7696 ×2c = ‎−$62.50 ✓ · ‏#662 LONG ‏7830.25→7817.25 ×2c = ‎−$130 ✓ (הסטופ היה 7822.50 — **‏5.25 נק' החלקה**, תקרית 13.08 "הספרים אומרים 4, סיירה אומרת 1", `bar_level_detector.py:1404-1408`). כל השורטים שנעצרו שליליים.

---

## 4. הנגדי שחשוב — כל זוג הורה+רגל

הרגל היא ברקט עצמאי (סטופ משלה ב-BE-של-ההורה / ממוצע-P3, T1 משלה ב-1.5R — `:1509-1515`), ולכן **ה-P&L של ההורה על שורתו הוא "ההורה בלי הרגל"** ו-P&L של הרגל הוא **מה שהיא הוסיפה**. (הנחת-המדידה: הרגל אינה משנה את יציאות-ההורה. הסייג: 3 הורים נסגרו `manual`/FLATTEN — האם היו נסגרים כך בלי הרגל, לא-ניתן-להכרעה.)

```sql
-- si_probe3.py §2 — recursive chain: leg → direct parent → root
WITH RECURSIVE legs AS (SELECT id, (quality->'metadata'->>'scale_in_parent')::int parent_id
                        FROM v9_trades WHERE quality->>'classification'='SCALE_IN'),
chain AS (SELECT l.id leg_id, l.parent_id cur, 1 depth FROM legs l
          UNION ALL SELECT c.leg_id, l2.parent_id, c.depth+1 FROM chain c JOIN legs l2 ON l2.id=c.cur)
SELECT … ORDER BY leg;
```

| רגל | הורה-ישיר | שורש | רמה | תאריך | כיוון | **הורה $** (בלי הרגל) | **רגל $** | **זוג $** | פסק |
|---:|---:|---:|---:|---|---|---:|---:|---:|---|
| 662 | 661 | 660 | **2** | 13.08 | LONG | ‏`usd*` +46.25 (ברוקר NULL) | **‎−130.00** | ‏(ספרים ‎−83.75) | הרגל הפכה זכייה להפסד — **על בסיס-ספרים** |
| 673 | 670 | 670 | 1 | 14.08 | SHORT | +81.25 | +11.25 | +92.50 | שיפרה |
| 770 | 766 | 766 | 1 | 21.08 | SHORT | +37.50 | **‎−57.50** | **‎−20.00** | **הפכה זכייה להפסד** |
| 775 | 774 | 774 | 1 | 21.08 | LONG | +40.00 | **‎−62.50** | **‎−22.50** | **הפכה זכייה להפסד** |
| 831 | 830 | 830 | 1 | 27.08 | SHORT | ‏`usd*` +22.50 (ברוקר NULL) | +52.50 | ‏(ספרים +75.00) | שיפרה (1c בלבד — החוזה השני נדחה-מרג'ין, T-111) |
| 840 | 838 | 838 | 1 | 28.08 | SHORT | +107.50 | +56.25 | +163.75 | שיפרה |
| 841 | 840 | 838 | **2** | 28.08 | SHORT | +56.25 | **‎−70.00** | **‎−13.75** | **הפכה זכייה להפסד** (5 דק' אחרי הרגל הקודמת; נעצרה תוך 2.5 דק') |
| 853 | 851 | 851 | 1 | 28.08 | SHORT | ‏`usd*` +58.75 (ברוקר NULL) | +60.00 | ‏(ספרים +118.75) | שיפרה |
| 881 | 877 | 877 | 1 | 31.08 | SHORT | +116.25 | +40.00 | +156.25 | שיפרה |
| 885 | 881 | 877 | **2** | 31.08 | SHORT | +40.00 | **‎−57.50** | **‎−17.50** | **הפכה זכייה להפסד** |

**ספירה (‏n=10 רגליים מתומחרות-ברוקר):**
- הרגל **שיפרה** את הזוג: **5** (‏+$220.00) · הרגל **הזיקה**: **5** (‏‎−$377.50).
- **כל 5 הרגליים שהזיקו הפכו הורה-מנצח לזוג-מפסיד** — 4 על בסיס-ברוקר (‏#770 · #775 · #841 · #885) ו-1 על בסיס-ספרים (‏#662, ההורה #661 אינו מתומחר-ברוקר). זה מבני: ההורה כבר בנק T1 ומגן על עצמו ב-BE, והרגל מסכנת ≥6 נק' × 2c ≈ ‎−$60 — הפסד-רגל אחד גדול מהרווח-הנותר של ההורה.
- **רמה-1 (הגדלה על הורה):** ‏n=7 · **‏+$100.00** · 5W/2L. **רמה-2 (הגדלה על הגדלה):** ‏n=3 · **‎−$257.50** · **‏0W/3L**. כל הכסף שאבד ברגליים אבד בהוספה-על-הוספה (‏#662 · #841 · #885). ‏n=3 — כיוון, לא גודל.
- **לפני/אחרי P3** (`SCALE_IN_P3_V1=1`, פסיקת 24.08): לפני — ‏n=4 · ‎−$238.75 (1W/3L) · אחרי — ‏n=6 · **‏+$81.25** (4W/2L); שני ההפסדים אחרי-P3 הם רמה-2. ‏n קטן להכרעה.
- **ברמת-שרשרת** (שורש + כל רגליו, ברוקר): ‏670 +92.50 · ‏766 ‎−20 · ‏774 ‎−22.50 · ‏838 +93.75 · ‏877 +98.75 (‏660/830/851 — השורש לא מתומחר).

**לפי ימים** (ברוקר, ימים עם רגל מתומחרת): ‏13.08 ‏+$10 (רגליים ‎−130 · בלעדיהן +140) · ‏14.08 ‎−$123.75 (+11.25 · ‎−135) · ‏**21.08 ‎−$45 (‎−120 · +75) — היום היחיד שהרגליים הפכו את סימנו** · ‏27.08 +$100 (+52.50 · +47.50) · ‏28.08 +$331.25 (+46.25 · +285) · ‏31.08 ‎−$65 (‎−17.50 · ‎−47.50).

---

## 5. ממצא-אגב שמשנה את "לנקות": ‏`SCALE_IN` **אינרטי בלייב מאז 01.09**

שלושת ניסיונות-ההגדלה האחרונים — **‏3 מ-3 — לא הגיעו לשוק:** ‏#955 (02.09 17:30) · ‏#979 (02.09 21:18) · ‏#1076 (04.09 20:30, `PLACE_REFUSED:t3_missing`, CANCELLED).
השרשרת (ראיה גולמית, `TASK_LOG` T-226 מ-`/tmp/backend.err.log` 02.09 17:30:04): ‏`[SierraCmd] RISK_BUDGET: risk=10.6 pts → raw=4.2 → floor=4 → min(ruled=5)=4` → `[ScaleIn] +2c LONG parent=953 child=955` → `T-214: PLACE rejected — t3=None invalid on 4 contracts`.
**השורש בקוד:** הילד מבקש `"contracts": dec.add_contracts` (=2) עם `"t2": None, "t3": None` **בכוונה** (`bar_level_detector.py:1515`). אבל ענף-`RISK_BUDGET_SIZING_V1` ב-`sierra_command.py:694-744` קורא רק `metadata.sizing_contracts`/`sizing` (‏`:725-728`) — לילד אין — ומחזיר `min(n, ruled)` = 4–5, בלי להביט ב-`contracts` של הילד; ואז שומר-T-214 (`:891-904`) דוחה כל פקודה ≥3 חוזים בלי t3. עד 31.08 `SIZE_CAP_OVER_FIXED_V1=1` כיבד את ה-2 של הילד (`:753-775`, `min(fixed, 2)`), ולכן הרגליים היו 2c; מ-01.09 (‏RISK_BUDGET + T-214, שתיהן פסיקות-01.09) **כל תוספת מנופחת ל-4–5c ונדחית.**
⇒ **כיבוי `SCALE_IN_V1` היום לא משנה שקל בלייב.** מה שרץ בפועל מאז 01.09 הוא "אפס הגדלות + שורת-ספרים/רולבק לכל ניסיון". החזרת-ההגדלה (אם מייקל רוצה אותה) = ילד שנושא `metadata.sizing_contracts=2` (או `fixed_contracts_exempt`) + t3 — שינוי-סיכון-מסחר ⇒ פסיקה, לא כאן.

---

## 6. פסק

**‏`SCALE_IN` על שורות-ברוקר: שלילי — ‏n=10, ‏Σ ‎−$157.50, ‏5W/5L, ממוצע-זכייה +$44 מול ממוצע-הפסד ‎−$75.50; כל 5 הרגליים המפסידות הפכו הורה-מנצח לזוג-מפסיד.** ‏n=10 הוא בדיוק סף-ההכרעה — כיוון מבוסס, גודל לא (הרגל הבודדת #662 = ‎−$130 היא 82% מהסכום; בלעדיה ‎−$27.50/n=9 — לא מסירים אותה, מציינים).
תת-החלוקות (רמה-1 ‏+$100/n=7 · רמה-2 ‎−$257.50/n=3 · אחרי-P3 ‏+$81.25/n=6) הן ‏n<10 ⇒ כיוון בלבד. המבנה היחיד בנתונים: **הכסף אבד בהוספה-על-הוספה, לא בהוספה-על-הורה.**
המספר ‎+$628.75 אינו מדידה של הגדלה (10 מ-20 שורותיו הן הורים, 3 מהם בלי תוספת שנסחרה); ‎−$256.25 הוא האוכלוסייה הנכונה במצב-הנתונים של אתמול (היום ‎−$140 באותו חלון).
ומה שמייקל שואל בפועל — "האם לנקות" — נתקל בממצא §5: **המנגנון כבר לא סוחר מאז 01.09.** מה שיש לנקות הוא הדגל-שנראה-חי, או לתקן את גודל-הילד — שתי אפשרויות, שתיהן פסיקה.

---

## 7. מה **לא** נבדק כאן
- לא ריפליי-נגדי ("מה היה קורה בלי ההגדלה" על ברים) — המדידה היא **כפי-שנסחר**, שורה מול שורה.
- לא נבדק האם רגל מפסידה "עצרה" את ההורה מוקדם (3 סגירות `manual`) — לא-ניתן-להכרעה מהספרים.
- ‏#708/#818/#820 (`usd*`) — לא נסכמו; ‏#708 ו-#820 רשומים 0 (`SIERRA_FLAT`) ואין להם מחיר בשום מקום.

*קבצי-הראיה: `outputs/si_probe.py`, `si_probe2.py`, `si_probe3.py` (סשן cowork 09.09), פלט מלא בטרנסקריפט. HEAD בזמן-המדידה: `ccd62eea` 09.09 08:46.*
