# Fake @5900 Trades — Root Cause (Cowork, read-only) · 2026-05-30

## תמצית
עסקאות ה‑`entry_price=5900` **אינן באג מסחר** — הן **זיהום מהרצת טסטים אל ה‑DB החי**,
שנגרם מנתיב DB מקודד קשיח ב‑Gateway שעוקף את `DATABASE_URL`.

## שרשרת הראיות

1. **אין 5900 בקוד הייצור.** `grep 5900 backend/v9 bridge --include=*.py` → 0 התאמות.
   5900 קיים **רק בטסטים**: `tests/v9/gateway/test_d088_shadow_cluster_guard.py`,
   `tests/v9/gateway/test_gw02_record_attempt.py`, `tests/v9/api/test_trades_exit.py`.

2. **הטסט כותב דרך ה‑Gateway:** `test_d088` עושה
   `gw = TradingGateway(); gw.route_setup({entry_price:5900, firing_system:4, ...})`.
   `route_setup` רושם עסקת SHADOW ל‑DB.

3. **ה‑Gateway מקודד את ה‑DB החי:**
   `backend/v9/gateway/trading_gateway.py:25` →
   `DB_PATH = "/Users/michael/Downloads/mems26_web_git/data/mems26_local.db"`.
   זה **עוקף** את `DATABASE_URL` שב‑`db/session.py:12`. לכן כל הרצת טסט שמפעילה
   `route_setup` כותבת ל‑DB הייצרני.

4. **הנתונים תואמים בדיוק:**

| קבוצה | id | created_at | firing_system | dir | entry | is_synthetic |
|-------|----|-----------|--------------|-----|-------|--------------|
| ישנות | 390–401 (12) | 2026-05-29 09:34–10:34 | 4 | — | 5900 | **1** (סומנו) |
| חדשות | 844–846 (3) | **2026-05-30 14:19:50** | 4 | LONG | 5900 | **0** |

   3 החדשות נוצרו ב‑14:19:50 — בדיוק בחלון שבו CC הריץ pytest (PYTEST_TRIAGE 14:22).
   `created_at` של 3 השורות במרווחי ~8ms = שלוש קריאות `route_setup` רצופות מטסטים.
   **רק 3 עסקאות נוצרו היום בכלל — כולן 5900.** אין עסקאות מסחר אמיתיות היום (שוק סגור).

## מסקנה
- אלו לא "עסקאות מזויפות שמישהו זרע" אלא **תוצר לוואי של טסטים שאין להם DB מבודד**.
- זה גם אומר ש‑**ה‑Gateway המחובר לא מכבד `DATABASE_URL`** — סיכון נוסף ל‑LIVE/DEMO
  (מתחבר ל‑SYSTEM_REVIEW §4 #15: נתיבי DB קשיחים).

## תיקון מוצע (אין שינוי לוגיקת-מסחר)
1. **`trading_gateway.py:25`** — להחליף את `DB_PATH` הקשיח בפתרון מ‑`db/session.py`
   (קריאת `DATABASE_URL`/`db_path`), כך שב‑prod זה ה‑DB הנכון וב‑טסט זה DB מבודד.
2. **בידוד DB לטסטים** — fixture/conftest שמכוון `DATABASE_URL` ל‑temp file (או monkeypatch
   ל‑DB_PATH של ה‑Gateway), כך שטסטים לעולם לא נוגעים ב‑`mems26_local.db`.
3. **ניקוי 3 השורות 844–846** — דורש **גיבוי + אישור Michael** (CLAUDE.md). לסמן
   `is_synthetic=1` (כמו 390–401) או למחוק. **לא לבצע בלי אישור.**

## אימות אחרי תיקון
- הרץ את הטסטים → `SELECT COUNT(*) FROM v9_trades WHERE entry_price=5900 AND is_synthetic=0` נשאר 3 (לא גדל).
- `grep DB_PATH backend/v9/gateway/trading_gateway.py` → לא נתיב קשיח.
