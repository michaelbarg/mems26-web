# משימה — הגדלת-פוזיציה חכמה: לחזק איפה שיש טווח וסיכוי (להערב, אחרי 23:00)
**פסיקת-מייקל 21.08 ~20:40:** "מה עם הגדלת פוזיציה שיש לה טווח ארוך, או לנסות למקסם
איפה שיש סיכוי גבוה" → **מאשר** את שלושת הסעיפים. **§0 חל** (אל תשאל, תכריע, תתעד).
**כלל-§D מחייב: בלי רפליי היסטורי ירוק — לא מדליקים.**

## הרקע (מדוד היום)
`SCALE_IN_V1=1` חי ועבד 3 פעמים (13.08 · 14.08 · 17.08): `+2c` עם סטופ בכניסת-האם
= חיזוק בסיכון-אפס. תנאיו: T1 נבנק **וגם** ‎6 נק' מעבר לכניסה **וגם** עם-המגמה.
ב-21.08 הוא לא ירה על #766 (SHORT ‎@7695.50) כי היו רק ~3 נק' לטובתנו — **התנאי
לא התקיים, לא באג**. אבל 6-הנקודות הן **סף קבוע** — בדיוק המחלקה שכל המערכת עוברת
ממנה ליחסי, והוא גם **עיוור לפוטנציאל**: לא שואל כמה מקום נשאר עד היעד המבני הבא.

## מה לבנות (דגל אחד לכל סעיף, default OFF, RULED_FLAGS באותו קומיט)
1. **`SCALE_IN_ATR_TRIGGER_V1`** — הסף 6 נק' → **‎0.5×ATR14** (רצפה/תקרה מהנתונים,
   לא מהעין). לגזור k מהשלושה שירו היסטורית: הוא חייב לשחזר אותם ביום-ATR-חציוני.
2. **`SCALE_IN_POTENTIAL_V1`** — לחזק **רק** כשנשאר מרחק משמעותי ליעד/לרמה המבנית
   הבאה (הצעה: ‎≥1.5R או ‎≥1.0×ATR — למדוד את שניהם). זה "למקסם איפה שיש סיכוי גבוה":
   לא מחזקים לתוך קיר.
3. **`SCALE_IN_SECOND_ROUND_V1`** — היום החיזוק חד-פעמי. ביום-מגמה מאומת (רגל חיה +
   מצפן מסכים) לאפשר סבב שני `+2c`, **כל אחד עם סטופ מוגן** (סטופ-האם/BE, לעולם לא
   מרחיב סיכון). תקרה: לא לחרוג מ-`MAX_PROTECTED_CONTRACTS`, וה-DLL חייב לברקט הכל.

## אימות (§D — חובה לפני הדלקה)
`scripts/week_replay.py` על כל עידן-הלייב, 3 רמות סליפג', עמלות: לפני מול אחרי לכל
סעיף **בנפרד** ואז יחד. לדווח דלתא כוללת **וחציון-יום** (כיול שמשפר סכום ופוגע בחציון
לא עולה), כמה חיזוקים נוספים נוצרים, וכמה מהם היו מפסידים.
→ `docs/reports/REPLAY_SCALE_IN_2026-08-21.md`. בלי ירוק — לא ל-.env.

## טסטים
דגל-OFF זהה-בייט · שלושת החיזוקים ההיסטוריים משוחזרים · חיזוק לתוך יעד-קרוב נחסם ·
סבב-שני לא חורג מהתקרה ואינו מרחיב סיכון · סטופ-הילד תמיד ‎<= סטופ-האם.

## ⚠️ קודם-כל: פער-הרישום (חוסם את כל המדידה)
היום #766: סיירה ‎−2 (יצאו 2 חוזים), הספרים רשמו **מילוי אחד** (`exit_fills` = T1 בלבד)
⇒ המסך הראה `1/4 hit` ו-$25 במקום 2 ו-$50. אותה מחלקה כמו T-62 שתוקן הבוקר — התיקון
לא כיסה את כל מסלולי-היציאה (OCO/מילוי-כפול-באותו-רגע). **זה מרעיל כל כיול שנשען על
P&L, כולל הרפליי של המשימה הזו — לתקן לפני, ולהריץ `pnl_reconcile.py --mode live`.**

---

## 4 · חיזוק חוצה-מערכות (פסיקת-מייקל 21.08 ~21:00 — סעיף חדש)
**השאלה שלו מילולית:** "האם מערכת 4 יכולה להרחיב עסקה כאשר היא מזהה תבנית שמתאימה
לכיוון של מערכת 2 ולהיכנס?"

**המצב היום:** לא. `_maybe_scale_in` מופעל **רק** ע"י העסקה של עצמה (T1 + 6 נק' +
עם-מגמה). **אבל הנכס כבר קיים ולא מנוצל:** `K4 confluence tag` בגייטוויי מזהה הסכמת
S2×S4 על אותו כיוון, והמחקר מדד **65% הצלחה בקונפלואנס מול 50% בודד** — והקוד עצמו
מתעד שהוא משמש ללוג/עדיפות בלבד ומסומן `future sizing hints`. זה ה-hint.

### א · המחקר (קודם — בלי מספר אין דגל)
לסרוק **כל** הכניסות בעידן-הלייב ולמצוא כל מקרה שבו, בזמן שעסקה של מערכת אחת הייתה
**פתוחה**, המערכת האחרת ייצרה מועמד **באותו כיוון** (בין אם ירה, נחסם, או רק זוהה).
לכל מקרה: הזמן, ההפרש מהכניסה, מה קרה למחיר אחרי (MFE/MAE), ומה חיזוק של ‎+2c היה
שווה — **בהנחה שהמערכת עובדת כמו שצריך** (בלי הבאגים שתוקנו היום). לפלח לפי סוג-יום,
לפי מי-האם/מי-המחזק, ולפי מרחק-ליעד. גם את הצד השני: כמה מהם היו **מפסידים**.
→ `docs/reports/CROSS_SYSTEM_SCALEIN_STUDY.md` עם טבלה פר-מקרה + סיכום ‎$.

### ב · הבנייה (רק אם המחקר ירוק)
`SCALE_IN_CROSS_SYSTEM_V1` (default OFF): עסקה פתוחה + מועמד **כשר** מהמערכת השנייה
באותו כיוון (עבר את כל השערים — לא "זוהה בלבד") ⇒ חיזוק `+2c` **בכללי-הבטיחות
הקיימים**: סטופ בכניסת-האם/BE, לעולם לא מרחיב סיכון, לא חורג מ-`MAX_PROTECTED_CONTRACTS`,
וה-DLL חייב לברקט הכל. להשתמש ב-K4 הקיים כמקור-ההסכמה — לא לבנות גלאי שני.
כפוף לסעיפים 1-3 לעיל (טריגר יחסי, מודעות-פוטנציאל).

### ג · אימות
כלל §D: `week_replay` לפני/אחרי, דלתא **וחציון-יום**, כמה חיזוקים נוספים וכמה מהם
מפסידים. בלי ירוק — לא ל-.env. טסטים: אם המועמד השני נחסם בשער — אין חיזוק; סטופ-הילד
תמיד מוגן; אין חריגה מהתקרה.
