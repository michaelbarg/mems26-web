## פסיקת-מייקל 10.09 18:45 — T-301 מאושר ("כן"). בנייה עכשיו, בלי ריסטארט עד ש-#1409 סגורה.

**הפסיקה:** תקרה/רצפה כפולה **מאושרת** (CEILING_FAILED/FLOOR_FAILED לפי הגלאי הקיים, פסיקת 28.08: אישור = סגירה מתחת/מעל לצוואר) **נגד** עסקה פתוחה — **גם לפני T1**, כשיש רווח-פתוח > 0 ⇒ מימוש. מחליף את סעיף "לפני-T1 ⇒ אפס פעולה" מ-02.09. אחרי-T1 — ללא שינוי (הידוק-סטופ לכל הרגליים).

### T-301a — לבנות (אופציה א', הפסיקה)
- מקום: הלימב של `STRUCTURE_EXIT_REALIZE_V1` ב-`bar_level_detector.py` (~1682). בעל-אחד: אותו לימב, לא צרכן חדש.
- תנאי: `ceiling_floor_state.state` == CEILING_FAILED ∧ LONG (מראה ל-FLOOR_FAILED ∧ SHORT) ∧ `t1_hit == False` ∧ רווח-פתוח > 0 (מול close של בר-האישור; T0-aware — רגליים שכבר יצאו לא נספרות).
- פעולה: `_emit_modify_target` לכל הרגליים הפתוחות → `confirm_close` (קו-החזרה; לעגל לטיק לכיוון הכניסה) + `_emit_modify_stop` → BE (entry). **לעולם לא FLATTEN, לעולם לא op=EXIT** (אתי בחשבון; op=EXIT שבור).
- אידמפוטנטי: מפתח `SE_R_PRE_{trade.id}` ב-`_se_fired`. פעם אחת לעסקה.
- `config/RULED_FLAGS.yaml`: לעדכן את שורת `STRUCTURE_EXIT_REALIZE_V1` — ציטוט: 'כן' (10.09 18:45) על "תקרה כפולה מאושרת נגד LONG פתוח, גם לפני T1, כשיש רווח ⇒ משיכת היעד לקו-החזרה + סטופ ל-BE, לעולם לא FLATTEN"; `measured:` = מספר-הריפליי מהסעיף הבא.
- הערת-.env (שורות 730-733): לעדכן את הטקסט "לפני-T1 ⇒ אפס פעולה" ← בוטל 10.09. (snapshot לפני עריכת .env.)

### מדידה (דוקטרינת-הלמידה: הוראה ⇒ קודם ריפליי) — לפני ההדלקה
- ריפליי על כל עסקאות-הלייב מאז 28.08 (broker-only `pnl_sierra`): כמה עסקאות היו נפגשות עם CEILING/FLOOR_FAILED נגדן לפני T1 ברווח, ומה ההפרש בין "מימוש בקו-החזרה" ל-"מה שקרה בפועל" (T1/סטופ). מספר אחד: Σ$ + n. זה ה-`measured:`.
- הרנס `fwd_harness.py` על 08-28 (העוגן) + 09-09 + 10.09 — אפס חריגות, אפס שינוי ב-routes (זה ניהול-עסקה, לא ניתוב) — ולהראות בלוג-ההרנס את שורת `[StructureExit] REALIZE-PRE-T1` על מקרה אחד לפחות (אם אין מקרה בשלושת הימים — להוסיף בדיקת-יחידה על גיאומטריה סינתטית + להגיד NOT-COVERED).

### T-301b — אופציה ב' ("נכשל להתקדם" בלי שבירת-צוואר) — **צל בלבד**
- מפיק: N ברים (ATR-יחסי לפי פסיקת 28.08 18:55, לא נקודות) מתחת לתקרה-כפולה בלי שיא חדש ⇒ שורת-לוג `[StructureExit] STALL-UNDER-DOUBLE shadow` + מונה בריפליי. אין פעולה. מייקל יכריע על N אחרי שיש מספר.

### דיווח
שורות TASK_LOG T-298…T-301 (סטטוס + הצעד-הבא), LOG ב-LIVE_CHANNEL עם פלט-הריפליי והרנס הגולמיים (Rule 5). ריסטארט — רק על מילה שלי.
