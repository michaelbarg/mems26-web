# תוכנית-עבודה + משימות · 2026-07-14 (יום-לייב)

**מצב-רגע:** ‏is_sim=1 (סיירה בסים) · In:22=1 (מזוין) · flag_guard 62 · fire_drill 🟢.
**סטטוס-רשימה בלבד — לא לביצוע עד אישור-מייקל פר-שורה.**

---

## 0 · הבהרה קבועה (לא-משימה, לתיעוד)
- **In:22 (Sierra UI) = EnableOrderPlacement = חימוש בלבד** (0=כבוי / 1=שולח-הזמנות). *לא* קובע סים/לייב. התווית "1=DEMO" בקוד מטעה.
- **סים/לייב = ‏GlobalTradeSimulationIsOn של סיירה** = `is_sim` ב-state. לייב = לכבות Trade-Simulation-Mode בסיירה (→ is_sim=0), In:22 נשאר 1.

---

## 1 · לתיקון/פתיחה **היום** (בטוח, לא נוגע בנתיב-המסחר החי; בהמתנה לאישורך)

| # | משימה | סוג | למה היום | אחראי |
|---|--------|-----|----------|-------|
| T1 | **מוניטור-אמת "סיירה חיה + עסקה פתוחה + נסגרה-על-מימוש"** — endpoint+פאנל שמוכיח חי: state טרי (<10ש') · position_qty>0 = עסקה · ירידת-qty על fill = מימוש/סגירה, עם חותמות-זמן | בקאנד+פרונט (read-only) | אתה רוצה לדעת בוודאות שהמערכת "יודעת" מה קורה בסיירה בלייב | 🔵 |
| T2 | **טבלת-Google Sheets מסודרת** — הרחבת ‏GSHEETS_TRADE_LOG (כבר דלוק): כל עסקת-לייב מ**סיירה** (entry/exit/חוזים/מימושים) + **שורת-סיכום-כסף** (P&L יומי/מצטבר, R) | אינטגרציה (קורא סיירה/DB) | דרישתך — נתוני-אמת מסיירה עם סיכום-כסף | 🔵 |
| T3 | **תיקון-תווית In:22** בקוד ה-DLL ("0=OFF,1=ARMED", לא "DEMO") + הבהרה בלוג | DLL — **טקסט-בלבד** | מונע בלבול-סים/לייב חוזר. **⚠️ נדרש re-add — לא היום בלייב; רק להכין את השינוי בגיט** | 🔵→🟣 |
| T4 | **התראות-לטלפון** — הדלקה+בדיקה (‏PHONE_ALERTS_V1) | בקאנד | ללייב, כשאתה לא מול המסך | ⚪ טוקן→🔵 |

## 2 · פעולות-שלך שחוסמות-לייב (לא-קוד)
- 🔴 כיבוי Trade-Simulation-Mode בסיירה (is_sim→0) + חשבון-אמת ממומן.
- ✅ In:22=1 כבר (מזוין).

## 3 · תוכנית-עבודה טורית **ממחר** (מדורגת, אחת-אחרי-השנייה)

**מדרגה P1 (קריטי לפני שסומכים על אוטומציה מלאה):**
1. **op=EXIT v2** (DLL, cancel-then-exit + never-naked) — 🟣 CC, פרומפט מוכן. חלון-סים.
2. **בדיקת-Sierra-על-המק (dev)** — פרומפט-CC מלא (ראה §5, ממתין לאישורך). מוודא שהמק יכול לשמש גיבוי-מסחר.
3. **הידרציה-מלאה בבוט** — 🟣 CC (שחזור שלב-S1+צוברי-S2 בריסטארט-אמצע-יום).

**מדרגה P2:**
4. ‏LaunchAgent לפרונטאנד (🟣 CC) — פרונט לא-נופל-בריבוט.
5. הדלקות-בהמתנה: ‏TPO-count · כיול-יעדים-3c (אחרי שבוע-נתוני-לייב).
6. הדרגת ‏SYSTEM6_AUTOCORRECT (אחרי שבוע-צפייה-נקי של ה-supervisor).

**מדרגה P3:**
7. ‏7 פיקסטורות-טסט ישנות (🟣 CC).
8. רעיונות טבלה-5: קורלציית-ES/NQ · צילום-גרף-אוטומטי · תרגול-כאוס · הקשר-רב-יומי.

## 4 · מנגנון עדכון-יומי (מתבצע אוטומטית)
- **כל בוקר 15:55:** בריפינג + ‏GO/NO-GO + מה-תוקן-אתמול (מ-STATUS_BOARD).
- **כל ערב 23:40:** לולאת-למידה + מה-נבדק-היום.
- **מסמך-זה מתעדכן:** כל פריט שנסגר עובר ל"בוצע-ואומת" עם ראיה; כל בעיה-חדשה נכנסת למדרגה הנכונה. הלוח החי ב-/board משקף בזמן-אמת.

## 5 · ⏸️ ממתין-לאישורך: פרומפט-CC לבדיקת-Sierra-על-המק
לא הגיע אישור ש-Sierra עובדת על מכונת-המק (dev). כשתאשר — אכין פרומפט-בדיקה מלא ל-CC על המק: אימות DLL-נטען · state-נכתב · פקודת-BUY-סים עוברת · fills-נקראים · reconciler-מזהה — הכל על סים, עם ראיות. **כרגע רק רשום; לא מבצע.**
