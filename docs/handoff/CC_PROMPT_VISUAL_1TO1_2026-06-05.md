# CC — UX/UI 1:1 לפי ההמחשות (נאמנות-ויזואלית, לא פונקציונליות) · 2026-06-05

חוזה + `CC_VERIFICATION_PROTOCOL`. **המטרה: העמודים החיים ייראו זהים (פיקסל-לפיקסל) להמחשות.**
הפונקציונליות כבר קיימת — **חסרה הנאמנות-הויזואלית.** העיצוב-הישן **נזרק במלואו**.

## מקור-אמת ויזואלי (ה-spec הפיקסלי — בנֵה 1:1)
- **Build Status:** `docs/plans/BUILD_STATUS_REDESIGN_MOCKUP_V2_2026-06-04.html`
  → היעד עבור route `/build` (`BuildTreeView`) **וגם** הטאב בדאשבורד (`BuildStatusTab` ב-`V9Dashboard`).
- **Trades:** `docs/plans/TRADES_PAGE_PROTOTYPE_2026-06-03.html`
  → היעד עבור route `/trades` (`TradesView`). (משתמש בטוקנים אמיתיים מ-`app/globals.css`.)

## מה לעשות
1. **פתח כל המחשה בדפדפן** (קובץ HTML) **לצד העמוד החי**, והשווה: layout, grid, צבעים (טוקנים),
   טיפוגרפיה (font/size/weight), spacing/padding, סדר+מיקום הרכיבים, גבולות, כותרות, badges, טאבים.
2. **שכתב את ה-React/CSS כך שהעמוד החי = ההמחשה 1:1.** השתמש באותם טוקנים מ-`globals.css`
   (ההמחשה כבר עושה זאת). אל תשמר שום אלמנט מהעיצוב-הישן — **החלף, אל תוסיף.**
3. **איחוד דאשבורד↔/build:** הטאב "Build Status" בדאשבורד (`BuildStatusTab`) צריך להציג את אותו
   עיצוב-חדש (או להפנות ל-`BuildTreeView`), כך שאין שתי גרסאות. (Michael ראה את הישן בדאשבורד.)
4. שמור את **כל הנתונים-החיים** שכבר מחווטים (pre_fire/risk_checks/Edge-Matrix/presets) — רק העטיפה הויזואלית משתנה.

## VERIFY (חובה — השוואה ויזואלית, לא טקסט)
- **צילום side-by-side**: ההמחשה מול העמוד החי (`/build`, `/trades`, ודאשבורד-טאב) — צריך להיראות זהה.
- רשימת-הפרשים שתוקנו (layout/color/type/spacing).
- אם פריט בהמחשה לא ניתן ל-1:1 (נתון חסר) — סמן ב-NOT-DONE עם הסיבה.
- typecheck/build עוברים.

**זה visual-fidelity בלבד — אל תיגע בלוגיקת-נתונים/backend.** המחשה = החוק.
