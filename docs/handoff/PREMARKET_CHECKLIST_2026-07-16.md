# צ'ק-ליסט קדם-שוק מקיף — cc-imac · 2026-07-16 (פסיקת-מייקל 10:05: "בדיקות מקיפות כבר עכשיו")

**מבצע: cc-imac. דדליין: 14:30 (באפר לפני דוח-15:30). הכל מתועד ב-EXECUTION_REPORT עם ראיות (פקודה+פלט ≤5 שורות).**
**כלל-על: לפני כל פקודת-מסחר — ‏gate: ‏`is_sim==1` ב-sierra_state טרי (<15ש'). ‏is_sim=0 ⇒ עצור מיד.**

## שלב 0 — מעבר לסים (לפני הכל)
1. ‏Sierra: ‏Trade → **Trade Simulation Mode ON** (מייקל שם — בקש ממנו, או ‏computer-use באישורו).
2. אמת: ‏sierra_state ‏is_sim=1 · מחיר-שפוי (בדיקת-996150) · ‏qty=0 · ‏working=0.
3. רשום שעת-מעבר. **שלב 5 מחזיר ללייב — חובה לפני 16:00.**

## שלב 1 — דימוי-שערים לכל תבנית (אין לה חסמים?)
לכל תבנית × הקשר-דוקטרינה-נכון, ‏POST ל-`/api/v9/gateway/route_setup` עם סטאפ כשר (מיקום/כיוון נכונים, ‏T1 בכיוון, סטופ סביר) ואמת ב-`/api/v9/gateway/decisions` שהתוצאה **לא-חסומה** (צל/דמו). ואז מקרה-הפוך אחד (מיקום-שגוי) ואמת שנחסם **בשער הנכון**:
- **S2:** ‏REACTIVE_LONG/SHORT · INITIATIVE_LONG/SHORT · INVERSE_HNS/HNS_TOP · DOUBLE_BOTTOM_EE/DOUBLE_TOP_AA · BULL_FLAG/BEAR_FLAG.
- **S4:** ‏ZLR · GB100 · FAMIR · HFE · CONT_TREND (+כל תבנית חיה אחרת ב-playbook).
**תוצר: טבלה תבנית → עבר-כשר ✅ / נחסם-שגוי ✅ / חסם-לא-צפוי ❌ (+שם-השער).** כל ❌ על סטאפ-כשר = ממצא-קריטי לתיקון לפני 16:30.

## שלב 2 — כל הפקודות האפשריות על סים (פרוטוקול-DLL מלא)
רצף מלא, ראיה לכל צעד (‏trade_result r>0, ‏sierra_state, לוג):
1. ‏PLACE ‏BUY 4 (‏T0=+3.5 סולם מלא) → **8 הוראות / 4 זוגות**, מחירים=מבוקש.
2. ‏MODIFY_STOP → **כל 4 הסטופים** זזים (אנטי-רגרסיית-"1/2").
3. פגיעת-יעד חלקית (תן ל-T0 להתמלא או קרב אותו) → ‏fill נקלט, זוג-1 נסגר, סטופים נשארים ל-3.
4. ‏FLATTEN_ACCOUNT → ‏flat מלא + 0 working.
5. ‏PLACE ‏SELL 4 → ‏CANCEL (לפני מילוי) → נקי.
6. ‏STATUS → ‏ACK. ‏**op=EXIT — לא לבדוק (שבור-ידוע, אסור-קריאה).**
כשל בכל צעד = ממצא-קריטי + עצירה.

## שלב 3 — מימוש+סטופים פר-תבנית × סוג-יום (קריאה, בלי פקודות)
בנה **את הטבלה המלאה מהקוד/קונפיג** (stop_anchors.yaml · pattern_t1_points · TARGET_ZONES · ZLR_MGMT · daytype_playbook · resolve_stop):
- שורות: כל תבנית. עמודות לכל סוג-יום (7): מותרת? (playbook+location) · עוגן-סטופ (מבני/ATR-רצפה 0.8 ברוטציה) · ‏T0/T1/T2/T3 (מקור: מדף/זון/R) · חלוקת-חוזים (ZLR 2/1/1 · אחרות 1/1/1/1) · ‏BE-אחרי-T1 · מונוטוניות-סולם.
**סמן כל תא שסותר את האפיון** (למשל REV מותר-במרכז, סטופ<רצפה, T1<T0). זו התשובה ל"איך כל תבנית מיישמת מימוש וסטופים בכל סוג-יום".

## שלב 4 — איפה נקלטות העסקאות (שרשרת-רישום end-to-end)
על עסקת-סים אחת מלאה משלב 2, הצג את **אותה עסקה** ב-5 המקומות עם מספרים תואמים:
1. ‏`trade_command.json` (הפקודה) → 2. ‏`trade_fills.json` (מילוי-סיירה) → 3. ‏`v9_trades` (שורת-DB: ‏id/mode/entry/pnl) → 4. ‏ledger (‏`/api/v9/live_ledger`) → 5. דשבורד+כיס (‏TradesView / ‏/api/v9/mobile/data active).
פער בכל חוליה (fill בלי שורה, מחיר-זבל, mode שגוי) = ממצא-קריטי (זו משפחת-היתומים של אתמול).

## שלב 5 — חזרה ללייב + חתימה
1. ‏flat מוחלט (qty=0, working=0) → ‏Trade Simulation Mode **OFF** → אמת ‏is_sim=0 + מחיר-שפוי.
2. ‏flag_guard מלא · ‏post_restart_verify · חימוש (Input 22=1) · ‏fire_drill קריאה (effective=4).
3. דיווח-סיכום ב-SYNC: ‏"צ'ק-ליסט: X✅ Y❌ · חזרה-ללייב HH:MM · ממצאים-קריטיים: …" + הטבלאות ב-EXECUTION_REPORT.
