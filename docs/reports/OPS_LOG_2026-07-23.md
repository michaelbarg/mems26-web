# OPS LOG — 2026-07-23

לוג-תפעול מרכזי (N12): כל הוואצ'רים/המתזמנים/הסוכנים כותבים לכאן. append-only, מסודר כרונולוגית.

[2026-07-23T01:39:58-04:00] [cowork-dev] [WARN] ROOT of the 12 regressions FOUND (cowork forensic): ALL 12 fail returning the SAME value 'Trend_Normal' = today's LIVE day-type. Tests reach the REAL running backend.main app.state.day_type_machine instead of their mocks. Introduced by cc c556a5bf day-type path (boot-seed/demotion): the demotion code MUTATES day_type_machine.day_type = _dem_enum + _last_state.day_type on the shared machine, and the boot-seed imports backend.main during tests -> get_live_day_type resolves the live machine (Trend_Normal). ONE root, 12 symptoms, order-dependent (pass alone). Fix = cc: test-isolate (don't reach real backend.main / reset day_type_machine between tests) OR guard the mutation; NOT a per-test fix. My import_module attempt did not help (reverted). WHY-SLOW: cc declared done 5x without running the gate + no green baseline + DB wedge lost 95min. Recommend: ONE owner, single gate 145/145, no other work until green.
[2026-07-23T01:57:16-04:00] [cowork-dev] [INFO] S1 GROUND TRUTH (Michael, S1 authority): 07-21 = Variation (NOT Trend). Canonical classify_replay AGREES: 07-21 = Normal_Variation UP -> confirms S1's brain is CORRECT when read canonically; the bug is live-machine plumbing (escalation-only + boot-seed from wrong engine), not the classifier. Fixed demotion spec: counter-fixture was wrongly 07-21; real Trend day for 'must-not-demote' = 07-16 (Trend_Normal DOWN, canonical-verified). Reinforces: boot-seed-canonical (P6b) is the right fix — pipe the correct canonical conclusion to the live machine.
[2026-07-23T02:05:06-04:00] [cowork-dev] [WARN] CLEANUP MASTER -> cc (Michael: 'let cc clean it properly'): CC_CLEANUP_MASTER_2026-07-22.md — 6 stations ordered 1(regressions gate)->4(idle-txn leak)->2(TS root)->3(VA canonical)->5(enable+sim)->6(faults sim-verified). Station-1 exact root handed: 12 regressions all leak live 'Trend_Normal', cc demotion mutates shared machine + boot-seed reaches backend.main in tests -> fix by isolation fixture, target 145/145. Contamination A5 reservation: verify =0 lands bars at correct ts not -1h. cc owns cleanup; cowork verifies symmetric; return-to-live immediate on green.
[2026-07-23T02:52:58-04:00] [cowork-dev] [WARN] STATION-1 GATE GREEN (cowork completed cc's half-fix): cc conftest handled sys.modules but NOT the env — real root: 3 test modules import backend.main AT COLLECTION -> env_loader floods ~150 production flags (DAYTYPE_PATTERN_AWARE_V1=1 flips position_gate branch etc). Fix: conftest captures BASELINE env at conftest-import (pre-collection) + autouse restore per test (preserving PYTEST_* keys). Results: -k gate 148 passed/0 failed/0 errors (was 12F); full dir 114F->43F (fix saves 71). vs parent baseline 28F: 15 residual = 1 intended-change (test_tzoffset_exactly_1h asserts old hour-fix default, needs update per P2 ruling) + 14 env-dependent tests (dll_exit_monitor/pipeline5/orphan_place_stop read .env paths, previously fed by the flood) -> test-debt for cc: give them explicit env setup. NO production regressions remain in the gate.
