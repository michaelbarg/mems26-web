# cc — משימה: רצפת-סטופ מותאמת-יום-מגמה (TREND_STOP_FLOOR_V1, בנה-OFF)

**הבסיס (EFFICIENCY_2026-08-03.md §Q2):** ביום-המגמה אתמול כל 4 ההפסדים היו עסקאות בכיוון-
הנכון שנעצרו על רעש (סטופ 2.5-4.0 נק'); רצפת-DALTON למדגם 8.59 נק'. ‏replay: 4/4 היו מגיעות
ל-T1 עם רצפה, 0/4 נגעו ברצפה ⇒ ‎+$412.50 באותו יום. ‏MAE-כיול: מנצחות-מגמה נושמות יותר ממאזן.

**הכלל (מייקל 04.08 — "תיקון סטופים לסוג-היום הזה"):**
כשהסיווג-החי הוא Trend_* (או Variation עם רגל-חיה בכיוון-העסקה), והעסקה עם-המגמה:
`stop_final = clamp(max(stop_structural, TREND_STOP_FLOOR_PTS), pattern_max_risk)`
- `TREND_STOP_FLOOR_PTS = max(6.0, 0.15 × IB_width)` (קונפיג ב-stop_targets.yaml, לא קבוע-בקוד).
- חל רק לפני-T1 (אחרי-T1 → BE כרגיל; MAE-scratch ממשיך לגבור כרשת-ביטחון ב-8+ נק').
- לא חל: ימי-מאזן/Neutral (שם סטופ-צר נכון — mean-reversion), עסקאות נגד-המגמה, ופתיחה
  (לפתיחה יש תקרות משלה 15/25).
- יחידת-בקרה: $-סיכון עם 3 חוזים = רצפה 6-9 נק' ⇒ $90-135 לעסקה — בתוך התקרות. אם
  הרצפה גוררת סיכון>תקרת-התבנית — SIZE-CUT (לא הרחבת-תקרה), עקבי עם SIZE_CAP_CUT.

**קבלה (לפני כל הדלקה):** replay על 14 ימי-האמת — ‏(א) ימי-המגמה (כולל אתמול): ההפסדים-
שנחסכו מול עלות-הרצפה, ‏NET חייב חיובי; ‏(ב) ימי-המאזן: אפס-שינוי (הרצפה לא חלה); ‏(ג) טסטים:
עם-מגמה/נגד-מגמה/מאזן/פתיחה/גבול-תקרה. דוח + RULED_FLAGS pointer ⇒ פסיקת-מייקל להדלקה.
