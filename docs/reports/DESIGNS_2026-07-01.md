# MEMS26 · טיוטות-עיצוב — 2026-07-01

לכל בעיה: **שורש · השינוי-המוצע (קובץ/שכבה) · regression שיוכיח · trading-logic (→Michael) או display/safe.**
**אל תממש — טיוטה מוכנה-לפרומפט-CC.** מסומן אם דורש הצלבת-Sierra/לוג תחילה (Rule 2/5).

> **רקע-יום (07-01):** 🟢🟢 **אבן-דרך — DEMO end-to-end לסיארה, וזכתה** (261 ZLR LONG, +$71.25). המערכת **יורה שוב** (11 עסקאות) אחרי `d4363a1` + `DAYTYPE_POSITION_GATE=0`. **הזיהוי היה נכון-בכיוון** (בוקר-LONG / אחה"צ-SHORT) — **ההפסד נבע מ-execution-layer:** short-targets מבניים-רחוקים חנקו 2 שורטים-מנצחים (≈+11R אבודים). דוח-זה מתעדף את תיקוני-ה-execution שיהפכו את הזיהוי-הנכון לרווח.

---

## עדיפות-1 (עלות-#1 של היום) — D-short-target-intermediate (I-54) (🔴 trading-risk → Michael) · **דורש הצלבת-CC**

**שורש (raw, מאומת):** 3 שורטים (266/267/268) קיבלו `T1=7482.125 · T2=7458.25 · T3=7434.375` — **רמות-מבניות-קבועות, 88–92pt מתחת לכניסה** (7570–7574.5). ביום-טווח-73pt השורטים ירדו +27..+31.5pt (best ~7543) אך **מעולם לא נגעו ב-T1** → 267/268 נסגרו **שטוח ב-EOD_MANUAL** (CF: +6.2R/+4.9R = **+11R אבודים**), 266 ניצל רק ב-TIME_STOP. ה-target-map מחזיר את הרמה-המבנית-הבאה **בלי target-ביניים** כשהיא רחוקה.

**השינוי-המוצע (→Michael):** ב-שכבת-בניית-ה-targets (target-map / sizing) — כש-`distance(entry→T1_structural) > K×risk` (למשל K=4 או > מרחק-ATR), **הכנס T1-ביניים R-based** (למשל 1.5–2R) לפני הרמה-המבנית, כך ששורט-מנצח פודה חלק לפני שהמבני הרחוק נדרש. שמור על ה-T2/T3-מבניים לרַנֶר. **אל תמחק את הרמות-המבניות** — הוסף רק ביניים כשהמבני מחוץ-לטווח-סביר.

**regression:** `test_short_target_intermediate_when_structural_far.py` — short entry 7570, structural T1=7482 (88pt), risk=5.75pt → assert `T1_effective ≤ entry − K×risk` (target-ביניים קיים) · `T3` נשאר מבני. replay 07-01-fixture (267/268) → assert פדיה חלקית ≥+2R לפני-EOD.

**סיווג:** **trading-risk-surface → Michael.** **מקור-אמת:** להצליב את הגדרת-ה-target-map (מבני מול R/hybrid) + לאמת ש-7482 הוא הרמה ולא באג-סימן, לפני-מימוש.

---

## עדיפות-2 — D-opening-cvd-ts-cast (I-53) (🔴 trading-logic → Michael) · **handoff/verify קיים (STATUS_BOARD 06-30)**

**שורש (raw, מאומת ב-verify):** `_compute_opening_cvd_pos()` (`opening_type_gate.py:109`) מריץ `(ts AT TIME ZONE 'America/New_York')::date` על `v9_bars_cumulative_delta`, אך **`ts` = `character varying`** → `timezone(unknown, character varying) does not exist` → **נבלע ב-`except Exception: return None`** → `cvd_pos=None` תמיד → `detect_opening_type` נקרא **בלי CVD** → OPEN_DRIVE נפלט ללא בדיקת-ספיגה ⇒ `OPENING_FIRE_CVD_V1` (fix `f1304b6`) **inert**. מפר Rule-1 (no-silent-failure).

**השינוי-המוצע (→Michael):** (1) הטל `ts::timestamptz` בשני ביטויי-ה-`AT TIME ZONE` (או אחסן `ts` כ-timestamptz בטבלה); (2) **החלף את ה-bare-except-הבולע ב-fail-loud** (log.warning + propagate `None` מפורש), Rule-1. אחרי-cast: אמת ש-OPEN_DRIVE **נדחה/מאושר** לפי CVD בפועל.

**regression:** `test_opening_cvd_ts_cast_returns_rows.py` — קרא `_compute_opening_cvd_pos` על fixture → assert `cvd_pos != None` (6 שורות, לא-בלוע) · `test_opening_cvd_except_is_loud.py` — SQL-error → assert log.warning נרשם (לא-שקט).

**סיווג:** **trading-logic → Michael.** **מקור-אמת:** CC לאשש `(B) ts::timestamptz → 6 rows` מול `(A) code-SQL → ERROR` (STATUS_BOARD verify).

---

## עדיפות-3 — D-pnl-r-formula (I-22, שוחזר) (🔴 trading-logic → Michael) · **חוסם-דיווח לפני-LIVE**

**שורש:** `pnl_r` מנופח — shadow Σ=+57.83R מול −$103.12 USD; demo=+38 על עסקה-אחת. ה-R מחושב מול **stop-מטויל-קרוב (post-BE)** במקום risk-התחלתי (`entry − stop_initial`). כל ΣR/win-rate מ-pnl_r פסול (היום עקפנו עם USD/pts).

**השינוי-המוצע (→Michael):** קבע `pnl_r = pnl_pts / |entry − stop_initial|` (risk-התחלתי הקבוע), **לא** `stop`-הנוכחי-המטויל. אחסן `risk_pts` בכניסה ושמור אותו לחישוב-R לאורך-חיי-העסקה.

**regression:** `test_pnl_r_uses_initial_risk.py` — עסקה entry 7537 stop_initial 7535.5 (risk 1.5) → T3 +14pt → assert `pnl_r ≈ 9.3` (לא 38); trail-to-BE **לא** משנה את המכנה.

**סיווג:** **trading-logic (מדד-סיכון) → Michael.** design קיים מ-I-22; לאמת שלא-יושם.

---

## עדיפות-4 (שורש-העל, ממשיך) — D-daytype-source-consistency + gate re-enable (I-44/I-51) (🔴 → Michael) · **handoff קיים**

**שורש (ממשיך):** gate (`daytype_position_gate.py`) + S2 Auth (`auth_table_v1.py:52,144`) קוראים day_type-תקוע ולא 7-טיפוסי-חי. **מצב-חדש היום:** ה-gate **כבוי** (`DAYTYPE_POSITION_GATE=0`, validation) → 07-01 מדגים **under-block** (counter-fade LONG 269/270 עברו, −2R), כמראה-הופכית ל-06-30 **over-block**. שני-קצות אותו כשל-source/כיול.

**השינוי-המוצע (ב-handoff `CC_DAYTYPE_SOURCE_CONSISTENCY_2026-06-30.md`):** (1) gate+Auth יקראו מקור-7-טיפוסי-חי; (2) שער מודע-כיוון/location (לחסום counter-fade, להתיר with-trend) ולא label-בלבד; ואז **החזר `DAYTYPE_POSITION_GATE=1`** — **החלטת-Michael**, אחרי-source-fix, SHADOW-first.

**regression:** `test_gate_reads_live_daytype.py` + `test_gate_blocks_counter_fade_allows_with_trend.py` — replay 07-01: assert 269/270 (counter-fade LONG) נחסמים · 266/267/268 (with-fade SHORT) עוברים. replay 06-30: CONT-LONG עוברים.

**סיווג:** **trading-risk-surface → Michael (SHADOW first).** שורש-העל; **זוג-fixtures 06-30(over)/07-01(under)** = מבחן-הכיול.

---

## D-daytype-stability (I-1 המשך, 🔴 → Michael) · **שוחזר בשלישית**

**שורש:** day_type מרצד גם ב-**stamp-על-עסקאות** היום: `UNKNOWN×2 → Trend_Normal×2 → Variation×7` בתוך סשן-אחד. החלטות-שער (כשיוחזר) רוכבות על קריאה-רגעית-שבירה. escalation-only/hysteresis לא-נאכף.

**השינוי-המוצע (→Michael):** אכוף hysteresis/escalation-only על `current_day_type` post-lock (אל-תרד-מ-Trend ל-balanced בלי-סף); הצמד stamp-העסקה ל-day_type-הנעול. מקור: `day_type_machine`.

**regression:** `test_daytype_no_flicker_postlock.py` — סדרת-ברים-מעורבת → assert stamp-עסקה לא-מרצד מתחת-סף.

**סיווג:** **trading-risk-surface → Michael.** echoes I-44.

---

## D-I53b — `cvd_continuous` ל-STREAM_NAMES (🟢 display/safe)

**שורש:** `[StreamHealth] unknown stream: cvd_continuous` כל ~2-3ש — `cvd_continuous` חסר מ-`STREAM_NAMES` (`stream_health/health.py:21`); ה-POST (`bars.py:1047`) קולט נתונים; **לא-קשור** לנתיב-CVD-של-הירי. **שינוי:** הוסף `'cvd_continuous'` ל-STREAM_NAMES. **regression:** `test_stream_health_knows_cvd_continuous.py`. **סיווג:** display/safe.

---

## D-limit-cap (I-25, 🟢 safe/quick) · **שוחזר יום-אחר-יום**

**שורש:** `/trades/recent?limit=200`→422 (cap=100). **שינוי:** הרחב cap ל-≥200 **או** עדכן את מפרט-סוכן-ה-EOD ל-limit=100 (הסוכן כבר עושה fallback אוטומטי, אך ה-422 מלכלך). **regression:** `test_trades_recent_limit_200_ok.py`. **safe.**

---

## Watch-items (לא-design-מיידי — דורשים עוד-דגימות)

- **🟡 I-55 (INITIATIVE_LONG stop premature?):** 262/263 נעצרו −1R על pullback-רדוד ואז הראלי המשיך 30pt. **n=2 — לא-מספיק.** stop-צמוד גם חוסך הפסדים בימי-כשל. **watch רב-יומי** — אם חוזר, design ל-anti-noise-stop / structure-aware-stop. **לא-לשנות עכשיו.**
- **🟡 I-56 (shadow↔demo divergence):** 260(shadow)/261(demo) אותו-סיגנל, תוצאה-הפוכה (scratch מול T3). חשד: exit-logic-נפרד. משיק ל-I-22/I-23 (shadow-accounting). **watch** — לאמת ש-shadow-sim ו-demo-execution חולקים exit-model לפני-LIVE.

---

## ממשיכים — פתוחים

- **⚪ D-guards-bus (I-48+D-037+D-049+I-23):** 4 stops היום אך `cooldown.consecutive_stops=0` post-close — **לא-נבחן חי** אם demo/shadow-stops דורכים cooldown/cluster. עיצוב מ-DESIGNS_2026-06-29 עומד. **trading-safety → Michael.** דורש הצלבת-CC.
- **🟡 I-23 (gateway-counters):** `trades_today=0/daily_pnl=0` מול 11 עסקאות. design קיים.
- **🟡 D-daytype-source-endpoint (I-44 display):** `/day_type/state` קפוא (זהה-ל-06-29/06-30). הפנה ל-`app.state.day_type_machine`. safe. כדאי לאחד עם עדיפות-4.

---

## סדר-עדיפויות מוצע

1. **D-short-target-intermediate (I-54)** — עלות-#1 של היום (+11R אבודים); הופך זיהוי-נכון לרווח. →Michael + הצלבת-CC.
2. **D-opening-cvd-ts-cast (I-53)** — silent-failure; fix קטן ומדויק; verify קיים. →Michael.
3. **D-pnl-r-formula (I-22)** — חוסם-דיווח לפני-LIVE. →Michael.
4. **D-daytype-source-consistency + gate re-enable (I-44/I-51)** — שורש-העל; handoff קיים; זוג-fixtures 06-30/07-01. →Michael, SHADOW.
5. **D-daytype-stability** — flicker (שוחזר ×3). →Michael.
6. **D-I53b + D-limit-cap** — safe/מהירים.
7. **D-guards-bus** — חוסם-LIVE, לא-נבחן. →Michael + הצלבת-CC.

**הערה:** אבן-הדרך של היום (DEMO→Sierra) פותחת את פאזת-ה-DEMO החיה; מכאן ה-execution-layer (targets/stops/gate/R) הוא ה-lever. **פריטי trading-risk → Michael.** **שום-קוד לא-שונה בריצה-זו** (read-only EOD).
