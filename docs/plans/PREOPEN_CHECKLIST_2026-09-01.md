# צ'קליסט לפני-פתיחה · 01.09

**נבנה 14:20 · הקפאה 15:50 · ריסטארט 16:00 · שער 16:10 · פתיחה 16:30**
**בקשת-מייקל:** *"מה חסר, מה בוצע כמו שצריך."*

**‏🔴 הבקאנד החי עלה ב-11:22. `.env` נגע ב-13:12.** ⇒ כל מה שנוסף אחרי 11:22
**אינו חי כרגע**. הריסטארט ב-16:00 הוא מה שהופך את הרשימה למציאות.

---

## ✅ בוצע ואומת — יש ראיה, לא רק קוד

| # | מה | הראיה |
|---|---|---|
| 1 | **‏T-178** שחרור-סלוט אחרי phantom | `test_phantom_slot_release.py` — **6 טסטים + מוטציה**: החזרת ה-`if` ⇒ נכשל |
| 2 | **‏T-177** flush לפני return | הוכח חי ב-18:09:30 על #939 |
| 3 | **‏T-152** ניפוח-הצל | נמדד **2.42×**, הלוח **6.37×** — `shadow_slot_scan.py` |
| 4 | **‏T-183** אזעקת-סלוט-תקוע | `test_stuck_live_slot_alarm.py` |
| 5 | **תקרה/רצפה** גלאי | `test_ceiling_floor_state.py` · רץ `shadow` מ-29.08 |
| 6 | **‏op=EXIT** עדיין `r=-1` | נבדק בסים — **ההשערה שלי הופרכה**, לא לבנות עליה |
| 7 | **‏T-202** היפותזת-BE | **הופרכה** — הפסיקה שלך מ-14.07 עומדת |
| 8 | **‏flag_guard** | **228 PASS**, כל דגל-ON עם אתר-קריאה בייצור |

---

## 🔴 מצב-האימות — **הגרסה הראשונה כאן הייתה שגויה. זו המתוקנת (15:05)**

**מה כתבתי ב-14:20:** *"אפס טסטים"* על 5 פריטים, ו-*"2 מ-7 מוגנים"*.
**חיפשתי ב-`tests/` בלבד. יש שני שורשי-טסטים בריפו הזה.**

```
tests/                 ← חיפשתי כאן
backend/v9/tests/      ← וכאן CC בנה את רוב הטסטים של היום
```

**‏CC בנה טסט כמעט לכל פריט — ו-110 מהם עוברים.** התיקון מגיע לו.

| # | מה | המצב האמיתי |
|---|---|---|
| 1 | **‏`RISK_BUDGET_SIZING_V1`** | ✅ `backend/v9/tests/` (‏CC) **+** `tests/v9/regression/` (אני, 15:00) — **17 טסטים, 2 מוטציות נתפסו**: `floor→round` ⇒ 5 נכשלו · `דחייה→clamp` ⇒ 7 נכשלו |
| 2 | **‏`quality.entry_stop`** | ✅ `test_entry_stop_immutable.py` — **כולל שער אתר-כתיבה-יחיד** שביקשתי. הוא כן נבנה |
| 3 | **‏`va_sanity`** | ✅ `test_va_sanity.py` · **אפס צרכנים** (לא נדרש — ראה למטה) |
| 4 | **‏`entry_location_quality`** | ✅ `test_entry_location_quality.py` · **אפס קוראים** ← זה הפער האמיתי |
| 5 | **‏`OPENING_DRIVE_SKIP_V1`** | 🟠 **אין טסט** — הפריט היחיד שבאמת חסר |

## 🔴 ובמקום זה — הפער האמיתי, וגרוע מ"אין טסטים"

**‏`fire_drill.py` אינו מריץ `pytest` בכלל.** אין `testpaths` בשום קובץ-קונפיג,
והפעלות-ה-pytate היחידות ב-`scripts/` הן UAT ישנים שמצביעים על `tests/`.

**⇒ הטסטים קיימים, עוברים — ואף אחד לא הריץ אותם.** ‏`test_entry_stop_immutable`
הוא הדוגמה החדה: כל תפקידו להיכשל ברגע שנוסף אתר-כתיבה שני, **והוא מעולם לא רץ.**

**נסגר 15:05 — `scripts/guard_tests.sh`:** מריץ את השומרים **בלבד** משני
השורשים (‏**110 עוברים**). ‏🔴 **מכוון בכוונה צר** — הסוויטה המלאה נושאת ‎~490
כשלים פרה-קיימים, ושער-סשן עליה היה חוסם את הפתיחה מסיבות שאינן קשורות לסיכון.
קובץ-שומר שנעלם ⇒ **כישלון**, לא ריצה קטנה יותר.

**‏⇒ לחווט ל-`fire_drill` — אחרי הפתיחה, לא לפניה.**

---

## 🟢 תיקון-שורש: `T-203` **אינו חוסם** — וזו טעות שלי שעלתה לנו את היום

אמרתי הבוקר *"אזור-הערך שגוי ב-7 מ-8 ימים, 4 צרכנים חיים מורעלים"*. **בדקתי את
המקור הלא-נכון.**

```
tpo.json (הייצוא החי, נכתב 13:22):
  previous_session   VA=49.5   טווח=70.75   →  70%   ✅ תקין בדיוק
  session (לילה)     VA=14.5                          חלקי — הפתיחה טרם
v9_tpo_sessions (הטבלה):  8-33% מהטווח                ❌ שבורה
```

**הייצוא נכון. הטבלה שבורה. והצרכנים החיים (`location_gate:1649`) קוראים
מהייצוא.** ⇒ **פסיקות-הדלתון שלך פועלות על רמות נכונות. רק המדידות שלי היו
מורעלות.**

**זו בדיוק מחלקת 22.06** (`v9_bars_5min` מול `v9_bars_5min_woodies`) —
**מדדתי מהטבלה במקום מהמקור החי.** ‏CC כתב זאת בקומיט `9dc4848c` לפני שעה,
ואני קראתי ולא המשכתי.

**מה שמשתנה בגללו:**
- **מחקר תקרה/רצפה אינו חסום.** אפשר להתחיל היום.
- **פוסל 3 של `ENTRY_LOCATION_QUALITY` ניתן לחיווט** — אם הקורא מזין מהייצוא.
- `T-203` יורד מ-🔴 חוסם ל-🟠 איכות-מדידה.

---

## מה חסר — לפי סדר, עד 15:50

| סדר | מה | למה עכשיו | מי |
|---|---|---|---|
| ~~1~~ | ~~טסט ל-`RISK_BUDGET_SIZING_V1`~~ | ✅ **בוצע 15:00** — 17 טסטים, 2 מוטציות נתפסו | cowork |
| ~~2~~ | ~~שער `entry_stop`~~ | ✅ **כבר היה** — טעות שלי | cc |
| **1** | **‏`guard_tests.sh` ל-`fire_drill`** | שומר שלא רץ אינו שומר — **אחרי הפתיחה** | cc |
| **3** | **שורש ATR-הפתיחה** — רצפה 2.1 מול 7.6 | **קבוצת-ההפסד "סטופ קרוב"**. אבחון לפני תיקון | cc |
| **4** | **לחווט `ENTRY_LOCATION_QUALITY_V1`** — עכשיו **שלושת** הפוסלים | פסיקת-28.08 שמעולם לא נכנסה. 4 אנקורים בדוקסטרינג | cc |
| **5** | **רפליי-מצפן** על 10 עסקאות-הפתיחה | מדידה, אפס קוד ⇒ אפס סיכון-פתיחה | cc |
| **6** | **טסט ל-`OPENING_DRIVE_SKIP`** | הוא ידלק ב-16:00 | cc |

**‏🔴 אם צריך לבחור — 1 ו-2 לפני 3-6.** דגל שקובע גודל-עסקה ורץ בלי טסט הוא
סיכון גדול יותר מכל תיקון שטרם נבנה.

---

## שער 16:10 — ירוק בכל השורות או שלא סוחרים את הפתיחה

```
□  is_sim = 0                      ← 🔴 מייקל בלבד. אזכיר ב-15:30
□  fire_drill                      GO
□  flag_guard                      PASS
□  פיד-ברים טרי (< 2 דק')
□  [env_loader] מראה OPENING_DRIVE_SKIP_V1=1 + RISK_BUDGET_SIZING_V1=1
□  live_slot = None · position_qty = 0
```

**פריט שלא אומת מלא ⇒ נוחת `shadow`, לא `live`.** חצי-בנוי בפתיחה גרוע
מלא-בנוי.
