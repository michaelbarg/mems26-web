# תוכנית-עבודה אחת — 2026-08-12 — "שהיום המערכת תירה על הזיהויים שלה"

**מקור:** `docs/research/QUALITY_BLOCKS_2026-08-10_11.md` (רפליי-איכות של כל חסימות 08-10/08-11) +
שתי ביקורות-48-הסשנים + הפורנזיקה. **מצב-פתיחה:** F1/F2/F4/F6 קומיטו הבוקר (06:39-07:00) אבל
ה-backend רץ מ-08-11 22:25 — **שום תיקון עוד לא חי**. פתיחה 16:30 IL. היום CPI (08:30 ET, אדום).
סדר לפי קריטיות-כסף; כל פריט: מה · בעלים · ראיה · חלון · אפקט-על-הירי-של-היום.

---

## 1) 🔴 להחיות את מה שכבר נבנה: snapshot → restart → אימות-בוט (זה F1+F2+F4+F6 ביחד)

- **מה:** `scripts/mems26_snapshot.sh "pre-restart-0812"` → ריסטארט backend
  (`launchctl kickstart -k gui/$UID/com.mems26.backend`) → אימות דרך שורת-הבוט של `[env_loader]`
  (לא `ps eww`) + `scripts/flag_guard.py` + `scripts/mems26_verify.sh`.
- **בעלים:** cc (הריסטארט על מכונת-הלייב) · cowork מאמת · זו בדיוק משימת ה-GO/NO-GO הפתוחה של exec-agent — **לא לפצל לשני ריסטארטים**.
- **ראיה:** backend PID 74390 עלה 08-11 22:25 < קומיטים 06:39-07:00; הפלייבוק נטען-פעם-אחת לקאש (`daytype_playbook.py:113-116`).
- **חלון:** לפני-פתיחה, מוקדם (לא בחלון-CPI 15:25-15:40 IL; רצוי עד ~14:00 IL כדי להשאיר זמן-הוכחות).
- **אפקט-היום:** בלעדיו — ORDER_FAILED עדיין בולע ניתובים (32%), ההזנה-הכפולה עדיין מרעילה את S2/D-RVX, והפלייבוק עדיין נותן ZLR-FULL/GB100-REDUCED ההפוך. **זה הפריט שכל השאר תלויים בו.**

## 2) 🔴 ניקוי קובץ-ההחלטות המזוהם לפני הפתיחה

- **מה:** הקובץ השוטף `~/SierraChart_Data/v9_export/gateway_decisions.jsonl` = **510 שורות-pytest בלבד**
  (07:00, `T-TEST`/`t1`-`t5`/entries 7405-7601). להעביר ל-`decisions_archive/gateway_decisions.2026-08-12.pytest-pollution.jsonl`
  ולהתחיל קובץ ריק. הארכיון האמיתי תקין (1.5MB, 07-22→08-11).
- **בעלים:** cc (פעולת-קובץ יחידה) · **ראיה:** QUALITY_BLOCKS §6.2 · **חלון:** לפני-פתיחה (עם/מיד-אחרי הריסטארט).
- **אפקט-היום:** context_radar/הידרציה לא ילמדו מ-510 שורות-פיקציה; לוג-ההחלטות של היום נקי למדידה.
- **נגזרת (אחרי-סגירה, cc):** לסגור סוף-סוף את חוב-ה-conftest — כותב-ההחלטות ל-`tmp_path` בטסטים; ואסור pytest על מכונת-לייב ב-RTH (הפורנזיקה §1, הופר שוב הבוקר).

## 3) 🟠 Rule-5 הוכחות-חיות אחרי הריסטארט (לפני הפתיחה, על נתונים חיים)

- **מה:** ארבע הוכחות מצוטטות ל-LIVE_CHANNEL: (א) בוט-ליין מציג `PRE_SEND_ENTRY_GUARD_V1` פעיל;
  (ב) אין יותר פרסום-אגרגטור לערוץ `"5min"` (גרעפ על backend.err — vol-סיירה בלבד, אין כפילות-ts);
  (ג) החלטת-playbook ראשונה מצטטת תא חדש (למשל `GB100 FULL on Variation`);
  (ד) בהחלטת-chase ראשונה עם רגל-חיה — בייפס עומד, **אין** `BYPASS REVOKED` (עד כה **0 כיסוי-חי** לפטור — פורנזיקה §3).
- **בעלים:** cowork (אימות) · **ראיה:** `trading_gateway.py:1651/:1680/:1707` · **חלון:** לפני-פתיחה + ההחלטה האמיתית הראשונה.
- **אפקט-היום:** בלי זה אין לנו הוכחה שהתיקונים באמת חיים — בדיוק הפער שקבר את 08-11 (הפטור קומיט 19:27, חי רק 19:47, כיסה 0).

## 4) 🟠 משמרת-פתיחה: לוודא שירי הופך לפוזיציה (יום-CPI)

- **מה:** מהפתיחה — מעקב חי: ניתוב-ראשון → פוזיציה (F1: retry-once/ntfy 🚨 על חסימת-guard) ·
  `ORDER_FAILED=0` בציפייה · ואם יש `-1` — לעצור ולקרוא את ציטוט-sierra_state לפני כל מסקנה.
  התראת-דחייה = לבדוק פוזיציה-עומדת (ownership!) לפני "אורפן".
- **בעלים:** cc-watch + מייקל (מחמש; cowork לא מחמש) · **ראיה:** F1 ב-LIVE_CHANNEL · **חלון:** during.
- **אפקט-היום:** 32% מהניתובים שמתו = ההבדל בין "המערכת ירתה" ל"המערכת הרוויחה". CPI ⇒ צפוי יום-תזוזה — בדיוק ימי-ה-+$400/+$620 של המדרגות, אם הביצוע מחזיק.

## 5) 🟠 F3 — `STEP_SCALED_LADDER_V1` (סולם-סטופ/יעדים לפי-מדרגה) — לבנות flag-OFF + רפליי ⇒ פסיקה

- **מה:** `five_min_system.py:1586+` זורק את סטופ-הגלאי ל-StopResolver ⇒ R=10-15 על מדרגה של ~11 ⇒ T1 בלתי-השגה
  (ב-08-11: 4 כניסות פתוחות, 0 נבנקו). סטופ `max(4, 0.6×מדרגה)`, יעדים 0.5/1.0/1.5×מדרגה. בנייה+טסטים+רפליי,
  דגל-OFF, ואז פסיקת-מייקל אחת (נרשמת ב-RULED_FLAGS).
- **בעלים:** cc (בנייה+רפליי) → מייקל (פסיקה) · **ראיה:** שלוש אנליזות בלתי-תלויות התכנסו (leg-exemption §6 · trend-step §6 · S4 §5/D4 — "שווה פי-~50 מכל שינוי-שער"). · **חלון:** בנייה during/אחרי-סגירה; **הדלקה רק אחרי רפליי+פסיקה — לא היום בכוח**.
- **אפקט-היום:** גם כשהשערים פתוחים, בלי סולם-מדרגה הכניסות-שעוברות לא מתממשות לרווח (זה ה-×47). אם הפסיקה מגיעה היום — מחר זה חי.

## 6) 🟡 השארית השערית היחידה שמצדיקה פעולה: `awaiting_release` ענף-הנפח — פסיקה אחת

- **מה:** הצעה למייקל (פסיקה כתובה אחת): `RELEASE_TREND_BYPASS_PTS=12` (דיפולט-קוד 15; חסר ב-.env) + ריסטארט.
  **לא** נוגעים בענף-המבנה, **לא** בפטור-רגל (R6 נשאר לא).
- **בעלים:** מייקל-ruling → cc מיישם · **ראיה:** אתמול: 3 שורטים-עם-מגמה נחסמו בענף-הנפח (10:25/10:40/10:45, **+$540 per-signal**) כשהתזוזה −11.5→−12.25 מול סף-15; ביקורת-הרגל: סף-12 = הברך, חיובי בשני המשטרים (**⚠️ n=3**); 48-סשנים: השער עולה +$465. · **חלון:** אם נפסק — לפני-פתיחה יחד עם הריסטארט של #1; אחרת אחרי-סגירה עם רפליי-רענון על פיד-נקי.
- **אפקט-היום:** פותח את מחלקת-החסימה השיורית הגדולה ביותר מאתמול. על n=3 — לכן פסיקה, לא ברירת-מחדל.

## 7) 🟡 צל והמשך-מדידה (אפס סיכון, אפס דגלים)

- **מה:** (א) `TREND_STEP_ENTRY_V1` — לבנות מודול+טסטים **כבוי**, SHADOW שבוע (S1/S3 מהדוח; פותר מבנית גם את
  cont_trend-flip וגם את lsma_flat-בהפוגה — שאריות #2/#3 שלנו, n=1 כ"א, לא מצדיקות ניתוח-שער);
  (ב) S7 ממשיך איסוף-צל; (ג) F5 — **מאומת:** `HIGHER_LOW_SECOND_TEST_V1` נעדר מ-.env = כבוי, נשאר כבוי;
  (ד) cc לבדוק את ממצא-K5 של exec-agent: פטור-EXCESS אולי מת (RESPONSIVE_WITH_DAY_TREND חוסם לפניו — רגרסיה-חיה אפשרית מאז 07-23).
- **בעלים:** cc · **חלון:** during/אחרי-סגירה · **אפקט-היום:** עקיף — ראיות למחר; K5 עלול להסתיר כניסת-EXCESS מותרת גם היום.

## 8) ⚪ אחרי-סגירה: היגיינת-ריפו ומדידה

- OPS_LOG/PM_1/PM_2/E2E_FIRE_PROOF + `news_calendar.yaml` — שינויים לא-מקומטים של exec/cc בעץ — לקמט ע"י בעליהם (D4 בבקלוג).
- למדוד מחדש `variant: UNION` של S2 על פיד-נקי (A3) + לרענן את טבלת-עלות-השערים על הלוג הנקי של היום.

---

## מה לא נוגעים בו היום (רשימת-איסור מפורשת)

| לא נוגעים | למה |
|---|---|
| `daytype_playbook` (מעבר ל-F4 שכבר בוצע) | חסך $670/48-סשנים · $111 אתמול · כל יום-08-10 ($452). השער הטוב במערכת |
| `rr_entry_gate` | חסך $429/48-סשנים; אם יתפוס כניסות-מדרגה היום — הפתרון הוא F3 (סטופ נכון), לא פתיחת-השער |
| `extreme_chase_guard` — כלל-המרחק + `EXTREME_CHASE_SCOPE=CONT` | המרחק חוסך בניטרל (08-10: −$240); ה-scope=CONT הוא פסיקת-מייקל מ-ceb4682a — **ה-GO הישן של C3 (CONT+REV) SUPERSEDED, לא להחיות** |
| tip-revocation | נשאר OFF (דיפולט-קוד, פסיקה standing). לא "לשחזר" |
| `direction_context` / `cont_trend_filter` / `lsma_flat` / `location_gate` | אתמול היו ⚪/✅; העלות ההיסטורית מטופלת מבנית (F3/step-entry), לא בפתיחת-שער על n=1 |
| eod/session gates, S2 flags (`S2_CHART_ALL_DAYTYPES=1` וכו') | by-design / A2 של ביקורת-S2: אפס שינויי-דגלים |
| HLST / RE_PULLBACK / EDGE_FADE | כבויים ונשארים (אישור-כפול לשלילת-HLST; EDGE_FADE ממתין scid-replay) |
| כל `.env` מלבד (אם-ייפסק) `RELEASE_TREND_BYPASS_PTS` | "אף דגל לא צריך להשתנות" — מסקנת שתי הביקורות |

**סיכום-הסדר:** 1 ריסטארט-מחייה → 2 ניקוי-לוג → 3 הוכחות-חיות → 4 משמרת-ביצוע → 5 F3-סולם (הבנייה החשובה) → 6 פסיקת-release יחידה → 7 צל → 8 היגיינה. פריטים 1-3 חייבים להסתיים לפני 16:30.

**חתום: quality-blocks-agent · 2026-08-12**
