# MEMS26 · טיוטות-עיצוב — 2026-07-03

לכל בעיה: **שורש · השינוי-המוצע (קובץ/שכבה) · regression שיוכיח · trading-logic (→Michael) או display/safe.**
**אל תממש — טיוטה מוכנה-לפרומפט-CC.** מסומן אם דורש הצלבת-Sierra/לוג תחילה (Rule 2/5).

> **רקע-יום (07-03 · חצי-יום-חג):** יום-רנג' דחוס (טווח 12.5pt, נטו +2.5pt, ווליום-נמוך). **סיגנל-בודד נורה (REACTIVE_SHORT) ובוצע-אופטימלית: T1→BE→+$17.5 · shadow תאם-דמו · ΣCF-היום ≈ 0R.** **אין באג-trading-logic חדש היום** — הביצוע היה נקי. הממצאים-האקציוניים הם **instrumentation (display/safe)** שנועד להפוך את החוסם-הכרוני I-3 לבר-כיול, ולתקן את `/missed-trades` שחזר שוב חסר-ערך. אין פריט-חדש שדורש-אישור-Michael היום; הפריטים trading-logic הם שוחזרים-קיימים (I-22/I-23) שהעיצוב שלהם כבר-עומד.

---

## עדיפות-1 — D-missed-buffer: `/missed-trades` חסר-ערך-אינטרה-דיי (🟢 display/safe) · **מאחד I-60** · דורש הצלבת-לוג-CC

**שורש (raw, מאומת-חי + היסטורי):** ה-endpoint החזיר היום **50 מועמדים, כולם ZLR-SHORT `ready_to_route=False`, מרוכזים 11:52–11:55 CT** (4 הדקות לפני ההשבתה), מחירים 7555.5–7556.75, `hypothetical_r=null`. אלו **אינם 50 החמצות** — אלא ~1-2 סיגנלים שנרשמו ~50× כי Sierra שולח ריבוי-UPDATE לכל בר-5דק' וה-`missed_trade_detector` (`backend/v9/systems/build_status/missed_trade_detector.py:115,127`) לא-מדדד. **תבנית-שחזרה מדויקת:** register מתעד "50 candidates all post-close / buffer-artifact / r=null" ב-06-16/18/19/22 + 07-01. שני-פגמים-מבניים: (א) **אין-dedup** (ריצוף עד תקרת-50 של אותו-מועמד); (ב) **`hypothetical_r` תמיד-null** — ה-CF לא-מחושב-בכתיבה, אז ה-store לא-לוכד את החסימות-האמיתיות של אמצע-היום (שם ה-R משמעותי). I-60 (מחיקת-store-בריסטארט) הוא תסמין-נוסף של אותו-כלי-שבור.

**השינוי-המוצע (`missed_trade_detector`):**
1. **Dedup-key** = `(bar_ts, pattern, direction, round(entry,2), why_not)` — מועמד-קיים מעדכן-מונה (`seen_count`) במקום שורה-חדשה. מסלק את פרץ-ה-50.
2. **חישוב `hypothetical_r` בכתיבה** (או ב-lazy-read): entry/stop/T1 מספק-התבנית → replay N-הברים-הבאים מ-`v9_bars_5min_woodies` → `hit_T1/stop/timeout` → R. (זהה למתודת-ה-CF של דוח-ה-EOD; מנוע-אחד משותף.)
3. **Persistence** (סוגר I-60): כתיבה ל-PG (`v9_missed_trades`) או rebuild-on-boot, כדי ששרידת-ריסטארט לא-תמחק את היום.
4. חשיפת `failed_stages`/`seen_count` בתגובת-ה-endpoint (ראו design §2 — משותף).

**regression:** `test_missed_dedup_collapses_updates.py` — הזרמת 50 UPDATE-events לאותו בר/תבנית → assert `count==1, seen_count==50`; `test_missed_hypothetical_r_computed.py` — fixture-בלוק עם ברי-המשך → assert `hypothetical_r` מספרי (לא-null) ותואם replay-ידני; `test_missed_survives_restart.py` — כתיבה→boot→assert נשמר.

**סיווג:** **display/safe** (כלי-מדידה, לא-נוגע-בירי). **מקור-אמת (CC):** לצלב מול לוג ה-detector שה-50-של-היום = דופ' של ~1-2 סיגנלים; לצלב ערכי-ZLR (CCI-14/TCCI) 11:52–55 מול `~/SierraChart_Data/v9_export/`.

---

## עדיפות-2 — D-ready-route-instrument: לחשוף איזה `failed_stage` הפיל את S4 (🟢 display/safe) · **תנאי-מוקדם לכיול I-3** · דורש הצלבת-לוג-CC

**שורש (raw):** `ready_to_route=False` (I-3) הוא החוסם-הכרוני של S4-continuation — אבל **אנחנו-מנחשים למה בכל-חסימה.** היום הוא חסם-נכון (רנג'+סגירה); ב-07-01 הוא חסם winner (+3R). הקוד **כן-יודע** את הסיבה: `decision_tree.evaluate_bar` (`decision_tree.py:434`) מחזיר `failed_stages`/`pending_stages`, ו-`woodies_system.py:1035–1039` שומר אותם ב-`current_state.last_route`. אבל ה-`missed_trade_detector` (`:127`) כותב רק `why_not="ready_to_route=False"` — **בלי ה-stage.** ⇒ אין דרך-לדעת אם החסימה = `A7:TOO_CLOSE_TO_EOD` (צפוי היום, `MIN_EOD_DISTANCE_MINUTES=60` מול השבתה-12:00), `A7:STOP_OUT_OF_RANGE`, `sizing=reject`, או pending-stage.

**השינוי-המוצע (`missed_trade_detector` + `/missed-trades`):** להעביר את `failed_stages`/`pending_stages`/`sizing` מ-`s4_state`/`last_route` אל `why_not` (למשל `why_not="ready_to_route=False[A7:TOO_CLOSE_TO_EOD]"`) ולתוך ה-payload. **אפס-שינוי-לוגיקה** — רק-הצפת-מה-שכבר-מחושב. זה-הופך את I-3 ממ"סתום" ל"בר-כיול": אחרי כמה-ימים נראה את התפלגות-ה-stages ונדע אם השער שמרני-מדי או מוצדק — **ואז** (ורק-אז) כיול-לוגיקה יעלה לדיון-Michael.

**regression:** `test_missed_exposes_failed_stage.py` — fixture עם `last_route.failed_stages=["A7"]` + A7 skip_reason → assert `why_not` מכיל `A7` ואת ה-reason; `test_ready_route_reason_passthrough.py` — assert אין-איבוד-מידע בין `decision_tree`→`detector`.

**סיווג:** **display/safe** (instrumentation בלבד). **מקור-אמת (CC):** לחלץ מ-`current_state.last_route.failed_stages` עבור בלוקי-11:52–55 של-היום ולאשר את ה-stage בפועל (A7 EOD-distance צפוי). **הערה-קריטית:** לא-לגעת בלוגיקת-`ready_to_route`/A7 לפני-שה-instrumentation רץ כמה-ימים — כל-כיול-שער = trading-risk → Michael.

---

## עדיפות-3 — D-daytype-endpoint-plausibility (I-44 שוחזר; fixture-חדש) (🟢 display/safe) · **spec/handoff קיים**

**שורש (raw, חי):** `/day_type/state` החזיר `Variation · OPEN_DRIVE · ib_width=EXTREME · session_min=0 · vote_history=[]` **על יום שטווחו-הכולל 12.5pt** — `ib_width=EXTREME` (רוחב-IB-קיצוני) הוא **בלתי-אפשרי** כשכל-היום 12.5pt, ו-`OPEN_DRIVE` סותר פתיחה-שחזרה-לרנג'. `session_min=0`+`vote_history=[]` = חתימת-ה-wrapper-הקפוא (I-44) המוכרת. המשטח-החי (`/status.day_type=Variation/LOCKED/conf=0.26`) סביר-יותר אך גם-low-conf. יום-4-לפיצול-המשטחים.

**השינוי-המוצע:** (א) כמתועד ב-`CC_DAYTYPE_SOURCE_CONSISTENCY_2026-06-30.md` — לאחד את מקור-ה-endpoint אל `app.state.day_type_machine` החי (לא ה-wrapper-הדד). (ב) **בנוסף — plausibility-guard display/safe:** אם `ib_width==EXTREME` אך `session_range < X pt` (או `session_min==0` עם `vote_history==[]`) ⇒ לסמן `stale: true` בתגובה במקום-להגיש-ערך-מטעה (Rule-1: כישלון-כן > ערך-סינתטי).

**regression:** `test_daytype_endpoint_uses_live_machine.py` — assert ה-endpoint משקף את `app.state.day_type_machine`; `test_daytype_stale_guard.py` — fixture `ib_width=EXTREME`+range=12.5 → assert `stale:true` (לא-EXTREME-נקי).

**סיווג:** **display/safe** (איחוד-מקור + דגל-stale; לא-נוגע-בסיווג-עצמו). **מקור-אמת (CC):** ההצלבה מ-handoff-06-30 עומדת; להוסיף את fixture-היום (EXTREME על 12.5pt).

---

## שוחזרו — עיצובים-עומדים (display/safe; לא-חסמו-מסחר היום)

### D-pnl-r-fix (I-22) 🔴-דיווח / display/safe
**שורש:** 290 `pnl_r=14` · C1 `pnl_r=12` על מהלך-1R (3pt) → ניפוח ~12–14×. **השינוי:** לתקן את נתיב-חישוב-R (`pnl_usd / (risk_pt × $/pt × contracts)`) או להשמיט את-השדה מ-`/trades/recent` עד-תיקון. **regression:** `test_pnl_r_matches_usd_over_risk.py` (290: +$17.5, risk 3pt → assert |pnl_r| < 1). **חוסם:** כל-סיכום-ΣR מ-trades. עיצוב-עומד.

### D-gateway-counters (I-23) 🟡 display/safe
**שורש:** `trades_today=0/daily_pnl=0` מול fire-דמו-אמת (290). **השינוי:** לחווט את מוני-ה-gateway לספור demo+shadow fires (או לתעד שהם live-only-בכוונה). **regression:** `test_gateway_counts_demo_fire.py`. עיצוב-עומד.

---

## Watch — לצלב לפני-עיצוב (Rule 2, CC)

- **gateway-slot (I-57-adjacent):** `demo_slot="290"/shadow_active_count=1` בעוד 289/290 **CLOSED** (exit 11:20). לצלב `fill_poller.on_trade_close` — האם הסלוט שוחרר-בפועל או שזו תצוגת-"אחרון-ידוע". אם-לא-שוחרר → I-57-רגרסיה (🔴). אם-תצוגה → display-label-fix (🟢). **אין-עיצוב עד-הצלבה.**
- **bridge.running=false / streams 0/11:** מול `/tmp/bridge.err.log` + LaunchAgent — artifact-post-close (Sierra-direct+promoter הזרימו 42 ברים) או drift-אמיתי? **אין-עיצוב עד-הצלבה.**

---

## סדר-בנייה מוצע (הכל display/safe — לא-דורש-Michael)
1. **D-ready-route-instrument** (§2) — הזול-ביותר, פותח את I-3 לכיול. חצי-שעה.
2. **D-missed-buffer** (§1) — dedup + hypothetical_r + persistence; מנוע-CF משותף עם §2. סוגר I-60.
3. **D-daytype-plausibility** (§3) — דגל-stale + איחוד-מקור.
> אף-אחד מ-1–3 לא-נוגע בלוגיקת-ירי/risk ⇒ **ניתן-לבנות מיידית ללא-strategic-stop.** כיול-I-3 עצמו (אם ה-instrumentation יראה over-blocking) = trading-risk → Michael, **בעתיד, על-בסיס-נתונים.**
