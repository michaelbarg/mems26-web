# טיוטות-עיצוב — 2026-06-05

לכל בעיה מאומתת: שורש · השינוי המוצע (קובץ/שכבה) · regression שיוכיח · סיווג
**trading-logic (דורש אישור Michael)** מול **display/safe**. **אל תממש — טיוטה מוכנה-להפיכה-לפרומפט.**
מקור-אמת Sierra (`v9_export`) טרם הוצלב היכן שמסומן → **CC**.

---

## D-1 · I-9 — ה-EOD רץ בבוקר ולא אחרי-סגירה  *(safe · תשתית)*

- **שורש:** ה-scheduled-task נורתה ב-**09:25 CT** (54 דק' לתוך RTH) במקום אחרי 15:00 CT.
  ה-gating-הפנימי-לפי-CT מאמת שזה RTH, אבל לא שזה after-close → דוח-ה-EOD כיסה רק את
  השעה-הראשונה (11 ברים), והתחזית-נגד נחתכה ל-3 ברים בלבד.
- **השינוי המוצע:**
  1. תיקון שעת-ה-trigger של ה-scheduled-task ל-23:05 IL / 15:05 CT (אחרי סגירת RTH), **או**
  2. guard בתחילת הסוכן: `if now_ct < time(15,0): exit("pre-close, skip EOD")` — כדי שגם אם
     ה-trigger מוקדם, לא ייכתב דוח-EOD חלקי. אפשר במקביל לכתוב snapshot-ביניים בקובץ DIAG.
- **regression שיוכיח:** unit על פונקציית-ה-guard — מזריקים `now_ct=09:25` → exit/skip;
  `now_ct=15:05` → run. + בדיקה ש-DST (CDT/CST) מטופל (Asia/Jerusalem↔America/Chicago).
- **סיווג:** display/safe — לא נוגע ב-trading-logic.

## D-2 · I-11 — S3 footprint, 0 ברים עובדו  *(safe לאבחון · CC)*

- **שורש (חשוד):** המערכת `running+hydrated` אבל `bars_processed_today=0` בשני snapshots
  (09:12 + 09:24 CT). אחת מ-3: (א) Sierra footprint export לא נכתב היום, (ב) הגשר לא קורא
  את נתיב-ה-export, (ג) ingest שובר בכתיבה ל-DB. read-only כאן → לא אובחן שורש.
- **השינוי המוצע (אבחון לפני תיקון, לפי Pre-LIVE "Diagnose first"):**
  1. **CC:** `ls -la ~/SierraChart_Data/v9_export/` — האם קובץ footprint עודכן היום? (mtime)
  2. אם כן → קריאת `/tmp/bridge.err.log`/`bridge.out.log` ל-`footprint` push; אם FAILED →
     ממצא-הזנה. אם אין שורות footprint כלל → הגשר לא מנוי על הזרם.
  3. DB: `SELECT count(*),max(ts) FROM v9_footprint_bars WHERE ts::date=current_date;`
- **regression שיוכיח:** אחרי שורש+תיקון — `bars_processed_today > 0` תוך ≤10 דק' מפתיחת-RTH,
  ו-`SELECT count(*) FROM v9_footprint... WHERE ts::date=today` עולה מונוטונית. **Rule 5: paste raw.**
- **סיווג:** האבחון safe. התיקון תלוי-שורש — אם נתיב-export/bridge → safe; אם נוגעים ל-S3
  detection logic → **trading-logic, אישור Michael.**

## D-3 · I-1 — day_type=UNKNOWN חי; sizing פעל בלי קונטקסט-יום  *(trading-logic · אישור)*

- **שורש (חשוד קודם):** `bar.atr`=None → classify_ib_width_atr לא רץ → day_type נשאר UNKNOWN;
  A4 מדווח `day_type/tpo/killzone/layer0 missing`. מאומת חי ב-HFE dtree (09:24 CT).
- **השינוי המוצע:**
  1. **CC קודם:** הצלב `atr_daily` ב-`~/SierraChart_Data/v9_export/` — מגיע ל-`classify`?
     אם הערך **קיים ב-Sierra** אך לא מגיע לסיווג → באג-העברה (safe-ish). אם **חסר ב-export** →
     צריך החלטה האם להזין/לחשב (לפי CLAUDE.md אסור לסנתז כשה-DLL שותק — **honest None**).
  2. רק אחרי-שורש: לתקן את נתיב-ה-atr→classify, **בלי** לסנתז day_type כשהמקור שותק.
- **regression שיוכיח:** בפתיחת-RTH עם atr-תקין → `day_type != UNKNOWN` תוך 3–6 ברים;
  בהיעדר-מקור → `day_type=UNKNOWN` מפורש (לא ערך מסונתז). מבחן anti-synthesis.
- **סיווג:** **trading-logic** (משפיע על day-patterns + Woodies A2/A4 + sizing) → **אישור Michael.**

## D-4 · I-12 — A5 sizing reject אטום + פעל בלי day-context  *(trading-logic · אישור)*

- **שורש:** A5 `calculate_size=reject` עם `details{}` ריק; ההסבר רק ב-`last_reasoning_notes`
  (`HFE LONG size=reject: CCI=-192.9, trend=RED, group=REVERSAL`). בנוסף — רץ בזמן ש-A4
  דיווח context degraded (day_type missing), כך שה-sizing הסתמך על trend/CCI בלבד.
- **השינוי המוצע:**
  1. **(display/safe)** למלא את `A5.details{}` עם הקלט-שחתך: `cci`, `trend`, `group`,
     `risk_per_trade`, `equity`, `contract`, `computed_size`, `reject_rule` — כדי שדיאגנוסטיקה
     לא תצטרך לקרוא לוג. שינוי-תיעוד בלבד, לא משנה את ההחלטה.
  2. **(trading-logic · אישור)** להחליט אם sizing **אמור** לרוץ כש-day_type חסר, או להחזיר
     `defer/None` עד שהקונטקסט זמין (תלוי בכיוון שתבחר ל-D-3).
- **regression שיוכיח:** (1) על reject — `details` כולל את כל השדות + `reject_rule` שם-חוקה;
  הערך זהה ל-`reasoning_notes`. (2) אם נבחר defer → כש-day_type=UNKNOWN ה-A5 מחזיר
  `defer` לא `reject` שקרי.
- **סיווג:** §1 display/safe · §2 **trading-logic → אישור Michael.**
- **הקשר תחזית-נגד:** ב-2026-06-05 ה-reject **חסך ≈1R** (HFE LONG נמילא ב-7519.25, נעצר
  באותו בר ב-7516.75) → ההחלטה נראית מוצדקת, אך התבססה על קונטקסט חלקי. לא לשחרר את A5
  לפני שמבינים אם ה"צדק" מקרי (תלוי I-1).

---

### לא-מעוצב היום (חסר אימות-מלא)
- **I-3 (ZLR שמרני-מדי?)** — לא נדרכה היום (signal=HFE בלבד); אין counterfactual → אין עיצוב.
  ממתין ליום עם ZLR ארמד.
- **I-5/I-6/I-7/I-8/I-10** — מצב לא-השתנה ב-snapshots של היום; עיצובים קיימים/לא-מושפעים מהריצה.

### תלות-Sierra (CC, מקור-אמת)
D-2 (export footprint mtime + bridge log) · D-3 (`atr_daily` ב-v9_export) · והצלבת
`cci_14=-192.9 / tcci=-99.7 / OHLC` של בר-ה-HFE — כולם read-only פה, **CC לבצע ולהדביק raw**.
