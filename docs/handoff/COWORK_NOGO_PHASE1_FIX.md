# 🔴 NO-GO ממוקד · פאזה 1.1 — השומר לא ישחרר את הירי · תיקון מיידי

**מאת:** cowork · **אל:** cc · 2026-08-26 09:40 · **שאר פאזה-1 ✅ אומת** (טסטים 3/3,
דגלים OFF, resolver מחוץ לנתיב-הירי, flag_guard 207+liveness).

## הממצא — dead wiring, אותה מחלקה משלשום

`entry_guard.py:138-147` מייבא:
```python
from backend.v9.services.sierra_position_reconciler import _ownership_attributed
```
```
$ grep -n "_ownership_attributed" backend/v9/services/sierra_position_reconciler.py
(ריק — הפונקציה לא קיימת)
```
‏ImportError נבלע ב-`except (ImportError, AttributeError): pass` ⇒ מסלול-2 מת בשקט.
ומסלול-1 (התאמת-עסקת-TM) לא חל על פוזיציה ידנית של מייקל — אין עסקת-TM.
**⇒ גם עם הדגל דלוק, הפוזיציה של מייקל עדיין חוסמת. הדגל לא מבצע את הפסיקה.**
הטסטים עברו כי הם מכסים את מסלול-1 בלבד — **חסר טסט-מוטציה על מסלול-2.**

## התיקון — לפי הפסיקה, בלי להרוס את הגנת-האורפן

**אסור** לשייך אוטומטית כל פוזיציה זרה ל"מייקל" — זה מוחק את הגנת-האורפן-העירום
(המחלקה שעלתה כסף ב-07-10/14/17/20/23). השיוך חייב להיות **מפורש**:

1. קובץ `config/manual_position_ack.json`:
   `{"date":"2026-08-26","owner":"michael","note":"פסיקת 26.08: אני","max_abs_qty":10}`
   נכתב רק ע"י cowork/מייקל (לא ע"י cc, לא אוטומטית). **תקף ליום אחד** — `date` חייב
   להיות היום, אחרת מתעלמים (פוזיציית-מחר הלא-מוסברת = אורפן כרגיל).
2. ‏`entry_guard`: מסלול-2 = הקובץ קיים + `date==today` + `abs(pos)<=max_abs_qty`
   ⇒ explained, לוג `[EntryGuard] coexist_manual (ack 2026-08-26)`.
3. להסיר את הייבוא המת, או לממש `_ownership_attributed()` אמיתי — לא להשאיר ייבוא-רפאים.
4. **טסט-מוטציה:** מחיקת הקובץ ⇒ חסימה. תאריך-אתמול ⇒ חסימה. qty מעל התקרה ⇒ חסימה.
5. עדכן את `test_entry_guard_ownership.py` לכסות את שלושת המסלולים.

בסיום: דוח מעודכן + LOG + push. **אל תדליק** — אני מדליק אחרי אימות, לפני 16:10.
