## T-298 + T-299 — שני פגמי-עץ שנחשפו בטייפ של היום (10.09). בנייה עכשיו, **בלי ריסטארט** (עסקה חיה #1409 פתוחה; ריסטארט רק על מילה של מייקל). שער-הקבלה = `scripts/fwd_harness.py` בלבד.

### הראיה (backend.err.log, 10.09)
```
17:30:02 [FiveMin] Mode transition: FIRST_HOUR_TACTICAL → DAY_TYPE_MODE
17:30:09 [S2] T1Setup emitted: INITIATIVE_LONG LONG entry=7606.50 …
17:30:09 [Gateway] LIVE trade TM id=1409: LONG INITIATIVE_LONG system=2   ← עבר, אפס שורת-dalton
17:30:09 [S1-BINARY] EVENT=acceptance_DOWN: Normal→Normal (old=Normal)
17:30:09 [S1-NEW-CLS] DAYTYPE_RECLASS_STABILITY held: Trend_Normal → Normal not published (pending Normal 1/2, PROVISIONAL)
17:30:09 DayTypeConsumer upserted: type=Trend_Normal prob=0.58
17:35:08 [S1-NEW-CLS] promoted: Trend_Normal → Normal
```
כלומר: S1 בנעילת-IB אמר **Normal**; ההיסטרזיס (DAYTYPE_RECLASS_STABILITY) החזיק את התווית ה**פרוביזורית** מ-17:00 (`Trend_Normal 0.35`) מעבר לאירוע-הנעילה; השער ניתב על שורת `Trend_Normal` (bias=trend_direction=LONG, kinds [PULLBACK,BREAK]) ⇒ INITIATIVE_LONG (kind=BREAK) עבר. על שורת `Normal` (bias BOTH, kinds [EDGE_FADE,VALUE_RETURN]) הוא נחסם `dalton_intent:kind`. זו T-286 שוב, הפעם בנעילה עצמה.

סוג-הפתיחה הקנוני נדד במהלך הסשן:
```
16:45:05 ot=OPEN_REJECTION_REVERSE hint=LONG bias=SHORT   (חסם OPENING_ORR LONG @7601)
17:05:06 ot=OPEN_AUCTION_IN hint=LONG bias=BOTH
17:20:03 ot=OPEN_AUCTION_IN hint=LONG bias=BOTH
17:41    /mobile/data → opening_type=OPEN_AUCTION_OUT, ext_dir=SHORT (ext_up=ext_dn=0)
```
הרמז-הכיווני מתהפך עם הקריאה-מחדש ⇒ ה-bias מתהפך **אחרי** הכניסה. מפרט S1 (staging PERMANENT): opening@15min. ⚠️ תיקון ל-T-297: היום Source 1 (`_resolve_live_cls`) סיפק ot כבר מ-16:45 — Source 2 (registry) מת, אבל "UNKNOWN כל שלב B" לא קרה. T-297 נשאר (מבחן-חיווט + סריקת-רג'יסטרי), בעדיפות אחרי T-298/299.

### T-298 — מסירת-תווית בנעילת-IB (day_type handoff)
בנעילת-IB (בר 12 / 17:30) הקריאה של S1 **מחליפה מיד** את התווית הפרוביזורית. ההיסטרזיס של DAYTYPE_RECLASS_STABILITY (פסיקת 08-19/20, נשאר דלוק) חל רק על סיווג-מחדש **אחרי** הנעילה (locked→locked), לא על המעבר provisional→locked. אפס שינוי בדגל, אפס שינוי ב-.env.
- golden (חובה, מהטייפ של היום): `2026-09-10 17:30:09` ⇒ day_type בזמן-הניתוב = `Normal` ⇒ INITIATIVE_LONG 7606.50 ⇒ `blocked_by=dalton_intent:kind` (צל, לא לייב).
- רגרסיה: 08-03 #593 ו-08-04 #612 נשארים admitted; 09-09 ללא שינוי.

### T-299 — נעילת סוג-הפתיחה + כלל-הפרכה
- סוג-הפתיחה הקנוני **נקפא** בסגירת בר 3 (16:45) יחד עם `direction_hint` שלו.
- חריג "מחפש": אם הקריאה ב-16:45 היא `OPEN_AUCTION_IN/OUT` — ממשיכים לקרוא כל בר עד 17:30, ואז מקפיאים.
- הפרכה: ORR/TEST_DRIVE/DRIVE מתבטלים כשמחיר **סוגר** מעבר לקיצון-שנדחה (ORR) / מקור-הדרייב (DRIVE) — ואז קריאה-מחדש פעם אחת, ונעילה. (זה "לא לפספס את הפתיחה, אלא אם המחיר מחפש" — פסיקת 09.09 11:15.)
- golden מהיום: 16:45 `ORR hint=LONG bias=SHORT` נשאר קפוא ב-16:50–17:15 (REVERSAL/PULLBACK SHORT מותר, LONG חסום `bias`); 17:20 close 7612.75 > 7609.25 (הקיצון-שנדחה) ⇒ הופרך ⇒ קריאה-מחדש ⇒ AUCTION_OUT ⇒ bias BOTH/EDGE_FADE עד 17:30.
- `/mobile/data.dalton` מציג `opening_locked_at` + `negated_at` (תצוגה בלבד).

### כללים
- `fwd_harness.py` על 08-03 · 08-04 · 09-09 · **10.09** — PASS על שלוש התוצאות הישנות + שני ה-golden החדשים, אפס חריגות. אין pytest כבד ב-RTH.
- אפס נגיעה ב-`RISK_BUDGET_USD`/`RISK_MIN_CONTRACTS`/`FIXED_CONTRACTS_5`; אפס שינוי בדגלים.
- דיווח: שורות TASK_LOG T-298/T-299 + LOG ב-LIVE_CHANNEL עם פלט-ההרנס הגולמי (Rule 5). **לא לעשות ריסטארט.**
