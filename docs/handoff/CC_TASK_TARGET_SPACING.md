# משימה דחופה — מבחן-מרווח ליעדים, בשדואו היום
**פסיקת-מייקל 21.08 11:30:** "לבנות ולהפעיל בשדואו ולבדוק איך זה פותר."
**§0 חל:** אל תשאל — תכריע, תתעד, תמשיך. השוק נפתח 16:30; **ריסטארט אסור אחרי 16:10**.

## הראיה
עסקת-לייב **#756** (20.08): SHORT כניסה 7696.75 · סטופ 7700.25 (מבני,
`stop_source=TREND_STEP_LEG`, סיכון 3.5 נק') · יעדים **7691.50 / 7691.00 / 7690.50** —
שלוש רמות מבניות שהצטופפו ב-0.5 נק'. הסולם מאבד משמעות: כל הרגליים יוצאות באותו מחיר.
`DAYTYPE_TARGETS_STRUCTURAL=1` דלוק — המבנה **כן** נתן רמות, הן פשוט באשכול אחד.
**הפער:** `_target_degeneracy_guard` (`trading_gateway.py:3703`, נבנה 20.08 ל-T-06)
בודק רק **זהות מוחלטת** (`abs diff < 1e-9`) ⇒ מרווח 0.5 נק' עובר.

## מה לבנות — `TARGET_MIN_SPACING_V1` (default OFF בקוד)
- מרווח-מינימלי **יחסי** (עקרון עומד — אין מספרים קבועים):
  `min_gap = max(k × ATR14, m × risk)`, ‏k/m ב-YAML/config — לא בקוד.
- הפרה ⇒ (א) לשמור את הרגל הראשונה ולדחוף את הבאות **לרמה המבנית הבאה** שעומדת
  במרווח; (ב) אין רמה כזו ⇒ להשמיט את הרגל ולתת לחוזה לרוץ ליעד התקף הבא/לראנר.
  **אסור להמציא מחיר** שאינו רמה אמיתית או כפולת-R נקייה. ללוג איזה ענף פעל + מספרים.
- **שדואו קודם (הדרישה המפורשת):** ערך-דגל של **תצפית-בלבד** — לחשב מה הסולם *היה*
  צריך להיות, לרשום `[TargetSpacing] SHADOW would-be: ...` ולשמור
  `quality["target_spacing_shadow"]` — **בלי לשנות הזמנות חיות**. החלת-לייב = ערך/דגל
  נפרד, כדי שההדלקה בהמשך תהיה פסיקה אחת.

## מדידה (חובה)
רפליי על כל עסקאות עידן-הלייב עם המנועים הקיימים (`week_replay.py`,
`gen_daily_trade_tables.py`) — **לא לבנות מנוע חדש**: כמה סולמות היסטוריים מפרים
את הכלל (לרשום את הגרועים, כולל #756), ה-P&L שלהם מול הסולם המתוקן (להשתמש ב-
`exit_fills`/`pnl_sierra` שאוכלסו ב-F2), דלתא כוללת **וחציון-יום**.
→ `docs/reports/REPLAY_TARGET_SPACING_2026-08-21.md`

## טסטים
דגל-OFF זהה-בייט · הסולם של #756 מפיק את ערכי-הצל הנכונים · סולם בריא לא נגע ·
אפס מחירים מומצאים · רצפת-ATR-מת מתנהגת.

## הפעלה
אם סיימת **לפני 15:45 IL** — להוסיף את דגל-השדואו ל-`.env`, לרסטרט, ולאמת:
`[boot] logging OK` + `flag_guard` + `fire_drill` 🟢 (פלט גולמי בדיווח).
אחרי 15:45 — **לא לרסטרט**, להשאיר להערב ולומר זאת. commit+push + שורה ב-LIVE_CHANNEL.
