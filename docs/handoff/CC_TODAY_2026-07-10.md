# CC — יום שישי 2026-07-10: השלמות-בוקר לפני הפתיחה (16:30)

**חוזה:** CC_HANDOFF_CONTRACT.md. דוח חי: `docs/reports/CC_DAY_REPORT_2026-07-10.md`
מתעדכן אחרי כל פאזה (Rule 5, NOT-DONE). ‏Cowork מבקר.
**שערי-יום-מסחר:** כל ריסטארט מסתיים עד **15:30**. אחרי 15:55 (שער-GO) — אין נגיעה בקוד/דגלים/סיירה
עד סגירה, אלא בהוראת Cowork/מייקל. ‏snapshot לפני .env/DLL. אתמול: ‏2 חוזים (פסוק, 40 דגלים).

## P0 — בירור-בוקר (10 דק')
ה-backend עלה 10:47 והלוג רוטט (env_loader יחיד). ברר סיבת-ריסטארט (launchd? crash? מייקל?)
— `log show --predicate 'process == "launchd"' --last 2h | grep mems26` או plist logs. ודא
שהלוג הישן לא אבד (אם יש rotate — איפה הארכיון; אם אין — הוסף שימור). דוח קצר.

## P1 — הידרציית-בוט (P1.5 מהמגה-לילה — דילגת עליו; עדיפות-על)
קרא `docs/handoff/CC_NIGHT_MEGA_2026-07-10.md` סעיף P1.5 במלואו ובצע:
‏BOOT_HYDRATION_V1 (default OFF) — סדר עדיפות פנימי:
1. **‏daily_pnl של ה-gateway** — חור-סיכון (עצירת −$400 שוכחת הפסדים בריסטארט): שחזור
   מסכימת v9_trades סגורות-היום בבוט. טסט: אחרי "ריסטארט" מדומה daily_pnl שווה לסכימה.
2. צוברי S2 (COT/AMT) מברי-היום.
3. ‏boot-verify: טבלת HYDRATION בלוג בסוף עלייה (רכיב | משוחזר | מקור | פער→WARNING).
‏RULED: unset_or_0 עד פסיקת מייקל (מוצג לו הבוקר). מוכן-לפריסה עד 14:30.

## P2 — סגירת D1: הוכחת EXIT + קיפאון-מחיר (עם מייקל, בחלון שלו הבוקר)
הסטאדי החדש פרוס (מייקל, אמש) וה-price זז (אימות פסיבי של Cowork 10:55: ‏7570.5→7570.75).
א. אשש בקוד/גרסה שהתיקון ב-DLL הרץ (חותם-גרסה/סטרינג בבינארי מול sc_study) וסגור את
   ה-NOT-DONE של הקיפאון עם ראיה.
ב. **הוכחת EXIT על סים עם מייקל:** ‏Sim ON → ‏PLACE קטן → ‏EXIT חלקי (op שהיה error=-1)
   → צפה error≠-1 + fill חלקי → ‏flatten → ‏Sim OFF → ודא חמוש-אמיתי חזרה (37138283).
   תסריט מדויק + פלטים בדוח. אם מייקל לא זמין עד 14:00 — NOT-DONE ולא מאלתרים.

## P3 — יישום פסיקות-הבוקר (רק אחרי שמייקל פוסק — Cowork מעביר לך את הפסיקות)
מוכנות מראש (קוד/דיפים מוכנים, לא מוחלים):
1. ‏EARLY_ATR_FLOOR_V1=1 (.env+RULED באותו קומיט).
2. טבלת-יעדים: החלת השורות שמייקל מאשר מ-TARGET_PROPOSAL_2026-07-10.md על targets.yaml
   (או המנגנון הקיים) + טסט פר-שורה + עדכון SPEC ‏TP-1/TP-2 verified.
3. רצפת confirm-tol (אם נפסק): ‏max(0.10×ATR, 0.5pt) — ‏ENTRY_CONFIRM_TOL_MIN_PTS param.
4. ‏auth-UNKNOWN בפתיחה (אם נפסק לפתוח): אל תבנה חדש — ‏OPENING_WINDOW_FIRE_V1 קיים
   ופסוק OFF; הפעלה = פסיקה מפורשת + עדכון RULED בלבד.
5. ‏BOOT_HYDRATION_V1=1 (אם נפסק, אחרי P1).
פריסה: ריסטארט אחד מרוכז ≤15:30 + ‏flag_guard + ‏fire_drill + ‏decision_replay של אתמול.

## P4 — חובות-פרוטוקול
‏ROADMAP_TO_LIVE.html (סעיף 07-09/07-10 + "אתה כאן") · ‏STATUS_BOARD · ‏task_board.json ·
‏gen_index/gen_flag_index אם השתנה מבנה/דגל. ‏Replay-v2 = DEFER מתועד.

**סדר: P0→P1→P2 במקביל-לפסיקות→P3→P4. תקוע >45 דק' → NOT-DONE והלאה.**

---
## פסיקות-הבוקר (מייקל 10:5x): "מאשר" — הכל לפי טבלת-ההמלצות של Cowork
1. ‏EARLY_ATR_FLOOR_V1=1 — **בוצע ע"י Cowork** (.env+RULED, קומיט זה). חי בריסטארט המרוכז שלך.
2. **טבלת-היעדים מאושרת במלואה** (5 השינויים) — החל על targets.yaml + טסטים (P3.2).
3. רצפת confirm-tol מאושרת — בנה ‏ENTRY_CONFIRM_TOL_MIN_PTS=0.5 (2 טיקים) + הוסף ל-.env+RULED כשמוכן.
4. ‏auth-UNKNOWN — **נשאר סגור היום** (פסיקה: לא לערום שינויים).
5. ‏BOOT_HYDRATION_V1 — מאושר-להדלקה כשמוכן (P1): בנה → ‏env=1+RULED באותו קומיט.
ריסטארט מרוכז אחד ≤15:30 עם הכל, ואז flag_guard+fire_drill+replay-של-אתמול. עדכן את הדוח.
