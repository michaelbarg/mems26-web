# פקודת-עבודה — לולאת-כיול לילית + הרחבת הגמישות לכל התבניות
**פסיקת-מייקל 21.08 ~00:45:** "אפשר כל יום בדיקה מה היה בפועל מול מה שסוחר מקצועי
היה מבצע, ומכיילים? כל יום מכיל אלמנטים שונים ואותה תבנית צריכה להיסחר קצת אחרת."
**אוטונומיה:** §0 של `CC_WORKORDER_2026-08-20_ADAPTIVE.md` חל — להכריע, לתעד, להמשיך.

## N · לולאת-הכיול הלילית (`nightly_calibration`)
LaunchAgent ב-23:15 IL, ימי-מסחר. משרשר רכיבים **קיימים** — לא לבנות מנוע חדש:
1. `pnl_reconcile.py --mode live` — ספרים מול ברוקר; סטייה = 🔴 בראש הדוח.
2. `gen_daily_trade_tables.py` ליום שהיה — הטבלה "מה סוחר מקצועי היה עושה".
3. השוואה: בפועל מול הטבלה → פער בדולרים + סיווג-סיבה (לא-זוהה / נחסם+שער /
   כניסה-מאוחרת / יציאה-מוקדמת / גודל) — אותו סיווג של `ORACLE_STUDY`.
4. `week_replay.py` על היום עם 2-3 וריאציות-סף מועמדות (סביב הערכים הנוכחיים).
5. **פלט:** `docs/reports/NIGHTLY_<date>.md` — עמוד אחד: כמה עשינו · כמה היה אפשר ·
   שלוש הסיבות היקרות · **הצעת-כיול אחת עם המספר שמאחוריה** · commit+push +
   שורה ב-LIVE_CHANNEL + התראת-פלאפון קצרה ("דוח-לילה מוכן, הצעה: X").
6. **הכיול לא מוחל אוטומטית.** מייקל מאשר בבוקר במילה אחת; אז CC מחיל + מריץ
   `week_replay` על 32 הסשנים (כלל §D: בלי דלתא ירוקה — לא נדלק).

## P · הרחבת הגמישות פר-סוג-יום לכל התבניות
היום רק REACTIVE מכויל פר-סוג-יום. להרחיב **באותה שיטה** (YAML, לא קוד; כל שינוי
מגובה במספר מהטבלאות ומ-`week_replay`):
- **P1 · ZLR / GB100 / TREND_STEP / INITIATIVE** — טבלת-כיול פר-סוג-יום כמו
  `s2_reactive_daytype.yaml`: סובלנות-גיאומטריה יחסית ל-ATR, דרישת-נפח באחוזון,
  והקלה/החמרה לפי היום (למשל: ZLR-מהקצה מוקל ב-Neutral, מוחמר ב-Trend_DD).
- **P2 · סטופים ויעדים פר-סוג-יום** — רוחב-סטופ ומרווח-יעדים כפונקציה של ATR/IB
  **וסוג-היום** (Trend=רחב+יעדים רחוקים · Neutral/Normal=צמוד+רוטציה). קיים
  `config/` לטבלאות — להשתמש בו.
- **P3 · בחירת-מודל-ניהול אוטומטית** — לפי הפלייבוק שנמדד: Variation→trail ·
  Trend_DD→סולם · Trend_Normal→trail+הוספות · Neutral→סולם 3 כניסות · Normal→סלקטיבי.
  לחווט כשער-בחירה יחיד שקורא את התווית המיוצבת (F6) ומחליט trail-vs-ladder.
**סדר:** N (הלולאה — מייצרת את הראיות) → P3 → P2 → P1.
