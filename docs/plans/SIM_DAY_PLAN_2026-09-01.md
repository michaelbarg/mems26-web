# תוכנית · 01.09 · שני מסלולים במקביל · 47 הפתוחים משובצים

**מייקל ב-`is_sim=1`** (אומת 11:13: `qty=0 · orders=0`). **דדליין-ריסטארט 16:10. פתיחה 16:30.**

**התיקון לגרסה הקודמת:** תכננתי **טורי**. ‏CC אינו צריך את חלון-הסים — הוא בונה
backend. ⇒ **שני מסלולים רצים במקביל, ו-47 הפריטים משובצים לכולם.**

---

# 🅰️ מסלול cowork · חלון-הסים — **רק מה שדורש פיד חי**

*הכלל: אם אפשר לבדוק בלי סים — זה לא שייך לכאן.*

| שעה | מה | המדד שמכריע |
|---|---|---|
| **11:15-12:15** | `RISK_BUDGET_SIZING_V1=1` + `150/3` → ריסטארט → **למדוד שיעור-דחייה** | >70% נדחים ⇒ שלב הבא חובה · <30% ⇒ עולה כמו שהוא |
| **12:15-13:15** | `RELEASE_ZONE_POINTS` 8→5 (אישור א׳) + לרשום ב-`RULED_FLAGS` (הוא **לא שם**) | כמה מהנדחים עוברים אחרי השינוי |
| **13:15-14:15** | **`op=EXIT`** — 3 חוזים, יציאה על אחד, `r` גולמי | **🔴 האם שני הנותרים עדיין מוגנים?** |
| **14:15-14:45** | **הטיית מחיר-הכניסה** — ששלחנו · שביומן · `AvgFillPrice` | **בכמה 44.9% שגוי** — ואיתו אם `$150` נכון |
| **14:45-15:30** | להכריע לפי המדידות · ריסטארט אחרון | אם המדידה סותרת את `$150` — **משנים** |
| **15:30-16:10** | 🔴 **חזרה ללייב** (למטה) | `is_sim=0` מאומת · `fire_drill` GO |

**‏🔴 הסיכון הגדול היום אינו ה-DLL — הוא לשכוח להחזיר את המתג.**
מערכת שחושבת שהיא סוחרת ואינה סוחרת = **יום אבוד בשקט**, בדיוק כמו הסלוט של אתמול.
**אני מזכיר ב-15:30. אתה מעביר. אני מאמת בפקודה.**

---

# 🅱️ מסלול cc · **מתחיל עכשיו, במקביל, אפס תלות בסים**

## ב-1 · עד 16:10 — backend בלבד, נכנס בריסטארט של 14:45

| # | פריט | למה עכשיו |
|---|---|---|
| **T-193** | להשלים 5/5 — הצד ה-backend (`exit_verifier` כותב שורת-`FLATTEN` ליומן) | **פותח 57% מהעסקאות למדידה.** ‏4/5 כבר בוצע |
| **T-190** | ‏`quality.exit_fills` נכתב ב-12 מ-413 | אותה מחלקה |
| **T-191** | טסטי-`delta_dbl` תלויי-מצב-שוק | **טסט שנכשל בבוקר ועובר בלילה אינו טסט** |
| **T-168** | `RULED_FLAGS` אינו YAML תקין | 10 דקות. מונע מכל כלי עתידי ליפול |
| **T-184** | יחידת-`atr_from` | אפס צרכני-ייצור ⇒ אפס סיכון |
| **T-175** | `NAKED_STOP_SUSPECT` שקרי — לספור סטופים במקום `last_result` | ‏60 שורות `CRITICAL` על פוזיציה מכוסה שורפות אזעקה אמיתית |

## ב-2 · תור-הלילה (אחרי 23:00 / 01:00)

| # | פריט | תנאי |
|---|---|---|
| **T-152** | §D מודע-סלוטים — `fires_raw` מול `fires_accepted` + `inflation_factor` | **שער-הקידום קורא N ולא K** ⇒ "20 אירועים" הם 4 |
| **T-103** | ‏`RESOLVED` + MFE/MAE לתוך `v9_shadow_events` | הטבלה **כבר נבנתה** היום (`ca78b79e`) |
| **T-185** | TPO `market_ts`/`available_at` | snapshot · `lock_timeout=3s` · dry-run |
| **T-196** | פטור-קרבה ב-`awaiting_release` (אישור ב׳) | **בצל בלבד**, ‏default OFF |
| **T-174** | `t3=0.00` ב-`OPENING_DRIVE` + שער-שפיות ליעד לא-חיובי | |
| **T-170** | `5min.json` קפוא מ-28.08 | לברר בסיירה — לא בחלון-מסחר |
| **DLL 1-2** | מחיר-כניסה · FLATTEN | **רק אחרי שהמדידות של 🅰️ חוזרות** · snapshot · אחרי 01:00 |

## ב-3 · השבוע — לא היום, ולא להיעלם

`T-159` ציון-מודעות · `T-162`/`T-163`/`T-164` רעש ותיעוד · `T-157` הצלבת-ברוקר ·
`T-154` `entry_location_quality` · `T-151` שורות סוג-יום · `T-150` פער-ערימה ·
`T-145` טסטי-`risk_caps` · `T-124`/`T-125` ‏UX · `T-102` איחוד 52 כלי-רפליי ·
`T-23` ~490 כשלי-סוויטה · `D1-D3` שלושת השורשים · `DLL 3+5` לימיט ו-`MODIFY_TARGET`.

---

# 🅲 ממתין להכרעתך — **לא אתקדם בלי מילה**

| # | מה | ההכרעה |
|---|---|---|
| **T-188** | `MISMATCH_PHANTOM_SLOT` **קוד-מת מאז 27.07** | **גלאי-בטיחות** ⇒ חתימה שלך |
| **T-141** | סולם-יציאה-על-מבנה, דרגות 3-4 | נפסק חלקית 30.08 |
| **T-140** | פסיקה פתוחה מ-30.08 | |
| **T-04** | **מק-2 מנותק** (`ping` ⇒ אין מענה) | אם לא יחזור — **אין מכונת-סים ייעודית**, וכל בדיקה גוזלת מהמסחר |
| **T-42** | המק ישן ב-00:00 כל לילה | **הורג את תור-הלילה.** תיקון-הגדרות, לא קוד |

---

# 🅳 לסגור כתצפיות — **לא חוב-עבודה**

`T-172` · `T-173` · `T-179` · `T-180` · `T-181` — כולן **נענו ותועדו**; הן יושבות
כ"פתוחות" ומנפחות את המונה. **⇒ להעביר ל-✅ עם שורת-לוח.**

**‏"139 פתוחים" הוא מספר שמפחיד ומטעה.** אחרי הניקוי הזה החוב האמיתי הוא **~35**,
וזה מספר שאפשר לנהל.

---

## 🔴 15:30-16:10 · חזרה ללייב — השלב שהכי קל לפשל בו

| שעה | פעולה | מי |
|---|---|---|
| **15:30** | אני מזכיר — **בתוכנית, לא בזיכרון** | cowork |
| 15:35 | `Trade ▸ Trade Simulation Mode Off` | **מייקל בלבד** |
| 15:40 | `is_sim` ⇒ **חייב 0**. אחרת **לא פותחים** | cowork |
| 15:45 | `fire_drill` 🟢 · `flag_guard` PASS · פיד <10 דק' · סלוט פנוי · `TRADING_ARMED=1` | cowork |
| **16:10** | **דדליין. אין ריסטארט אחריו.** | — |

## מה שלא נעשה היום, ואומר מראש

**לא נערוך DLL** (בדיקות בלבד) · **לא נדליק דגל שלא נמדד בסים** · לא נבנה
כניסת-לימיט (מחלקת-סיכון חדשה) · לא נגע ב-`RISK_DAILY_LOSS_CAP` · לא נגע במתג.
